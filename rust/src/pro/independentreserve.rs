#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Independentreserve : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "independentreserve",
            "name": "Independent Reserve",
            "countries": [
                "AU",
                "NZ"
            ],
            "enableRateLimit": true,
            "rateLimit": 1000,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": false,
                "swap": false,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "borrowMargin": false,
                "cancelOrder": true,
                "closeAllPositions": false,
                "closePosition": false,
                "createLimitOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createOrder": true,
                "createReduceOnlyOrder": false,
                "createStopLimitOrder": false,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": false,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchIsolatedPositions": false,
                "fetchL2OrderBook": true,
                "fetchLeverage": false,
                "fetchLeverages": false,
                "fetchLeverageTiers": false,
                "fetchLiquidations": false,
                "fetchLongShortRatio": false,
                "fetchLongShortRatioHistory": false,
                "fetchMarginMode": false,
                "fetchMarginModes": false,
                "fetchMarketLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyLiquidations": false,
                "fetchMySettlementHistory": false,
                "fetchMyTrades": true,
                "fetchOpenInterest": false,
                "fetchOpenInterests": false,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchPosition": false,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": false,
                "fetchPositionsForSymbol": false,
                "fetchPositionsForSymbolWs": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchSettlementHistory": false,
                "fetchTicker": true,
                "fetchMarkPrices": false,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchUnderlyingAssets": false,
                "fetchVolatilityHistory": false,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "setLeverage": false,
                "setMargin": false,
                "setMarginMode": false,
                "setPositionMode": false,
                "watchBalance": false,
                "watchMyTrades": false,
                "watchOHLCV": false,
                "watchOrderBook": true,
                "watchOrders": false,
                "watchTicker": false,
                "watchTickers": false,
                "watchTrades": true,
                "watchTradesForSymbols": false,
                "withdraw": true,
                "ws": true,
                "fetchAllGreeks": false,
                "fetchMarkPrice": false,
                "fetchPositionForSymbolWs": false
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/51840849/87182090-1e9e9080-c2ec-11ea-8e49-563db9a38f37.jpg",
                "api": {
                    "public": "https://api.independentreserve.com/Public",
                    "private": "https://api.independentreserve.com/Private",
                    "ws": "wss://websockets.independentreserve.com"
                },
                "www": "https://www.independentreserve.com",
                "doc": "https://www.independentreserve.com/API"
            },
            "api": {
                "public": {
                    "get": [
                        "GetValidPrimaryCurrencyCodes",
                        "GetValidSecondaryCurrencyCodes",
                        "GetValidLimitOrderTypes",
                        "GetValidMarketOrderTypes",
                        "GetValidOrderTypes",
                        "GetValidTransactionTypes",
                        "GetMarketSummary",
                        "GetOrderBook",
                        "GetAllOrders",
                        "GetTradeHistorySummary",
                        "GetRecentTrades",
                        "GetFxRates",
                        "GetOrderMinimumVolumes",
                        "GetCryptoWithdrawalFees",
                        "GetCryptoWithdrawalFees2",
                        "GetNetworks",
                        "GetPrimaryCurrencyConfig2"
                    ]
                },
                "private": {
                    "post": [
                        "GetOpenOrders",
                        "GetClosedOrders",
                        "GetClosedFilledOrders",
                        "GetOrderDetails",
                        "GetAccounts",
                        "GetTransactions",
                        "GetFiatBankAccounts",
                        "GetDigitalCurrencyDepositAddress",
                        "GetDigitalCurrencyDepositAddress2",
                        "GetDigitalCurrencyDepositAddresses",
                        "GetDigitalCurrencyDepositAddresses2",
                        "GetTrades",
                        "GetBrokerageFees",
                        "GetDigitalCurrencyWithdrawal",
                        "PlaceLimitOrder",
                        "PlaceMarketOrder",
                        "CancelOrder",
                        "SynchDigitalCurrencyDepositAddressWithBlockchain",
                        "RequestFiatWithdrawal",
                        "WithdrawFiatCurrency",
                        "WithdrawDigitalCurrency",
                        "WithdrawCrypto"
                    ]
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "fees": {
                "trading": {
                    "tierBased": false,
                    "percentage": true,
                    "taker": 0.005,
                    "maker": 0.005
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {},
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "PLA": "PlayChip"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "features": {
                "spot": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": false,
                            "FOK": false,
                            "PO": false,
                            "GTD": false
                        },
                        "hedged": false,
                        "selfTradePrevention": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false,
                        "iceberg": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 100,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    }
                },
                "swap": {},
                "future": {}
            },
            "options": {
                "defaultNetworks": {
                    "USDT": "Ethereum",
                    "USDC": "Ethereum",
                    "BTC": "Bitcoin",
                    "BCH": "BitcoinCash",
                    "ETH": "Ethereum",
                    "LTC": "Litecoin",
                    "XRP": "XrpLedger",
                    "ZRX": "Ethereum",
                    "EOS": "EosIo",
                    "XLM": "Stellar",
                    "BAT": "Ethereum",
                    "ETC": "EthereumClassic",
                    "LINK": "Ethereum",
                    "MKR": "Ethereum",
                    "DAI": "Ethereum",
                    "COMP": "Ethereum",
                    "SNX": "Ethereum",
                    "YFI": "Ethereum",
                    "AAVE": "Ethereum",
                    "GRT": "Ethereum",
                    "DOT": "Polkadot",
                    "UNI": "Ethereum",
                    "ADA": "Cardano",
                    "MATIC": "Ethereum",
                    "DOGE": "Dogecoin",
                    "SOL": "Solana",
                    "MANA": "Ethereum",
                    "SAND": "Ethereum",
                    "SHIB": "Ethereum",
                    "TRX": "Tron",
                    "RENDER": "Solana",
                    "WIF": "Solana",
                    "RLUSD": "Ethereum",
                    "PEPE": "Ethereum"
                },
                "networks": {
                    "BTC": "Bitcoin",
                    "ETH": "Ethereum",
                    "BCH": "BitcoinCash",
                    "LTC": "Litecoin",
                    "XRP": "XrpLedger",
                    "EOS": "EosIo",
                    "XLM": "Stellar",
                    "ETC": "EthereumClassic",
                    "BSV": "BitcoinSV",
                    "DOGE": "Dogecoin",
                    "DOT": "Polkadot",
                    "ADA": "Cardano",
                    "SOL": "Solana",
                    "TRX": "Tron"
                },
                "watchOrderBook": {
                    "checksum": true
                }
            },
            "streaming": {}
        }"###).unwrap())
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")) + Value::from("?subscribe=ticker-") + market.get(Value::from("base")) + Value::from("-") + market.get(Value::from("quote"));
        let mut message_hash: Value = Value::from("trades:") + symbol.clone();
        let mut trades: Value = self.watch(url.clone(), message_hash.clone(), Value::Undefined, message_hash.clone()).await;
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "Channel": "ticker-btc-usd",
        //        "Nonce": 130,
        //        "Data": {
        //          "TradeGuid": "7a669f2a-d564-472b-8493-6ef982eb1e96",
        //          "Pair": "btc-aud",
        //          "TradeDate": "2023-02-12T10:04:13.0804889+11:00",
        //          "Price": 31640,
        //          "Volume": 0.00079029,
        //          "BidGuid": "ba8a78b5-be69-4d33-92bb-9df0daa6314e",
        //          "OfferGuid": "27d20270-f21f-4c25-9905-152e70b2f6ec",
        //          "Side": "Buy"
        //        },
        //        "Time": 1676156653111,
        //        "Event": "Trade"
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("Data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("Pair"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::from("-"), Value::Undefined);
        let mut message_hash: Value = Value::from("trades:") + symbol.clone();
        let mut stored: Value = self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined);
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            stored = ArrayCache::new(limit);
            self.get("trades".into()).set(symbol.clone(), stored.clone());
        };
        let mut trade: Value = <Self as Independentreserve>::parse_ws_trade(self, data.clone(), Value::Undefined);
        stored.append(trade.clone());
        self.get("trades".into()).set(symbol.clone(), stored.clone());
        client.resolve(self.get("trades".into()).get(symbol.clone()), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        //    {
        //        "TradeGuid": "2f316718-0d0b-4e33-a30c-c2c06f3cfb34",
        //        "Pair": "xbt-aud",
        //        "TradeDate": "2023-02-12T09:22:35.4207494+11:00",
        //        "Price": 31573.8,
        //        "Volume": 0.05,
        //        "BidGuid": "adb63d74-4c02-47f9-9cc3-f287e3b48ab6",
        //        "OfferGuid": "b94d9bc4-addd-4633-a18f-69cf7e1b6f47",
        //        "Side": "Buy"
        //    }
        //
        let mut datetime: Value = self.safe_string(trade.clone(), Value::from("TradeDate"), Value::Undefined);
        let mut market_id: Value = self.safe_string(market.clone(), Value::from("Pair"), Value::Undefined);
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade,
            "id": self.safe_string(trade.clone(), Value::from("TradeGuid"), Value::Undefined),
            "order": self.safe_string(trade.clone(), Value::from("orderNo"), Value::Undefined),
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::from("-"), Value::Undefined),
            "side": self.safe_string_lower(trade.clone(), Value::from("Side"), Value::Undefined),
            "type": Value::Undefined,
            "takerOrMaker": Value::Undefined,
            "price": self.safe_string(trade.clone(), Value::from("Price"), Value::Undefined),
            "amount": self.safe_string(trade.clone(), Value::from("Volume"), Value::Undefined),
            "cost": Value::Undefined,
            "fee": Value::Undefined,
            "timestamp": self.parse8601(datetime.clone()),
            "datetime": datetime
        }))).unwrap()), market.clone());
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        if limit.clone().is_nullish() {
            limit = Value::from(100);
        };
        let mut limit_string: Value = self.number_to_string(limit.clone());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")) + Value::from("/orderbook/") + limit_string.clone() + Value::from("?subscribe=") + market.get(Value::from("base")) + Value::from("-") + market.get(Value::from("quote"));
        let mut message_hash: Value = Value::from("orderbook:") + symbol.clone() + Value::from(":") + limit_string.clone();
        let mut subscription: Value = Value::Json(normalize(&Value::Json(json!({
            "receivedSnapshot": false
        }))).unwrap());
        let mut orderbook: Value = self.watch(url.clone(), message_hash.clone(), Value::Undefined, message_hash.clone(), subscription.clone()).await;
        return orderbook.limit();
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "Channel": "orderbook/1/eth/aud",
        //        "Data": {
        //          "Bids": [
        //            {
        //              "Price": 2198.09,
        //              "Volume": 0.16143952,
        //            },
        //          ],
        //          "Offers": [
        //            {
        //              "Price": 2201.25,
        //              "Volume": 15,
        //            },
        //          ],
        //          "Crc32": 1519697650,
        //        },
        //        "Time": 1676150558254,
        //        "Event": "OrderBookSnapshot",
        //    }
        //
        let mut event: Value = self.safe_string(message.clone(), Value::from("Event"), Value::Undefined);
        let mut channel: Value = self.safe_string(message.clone(), Value::from("Channel"), Value::Undefined);
        let mut parts: Value = channel.split(Value::from("/"));
        let mut depth: Value = self.safe_string(parts.clone(), Value::from(1), Value::Undefined);
        let mut base_id: Value = self.safe_string(parts.clone(), Value::from(2), Value::Undefined);
        let mut quote_id: Value = self.safe_string(parts.clone(), Value::from(3), Value::Undefined);
        let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        let mut symbol: Value = base.clone() + Value::from("/") + quote.clone();
        let mut order_book: Value = self.safe_dict(message.clone(), Value::from("Data"), Value::new_object());
        let mut message_hash: Value = Value::from("orderbook:") + symbol.clone() + Value::from(":") + depth.clone();
        let mut subscription: Value = self.safe_value(client.get(subscriptions.clone()), message_hash.clone(), Value::new_object());
        let mut received_snapshot: Value = self.safe_bool(subscription.clone(), Value::from("receivedSnapshot"), false.into());
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("Time"), Value::Undefined);
        // let orderbook = this.safeValue (this.orderbooks, symbol);
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            self.get("orderbooks".into()).set(symbol.clone(), self.order_book(Value::new_object()));
        };
        let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
        if event.clone() == Value::from("OrderBookSnapshot") {
            let mut snapshot: Value = self.parse_order_book(order_book.clone(), symbol.clone(), timestamp.clone(), Value::from("Bids"), Value::from("Offers"), Value::from("Price"), Value::from("Volume"), Value::Undefined);
            orderbook.reset(snapshot.clone());
            subscription.set("receivedSnapshot".into(), true.into());
        } else {
            let mut asks: Value = self.safe_list(order_book.clone(), Value::from("Offers"), Value::new_array());
            let mut bids: Value = self.safe_list(order_book.clone(), Value::from("Bids"), Value::new_array());
            <Self as Independentreserve>::handle_deltas(self, orderbook.get(Value::from("asks")), asks.clone());
            <Self as Independentreserve>::handle_deltas(self, orderbook.get(Value::from("bids")), bids.clone());
            orderbook.set("timestamp".into(), timestamp.clone());
            orderbook.set("datetime".into(), self.iso8601(timestamp.clone()));
        };
        let mut checksum: Value = self.handle_option(Value::from("watchOrderBook"), Value::from("checksum"), true.into());
        if checksum.is_truthy() && received_snapshot.is_truthy() {
            let mut stored_asks: Value = orderbook.get(Value::from("asks"));
            let mut stored_bids: Value = orderbook.get(Value::from("bids"));
            let mut asks_length: usize = stored_asks.len();
            let mut bids_length: usize = stored_bids.len();
            let mut payload: Value = Value::from("");
            let mut i: usize = 0;
            while i < 10 {
                if i < bids_length.clone().into() {
                    payload = payload.clone() + <Self as Independentreserve>::value_to_checksum(self, stored_bids.get(i.into()).get(Value::from(0))) + <Self as Independentreserve>::value_to_checksum(self, stored_bids.get(i.into()).get(Value::from(1)));
                };
                i += 1;
            };
            let mut i: usize = 0;
            while i < 10 {
                if i < asks_length.clone().into() {
                    payload = payload.clone() + <Self as Independentreserve>::value_to_checksum(self, stored_asks.get(i.into()).get(Value::from(0))) + <Self as Independentreserve>::value_to_checksum(self, stored_asks.get(i.into()).get(Value::from(1)));
                };
                i += 1;
            };
            let mut calculated_checksum: Value = self.crc32(payload.clone(), true.into());
            let mut response_checksum: Value = self.safe_integer(order_book.clone(), Value::from("Crc32"), Value::Undefined);
            if calculated_checksum.clone() != response_checksum.clone() {
                let mut error: Value = ChecksumError::new(self.get("id".into()) + Value::from(" ") + self.orderbook_checksum_message(symbol.clone()));
                client.get(subscriptions.clone()).get(message_hash.clone());
                self.get("orderbooks".into()).get(symbol.clone());
                client.reject(error.clone(), message_hash.clone());
                return Value::Undefined;
            };
        };
        if received_snapshot.is_truthy() {
            client.resolve(orderbook.clone(), message_hash.clone());
        };
        Value::Undefined
    }

    fn value_to_checksum(&mut self, mut value: Value) -> Value {
        let mut result: Value = value.to_fixed(Value::from(8));
        result = result.replace(Value::from("."), Value::from(""));
        // remove leading zeros
        result = self.parse_number(result.clone(), Value::Undefined);
        result = self.number_to_string(result.clone());
        return result.clone();
    }

    fn handle_delta(&mut self, mut bookside: Value, mut delta: Value) -> Value {
        let mut bid_ask: Value = self.parse_bid_ask(delta.clone(), Value::from("Price"), Value::from("Volume"));
        bookside.store_array(bid_ask.clone());
        Value::Undefined
    }

    fn handle_deltas(&mut self, mut bookside: Value, mut deltas: Value) -> Value {
        let mut i: usize = 0;
        while i < deltas.len() {
            <Self as Independentreserve>::handle_delta(self, bookside.clone(), deltas.get(i.into()));
            i += 1;
        };
        Value::Undefined
    }

    fn handle_heartbeat(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "Time": 1676156208182,
        //        "Event": "Heartbeat"
        //    }
        //
        return message.clone();
    }

    fn handle_subscriptions(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "Data": [ "ticker-btc-sgd" ],
        //        "Time": 1676157556223,
        //        "Event": "Subscriptions"
        //    }
        //
        return message.clone();
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        let mut event: Value = self.safe_string(message.clone(), Value::from("Event"), Value::Undefined);
        let mut handlers: Value = Value::Json(normalize(&Value::Json(json!({
            "Subscriptions": self.get("handleSubscriptions".into()),
            "Heartbeat": self.get("handleHeartbeat".into()),
            "Trade": self.get("handleTrades".into()),
            "OrderBookSnapshot": self.get("handleOrderBook".into()),
            "OrderBookChange": self.get("handleOrderBook".into())
        }))).unwrap());
        let mut handler: Value = self.safe_value(handlers.clone(), event.clone(), Value::Undefined);
        if handler.clone().is_nonnullish() {
            handler.call(self, client.clone(), message.clone());
            return Value::Undefined;
        };
        panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" received an unsupported message: ") + self.json(message.clone()))"###);
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetGetvalidprimarycurrencycodes" => self.request("GetValidPrimaryCurrencyCodes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetvalidsecondarycurrencycodes" => self.request("GetValidSecondaryCurrencyCodes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetvalidlimitordertypes" => self.request("GetValidLimitOrderTypes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetvalidmarketordertypes" => self.request("GetValidMarketOrderTypes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetvalidordertypes" => self.request("GetValidOrderTypes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetvalidtransactiontypes" => self.request("GetValidTransactionTypes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetmarketsummary" => self.request("GetMarketSummary".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetorderbook" => self.request("GetOrderBook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetallorders" => self.request("GetAllOrders".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGettradehistorysummary" => self.request("GetTradeHistorySummary".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetrecenttrades" => self.request("GetRecentTrades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetfxrates" => self.request("GetFxRates".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetorderminimumvolumes" => self.request("GetOrderMinimumVolumes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetcryptowithdrawalfees" => self.request("GetCryptoWithdrawalFees".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetcryptowithdrawalfees2" => self.request("GetCryptoWithdrawalFees2".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetnetworks" => self.request("GetNetworks".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetprimarycurrencyconfig2" => self.request("GetPrimaryCurrencyConfig2".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetopenorders" => self.request("GetOpenOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetclosedorders" => self.request("GetClosedOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetclosedfilledorders" => self.request("GetClosedFilledOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetorderdetails" => self.request("GetOrderDetails".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetaccounts" => self.request("GetAccounts".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGettransactions" => self.request("GetTransactions".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetfiatbankaccounts" => self.request("GetFiatBankAccounts".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetdigitalcurrencydepositaddress" => self.request("GetDigitalCurrencyDepositAddress".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetdigitalcurrencydepositaddress2" => self.request("GetDigitalCurrencyDepositAddress2".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetdigitalcurrencydepositaddresses" => self.request("GetDigitalCurrencyDepositAddresses".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetdigitalcurrencydepositaddresses2" => self.request("GetDigitalCurrencyDepositAddresses2".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGettrades" => self.request("GetTrades".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetbrokeragefees" => self.request("GetBrokerageFees".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetdigitalcurrencywithdrawal" => self.request("GetDigitalCurrencyWithdrawal".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostPlacelimitorder" => self.request("PlaceLimitOrder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostPlacemarketorder" => self.request("PlaceMarketOrder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCancelorder" => self.request("CancelOrder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostSynchdigitalcurrencydepositaddresswithblockchain" => self.request("SynchDigitalCurrencyDepositAddressWithBlockchain".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostRequestfiatwithdrawal" => self.request("RequestFiatWithdrawal".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawfiatcurrency" => self.request("WithdrawFiatCurrency".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawdigitalcurrency" => self.request("WithdrawDigitalCurrency".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawcrypto" => self.request("WithdrawCrypto".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct IndependentreserveImpl(Value);
impl Exchange for IndependentreserveImpl {}
impl Independentreserve for IndependentreserveImpl {}
impl ValueTrait for IndependentreserveImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl IndependentreserveImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = IndependentreserveImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Independentreserve::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


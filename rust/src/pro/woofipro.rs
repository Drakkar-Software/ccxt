#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, JSON, Array, Object, Math, parse_int, shift_2, extend_2, normalize};

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Woofipro : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "woofipro",
            "name": "WOOFI PRO",
            "countries": [
                "KY"
            ],
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": true,
            "pro": true,
            "alias": false,
            "dex": true,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "borrowMargin": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "cancelOrders": true,
                "closeAllPositions": false,
                "closePosition": false,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": false,
                "createMarketOrder": false,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrderWithTakeProfitAndStopLoss": true,
                "createReduceOnlyOrder": true,
                "createStopLimitOrder": false,
                "createStopLossOrder": true,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "createTakeProfitOrder": true,
                "createTrailingAmountOrder": false,
                "createTrailingPercentOrder": false,
                "createTriggerOrder": true,
                "editOrder": "emulated",
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchCanceledOrders": false,
                "fetchClosedOrder": false,
                "fetchClosedOrders": true,
                "fetchConvertCurrencies": false,
                "fetchConvertQuote": false,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": false,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": true,
                "fetchFundingHistory": true,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": true,
                "fetchFundingInterval": true,
                "fetchFundingIntervals": false,
                "fetchFundingRates": true,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLeverage": true,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrder": false,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchOrderTrades": true,
                "fetchPosition": true,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": false,
                "fetchTickers": false,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchTransactions": "emulated",
                "fetchTransfers": false,
                "fetchVolatilityHistory": false,
                "fetchWithdrawals": true,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "setLeverage": true,
                "setMargin": false,
                "setPositionMode": false,
                "transfer": false,
                "watchBalance": true,
                "watchMyTrades": true,
                "watchOHLCV": true,
                "watchOrderBook": true,
                "watchBidsAsks": true,
                "watchOrders": true,
                "watchPositions": true,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": false,
                "withdraw": true,
                "ws": true,
                "cancelWithdraw": false,
                "createConvertTrade": false,
                "fetchAllGreeks": false
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/9ba21b8a-a9c7-4770-b7f1-ce3bcbde68c1",
                "api": {
                    "public": "https://api-evm.orderly.org",
                    "private": "https://api-evm.orderly.org",
                    "ws": {
                        "public": "wss://ws-evm.orderly.org/ws/stream",
                        "private": "wss://ws-private-evm.orderly.org/v2/ws/private/stream"
                    }
                },
                "www": "https://dex.woo.org",
                "doc": [
                    "https://orderly.network/docs/build-on-omnichain/building-on-evm"
                ],
                "fees": [
                    "https://dex.woo.org/en/orderly"
                ],
                "test": {
                    "public": "https://testnet-api-evm.orderly.org",
                    "private": "https://testnet-api-evm.orderly.org",
                    "ws": {
                        "public": "wss://testnet-ws-evm.orderly.org/ws/stream",
                        "private": "wss://testnet-ws-private-evm.orderly.org/v2/ws/private/stream"
                    }
                },
                "referral": {
                    "url": "https://dex.woo.org/en/trade?ref=CCXT",
                    "discount": 0.05
                }
            },
            "api": {
                "v1": {
                    "public": {
                        "get": {
                            "public/volume/stats": 1,
                            "public/broker/name": 1,
                            "public/chain_info/{broker_id}": 1,
                            "public/system_info": 1,
                            "public/vault_balance": 1,
                            "public/insurancefund": 1,
                            "public/chain_info": 1,
                            "faucet/usdc": 1,
                            "public/account": 1,
                            "get_account": 1,
                            "registration_nonce": 1,
                            "get_orderly_key": 1,
                            "public/liquidation": 1,
                            "public/liquidated_positions": 1,
                            "public/config": 1,
                            "public/campaign/ranking": 10,
                            "public/campaign/stats": 10,
                            "public/campaign/user": 10,
                            "public/campaign/stats/details": 10,
                            "public/campaigns": 10,
                            "public/points/leaderboard": 1,
                            "client/points": 1,
                            "public/points/epoch": 1,
                            "public/points/epoch_dates": 1,
                            "public/referral/check_ref_code": 1,
                            "public/referral/verify_ref_code": 1,
                            "referral/admin_info": 1,
                            "referral/info": 1,
                            "referral/referee_info": 1,
                            "referral/referee_rebate_summary": 1,
                            "referral/referee_history": 1,
                            "referral/referral_history": 1,
                            "referral/rebate_summary": 1,
                            "client/distribution_history": 1,
                            "tv/config": 1,
                            "tv/history": 1,
                            "tv/symbol_info": 1,
                            "public/funding_rate_history": 1,
                            "public/funding_rate/{symbol}": 0.33,
                            "public/funding_rates": 1,
                            "public/info": 1,
                            "public/info/{symbol}": 1,
                            "public/market_trades": 1,
                            "public/token": 1,
                            "public/futures": 1,
                            "public/futures/{symbol}": 1
                        },
                        "post": {
                            "register_account": 1
                        }
                    },
                    "private": {
                        "get": {
                            "client/key_info": 6,
                            "client/orderly_key_ip_restriction": 6,
                            "order/{oid}": 1,
                            "client/order/{client_order_id}": 1,
                            "algo/order/{oid}": 1,
                            "algo/client/order/{client_order_id}": 1,
                            "orders": 1,
                            "algo/orders": 1,
                            "trade/{tid}": 1,
                            "trades": 1,
                            "order/{oid}/trades": 1,
                            "client/liquidator_liquidations": 1,
                            "liquidations": 1,
                            "asset/history": 60,
                            "client/holding": 1,
                            "withdraw_nonce": 1,
                            "settle_nonce": 1,
                            "pnl_settlement/history": 1,
                            "volume/user/daily": 60,
                            "volume/user/stats": 60,
                            "client/statistics": 60,
                            "client/info": 60,
                            "client/statistics/daily": 60,
                            "positions": 3.33,
                            "position/{symbol}": 3.33,
                            "funding_fee/history": 30,
                            "notification/inbox/notifications": 60,
                            "notification/inbox/unread": 60,
                            "volume/broker/daily": 60,
                            "broker/fee_rate/default": 10,
                            "broker/user_info": 10,
                            "orderbook/{symbol}": 1,
                            "kline": 1
                        },
                        "post": {
                            "orderly_key": 1,
                            "client/set_orderly_key_ip_restriction": 6,
                            "client/reset_orderly_key_ip_restriction": 6,
                            "order": 1,
                            "batch-order": 10,
                            "algo/order": 1,
                            "liquidation": 1,
                            "claim_insurance_fund": 1,
                            "withdraw_request": 1,
                            "settle_pnl": 1,
                            "notification/inbox/mark_read": 60,
                            "notification/inbox/mark_read_all": 60,
                            "client/leverage": 120,
                            "client/maintenance_config": 60,
                            "delegate_signer": 10,
                            "delegate_orderly_key": 10,
                            "delegate_settle_pnl": 10,
                            "delegate_withdraw_request": 10,
                            "broker/fee_rate/set": 10,
                            "broker/fee_rate/set_default": 10,
                            "broker/fee_rate/default": 10,
                            "referral/create": 10,
                            "referral/update": 10,
                            "referral/bind": 10,
                            "referral/edit_split": 10
                        },
                        "put": {
                            "order": 1,
                            "algo/order": 1
                        },
                        "delete": {
                            "order": 1,
                            "algo/order": 1,
                            "client/order": 1,
                            "algo/client/order": 1,
                            "algo/orders": 1,
                            "orders": 1,
                            "batch-order": 1,
                            "client/batch-order": 1
                        }
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": true,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1h",
                "4h": "4h",
                "12h": "12h",
                "1d": "1d",
                "1w": "1w",
                "1M": "1mon",
                "1y": "1y"
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.0005,
                    "maker": 0.0002
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {},
                "ws": {
                    "exact": {}
                }
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "hostname": "dex.woo.org",
            "options": {
                "sandboxMode": false,
                "brokerId": "CCXT",
                "verifyingContractAddress": "0x6F7a338F2aA472838dEFD3283eB360d4Dff5D203",
                "tradesLimit": 1000,
                "ordersLimit": 1000,
                "requestId": {},
                "watchPositions": {
                    "fetchPositionsSnapshot": true,
                    "awaitPositionsSnapshot": true
                }
            },
            "features": {
                "default": {
                    "sandbox": true,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": true,
                        "leverage": true,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": false,
                        "iceberg": true
                    },
                    "createOrders": {
                        "max": 10
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 500,
                        "untilDays": 100000,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "untilDays": 100000,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "spot": {
                    "extends": "default"
                },
                "forDerivatives": {
                    "extends": "default",
                    "createOrder": {
                        "attachedStopLossTakeProfit": {
                            "price": false
                        }
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forDerivatives"
                    }
                },
                "future": {}
            },
            "streaming": {
                "keepAlive": 10000
            }
        }"###).unwrap())
    }

    fn request_id(&mut self, mut url: Value) -> Value {
        let mut options: Value = self.safe_dict(self.get("options".into()), Value::from("requestId"), Value::new_object());
        let mut previous_value: Value = self.safe_integer(options.clone(), url.clone(), Value::from(0));
        let mut new_value: Value = self.sum(previous_value.clone(), Value::from(1));
        self.get("options".into()).get(Value::from("requestId")).set(url.clone(), new_value.clone());
        return new_value.clone();
    }

    async fn watch_public(&mut self, mut message_hash: Value, mut message: Value) -> Value {
        // the default id
        let mut id: Value = Value::from("OqdphuyCtYWxwzhxyLLjOWNdFP7sQt8RPWzmb5xY");
        if self.get("accountId".into()).is_nonnullish() && self.get("accountId".into()) != Value::from("") {
            id = self.get("accountId".into());
        };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public")) + Value::from("/") + id.clone();
        let mut request_id: Value = <Self as Woofipro>::request_id(self, url.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id
        }))).unwrap());
        let mut request: Value = extend_2(subscribe.clone(), message.clone());
        return self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), subscribe.clone()).await;
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("orderbook");
        let mut market: Value = self.market(symbol.clone());
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@") + name.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut orderbook: Value = <Self as Woofipro>::watch_public(self, topic.clone(), message.clone()).await;
        return orderbook.limit();
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "PERP_BTC_USDC@orderbook",
        //         "ts": 1650121915308,
        //         "data": {
        //             "symbol": "PERP_BTC_USDC",
        //             "bids": [
        //                 [
        //                     0.30891,
        //                     2469.98
        //                 ]
        //             ],
        //             "asks": [
        //                 [
        //                     0.31075,
        //                     2379.63
        //                 ]
        //             ]
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"));
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            self.get("orderbooks".into()).set(symbol.clone(), self.order_book(Value::Undefined, Value::Undefined));
        };
        let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"));
        let mut snapshot: Value = self.parse_order_book(data.clone(), symbol.clone(), timestamp.clone(), Value::from("bids"), Value::from("asks"), Value::Undefined, Value::Undefined, Value::Undefined);
        orderbook.reset(snapshot.clone());
        client.resolve(orderbook.clone(), topic.clone());
        Value::Undefined
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("ticker");
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@") + name.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return <Self as Woofipro>::watch_public(self, topic.clone(), message.clone()).await;
    }

    fn parse_ws_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     {
        //         "symbol": "PERP_BTC_USDC",
        //         "open": 19441.5,
        //         "close": 20147.07,
        //         "high": 20761.87,
        //         "low": 19320.54,
        //         "volume": 2481.103,
        //         "amount": 50037935.0286,
        //         "count": 3689
        //     }
        //
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": self.safe_symbol(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "high": self.safe_string(ticker.clone(), Value::from("high")),
            "low": self.safe_string(ticker.clone(), Value::from("low")),
            "bid": Value::Undefined,
            "bidVolume": Value::Undefined,
            "ask": Value::Undefined,
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": self.safe_string(ticker.clone(), Value::from("open")),
            "close": self.safe_string(ticker.clone(), Value::from("close")),
            "last": Value::Undefined,
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": self.safe_string(ticker.clone(), Value::from("volume")),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("amount")),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "PERP_BTC_USDC@ticker",
        //         "ts": 1657120017000,
        //         "data": {
        //             "symbol": "PERP_BTC_USDC",
        //             "open": 19441.5,
        //             "close": 20147.07,
        //             "high": 20761.87,
        //             "low": 19320.54,
        //             "volume": 2481.103,
        //             "amount": 50037935.0286,
        //             "count": 3689
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"));
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"));
        data.set("date".into(), timestamp.clone());
        let mut ticker: Value = <Self as Woofipro>::parse_ws_ticker(self, data.clone(), market.clone());
        ticker.set("symbol".into(), market.get(Value::from("symbol")));
        self.get("tickers".into()).set(market.get(Value::from("symbol")), ticker.clone());
        client.resolve(ticker.clone(), topic.clone());
        return message.clone();
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut name: Value = Value::from("tickers");
        let mut topic: Value = name.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut tickers: Value = <Self as Woofipro>::watch_public(self, topic.clone(), message.clone()).await;
        return self.filter_by_array(tickers.clone(), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    fn handle_tickers(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic":"tickers",
        //         "ts":1618820615000,
        //         "data":[
        //             {
        //                 "symbol":"PERP_NEAR_USDC",
        //                 "open":16.297,
        //                 "close":17.183,
        //                 "high":24.707,
        //                 "low":11.997,
        //                 "volume":0,
        //                 "amount":0,
        //                 "count":0
        //             },
        //         ...
        //         ]
        //     }
        //
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"));
        let mut result: Value = Value::new_array();
        let mut i: usize = 0;
        while i < data.len() {
            let mut market_id: Value = self.safe_string(data.get(i.into()), Value::from("symbol"));
            let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            let mut ticker: Value = <Self as Woofipro>::parse_ws_ticker(self, extend_2(data.get(i.into()), Value::Json(normalize(&Value::Json(json!({
                "date": timestamp
            }))).unwrap())), market.clone());
            self.get("tickers".into()).set(market.get(Value::from("symbol")), ticker.clone());
            result.push(ticker.clone());
            i += 1;
        };
        client.resolve(result.clone(), topic.clone());
        Value::Undefined
    }

    async fn watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut name: Value = Value::from("bbos");
        let mut topic: Value = name.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut tickers: Value = <Self as Woofipro>::watch_public(self, topic.clone(), message.clone()).await;
        return self.filter_by_array(tickers.clone(), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    fn handle_bid_ask(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //       "topic": "bbos",
        //       "ts": 1726212495000,
        //       "data": [
        //         {
        //           "symbol": "PERP_WOO_USDC",
        //           "ask": 0.16570,
        //           "askSize": 4224,
        //           "bid": 0.16553,
        //           "bidSize": 6645
        //         }
        //       ]
        //     }
        //
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"));
        let mut result: Value = Value::new_array();
        let mut i: usize = 0;
        while i < data.len() {
            let mut ticker: Value = <Self as Woofipro>::parse_ws_bid_ask(self, extend_2(data.get(i.into()), Value::Json(normalize(&Value::Json(json!({
                "ts": timestamp
            }))).unwrap())), Value::Undefined);
            self.get("tickers".into()).set(ticker.get(Value::from("symbol")), ticker.clone());
            result.push(ticker.clone());
            i += 1;
        };
        client.resolve(result.clone(), topic.clone());
        Value::Undefined
    }

    fn parse_ws_bid_ask(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("symbol"));
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_string(market.clone(), Value::from("symbol"));
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("ts"));
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "ask": self.safe_string(ticker.clone(), Value::from("ask")),
            "askVolume": self.safe_string(ticker.clone(), Value::from("askSize")),
            "bid": self.safe_string(ticker.clone(), Value::from("bid")),
            "bidVolume": self.safe_string(ticker.clone(), Value::from("bidSize")),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        if timeframe.clone() != Value::from("1m") && timeframe.clone() != Value::from("5m") && timeframe.clone() != Value::from("15m") && timeframe.clone() != Value::from("30m") && timeframe.clone() != Value::from("1h") && timeframe.clone() != Value::from("1d") && timeframe.clone() != Value::from("1w") && timeframe.clone() != Value::from("1M") {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" watchOHLCV timeframe argument must be 1m, 5m, 15m, 30m, 1h, 1d, 1w, 1M"))"###);
        };
        let mut market: Value = self.market(symbol.clone());
        let mut interval: Value = self.safe_string(self.get("timeframes".into()), timeframe.clone(), timeframe.clone());
        let mut name: Value = Value::from("kline");
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@") + name.clone() + Value::from("_") + interval.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut ohlcv: Value = <Self as Woofipro>::watch_public(self, topic.clone(), message.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = ohlcv.get_limit(market.get(Value::from("symbol")), limit.clone());
        };
        return self.filter_by_since_limit(ohlcv.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic":"PERP_BTC_USDC@kline_1m",
        //         "ts":1618822432146,
        //         "data":{
        //             "symbol":"PERP_BTC_USDC",
        //             "type":"1m",
        //             "open":56948.97,
        //             "close":56891.76,
        //             "high":56948.97,
        //             "low":56889.06,
        //             "volume":44.00947568,
        //             "amount":2504584.9,
        //             "startTime":1618822380000,
        //             "endTime":1618822440000
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"));
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut interval: Value = self.safe_string(data.clone(), Value::from("type"));
        let mut timeframe: Value = self.find_timeframe(interval.clone(), Value::Undefined);
        let mut parsed: Value = Value::Json(serde_json::Value::Array(vec![self.safe_integer(data.clone(), Value::from("startTime")).into(), self.safe_number(data.clone(), Value::from("open"), Value::Undefined).into(), self.safe_number(data.clone(), Value::from("high"), Value::Undefined).into(), self.safe_number(data.clone(), Value::from("low"), Value::Undefined).into(), self.safe_number(data.clone(), Value::from("close"), Value::Undefined).into(), self.safe_number(data.clone(), Value::from("volume"), Value::Undefined).into()]));
        self.get("ohlcvs".into()).set(symbol.clone(), self.safe_value(self.get("ohlcvs".into()), symbol.clone(), Value::new_object()));
        let mut stored: Value = self.safe_value(self.get("ohlcvs".into()).get(symbol.clone()), timeframe.clone());
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            stored = ArrayCacheByTimestamp::new(limit);
            self.get("ohlcvs".into()).get(symbol.clone()).set(timeframe.clone(), stored.clone());
        };
        let mut ohlcv_cache: Value = self.get("ohlcvs".into()).get(symbol.clone()).get(timeframe.clone());
        ohlcv_cache.append(parsed.clone());
        client.resolve(ohlcv_cache.clone(), topic.clone());
        Value::Undefined
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@trade");
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut trades: Value = <Self as Woofipro>::watch_public(self, topic.clone(), message.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(market.get(Value::from("symbol")), limit.clone());
        };
        return self.filter_by_symbol_since_limit(trades.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_trade(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "topic":"PERP_ADA_USDC@trade",
        //     "ts":1618820361552,
        //     "data":{
        //         "symbol":"PERP_ADA_USDC",
        //         "price":1.27988,
        //         "size":300,
        //         "side":"BUY",
        //     }
        // }
        //
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"));
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"));
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut trade: Value = <Self as Woofipro>::parse_ws_trade(self, extend_2(data.clone(), Value::Json(normalize(&Value::Json(json!({
            "timestamp": timestamp
        }))).unwrap())), market.clone());
        if !self.get("trades".into()).contains_key(symbol.clone()) {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            let mut stored: Value = ArrayCache::new(limit);
            self.get("trades".into()).set(symbol.clone(), stored.clone());
        };
        let mut trades: Value = self.get("trades".into()).get(symbol.clone());
        trades.append(trade.clone());
        self.get("trades".into()).set(symbol.clone(), trades.clone());
        client.resolve(trades.clone(), topic.clone());
        Value::Undefined
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        //     {
        //         "symbol":"PERP_ADA_USDC",
        //         "timestamp":1618820361552,
        //         "price":1.27988,
        //         "size":300,
        //         "side":"BUY",
        //     }
        // private stream
        //     {
        //         symbol: 'PERP_XRP_USDC',
        //         clientOrderId: '',
        //         orderId: 1167632251,
        //         type: 'MARKET',
        //         side: 'BUY',
        //         quantity: 20,
        //         price: 0,
        //         tradeId: '1715179456664012',
        //         executedPrice: 0.5276,
        //         executedQuantity: 20,
        //         fee: 0.006332,
        //         feeAsset: 'USDC',
        //         totalExecutedQuantity: 20,
        //         avgPrice: 0.5276,
        //         averageExecutedPrice: 0.5276,
        //         status: 'FILLED',
        //         reason: '',
        //         totalFee: 0.006332,
        //         visible: 0,
        //         visibleQuantity: 0,
        //         timestamp: 1715179456660,
        //         orderTag: 'CCXT',
        //         createdTime: 1715179456656,
        //         maker: false
        //     }
        //
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("symbol"));
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut price: Value = self.safe_string_2(trade.clone(), Value::from("executedPrice"), Value::from("price"));
        let mut amount: Value = self.safe_string_2(trade.clone(), Value::from("executedQuantity"), Value::from("size"));
        let mut cost: Value = Precise::string_mul(price.clone(), amount.clone());
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::from("side"));
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("timestamp"));
        let mut taker_or_maker: Value = Value::Undefined;
        let mut maker: Value = self.safe_bool(trade.clone(), Value::from("maker"), Value::Undefined);
        if maker.clone().is_nonnullish() {
            taker_or_maker = if maker.is_truthy() { Value::from("maker") } else { Value::from("taker") };
        };
        let mut fee: Value = Value::Undefined;
        let mut fee_value: Value = self.safe_string(trade.clone(), Value::from("fee"));
        if fee_value.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_value,
                "currency": self.safe_currency_code(self.safe_string(trade.clone(), Value::from("feeAsset")), Value::Undefined)
            }))).unwrap());
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(trade.clone(), Value::from("tradeId")),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol,
            "side": side,
            "price": price,
            "amount": amount,
            "cost": cost,
            "order": self.safe_string(trade.clone(), Value::from("orderId")),
            "takerOrMaker": taker_or_maker,
            "type": self.safe_string_lower(trade.clone(), Value::from("type")),
            "fee": fee,
            "info": trade
        }))).unwrap()), market.clone());
    }

    fn handle_auth(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "event": "auth",
        //         "success": true,
        //         "ts": 1657463158812
        //     }
        //
        let mut message_hash: Value = Value::from("authenticated");
        let mut success: Value = self.safe_value(message.clone(), Value::from("success"));
        if success.is_truthy() {
            // client.resolve (message, messageHash);
            let mut future: Value = self.safe_value(client.get(futures.clone()), Value::from("authenticated"));
            future.resolve(true.into());
        } else {
            let mut error: Value = AuthenticationError::new(self.json(message.clone()));
            client.reject(error.clone(), message_hash.clone());
            // allows further authentication attempts
            if client.get(subscriptions.clone()).contains_key(message_hash.clone()) {
                client.get(subscriptions.clone()).get(Value::from("authenticated"));
            };
        };
        Value::Undefined
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.check_required_credentials(Value::Undefined);
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("accountId".into());
        let mut client: Value = self.client(url.clone());
        let mut message_hash: Value = Value::from("authenticated");
        let mut event: Value = Value::from("auth");
        let mut future: Value = client.reusable_future(message_hash.clone());
        let mut authenticated: Value = self.safe_value(client.get(subscriptions.clone()), message_hash.clone());
        if authenticated.clone().is_nullish() {
            let mut ts: Value = self.nonce().to_string();
            let mut auth: Value = ts.clone();
            let mut secret: Value = self.get("secret".into());
            if secret.index_of(Value::from("ed25519:")) >= Value::from(0) {
                let mut parts: Value = secret.split(Value::from("ed25519:"));
                secret = parts.get(Value::from(1));
            };
            let mut signature: Value = eddsa(self.encode(auth.clone()), self.base58_to_binary(secret.clone()), ed25519.clone());
            let mut request: Value = Value::Json(normalize(&Value::Json(json!({
                "event": event,
                "params": Value::Json(normalize(&Value::Json(json!({
                    "orderly_key": self.get("apiKey".into()),
                    "sign": signature,
                    "timestamp": ts
                }))).unwrap())
            }))).unwrap());
            let mut message: Value = extend_2(request.clone(), params.clone());
            self.watch(url.clone(), message_hash.clone(), message.clone(), message_hash.clone(), Value::Undefined);
        };
        return future.clone();
    }

    async fn watch_private(&mut self, mut message_hash: Value, mut message: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Woofipro>::authenticate(self, params.clone()).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("accountId".into());
        let mut request_id: Value = <Self as Woofipro>::request_id(self, url.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id
        }))).unwrap());
        let mut request: Value = extend_2(subscribe.clone(), message.clone());
        return self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), subscribe.clone()).await;
    }

    async fn watch_private_multiple(&mut self, mut message_hashes: Value, mut message: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Woofipro>::authenticate(self, params.clone()).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("accountId".into());
        let mut request_id: Value = <Self as Woofipro>::request_id(self, url.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id
        }))).unwrap());
        let mut request: Value = extend_2(subscribe.clone(), message.clone());
        return self.watch_multiple(url.clone(), message_hashes.clone(), request.clone(), message_hashes.clone(), subscribe.clone()).await;
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut trigger: Value = self.safe_bool_2(params.clone(), Value::from("stop"), Value::from("trigger"), false.into());
        let mut topic: Value = if trigger.is_truthy() { Value::from("algoexecutionreport") } else { Value::from("executionreport") };
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stop").into(), Value::from("trigger").into()])));
        let mut message_hash: Value = topic.clone();
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
            message_hash = message_hash +  Value::from(":") + symbol.clone();
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut orders: Value = <Self as Woofipro>::watch_private(self, message_hash.clone(), message.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    async fn watch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut trigger: Value = self.safe_bool_2(params.clone(), Value::from("stop"), Value::from("trigger"), false.into());
        let mut topic: Value = if trigger.is_truthy() { Value::from("algoexecutionreport") } else { Value::from("executionreport") };
        params = self.omit(params.clone(), Value::from("stop"));
        let mut message_hash: Value = Value::from("myTrades");
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
            message_hash = message_hash +  Value::from(":") + symbol.clone();
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut orders: Value = <Self as Woofipro>::watch_private(self, message_hash.clone(), message.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn parse_ws_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        //     {
        //         "symbol": "PERP_BTC_USDT",
        //         "clientOrderId": 0,
        //         "orderId": 52952826,
        //         "type": "LIMIT",
        //         "side": "SELL",
        //         "quantity": 0.01,
        //         "price": 22000,
        //         "tradeId": 0,
        //         "executedPrice": 0,
        //         "executedQuantity": 0,
        //         "fee": 0,
        //         "feeAsset": "USDT",
        //         "totalExecutedQuantity": 0,
        //         "status": "NEW",
        //         "reason": '',
        //         "orderTag": "default",
        //         "totalFee": 0,
        //         "visible": 0.01,
        //         "timestamp": 1657515556799,
        //         "reduceOnly": false,
        //         "maker": false
        //     }
        // algo order
        //     {
        //         "symbol":"PERP_MATIC_USDC",
        //         "rootAlgoOrderId":123,
        //         "parentAlgoOrderId":123,
        //         "algoOrderId":123,
        //         "orderTag":"some tags",
        //         "algoType": "STOP",
        //         "clientOrderId":"client_id",
        //         "type":"LIMIT",
        //         "side":"BUY",
        //         "quantity":7029.0,
        //         "price":0.7699,
        //         "tradeId":0,
        //         "triggerTradePrice":0,
        //         "triggerTime":1234567,
        //         "triggered": false,
        //         "activated": false,
        //         "executedPrice":0.0,
        //         "executedQuantity":0.0,
        //         "fee":0.0,
        //         "feeAsset":"USDC",
        //         "totalExecutedQuantity":0.0,
        //         "averageExecutedQuantity":0.0,
        //         "avgPrice":0,
        //         "triggerPrice":0.0,
        //         "triggerPriceType":"STOP",
        //         "isActivated": false,
        //         "status":"NEW",
        //         "rootAlgoStatus": "FILLED",
        //         "algoStatus": "FILLED",
        //         "reason":"",
        //         "totalFee":0.0,
        //         "visible": 7029.0,
        //         "visibleQuantity":7029.0,
        //         "timestamp":1704679472448,
        //         "maker":false,
        //         "isMaker":false,
        //         "createdTime":1704679472448
        //     }
        //
        let mut order_id: Value = self.safe_string(order.clone(), Value::from("orderId"));
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"));
        market = self.market(market_id.clone());
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut timestamp: Value = self.safe_integer(order.clone(), Value::from("timestamp"));
        let mut fee: Value = Value::Json(normalize(&Value::Json(json!({
            "cost": self.safe_string(order.clone(), Value::from("totalFee")),
            "currency": self.safe_string(order.clone(), Value::from("feeAsset"))
        }))).unwrap());
        let mut price_string: Value = self.safe_string(order.clone(), Value::from("price"));
        let mut price: Value = self.safe_number(order.clone(), Value::from("price"), Value::Undefined);
        let mut avg_price: Value = self.safe_number(order.clone(), Value::from("avgPrice"), Value::Undefined);
        if Precise::string_eq(price_string.clone(), Value::from("0")) && avg_price.clone().is_nonnullish() {
            price = avg_price.clone();
        };
        let mut amount: Value = self.safe_string(order.clone(), Value::from("quantity"));
        let mut side: Value = self.safe_string_lower(order.clone(), Value::from("side"));
        let mut r#type: Value = self.safe_string_lower(order.clone(), Value::from("type"));
        let mut filled: Value = self.safe_number(order.clone(), Value::from("totalExecutedQuantity"), Value::Undefined);
        let mut total_exec_quantity: Value = self.safe_string(order.clone(), Value::from("totalExecutedQuantity"));
        let mut remaining: Value = amount.clone();
        if Precise::string_ge(amount.clone(), total_exec_quantity.clone()) {
            remaining = Precise::string_sub(remaining.clone(), total_exec_quantity.clone());
        };
        let mut raw_status: Value = self.safe_string(order.clone(), Value::from("status"));
        let mut status: Value = self.parse_order_status(raw_status.clone());
        let mut trades: Value = Value::Undefined;
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("clientOrderId"));
        let mut trigger_price: Value = self.safe_number(order.clone(), Value::from("triggerPrice"), Value::Undefined);
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "symbol": symbol,
            "id": order_id,
            "clientOrderId": client_order_id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": timestamp,
            "type": r#type,
            "timeInForce": Value::Undefined,
            "postOnly": Value::Undefined,
            "side": side,
            "price": price,
            "stopPrice": trigger_price,
            "triggerPrice": trigger_price,
            "amount": amount,
            "cost": Value::Undefined,
            "average": Value::Undefined,
            "filled": filled,
            "remaining": remaining,
            "status": status,
            "fee": fee,
            "trades": trades
        }))).unwrap()), Value::Undefined);
    }

    fn handle_order_update(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "executionreport",
        //         "ts": 1657515556799,
        //         "data": {
        //             "symbol": "PERP_BTC_USDT",
        //             "clientOrderId": 0,
        //             "orderId": 52952826,
        //             "type": "LIMIT",
        //             "side": "SELL",
        //             "quantity": 0.01,
        //             "price": 22000,
        //             "tradeId": 0,
        //             "executedPrice": 0,
        //             "executedQuantity": 0,
        //             "fee": 0,
        //             "feeAsset": "USDT",
        //             "totalExecutedQuantity": 0,
        //             "status": "NEW",
        //             "reason": '',
        //             "orderTag": "default",
        //             "totalFee": 0,
        //             "visible": 0.01,
        //             "timestamp": 1657515556799,
        //             "maker": false
        //         }
        //     }
        //
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"));
        if Array::is_array(data.clone()).is_truthy() {
            // algoexecutionreport
            let mut i: usize = 0;
            while i < data.len() {
                let mut order: Value = data.get(i.into());
                let mut trade_id: Value = self.omit_zero(self.safe_string(data.clone(), Value::from("tradeId")));
                if trade_id.clone().is_nonnullish() {
                    <Self as Woofipro>::handle_my_trade(self, client.clone(), order.clone());
                };
                <Self as Woofipro>::handle_order(self, client.clone(), order.clone(), topic.clone());
                i += 1;
            };
        } else {
            // executionreport
            let mut trade_id: Value = self.omit_zero(self.safe_string(data.clone(), Value::from("tradeId")));
            if trade_id.clone().is_nonnullish() {
                <Self as Woofipro>::handle_my_trade(self, client.clone(), data.clone());
            };
            <Self as Woofipro>::handle_order(self, client.clone(), data.clone(), topic.clone());
        };
        Value::Undefined
    }

    fn handle_order(&mut self, mut client: Value, mut message: Value, mut topic: Value) -> Value {
        let mut parsed: Value = <Self as Woofipro>::parse_ws_order(self, message.clone(), Value::Undefined);
        let mut symbol: Value = self.safe_string(parsed.clone(), Value::from("symbol"));
        let mut order_id: Value = self.safe_string(parsed.clone(), Value::from("id"));
        if symbol.clone().is_nonnullish() {
            if self.get("orders".into()).is_nullish() {
                let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
                self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
            };
            let mut cached_orders: Value = self.get("orders".into());
            let mut orders: Value = self.safe_dict(cached_orders.get(hashmap.clone()), symbol.clone(), Value::new_object());
            let mut order: Value = self.safe_dict(orders.clone(), order_id.clone(), Value::Undefined);
            if order.clone().is_nonnullish() {
                let mut fee: Value = self.safe_value(order.clone(), Value::from("fee"));
                if fee.clone().is_nonnullish() {
                    parsed.set("fee".into(), fee.clone());
                };
                let mut fees: Value = self.safe_list(order.clone(), Value::from("fees"), Value::Undefined);
                if fees.clone().is_nonnullish() {
                    parsed.set("fees".into(), fees.clone());
                };
                parsed.set("trades".into(), self.safe_list(order.clone(), Value::from("trades"), Value::Undefined));
                parsed.set("timestamp".into(), self.safe_integer(order.clone(), Value::from("timestamp")));
                parsed.set("datetime".into(), self.safe_string(order.clone(), Value::from("datetime")));
            };
            cached_orders.append(parsed.clone());
            client.resolve(self.get("orders".into()), topic.clone());
            let mut message_hash_symbol: Value = topic.clone() + Value::from(":") + symbol.clone();
            client.resolve(self.get("orders".into()), message_hash_symbol.clone());
        };
        Value::Undefined
    }

    fn handle_my_trade(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     symbol: 'PERP_XRP_USDC',
        //     clientOrderId: '',
        //     orderId: 1167632251,
        //     type: 'MARKET',
        //     side: 'BUY',
        //     quantity: 20,
        //     price: 0,
        //     tradeId: '1715179456664012',
        //     executedPrice: 0.5276,
        //     executedQuantity: 20,
        //     fee: 0.006332,
        //     feeAsset: 'USDC',
        //     totalExecutedQuantity: 20,
        //     avgPrice: 0.5276,
        //     averageExecutedPrice: 0.5276,
        //     status: 'FILLED',
        //     reason: '',
        //     totalFee: 0.006332,
        //     visible: 0,
        //     visibleQuantity: 0,
        //     timestamp: 1715179456660,
        //     orderTag: 'CCXT',
        //     createdTime: 1715179456656,
        //     maker: false
        // }
        //
        let mut message_hash: Value = Value::from("myTrades");
        let mut market_id: Value = self.safe_string(message.clone(), Value::from("symbol"));
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut trade: Value = <Self as Woofipro>::parse_ws_trade(self, message.clone(), market.clone());
        let mut trades: Value = self.get("myTrades".into());
        if trades.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            trades = ArrayCacheBySymbolById::new(limit);
            self.set("my_trades".into(), trades.clone());
        };
        trades.append(trade.clone());
        client.resolve(trades.clone(), message_hash.clone());
        let mut symbol_specific_message_hash: Value = message_hash.clone() + Value::from(":") + symbol.clone();
        client.resolve(trades.clone(), symbol_specific_message_hash.clone());
        Value::Undefined
    }

    async fn watch_positions(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut message_hashes: Value = Value::new_array();
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        if !self.is_empty(symbols.clone()).is_truthy() {
            let mut i: usize = 0;
            while i < symbols.len() {
                let mut symbol: Value = symbols.get(i.into());
                message_hashes.push(Value::from("positions::") + symbol.clone());
                i += 1;
            };
        } else {
            message_hashes.push(Value::from("positions"));
        };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("accountId".into());
        let mut client: Value = self.client(url.clone());
        <Self as Woofipro>::set_positions_cache(self, client.clone(), symbols.clone(), Value::Undefined);
        let mut fetch_positions_snapshot: Value = self.handle_option(Value::from("watchPositions"), Value::from("fetchPositionsSnapshot"), true.into());
        let mut await_positions_snapshot: Value = self.handle_option(Value::from("watchPositions"), Value::from("awaitPositionsSnapshot"), true.into());
        if fetch_positions_snapshot.is_truthy() && await_positions_snapshot.is_truthy() && self.get("positions".into()).is_nullish() {
            let mut snapshot: Value = client.future(Value::from("fetchPositionsSnapshot")).await;
            return self.filter_by_symbols_since_limit(snapshot.clone(), symbols.clone(), since.clone(), limit.clone(), true.into());
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": "position"
        }))).unwrap());
        let mut new_positions: Value = <Self as Woofipro>::watch_private_multiple(self, message_hashes.clone(), request.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            return new_positions.clone();
        };
        return self.filter_by_symbols_since_limit(self.get("positions".into()), symbols.clone(), since.clone(), limit.clone(), true.into());
    }

    fn set_positions_cache(&mut self, mut client: Value, mut r#type: Value, mut symbols: Value) -> Value {
        let mut fetch_positions_snapshot: Value = self.handle_option(Value::from("watchPositions"), Value::from("fetchPositionsSnapshot"), false.into());
        if fetch_positions_snapshot.is_truthy() {
            let mut message_hash: Value = Value::from("fetchPositionsSnapshot");
            if !client.get(futures.clone()).contains_key(message_hash.clone()) {
                client.future(message_hash.clone());
                self.spawn(self.get("loadPositionsSnapshot".into()), client.clone(), message_hash.clone());
            };
        } else {
            self.set("positions".into(), ArrayCacheBySymbolBySide::new());
        };
        Value::Undefined
    }

    async fn load_positions_snapshot(&mut self, mut client: Value, mut message_hash: Value) -> Value {
        let mut positions: Value = self.fetch_positions(Value::Undefined, Value::Undefined).await;
        self.set("positions".into(), ArrayCacheBySymbolBySide::new());
        let mut cache: Value = self.get("positions".into());
        let mut i: usize = 0;
        while i < positions.len() {
            let mut position: Value = positions.get(i.into());
            let mut contracts: Value = self.safe_string(position.clone(), Value::from("contracts"), Value::from("0"));
            if Precise::string_gt(contracts.clone(), Value::from("0")) {
                cache.append(position.clone());
            };
            i += 1;
        };
        // don't remove the future from the .futures cache
        if client.get(futures.clone()).contains_key(message_hash.clone()) {
            let mut future: Value = client.get(futures.clone()).get(message_hash.clone());
            future.resolve(cache.clone());
            client.resolve(cache.clone(), Value::from("positions"));
        };
        Value::Undefined
    }

    fn handle_positions(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "topic":"position",
        //        "ts":1705292345255,
        //        "data":{
        //           "positions":[
        //              {
        //                     "symbol":"PERP_ETH_USDC",
        //                     "positionQty":3.1408,
        //                     "costPosition":5706.51952,
        //                     "lastSumUnitaryFunding":0.804,
        //                     "sumUnitaryFundingVersion":0,
        //                     "pendingLongQty":0.0,
        //                     "pendingShortQty":-1.0,
        //                     "settlePrice":1816.9,
        //                     "averageOpenPrice":1804.51490427,
        //                     "unsettledPnl":-2.79856,
        //                     "pnl24H":-338.90179488,
        //                     "fee24H":4.242423,
        //                     "markPrice":1816.2,
        //                     "estLiqPrice":0.0,
        //                     "version":179967,
        //                     "imrwithOrders":0.1,
        //                     "mmrwithOrders":0.05,
        //                     "mmr":0.05,
        //                     "imr":0.1,
        //                     "timestamp":1685154032762
        //              }
        //           ]
        //        }
        //    }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut raw_positions: Value = self.safe_list(data.clone(), Value::from("positions"), Value::new_array());
        if self.get("positions".into()).is_nullish() {
            self.set("positions".into(), ArrayCacheBySymbolBySide::new());
        };
        let mut cache: Value = self.get("positions".into());
        let mut new_positions: Value = Value::new_array();
        let mut i: usize = 0;
        while i < raw_positions.len() {
            let mut raw_position: Value = raw_positions.get(i.into());
            let mut market_id: Value = self.safe_string(raw_position.clone(), Value::from("symbol"));
            let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            let mut position: Value = <Self as Woofipro>::parse_ws_position(self, raw_position.clone(), market.clone());
            new_positions.push(position.clone());
            cache.append(position.clone());
            let mut message_hash: Value = Value::from("positions::") + market.get(Value::from("symbol"));
            client.resolve(position.clone(), message_hash.clone());
            i += 1;
        };
        client.resolve(new_positions.clone(), Value::from("positions"));
        Value::Undefined
    }

    fn parse_ws_position(&self, mut position: Value, mut market: Value) -> Value {
        //
        //     {
        //         "symbol":"PERP_ETH_USDC",
        //         "positionQty":3.1408,
        //         "costPosition":5706.51952,
        //         "lastSumUnitaryFunding":0.804,
        //         "sumUnitaryFundingVersion":0,
        //         "pendingLongQty":0.0,
        //         "pendingShortQty":-1.0,
        //         "settlePrice":1816.9,
        //         "averageOpenPrice":1804.51490427,
        //         "unsettledPnl":-2.79856,
        //         "pnl24H":-338.90179488,
        //         "fee24H":4.242423,
        //         "markPrice":1816.2,
        //         "estLiqPrice":0.0,
        //         "version":179967,
        //         "imrwithOrders":0.1,
        //         "mmrwithOrders":0.05,
        //         "mmr":0.05,
        //         "imr":0.1,
        //         "timestamp":1685154032762
        //     }
        //
        let mut contract: Value = self.safe_string(position.clone(), Value::from("symbol"));
        market = self.safe_market(contract.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut size: Value = self.safe_string(position.clone(), Value::from("positionQty"));
        let mut side: Value = Value::Undefined;
        if Precise::string_gt(size.clone(), Value::from("0")) {
            side = Value::from("long");
        } else {
            side = Value::from("short");
        };
        let mut contract_size: Value = self.safe_string(market.clone(), Value::from("contractSize"));
        let mut mark_price: Value = self.safe_string(position.clone(), Value::from("markPrice"));
        let mut timestamp: Value = self.safe_integer(position.clone(), Value::from("timestamp"));
        let mut entry_price: Value = self.safe_string(position.clone(), Value::from("averageOpenPrice"));
        let mut unrealised_pnl: Value = self.safe_string(position.clone(), Value::from("unsettledPnl"));
        size = Precise::string_abs(size.clone());
        let mut notional: Value = Precise::string_mul(size.clone(), mark_price.clone());
        return self.safe_position(Value::Json(normalize(&Value::Json(json!({
            "info": position,
            "id": Value::Undefined,
            "symbol": self.safe_string(market.clone(), Value::from("symbol")),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastUpdateTimestamp": Value::Undefined,
            "initialMargin": Value::Undefined,
            "initialMarginPercentage": Value::Undefined,
            "maintenanceMargin": Value::Undefined,
            "maintenanceMarginPercentage": Value::Undefined,
            "entryPrice": self.parse_number(entry_price.clone(), Value::Undefined),
            "notional": self.parse_number(notional.clone(), Value::Undefined),
            "leverage": Value::Undefined,
            "unrealizedPnl": self.parse_number(unrealised_pnl.clone(), Value::Undefined),
            "contracts": self.parse_number(size.clone(), Value::Undefined),
            "contractSize": self.parse_number(contract_size.clone(), Value::Undefined),
            "marginRatio": Value::Undefined,
            "liquidationPrice": self.safe_number(position.clone(), Value::from("estLiqPrice"), Value::Undefined),
            "markPrice": self.parse_number(mark_price.clone(), Value::Undefined),
            "lastPrice": Value::Undefined,
            "collateral": Value::Undefined,
            "marginMode": "cross",
            "marginType": Value::Undefined,
            "side": side,
            "percentage": Value::Undefined,
            "hedged": Value::Undefined,
            "stopLossPrice": Value::Undefined,
            "takeProfitPrice": Value::Undefined
        }))).unwrap()));
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut topic: Value = Value::from("balance");
        let mut message_hash: Value = topic.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return <Self as Woofipro>::watch_private(self, message_hash.clone(), message.clone(), Value::Undefined).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic":"balance",
        //         "ts":1651836695254,
        //         "data":{
        //             "balances":{
        //                 "USDC":{
        //                     "holding":5555815.47398272,
        //                     "frozen":0,
        //                     "interest":0,
        //                     "pendingShortQty":0,
        //                     "pendingExposure":0,
        //                     "pendingLongQty":0,
        //                     "pendingLongExposure":0,
        //                     "version":894,
        //                     "staked":51370692,
        //                     "unbonding":0,
        //                     "vault":0,
        //                     "averageOpenPrice":0.00000574,
        //                     "pnl24H":0,
        //                     "fee24H":0.01914,
        //                     "markPrice":0.31885
        //                 }
        //             }
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut balances: Value = self.safe_dict(data.clone(), Value::from("balances"), Value::new_object());
        let mut keys: Value = Object::keys(balances.clone());
        let mut ts: Value = self.safe_integer(message.clone(), Value::from("ts"));
        self.get("balance".into()).set("info".into(), data.clone());
        self.get("balance".into()).set("timestamp".into(), ts.clone());
        self.get("balance".into()).set("datetime".into(), self.iso8601(ts.clone()));
        let mut i: usize = 0;
        while i < keys.len() {
            let mut key: Value = keys.get(i.into());
            let mut value: Value = balances.get(key.clone());
            let mut code: Value = self.safe_currency_code(key.clone(), Value::Undefined);
            let mut account: Value = if self.get("balance".into()).contains_key(code.clone()) { self.get("balance".into()).get(code.clone()) } else { self.account() };
            let mut total: Value = self.safe_string(value.clone(), Value::from("holding"));
            let mut used: Value = self.safe_string(value.clone(), Value::from("frozen"));
            account.set("total".into(), total.clone());
            account.set("used".into(), used.clone());
            account.set("free".into(), Precise::string_sub(total.clone(), used.clone()));
            self.get("balance".into()).set(code.clone(), account.clone());
            i += 1;
        };
        self.set("balance".into(), self.safe_balance(self.get("balance".into())));
        client.resolve(self.get("balance".into()), Value::from("balance"));
        Value::Undefined
    }

    fn handle_error_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {"id":"1","event":"subscribe","success":false,"ts":1710780997216,"errorMsg":"Auth is needed."}
        //
        if !message.contains_key(Value::from("success")) {
            return false.into();
        };
        let mut success: Value = self.safe_bool(message.clone(), Value::from("success"), Value::Undefined);
        if success.is_truthy() {
            return false.into();
        };
        let mut error_message: Value = self.safe_string(message.clone(), Value::from("errorMsg"));
                if error_message.clone().is_nonnullish() {
            let mut feedback: Value = self.get("id".into()) + Value::from(" ") + self.json(message.clone());
            self.throw_exactly_matched_exception(self.get("exceptions".into()).get(Value::from("exact")), error_message.clone(), feedback.clone());
        };
        return false.into();
        // catch block omitted (no exception support in Value runtime)
;
        Value::Undefined
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        if <Self as Woofipro>::handle_error_message(self, client.clone(), message.clone()).is_truthy() {
            return Value::Undefined;
        };
        let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
            "ping": self.get("handlePing".into()),
            "pong": self.get("handlePong".into()),
            "subscribe": self.get("handleSubscribe".into()),
            "orderbook": self.get("handleOrderBook".into()),
            "ticker": self.get("handleTicker".into()),
            "tickers": self.get("handleTickers".into()),
            "kline": self.get("handleOHLCV".into()),
            "trade": self.get("handleTrade".into()),
            "auth": self.get("handleAuth".into()),
            "executionreport": self.get("handleOrderUpdate".into()),
            "algoexecutionreport": self.get("handleOrderUpdate".into()),
            "position": self.get("handlePositions".into()),
            "balance": self.get("handleBalance".into()),
            "bbos": self.get("handleBidAsk".into())
        }))).unwrap());
        let mut event: Value = self.safe_string(message.clone(), Value::from("event"));
        let mut method: Value = self.safe_value(methods.clone(), event.clone());
        if method.clone().is_nonnullish() {
            method.call(self, client.clone(), message.clone());
            return Value::Undefined;
        };
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"));
        if topic.clone().is_nonnullish() {
            method = self.safe_value(methods.clone(), topic.clone());
            if method.clone().is_nonnullish() {
                method.call(self, client.clone(), message.clone());
                return Value::Undefined;
            };
            let mut split_topic: Value = topic.split(Value::from("@"));
            let mut split_length: usize = split_topic.len();
            if split_length.clone() == Value::from(2) {
                let mut name: Value = self.safe_string(split_topic.clone(), Value::from(1));
                method = self.safe_value(methods.clone(), name.clone());
                if method.clone().is_nonnullish() {
                    method.call(self, client.clone(), message.clone());
                    return Value::Undefined;
                };
                let mut split_name: Value = name.split(Value::from("_"));
                let mut split_name_length: usize = split_topic.len();
                if split_name_length.clone() == Value::from(2) {
                    method = self.safe_value(methods.clone(), self.safe_string(split_name.clone(), Value::from(0)));
                    if method.clone().is_nonnullish() {
                        method.call(self, client.clone(), message.clone());
                    };
                };
            };
        };
        Value::Undefined
    }

    fn ping(&mut self, mut client: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "event": "ping"
        }))).unwrap());
    }

    fn handle_ping(&mut self, mut client: Value, mut message: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "event": "pong"
        }))).unwrap());
    }

    fn handle_pong(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // { event: "pong", ts: 1614667590000 }
        //
        client.set("last_pong".into(), self.milliseconds());
        return message.clone();
    }

    fn handle_subscribe(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "id": "666888",
        //         "event": "subscribe",
        //         "success": true,
        //         "ts": 1657117712212
        //     }
        //
        return message.clone();
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "v1PublicGetPublicvolumestats" => Woofipro::request(self, "public/volume/stats".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicbrokername" => Woofipro::request(self, "public/broker/name".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicchaininfobrokerid" => Woofipro::request(self, "public/chain_info/{broker_id}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicsysteminfo" => Woofipro::request(self, "public/system_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicvaultbalance" => Woofipro::request(self, "public/vault_balance".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicinsurancefund" => Woofipro::request(self, "public/insurancefund".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicchaininfo" => Woofipro::request(self, "public/chain_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetFaucetusdc" => Woofipro::request(self, "faucet/usdc".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicaccount" => Woofipro::request(self, "public/account".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetGetaccount" => Woofipro::request(self, "get_account".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetRegistrationnonce" => Woofipro::request(self, "registration_nonce".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetGetorderlykey" => Woofipro::request(self, "get_orderly_key".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicliquidation" => Woofipro::request(self, "public/liquidation".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicliquidatedpositions" => Woofipro::request(self, "public/liquidated_positions".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicconfig" => Woofipro::request(self, "public/config".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPubliccampaignranking" => Woofipro::request(self, "public/campaign/ranking".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPubliccampaignstats" => Woofipro::request(self, "public/campaign/stats".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPubliccampaignuser" => Woofipro::request(self, "public/campaign/user".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPubliccampaignstatsdetails" => Woofipro::request(self, "public/campaign/stats/details".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPubliccampaigns" => Woofipro::request(self, "public/campaigns".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicpointsleaderboard" => Woofipro::request(self, "public/points/leaderboard".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetClientpoints" => Woofipro::request(self, "client/points".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicpointsepoch" => Woofipro::request(self, "public/points/epoch".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicpointsepochdates" => Woofipro::request(self, "public/points/epoch_dates".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicreferralcheckrefcode" => Woofipro::request(self, "public/referral/check_ref_code".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicreferralverifyrefcode" => Woofipro::request(self, "public/referral/verify_ref_code".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetReferraladmininfo" => Woofipro::request(self, "referral/admin_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetReferralinfo" => Woofipro::request(self, "referral/info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetReferralrefereeinfo" => Woofipro::request(self, "referral/referee_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetReferralrefereerebatesummary" => Woofipro::request(self, "referral/referee_rebate_summary".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetReferralrefereehistory" => Woofipro::request(self, "referral/referee_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetReferralreferralhistory" => Woofipro::request(self, "referral/referral_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetReferralrebatesummary" => Woofipro::request(self, "referral/rebate_summary".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetClientdistributionhistory" => Woofipro::request(self, "client/distribution_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetTvconfig" => Woofipro::request(self, "tv/config".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetTvhistory" => Woofipro::request(self, "tv/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetTvsymbolinfo" => Woofipro::request(self, "tv/symbol_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicfundingratehistory" => Woofipro::request(self, "public/funding_rate_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicfundingratesymbol" => Woofipro::request(self, "public/funding_rate/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicfundingrates" => Woofipro::request(self, "public/funding_rates".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicinfo" => Woofipro::request(self, "public/info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicinfosymbol" => Woofipro::request(self, "public/info/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicmarkettrades" => Woofipro::request(self, "public/market_trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublictoken" => Woofipro::request(self, "public/token".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicfutures" => Woofipro::request(self, "public/futures".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetPublicfuturessymbol" => Woofipro::request(self, "public/futures/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicPostRegisteraccount" => Woofipro::request(self, "register_account".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientkeyinfo" => Woofipro::request(self, "client/key_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientorderlykeyiprestriction" => Woofipro::request(self, "client/orderly_key_ip_restriction".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetOrderoid" => Woofipro::request(self, "order/{oid}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientorderclientorderid" => Woofipro::request(self, "client/order/{client_order_id}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetAlgoorderoid" => Woofipro::request(self, "algo/order/{oid}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetAlgoclientorderclientorderid" => Woofipro::request(self, "algo/client/order/{client_order_id}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetOrders" => Woofipro::request(self, "orders".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetAlgoorders" => Woofipro::request(self, "algo/orders".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetTradetid" => Woofipro::request(self, "trade/{tid}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetTrades" => Woofipro::request(self, "trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetOrderoidtrades" => Woofipro::request(self, "order/{oid}/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientliquidatorliquidations" => Woofipro::request(self, "client/liquidator_liquidations".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetLiquidations" => Woofipro::request(self, "liquidations".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetAssethistory" => Woofipro::request(self, "asset/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientholding" => Woofipro::request(self, "client/holding".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetWithdrawnonce" => Woofipro::request(self, "withdraw_nonce".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetSettlenonce" => Woofipro::request(self, "settle_nonce".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetPnlsettlementhistory" => Woofipro::request(self, "pnl_settlement/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetVolumeuserdaily" => Woofipro::request(self, "volume/user/daily".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetVolumeuserstats" => Woofipro::request(self, "volume/user/stats".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientstatistics" => Woofipro::request(self, "client/statistics".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientinfo" => Woofipro::request(self, "client/info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientstatisticsdaily" => Woofipro::request(self, "client/statistics/daily".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetPositions" => Woofipro::request(self, "positions".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetPositionsymbol" => Woofipro::request(self, "position/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetFundingfeehistory" => Woofipro::request(self, "funding_fee/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetNotificationinboxnotifications" => Woofipro::request(self, "notification/inbox/notifications".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetNotificationinboxunread" => Woofipro::request(self, "notification/inbox/unread".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetVolumebrokerdaily" => Woofipro::request(self, "volume/broker/daily".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetBrokerfeeratedefault" => Woofipro::request(self, "broker/fee_rate/default".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetBrokeruserinfo" => Woofipro::request(self, "broker/user_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetOrderbooksymbol" => Woofipro::request(self, "orderbook/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetKline" => Woofipro::request(self, "kline".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostOrderlykey" => Woofipro::request(self, "orderly_key".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientsetorderlykeyiprestriction" => Woofipro::request(self, "client/set_orderly_key_ip_restriction".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientresetorderlykeyiprestriction" => Woofipro::request(self, "client/reset_orderly_key_ip_restriction".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostOrder" => Woofipro::request(self, "order".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostBatchorder" => Woofipro::request(self, "batch-order".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostAlgoorder" => Woofipro::request(self, "algo/order".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostLiquidation" => Woofipro::request(self, "liquidation".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClaiminsurancefund" => Woofipro::request(self, "claim_insurance_fund".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostWithdrawrequest" => Woofipro::request(self, "withdraw_request".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostSettlepnl" => Woofipro::request(self, "settle_pnl".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostNotificationinboxmarkread" => Woofipro::request(self, "notification/inbox/mark_read".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostNotificationinboxmarkreadall" => Woofipro::request(self, "notification/inbox/mark_read_all".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientleverage" => Woofipro::request(self, "client/leverage".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientmaintenanceconfig" => Woofipro::request(self, "client/maintenance_config".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostDelegatesigner" => Woofipro::request(self, "delegate_signer".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostDelegateorderlykey" => Woofipro::request(self, "delegate_orderly_key".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostDelegatesettlepnl" => Woofipro::request(self, "delegate_settle_pnl".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostDelegatewithdrawrequest" => Woofipro::request(self, "delegate_withdraw_request".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostBrokerfeerateset" => Woofipro::request(self, "broker/fee_rate/set".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostBrokerfeeratesetdefault" => Woofipro::request(self, "broker/fee_rate/set_default".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostBrokerfeeratedefault" => Woofipro::request(self, "broker/fee_rate/default".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostReferralcreate" => Woofipro::request(self, "referral/create".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostReferralupdate" => Woofipro::request(self, "referral/update".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostReferralbind" => Woofipro::request(self, "referral/bind".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostReferraleditsplit" => Woofipro::request(self, "referral/edit_split".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutOrder" => Woofipro::request(self, "order".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutAlgoorder" => Woofipro::request(self, "algo/order".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteOrder" => Woofipro::request(self, "order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteAlgoorder" => Woofipro::request(self, "algo/order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteClientorder" => Woofipro::request(self, "client/order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteAlgoclientorder" => Woofipro::request(self, "algo/client/order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteAlgoorders" => Woofipro::request(self, "algo/orders".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteOrders" => Woofipro::request(self, "orders".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteBatchorder" => Woofipro::request(self, "batch-order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteClientbatchorder" => Woofipro::request(self, "client/batch-order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct WoofiproImpl(Value);
impl Exchange for WoofiproImpl {}
impl Woofipro for WoofiproImpl {}
impl ValueTrait for WoofiproImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Vec<Value> { self.0.keys() }
    fn values(&self) -> Vec<Value> { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value) -> Value { self.0.slice(start) }
}

impl WoofiproImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = WoofiproImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Woofipro::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


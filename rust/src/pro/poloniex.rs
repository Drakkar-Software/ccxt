#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Poloniex : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "poloniex",
            "name": "Poloniex",
            "countries": [
                "US"
            ],
            "enableRateLimit": true,
            "rateLimit": 5,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "sandbox": true,
                "spot": true,
                "swap": true,
                "future": true,
                "option": false,
                "addMargin": true,
                "cancelAllOrders": true,
                "cancelAllOrdersWs": true,
                "cancelOrder": true,
                "cancelOrderWs": true,
                "cancelOrdersWs": true,
                "createDepositAddress": true,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrderWs": true,
                "createStopOrder": true,
                "createTriggerOrder": true,
                "editOrder": true,
                "editOrderWs": false,
                "fetchBalance": true,
                "fetchBalanceWs": false,
                "fetchClosedOrder": false,
                "fetchClosedOrders": true,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": true,
                "fetchDepositWithdrawFee": "emulated",
                "fetchDepositWithdrawFees": true,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchL2OrderBook": true,
                "fetchLeverage": true,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrder": false,
                "fetchOpenOrders": true,
                "fetchOpenOrdersWs": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrderBooks": false,
                "fetchOrderTrades": true,
                "fetchOrderWs": false,
                "fetchPosition": false,
                "fetchPositionMode": true,
                "fetchPositions": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradesWs": false,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchTransactions": "emulated",
                "fetchTransfer": false,
                "fetchTransfers": false,
                "fetchWithdrawals": true,
                "reduceMargin": true,
                "setLeverage": true,
                "setPositionMode": true,
                "transfer": true,
                "watchBalance": true,
                "watchMyTrades": true,
                "watchOHLCV": true,
                "watchOrderBook": true,
                "watchOrders": true,
                "watchStatus": false,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": true,
                "withdraw": true,
                "ws": true
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/1294454/27766817-e9456312-5ee6-11e7-9b3c-b628ca5626a5.jpg",
                "api": {
                    "spot": "https://api.poloniex.com",
                    "swap": "https://api.poloniex.com",
                    "ws": {
                        "public": "wss://ws.poloniex.com/ws/public",
                        "private": "wss://ws.poloniex.com/ws/private"
                    }
                },
                "www": "https://www.poloniex.com",
                "doc": "https://api-docs.poloniex.com/spot/",
                "fees": "https://poloniex.com/fees",
                "test": {
                    "spot": "https://sand-spot-api-gateway.poloniex.com"
                },
                "referral": "https://poloniex.com/signup?c=UBFZJRPJ"
            },
            "api": {
                "public": {
                    "get": {
                        "markets": 20,
                        "markets/{symbol}": 1,
                        "currencies": 20,
                        "currencies/{currency}": 20,
                        "v2/currencies": 20,
                        "v2/currencies/{currency}": 20,
                        "timestamp": 1,
                        "markets/price": 1,
                        "markets/{symbol}/price": 1,
                        "markets/markPrice": 1,
                        "markets/{symbol}/markPrice": 1,
                        "markets/{symbol}/markPriceComponents": 1,
                        "markets/{symbol}/orderBook": 1,
                        "markets/{symbol}/candles": 1,
                        "markets/{symbol}/trades": 20,
                        "markets/ticker24h": 20,
                        "markets/{symbol}/ticker24h": 20,
                        "markets/collateralInfo": 1,
                        "markets/{currency}/collateralInfo": 1,
                        "markets/borrowRatesInfo": 1
                    }
                },
                "private": {
                    "get": {
                        "accounts": 4,
                        "accounts/balances": 4,
                        "accounts/{id}/balances": 4,
                        "accounts/activity": 20,
                        "accounts/transfer": 20,
                        "accounts/transfer/{id}": 4,
                        "feeinfo": 20,
                        "accounts/interest/history": 1,
                        "subaccounts": 4,
                        "subaccounts/balances": 20,
                        "subaccounts/{id}/balances": 4,
                        "subaccounts/transfer": 20,
                        "subaccounts/transfer/{id}": 4,
                        "wallets/addresses": 20,
                        "wallets/addresses/{currency}": 20,
                        "wallets/activity": 20,
                        "margin/accountMargin": 4,
                        "margin/borrowStatus": 4,
                        "margin/maxSize": 4,
                        "orders": 20,
                        "orders/{id}": 4,
                        "orders/killSwitchStatus": 4,
                        "smartorders": 20,
                        "smartorders/{id}": 4,
                        "orders/history": 20,
                        "smartorders/history": 20,
                        "trades": 20,
                        "orders/{id}/trades": 4
                    },
                    "post": {
                        "accounts/transfer": 4,
                        "subaccounts/transfer": 20,
                        "wallets/address": 20,
                        "wallets/withdraw": 20,
                        "v2/wallets/withdraw": 20,
                        "orders": 4,
                        "orders/batch": 20,
                        "orders/killSwitch": 4,
                        "smartorders": 4
                    },
                    "delete": {
                        "orders/{id}": 4,
                        "orders/cancelByIds": 20,
                        "orders": 20,
                        "smartorders/{id}": 4,
                        "smartorders/cancelByIds": 20,
                        "smartorders": 20
                    },
                    "put": {
                        "orders/{id}": 20,
                        "smartorders/{id}": 20
                    }
                },
                "swapPublic": {
                    "get": {
                        "v3/market/allInstruments": 0.6666666666666666,
                        "v3/market/instruments": 0.6666666666666666,
                        "v3/market/orderBook": 0.6666666666666666,
                        "v3/market/candles": 10,
                        "v3/market/indexPriceCandlesticks": 10,
                        "v3/market/premiumIndexCandlesticks": 10,
                        "v3/market/markPriceCandlesticks": 10,
                        "v3/market/trades": 0.6666666666666666,
                        "v3/market/liquidationOrder": 0.6666666666666666,
                        "v3/market/tickers": 0.6666666666666666,
                        "v3/market/markPrice": 0.6666666666666666,
                        "v3/market/indexPrice": 0.6666666666666666,
                        "v3/market/indexPriceComponents": 0.6666666666666666,
                        "v3/market/fundingRate": 0.6666666666666666,
                        "v3/market/openInterest": 0.6666666666666666,
                        "v3/market/insurance": 0.6666666666666666,
                        "v3/market/riskLimit": 0.6666666666666666
                    }
                },
                "swapPrivate": {
                    "get": {
                        "v3/account/balance": 4,
                        "v3/account/bills": 20,
                        "v3/trade/order/opens": 20,
                        "v3/trade/order/trades": 20,
                        "v3/trade/order/history": 20,
                        "v3/trade/position/opens": 20,
                        "v3/trade/position/history": 20,
                        "v3/position/leverages": 20,
                        "v3/position/mode": 20
                    },
                    "post": {
                        "v3/trade/order": 4,
                        "v3/trade/orders": 40,
                        "v3/trade/position": 20,
                        "v3/trade/positionAll": 100,
                        "v3/position/leverage": 20,
                        "v3/position/mode": 20,
                        "v3/trade/position/margin": 20
                    },
                    "delete": {
                        "v3/trade/order": 2,
                        "v3/trade/batchOrders": 20,
                        "v3/trade/allOrders": 20
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "MINUTE_1",
                "5m": "MINUTE_5",
                "10m": "MINUTE_10",
                "15m": "MINUTE_15",
                "30m": "MINUTE_30",
                "1h": "HOUR_1",
                "2h": "HOUR_2",
                "4h": "HOUR_4",
                "6h": "HOUR_6",
                "12h": "HOUR_12",
                "1d": "DAY_1",
                "3d": "DAY_3",
                "1w": "WEEK_1",
                "1M": "MONTH_1"
            },
            "fees": {
                "trading": {
                    "taker": 0.0009,
                    "maker": 0.0009,
                    "feeSide": "get"
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "AIR": "AirCoin",
                "APH": "AphroditeCoin",
                "BCC": "BTCtalkcoin",
                "BCHABC": "BCHABC",
                "BDG": "Badgercoin",
                "BTM": "Bitmark",
                "CON": "Coino",
                "ETHTRON": "ETH",
                "GOLD": "GoldEagles",
                "GPUC": "GPU",
                "HOT": "Hotcoin",
                "ITC": "Information Coin",
                "KEY": "KEYCoin",
                "MASK": "NFTX Hashmasks Index",
                "MEME": "Degenerator Meme",
                "PLX": "ParallaxCoin",
                "REPV2": "REP",
                "STR": "XLM",
                "SOC": "SOCC",
                "TRADE": "Unitrade",
                "TRXETH": "TRX",
                "XAP": "API Coin",
                "USDTBSC": "USDT",
                "USDTTRON": "USDT",
                "USDTETH": "USDT",
                "UST": "USTC"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "options": {
                "defaultType": "spot",
                "createMarketBuyOrderRequiresPrice": true,
                "networks": {
                    "BEP20": "BSC",
                    "ERC20": "ETH",
                    "TRC20": "TRON",
                    "TRX": "TRON"
                },
                "networksById": {
                    "TRX": "TRC20",
                    "TRON": "TRC20"
                },
                "limits": {
                    "cost": {
                        "min": {
                            "BTC": 0.0001,
                            "ETH": 0.0001,
                            "USDT": 1,
                            "TRX": 100,
                            "BNB": 0.06,
                            "USDC": 1,
                            "USDJ": 1,
                            "TUSD": 0.0001,
                            "DAI": 1,
                            "PAX": 1,
                            "BUSD": 1
                        }
                    }
                },
                "accountsByType": {
                    "spot": "spot",
                    "future": "futures"
                },
                "accountsById": {
                    "exchange": "spot",
                    "futures": "future"
                },
                "tradesLimit": 1000,
                "ordersLimit": 1000,
                "OHLCVLimit": 1000,
                "watchOrderBook": {
                    "name": "book_lv2"
                },
                "connectionsLimit": 2000,
                "requestsLimit": 500,
                "timeframes": {
                    "1m": "candles_minute_1",
                    "5m": "candles_minute_5",
                    "10m": "candles_minute_10",
                    "15m": "candles_minute_15",
                    "30m": "candles_minute_30",
                    "1h": "candles_hour_1",
                    "2h": "candles_hour_2",
                    "4h": "candles_hour_4",
                    "6h": "candles_hour_6",
                    "12h": "candles_hour_12",
                    "1d": "candles_day_1",
                    "3d": "candles_day_3",
                    "1w": "candles_week_1",
                    "1M": "candles_month_1"
                }
            },
            "features": {
                "default": {
                    "sandbox": true,
                    "createOrder": {
                        "marginMode": true,
                        "triggerPrice": true,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "leverage": false,
                        "marketBuyByCost": true,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": true,
                        "trailing": false,
                        "iceberg": false
                    },
                    "createOrders": {
                        "max": 20
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 1000,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 2000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 500
                    }
                },
                "spot": {
                    "extends": "default"
                },
                "forContracts": {
                    "extends": "default",
                    "createOrder": {
                        "marginMode": true,
                        "triggerPrice": false,
                        "hedged": true,
                        "stpMode": true,
                        "marketBuyByCost": false
                    },
                    "createOrders": {
                        "max": 10
                    },
                    "fetchOpenOrders": {
                        "limit": 100
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBackCanceled": 0.16666666666666666,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchMyTrades": {
                        "limit": 100,
                        "untilDays": 90
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forContracts"
                    },
                    "inverse": {
                        "extends": "forContracts"
                    }
                },
                "future": {
                    "linear": {
                        "extends": "forContracts"
                    },
                    "inverse": {
                        "extends": "forContracts"
                    }
                }
            },
            "streaming": {
                "keepAlive": 15000
            }
        }"###).unwrap())
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.check_required_credentials(Value::Undefined);
        let mut timestamp: Value = self.number_to_string(self.milliseconds());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut message_hash: Value = Value::from("authenticated");
        let mut client: Value = self.client(url.clone());
        let mut future: Value = self.safe_value(client.get(subscriptions.clone()), message_hash.clone(), Value::Undefined);
        if future.clone().is_nullish() {
            let mut access_path: Value = Value::from("/ws");
            let mut request_string: Value = Value::from("GET
") + access_path.clone() + Value::from("
signTimestamp=") + timestamp.clone();
            let mut signature: Value = self.hmac(self.encode(request_string.clone()), self.encode(self.get("secret".into())), sha256().clone(), Value::from("base64"));
            let mut request: Value = Value::Json(normalize(&Value::Json(json!({
                "event": "subscribe",
                "channel": Value::Json(serde_json::Value::Array(vec![Value::from("auth").into()])),
                "params": Value::Json(normalize(&Value::Json(json!({
                    "key": self.get("apiKey".into()),
                    "signTimestamp": timestamp,
                    "signature": signature,
                    "signatureMethod": "HmacSHA256",
                    "signatureVersion": "2"
                }))).unwrap())
            }))).unwrap());
            // optional
            let mut message: Value = extend_2(request.clone(), params.clone());
            future = self.watch(url.clone(), message_hash.clone(), message.clone(), message_hash.clone(), Value::Undefined).await;
            //
            //    {
            //        "data": {
            //            "success": true,
            //            "ts": 1645597033915
            //        },
            //        "channel": "auth"
            //    }
            //
            //    # Failure to return results
            //
            //    {
            //        "data": {
            //            "success": false,
            //            "message": "Authentication failed!",
            //            "ts": 1646276295075
            //        },
            //        "channel": "auth"
            //    }
            //
            client.get(subscriptions.clone()).set(message_hash.clone(), future.clone());
        };
        return future.clone();
    }

    async fn subscribe(&mut self, mut name: Value, mut message_hash: Value, mut is_private: Value, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut public_or_private: Value = if is_private.is_truthy() { Value::from("private") } else { Value::from("public") };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(public_or_private.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "channel": Value::Json(serde_json::Value::Array(vec![name.clone().into()]))
        }))).unwrap());
        let mut market_ids: Value = Value::new_array();
        if self.is_empty(symbols.clone()).is_truthy() {
            market_ids.push(Value::from("all"));
        } else {
            message_hash = message_hash.clone() + Value::from("::") + symbols.join(Value::from(","));
            market_ids = self.market_ids(symbols.clone());
        };
        if name.clone() != Value::from("balances") {
            subscribe.set("symbols".into(), market_ids.clone());
        };
        let mut request: Value = extend_2(subscribe.clone(), params.clone());
        return self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), Value::Undefined).await;
    }

    async fn trade_request(&mut self, mut name: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut message_hash: Value = self.nonce().to_string();
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "id": message_hash,
            "event": name,
            "params": params
        }))).unwrap());
        return self.watch(url.clone(), message_hash.clone(), subscribe.clone(), message_hash.clone(), Value::Undefined).await;
    }

    async fn create_order_ws(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        <Self as Poloniex>::authenticate(self).await;
        let mut market: Value = self.market(symbol.clone());
        let mut uppercase_type: Value = r#type.to_upper_case();
        let mut uppercase_side: Value = side.to_upper_case();
        let mut is_post_only: Value = self.is_post_only((uppercase_type.clone() == Value::from("MARKET")).into(), (uppercase_type.clone() == Value::from("LIMIT_MAKER")).into(), params.clone());
        if is_post_only.is_truthy() {
            uppercase_type = Value::from("LIMIT_MAKER");
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id")),
            "side": side.to_upper_case(),
            "type": r#type.to_upper_case()
        }))).unwrap());
        if uppercase_type.clone() == Value::from("MARKET") && uppercase_side.clone() == Value::from("BUY") {
            let mut quote_amount: Value = Value::Undefined;
            let mut create_market_buy_order_requires_price: Value = true.into();
            (create_market_buy_order_requires_price, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("createOrder"), Value::from("createMarketBuyOrderRequiresPrice"), true.into()));
            let mut cost: Value = self.safe_number(params.clone(), Value::from("cost"), Value::Undefined);
            params = self.omit(params.clone(), Value::from("cost"));
            if cost.clone().is_nonnullish() {
                quote_amount = self.cost_to_precision(symbol.clone(), cost.clone());
            } else if create_market_buy_order_requires_price.is_truthy() {
                if price.clone().is_nullish() {
                    panic!(r###"InvalidOrder::new(self.get("id".into()) + Value::from(" createOrder() requires the price argument for market buy orders to calculate the total cost to spend (amount * price), alternatively set the createMarketBuyOrderRequiresPrice option or param to false and pass the cost to spend (quote quantity) in the amount argument"))"###);
                } else {
                    let mut amount_string: Value = self.number_to_string(amount.clone());
                    let mut price_string: Value = self.number_to_string(price.clone());
                    let mut cost_request: Value = Precise::string_mul(amount_string.clone(), price_string.clone());
                    quote_amount = self.cost_to_precision(symbol.clone(), cost_request.clone());
                };
            } else {
                quote_amount = self.cost_to_precision(symbol.clone(), amount.clone());
            };
            request.set("amount".into(), quote_amount.clone());
        } else {
            request.set("quantity".into(), self.amount_to_precision(market.get(Value::from("symbol")), amount.clone()));
            if price.clone().is_nonnullish() {
                request.set("price".into(), self.price_to_precision(symbol.clone(), price.clone()));
            };
        };
        let mut orders: Value = self.dispatch("tradeRequest".into(), Value::from("createOrder"), extend_2(request.clone(), params.clone())).await;
        let mut order: Value = self.safe_dict(orders.clone(), Value::from(0), Value::Undefined);
        return order.clone();
    }

    async fn cancel_order_ws(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut client_order_id: Value = self.safe_string(params.clone(), Value::from("clientOrderId"), Value::Undefined);
        if client_order_id.clone().is_nonnullish() {
            let mut client_order_ids: Value = self.safe_value(params.clone(), Value::from("clientOrderId"), Value::new_array());
            params.set("clientOrderIds".into(), self.array_concat(client_order_ids.clone(), Value::Json(serde_json::Value::Array(vec![client_order_id.clone().into()]))));
        };
        let mut orders: Value = <Self as Poloniex>::cancel_orders_ws(self, Value::Json(serde_json::Value::Array(vec![id.clone().into()])), symbol.clone(), params.clone()).await;
        let mut order: Value = self.safe_dict(orders.clone(), Value::from(0), Value::Undefined);
        return order.clone();
    }

    async fn cancel_orders_ws(&mut self, mut ids: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        <Self as Poloniex>::authenticate(self).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "orderIds": ids
        }))).unwrap());
        return self.dispatch("tradeRequest".into(), Value::from("cancelOrders"), extend_2(request.clone(), params.clone())).await;
    }

    async fn cancel_all_orders_ws(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        <Self as Poloniex>::authenticate(self).await;
        return self.dispatch("tradeRequest".into(), Value::from("cancelAllOrders"), params.clone()).await;
    }

    fn handle_order_request(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "id": "1234567",
        //        "data": [{
        //           "orderId": 205343650954092544,
        //           "clientOrderId": "",
        //           "message": "",
        //           "code": 200
        //        }]
        //    }
        //
        let mut message_hash: Value = self.safe_string(message.clone(), Value::from("id"), Value::Undefined);
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
        let mut orders: Value = Value::new_array();
        let mut i: usize = 0;
        while i < data.len() {
            let mut order: Value = data.get(i.into());
            let mut parsed_order: Value = <Self as Poloniex>::parse_ws_order(self, order.clone(), Value::Undefined);
            orders.push(parsed_order.clone());
            i += 1;
        };
        client.resolve(orders.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut timeframes: Value = self.safe_value(self.get("options".into()), Value::from("timeframes"), Value::new_object());
        let mut channel: Value = self.safe_string(timeframes.clone(), timeframe.clone(), timeframe.clone());
        if channel.clone().is_nullish() {
            panic!(r###"BadRequest::new(self.get("id".into()) + Value::from(" watchOHLCV cannot take a timeframe of ") + timeframe.clone())"###);
        };
        let mut ohlcv: Value = <Self as Poloniex>::subscribe(self, channel.clone(), channel.clone(), false.into(), Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = ohlcv.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(ohlcv.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        symbol = self.symbol(symbol.clone());
        let mut tickers: Value = <Self as Poloniex>::watch_tickers(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
        return self.safe_value(tickers.clone(), symbol.clone(), Value::Undefined);
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("ticker");
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut new_tickers: Value = <Self as Poloniex>::subscribe(self, name.clone(), name.clone(), false.into(), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            return new_tickers.clone();
        };
        return self.filter_by_array(self.get("tickers".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Poloniex>::watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn watch_trades_for_symbols(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), true.into(), true.into());
        let mut name: Value = Value::from("trades");
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        let mut market_ids: Value = self.market_ids(symbols.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "channel": Value::Json(serde_json::Value::Array(vec![name.clone().into()])),
            "symbols": market_ids
        }))).unwrap());
        let mut request: Value = extend_2(subscribe.clone(), params.clone());
        let mut message_hashes: Value = Value::new_array();
        if symbols.clone().is_nonnullish() {
            let mut i: usize = 0;
            while i < symbols.len() {
                message_hashes.push(name.clone() + Value::from("::") + symbols.get(i.into()));
                i += 1;
            };
        };
        let mut trades: Value = self.watch_multiple(url.clone(), message_hashes.clone(), request.clone(), message_hashes.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut first: Value = self.safe_value(trades.clone(), Value::from(0), Value::Undefined);
            let mut trade_symbol: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
            limit = trades.get_limit(trade_symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut watch_order_book_options: Value = self.safe_value(self.get("options".into()), Value::from("watchOrderBook"), Value::Undefined);
        let mut name: Value = self.safe_string(watch_order_book_options.clone(), Value::from("name"), Value::from("book_lv2"));
        (name, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("method"), Value::from("name"), name.clone()));
        let mut orderbook: Value = <Self as Poloniex>::subscribe(self, name.clone(), name.clone(), false.into(), Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
        return orderbook.limit();
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("orders");
        <Self as Poloniex>::authenticate(self).await;
        if symbol.clone().is_nonnullish() {
            symbol = self.symbol(symbol.clone());
        };
        let mut symbols: Value = if symbol.clone().is_nullish() { Value::Undefined } else { Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])) };
        let mut orders: Value = <Self as Poloniex>::subscribe(self, name.clone(), name.clone(), true.into(), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(orders.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    async fn watch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("orders");
        let mut message_hash: Value = Value::from("myTrades");
        <Self as Poloniex>::authenticate(self).await;
        if symbol.clone().is_nonnullish() {
            symbol = self.symbol(symbol.clone());
        };
        let mut symbols: Value = if symbol.clone().is_nullish() { Value::Undefined } else { Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])) };
        let mut trades: Value = <Self as Poloniex>::subscribe(self, name.clone(), message_hash.clone(), true.into(), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Poloniex>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("balances");
        <Self as Poloniex>::authenticate(self).await;
        return <Self as Poloniex>::subscribe(self, name.clone(), name.clone(), true.into(), Value::Undefined, params.clone()).await;
    }

    fn parse_ws_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        //    {
        //        "symbol": "BTC_USDT",
        //        "amount": "840.7240416",
        //        "high": "24832.35",
        //        "quantity": "0.033856",
        //        "tradeCount": 1,
        //        "low": "24832.35",
        //        "closeTime": 1676942519999,
        //        "startTime": 1676942460000,
        //        "close": "24832.35",
        //        "open": "24832.35",
        //        "ts": 1676942492072
        //    }
        //
        return Value::Json(serde_json::Value::Array(vec![self.safe_integer(ohlcv.clone(), Value::from("startTime"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("open"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("high"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("low"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("close"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("quantity"), Value::Undefined).into()]));
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "channel": "candles_minute_1",
        //        "data": [
        //            {
        //                "symbol": "BTC_USDT",
        //                "amount": "840.7240416",
        //                "high": "24832.35",
        //                "quantity": "0.033856",
        //                "tradeCount": 1,
        //                "low": "24832.35",
        //                "closeTime": 1676942519999,
        //                "startTime": 1676942460000,
        //                "close": "24832.35",
        //                "open": "24832.35",
        //                "ts": 1676942492072
        //            }
        //        ]
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        data = self.safe_value(data.clone(), Value::from(0), Value::Undefined);
        let mut channel: Value = self.safe_string(message.clone(), Value::from("channel"), Value::Undefined);
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut market: Value = self.safe_market(symbol.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut timeframes: Value = self.safe_value(self.get("options".into()), Value::from("timeframes"), Value::new_object());
        let mut timeframe: Value = self.find_timeframe(channel.clone(), timeframes.clone());
        let mut message_hash: Value = channel.clone() + Value::from("::") + symbol.clone();
        let mut parsed: Value = <Self as Poloniex>::parse_ws_ohlcv(self, data.clone(), market.clone());
        self.get("ohlcvs".into()).set(symbol.clone(), self.safe_value(self.get("ohlcvs".into()), symbol.clone(), Value::new_object()));
        let mut stored: Value = self.safe_value(self.get("ohlcvs".into()).get(symbol.clone()), timeframe.clone(), Value::Undefined);
        if symbol.clone().is_nonnullish() {
            if stored.clone().is_nullish() {
                let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
                stored = ArrayCacheByTimestamp::new(limit);
                self.get("ohlcvs".into()).get(symbol.clone()).set(timeframe.clone(), stored.clone());
            };
            stored.append(parsed.clone());
            client.resolve(stored.clone(), message_hash.clone());
        };
        return message.clone();
    }

    fn handle_trade(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "channel": "trades",
        //        "data": [
        //            {
        //                "symbol": "BTC_USDT",
        //                "amount": "13.41634893",
        //                "quantity": "0.000537",
        //                "takerSide": "buy",
        //                "createTime": 1676950548834,
        //                "price": "24983.89",
        //                "id": "62486976",
        //                "ts": 1676950548839
        //            }
        //        ]
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
        let mut i: usize = 0;
        while i < data.len() {
            let mut item: Value = data.get(i.into());
            let mut market_id: Value = self.safe_string(item.clone(), Value::from("symbol"), Value::Undefined);
            if market_id.clone().is_nonnullish() {
                let mut trade: Value = <Self as Poloniex>::parse_ws_trade(self, item.clone(), Value::Undefined);
                let mut symbol: Value = trade.get(Value::from("symbol"));
                let mut r#type: Value = Value::from("trades");
                let mut message_hash: Value = r#type.clone() + Value::from("::") + symbol.clone();
                let mut trades_array: Value = self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined);
                if trades_array.clone().is_nullish() {
                    let mut trades_limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
                    trades_array = ArrayCache::new(trades_limit);
                    self.get("trades".into()).set(symbol.clone(), trades_array.clone());
                };
                trades_array.append(trade.clone());
                client.resolve(trades_array.clone(), message_hash.clone());
            };
            i += 1;
        };
        return message.clone();
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        // handleTrade
        //
        //    {
        //        "symbol": "BTC_USDT",
        //        "amount": "13.41634893",
        //        "quantity": "0.000537",
        //        "takerSide": "buy",
        //        "createTime": 1676950548834,
        //        "price": "24983.89",
        //        "id": "62486976",
        //        "ts": 1676950548839
        //    }
        //
        // private trade
        //    {
        //        "orderId":"186250258089635840",
        //        "tradeId":"62036513",
        //        "clientOrderId":"",
        //        "accountType":"SPOT",
        //        "eventType":"trade",
        //        "symbol":"ADA_USDT",
        //        "side":"SELL",
        //        "type":"MARKET",
        //        "price":"0",
        //        "quantity":"3",
        //        "state":"FILLED",
        //        "createTime":1685371921891,
        //        "tradeTime":1685371921908,
        //        "tradePrice":"0.37694",
        //        "tradeQty":"3",
        //        "feeCurrency":"USDT",
        //        "tradeFee":"0.00226164",
        //        "tradeAmount":"1.13082",
        //        "filledQuantity":"3",
        //        "filledAmount":"1.13082",
        //        "ts":1685371921945,
        //        "source":"WEB",
        //        "orderAmount":"0",
        //        "matchRole":"TAKER"
        //     }
        //
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("createTime"), Value::Undefined);
        let mut taker_maker: Value = self.safe_string_lower_2(trade.clone(), Value::from("matchRole"), Value::from("taker"), Value::Undefined);
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade,
            "id": self.safe_string_2(trade.clone(), Value::from("id"), Value::from("tradeId"), Value::Undefined),
            "symbol": self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "order": self.safe_string(trade.clone(), Value::from("orderId"), Value::Undefined),
            "type": self.safe_string_lower(trade.clone(), Value::from("type"), Value::Undefined),
            "side": self.safe_string_lower_2(trade.clone(), Value::from("takerSide"), Value::from("side"), Value::Undefined),
            "takerOrMaker": taker_maker,
            "price": self.omit_zero(self.safe_string_2(trade.clone(), Value::from("tradePrice"), Value::from("price"), Value::Undefined)),
            "amount": self.omit_zero(self.safe_string_2(trade.clone(), Value::from("filledQuantity"), Value::from("quantity"), Value::Undefined)),
            "cost": self.safe_string_2(trade.clone(), Value::from("amount"), Value::from("filledAmount"), Value::Undefined),
            "fee": Value::Json(normalize(&Value::Json(json!({
                "rate": Value::Undefined,
                "cost": self.safe_string(trade.clone(), Value::from("tradeFee"), Value::Undefined),
                "currency": self.safe_string(trade.clone(), Value::from("feeCurrency"), Value::Undefined)
            }))).unwrap())
        }))).unwrap()), market.clone());
    }

    fn parse_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "NEW": "open",
            "PARTIALLY_FILLED": "open",
            "FILLED": "closed",
            "PENDING_CANCEL": "open",
            "PARTIALLY_CANCELED": "open",
            "CANCELED": "canceled"
        }))).unwrap());
        // FAILED
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_ws_order_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        //    {
        //        "symbol": "BTC_USDT",
        //        "type": "LIMIT",
        //        "quantity": "1",
        //        "orderId": "32471407854219264",
        //        "tradeFee": "0",
        //        "clientOrderId": "",
        //        "accountType": "SPOT",
        //        "feeCurrency": "",
        //        "eventType": "place",
        //        "source": "API",
        //        "side": "BUY",
        //        "filledQuantity": "0",
        //        "filledAmount": "0",
        //        "matchRole": "MAKER",
        //        "state": "NEW",
        //        "tradeTime": 0,
        //        "tradeAmount": "0",
        //        "orderAmount": "0",
        //        "createTime": 1648708186922,
        //        "price": "47112.1",
        //        "tradeQty": "0",
        //        "tradePrice": "0",
        //        "tradeId": "0",
        //        "ts": 1648708187469
        //    }
        //
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("tradeTime"), Value::Undefined);
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade,
            "id": self.safe_string(trade.clone(), Value::from("tradeId"), Value::Undefined),
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "order": self.safe_string(trade.clone(), Value::from("orderId"), Value::Undefined),
            "type": self.safe_string_lower(trade.clone(), Value::from("type"), Value::Undefined),
            "side": self.safe_string(trade.clone(), Value::from("side"), Value::Undefined),
            "takerOrMaker": self.safe_string_lower(trade.clone(), Value::from("matchRole"), Value::Undefined),
            "price": self.safe_string(trade.clone(), Value::from("price"), Value::Undefined),
            "amount": self.safe_string(trade.clone(), Value::from("tradeAmount"), Value::Undefined),
            "cost": Value::Undefined,
            "fee": Value::Json(normalize(&Value::Json(json!({
                "rate": Value::Undefined,
                "cost": self.safe_string(trade.clone(), Value::from("tradeFee"), Value::Undefined),
                "currency": self.safe_string(trade.clone(), Value::from("feeCurrency"), Value::Undefined)
            }))).unwrap())
        }))).unwrap()), market.clone());
    }

    fn handle_order(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // Order is created
        //
        //    {
        //        "channel": "orders",
        //        "data": [
        //            {
        //                "symbol": "BTC_USDT",
        //                "type": "LIMIT",
        //                "quantity": "1",
        //                "orderId": "32471407854219264",
        //                "tradeFee": "0",
        //                "clientOrderId": "",
        //                "accountType": "SPOT",
        //                "feeCurrency": "",
        //                "eventType": "place",
        //                "source": "API",
        //                "side": "BUY",
        //                "filledQuantity": "0",
        //                "filledAmount": "0",
        //                "matchRole": "MAKER",
        //                "state": "NEW",
        //                "tradeTime": 0,
        //                "tradeAmount": "0",
        //                "orderAmount": "0",
        //                "createTime": 1648708186922,
        //                "price": "47112.1",
        //                "tradeQty": "0",
        //                "tradePrice": "0",
        //                "tradeId": "0",
        //                "ts": 1648708187469
        //            }
        //        ]
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
        let mut orders: Value = self.get("orders".into());
        if orders.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::Undefined);
            orders = ArrayCacheBySymbolById::new(limit);
            self.set("orders".into(), orders.clone());
        };
        let mut market_ids: Value = Value::new_array();
        let mut i: usize = 0;
        while i < data.len() {
            let mut order: Value = self.safe_value(data.clone(), Value::from(i), Value::Undefined);
            let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
            let mut event_type: Value = self.safe_string(order.clone(), Value::from("eventType"), Value::Undefined);
            if market_id.clone().is_nonnullish() {
                let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
                let mut order_id: Value = self.safe_string(order.clone(), Value::from("orderId"), Value::Undefined);
                let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("clientOrderId"), Value::Undefined);
                if event_type.clone() == Value::from("place") || event_type.clone() == Value::from("canceled") {
                    let mut parsed: Value = <Self as Poloniex>::parse_ws_order(self, order.clone(), Value::Undefined);
                    orders.append(parsed.clone());
                } else {
                    let mut previous_orders: Value = self.safe_value(orders.get(hashmap.clone()), symbol.clone(), Value::new_object());
                    let mut previous_order: Value = self.safe_value_2(previous_orders.clone(), order_id.clone(), client_order_id.clone(), Value::Undefined);
                    let mut trade: Value = <Self as Poloniex>::parse_ws_trade(self, order.clone(), Value::Undefined);
                    <Self as Poloniex>::handle_my_trades(self, client.clone(), trade.clone());
                    if previous_order.get(Value::from("trades")).is_nullish() {
                        previous_order.set("trades".into(), Value::new_array());
                    };
                    previous_order.get(Value::from("trades")).push(trade.clone());
                    previous_order.set("lastTradeTimestamp".into(), trade.get(Value::from("timestamp")));
                    let mut total_cost: Value = Value::from("0");
                    let mut total_amount: Value = Value::from("0");
                    let mut previous_order_trades: Value = previous_order.get(Value::from("trades"));
                    let mut j: usize = 0;
                    while j < previous_order_trades.len() {
                        let mut previous_order_trade: Value = previous_order_trades.get(j.into());
                        let mut cost: Value = self.number_to_string(previous_order_trade.get(Value::from("cost")));
                        let mut amount: Value = self.number_to_string(previous_order_trade.get(Value::from("amount")));
                        total_cost = Precise::string_add(total_cost.clone(), cost.clone());
                        total_amount = Precise::string_add(total_amount.clone(), amount.clone());
                        j += 1;
                    };
                    if Precise::string_gt(total_amount.clone(), Value::from("0")) {
                        previous_order.set("average".into(), self.parse_number(Precise::string_div(total_cost.clone(), total_amount.clone(), Value::Undefined), Value::Undefined));
                    };
                    previous_order.set("cost".into(), self.parse_number(total_cost.clone(), Value::Undefined));
                    if previous_order.get(Value::from("filled")).is_nonnullish() {
                        let mut trade_amount: Value = self.number_to_string(trade.get(Value::from("amount")));
                        let mut previous_order_filled: Value = self.number_to_string(previous_order.get(Value::from("filled")));
                        previous_order_filled = Precise::string_add(previous_order_filled.clone(), trade_amount.clone());
                        previous_order.set("filled".into(), previous_order_filled.clone());
                        if previous_order.get(Value::from("amount")).is_nonnullish() {
                            let mut previous_order_amount: Value = self.number_to_string(previous_order.get(Value::from("amount")));
                            previous_order.set("remaining".into(), self.parse_number(Precise::string_sub(previous_order_amount.clone(), previous_order_filled.clone()), Value::Undefined));
                        };
                    };
                    if previous_order.get(Value::from("fee")).is_nullish() {
                        previous_order.set("fee".into(), Value::Json(normalize(&Value::Json(json!({
                            "rate": Value::Undefined,
                            "cost": 0,
                            "currency": trade.get(Value::from("fee")).get(Value::from("currency"))
                        }))).unwrap()));
                    };
                    if previous_order.get(Value::from("fee")).get(Value::from("cost")).is_nonnullish() && trade.get(Value::from("fee")).get(Value::from("cost")).is_nonnullish() {
                        let mut string_order_cost: Value = self.number_to_string(previous_order.get(Value::from("fee")).get(Value::from("cost")));
                        let mut string_trade_cost: Value = self.number_to_string(trade.get(Value::from("fee")).get(Value::from("cost")));
                        previous_order.get(Value::from("fee")).set("cost".into(), Precise::string_add(string_order_cost.clone(), string_trade_cost.clone()));
                    };
                    let mut raw_state: Value = self.safe_string(order.clone(), Value::from("state"), Value::Undefined);
                    let mut state: Value = <Self as Poloniex>::parse_status(self, raw_state.clone());
                    previous_order.set("status".into(), state.clone());
                    // update the newUpdates count
                    orders.append(previous_order.clone());
                };
                market_ids.push(market_id.clone());
            };
            i += 1;
        };
        let mut i: usize = 0;
        while i < market_ids.len() {
            let mut market_id: Value = market_ids.get(i.into());
            let mut market: Value = self.market(market_id.clone());
            let mut symbol: Value = market.get(Value::from("symbol"));
            let mut message_hash: Value = Value::from("orders::") + symbol.clone();
            client.resolve(orders.clone(), message_hash.clone());
            i += 1;
        };
        client.resolve(orders.clone(), Value::from("orders"));
        return message.clone();
    }

    fn parse_ws_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        //    {
        //        "symbol": "BTC_USDT",
        //        "type": "LIMIT",
        //        "quantity": "1",
        //        "orderId": "32471407854219264",
        //        "tradeFee": "0",
        //        "clientOrderId": "",
        //        "accountType": "SPOT",
        //        "feeCurrency": "",
        //        "eventType": "place",
        //        "source": "API",
        //        "side": "BUY",
        //        "filledQuantity": "0",
        //        "filledAmount": "0",
        //        "matchRole": "MAKER",
        //        "state": "NEW",
        //        "tradeTime": 0,
        //        "tradeAmount": "0",
        //        "orderAmount": "0",
        //        "createTime": 1648708186922,
        //        "price": "47112.1",
        //        "tradeQty": "0",
        //        "tradePrice": "0",
        //        "tradeId": "0",
        //        "ts": 1648708187469
        //    }
        //
        let mut id: Value = self.safe_string(order.clone(), Value::from("orderId"), Value::Undefined);
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("clientOrderId"), Value::Undefined);
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
        let mut timestamp: Value = self.safe_string(order.clone(), Value::from("ts"), Value::Undefined);
        let mut filled_amount: Value = self.safe_string(order.clone(), Value::from("filledAmount"), Value::Undefined);
        let mut status: Value = self.safe_string(order.clone(), Value::from("state"), Value::Undefined);
        let mut trades: Value = Value::Undefined;
        if !Precise::string_eq(filled_amount.clone(), Value::from("0")) {
            trades = Value::new_array();
            let mut trade: Value = <Self as Poloniex>::parse_ws_order_trade(self, order.clone(), Value::Undefined);
            trades.push(trade.clone());
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined),
            "id": id,
            "clientOrderId": client_order_id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": Value::Undefined,
            "type": self.safe_string(order.clone(), Value::from("type"), Value::Undefined),
            "timeInForce": Value::Undefined,
            "postOnly": Value::Undefined,
            "side": self.safe_string(order.clone(), Value::from("side"), Value::Undefined),
            "price": self.safe_string(order.clone(), Value::from("price"), Value::Undefined),
            "stopPrice": Value::Undefined,
            "triggerPrice": Value::Undefined,
            "amount": self.safe_string(order.clone(), Value::from("quantity"), Value::Undefined),
            "cost": Value::Undefined,
            "average": Value::Undefined,
            "filled": filled_amount,
            "remaining": self.safe_string(order.clone(), Value::from("remaining_size"), Value::Undefined),
            "status": <Self as Poloniex>::parse_status(self, status.clone()),
            "fee": Value::Json(normalize(&Value::Json(json!({
                "rate": Value::Undefined,
                "cost": self.safe_string(order.clone(), Value::from("tradeFee"), Value::Undefined),
                "currency": self.safe_string(order.clone(), Value::from("feeCurrency"), Value::Undefined)
            }))).unwrap()),
            "trades": trades
        }))).unwrap()), Value::Undefined);
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "channel": "ticker",
        //        "data": [
        //            {
        //                "symbol": "BTC_USDT",
        //                "startTime": 1677280800000,
        //                "open": "23154.32",
        //                "high": "23212.21",
        //                "low": "22761.01",
        //                "close": "23148.86",
        //                "quantity": "105.179566",
        //                "amount": "2423161.17436702",
        //                "tradeCount": 17582,
        //                "dailyChange": "-0.0002",
        //                "markPrice": "23151.09",
        //                "closeTime": 1677367197924,
        //                "ts": 1677367251090
        //            }
        //        ]
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
        let mut new_tickers: Value = Value::new_object();
        let mut i: usize = 0;
        while i < data.len() {
            let mut item: Value = data.get(i.into());
            let mut market_id: Value = self.safe_string(item.clone(), Value::from("symbol"), Value::Undefined);
            if market_id.clone().is_nonnullish() {
                let mut ticker: Value = self.parse_ticker(item.clone(), Value::Undefined);
                let mut symbol: Value = ticker.get(Value::from("symbol"));
                self.get("tickers".into()).set(symbol.clone(), ticker.clone());
                new_tickers.set(symbol.clone(), ticker.clone());
            };
            i += 1;
        };
        let mut message_hashes: Value = self.find_message_hashes(client.clone(), Value::from("ticker::"));
        let mut i: usize = 0;
        while i < message_hashes.len() {
            let mut message_hash: Value = message_hashes.get(i.into());
            let mut parts: Value = message_hash.split(Value::from("::"));
            let mut symbols_string: Value = parts.get(Value::from(1));
            let mut symbols: Value = symbols_string.split(Value::from(","));
            let mut tickers: Value = self.filter_by_array(new_tickers.clone(), Value::from("symbol"), symbols.clone(), Value::Undefined);
            if !self.is_empty(tickers.clone()).is_truthy() {
                client.resolve(tickers.clone(), message_hash.clone());
            };
            i += 1;
        };
        client.resolve(new_tickers.clone(), Value::from("ticker"));
        return message.clone();
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // snapshot
        //
        //    {
        //        "channel": "book_lv2",
        //        "data": [
        //            {
        //                "symbol": "BTC_USDT",
        //                "createTime": 1677368876253,
        //                "asks": [
        //                    ["5.65", "0.02"],
        //                    ...
        //                ],
        //                "bids": [
        //                    ["6.16", "0.6"],
        //                    ...
        //                ],
        //                "lastId": 164148724,
        //                "id": 164148725,
        //                "ts": 1677368876316
        //            }
        //        ],
        //        "action": "snapshot"
        //    }
        //
        // update
        //
        //    {
        //        "channel": "book_lv2",
        //        "data": [
        //            {
        //                "symbol": "BTC_USDT",
        //                "createTime": 1677368876882,
        //                "asks": [
        //                    ["6.35", "3"]
        //                ],
        //                "bids": [
        //                    ["5.65", "0.02"]
        //                ],
        //                "lastId": 164148725,
        //                "id": 164148726,
        //                "ts": 1677368876890
        //            }
        //        ],
        //        "action": "update"
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
        let mut r#type: Value = self.safe_string(message.clone(), Value::from("action"), Value::Undefined);
        let mut snapshot: Value = (r#type.clone() == Value::from("snapshot")).into();
        let mut update: Value = (r#type.clone() == Value::from("update")).into();
        let mut i: usize = 0;
        while i < data.len() {
            let mut item: Value = data.get(i.into());
            let mut market_id: Value = self.safe_string(item.clone(), Value::from("symbol"), Value::Undefined);
            let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            let mut symbol: Value = market.get(Value::from("symbol"));
            let mut name: Value = Value::from("book_lv2");
            let mut message_hash: Value = name.clone() + Value::from("::") + symbol.clone();
            let mut subscription: Value = self.safe_value(client.get(subscriptions.clone()), message_hash.clone(), Value::new_object());
            let mut limit: Value = self.safe_integer(subscription.clone(), Value::from("limit"), Value::Undefined);
            let mut timestamp: Value = self.safe_integer(item.clone(), Value::from("ts"), Value::Undefined);
            let mut asks: Value = self.safe_value(item.clone(), Value::from("asks"), Value::Undefined);
            let mut bids: Value = self.safe_value(item.clone(), Value::from("bids"), Value::Undefined);
            if snapshot.is_truthy() || update.is_truthy() {
                if snapshot.is_truthy() {
                    self.get("orderbooks".into()).set(symbol.clone(), self.order_book(Value::new_object(), limit.clone()));
                };
                let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
                if bids.clone().is_nonnullish() {
                    let mut j: usize = 0;
                    while j < bids.len() {
                        let mut bid: Value = self.safe_value(bids.clone(), Value::from(j), Value::Undefined);
                        let mut price: Value = self.safe_number(bid.clone(), Value::from(0), Value::Undefined);
                        let mut amount: Value = self.safe_number(bid.clone(), Value::from(1), Value::Undefined);
                        let mut bids_side: Value = orderbook.get(Value::from("bids"));
                        bids_side.store(price.clone(), amount.clone());
                        j += 1;
                    };
                };
                if asks.clone().is_nonnullish() {
                    let mut j: usize = 0;
                    while j < asks.len() {
                        let mut ask: Value = self.safe_value(asks.clone(), Value::from(j), Value::Undefined);
                        let mut price: Value = self.safe_number(ask.clone(), Value::from(0), Value::Undefined);
                        let mut amount: Value = self.safe_number(ask.clone(), Value::from(1), Value::Undefined);
                        let mut asks_side: Value = orderbook.get(Value::from("asks"));
                        asks_side.store(price.clone(), amount.clone());
                        j += 1;
                    };
                };
                orderbook.set("symbol".into(), symbol.clone());
                orderbook.set("timestamp".into(), timestamp.clone());
                orderbook.set("datetime".into(), self.iso8601(timestamp.clone()));
                client.resolve(orderbook.clone(), message_hash.clone());
            };
            i += 1;
        };
        Value::Undefined
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //       "channel": "balances",
        //       "data": [
        //            {
        //                "changeTime": 1657312008411,
        //                "accountId": "1234",
        //                "accountType": "SPOT",
        //                "eventType": "place_order",
        //                "available": "9999999983.668",
        //                "currency": "BTC",
        //                "id": 60018450912695040,
        //                "userId": 12345,
        //                "hold": "16.332",
        //                "ts": 1657312008443
        //            }
        //        ]
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
        let mut message_hash: Value = Value::from("balances");
        self.set("balance".into(), <Self as Poloniex>::parse_ws_balance(self, data.clone()));
        client.resolve(self.get("balance".into()), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_balance(&self, mut response: Value) -> Value {
        //
        //    [
        //        {
        //            "changeTime": 1657312008411,
        //            "accountId": "1234",
        //            "accountType": "SPOT",
        //            "eventType": "place_order",
        //            "available": "9999999983.668",
        //            "currency": "BTC",
        //            "id": 60018450912695040,
        //            "userId": 12345,
        //            "hold": "16.332",
        //            "ts": 1657312008443
        //        }
        //    ]
        //
        let mut first_balance: Value = self.safe_value(response.clone(), Value::from(0), Value::new_object());
        let mut timestamp: Value = self.safe_integer(first_balance.clone(), Value::from("ts"), Value::Undefined);
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone())
        }))).unwrap());
        let mut i: usize = 0;
        while i < response.len() {
            let mut balance: Value = self.safe_value(response.clone(), Value::from(i), Value::Undefined);
            let mut currency_id: Value = self.safe_string(balance.clone(), Value::from("currency"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut new_account: Value = self.account();
            new_account.set("free".into(), self.safe_string(balance.clone(), Value::from("available"), Value::Undefined));
            new_account.set("used".into(), self.safe_string(balance.clone(), Value::from("hold"), Value::Undefined));
            result.set(code.clone(), new_account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    fn handle_my_trades(&mut self, mut client: Value, mut parsed_trade: Value) -> Value {
        // emulated using the orders' stream
        let mut message_hash: Value = Value::from("myTrades");
        let mut symbol: Value = parsed_trade.get(Value::from("symbol"));
        if self.get("myTrades".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            self.set("my_trades".into(), ArrayCacheBySymbolById::new(limit));
        };
        let mut trades: Value = self.get("myTrades".into());
        trades.append(parsed_trade.clone());
        client.resolve(trades.clone(), message_hash.clone());
        let mut symbol_message_hash: Value = message_hash.clone() + Value::from(":") + symbol.clone();
        client.resolve(trades.clone(), symbol_message_hash.clone());
        Value::Undefined
    }

    fn handle_pong(&mut self, mut client: Value) -> Value {
        client.set("last_pong".into(), self.milliseconds());
        Value::Undefined
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        if <Self as Poloniex>::handle_error_message(self, client.clone(), message.clone()).is_truthy() {
            return Value::Undefined;
        };
        let mut r#type: Value = self.safe_string(message.clone(), Value::from("channel"), Value::Undefined);
        let mut event: Value = self.safe_string(message.clone(), Value::from("event"), Value::Undefined);
        if event.clone() == Value::from("pong") {
            client.set("last_pong".into(), self.milliseconds());
        };
        let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
            "candles_minute_1": self.get("handleOHLCV".into()),
            "candles_minute_5": self.get("handleOHLCV".into()),
            "candles_minute_10": self.get("handleOHLCV".into()),
            "candles_minute_15": self.get("handleOHLCV".into()),
            "candles_minute_30": self.get("handleOHLCV".into()),
            "candles_hour_1": self.get("handleOHLCV".into()),
            "candles_hour_2": self.get("handleOHLCV".into()),
            "candles_hour_4": self.get("handleOHLCV".into()),
            "candles_hour_6": self.get("handleOHLCV".into()),
            "candles_hour_12": self.get("handleOHLCV".into()),
            "candles_day_1": self.get("handleOHLCV".into()),
            "candles_day_3": self.get("handleOHLCV".into()),
            "candles_week_1": self.get("handleOHLCV".into()),
            "candles_month_1": self.get("handleOHLCV".into()),
            "book": self.get("handleOrderBook".into()),
            "book_lv2": self.get("handleOrderBook".into()),
            "ticker": self.get("handleTicker".into()),
            "trades": self.get("handleTrade".into()),
            "orders": self.get("handleOrder".into()),
            "balances": self.get("handleBalance".into()),
            "createOrder": self.get("handleOrderRequest".into()),
            "cancelOrder": self.get("handleOrderRequest".into()),
            "cancelAllOrders": self.get("handleOrderRequest".into()),
            "auth": self.get("handleAuthenticate".into())
        }))).unwrap());
        let mut method: Value = self.safe_value(methods.clone(), r#type.clone(), Value::Undefined);
        if r#type.clone() == Value::from("auth") {
            <Self as Poloniex>::handle_authenticate(self, client.clone(), message.clone());
        } else if r#type.clone().is_nullish() {
            <Self as Poloniex>::handle_order_request(self, client.clone(), message.clone());
        } else {
            let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
            let mut data_length: usize = data.len();
            if data_length.clone() > Value::from(0) {
                method.call(self, client.clone(), message.clone());
            };
        };
        Value::Undefined
    }

    fn handle_error_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        message: 'Invalid channel value ["ordersss"]',
        //        event: 'error'
        //    }
        //
        //    {
        //        "orderId": 0,
        //        "clientOrderId": null,
        //        "message": "Currency trade disabled",
        //        "code": 21352
        //    }
        //
        //    {
        //       "event": "error",
        //       "message": "Platform in maintenance mode"
        //    }
        //    {
        //       "id":"1722386782048",
        //       "data":[
        //          {
        //             "orderId":0,
        //             "clientOrderId":null,
        //             "message":"available insufficient",
        //             "code":21721
        //          }
        //       ]
        //    }
        //
        let mut id: Value = self.safe_string(message.clone(), Value::from("id"), Value::Undefined);
        let mut event: Value = self.safe_string(message.clone(), Value::from("event"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::Undefined);
        let mut first: Value = self.safe_dict(data.clone(), Value::from(0), Value::Undefined);
        let mut order_id: Value = self.safe_string(first.clone(), Value::from("orderId"), Value::Undefined);
        if event.clone() == Value::from("error") || order_id.clone() == Value::from("0") {
                        let mut error: Value = self.safe_string(first.clone(), Value::from("message"), Value::Undefined);
            let mut code: Value = self.safe_string(first.clone(), Value::from("code"), Value::Undefined);
            let mut feedback: Value = self.get("id".into()) + Value::from(" ") + self.json(message.clone());
            self.throw_exactly_matched_exception(self.get("exceptions".into()).get(Value::from("exact")), code.clone(), feedback.clone());
            self.throw_broadly_matched_exception(self.get("exceptions".into()).get(Value::from("broad")), error.clone(), feedback.clone());
            panic!(r###"ExchangeError::new(feedback)"###);
            // catch block omitted (no exception support in Value runtime)
;
        };
        return false.into();
    }

    fn handle_authenticate(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "success": true,
        //        "ret_msg": '',
        //        "op": "auth",
        //        "conn_id": "ce3dpomvha7dha97tvp0-2xh"
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut success: Value = self.safe_value(data.clone(), Value::from("success"), Value::Undefined);
        let mut message_hash: Value = Value::from("authenticated");
        if success.is_truthy() {
            client.resolve(message.clone(), message_hash.clone());
        } else {
            let mut error: Value = AuthenticationError::new(self.get("id".into()) + Value::from(" ") + self.json(message.clone()));
            client.reject(error.clone(), message_hash.clone());
            if client.get(subscriptions.clone()).contains_key(message_hash.clone()) {
                client.get(subscriptions.clone()).get(message_hash.clone());
            };
        };
        return message.clone();
    }

    fn ping(&mut self, mut client: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "event": "ping"
        }))).unwrap());
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetMarkets" => self.request("markets".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymbol" => self.request("markets/{symbol}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCurrencies" => self.request("currencies".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCurrenciescurrency" => self.request("currencies/{currency}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetV2currencies" => self.request("v2/currencies".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetV2currenciescurrency" => self.request("v2/currencies/{currency}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTimestamp" => self.request("timestamp".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketsprice" => self.request("markets/price".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymbolprice" => self.request("markets/{symbol}/price".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketsmarkprice" => self.request("markets/markPrice".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymbolmarkprice" => self.request("markets/{symbol}/markPrice".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymbolmarkpricecomponents" => self.request("markets/{symbol}/markPriceComponents".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymbolorderbook" => self.request("markets/{symbol}/orderBook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymbolcandles" => self.request("markets/{symbol}/candles".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymboltrades" => self.request("markets/{symbol}/trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketsticker24h" => self.request("markets/ticker24h".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketssymbolticker24h" => self.request("markets/{symbol}/ticker24h".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketscollateralinfo" => self.request("markets/collateralInfo".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketscurrencycollateralinfo" => self.request("markets/{currency}/collateralInfo".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketsborrowratesinfo" => self.request("markets/borrowRatesInfo".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccounts" => self.request("accounts".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountsbalances" => self.request("accounts/balances".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountsidbalances" => self.request("accounts/{id}/balances".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountsactivity" => self.request("accounts/activity".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountstransfer" => self.request("accounts/transfer".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountstransferid" => self.request("accounts/transfer/{id}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetFeeinfo" => self.request("feeinfo".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountsinteresthistory" => self.request("accounts/interest/history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubaccounts" => self.request("subaccounts".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubaccountsbalances" => self.request("subaccounts/balances".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubaccountsidbalances" => self.request("subaccounts/{id}/balances".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubaccountstransfer" => self.request("subaccounts/transfer".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubaccountstransferid" => self.request("subaccounts/transfer/{id}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWalletsaddresses" => self.request("wallets/addresses".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWalletsaddressescurrency" => self.request("wallets/addresses/{currency}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWalletsactivity" => self.request("wallets/activity".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetMarginaccountmargin" => self.request("margin/accountMargin".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetMarginborrowstatus" => self.request("margin/borrowStatus".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetMarginmaxsize" => self.request("margin/maxSize".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrders" => self.request("orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrdersid" => self.request("orders/{id}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrderskillswitchstatus" => self.request("orders/killSwitchStatus".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSmartorders" => self.request("smartorders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSmartordersid" => self.request("smartorders/{id}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrdershistory" => self.request("orders/history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSmartordershistory" => self.request("smartorders/history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTrades" => self.request("trades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrdersidtrades" => self.request("orders/{id}/trades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountstransfer" => self.request("accounts/transfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostSubaccountstransfer" => self.request("subaccounts/transfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWalletsaddress" => self.request("wallets/address".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWalletswithdraw" => self.request("wallets/withdraw".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostV2walletswithdraw" => self.request("v2/wallets/withdraw".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrders" => self.request("orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdersbatch" => self.request("orders/batch".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrderskillswitch" => self.request("orders/killSwitch".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostSmartorders" => self.request("smartorders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteOrdersid" => self.request("orders/{id}".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteOrderscancelbyids" => self.request("orders/cancelByIds".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteOrders" => self.request("orders".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteSmartordersid" => self.request("smartorders/{id}".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteSmartorderscancelbyids" => self.request("smartorders/cancelByIds".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteSmartorders" => self.request("smartorders".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePutOrdersid" => self.request("orders/{id}".into(), "private".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePutSmartordersid" => self.request("smartorders/{id}".into(), "private".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketallinstruments" => self.request("v3/market/allInstruments".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketinstruments" => self.request("v3/market/instruments".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketorderbook" => self.request("v3/market/orderBook".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketcandles" => self.request("v3/market/candles".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketindexpricecandlesticks" => self.request("v3/market/indexPriceCandlesticks".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketpremiumindexcandlesticks" => self.request("v3/market/premiumIndexCandlesticks".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketmarkpricecandlesticks" => self.request("v3/market/markPriceCandlesticks".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3markettrades" => self.request("v3/market/trades".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketliquidationorder" => self.request("v3/market/liquidationOrder".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3markettickers" => self.request("v3/market/tickers".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketmarkprice" => self.request("v3/market/markPrice".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketindexprice" => self.request("v3/market/indexPrice".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketindexpricecomponents" => self.request("v3/market/indexPriceComponents".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketfundingrate" => self.request("v3/market/fundingRate".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketopeninterest" => self.request("v3/market/openInterest".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketinsurance" => self.request("v3/market/insurance".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swappublicGetV3marketrisklimit" => self.request("v3/market/riskLimit".into(), "swapPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3accountbalance" => self.request("v3/account/balance".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3accountbills" => self.request("v3/account/bills".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3tradeorderopens" => self.request("v3/trade/order/opens".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3tradeordertrades" => self.request("v3/trade/order/trades".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3tradeorderhistory" => self.request("v3/trade/order/history".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3tradepositionopens" => self.request("v3/trade/position/opens".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3tradepositionhistory" => self.request("v3/trade/position/history".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3positionleverages" => self.request("v3/position/leverages".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateGetV3positionmode" => self.request("v3/position/mode".into(), "swapPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivatePostV3tradeorder" => self.request("v3/trade/order".into(), "swapPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivatePostV3tradeorders" => self.request("v3/trade/orders".into(), "swapPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivatePostV3tradeposition" => self.request("v3/trade/position".into(), "swapPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivatePostV3tradepositionall" => self.request("v3/trade/positionAll".into(), "swapPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivatePostV3positionleverage" => self.request("v3/position/leverage".into(), "swapPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivatePostV3positionmode" => self.request("v3/position/mode".into(), "swapPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivatePostV3tradepositionmargin" => self.request("v3/trade/position/margin".into(), "swapPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateDeleteV3tradeorder" => self.request("v3/trade/order".into(), "swapPrivate".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateDeleteV3tradebatchorders" => self.request("v3/trade/batchOrders".into(), "swapPrivate".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "swapprivateDeleteV3tradeallorders" => self.request("v3/trade/allOrders".into(), "swapPrivate".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct PoloniexImpl(Value);
impl Exchange for PoloniexImpl {}
impl Poloniex for PoloniexImpl {}
impl ValueTrait for PoloniexImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl PoloniexImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = PoloniexImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Poloniex::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


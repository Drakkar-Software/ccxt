#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256") }
fn sha384() -> Value { Value::from("sha384") }
fn sha512() -> Value { Value::from("sha512") }
fn md5() -> Value { Value::from("md5") }
fn ed25519() -> Value { Value::from("ed25519") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1") }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Defx : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "defx",
            "name": "Defx X",
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": true,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "sandbox": true,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": true,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "closeAllPositions": true,
                "closePosition": true,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": false,
                "createMarketOrder": false,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrderWithTakeProfitAndStopLoss": true,
                "createReduceOnlyOrder": true,
                "createStopLimitOrder": false,
                "createStopLossOrder": false,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "createTakeProfitOrder": true,
                "createTrailingAmountOrder": false,
                "createTrailingPercentOrder": false,
                "createTriggerOrder": true,
                "editOrder": "emulated",
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchCanceledOrders": true,
                "fetchClosedOrder": false,
                "fetchClosedOrders": true,
                "fetchConvertCurrencies": false,
                "fetchConvertQuote": false,
                "fetchConvertTrade": false,
                "fetchConvertTradeHistory": false,
                "fetchCurrencies": false,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": false,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": false,
                "fetchDepositsWithdrawals": false,
                "fetchFundingHistory": false,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": false,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchFundingRates": false,
                "fetchIndexOHLCV": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLeverage": false,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrder": false,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchOrderTrades": false,
                "fetchPosition": true,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchMarkPrices": false,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": false,
                "fetchTransactions": false,
                "fetchTransfers": false,
                "fetchWithdrawals": false,
                "reduceMargin": false,
                "setLeverage": true,
                "setMargin": false,
                "setPositionMode": false,
                "transfer": false,
                "watchBalance": true,
                "watchMyTrades": false,
                "watchOHLCV": true,
                "watchOHLCVForSymbols": true,
                "watchOrderBook": true,
                "watchBidsAsks": true,
                "watchOrderBookForSymbols": true,
                "watchOrders": true,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": true,
                "withdraw": true,
                "ws": true,
                "cancelAllOrdersAfter": false,
                "cancelWithdraw": false,
                "createConvertTrade": false,
                "fetchMarkPrice": false
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/4e92bace-d7a9-45ea-92be-122168dc87e4",
                "api": {
                    "public": "https://api.{hostname}",
                    "private": "https://api.{hostname}",
                    "ws": {
                        "public": "wss://marketfeed.api.defx.com/pricefeed",
                        "private": "wss://userfeed.api.defx.com/user"
                    }
                },
                "www": "https://defx.com/home",
                "doc": [
                    "https://docs.defx.com/docs",
                    "https://api-docs.defx.com/"
                ],
                "fees": [
                    ""
                ],
                "test": {
                    "public": "https://api.testnet.{hostname}",
                    "private": "https://api.testnet.{hostname}",
                    "ws": {
                        "public": "wss://stream.testnet.defx.com/pricefeed",
                        "private": "wss://ws.testnet.defx.com/user"
                    }
                },
                "referral": {
                    "url": "https://app.defx.com/join/6I2CZ7"
                }
            },
            "api": {
                "v1": {
                    "public": {
                        "get": {
                            "healthcheck/ping": 1,
                            "symbols/{symbol}/ohlc": 1,
                            "symbols/{symbol}/trades": 1,
                            "symbols/{symbol}/prices": 1,
                            "symbols/{symbol}/ticker/24hr": 1,
                            "symbols/{symbol}/depth/{level}/{slab}": 1,
                            "ticker/24HrAgg": 1,
                            "c/markets": 1,
                            "c/markets/metadata": 1,
                            "analytics/market/stats/newUsers": 1,
                            "analytics/market/stats/tvl": 1,
                            "analytics/market/stats/volumeByInstrument": 1,
                            "analytics/market/stats/liquidation": 1,
                            "analytics/market/stats/totalVolume": 1,
                            "analytics/market/stats/openInterest": 1,
                            "analytics/market/stats/totalTrades": 1,
                            "analytics/market/stats/basis": 1,
                            "analytics/market/stats/insuranceFund": 1,
                            "analytics/market/stats/longAndShortRatio": 1,
                            "analytics/market/stats/fundingRate": 1,
                            "analytics/market/overview": 1,
                            "explorer/search": 1,
                            "explorer/transactions": 1,
                            "explorer/blocks": 1
                        }
                    },
                    "private": {
                        "get": {
                            "api/order/{orderId}": 1,
                            "api/orders": 1,
                            "api/orders/oco/{parentOrderId}": 1,
                            "api/trades": 1,
                            "api/position/active": 1,
                            "api/users/metadata/leverage": 1,
                            "api/users/metadata/feeMultiplier": 1,
                            "api/users/metadata/slippage": 1,
                            "api/users/referral": 1,
                            "api/users/apikeys": 1,
                            "connection-signature-message/evm": 1,
                            "api/users/profile/wallets": 1,
                            "api/notifications": 1,
                            "api/wallet/balance": 1,
                            "api/wallet/transactions": 1,
                            "api/analytics/user/overview": 1,
                            "api/analytics/user/pnl": 1,
                            "api/analytics/points/overview": 1,
                            "api/analytics/points/history": 1
                        },
                        "post": {
                            "api/order": 1,
                            "api/position/oco": 1,
                            "api/users/socket/listenKeys": 1,
                            "api/users/metadata/leverage": 1,
                            "api/users/metadata/feeMultiplier": 1,
                            "api/users/metadata/slippage": 1,
                            "api/users/referral/recordReferralSignup": 1,
                            "api/users/apikeys": 1,
                            "api/users/profile/wallets": 1,
                            "api/transfers/withdrawal": 1,
                            "api/transfers/bridge/withdrawal": 1
                        },
                        "put": {
                            "api/position/updatePositionMargin": 1,
                            "api/users/socket/listenKeys/{listenKey}": 1,
                            "api/users/apikeys/{accessKey}/status": 1,
                            "api/users/referral": 1
                        },
                        "patch": {
                            "api/users/apikeys/{accessKey}": 1
                        },
                        "delete": {
                            "api/orders/allOpen": 1,
                            "api/order/{orderId}": 1,
                            "api/position/{positionId}": 1,
                            "api/position/all": 1,
                            "api/users/socket/listenKeys/{listenKey}": 1,
                            "api/users/apikeys/{accessKey}": 1
                        }
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "3m": "3m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1h",
                "2h": "2h",
                "4h": "4h",
                "12h": "12h",
                "1d": "1d",
                "1w": "1w",
                "1M": "1M"
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.0005,
                    "maker": 0.0002
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "hostname": "defx.com",
            "options": {
                "sandboxMode": false,
                "listenKeyRefreshRate": 3540000,
                "ws": {
                    "timeframes": {
                        "1m": "1m",
                        "3m": "3m",
                        "5m": "5m",
                        "15m": "15m",
                        "30m": "30m",
                        "1h": "1h",
                        "2h": "2h",
                        "4h": "4h",
                        "12h": "12h",
                        "1d": "1d",
                        "1w": "1w",
                        "1M": "1M"
                    }
                }
            },
            "features": {
                "forDerivatives": {
                    "sandbox": true,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerPriceType": {
                            "last": true,
                            "mark": true,
                            "index": false
                        },
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "selfTradePrevention": false,
                        "trailing": false,
                        "iceberg": false,
                        "leverage": false,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 1000,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": true,
                        "limit": 100,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "daysBack": 100000,
                        "daysBackCanceled": 1,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forDerivatives"
                    }
                },
                "future": {}
            },
            "streaming": {}
        }"###).unwrap())
    }

    async fn watch_public(&mut self, mut topics: Value, mut message_hashes: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "SUBSCRIBE",
            "topics": topics
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return self.watch_multiple(url.clone(), message_hashes.clone(), message.clone(), message_hashes.clone(), Value::Undefined).await;
    }

    async fn un_watch_public(&mut self, mut topics: Value, mut message_hashes: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "UNSUBSCRIBE",
            "topics": topics
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return self.watch_multiple(url.clone(), message_hashes.clone(), message.clone(), message_hashes.clone(), Value::Undefined).await;
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        let mut result: Value = <Self as Defx>::watch_ohlcv_for_symbols(self, Value::Json(serde_json::Value::Array(vec![Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into()])).into()])), since.clone(), limit.clone(), params.clone()).await;
        return result.get(symbol.clone()).get(timeframe.clone());
    }

    async fn un_watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        return <Self as Defx>::un_watch_ohlcv_for_symbols(self, Value::Json(serde_json::Value::Array(vec![Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into()])).into()])), params.clone()).await;
    }

    async fn watch_ohlcv_for_symbols(&mut self, mut symbols_and_timeframes: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut symbols_length: usize = symbols_and_timeframes.len();
        if symbols_length.clone() == Value::from(0) || !Array::is_array(symbols_and_timeframes.get(Value::from(0))).is_truthy() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" watchOHLCVForSymbols() requires a an array of symbols and timeframes, like  [['BTC/USDT', '1m'], ['LTC/USDT', '5m']]"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols_and_timeframes.len() {
            let mut symbol_and_timeframe: Value = symbols_and_timeframes.get(i.into());
            let mut market_id: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(0), Value::Undefined);
            let mut market: Value = self.market(market_id.clone());
            let mut tf: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(1), Value::Undefined);
            let mut interval: Value = self.safe_string(self.get("timeframes".into()), tf.clone(), tf.clone());
            topics.push(Value::from("symbol:") + market.get(Value::from("id")) + Value::from(":ohlc:") + interval.clone());
            message_hashes.push(Value::from("candles:") + interval.clone() + Value::from(":") + market.get(Value::from("symbol")));
            i += 1;
        };
        let (mut symbol, mut timeframe, mut candles) = shift_3(<Self as Defx>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await);
        if self.get("newUpdates".into()).is_truthy() {
            limit = candles.get_limit(symbol.clone(), limit.clone());
        };
        let mut filtered: Value = self.filter_by_since_limit(candles.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
        return self.create_ohlcv_object(symbol.clone(), timeframe.clone(), filtered.clone());
    }

    async fn un_watch_ohlcv_for_symbols(&mut self, mut symbols_and_timeframes: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut symbols_length: usize = symbols_and_timeframes.len();
        if symbols_length.clone() == Value::from(0) || !Array::is_array(symbols_and_timeframes.get(Value::from(0))).is_truthy() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" unWatchOHLCVForSymbols() requires a an array of symbols and timeframes, like  [['BTC/USDT', '1m'], ['LTC/USDT', '5m']]"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols_and_timeframes.len() {
            let mut symbol_and_timeframe: Value = symbols_and_timeframes.get(i.into());
            let mut market_id: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(0), Value::Undefined);
            let mut market: Value = self.market(market_id.clone());
            let mut tf: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(1), Value::Undefined);
            let mut interval: Value = self.safe_string(self.get("timeframes".into()), tf.clone(), tf.clone());
            topics.push(Value::from("symbol:") + market.get(Value::from("id")) + Value::from(":ohlc:") + interval.clone());
            message_hashes.push(Value::from("candles:") + interval.clone() + Value::from(":") + market.get(Value::from("symbol")));
            i += 1;
        };
        return <Self as Defx>::un_watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "topic": "symbol:BTC_USDC:ohlc:3m",
        //     "event": "ohlc",
        //     "timestamp": 1730794277104,
        //     "data": {
        //         "symbol": "BTC_USDC",
        //         "window": "3m",
        //         "open": "57486.90000000",
        //         "high": "57486.90000000",
        //         "low": "57486.90000000",
        //         "close": "57486.90000000",
        //         "volume": "0.000",
        //         "quoteAssetVolume": "0.00000000",
        //         "takerBuyAssetVolume": "0.000",
        //         "takerBuyQuoteAssetVolume": "0.00000000",
        //         "numberOfTrades": 0,
        //         "start": 1730794140000,
        //         "end": 1730794320000,
        //         "isClosed": false
        //     }
        // }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.market(market_id.clone());
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut timeframe: Value = self.safe_string(data.clone(), Value::from("window"), Value::Undefined);
        if !self.get("ohlcvs".into()).contains_key(symbol.clone()) {
            self.get("ohlcvs".into()).set(symbol.clone(), Value::new_object());
        };
        if !self.get("ohlcvs".into()).get(symbol.clone()).contains_key(timeframe.clone()) {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            let mut stored: Value = ArrayCacheByTimestamp::new(limit);
            self.get("ohlcvs".into()).get(symbol.clone()).set(timeframe.clone(), stored.clone());
        };
        let mut ohlcv: Value = self.get("ohlcvs".into()).get(symbol.clone()).get(timeframe.clone());
        let mut parsed: Value = self.parse_ohlcv(data.clone(), Value::Undefined);
        ohlcv.append(parsed.clone());
        let mut message_hash: Value = Value::from("candles:") + timeframe.clone() + Value::from(":") + symbol.clone();
        client.resolve(Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into(), ohlcv.clone().into()])), message_hash.clone());
        Value::Undefined
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut topic: Value = Value::from("symbol:") + market.get(Value::from("id")) + Value::from(":24hrTicker");
        let mut message_hash: Value = Value::from("ticker:") + symbol.clone();
        return <Self as Defx>::watch_public(self, Value::Json(serde_json::Value::Array(vec![topic.clone().into()])), Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()])), params.clone()).await;
    }

    async fn un_watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Defx>::un_watch_tickers(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("symbol:") + market_id.clone() + Value::from(":24hrTicker"));
            message_hashes.push(Value::from("ticker:") + symbol.clone());
            i += 1;
        };
        <Self as Defx>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
        return self.filter_by_array(self.get("tickers".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    async fn un_watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("symbol:") + market_id.clone() + Value::from(":24hrTicker"));
            message_hashes.push(Value::from("ticker:") + symbol.clone());
            i += 1;
        };
        return <Self as Defx>::un_watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "topic": "symbol:BTC_USDC:24hrTicker",
        //     "event": "24hrTicker",
        //     "timestamp": 1730862543095,
        //     "data": {
        //         "symbol": "BTC_USDC",
        //         "priceChange": "17114.70000000",
        //         "priceChangePercent": "29.77",
        //         "weightedAvgPrice": "6853147668",
        //         "lastPrice": "74378.90000000",
        //         "lastQty": "0.107",
        //         "bestBidPrice": "61987.60000000",
        //         "bestBidQty": "0.005",
        //         "bestAskPrice": "84221.60000000",
        //         "bestAskQty": "0.015",
        //         "openPrice": "57486.90000000",
        //         "highPrice": "88942.60000000",
        //         "lowPrice": "47364.20000000",
        //         "volume": "28.980",
        //         "quoteVolume": "1986042.19424035",
        //         "openTime": 1730776080000,
        //         "closeTime": 1730862540000,
        //         "openInterestBase": "67.130",
        //         "openInterestQuote": "5008005.40800000"
        //     }
        // }
        //
        <Self as Defx>::handle_bid_ask(self, client.clone(), message.clone());
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut parsed_ticker: Value = self.parse_ticker(data.clone(), Value::Undefined);
        let mut symbol: Value = parsed_ticker.get(Value::from("symbol"));
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("timestamp"), Value::Undefined);
        parsed_ticker.set("timestamp".into(), timestamp.clone());
        parsed_ticker.set("datetime".into(), self.iso8601(timestamp.clone()));
        self.get("tickers".into()).set(symbol.clone(), parsed_ticker.clone());
        let mut message_hash: Value = Value::from("ticker:") + symbol.clone();
        client.resolve(parsed_ticker.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("symbol:") + market_id.clone() + Value::from(":24hrTicker"));
            message_hashes.push(Value::from("bidask:") + symbol.clone());
            i += 1;
        };
        <Self as Defx>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
        return self.filter_by_array(self.get("bidsasks".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    fn handle_bid_ask(&mut self, mut client: Value, mut message: Value) -> Value {
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut parsed_ticker: Value = <Self as Defx>::parse_ws_bid_ask(self, data.clone());
        let mut symbol: Value = parsed_ticker.get(Value::from("symbol"));
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("timestamp"), Value::Undefined);
        parsed_ticker.set("timestamp".into(), timestamp.clone());
        parsed_ticker.set("datetime".into(), self.iso8601(timestamp.clone()));
        self.get("bidsasks".into()).set(symbol.clone(), parsed_ticker.clone());
        let mut message_hash: Value = Value::from("bidask:") + symbol.clone();
        client.resolve(parsed_ticker.clone(), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_bid_ask(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "ask": self.safe_string(ticker.clone(), Value::from("bestAskPrice"), Value::Undefined),
            "askVolume": self.safe_string(ticker.clone(), Value::from("bestAskQty"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("bestBidPrice"), Value::Undefined),
            "bidVolume": self.safe_string(ticker.clone(), Value::from("bestBidQty"), Value::Undefined),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Defx>::watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn un_watch_trades(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Defx>::un_watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
    }

    async fn watch_trades_for_symbols(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbols_length: usize = symbols.len();
        if symbols_length.clone() == Value::from(0) {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" watchTradesForSymbols() requires a non-empty array of symbols"))"###);
        };
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("symbol:") + market_id.clone() + Value::from(":trades"));
            message_hashes.push(Value::from("trade:") + symbol.clone());
            i += 1;
        };
        let mut trades: Value = <Self as Defx>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut first: Value = self.safe_value(trades.clone(), Value::from(0), Value::Undefined);
            let mut trade_symbol: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
            limit = trades.get_limit(trade_symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    async fn un_watch_trades_for_symbols(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbols_length: usize = symbols.len();
        if symbols_length.clone() == Value::from(0) {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" unWatchTradesForSymbols() requires a non-empty array of symbols"))"###);
        };
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("symbol:") + market_id.clone() + Value::from(":trades"));
            message_hashes.push(Value::from("trade:") + symbol.clone());
            i += 1;
        };
        return <Self as Defx>::un_watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "topic": "symbol:SOL_USDC:trades",
        //     "event": "trades",
        //     "timestamp": 1730967426331,
        //     "data": {
        //         "buyerMaker": true,
        //         "price": "188.38700000",
        //         "qty": "1.00",
        //         "symbol": "SOL_USDC",
        //         "timestamp": 1730967426328
        //     }
        // }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut parsed_trade: Value = self.parse_trade(data.clone(), Value::Undefined);
        let mut symbol: Value = parsed_trade.get(Value::from("symbol"));
        if !self.get("trades".into()).contains_key(symbol.clone()) {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            let mut stored: Value = ArrayCache::new(limit);
            self.get("trades".into()).set(symbol.clone(), stored.clone());
        };
        let mut trades: Value = self.get("trades".into()).get(symbol.clone());
        trades.append(parsed_trade.clone());
        let mut message_hash: Value = Value::from("trade:") + symbol.clone();
        client.resolve(trades.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Defx>::watch_order_book_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), limit.clone(), params.clone()).await;
    }

    async fn un_watch_order_book(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Defx>::un_watch_order_book_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
    }

    async fn watch_order_book_for_symbols(&mut self, mut symbols: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut symbols_length: usize = symbols.len();
        if symbols_length.clone() == Value::from(0) {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" watchOrderBookForSymbols() requires a non-empty array of symbols"))"###);
        };
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("symbol:") + market_id.clone() + Value::from(":depth:20:0.001"));
            message_hashes.push(Value::from("orderbook:") + symbol.clone());
            i += 1;
        };
        let mut orderbook: Value = <Self as Defx>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
        return orderbook.limit();
    }

    async fn un_watch_order_book_for_symbols(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut symbols_length: usize = symbols.len();
        if symbols_length.clone() == Value::from(0) {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" unWatchOrderBookForSymbols() requires a non-empty array of symbols"))"###);
        };
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("symbol:") + market_id.clone() + Value::from(":depth:20:0.001"));
            message_hashes.push(Value::from("orderbook:") + symbol.clone());
            i += 1;
        };
        return <Self as Defx>::un_watch_public(self, topics.clone(), message_hashes.clone(), params.clone()).await;
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "topic": "symbol:SOL_USDC:depth:20:0.01",
        //     "event": "depth",
        //     "timestamp": 1731030695319,
        //     "data": {
        //         "symbol": "SOL_USDC",
        //         "timestamp": 1731030695319,
        //         "lastTradeTimestamp": 1731030275258,
        //         "level": "20",
        //         "slab": "0.01",
        //         "bids": [
        //             {
        //                 "price": "198.27000000",
        //                 "qty": "1.52"
        //             }
        //         ],
        //         "asks": [
        //             {
        //                 "price": "198.44000000",
        //                 "qty": "6.61"
        //             }
        //         ]
        //     }
        // }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.market(market_id.clone());
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut timestamp: Value = self.safe_integer(data.clone(), Value::from("timestamp"), Value::Undefined);
        let mut snapshot: Value = self.parse_order_book(data.clone(), symbol.clone(), timestamp.clone(), Value::from("bids"), Value::from("asks"));
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            let mut ob: Value = self.order_book(snapshot.clone(), Value::Undefined);
            self.get("orderbooks".into()).set(symbol.clone(), ob.clone());
        };
        let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
        orderbook.reset(snapshot.clone());
        let mut message_hash: Value = Value::from("orderbook:") + symbol.clone();
        client.resolve(orderbook.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn keep_alive_listen_key(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut listen_key: Value = self.safe_string(self.get("options".into()), Value::from("listenKey"), Value::Undefined);
        if listen_key.clone().is_nullish() {
            // A network error happened: we can't renew a listen key that does not exist.
            return Value::Undefined;
        };
                self.dispatch("v1PrivatePutApiUsersSocketListenKeysListenKey".into(), Value::Json(normalize(&Value::Json(json!({
            "listenKey": listen_key
        }))).unwrap()), Value::Undefined).await;
        // catch block omitted (no exception support in Value runtime)
;
        // extend the expiry
        // whether or not to schedule another listenKey keepAlive request
        let mut listen_key_refresh_rate: Value = self.safe_integer(self.get("options".into()), Value::from("listenKeyRefreshRate"), Value::from(3540000));
        self.delay(listen_key_refresh_rate.clone(), self.get("keepAliveListenKey".into()), params.clone());
        Value::Undefined
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut time: Value = self.milliseconds();
        let mut last_authenticated_time: Value = self.safe_integer(self.get("options".into()), Value::from("lastAuthenticatedTime"), Value::from(0));
        let mut listen_key_refresh_rate: Value = self.safe_integer(self.get("options".into()), Value::from("listenKeyRefreshRate"), Value::from(3540000));
        // 1 hour
        if time.clone() - last_authenticated_time.clone() > listen_key_refresh_rate.clone() {
            let mut response: Value = self.dispatch("v1PrivatePostApiUsersSocketListenKeys".into(), Value::Undefined, Value::Undefined).await;
            self.get("options".into()).set("listenKey".into(), self.safe_string(response.clone(), Value::from("listenKey"), Value::Undefined));
            self.get("options".into()).set("lastAuthenticatedTime".into(), time.clone());
            self.delay(listen_key_refresh_rate.clone(), self.get("keepAliveListenKey".into()), params.clone());
        };
        Value::Undefined
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        <Self as Defx>::authenticate(self).await;
        let mut base_url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut message_hash: Value = Value::from("WALLET_BALANCE_UPDATE");
        let mut url: Value = base_url.clone() + Value::from("?listenKey=") + self.get("options".into()).get(Value::from("listenKey"));
        return self.watch(url.clone(), message_hash.clone(), Value::Undefined, message_hash.clone(), Value::Undefined).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "event": "WALLET_BALANCE_UPDATE",
        //     "timestamp": 1711015961397,
        //     "data": {
        //         "asset": "USDC", "balance": "27.64712963"
        //     }
        // }
        //
        let mut message_hash: Value = self.safe_string(message.clone(), Value::from("event"), Value::Undefined);
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_array());
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("timestamp"), Value::Undefined);
        if self.get("balance".into()).is_nullish() {
            self.set("balance".into(), Value::new_object());
        };
        self.get("balance".into()).set("info".into(), data.clone());
        self.get("balance".into()).set("timestamp".into(), timestamp.clone());
        self.get("balance".into()).set("datetime".into(), self.iso8601(timestamp.clone()));
        let mut currency_id: Value = self.safe_string(data.clone(), Value::from("asset"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
        let mut account: Value = if self.get("balance".into()).contains_key(code.clone()) { self.get("balance".into()).get(code.clone()) } else { self.account() };
        account.set("free".into(), self.safe_string(data.clone(), Value::from("balance"), Value::Undefined));
        self.get("balance".into()).set(code.clone(), account.clone());
        self.set("balance".into(), self.safe_balance(self.get("balance".into())));
        client.resolve(self.get("balance".into()), message_hash.clone());
        Value::Undefined
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        <Self as Defx>::authenticate(self).await;
        let mut base_url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut message_hash: Value = Value::from("orders");
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            message_hash = message_hash +  Value::from(":") + market.get(Value::from("symbol"));
        };
        let mut url: Value = base_url.clone() + Value::from("?listenKey=") + self.get("options".into()).get(Value::from("listenKey"));
        let mut orders: Value = self.watch(url.clone(), message_hash.clone(), Value::Undefined, message_hash.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_order(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "event": "ORDER_UPDATE",
        //     "timestamp": 1731417961446,
        //     "data": {
        //         "orderId": "766738557656630928",
        //         "symbol": "SOL_USDC",
        //         "side": "SELL",
        //         "type": "MARKET",
        //         "status": "FILLED",
        //         "clientOrderId": "0193208d-717b-7811-a80e-c036e220ad9b",
        //         "reduceOnly": false,
        //         "postOnly": false,
        //         "timeInForce": "GTC",
        //         "isTriggered": false,
        //         "createdAt": "2024-11-12T13:26:00.829Z",
        //         "updatedAt": "2024-11-12T13:26:01.436Z",
        //         "avgPrice": "209.60000000",
        //         "cumulativeQuote": "104.80000000",
        //         "totalFee": "0.05764000",
        //         "executedQty": "0.50",
        //         "origQty": "0.50",
        //         "role": "TAKER",
        //         "pnl": "0.00000000",
        //         "lastFillPnL": "0.00000000",
        //         "lastFillPrice": "209.60000000",
        //         "lastFillQty": "0.50",
        //         "linkedOrderParentType": null,
        //         "workingType": null
        //     }
        // }
        //
        let mut channel: Value = Value::from("orders");
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        if self.get("orders".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
            self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
        };
        let mut orders: Value = self.get("orders".into());
        let mut parsed_order: Value = self.parse_order(data.clone(), Value::Undefined);
        orders.append(parsed_order.clone());
        let mut message_hash: Value = channel.clone() + Value::from(":") + parsed_order.get(Value::from("symbol"));
        client.resolve(orders.clone(), channel.clone());
        client.resolve(orders.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_positions(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        <Self as Defx>::authenticate(self).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut base_url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut channel: Value = Value::from("positions");
        let mut url: Value = base_url.clone() + Value::from("?listenKey=") + self.get("options".into()).get(Value::from("listenKey"));
        let mut new_position: Value = Value::Undefined;
        if symbols.clone().is_nonnullish() {
            let mut message_hashes: Value = Value::new_array();
            let mut i: usize = 0;
            while i < symbols.len() {
                let mut symbol: Value = symbols.get(i.into());
                message_hashes.push(channel.clone() + Value::from(":") + symbol.clone());
                i += 1;
            };
            new_position = self.watch_multiple(url.clone(), message_hashes.clone(), Value::Undefined, message_hashes.clone(), Value::Undefined).await;
        } else {
            new_position = self.watch(url.clone(), channel.clone(), Value::Undefined, channel.clone(), Value::Undefined).await;
        };
        if self.get("newUpdates".into()).is_truthy() {
            return new_position.clone();
        };
        return self.filter_by_symbols_since_limit(self.get("positions".into()), symbols.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_positions(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "event": "POSITION_UPDATE",
        //     "timestamp": 1731417961456,
        //     "data": {
        //         "positionId": "0193208d-735d-7fe9-90bd-8bc6d6bc1eda",
        //         "createdAt": 1289847904328,
        //         "symbol": "SOL_USDC",
        //         "positionSide": "SHORT",
        //         "entryPrice": "209.60000000",
        //         "quantity": "0.50",
        //         "status": "ACTIVE",
        //         "marginAsset": "USDC",
        //         "marginAmount": "15.17475649",
        //         "realizedPnL": "0.00000000"
        //     }
        // }
        //
        let mut channel: Value = Value::from("positions");
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        if self.get("positions".into()).is_nullish() {
            self.set("positions".into(), ArrayCacheBySymbolById::new());
        };
        let mut cache: Value = self.get("positions".into());
        let mut parsed_position: Value = self.parse_position(data.clone(), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("timestamp"), Value::Undefined);
        parsed_position.set("timestamp".into(), timestamp.clone());
        parsed_position.set("datetime".into(), self.iso8601(timestamp.clone()));
        cache.append(parsed_position.clone());
        let mut message_hash: Value = channel.clone() + Value::from(":") + parsed_position.get(Value::from("symbol"));
        client.resolve(Value::Json(serde_json::Value::Array(vec![parsed_position.clone().into()])), channel.clone());
        client.resolve(Value::Json(serde_json::Value::Array(vec![parsed_position.clone().into()])), message_hash.clone());
        Value::Undefined
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        let mut error: Value = self.safe_string(message.clone(), Value::from("code"), Value::Undefined);
        if error.clone().is_nonnullish() {
            let mut error_msg: Value = self.safe_string(message.clone(), Value::from("msg"), Value::Undefined);
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" ") + error_msg.clone())"###);
        };
        let mut event: Value = self.safe_string(message.clone(), Value::from("event"), Value::Undefined);
        if event.clone().is_nonnullish() {
            let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
                "ohlc": self.get("handleOHLCV".into()),
                "24hrTicker": self.get("handleTicker".into()),
                "trades": self.get("handleTrades".into()),
                "depth": self.get("handleOrderBook".into()),
                "WALLET_BALANCE_UPDATE": self.get("handleBalance".into()),
                "ORDER_UPDATE": self.get("handleOrder".into()),
                "POSITION_UPDATE": self.get("handlePositions".into())
            }))).unwrap());
            let mut exac_method: Value = self.safe_value(methods.clone(), event.clone(), Value::Undefined);
            if exac_method.clone().is_nonnullish() {
                exac_method.call(self, client.clone(), message.clone());
            };
        };
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "v1PublicGetHealthcheckping" => Defx::request(self, "healthcheck/ping".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymbolohlc" => Defx::request(self, "symbols/{symbol}/ohlc".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymboltrades" => Defx::request(self, "symbols/{symbol}/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymbolprices" => Defx::request(self, "symbols/{symbol}/prices".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymbolticker24hr" => Defx::request(self, "symbols/{symbol}/ticker/24hr".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymboldepthlevelslab" => Defx::request(self, "symbols/{symbol}/depth/{level}/{slab}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetTicker24hragg" => Defx::request(self, "ticker/24HrAgg".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetCmarkets" => Defx::request(self, "c/markets".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetCmarketsmetadata" => Defx::request(self, "c/markets/metadata".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsnewusers" => Defx::request(self, "analytics/market/stats/newUsers".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatstvl" => Defx::request(self, "analytics/market/stats/tvl".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsvolumebyinstrument" => Defx::request(self, "analytics/market/stats/volumeByInstrument".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsliquidation" => Defx::request(self, "analytics/market/stats/liquidation".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatstotalvolume" => Defx::request(self, "analytics/market/stats/totalVolume".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsopeninterest" => Defx::request(self, "analytics/market/stats/openInterest".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatstotaltrades" => Defx::request(self, "analytics/market/stats/totalTrades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsbasis" => Defx::request(self, "analytics/market/stats/basis".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsinsurancefund" => Defx::request(self, "analytics/market/stats/insuranceFund".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatslongandshortratio" => Defx::request(self, "analytics/market/stats/longAndShortRatio".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsfundingrate" => Defx::request(self, "analytics/market/stats/fundingRate".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketoverview" => Defx::request(self, "analytics/market/overview".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetExplorersearch" => Defx::request(self, "explorer/search".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetExplorertransactions" => Defx::request(self, "explorer/transactions".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetExplorerblocks" => Defx::request(self, "explorer/blocks".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiorderorderid" => Defx::request(self, "api/order/{orderId}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiorders" => Defx::request(self, "api/orders".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiordersocoparentorderid" => Defx::request(self, "api/orders/oco/{parentOrderId}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApitrades" => Defx::request(self, "api/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApipositionactive" => Defx::request(self, "api/position/active".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersmetadataleverage" => Defx::request(self, "api/users/metadata/leverage".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersmetadatafeemultiplier" => Defx::request(self, "api/users/metadata/feeMultiplier".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersmetadataslippage" => Defx::request(self, "api/users/metadata/slippage".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersreferral" => Defx::request(self, "api/users/referral".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersapikeys" => Defx::request(self, "api/users/apikeys".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetConnectionsignaturemessageevm" => Defx::request(self, "connection-signature-message/evm".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersprofilewallets" => Defx::request(self, "api/users/profile/wallets".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApinotifications" => Defx::request(self, "api/notifications".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiwalletbalance" => Defx::request(self, "api/wallet/balance".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiwallettransactions" => Defx::request(self, "api/wallet/transactions".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticsuseroverview" => Defx::request(self, "api/analytics/user/overview".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticsuserpnl" => Defx::request(self, "api/analytics/user/pnl".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticspointsoverview" => Defx::request(self, "api/analytics/points/overview".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticspointshistory" => Defx::request(self, "api/analytics/points/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiorder" => Defx::request(self, "api/order".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApipositionoco" => Defx::request(self, "api/position/oco".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiuserssocketlistenkeys" => Defx::request(self, "api/users/socket/listenKeys".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersmetadataleverage" => Defx::request(self, "api/users/metadata/leverage".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersmetadatafeemultiplier" => Defx::request(self, "api/users/metadata/feeMultiplier".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersmetadataslippage" => Defx::request(self, "api/users/metadata/slippage".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersreferralrecordreferralsignup" => Defx::request(self, "api/users/referral/recordReferralSignup".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersapikeys" => Defx::request(self, "api/users/apikeys".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersprofilewallets" => Defx::request(self, "api/users/profile/wallets".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApitransferswithdrawal" => Defx::request(self, "api/transfers/withdrawal".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApitransfersbridgewithdrawal" => Defx::request(self, "api/transfers/bridge/withdrawal".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApipositionupdatepositionmargin" => Defx::request(self, "api/position/updatePositionMargin".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApiuserssocketlistenkeyslistenkey" => Defx::request(self, "api/users/socket/listenKeys/{listenKey}".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApiusersapikeysaccesskeystatus" => Defx::request(self, "api/users/apikeys/{accessKey}/status".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApiusersreferral" => Defx::request(self, "api/users/referral".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiordersallopen" => Defx::request(self, "api/orders/allOpen".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiorderorderid" => Defx::request(self, "api/order/{orderId}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApipositionpositionid" => Defx::request(self, "api/position/{positionId}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApipositionall" => Defx::request(self, "api/position/all".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiuserssocketlistenkeyslistenkey" => Defx::request(self, "api/users/socket/listenKeys/{listenKey}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiusersapikeysaccesskey" => Defx::request(self, "api/users/apikeys/{accessKey}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct DefxImpl(Value);
impl Exchange for DefxImpl {}
impl Defx for DefxImpl {}
impl ValueTrait for DefxImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value) -> Value { self.0.slice(start) }
}

impl DefxImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = DefxImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Defx::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256") }
fn sha384() -> Value { Value::from("sha384") }
fn sha512() -> Value { Value::from("sha512") }
fn md5() -> Value { Value::from("md5") }
fn ed25519() -> Value { Value::from("ed25519") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1") }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Hollaex : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "hollaex",
            "name": "HollaEx",
            "countries": [
                "KR"
            ],
            "enableRateLimit": true,
            "rateLimit": 250,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "sandbox": true,
                "spot": true,
                "swap": false,
                "future": false,
                "option": false,
                "addMargin": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "createLimitBuyOrder": true,
                "createLimitOrder": true,
                "createLimitSellOrder": true,
                "createMarketBuyOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketSellOrder": true,
                "createOrder": true,
                "createPostOnlyOrder": true,
                "createReduceOnlyOrder": false,
                "createStopLimitOrder": true,
                "createStopMarketOrder": true,
                "createStopOrder": true,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": "emulated",
                "fetchDepositAddresses": true,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingRates": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchLeverage": false,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrder": true,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrderBooks": true,
                "fetchOrders": true,
                "fetchPosition": false,
                "fetchPositionMode": false,
                "fetchPositions": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchTransactions": false,
                "fetchTransfer": false,
                "fetchTransfers": false,
                "fetchWithdrawal": true,
                "fetchWithdrawals": true,
                "reduceMargin": false,
                "setLeverage": false,
                "setMarginMode": false,
                "setPositionMode": false,
                "transfer": false,
                "watchBalance": true,
                "watchMyTrades": false,
                "watchOHLCV": false,
                "watchOrderBook": true,
                "watchOrders": true,
                "watchTicker": false,
                "watchTickers": false,
                "watchTrades": true,
                "watchTradesForSymbols": false,
                "withdraw": true,
                "ws": true
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/1294454/75841031-ca375180-5ddd-11ea-8417-b975674c23cb.jpg",
                "api": {
                    "rest": "https://api.hollaex.com",
                    "ws": "wss://api.hollaex.com/stream"
                },
                "www": "https://hollaex.com",
                "doc": "https://apidocs.hollaex.com",
                "test": {
                    "rest": "https://api.sandbox.hollaex.com",
                    "ws": "wss://api.sandbox.hollaex.com/stream"
                },
                "referral": "https://pro.hollaex.com/signup?affiliation_code=QSWA6G"
            },
            "api": {
                "public": {
                    "get": {
                        "health": 1,
                        "constants": 1,
                        "kit": 1,
                        "tiers": 1,
                        "ticker": 1,
                        "tickers": 1,
                        "orderbook": 1,
                        "orderbooks": 1,
                        "trades": 1,
                        "chart": 1,
                        "charts": 1,
                        "minicharts": 1,
                        "oracle/prices": 1,
                        "quick-trade": 1,
                        "udf/config": 1,
                        "udf/history": 1,
                        "udf/symbols": 1
                    }
                },
                "private": {
                    "get": {
                        "user": 1,
                        "user/balance": 1,
                        "user/deposits": 1,
                        "user/withdrawals": 1,
                        "user/withdrawal/fee": 1,
                        "user/trades": 1,
                        "orders": 1,
                        "order": 1
                    },
                    "post": {
                        "user/withdrawal": 1,
                        "order": 1
                    },
                    "delete": {
                        "order/all": 1,
                        "order": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "5m": "5m",
                "15m": "15m",
                "1h": "1h",
                "4h": "4h",
                "1d": "1d",
                "1w": "1w"
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.001,
                    "maker": 0.001
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "broad": {},
                "exact": {},
                "ws": {
                    "exact": {}
                }
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v2",
            "features": {
                "spot": {
                    "sandbox": true,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": false,
                            "FOK": false,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "selfTradePrevention": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false,
                        "iceberg": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "daysBackCanceled": 1,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "swap": {},
                "future": {}
            },
            "options": {
                "api-expires": 10,
                "networks": {
                    "BTC": "btc",
                    "ETH": "eth",
                    "ERC20": "eth",
                    "TRX": "trx",
                    "TRC20": "trx",
                    "XRP": "xrp",
                    "XLM": "xlm",
                    "BNB": "bnb",
                    "MATIC": "matic"
                },
                "networksById": {
                    "eth": "ERC20",
                    "ETH": "ERC20",
                    "ERC20": "ERC20",
                    "trx": "TRC20",
                    "TRX": "TRC20",
                    "TRC20": "TRC20"
                },
                "watchBalance": {},
                "watchOrders": {}
            },
            "streaming": {}
        }"###).unwrap())
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut message_hash: Value = Value::from("orderbook") + Value::from(":") + market.get(Value::from("id"));
        let mut orderbook: Value = <Self as Hollaex>::watch_public(self, message_hash.clone(), params.clone()).await;
        return orderbook.limit();
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic":"orderbook",
        //         "action":"partial",
        //         "symbol":"ltc-usdt",
        //         "data":{
        //             "bids":[
        //                 [104.29, 5.2264],
        //                 [103.86,1.3629],
        //                 [101.82,0.5942]
        //             ],
        //             "asks":[
        //                 [104.81,9.5531],
        //                 [105.54,0.6416],
        //                 [106.18,1.4141],
        //             ],
        //             "timestamp":"2022-04-12T08:17:05.932Z"
        //         },
        //         "time":1649751425
        //     }
        //
        let mut market_id: Value = self.safe_string(message.clone(), Value::from("symbol"), Value::Undefined);
        let mut channel: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut timestamp: Value = self.safe_string(data.clone(), Value::from("timestamp"), Value::Undefined);
        let mut timestamp_ms: Value = self.parse8601(timestamp.clone());
        let mut snapshot: Value = self.parse_order_book(data.clone(), symbol.clone(), timestamp_ms.clone(), Value::Undefined, Value::Undefined);
        let mut orderbook: Value = Value::Undefined;
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            orderbook = self.order_book(snapshot.clone(), Value::Undefined);
            self.get("orderbooks".into()).set(symbol.clone(), orderbook.clone());
        } else {
            orderbook = self.get("orderbooks".into()).get(symbol.clone());
            orderbook.reset(snapshot.clone());
        };
        let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        client.resolve(orderbook.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut message_hash: Value = Value::from("trade") + Value::from(":") + market.get(Value::from("id"));
        let mut trades: Value = <Self as Hollaex>::watch_public(self, message_hash.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "trade",
        //         "action": "partial",
        //         "symbol": "btc-usdt",
        //         "data": [
        //             {
        //                 "size": 0.05145,
        //                 "price": 41977.9,
        //                 "side": "buy",
        //                 "timestamp": "2022-04-11T09:40:10.881Z"
        //             },
        //         ]
        //     }
        //
        let mut channel: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut market_id: Value = self.safe_string(message.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut stored: Value = self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined);
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            stored = ArrayCache::new(limit);
            self.get("trades".into()).set(symbol.clone(), stored.clone());
        };
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_array());
        let mut parsed_trades: Value = self.parse_trades(data.clone(), market.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut j: usize = 0;
        while j < parsed_trades.len() {
            stored.append(parsed_trades.get(j.into()));
            j += 1;
        };
        let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        client.resolve(stored.clone(), message_hash.clone());
        client.resolve(stored.clone(), channel.clone());
        Value::Undefined
    }

    async fn watch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut message_hash: Value = Value::from("usertrade");
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
            message_hash = message_hash +  Value::from(":") + market.get(Value::from("id"));
        };
        let mut trades: Value = <Self as Hollaex>::watch_private(self, message_hash.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(trades.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_my_trades(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // {
        //     "topic":"usertrade",
        //     "action":"insert",
        //     "user_id":"103",
        //     "symbol":"xht-usdt",
        //     "data":[
        //        {
        //           "size":1,
        //           "side":"buy",
        //           "price":0.24,
        //           "symbol":"xht-usdt",
        //           "timestamp":"2022-05-13T09:30:15.014Z",
        //           "order_id":"6065a66e-e9a4-44a3-9726-4f8fa54b6bb6",
        //           "fee":0.001,
        //           "fee_coin":"xht",
        //           "is_same":true
        //        }
        //     ],
        //     "time":1652434215
        // }
        //
        let mut channel: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut raw_trades: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        // usually the first message is an empty array
        // when the user does not have any trades yet
        let mut data_length: usize = raw_trades.len();
        if data_length.clone() == Value::from(0) {
            return Value::Undefined;
        };
        if self.get("myTrades".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            self.set("my_trades".into(), ArrayCache::new(limit));
        };
        let mut stored: Value = self.get("myTrades".into());
        let mut market_ids: Value = Value::new_object();
        let mut i: usize = 0;
        while i < raw_trades.len() {
            let mut trade: Value = raw_trades.get(i.into());
            let mut parsed: Value = self.parse_trade(trade.clone(), Value::Undefined);
            stored.append(parsed.clone());
            let mut symbol: Value = trade.get(Value::from("symbol"));
            let mut market: Value = self.market(symbol.clone());
            let mut market_id: Value = market.get(Value::from("id"));
            market_ids.set(market_id.clone(), true.into());
            i += 1;
        };
        // non-symbol specific
        client.resolve(self.get("myTrades".into()), channel.clone());
        let mut keys: Value = market_ids.clone().keys();
        let mut i: usize = 0;
        while i < keys.len() {
            let mut market_id: Value = keys.get(i.into());
            let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
            client.resolve(self.get("myTrades".into()), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut message_hash: Value = Value::from("order");
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
            message_hash = message_hash +  Value::from(":") + market.get(Value::from("id"));
        };
        let mut orders: Value = <Self as Hollaex>::watch_private(self, message_hash.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_order(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        //     {
        //         "topic": "order",
        //         "action": "insert",
        //         "user_id": 155328,
        //         "symbol": "ltc-usdt",
        //         "data": {
        //             "symbol": "ltc-usdt",
        //             "side": "buy",
        //             "size": 0.05,
        //             "type": "market",
        //             "price": 0,
        //             "fee_structure": { maker: 0.1, taker: 0.1 },
        //             "fee_coin": "ltc",
        //             "id": "ce38fd48-b336-400b-812b-60c636454231",
        //             "created_by": 155328,
        //             "filled": 0.05,
        //             "method": "market",
        //             "created_at": "2022-04-11T14:09:00.760Z",
        //             "updated_at": "2022-04-11T14:09:00.760Z",
        //             "status": "filled"
        //         },
        //         "time": 1649686140
        //     }
        //
        //    {
        //        "topic":"order",
        //        "action":"partial",
        //        "user_id":155328,
        //        "data":[
        //           {
        //              "created_at":"2022-05-13T08:19:07.694Z",
        //              "fee":0,
        //              "meta":{
        //
        //              },
        //              "symbol":"ltc-usdt",
        //              "side":"buy",
        //              "size":0.1,
        //              "type":"limit",
        //              "price":55,
        //              "fee_structure":{
        //                 "maker":0.1,
        //                 "taker":0.1
        //              },
        //              "fee_coin":"ltc",
        //              "id":"d5e77182-ad4c-4ac9-8ce4-a97f9b43e33c",
        //              "created_by":155328,
        //              "filled":0,
        //              "status":"new",
        //              "updated_at":"2022-05-13T08:19:07.694Z",
        //              "stop":null
        //           }
        //        ],
        //        "time":1652430035
        //       }
        //
        let mut channel: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_object());
        // usually the first message is an empty array
        let mut data_length: usize = data.len();
        if data_length.clone() == Value::from(0) {
            return Value::Undefined;
        };
        if self.get("orders".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
            self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
        };
        let mut stored: Value = self.get("orders".into());
        let mut raw_orders: Value = Value::Undefined;
        if !Array::is_array(data.clone()).is_truthy() {
            raw_orders = Value::Json(serde_json::Value::Array(vec![data.clone().into()]));
        } else {
            raw_orders = data.clone();
        };
        let mut market_ids: Value = Value::new_object();
        let mut i: usize = 0;
        while i < raw_orders.len() {
            let mut order: Value = raw_orders.get(i.into());
            let mut parsed: Value = self.parse_order(order.clone(), Value::Undefined);
            stored.append(parsed.clone());
            let mut symbol: Value = order.get(Value::from("symbol"));
            let mut market: Value = self.market(symbol.clone());
            let mut market_id: Value = market.get(Value::from("id"));
            market_ids.set(market_id.clone(), true.into());
            i += 1;
        };
        // non-symbol specific
        client.resolve(self.get("orders".into()), channel.clone());
        let mut keys: Value = market_ids.clone().keys();
        let mut i: usize = 0;
        while i < keys.len() {
            let mut market_id: Value = keys.get(i.into());
            let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
            client.resolve(self.get("orders".into()), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut message_hash: Value = Value::from("wallet");
        return <Self as Hollaex>::watch_private(self, message_hash.clone(), params.clone()).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "wallet",
        //         "action": "partial",
        //         "user_id": 155328,
        //         "data": {
        //             "eth_balance": 0,
        //             "eth_available": 0,
        //             "usdt_balance": 18.94344188,
        //             "usdt_available": 18.94344188,
        //             "ltc_balance": 0.00005,
        //             "ltc_available": 0.00005,
        //         },
        //         "time": 1649687396
        //     }
        //
        let mut message_hash: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut keys: Value = data.clone().keys();
        let mut timestamp: Value = self.safe_timestamp(message.clone(), Value::from("time"), Value::Undefined);
        self.get("balance".into()).set("info".into(), data.clone());
        self.get("balance".into()).set("timestamp".into(), timestamp.clone());
        self.get("balance".into()).set("datetime".into(), self.iso8601(timestamp.clone()));
        let mut i: usize = 0;
        while i < keys.len() {
            let mut key: Value = keys.get(i.into());
            let mut parts: Value = key.split(Value::from("_"));
            let mut currency_id: Value = self.safe_string(parts.clone(), Value::from(0), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut account: Value = if self.get("balance".into()).contains_key(code.clone()) { self.get("balance".into()).get(code.clone()) } else { self.account() };
            let mut second: Value = self.safe_string(parts.clone(), Value::from(1), Value::Undefined);
            let mut free_or_total: Value = if second.clone() == Value::from("available") { Value::from("free") } else { Value::from("total") };
            account.set(free_or_total.clone(), self.safe_string(data.clone(), key.clone(), Value::Undefined));
            self.get("balance".into()).set(code.clone(), account.clone());
            i += 1;
        };
        self.set("balance".into(), self.safe_balance(self.get("balance".into())));
        client.resolve(self.get("balance".into()), message_hash.clone());
        Value::Undefined
    }

    async fn watch_public(&mut self, mut message_hash: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "op": "subscribe",
            "args": Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()]))
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return self.watch(url.clone(), message_hash.clone(), message.clone(), message_hash.clone(), Value::Undefined).await;
    }

    async fn watch_private(&mut self, mut message_hash: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.check_required_credentials(Value::Undefined);
        let mut expires: Value = self.safe_string(self.get("options".into()), Value::from("ws-expires"), Value::Undefined);
        if expires.clone().is_nullish() {
            let mut timeout: Value = parse_int(self.get("timeout".into()) / Value::from(1000).to_string(), Value::Undefined);
            expires = self.sum(self.seconds(), timeout.clone());
            expires = expires.to_string();
            // we need to memoize these values to avoid generating a new url on each method execution
            // that would trigger a new connection on each received message
            self.get("options".into()).set("ws-expires".into(), expires.clone());
        };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut auth: Value = Value::from("CONNECT") + Value::from("/stream") + expires.clone();
        let mut signature: Value = self.hmac(self.encode(auth.clone()), self.encode(self.get("secret".into())), sha256.clone(), Value::Undefined);
        let mut auth_params: Value = Value::Json(normalize(&Value::Json(json!({
            "api-key": self.get("apiKey".into()),
            "api-signature": signature,
            "api-expires": expires
        }))).unwrap());
        let mut signed_url: Value = url.clone() + Value::from("?") + self.urlencode(auth_params.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "op": "subscribe",
            "args": Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()]))
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return self.watch(signed_url.clone(), message_hash.clone(), message.clone(), message_hash.clone(), Value::Undefined).await;
    }

    fn handle_error_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     { error: "Bearer or HMAC authentication required" }
        //     { error: "Error: wrong input" }
        //
        let mut error: Value = self.safe_integer(message.clone(), Value::from("error"), Value::Undefined);
                if error.clone().is_nonnullish() {
            let mut feedback: Value = self.get("id".into()) + Value::from(" ") + self.json(message.clone());
            self.throw_exactly_matched_exception(self.get("exceptions".into()).get(Value::from("ws")).get(Value::from("exact")), error.clone(), feedback.clone());
        };
        // catch block omitted (no exception support in Value runtime)
;
        return message.clone();
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // pong
        //
        //     { message: "pong" }
        //
        // trade
        //
        //     {
        //         "topic": "trade",
        //         "action": "partial",
        //         "symbol": "btc-usdt",
        //         "data": [
        //             {
        //                 "size": 0.05145,
        //                 "price": 41977.9,
        //                 "side": "buy",
        //                 "timestamp": "2022-04-11T09:40:10.881Z"
        //             },
        //         ]
        //     }
        //
        // orderbook
        //
        //     {
        //         "topic": "orderbook",
        //         "action": "partial",
        //         "symbol": "ltc-usdt",
        //         "data": {
        //             "bids": [
        //                 [104.29, 5.2264],
        //                 [103.86,1.3629],
        //                 [101.82,0.5942]
        //             ],
        //             "asks": [
        //                 [104.81,9.5531],
        //                 [105.54,0.6416],
        //                 [106.18,1.4141],
        //             ],
        //             "timestamp": "2022-04-11T10:37:01.227Z"
        //         },
        //         "time": 1649673421
        //     }
        //
        // order
        //
        //     {
        //         "topic": "order",
        //         "action": "insert",
        //         "user_id": 155328,
        //         "symbol": "ltc-usdt",
        //         "data": {
        //             "symbol": "ltc-usdt",
        //             "side": "buy",
        //             "size": 0.05,
        //             "type": "market",
        //             "price": 0,
        //             "fee_structure": { maker: 0.1, taker: 0.1 },
        //             "fee_coin": "ltc",
        //             "id": "ce38fd48-b336-400b-812b-60c636454231",
        //             "created_by": 155328,
        //             "filled": 0.05,
        //             "method": "market",
        //             "created_at": "2022-04-11T14:09:00.760Z",
        //             "updated_at": "2022-04-11T14:09:00.760Z",
        //             "status": "filled"
        //         },
        //         "time": 1649686140
        //     }
        //
        // balance
        //
        //     {
        //         "topic": "wallet",
        //         "action": "partial",
        //         "user_id": 155328,
        //         "data": {
        //             "eth_balance": 0,
        //             "eth_available": 0,
        //             "usdt_balance": 18.94344188,
        //             "usdt_available": 18.94344188,
        //             "ltc_balance": 0.00005,
        //             "ltc_available": 0.00005,
        //         }
        //     }
        //
        if !<Self as Hollaex>::handle_error_message(self, client.clone(), message.clone()).is_truthy() {
            return Value::Undefined;
        };
        let mut content: Value = self.safe_string(message.clone(), Value::from("message"), Value::Undefined);
        if content.clone() == Value::from("pong") {
            <Self as Hollaex>::handle_pong(self, client.clone(), message.clone());
            return Value::Undefined;
        };
        let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
            "trade": self.get("handleTrades".into()),
            "orderbook": self.get("handleOrderBook".into()),
            "order": self.get("handleOrder".into()),
            "wallet": self.get("handleBalance".into()),
            "usertrade": self.get("handleMyTrades".into())
        }))).unwrap());
        let mut topic: Value = self.safe_value(message.clone(), Value::from("topic"), Value::Undefined);
        let mut method: Value = self.safe_value(methods.clone(), topic.clone(), Value::Undefined);
        if method.clone().is_nonnullish() {
            method.call(self, client.clone(), message.clone());
        };
        Value::Undefined
    }

    fn ping(&mut self, mut client: Value) -> Value {
        // hollaex does not support built-in ws protocol-level ping-pong
        return Value::Json(normalize(&Value::Json(json!({
            "op": "ping"
        }))).unwrap());
    }

    fn handle_pong(&mut self, mut client: Value, mut message: Value) -> Value {
        client.set("last_pong".into(), self.milliseconds());
        return message.clone();
    }

    fn on_error(&mut self, mut client: Value, mut error: Value) -> Value {
        self.get("options".into()).set("ws-expires".into(), Value::Undefined);
        hollaexRest::on_error(self, client.clone(), error.clone());
        Value::Undefined
    }

    fn on_close(&mut self, mut client: Value, mut error: Value) -> Value {
        self.get("options".into()).set("ws-expires".into(), Value::Undefined);
        hollaexRest::on_close(self, client.clone(), error.clone());
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetHealth" => Hollaex::request(self, "health".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConstants" => Hollaex::request(self, "constants".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetKit" => Hollaex::request(self, "kit".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTiers" => Hollaex::request(self, "tiers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTicker" => Hollaex::request(self, "ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTickers" => Hollaex::request(self, "tickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOrderbook" => Hollaex::request(self, "orderbook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOrderbooks" => Hollaex::request(self, "orderbooks".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTrades" => Hollaex::request(self, "trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetChart" => Hollaex::request(self, "chart".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCharts" => Hollaex::request(self, "charts".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMinicharts" => Hollaex::request(self, "minicharts".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOracleprices" => Hollaex::request(self, "oracle/prices".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetQuicktrade" => Hollaex::request(self, "quick-trade".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetUdfconfig" => Hollaex::request(self, "udf/config".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetUdfhistory" => Hollaex::request(self, "udf/history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetUdfsymbols" => Hollaex::request(self, "udf/symbols".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUser" => Hollaex::request(self, "user".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUserbalance" => Hollaex::request(self, "user/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUserdeposits" => Hollaex::request(self, "user/deposits".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUserwithdrawals" => Hollaex::request(self, "user/withdrawals".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUserwithdrawalfee" => Hollaex::request(self, "user/withdrawal/fee".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUsertrades" => Hollaex::request(self, "user/trades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrders" => Hollaex::request(self, "orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrder" => Hollaex::request(self, "order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostUserwithdrawal" => Hollaex::request(self, "user/withdrawal".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrder" => Hollaex::request(self, "order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteOrderall" => Hollaex::request(self, "order/all".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteOrder" => Hollaex::request(self, "order".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct HollaexImpl(Value);
impl Exchange for HollaexImpl {}
impl Hollaex for HollaexImpl {}
impl ValueTrait for HollaexImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value) -> Value { self.0.slice(start) }
}

impl HollaexImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = HollaexImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Hollaex::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


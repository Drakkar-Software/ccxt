#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Blofin : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "blofin",
            "name": "BloFin",
            "countries": [
                "US"
            ],
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowMargin": false,
                "cancelAllOrders": false,
                "cancelOrder": true,
                "cancelOrders": true,
                "closeAllPositions": false,
                "closePosition": true,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": false,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrders": true,
                "createOrderWithTakeProfitAndStopLoss": true,
                "createPostOnlyOrder": false,
                "createReduceOnlyOrder": false,
                "createStopLimitOrder": false,
                "createStopLossOrder": true,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "createTakeProfitOrder": true,
                "createTriggerOrder": true,
                "editOrder": false,
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchCanceledOrders": false,
                "fetchClosedOrder": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": false,
                "fetchCurrenciesWs": "emulated",
                "fetchDeposit": false,
                "fetchDepositAddress": false,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": false,
                "fetchDepositWithdrawFee": "emulated",
                "fetchDepositWithdrawFees": false,
                "fetchFundingHistory": true,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": true,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchL3OrderBook": false,
                "fetchLedger": true,
                "fetchLeverage": true,
                "fetchLeverages": true,
                "fetchLeverageTiers": false,
                "fetchMarginMode": true,
                "fetchMarginModes": false,
                "fetchMarketLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMySettlementHistory": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterest": false,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOrderBook": true,
                "fetchOrderBooks": false,
                "fetchOrders": false,
                "fetchOrderTrades": true,
                "fetchPosition": true,
                "fetchPositionMode": true,
                "fetchPositions": true,
                "fetchPositionsForSymbol": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchSettlementHistory": false,
                "fetchStatus": false,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": false,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": false,
                "fetchTradingLimits": false,
                "fetchTransactionFee": false,
                "fetchTransactionFees": false,
                "fetchTransactions": false,
                "fetchTransfer": false,
                "fetchTransfers": false,
                "fetchUnderlyingAssets": false,
                "fetchVolatilityHistory": false,
                "fetchWithdrawal": false,
                "fetchWithdrawals": true,
                "fetchWithdrawalWhitelist": false,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "setLeverage": true,
                "setMargin": false,
                "setMarginMode": true,
                "setPositionMode": true,
                "signIn": false,
                "transfer": true,
                "watchOHLCV": true,
                "watchOHLCVForSymbols": true,
                "watchOrderBook": true,
                "watchBidsAsks": true,
                "watchOrderBookForSymbols": true,
                "watchOrders": true,
                "watchOrdersForSymbols": true,
                "watchPositions": true,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": true,
                "withdraw": false,
                "ws": true
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/518cdf80-f05d-4821-a3e3-d48ceb41d73b",
                "api": {
                    "rest": "https://openapi.blofin.com",
                    "ws": {
                        "swap": {
                            "public": "wss://openapi.blofin.com/ws/public",
                            "private": "wss://openapi.blofin.com/ws/private"
                        }
                    }
                },
                "www": "https://www.blofin.com",
                "doc": "https://blofin.com/docs",
                "test": {
                    "rest": "https://demo-trading-openapi.blofin.com",
                    "ws": {
                        "swap": {
                            "public": "wss://demo-trading-openapi.blofin.com/ws/public",
                            "private": "wss://demo-trading-openapi.blofin.com/ws/private"
                        }
                    }
                },
                "referral": {
                    "url": "https://blofin.com/register?referral_code=f79EsS",
                    "discount": 0.05
                }
            },
            "api": {
                "public": {
                    "get": {
                        "market/instruments": 1,
                        "market/tickers": 1,
                        "market/books": 1,
                        "market/trades": 1,
                        "market/candles": 1,
                        "market/mark-price": 1,
                        "market/funding-rate": 1,
                        "market/funding-rate-history": 1
                    }
                },
                "private": {
                    "get": {
                        "asset/balances": 1,
                        "trade/orders-pending": 1,
                        "trade/fills-history": 1,
                        "asset/deposit-history": 1,
                        "asset/withdrawal-history": 1,
                        "asset/bills": 1,
                        "account/balance": 1,
                        "account/positions": 1,
                        "account/leverage-info": 1,
                        "account/margin-mode": 1,
                        "account/position-mode": 1,
                        "account/batch-leverage-info": 1,
                        "trade/orders-tpsl-pending": 1,
                        "trade/orders-algo-pending": 1,
                        "trade/orders-history": 1,
                        "trade/orders-tpsl-history": 1,
                        "trade/orders-algo-history": 1,
                        "trade/order/price-range": 1,
                        "user/query-apikey": 1,
                        "affiliate/basic": 1,
                        "copytrading/instruments": 1,
                        "copytrading/account/balance": 1,
                        "copytrading/account/positions-by-order": 1,
                        "copytrading/account/positions-details-by-order": 1,
                        "copytrading/account/positions-by-contract": 1,
                        "copytrading/account/position-mode": 1,
                        "copytrading/account/leverage-info": 1,
                        "copytrading/trade/orders-pending": 1,
                        "copytrading/trade/pending-tpsl-by-contract": 1,
                        "copytrading/trade/position-history-by-order": 1,
                        "copytrading/trade/orders-history": 1,
                        "copytrading/trade/pending-tpsl-by-order": 1
                    },
                    "post": {
                        "account/set-margin-mode": 1,
                        "account/set-position-mode": 1,
                        "trade/order": 1,
                        "trade/order-algo": 1,
                        "trade/cancel-order": 1,
                        "trade/cancel-algo": 1,
                        "account/set-leverage": 1,
                        "trade/batch-orders": 1,
                        "trade/order-tpsl": 1,
                        "trade/cancel-batch-orders": 1,
                        "trade/cancel-tpsl": 1,
                        "trade/close-position": 1,
                        "asset/transfer": 1,
                        "copytrading/account/set-position-mode": 1,
                        "copytrading/account/set-leverage": 1,
                        "copytrading/trade/place-order": 1,
                        "copytrading/trade/cancel-order": 1,
                        "copytrading/trade/place-tpsl-by-contract": 1,
                        "copytrading/trade/cancel-tpsl-by-contract": 1,
                        "copytrading/trade/place-tpsl-by-order": 1,
                        "copytrading/trade/cancel-tpsl-by-order": 1,
                        "copytrading/trade/close-position-by-order": 1,
                        "copytrading/trade/close-position-by-contract": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": true,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "3m": "3m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1H",
                "2h": "2H",
                "4h": "4H",
                "6h": "6H",
                "8h": "8H",
                "12h": "12H",
                "1d": "1D",
                "3d": "3D",
                "1w": "1W",
                "1M": "1M"
            },
            "fees": {
                "trading": {},
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                },
                "swap": {
                    "taker": 0.0006,
                    "maker": 0.0002
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "hostname": "www.blofin.com",
            "features": {
                "default": {
                    "sandbox": false,
                    "createOrder": {
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "leverage": false,
                        "marketBuyRequiresPrice": false,
                        "marketBuyByCost": false,
                        "selfTradePrevention": false,
                        "trailing": false,
                        "iceberg": false
                    },
                    "createOrders": {
                        "max": 10
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "daysBack": 100000,
                        "daysBackCanceled": 1,
                        "untilDays": 100000,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1440
                    }
                },
                "spot": {
                    "extends": "default",
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "hedged": false
                    }
                },
                "forDerivatives": {
                    "extends": "default",
                    "createOrder": {
                        "marginMode": true,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": true,
                        "takeProfitPrice": true,
                        "attachedStopLossTakeProfit": {
                            "price": true
                        },
                        "hedged": true
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forDerivatives"
                    }
                },
                "future": {}
            },
            "options": {
                "brokerId": "ec6dd3a7dd982d0b",
                "accountsByType": {
                    "swap": "futures",
                    "funding": "funding",
                    "future": "futures",
                    "copy_trading": "copy_trading",
                    "earn": "earn",
                    "spot": "spot"
                },
                "accountsById": {
                    "funding": "funding",
                    "futures": "swap",
                    "copy_trading": "copy_trading",
                    "earn": "earn",
                    "spot": "spot"
                },
                "defaultNetwork": "ERC20",
                "defaultNetworks": {
                    "ETH": "ERC20",
                    "BTC": "BTC",
                    "USDT": "TRC20"
                },
                "networks": {
                    "BTC": "Bitcoin",
                    "BEP20": "BSC",
                    "ERC20": "ERC20",
                    "TRC20": "TRC20"
                },
                "fetchOpenInterestHistory": {
                    "timeframes": {
                        "5m": "5m",
                        "1h": "1H",
                        "8h": "8H",
                        "1d": "1D",
                        "5M": "5m",
                        "1H": "1H",
                        "8H": "8H",
                        "1D": "1D"
                    }
                },
                "fetchOHLCV": {
                    "timezone": "UTC"
                },
                "fetchPositions": {
                    "method": "privateGetAccountPositions"
                },
                "createOrder": "privatePostTradeOrder",
                "createMarketBuyOrderRequiresPrice": false,
                "fetchMarkets": [
                    "swap"
                ],
                "defaultType": "swap",
                "fetchLedger": {
                    "method": "privateGetAssetBills"
                },
                "fetchOpenOrders": {
                    "method": "privateGetTradeOrdersPending"
                },
                "cancelOrders": {
                    "method": "privatePostTradeCancelBatchOrders"
                },
                "fetchCanceledOrders": {
                    "method": "privateGetTradeOrdersHistory"
                },
                "fetchClosedOrders": {
                    "method": "privateGetTradeOrdersHistory"
                },
                "withdraw": {},
                "exchangeType": {
                    "spot": "SPOT",
                    "swap": "SWAP",
                    "SPOT": "SPOT",
                    "SWAP": "SWAP"
                },
                "tradesLimit": 1000,
                "watchOrderBook": {
                    "channel": "books"
                },
                "watchOrderBookForSymbols": {
                    "channel": "books"
                }
            },
            "streaming": {
                "keepAlive": 25000
            }
        }"###).unwrap())
    }

    fn ping(&mut self, mut client: Value) -> Value {
        return Value::from("ping");
    }

    fn handle_pong(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //   'pong'
        //
        client.set("last_pong".into(), self.milliseconds());
        Value::Undefined
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        params.set("callerMethodName".into(), Value::from("watchTrades"));
        return <Self as Blofin>::watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn watch_trades_for_symbols(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut trades: Value = <Self as Blofin>::watch_multiple_wrapper(self, true.into(), Value::from("trades"), Value::from("watchTradesForSymbols"), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut first_market: Value = self.safe_dict(trades.clone(), Value::from(0), Value::Undefined);
            let mut first_symbol: Value = self.safe_string(first_market.clone(), Value::from("symbol"), Value::Undefined);
            limit = trades.get_limit(first_symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //       arg: {
        //         channel: "trades",
        //         instId: "DOGE-USDT",
        //       },
        //       data : [
        //         <same object as shown in REST example>,
        //         ...
        //       ]
        //     }
        //
        let mut arg: Value = self.safe_dict(message.clone(), Value::from("arg"), Value::Undefined);
        let mut channel_name: Value = self.safe_string(arg.clone(), Value::from("channel"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::Undefined);
        if data.clone().is_nullish() {
            return Value::Undefined;
        };
        let mut i: usize = 0;
        while i < data.len() {
            let mut raw_trade: Value = data.get(i.into());
            let mut trade: Value = <Self as Blofin>::parse_ws_trade(self, raw_trade.clone(), Value::Undefined);
            let mut symbol: Value = trade.get(Value::from("symbol"));
            let mut stored: Value = self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined);
            if stored.clone().is_nullish() {
                let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
                stored = ArrayCache::new(limit);
                self.get("trades".into()).set(symbol.clone(), stored.clone());
            };
            stored.append(trade.clone());
            let mut message_hash: Value = channel_name.clone() + Value::from(":") + symbol.clone();
            client.resolve(stored.clone(), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        return self.parse_trade(trade.clone(), market.clone());
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        params.set("callerMethodName".into(), Value::from("watchOrderBook"));
        return <Self as Blofin>::watch_order_book_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), limit.clone(), params.clone()).await;
    }

    async fn watch_order_book_for_symbols(&mut self, mut symbols: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut caller_method_name: Value = Value::Undefined;
        (caller_method_name, params) = shift_2(self.handle_param_string(params.clone(), Value::from("callerMethodName"), Value::from("watchOrderBookForSymbols")));
        let mut channel_name: Value = Value::Undefined;
        (channel_name, params) = shift_2(self.handle_option_and_params(params.clone(), caller_method_name.clone(), Value::from("channel"), Value::from("books")));
        // due to some problem, temporarily disable other channels
        if channel_name.clone() != Value::from("books") {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" ") + caller_method_name.clone() + Value::from("() at this moment ") + channel_name.clone() + Value::from(" is not supported, coming soon"))"###);
        };
        let mut orderbook: Value = <Self as Blofin>::watch_multiple_wrapper(self, true.into(), channel_name.clone(), caller_method_name.clone(), symbols.clone(), params.clone()).await;
        return orderbook.limit();
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //   {
        //     arg: {
        //         channel: "books",
        //         instId: "DOGE-USDT",
        //     },
        //     action: "snapshot", // can be 'snapshot' or 'update'
        //     data: {
        //         asks: [   [ 0.08096, 1 ], [ 0.08097, 123 ], ...   ],
        //         bids: [   [ 0.08095, 4 ], [ 0.08094, 237 ], ...   ],
        //         ts: "1707491587909",
        //         prevSeqId: "0", // in case of 'update' there will be some value, less then seqId
        //         seqId: "3374250786",
        //     },
        // }
        //
        let mut arg: Value = self.safe_dict(message.clone(), Value::from("arg"), Value::Undefined);
        let mut channel_name: Value = self.safe_string(arg.clone(), Value::from("channel"), Value::Undefined);
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::Undefined);
        let mut market_id: Value = self.safe_string(arg.clone(), Value::from("instId"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut message_hash: Value = channel_name.clone() + Value::from(":") + symbol.clone();
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            self.get("orderbooks".into()).set(symbol.clone(), self.order_book());
        };
        let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
        let mut timestamp: Value = self.safe_integer(data.clone(), Value::from("ts"), Value::Undefined);
        let mut action: Value = self.safe_string(message.clone(), Value::from("action"), Value::Undefined);
        if action.clone() == Value::from("snapshot") {
            let mut order_book_snapshot: Value = self.parse_order_book(data.clone(), symbol.clone(), timestamp.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
            order_book_snapshot.set("nonce".into(), self.safe_integer(data.clone(), Value::from("seqId"), Value::Undefined));
            orderbook.reset(order_book_snapshot.clone());
        } else {
            let mut asks: Value = self.safe_list(data.clone(), Value::from("asks"), Value::new_array());
            let mut bids: Value = self.safe_list(data.clone(), Value::from("bids"), Value::new_array());
            self.handle_deltas_with_keys(orderbook.get(Value::from("asks")), asks.clone());
            self.handle_deltas_with_keys(orderbook.get(Value::from("bids")), bids.clone());
            orderbook.set("timestamp".into(), timestamp.clone());
            orderbook.set("datetime".into(), self.iso8601(timestamp.clone()));
        };
        self.get("orderbooks".into()).set(symbol.clone(), orderbook.clone());
        client.resolve(orderbook.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        params.set("callerMethodName".into(), Value::from("watchTicker"));
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut result: Value = <Self as Blofin>::watch_tickers(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
        return result.get(symbol.clone());
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbols.clone().is_nullish() {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" watchTickers() requires a list of symbols"))"###);
        };
        let mut ticker: Value = <Self as Blofin>::watch_multiple_wrapper(self, true.into(), Value::from("tickers"), Value::from("watchTickers"), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut tickers: Value = Value::new_object();
            tickers.set(ticker.get(Value::from("symbol")), ticker.clone());
            return tickers.clone();
        };
        return self.filter_by_array(self.get("tickers".into()), Value::from("symbol"), symbols.clone());
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // message
        //
        //     {
        //         arg: {
        //             channel: "tickers",
        //             instId: "DOGE-USDT",
        //         },
        //         data: [
        //             <same object as shown in REST example>
        //         ],
        //     }
        //
        <Self as Blofin>::handle_bid_ask(self, client.clone(), message.clone());
        let mut arg: Value = self.safe_dict(message.clone(), Value::from("arg"), Value::Undefined);
        let mut channel_name: Value = self.safe_string(arg.clone(), Value::from("channel"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::Undefined);
        let mut i: usize = 0;
        while i < data.len() {
            let mut ticker: Value = <Self as Blofin>::parse_ws_ticker(self, data.get(i.into()), Value::Undefined);
            let mut symbol: Value = ticker.get(Value::from("symbol"));
            let mut message_hash: Value = channel_name.clone() + Value::from(":") + symbol.clone();
            self.get("tickers".into()).set(symbol.clone(), ticker.clone());
            client.resolve(self.get("tickers".into()).get(symbol.clone()), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn parse_ws_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        return self.parse_ticker(ticker.clone(), market.clone());
    }

    async fn watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into());
        let mut first_market: Value = self.market(symbols.get(Value::from(0)));
        let mut channel: Value = Value::from("tickers");
        let mut market_type: Value = Value::Undefined;
        (market_type, params) = shift_2(self.handle_market_type_and_params(Value::from("watchBidsAsks"), first_market.clone(), params.clone()));
        let mut url: Value = self.implode_hostname(self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(market_type.clone()).get(Value::from("public")));
        let mut message_hashes: Value = Value::new_array();
        let mut args: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut market: Value = self.market(symbols.get(i.into()));
            message_hashes.push(Value::from("bidask:") + market.get(Value::from("symbol")));
            args.push(Value::Json(normalize(&Value::Json(json!({
                "channel": channel,
                "instId": market.get(Value::from("id"))
            }))).unwrap()));
            i += 1;
        };
        let mut request: Value = <Self as Blofin>::get_subscription_request(self, args.clone());
        let mut ticker: Value = self.watch_multiple(url.clone(), message_hashes.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hashes.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut tickers: Value = Value::new_object();
            tickers.set(ticker.get(Value::from("symbol")), ticker.clone());
            return tickers.clone();
        };
        return self.filter_by_array(self.get("bidsasks".into()), Value::from("symbol"), symbols.clone());
    }

    fn handle_bid_ask(&mut self, mut client: Value, mut message: Value) -> Value {
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::Undefined);
        let mut i: usize = 0;
        while i < data.len() {
            let mut ticker: Value = <Self as Blofin>::parse_ws_bid_ask(self, data.get(i.into()), Value::Undefined);
            let mut symbol: Value = ticker.get(Value::from("symbol"));
            let mut message_hash: Value = Value::from("bidask:") + symbol.clone();
            self.get("bidsasks".into()).set(symbol.clone(), ticker.clone());
            client.resolve(ticker.clone(), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn parse_ws_bid_ask(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("instId"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::from("-"), Value::Undefined);
        let mut symbol: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("ts"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "ask": self.safe_string(ticker.clone(), Value::from("askPrice"), Value::Undefined),
            "askVolume": self.safe_string(ticker.clone(), Value::from("askSize"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("bidPrice"), Value::Undefined),
            "bidVolume": self.safe_string(ticker.clone(), Value::from("bidSize"), Value::Undefined),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        params.set("callerMethodName".into(), Value::from("watchOHLCV"));
        let mut result: Value = <Self as Blofin>::watch_ohlcv_for_symbols(self, Value::Json(serde_json::Value::Array(vec![Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into()])).into()])), since.clone(), limit.clone(), params.clone()).await;
        return result.get(symbol.clone()).get(timeframe.clone());
    }

    async fn watch_ohlcv_for_symbols(&mut self, mut symbols_and_timeframes: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut symbols_length: usize = symbols_and_timeframes.len();
        if symbols_length.clone() == Value::from(0) || !Array::is_array(symbols_and_timeframes.get(Value::from(0))).is_truthy() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" watchOHLCVForSymbols() requires a an array of symbols and timeframes, like  [['BTC/USDT', '1m'], ['LTC/USDT', '5m']]"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let (mut symbol, mut timeframe, mut candles) = shift_3(<Self as Blofin>::watch_multiple_wrapper(self, true.into(), Value::from("candle"), Value::from("watchOHLCVForSymbols"), symbols_and_timeframes.clone(), params.clone()).await);
        if self.get("newUpdates".into()).is_truthy() {
            limit = candles.get_limit(symbol.clone(), limit.clone());
        };
        let mut filtered: Value = self.filter_by_since_limit(candles.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
        return self.create_ohlcv_object(symbol.clone(), timeframe.clone(), filtered.clone());
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // message
        //
        //     {
        //         arg: {
        //             channel: "candle1m",
        //             instId: "DOGE-USDT",
        //         },
        //         data: [
        //             [ same object as shown in REST example ]
        //         ],
        //     }
        //
        let mut arg: Value = self.safe_dict(message.clone(), Value::from("arg"), Value::Undefined);
        let mut channel_name: Value = self.safe_string(arg.clone(), Value::from("channel"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::Undefined);
        let mut market_id: Value = self.safe_string(arg.clone(), Value::from("instId"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut interval: Value = channel_name.replace(Value::from("candle"), Value::from(""));
        let mut unified_timeframe: Value = self.find_timeframe(interval.clone());
        self.get("ohlcvs".into()).set(symbol.clone(), self.safe_dict(self.get("ohlcvs".into()), symbol.clone(), Value::new_object()));
        let mut stored: Value = self.safe_value(self.get("ohlcvs".into()).get(symbol.clone()), unified_timeframe.clone(), Value::Undefined);
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            stored = ArrayCacheByTimestamp::new(limit);
            self.get("ohlcvs".into()).get(symbol.clone()).set(unified_timeframe.clone(), stored.clone());
        };
        let mut i: usize = 0;
        while i < data.len() {
            let mut candle: Value = data.get(i.into());
            let mut parsed: Value = self.parse_ohlcv(candle.clone(), market.clone());
            stored.append(parsed.clone());
            i += 1;
        };
        let mut resolve_data: Value = Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), unified_timeframe.clone().into(), stored.clone().into()]));
        let mut message_hash: Value = Value::from("candle") + interval.clone() + Value::from(":") + symbol.clone();
        client.resolve(resolve_data.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        <Self as Blofin>::authenticate(self, Value::Undefined).await;
        let mut market_type: Value = Value::Undefined;
        (market_type, params) = shift_2(self.handle_market_type_and_params(Value::from("watchBalance"), Value::Undefined, params.clone()));
        if market_type.clone() == Value::from("spot") {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" watchBalance() is not supported for spot markets yet"))"###);
        };
        let mut message_hash: Value = market_type.clone() + Value::from(":balance");
        let mut sub: Value = Value::Json(normalize(&Value::Json(json!({
            "channel": "account"
        }))).unwrap());
        let mut request: Value = <Self as Blofin>::get_subscription_request(self, Value::Json(serde_json::Value::Array(vec![sub.clone().into()])));
        let mut url: Value = self.implode_hostname(self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(market_type.clone()).get(Value::from("private")));
        return self.watch(url.clone(), message_hash.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hash.clone()).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         arg: {
        //           channel: "account",
        //         },
        //         data: <same object as shown in REST example>,
        //     }
        //
        let mut market_type: Value = Value::from("swap");
        // for now
        if !self.get("balance".into()).contains_key(market_type.clone()) {
            self.get("balance".into()).set(market_type.clone(), Value::new_object());
        };
        self.get("balance".into()).set(market_type.clone(), <Self as Blofin>::parse_ws_balance(self, message.clone()));
        let mut message_hash: Value = market_type.clone() + Value::from(":balance");
        client.resolve(self.get("balance".into()).get(market_type.clone()), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_balance(&self, mut message: Value) -> Value {
        return self.parse_balance(message.clone());
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        params.set("callerMethodName".into(), Value::from("watchOrders"));
        let mut symbols_array: Value = if symbol.clone().is_nonnullish() { Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])) } else { Value::new_array() };
        return <Self as Blofin>::watch_orders_for_symbols(self, symbols_array.clone(), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn watch_orders_for_symbols(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Blofin>::authenticate(self, Value::Undefined).await;
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut trigger: Value = self.safe_value_2(params.clone(), Value::from("stop"), Value::from("trigger"), Value::Undefined);
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stop").into(), Value::from("trigger").into()])));
        let mut channel: Value = if trigger.is_truthy() { Value::from("orders-algo") } else { Value::from("orders") };
        let mut orders: Value = <Self as Blofin>::watch_multiple_wrapper(self, false.into(), channel.clone(), Value::from("watchOrdersForSymbols"), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut first: Value = self.safe_value(orders.clone(), Value::from(0), Value::Undefined);
            let mut trade_symbol: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
            limit = orders.get_limit(trade_symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(orders.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    fn handle_orders(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         action: 'update',
        //         arg: { channel: 'orders' },
        //         data: [
        //           <same object as shown in REST example>
        //         ]
        //     }
        //
        if self.get("orders".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
            self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
        };
        let mut orders: Value = self.get("orders".into());
        let mut arg: Value = self.safe_dict(message.clone(), Value::from("arg"), Value::Undefined);
        let mut channel_name: Value = self.safe_string(arg.clone(), Value::from("channel"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::Undefined);
        let mut i: usize = 0;
        while i < data.len() {
            let mut order: Value = <Self as Blofin>::parse_ws_order(self, data.get(i.into()), Value::Undefined);
            let mut symbol: Value = order.get(Value::from("symbol"));
            let mut message_hash: Value = channel_name.clone() + Value::from(":") + symbol.clone();
            orders.append(order.clone());
            client.resolve(orders.clone(), message_hash.clone());
            client.resolve(orders.clone(), channel_name.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn parse_ws_order(&self, mut order: Value, mut market: Value) -> Value {
        return self.parse_order(order.clone(), market.clone());
    }

    async fn watch_positions(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Blofin>::authenticate(self, Value::Undefined).await;
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut new_positions: Value = <Self as Blofin>::watch_multiple_wrapper(self, false.into(), Value::from("positions"), Value::from("watchPositions"), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            return new_positions.clone();
        };
        return self.filter_by_symbols_since_limit(self.get("positions".into()), symbols.clone(), since.clone(), limit.clone());
    }

    fn handle_positions(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         arg: { channel: 'positions' },
        //         data: [
        //           <same object as shown in REST example>
        //         ]
        //     }
        //
        if self.get("positions".into()).is_nullish() {
            self.set("positions".into(), ArrayCacheBySymbolBySide::new());
        };
        let mut cache: Value = self.get("positions".into());
        let mut arg: Value = self.safe_dict(message.clone(), Value::from("arg"), Value::Undefined);
        let mut channel_name: Value = self.safe_string(arg.clone(), Value::from("channel"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::Undefined);
        let mut new_positions: Value = Value::new_array();
        let mut i: usize = 0;
        while i < data.len() {
            let mut position: Value = <Self as Blofin>::parse_ws_position(self, data.get(i.into()), Value::Undefined);
            new_positions.push(position.clone());
            cache.append(position.clone());
            let mut message_hash: Value = channel_name.clone() + Value::from(":") + position.get(Value::from("symbol"));
            client.resolve(position.clone(), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn parse_ws_position(&self, mut position: Value, mut market: Value) -> Value {
        return self.parse_position(position.clone(), market.clone());
    }

    async fn watch_multiple_wrapper(&mut self, mut is_public: Value, mut channel_name: Value, mut caller_method_name: Value, mut symbols_array: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        // underlier method for all watch-multiple symbols
        self.load_markets(Value::Undefined, Value::Undefined).await;
        (caller_method_name, params) = shift_2(self.handle_param_string(params.clone(), Value::from("callerMethodName"), caller_method_name.clone()));
        // if OHLCV method are being called, then symbols would be symbolsAndTimeframes (multi-dimensional) array
        let mut is_ohlcv: Value = (channel_name.clone() == Value::from("candle")).into();
        let mut symbols: Value = if is_ohlcv.is_truthy() { self.get_list_from_object_values(symbols_array.clone(), Value::from(0)) } else { symbols_array.clone() };
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, true.into(), true.into());
        let mut first_market: Value = Value::Undefined;
        let mut first_symbol: Value = self.safe_string(symbols.clone(), Value::from(0), Value::Undefined);
        if first_symbol.clone().is_nonnullish() {
            first_market = self.market(first_symbol.clone());
        };
        let mut market_type: Value = Value::Undefined;
        (market_type, params) = shift_2(self.handle_market_type_and_params(caller_method_name.clone(), first_market.clone(), params.clone()));
        if market_type.clone() != Value::from("swap") {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" ") + caller_method_name.clone() + Value::from("() does not support ") + market_type.clone() + Value::from(" markets yet"))"###);
        };
        let mut raw_subscriptions: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        if symbols.clone().is_nullish() {
            symbols = Value::new_array();
        };
        let mut symbols_length: usize = symbols.len();
        if symbols_length.clone() > Value::from(0) {
            let mut i: usize = 0;
            while i < symbols.len() {
                let mut current: Value = symbols.get(i.into());
                let mut market: Value = Value::Undefined;
                let mut channel: Value = channel_name.clone();
                if is_ohlcv.is_truthy() {
                    market = self.market(current.clone());
                    let mut tf_array: Value = symbols_array.get(i.into());
                    let mut tf: Value = tf_array.get(Value::from(1));
                    let mut interval: Value = self.safe_string(self.get("timeframes".into()), tf.clone(), tf.clone());
                    channel = channel +  interval.clone();
                } else {
                    market = self.market(current.clone());
                };
                let mut topic: Value = Value::Json(normalize(&Value::Json(json!({
                    "channel": channel,
                    "instId": market.get(Value::from("id"))
                }))).unwrap());
                raw_subscriptions.push(topic.clone());
                message_hashes.push(channel.clone() + Value::from(":") + market.get(Value::from("symbol")));
                i += 1;
            };
        } else {
            raw_subscriptions.push(Value::Json(normalize(&Value::Json(json!({
                "channel": channel_name
            }))).unwrap()));
            message_hashes.push(channel_name.clone());
        };
        // private channel are difference, they only need plural channel name for multiple symbols
        if self.in_array(channel_name.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("orders").into(), Value::from("orders-algo").into(), Value::from("positions").into()]))).is_truthy() {
            raw_subscriptions = Value::Json(serde_json::Value::Array(vec![Value::Json(normalize(&Value::Json(json!({
                "channel": channel_name
            }))).unwrap()).into()]));
        };
        let mut request: Value = <Self as Blofin>::get_subscription_request(self, raw_subscriptions.clone());
        let mut private_or_public: Value = if is_public.is_truthy() { Value::from("public") } else { Value::from("private") };
        let mut url: Value = self.implode_hostname(self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(market_type.clone()).get(private_or_public.clone()));
        return self.watch_multiple(url.clone(), message_hashes.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hashes.clone()).await;
    }

    fn get_subscription_request(&mut self, mut args: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "op": "subscribe",
            "args": args
        }))).unwrap());
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // message examples
        //
        // {
        //   arg: {
        //     channel: "trades",
        //     instId: "DOGE-USDT",
        //   },
        //   event: "subscribe"
        // }
        //
        // incoming data updates' examples can be seen under each handler method
        //
        let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
            "pong": self.get("handlePong".into()),
            "trades": self.get("handleTrades".into()),
            "books": self.get("handleOrderBook".into()),
            "tickers": self.get("handleTicker".into()),
            "candle": self.get("handleOHLCV".into()),
            "account": self.get("handleBalance".into()),
            "orders": self.get("handleOrders".into()),
            "orders-algo": self.get("handleOrders".into()),
            "positions": self.get("handlePositions".into())
        }))).unwrap());
        // public
        // private
        let mut method: Value = Value::Undefined;
        if message.clone() == Value::from("pong") {
            method = self.safe_value(methods.clone(), Value::from("pong"), Value::Undefined);
        } else {
            let mut event: Value = self.safe_string(message.clone(), Value::from("event"), Value::Undefined);
            if event.clone() == Value::from("subscribe") {
                return Value::Undefined;
            } else if event.clone() == Value::from("login") {
                let mut future: Value = self.safe_value(client.get(futures.clone()), Value::from("authenticate_hash"), Value::Undefined);
                future.resolve(true.into());
                return Value::Undefined;
            } else if event.clone() == Value::from("error") {
                panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" error: ") + self.json(message.clone()))"###);
            };
            let mut arg: Value = self.safe_dict(message.clone(), Value::from("arg"), Value::Undefined);
            let mut channel_name: Value = self.safe_string(arg.clone(), Value::from("channel"), Value::Undefined);
            method = self.safe_value(methods.clone(), channel_name.clone(), Value::Undefined);
            if !method.is_truthy() && channel_name.index_of(Value::from("candle")) >= Value::from(0) {
                method = methods.get(Value::from("candle"));
            };
        };
        if method.is_truthy() {
            method.call(self, client.clone(), message.clone());
        };
        Value::Undefined
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.check_required_credentials();
        let mut milliseconds: Value = self.milliseconds();
        let mut message_hash: Value = Value::from("authenticate_hash");
        let mut timestamp: Value = milliseconds.to_string();
        let mut nonce: Value = Value::from("n_") + timestamp.clone();
        let mut auth: Value = Value::from("/users/self/verify") + Value::from("GET") + timestamp.clone() + Value::from("") + nonce.clone();
        let mut signature: Value = self.string_to_base64(self.hmac(self.encode(auth.clone()), self.encode(self.get("secret".into())), sha256().clone(), Value::Undefined));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "op": "login",
            "args": Value::Json(serde_json::Value::Array(vec![Value::Json(normalize(&Value::Json(json!({
                "apiKey": self.get("apiKey".into()),
                "passphrase": self.get("password".into()),
                "timestamp": timestamp,
                "nonce": nonce,
                "sign": signature
            }))).unwrap()).into()]))
        }))).unwrap());
        let mut market_type: Value = Value::from("swap");
        // for now
        let mut url: Value = self.implode_hostname(self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(market_type.clone()).get(Value::from("private")));
        self.watch(url.clone(), message_hash.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hash.clone()).await;
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetMarketinstruments" => self.request("market/instruments".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarkettickers" => self.request("market/tickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketbooks" => self.request("market/books".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarkettrades" => self.request("market/trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketcandles" => self.request("market/candles".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketmarkprice" => self.request("market/mark-price".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketfundingrate" => self.request("market/funding-rate".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketfundingratehistory" => self.request("market/funding-rate-history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetbalances" => self.request("asset/balances".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderspending" => self.request("trade/orders-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradefillshistory" => self.request("trade/fills-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetdeposithistory" => self.request("asset/deposit-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetwithdrawalhistory" => self.request("asset/withdrawal-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetbills" => self.request("asset/bills".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountbalance" => self.request("account/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountpositions" => self.request("account/positions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountleverageinfo" => self.request("account/leverage-info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountmarginmode" => self.request("account/margin-mode".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountpositionmode" => self.request("account/position-mode".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountbatchleverageinfo" => self.request("account/batch-leverage-info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderstpslpending" => self.request("trade/orders-tpsl-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeordersalgopending" => self.request("trade/orders-algo-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeordershistory" => self.request("trade/orders-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderstpslhistory" => self.request("trade/orders-tpsl-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeordersalgohistory" => self.request("trade/orders-algo-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderpricerange" => self.request("trade/order/price-range".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUserqueryapikey" => self.request("user/query-apikey".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAffiliatebasic" => self.request("affiliate/basic".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradinginstruments" => self.request("copytrading/instruments".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountbalance" => self.request("copytrading/account/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionsbyorder" => self.request("copytrading/account/positions-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionsdetailsbyorder" => self.request("copytrading/account/positions-details-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionsbycontract" => self.request("copytrading/account/positions-by-contract".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionmode" => self.request("copytrading/account/position-mode".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountleverageinfo" => self.request("copytrading/account/leverage-info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradeorderspending" => self.request("copytrading/trade/orders-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradependingtpslbycontract" => self.request("copytrading/trade/pending-tpsl-by-contract".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradepositionhistorybyorder" => self.request("copytrading/trade/position-history-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradeordershistory" => self.request("copytrading/trade/orders-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradependingtpslbyorder" => self.request("copytrading/trade/pending-tpsl-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountsetmarginmode" => self.request("account/set-margin-mode".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountsetpositionmode" => self.request("account/set-position-mode".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeorder" => self.request("trade/order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeorderalgo" => self.request("trade/order-algo".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecancelorder" => self.request("trade/cancel-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecancelalgo" => self.request("trade/cancel-algo".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountsetleverage" => self.request("account/set-leverage".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradebatchorders" => self.request("trade/batch-orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeordertpsl" => self.request("trade/order-tpsl".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecancelbatchorders" => self.request("trade/cancel-batch-orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecanceltpsl" => self.request("trade/cancel-tpsl".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecloseposition" => self.request("trade/close-position".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAssettransfer" => self.request("asset/transfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingaccountsetpositionmode" => self.request("copytrading/account/set-position-mode".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingaccountsetleverage" => self.request("copytrading/account/set-leverage".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeplaceorder" => self.request("copytrading/trade/place-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradecancelorder" => self.request("copytrading/trade/cancel-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeplacetpslbycontract" => self.request("copytrading/trade/place-tpsl-by-contract".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradecanceltpslbycontract" => self.request("copytrading/trade/cancel-tpsl-by-contract".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeplacetpslbyorder" => self.request("copytrading/trade/place-tpsl-by-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradecanceltpslbyorder" => self.request("copytrading/trade/cancel-tpsl-by-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeclosepositionbyorder" => self.request("copytrading/trade/close-position-by-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeclosepositionbycontract" => self.request("copytrading/trade/close-position-by-contract".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BlofinImpl(Value);
impl Exchange for BlofinImpl {}
impl Blofin for BlofinImpl {}
impl ValueTrait for BlofinImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl BlofinImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = BlofinImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Blofin::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


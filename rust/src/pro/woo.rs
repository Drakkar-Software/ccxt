#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Woo : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "woo",
            "name": "WOO X",
            "countries": [
                "KY"
            ],
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": true,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "sandbox": true,
                "spot": true,
                "margin": true,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": true,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "closeAllPositions": false,
                "closePosition": false,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": true,
                "createMarketOrder": false,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": true,
                "createOrder": true,
                "createOrderWithTakeProfitAndStopLoss": true,
                "createReduceOnlyOrder": true,
                "createStopLimitOrder": false,
                "createStopLossOrder": true,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "createTakeProfitOrder": true,
                "createTrailingAmountOrder": true,
                "createTrailingPercentOrder": true,
                "createTriggerOrder": true,
                "editOrder": "emulated",
                "fetchAccounts": true,
                "fetchBalance": true,
                "fetchCanceledOrders": false,
                "fetchClosedOrder": false,
                "fetchClosedOrders": true,
                "fetchConvertCurrencies": true,
                "fetchConvertQuote": true,
                "fetchConvertTrade": true,
                "fetchConvertTradeHistory": true,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": true,
                "fetchFundingHistory": true,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": true,
                "fetchFundingInterval": true,
                "fetchFundingIntervals": false,
                "fetchFundingRates": true,
                "fetchIndexOHLCV": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLeverage": true,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrder": false,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchOrderTrades": true,
                "fetchPosition": true,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": false,
                "fetchTickers": false,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": true,
                "fetchTradingFees": true,
                "fetchTransactions": "emulated",
                "fetchTransfers": true,
                "fetchWithdrawals": true,
                "reduceMargin": false,
                "setLeverage": true,
                "setMargin": false,
                "setPositionMode": true,
                "transfer": true,
                "watchBalance": true,
                "watchMyTrades": true,
                "watchOHLCV": true,
                "watchOrderBook": true,
                "watchBidsAsks": true,
                "watchOrders": true,
                "watchPositions": true,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": false,
                "unWatchTrades": true,
                "unWatchOrderBook": true,
                "unWatchTickers": true,
                "unWatchTicker": true,
                "unWatchOHLCV": true,
                "withdraw": true,
                "ws": true,
                "cancelAllOrdersAfter": true,
                "cancelWithdraw": false,
                "createConvertTrade": true
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/1294454/150730761-1a00e5e0-d28c-480f-9e65-089ce3e6ef3b.jpg",
                "api": {
                    "pub": "https://api-pub.woox.io",
                    "public": "https://api.{hostname}",
                    "private": "https://api.{hostname}",
                    "ws": {
                        "public": "wss://wss.woox.io/ws/stream",
                        "private": "wss://wss.woox.io/v2/ws/private/stream"
                    }
                },
                "www": "https://woox.io/",
                "doc": [
                    "https://docs.woox.io/"
                ],
                "fees": [
                    "https://support.woox.io/hc/en-001/articles/4404611795353--Trading-Fees"
                ],
                "test": {
                    "pub": "https://api-pub.staging.woox.io",
                    "public": "https://api.staging.woox.io",
                    "private": "https://api.staging.woox.io",
                    "ws": {
                        "public": "wss://wss.staging.woox.io/ws/stream",
                        "private": "wss://wss.staging.woox.io/v2/ws/private/stream"
                    }
                },
                "referral": {
                    "url": "https://woox.io/register?ref=DIJT0CNL",
                    "discount": 0.35
                }
            },
            "api": {
                "v1": {
                    "pub": {
                        "get": {
                            "hist/kline": 10,
                            "hist/trades": 10
                        }
                    },
                    "public": {
                        "get": {
                            "info": 1,
                            "info/{symbol}": 1,
                            "system_info": 1,
                            "market_trades": 1,
                            "token": 1,
                            "token_network": 1,
                            "funding_rates": 1,
                            "funding_rate/{symbol}": 1,
                            "funding_rate_history": 1,
                            "futures": 1,
                            "futures/{symbol}": 1,
                            "orderbook/{symbol}": 1,
                            "kline": 1
                        }
                    },
                    "private": {
                        "get": {
                            "client/token": 1,
                            "order/{oid}": 1,
                            "client/order/{client_order_id}": 1,
                            "orders": 1,
                            "client/trade/{tid}": 1,
                            "order/{oid}/trades": 1,
                            "client/trades": 1,
                            "client/hist_trades": 1,
                            "staking/yield_history": 1,
                            "client/holding": 1,
                            "asset/deposit": 10,
                            "asset/history": 60,
                            "sub_account/all": 60,
                            "sub_account/assets": 60,
                            "sub_account/asset_detail": 60,
                            "sub_account/ip_restriction": 10,
                            "asset/main_sub_transfer_history": 30,
                            "token_interest": 60,
                            "token_interest/{token}": 60,
                            "interest/history": 60,
                            "interest/repay": 60,
                            "funding_fee/history": 30,
                            "positions": 3.33,
                            "position/{symbol}": 3.33,
                            "client/transaction_history": 60,
                            "client/futures_leverage": 60
                        },
                        "post": {
                            "order": 1,
                            "order/cancel_all_after": 1,
                            "asset/ltv": 30,
                            "asset/internal_withdraw": 30,
                            "interest/repay": 60,
                            "client/account_mode": 120,
                            "client/position_mode": 5,
                            "client/leverage": 120,
                            "client/futures_leverage": 30,
                            "client/isolated_margin": 30
                        },
                        "delete": {
                            "order": 1,
                            "client/order": 1,
                            "orders": 1,
                            "asset/withdraw": 120
                        }
                    }
                },
                "v2": {
                    "private": {
                        "get": {
                            "client/holding": 1
                        }
                    }
                },
                "v3": {
                    "public": {
                        "get": {
                            "systemInfo": 1,
                            "instruments": 1,
                            "token": 1,
                            "tokenNetwork": 1,
                            "tokenInfo": 1,
                            "marketTrades": 1,
                            "marketTradesHistory": 1,
                            "orderbook": 1,
                            "kline": 1,
                            "klineHistory": 1,
                            "futures": 1,
                            "fundingRate": 1,
                            "fundingRateHistory": 1,
                            "insuranceFund": 1
                        }
                    },
                    "private": {
                        "get": {
                            "trade/order": 2,
                            "trade/orders": 1,
                            "trade/algoOrder": 1,
                            "trade/algoOrders": 1,
                            "trade/transaction": 1,
                            "trade/transactionHistory": 5,
                            "trade/tradingFee": 5,
                            "account/info": 60,
                            "account/tokenConfig": 1,
                            "account/symbolConfig": 1,
                            "account/subAccounts/all": 60,
                            "account/referral/summary": 60,
                            "account/referral/rewardHistory": 60,
                            "account/credentials": 60,
                            "asset/balances": 1,
                            "asset/token/history": 60,
                            "asset/transfer/history": 30,
                            "asset/wallet/history": 60,
                            "asset/wallet/deposit": 60,
                            "asset/staking/yieldHistory": 60,
                            "futures/positions": 3.33,
                            "futures/leverage": 60,
                            "futures/defaultMarginMode": 60,
                            "futures/fundingFee/history": 30,
                            "spotMargin/interestRate": 60,
                            "spotMargin/interestHistory": 60,
                            "spotMargin/maxMargin": 60,
                            "algo/order/{oid}": 1,
                            "algo/orders": 1,
                            "positions": 3.33,
                            "buypower": 1,
                            "convert/exchangeInfo": 1,
                            "convert/assetInfo": 1,
                            "convert/rfq": 60,
                            "convert/trade": 1,
                            "convert/trades": 1
                        },
                        "post": {
                            "trade/order": 2,
                            "trade/algoOrder": 5,
                            "trade/cancelAllAfter": 1,
                            "account/tradingMode": 120,
                            "account/listenKey": 20,
                            "asset/transfer": 30,
                            "asset/wallet/withdraw": 60,
                            "spotMargin/leverage": 120,
                            "spotMargin/interestRepay": 60,
                            "algo/order": 5,
                            "convert/rft": 60
                        },
                        "put": {
                            "trade/order": 2,
                            "trade/algoOrder": 2,
                            "futures/leverage": 60,
                            "futures/positionMode": 120,
                            "order/{oid}": 2,
                            "order/client/{client_order_id}": 2,
                            "algo/order/{oid}": 2,
                            "algo/order/client/{client_order_id}": 2
                        },
                        "delete": {
                            "trade/order": 1,
                            "trade/orders": 1,
                            "trade/algoOrder": 1,
                            "trade/algoOrders": 1,
                            "trade/allOrders": 1,
                            "algo/order/{order_id}": 1,
                            "algo/orders/pending": 1,
                            "algo/orders/pending/{symbol}": 1,
                            "orders/pending": 1
                        }
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": true,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1h",
                "4h": "4h",
                "12h": "12h",
                "1d": "1d",
                "1w": "1w",
                "1M": "1mon",
                "1y": "1y"
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.0005,
                    "maker": 0.0002
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {},
                "ws": {
                    "exact": {}
                }
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "hostname": "woox.io",
            "options": {
                "timeDifference": 0,
                "adjustForTimeDifference": false,
                "sandboxMode": false,
                "createMarketBuyOrderRequiresPrice": true,
                "network-aliases-for-tokens": {
                    "HT": "ERC20",
                    "OMG": "ERC20",
                    "UATOM": "ATOM",
                    "ZRX": "ZRX"
                },
                "networks": {
                    "TRX": "TRON",
                    "TRC20": "TRON",
                    "ERC20": "ETH",
                    "BEP20": "BSC",
                    "ARB": "Arbitrum"
                },
                "networksById": {
                    "TRX": "TRC20",
                    "TRON": "TRC20"
                },
                "defaultNetworkCodeForCurrencies": {},
                "transfer": {
                    "fillResponseFromRequest": true
                },
                "brokerId": "bc830de7-50f3-460b-9ee0-f430f83f9dad",
                "tradesLimit": 1000,
                "ordersLimit": 1000,
                "requestId": {},
                "watchPositions": {
                    "fetchPositionsSnapshot": true,
                    "awaitPositionsSnapshot": true
                }
            },
            "features": {
                "default": {
                    "sandbox": true,
                    "createOrder": {
                        "marginMode": true,
                        "triggerPrice": true,
                        "triggerPriceType": {
                            "last": true,
                            "mark": true,
                            "index": false
                        },
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": true
                        },
                        "hedged": false,
                        "trailing": true,
                        "leverage": false,
                        "marketBuyByCost": true,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": false,
                        "iceberg": true
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 500,
                        "daysBack": 90,
                        "untilDays": 10000,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "trigger": true,
                        "trailing": true,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "untilDays": 100000,
                        "trigger": true,
                        "trailing": true,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "untilDays": 100000,
                        "trigger": true,
                        "trailing": true,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "spot": {
                    "extends": "default"
                },
                "forSwap": {
                    "extends": "default",
                    "createOrder": {
                        "hedged": true
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forSwap"
                    }
                },
                "future": {}
            },
            "streaming": {
                "keepAlive": 9000
            }
        }"###).unwrap())
    }

    fn request_id(&mut self, mut url: Value) -> Value {
        let mut options: Value = self.safe_value(self.get("options".into()), Value::from("requestId"), Value::new_object());
        let mut previous_value: Value = self.safe_integer(options.clone(), url.clone(), Value::from(0));
        let mut new_value: Value = self.sum(previous_value.clone(), Value::from(1));
        self.get("options".into()).get(Value::from("requestId")).set(url.clone(), new_value.clone());
        return new_value.clone();
    }

    async fn watch_public(&mut self, mut message_hash: Value, mut message: Value) -> Value {
        let mut url_uid: Value = if self.get("uid".into()).is_truthy() { Value::from("/") + self.get("uid".into()) } else { Value::from("") };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public")) + url_uid.clone();
        let mut request_id: Value = <Self as Woo>::request_id(self, url.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id
        }))).unwrap());
        let mut request: Value = extend_2(subscribe.clone(), message.clone());
        return self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), subscribe.clone()).await;
    }

    async fn unwatch_public(&mut self, mut sub_hash: Value, mut symbol: Value, mut topic: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut url_uid: Value = if self.get("uid".into()).is_truthy() { Value::from("/") + self.get("uid".into()) } else { Value::from("") };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public")) + url_uid.clone();
        let mut request_id: Value = <Self as Woo>::request_id(self, url.clone());
        let mut unsub_hash: Value = Value::from("unsubscribe::") + sub_hash.clone();
        let mut message: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id,
            "event": "unsubscribe",
            "topic": sub_hash
        }))).unwrap());
        let mut subscription: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id.to_string(),
            "unsubscribe": true,
            "symbols": Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])),
            "topic": topic,
            "subMessageHashes": Value::Json(serde_json::Value::Array(vec![sub_hash.clone().into()])),
            "unsubMessageHashes": Value::Json(serde_json::Value::Array(vec![unsub_hash.clone().into()]))
        }))).unwrap());
        let mut symbols_and_timeframes: Value = self.safe_list(params.clone(), Value::from("symbolsAndTimeframes"), Value::Undefined);
        if symbols_and_timeframes.clone().is_nonnullish() {
            subscription.set("symbolsAndTimeframes".into(), symbols_and_timeframes.clone());
            params = self.omit(params.clone(), Value::from("symbolsAndTimeframes"));
        };
        return self.watch(url.clone(), unsub_hash.clone(), extend_2(message.clone(), params.clone()), unsub_hash.clone(), subscription.clone()).await;
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut method: Value = Value::Undefined;
        (method, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchOrderBook"), Value::from("method"), Value::from("orderbook")));
        let mut market: Value = self.market(symbol.clone());
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@") + method.clone();
        let mut url_uid: Value = if self.get("uid".into()).is_truthy() { Value::from("/") + self.get("uid".into()) } else { Value::from("") };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public")) + url_uid.clone();
        let mut request_id: Value = <Self as Woo>::request_id(self, url.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic,
            "id": request_id
        }))).unwrap());
        let mut subscription: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id.to_string(),
            "name": method,
            "symbol": market.get(Value::from("symbol")),
            "limit": limit,
            "params": params
        }))).unwrap());
        if method.clone() == Value::from("orderbookupdate") {
            subscription.set("method".into(), self.get("handleOrderBookSubscription".into()));
        };
        let mut orderbook: Value = self.watch(url.clone(), topic.clone(), extend_2(request.clone(), params.clone()), topic.clone(), subscription.clone()).await;
        return orderbook.limit();
    }

    async fn un_watch_order_book(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut method: Value = Value::Undefined;
        (method, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchOrderBook"), Value::from("method"), Value::from("orderbook")));
        let mut market: Value = self.market(symbol.clone());
        let mut sub_hash: Value = market.get(Value::from("id")) + Value::from("@") + method.clone();
        let mut topic: Value = Value::from("orderbook");
        return <Self as Woo>::unwatch_public(self, sub_hash.clone(), market.get(Value::from("symbol")), topic.clone(), params.clone()).await;
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "PERP_BTC_USDT@orderbookupdate",
        //         "ts": 1722500373999,
        //         "data": {
        //             "symbol": "PERP_BTC_USDT",
        //             "prevTs": 1722500373799,
        //             "bids": [
        //                 [
        //                     0.30891,
        //                     2469.98
        //                 ]
        //             ],
        //             "asks": [
        //                 [
        //                     0.31075,
        //                     2379.63
        //                 ]
        //             ]
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::Undefined);
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut method: Value = self.safe_string(topic.split(Value::from("@")), Value::from(1), Value::Undefined);
        if method.clone() == Value::from("orderbookupdate") {
            if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
                return Value::Undefined;
            };
            let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
            let mut timestamp: Value = self.safe_integer(orderbook.clone(), Value::from("timestamp"), Value::Undefined);
            if timestamp.clone().is_nullish() {
                orderbook.get(cache.clone()).push(message.clone());
            } else {
                                let mut ts: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
                if ts.clone() > timestamp.clone() {
                    <Self as Woo>::handle_order_book_message(self, client.clone(), message.clone(), orderbook.clone());
                    client.resolve(orderbook.clone(), topic.clone());
                };
                // catch block omitted (no exception support in Value runtime)
;
            };
        } else {
            if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
                let mut default_limit: Value = self.safe_integer(self.get("options".into()), Value::from("watchOrderBookLimit"), Value::from(1000));
                let mut subscription: Value = client.get(subscriptions.clone()).get(topic.clone());
                let mut limit: Value = self.safe_integer(subscription.clone(), Value::from("limit"), default_limit.clone());
                self.get("orderbooks".into()).set(symbol.clone(), self.order_book(Value::new_object(), limit.clone()));
            };
            let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
            let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
            let mut snapshot: Value = self.parse_order_book(data.clone(), symbol.clone(), timestamp.clone(), Value::from("bids"), Value::from("asks"), Value::Undefined, Value::Undefined, Value::Undefined);
            orderbook.reset(snapshot.clone());
            client.resolve(orderbook.clone(), topic.clone());
        };
        Value::Undefined
    }

    fn handle_order_book_subscription(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        let mut default_limit: Value = self.safe_integer(self.get("options".into()), Value::from("watchOrderBookLimit"), Value::from(1000));
        let mut limit: Value = self.safe_integer(subscription.clone(), Value::from("limit"), default_limit.clone());
        let mut symbol: Value = self.safe_string(subscription.clone(), Value::from("symbol"), Value::Undefined);
        // watchOrderBook
        if self.get("orderbooks".into()).contains_key(symbol.clone()) {
            self.get("orderbooks".into()).get(symbol.clone());
        };
        self.get("orderbooks".into()).set(symbol.clone(), self.order_book(Value::new_object(), limit.clone()));
        self.spawn(self.get("fetchOrderBookSnapshot".into()), client.clone(), message.clone(), subscription.clone());
        Value::Undefined
    }

    async fn fetch_order_book_snapshot(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        let mut symbol: Value = self.safe_string(subscription.clone(), Value::from("symbol"), Value::Undefined);
        let mut message_hash: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
                let mut default_limit: Value = self.safe_integer(self.get("options".into()), Value::from("watchOrderBookLimit"), Value::from(1000));
        let mut limit: Value = self.safe_integer(subscription.clone(), Value::from("limit"), default_limit.clone());
        let mut params: Value = self.safe_value(subscription.clone(), Value::from("params"), Value::Undefined);
        let mut snapshot: Value = self.fetch_rest_order_book_safe(symbol.clone(), limit.clone(), params.clone()).await;
        if self.safe_value(self.get("orderbooks".into()), symbol.clone(), Value::Undefined).is_nullish() {
            // if the orderbook is dropped before the snapshot is received
            return Value::Undefined;
        };
        let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
        orderbook.reset(snapshot.clone());
        let mut messages: Value = orderbook.get(cache.clone());
        let mut i: usize = 0;
        while i < messages.len() {
            let mut message_item: Value = messages.get(i.into());
            let mut ts: Value = self.safe_integer(message_item.clone(), Value::from("ts"), Value::Undefined);
            if ts.clone() < orderbook.get(Value::from("timestamp")) {
                continue;
            } else {
                <Self as Woo>::handle_order_book_message(self, client.clone(), message_item.clone(), orderbook.clone());
            };
            i += 1;
        };
        self.get("orderbooks".into()).set(symbol.clone(), orderbook.clone());
        client.resolve(orderbook.clone(), message_hash.clone());
        // catch block omitted (no exception support in Value runtime)
;
        Value::Undefined
    }

    fn handle_order_book_message(&mut self, mut client: Value, mut message: Value, mut orderbook: Value) -> Value {
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::Undefined);
        <Self as Woo>::handle_deltas(self, orderbook.get(Value::from("asks")), self.safe_value(data.clone(), Value::from("asks"), Value::new_array()));
        <Self as Woo>::handle_deltas(self, orderbook.get(Value::from("bids")), self.safe_value(data.clone(), Value::from("bids"), Value::new_array()));
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
        orderbook.set("timestamp".into(), timestamp.clone());
        orderbook.set("datetime".into(), self.iso8601(timestamp.clone()));
        return orderbook.clone();
    }

    fn handle_delta(&mut self, mut bookside: Value, mut delta: Value) -> Value {
        let mut price: Value = self.safe_float_2(delta.clone(), Value::from("price"), Value::from(0), Value::Undefined);
        let mut amount: Value = self.safe_float_2(delta.clone(), Value::from("quantity"), Value::from(1), Value::Undefined);
        bookside.store(price.clone(), amount.clone());
        Value::Undefined
    }

    fn handle_deltas(&mut self, mut bookside: Value, mut deltas: Value) -> Value {
        let mut i: usize = 0;
        while i < deltas.len() {
            <Self as Woo>::handle_delta(self, bookside.clone(), deltas.get(i.into()));
            i += 1;
        };
        Value::Undefined
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("ticker");
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@") + name.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return <Self as Woo>::watch_public(self, topic.clone(), message.clone()).await;
    }

    async fn un_watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut method: Value = Value::Undefined;
        (method, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchTicker"), Value::from("method"), Value::from("ticker")));
        let mut market: Value = self.market(symbol.clone());
        let mut sub_hash: Value = market.get(Value::from("id")) + Value::from("@") + method.clone();
        let mut topic: Value = Value::from("ticker");
        return <Self as Woo>::unwatch_public(self, sub_hash.clone(), market.get(Value::from("symbol")), topic.clone(), params.clone()).await;
    }

    fn parse_ws_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     {
        //         "symbol": "PERP_BTC_USDT",
        //         "open": 19441.5,
        //         "close": 20147.07,
        //         "high": 20761.87,
        //         "low": 19320.54,
        //         "volume": 2481.103,
        //         "amount": 50037935.0286,
        //         "count": 3689
        //     }
        //
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": self.safe_symbol(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "high": self.safe_string(ticker.clone(), Value::from("high"), Value::Undefined),
            "low": self.safe_string(ticker.clone(), Value::from("low"), Value::Undefined),
            "bid": Value::Undefined,
            "bidVolume": Value::Undefined,
            "ask": Value::Undefined,
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": self.safe_string(ticker.clone(), Value::from("open"), Value::Undefined),
            "close": self.safe_string(ticker.clone(), Value::from("close"), Value::Undefined),
            "last": Value::Undefined,
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": self.safe_string(ticker.clone(), Value::from("volume"), Value::Undefined),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("amount"), Value::Undefined),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "PERP_BTC_USDT@ticker",
        //         "ts": 1657120017000,
        //         "data": {
        //             "symbol": "PERP_BTC_USDT",
        //             "open": 19441.5,
        //             "close": 20147.07,
        //             "high": 20761.87,
        //             "low": 19320.54,
        //             "volume": 2481.103,
        //             "amount": 50037935.0286,
        //             "count": 3689
        //         }
        //     }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut topic: Value = self.safe_value(message.clone(), Value::from("topic"), Value::Undefined);
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
        data.set("date".into(), timestamp.clone());
        let mut ticker: Value = <Self as Woo>::parse_ws_ticker(self, data.clone(), market.clone());
        ticker.set("symbol".into(), market.get(Value::from("symbol")));
        self.get("tickers".into()).set(market.get(Value::from("symbol")), ticker.clone());
        client.resolve(ticker.clone(), topic.clone());
        return message.clone();
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut name: Value = Value::from("tickers");
        let mut topic: Value = name.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut tickers: Value = <Self as Woo>::watch_public(self, topic.clone(), message.clone()).await;
        return self.filter_by_array(tickers.clone(), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    async fn un_watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        if symbols.clone().is_nonnullish() {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" unWatchTickers() does not support a symbols argument. Only unwatch all tickers at once"))"###);
        };
        let mut topic: Value = Value::from("ticker");
        let mut sub_hash: Value = Value::from("tickers");
        return <Self as Woo>::unwatch_public(self, sub_hash.clone(), Value::Undefined, topic.clone(), params.clone()).await;
    }

    fn handle_tickers(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic":"tickers",
        //         "ts":1618820615000,
        //         "data":[
        //             {
        //                 "symbol":"SPOT_OKB_USDT",
        //                 "open":16.297,
        //                 "close":17.183,
        //                 "high":24.707,
        //                 "low":11.997,
        //                 "volume":0,
        //                 "amount":0,
        //                 "count":0
        //             },
        //             {
        //                 "symbol":"SPOT_XRP_USDT",
        //                 "open":1.3515,
        //                 "close":1.43794,
        //                 "high":1.96674,
        //                 "low":0.39264,
        //                 "volume":750127.1,
        //                 "amount":985440.5122,
        //                 "count":396
        //             },
        //         ...
        //         ]
        //     }
        //
        let mut topic: Value = self.safe_value(message.clone(), Value::from("topic"), Value::Undefined);
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
        let mut result: Value = Value::new_array();
        let mut i: usize = 0;
        while i < data.len() {
            let mut market_id: Value = self.safe_string(data.get(i.into()), Value::from("symbol"), Value::Undefined);
            let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            let mut ticker: Value = <Self as Woo>::parse_ws_ticker(self, extend_2(data.get(i.into()), Value::Json(normalize(&Value::Json(json!({
                "date": timestamp
            }))).unwrap())), market.clone());
            self.get("tickers".into()).set(market.get(Value::from("symbol")), ticker.clone());
            result.push(ticker.clone());
            i += 1;
        };
        client.resolve(result.clone(), topic.clone());
        Value::Undefined
    }

    async fn watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut name: Value = Value::from("bbos");
        let mut topic: Value = name.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut bidsasks: Value = <Self as Woo>::watch_public(self, topic.clone(), message.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            return bidsasks.clone();
        };
        return self.filter_by_array(self.get("bidsasks".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    async fn un_watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        if symbols.clone().is_nonnullish() {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" unWatchBidsAsks() does not support a symbols argument. Only unwatch all bidsAsks at once"))"###);
        };
        let mut sub_hash: Value = Value::from("bbos");
        let mut topic: Value = Value::from("bidsasks");
        return <Self as Woo>::unwatch_public(self, sub_hash.clone(), Value::Undefined, topic.clone(), params.clone()).await;
    }

    fn handle_bid_ask(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "bbos",
        //         "ts": 1618822376000,
        //         "data": [
        //             {
        //                 "symbol": "SPOT_FIL_USDT",
        //                 "ask": 159.0318,
        //                 "askSize": 370.43,
        //                 "bid": 158.9158,
        //                 "bidSize": 16
        //             }
        //         ]
        //     }
        //
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < data.len() {
            let mut ticker: Value = self.safe_dict(data.clone(), Value::from(i), Value::Undefined);
            ticker.set("ts".into(), timestamp.clone());
            let mut parsed_ticker: Value = <Self as Woo>::parse_ws_bid_ask(self, ticker.clone());
            let mut symbol: Value = parsed_ticker.get(Value::from("symbol"));
            self.get("bidsasks".into()).set(symbol.clone(), parsed_ticker.clone());
            result.set(symbol.clone(), parsed_ticker.clone());
            i += 1;
        };
        client.resolve(result.clone(), topic.clone());
        Value::Undefined
    }

    fn parse_ws_bid_ask(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("ts"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "ask": self.safe_string(ticker.clone(), Value::from("ask"), Value::Undefined),
            "askVolume": self.safe_string(ticker.clone(), Value::from("askSize"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("bid"), Value::Undefined),
            "bidVolume": self.safe_string(ticker.clone(), Value::from("bidSize"), Value::Undefined),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        if timeframe.clone() != Value::from("1m") && timeframe.clone() != Value::from("5m") && timeframe.clone() != Value::from("15m") && timeframe.clone() != Value::from("30m") && timeframe.clone() != Value::from("1h") && timeframe.clone() != Value::from("1d") && timeframe.clone() != Value::from("1w") && timeframe.clone() != Value::from("1M") {
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" watchOHLCV timeframe argument must be 1m, 5m, 15m, 30m, 1h, 1d, 1w, 1M"))"###);
        };
        let mut market: Value = self.market(symbol.clone());
        let mut interval: Value = self.safe_string(self.get("timeframes".into()), timeframe.clone(), timeframe.clone());
        let mut name: Value = Value::from("kline");
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@") + name.clone() + Value::from("_") + interval.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut ohlcv: Value = <Self as Woo>::watch_public(self, topic.clone(), message.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = ohlcv.get_limit(market.get(Value::from("symbol")), limit.clone());
        };
        return self.filter_by_since_limit(ohlcv.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
    }

    async fn un_watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut interval: Value = self.safe_string(self.get("timeframes".into()), timeframe.clone(), timeframe.clone());
        let mut topic: Value = Value::from("ohlcv");
        let mut name: Value = Value::from("kline");
        let mut sub_hash: Value = market.get(Value::from("id")) + Value::from("@") + name.clone() + Value::from("_") + interval.clone();
        params.set("symbolsAndTimeframes".into(), Value::Json(serde_json::Value::Array(vec![Value::Json(serde_json::Value::Array(vec![market.get(Value::from("symbol")).into(), timeframe.clone().into()])).into()])));
        return <Self as Woo>::unwatch_public(self, sub_hash.clone(), market.get(Value::from("symbol")), topic.clone(), params.clone()).await;
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic":"SPOT_BTC_USDT@kline_1m",
        //         "ts":1618822432146,
        //         "data":{
        //             "symbol":"SPOT_BTC_USDT",
        //             "type":"1m",
        //             "open":56948.97,
        //             "close":56891.76,
        //             "high":56948.97,
        //             "low":56889.06,
        //             "volume":44.00947568,
        //             "amount":2504584.9,
        //             "startTime":1618822380000,
        //             "endTime":1618822440000
        //         }
        //     }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut topic: Value = self.safe_value(message.clone(), Value::from("topic"), Value::Undefined);
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut interval: Value = self.safe_string(data.clone(), Value::from("type"), Value::Undefined);
        let mut timeframe: Value = self.find_timeframe(interval.clone(), Value::Undefined);
        let mut parsed: Value = Value::Json(serde_json::Value::Array(vec![self.safe_integer(data.clone(), Value::from("startTime"), Value::Undefined).into(), self.safe_float(data.clone(), Value::from("open"), Value::Undefined).into(), self.safe_float(data.clone(), Value::from("high"), Value::Undefined).into(), self.safe_float(data.clone(), Value::from("low"), Value::Undefined).into(), self.safe_float(data.clone(), Value::from("close"), Value::Undefined).into(), self.safe_float(data.clone(), Value::from("volume"), Value::Undefined).into()]));
        self.get("ohlcvs".into()).set(symbol.clone(), self.safe_value(self.get("ohlcvs".into()), symbol.clone(), Value::new_object()));
        let mut stored: Value = self.safe_value(self.get("ohlcvs".into()).get(symbol.clone()), timeframe.clone(), Value::Undefined);
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            stored = ArrayCacheByTimestamp::new(limit);
            self.get("ohlcvs".into()).get(symbol.clone()).set(timeframe.clone(), stored.clone());
        };
        stored.append(parsed.clone());
        client.resolve(stored.clone(), topic.clone());
        Value::Undefined
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut topic: Value = market.get(Value::from("id")) + Value::from("@trade");
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut trades: Value = <Self as Woo>::watch_public(self, topic.clone(), message.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(market.get(Value::from("symbol")), limit.clone());
        };
        return self.filter_by_symbol_since_limit(trades.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    async fn un_watch_trades(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut topic: Value = Value::from("trades");
        let mut sub_hash: Value = market.get(Value::from("id")) + Value::from("@trade");
        return <Self as Woo>::unwatch_public(self, sub_hash.clone(), market.get(Value::from("symbol")), topic.clone(), params.clone()).await;
    }

    fn handle_trade(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "topic":"SPOT_ADA_USDT@trade",
        //     "ts":1618820361552,
        //     "data":{
        //         "symbol":"SPOT_ADA_USDT",
        //         "price":1.27988,
        //         "size":300,
        //         "side":"BUY",
        //         "source":0
        //     }
        // }
        //
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut trade: Value = <Self as Woo>::parse_ws_trade(self, extend_2(data.clone(), Value::Json(normalize(&Value::Json(json!({
            "timestamp": timestamp
        }))).unwrap())), market.clone());
        let mut trades_array: Value = self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined);
        if trades_array.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            trades_array = ArrayCache::new(limit);
        };
        trades_array.append(trade.clone());
        self.get("trades".into()).set(symbol.clone(), trades_array.clone());
        client.resolve(trades_array.clone(), topic.clone());
        Value::Undefined
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        //     {
        //         "symbol":"SPOT_ADA_USDT",
        //         "timestamp":1618820361552,
        //         "price":1.27988,
        //         "size":300,
        //         "side":"BUY",
        //         "source":0
        //     }
        // private trade
        //    {
        //     "msgType": 0,  // execution report
        //     "symbol": "SPOT_BTC_USDT",
        //     "clientOrderId": 0,
        //     "orderId": 54774393,
        //     "type": "MARKET",
        //     "side": "BUY",
        //     "quantity": 0.0,
        //     "price": 0.0,
        //     "tradeId": 56201985,
        //     "executedPrice": 23534.06,
        //     "executedQuantity": 0.00040791,
        //     "fee": 2.1E-7,
        //     "feeAsset": "BTC",
        //     "totalExecutedQuantity": 0.00040791,
        //     "avgPrice": 23534.06,
        //     "status": "FILLED",
        //     "reason": "",
        //     "orderTag": "default",
        //     "totalFee": 2.1E-7,
        //     "feeCurrency": "BTC",
        //     "totalRebate": 0,
        //     "rebateCurrency": "USDT",
        //     "visible": 0.0,
        //     "timestamp": 1675406261689,
        //     "reduceOnly": false,
        //     "maker": false
        //   }
        //
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut price: Value = self.safe_string_2(trade.clone(), Value::from("executedPrice"), Value::from("price"), Value::Undefined);
        let mut amount: Value = self.safe_string_2(trade.clone(), Value::from("executedQuantity"), Value::from("size"), Value::Undefined);
        let mut cost: Value = Precise::string_mul(price.clone(), amount.clone());
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::from("side"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("timestamp"), Value::Undefined);
        let mut maker: Value = self.safe_bool(trade.clone(), Value::from("marker"), Value::Undefined);
        let mut taker_or_maker: Value = Value::Undefined;
        if maker.clone().is_nonnullish() {
            taker_or_maker = if maker.is_truthy() { Value::from("maker") } else { Value::from("taker") };
        };
        let mut r#type: Value = self.safe_string_lower(trade.clone(), Value::from("type"), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        let mut fee_cost: Value = self.safe_number(trade.clone(), Value::from("fee"), Value::Undefined);
        if fee_cost.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_cost,
                "currency": self.safe_currency_code(self.safe_string(trade.clone(), Value::from("feeCurrency"), Value::Undefined), Value::Undefined)
            }))).unwrap());
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(trade.clone(), Value::from("tradeId"), Value::Undefined),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol,
            "side": side,
            "price": price,
            "amount": amount,
            "cost": cost,
            "order": self.safe_string(trade.clone(), Value::from("orderId"), Value::Undefined),
            "takerOrMaker": taker_or_maker,
            "type": r#type,
            "fee": fee,
            "info": trade
        }))).unwrap()), market.clone());
    }

    fn check_required_uid(&mut self, mut error: Value) -> Value {
        error = error.or_default(true.into());
        if !self.get("uid".into()).is_truthy() {
            if error.is_truthy() {
                panic!(r###"AuthenticationError::new(self.get("id".into()) + Value::from(" requires `uid` credential (woox calls it `application_id`)"))"###);
            } else {
                return false.into();
            };
        };
        return true.into();
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.check_required_credentials(Value::Undefined);
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("uid".into());
        let mut client: Value = self.client(url.clone());
        let mut message_hash: Value = Value::from("authenticated");
        let mut event: Value = Value::from("auth");
        let mut future: Value = client.reusable_future(message_hash.clone());
        let mut authenticated: Value = self.safe_value(client.get(subscriptions.clone()), message_hash.clone(), Value::Undefined);
        if authenticated.clone().is_nullish() {
            let mut ts: Value = self.nonce().to_string();
            let mut auth: Value = Value::from("|") + ts.clone();
            let mut signature: Value = self.hmac(self.encode(auth.clone()), self.encode(self.get("secret".into())), sha256().clone(), Value::Undefined);
            let mut request: Value = Value::Json(normalize(&Value::Json(json!({
                "event": event,
                "params": Value::Json(normalize(&Value::Json(json!({
                    "apikey": self.get("apiKey".into()),
                    "sign": signature,
                    "timestamp": ts
                }))).unwrap())
            }))).unwrap());
            let mut message: Value = extend_2(request.clone(), params.clone());
            self.watch(url.clone(), message_hash.clone(), message.clone(), message_hash.clone(), message.clone());
        };
        return future.clone();
    }

    async fn watch_private(&mut self, mut message_hash: Value, mut message: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Woo>::authenticate(self, params.clone()).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("uid".into());
        let mut request_id: Value = <Self as Woo>::request_id(self, url.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id
        }))).unwrap());
        let mut request: Value = extend_2(subscribe.clone(), message.clone());
        return self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), subscribe.clone()).await;
    }

    async fn watch_private_multiple(&mut self, mut message_hashes: Value, mut message: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Woo>::authenticate(self, params.clone()).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("uid".into());
        let mut request_id: Value = <Self as Woo>::request_id(self, url.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "id": request_id
        }))).unwrap());
        let mut request: Value = extend_2(subscribe.clone(), message.clone());
        return self.watch_multiple(url.clone(), message_hashes.clone(), request.clone(), message_hashes.clone(), subscribe.clone()).await;
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut trigger: Value = self.safe_bool_2(params.clone(), Value::from("stop"), Value::from("trigger"), false.into());
        let mut topic: Value = if trigger.is_truthy() { Value::from("algoexecutionreportv2") } else { Value::from("executionreport") };
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stop").into(), Value::from("trigger").into()])));
        let mut message_hash: Value = topic.clone();
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
            message_hash = message_hash +  Value::from(":") + symbol.clone();
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut orders: Value = <Self as Woo>::watch_private(self, message_hash.clone(), message.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    async fn watch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut trigger: Value = self.safe_bool_2(params.clone(), Value::from("stop"), Value::from("trigger"), false.into());
        let mut topic: Value = if trigger.is_truthy() { Value::from("algoexecutionreportv2") } else { Value::from("executionreport") };
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stop").into(), Value::from("trigger").into()])));
        let mut message_hash: Value = Value::from("myTrades");
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
            message_hash = message_hash +  Value::from(":") + symbol.clone();
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        let mut trades: Value = <Self as Woo>::watch_private(self, message_hash.clone(), message.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(trades.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn parse_ws_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        //     {
        //         "symbol": "PERP_BTC_USDT",
        //         "clientOrderId": 0,
        //         "orderId": 52952826,
        //         "type": "LIMIT",
        //         "side": "SELL",
        //         "quantity": 0.01,
        //         "price": 22000,
        //         "tradeId": 0,
        //         "executedPrice": 0,
        //         "executedQuantity": 0,
        //         "fee": 0,
        //         "feeAsset": "USDT",
        //         "totalExecutedQuantity": 0,
        //         "status": "NEW",
        //         "reason": '',
        //         "orderTag": "default",
        //         "totalFee": 0,
        //         "visible": 0.01,
        //         "timestamp": 1657515556799,
        //         "reduceOnly": false,
        //         "maker": false
        //     }
        //     {
        //      "symbol": "SPOT_BTC_USDT",
        //      "rootAlgoOrderId": 2573778,
        //      "parentAlgoOrderId": 0,
        //      "algoOrderId": 2573778,
        //      "clientOrderId": 0,
        //      "orderTag": "default",
        //      "algoType": "STOP_LOSS",
        //      "side": "SELL",
        //      "quantity": 0.00011,
        //      "triggerPrice": 98566.67,
        //      "triggerStatus": "USELESS",
        //      "price": 0,
        //      "type": "MARKET",
        //      "triggerTradePrice": 0,
        //      "triggerTime": 0,
        //      "tradeId": 0,
        //      "executedPrice": 0,
        //      "executedQuantity": 0,
        //      "fee": 0,
        //      "reason": "",
        //      "feeAsset": "",
        //      "totalExecutedQuantity": 0,
        //      "averageExecutedPrice": 0,
        //      "totalFee": 0,
        //      "timestamp": 1761030467426,
        //      "visibleQuantity": 0,
        //      "reduceOnly": false,
        //      "triggerPriceType": "MARKET_PRICE",
        //      "positionSide": "BOTH",
        //      "feeCurrency": "",
        //      "totalRebate": 0.0,
        //      "rebateCurrency": "",
        //      "triggered": false,
        //      "maker": false,
        //      "activated": false,
        //      "isTriggered": false,
        //      "isMaker": false,
        //      "isActivated": false,
        //      "rootAlgoStatus": "NEW",
        //      "algoStatus": "NEW"
        // }
        //
        let mut order_id: Value = self.safe_string_2(order.clone(), Value::from("orderId"), Value::from("algoOrderId"), Value::Undefined);
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
        market = self.market(market_id.clone());
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut timestamp: Value = self.safe_integer(order.clone(), Value::from("timestamp"), Value::Undefined);
        let mut fee: Value = Value::Json(normalize(&Value::Json(json!({
            "cost": self.safe_string(order.clone(), Value::from("totalFee"), Value::Undefined),
            "currency": self.safe_string(order.clone(), Value::from("feeAsset"), Value::Undefined)
        }))).unwrap());
        let mut price_string: Value = self.safe_string(order.clone(), Value::from("price"), Value::Undefined);
        let mut price: Value = self.safe_number(order.clone(), Value::from("price"), Value::Undefined);
        let mut avg_price: Value = self.safe_number(order.clone(), Value::from("avgPrice"), Value::Undefined);
        if Precise::string_eq(price_string.clone(), Value::from("0")) && avg_price.clone().is_nonnullish() {
            price = avg_price.clone();
        };
        let mut amount: Value = self.safe_float(order.clone(), Value::from("quantity"), Value::Undefined);
        let mut side: Value = self.safe_string_lower(order.clone(), Value::from("side"), Value::Undefined);
        let mut r#type: Value = self.safe_string_lower(order.clone(), Value::from("type"), Value::Undefined);
        let mut filled: Value = self.safe_number(order.clone(), Value::from("totalExecutedQuantity"), Value::Undefined);
        let mut total_exec_quantity: Value = self.safe_float(order.clone(), Value::from("totalExecutedQuantity"), Value::Undefined);
        let mut remaining: Value = amount.clone();
        if amount.clone() >= total_exec_quantity.clone() {
            remaining = remaining -  total_exec_quantity.clone();
        };
        let mut raw_status: Value = self.safe_string_2(order.clone(), Value::from("status"), Value::from("algoStatus"), Value::Undefined);
        let mut status: Value = <Self as Woo>::parse_order_status(self, raw_status.clone());
        let mut trades: Value = Value::Undefined;
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("clientOrderId"), Value::Undefined);
        let mut trigger_price: Value = self.safe_string(order.clone(), Value::from("triggerPrice"), Value::Undefined);
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "symbol": symbol,
            "id": order_id,
            "clientOrderId": client_order_id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": timestamp,
            "type": r#type,
            "timeInForce": Value::Undefined,
            "postOnly": Value::Undefined,
            "side": side,
            "price": price,
            "stopPrice": trigger_price,
            "triggerPrice": trigger_price,
            "reduceOnly": self.safe_bool(order.clone(), Value::from("reduceOnly"), Value::Undefined),
            "amount": amount,
            "cost": Value::Undefined,
            "average": avg_price,
            "filled": filled,
            "remaining": remaining,
            "status": status,
            "fee": fee,
            "trades": trades
        }))).unwrap()), Value::Undefined);
    }

    fn handle_order_update(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "topic": "executionreport",
        //         "ts": 1657515556799,
        //         "data": {
        //             "symbol": "PERP_BTC_USDT",
        //             "clientOrderId": 0,
        //             "orderId": 52952826,
        //             "type": "LIMIT",
        //             "side": "SELL",
        //             "quantity": 0.01,
        //             "price": 22000,
        //             "tradeId": 0,
        //             "executedPrice": 0,
        //             "executedQuantity": 0,
        //             "fee": 0,
        //             "feeAsset": "USDT",
        //             "totalExecutedQuantity": 0,
        //             "status": "NEW",
        //             "reason": '',
        //             "orderTag": "default",
        //             "totalFee": 0,
        //             "visible": 0.01,
        //             "timestamp": 1657515556799,
        //             "reduceOnly": false,
        //             "maker": false
        //         }
        //     }
        //
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        if Array::is_array(data.clone()).is_truthy() {
            // algoexecutionreportv2
            let mut i: usize = 0;
            while i < data.len() {
                let mut order: Value = data.get(i.into());
                let mut trade_id: Value = self.omit_zero(self.safe_string(data.clone(), Value::from("tradeId"), Value::Undefined));
                if trade_id.clone().is_nonnullish() {
                    <Self as Woo>::handle_my_trade(self, client.clone(), order.clone());
                };
                <Self as Woo>::handle_order(self, client.clone(), order.clone(), topic.clone());
                i += 1;
            };
        } else {
            // executionreport
            let mut trade_id: Value = self.omit_zero(self.safe_string(data.clone(), Value::from("tradeId"), Value::Undefined));
            if trade_id.clone().is_nonnullish() {
                <Self as Woo>::handle_my_trade(self, client.clone(), data.clone());
            };
            <Self as Woo>::handle_order(self, client.clone(), data.clone(), topic.clone());
        };
        Value::Undefined
    }

    fn handle_order(&mut self, mut client: Value, mut message: Value, mut topic: Value) -> Value {
        let mut parsed: Value = <Self as Woo>::parse_ws_order(self, message.clone(), Value::Undefined);
        let mut symbol: Value = self.safe_string(parsed.clone(), Value::from("symbol"), Value::Undefined);
        let mut order_id: Value = self.safe_string(parsed.clone(), Value::from("id"), Value::Undefined);
        if symbol.clone().is_nonnullish() {
            if self.get("orders".into()).is_nullish() {
                let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
                self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
            };
            let mut cached_orders: Value = self.get("orders".into());
            let mut orders: Value = self.safe_value(cached_orders.get(hashmap.clone()), symbol.clone(), Value::new_object());
            let mut order: Value = self.safe_value(orders.clone(), order_id.clone(), Value::Undefined);
            if order.clone().is_nonnullish() {
                let mut fee: Value = self.safe_value(order.clone(), Value::from("fee"), Value::Undefined);
                if fee.clone().is_nonnullish() {
                    parsed.set("fee".into(), fee.clone());
                };
                let mut fees: Value = self.safe_value(order.clone(), Value::from("fees"), Value::Undefined);
                if fees.clone().is_nonnullish() {
                    parsed.set("fees".into(), fees.clone());
                };
                parsed.set("trades".into(), self.safe_value(order.clone(), Value::from("trades"), Value::Undefined));
                parsed.set("timestamp".into(), self.safe_integer(order.clone(), Value::from("timestamp"), Value::Undefined));
                parsed.set("datetime".into(), self.safe_string(order.clone(), Value::from("datetime"), Value::Undefined));
            };
            cached_orders.append(parsed.clone());
            client.resolve(self.get("orders".into()), topic.clone());
            let mut message_hash_symbol: Value = topic.clone() + Value::from(":") + symbol.clone();
            client.resolve(self.get("orders".into()), message_hash_symbol.clone());
        };
        Value::Undefined
    }

    fn handle_my_trade(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //     "msgType": 0,  // execution report
        //     "symbol": "SPOT_BTC_USDT",
        //     "clientOrderId": 0,
        //     "orderId": 54774393,
        //     "type": "MARKET",
        //     "side": "BUY",
        //     "quantity": 0.0,
        //     "price": 0.0,
        //     "tradeId": 56201985,
        //     "executedPrice": 23534.06,
        //     "executedQuantity": 0.00040791,
        //     "fee": 2.1E-7,
        //     "feeAsset": "BTC",
        //     "totalExecutedQuantity": 0.00040791,
        //     "avgPrice": 23534.06,
        //     "status": "FILLED",
        //     "reason": "",
        //     "orderTag": "default",
        //     "totalFee": 2.1E-7,
        //     "feeCurrency": "BTC",
        //     "totalRebate": 0,
        //     "rebateCurrency": "USDT",
        //     "visible": 0.0,
        //     "timestamp": 1675406261689,
        //     "reduceOnly": false,
        //     "maker": false
        //   }
        //
        let mut my_trades: Value = self.get("myTrades".into());
        if my_trades.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            my_trades = ArrayCacheBySymbolById::new(limit);
        };
        let mut trade: Value = <Self as Woo>::parse_ws_trade(self, message.clone(), Value::Undefined);
        my_trades.append(trade.clone());
        let mut message_hash: Value = Value::from("myTrades:") + trade.get(Value::from("symbol"));
        client.resolve(my_trades.clone(), message_hash.clone());
        message_hash = Value::from("myTrades");
        client.resolve(my_trades.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_positions(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut message_hashes: Value = Value::new_array();
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        if !self.is_empty(symbols.clone()).is_truthy() {
            let mut i: usize = 0;
            while i < symbols.len() {
                let mut symbol: Value = symbols.get(i.into());
                message_hashes.push(Value::from("positions::") + symbol.clone());
                i += 1;
            };
        } else {
            message_hashes.push(Value::from("positions"));
        };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private")) + Value::from("/") + self.get("uid".into());
        let mut client: Value = self.client(url.clone());
        <Self as Woo>::set_positions_cache(self, client.clone(), symbols.clone());
        let mut fetch_positions_snapshot: Value = self.handle_option(Value::from("watchPositions"), Value::from("fetchPositionsSnapshot"), true.into());
        let mut await_positions_snapshot: Value = self.handle_option(Value::from("watchPositions"), Value::from("awaitPositionsSnapshot"), true.into());
        if fetch_positions_snapshot.is_truthy() && await_positions_snapshot.is_truthy() && self.get("positions".into()).is_nullish() {
            let mut snapshot: Value = client.future(Value::from("fetchPositionsSnapshot")).await;
            return self.filter_by_symbols_since_limit(snapshot.clone(), symbols.clone(), since.clone(), limit.clone(), true.into());
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": "position"
        }))).unwrap());
        let mut new_positions: Value = <Self as Woo>::watch_private_multiple(self, message_hashes.clone(), request.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            return new_positions.clone();
        };
        return self.filter_by_symbols_since_limit(self.get("positions".into()), symbols.clone(), since.clone(), limit.clone(), true.into());
    }

    fn set_positions_cache(&mut self, mut client: Value, mut r#type: Value, mut symbols: Value) -> Value {
        let mut fetch_positions_snapshot: Value = self.handle_option(Value::from("watchPositions"), Value::from("fetchPositionsSnapshot"), false.into());
        if fetch_positions_snapshot.is_truthy() {
            let mut message_hash: Value = Value::from("fetchPositionsSnapshot");
            if !client.get(futures.clone()).contains_key(message_hash.clone()) {
                client.future(message_hash.clone());
                self.spawn(self.get("loadPositionsSnapshot".into()), client.clone(), message_hash.clone());
            };
        } else {
            self.set("positions".into(), ArrayCacheBySymbolBySide::new());
        };
        Value::Undefined
    }

    async fn load_positions_snapshot(&mut self, mut client: Value, mut message_hash: Value) -> Value {
        let mut positions: Value = self.fetch_positions(Value::Undefined, Value::Undefined).await;
        self.set("positions".into(), ArrayCacheBySymbolBySide::new());
        let mut cache: Value = self.get("positions".into());
        let mut i: usize = 0;
        while i < positions.len() {
            let mut position: Value = positions.get(i.into());
            let mut contracts: Value = self.safe_number(position.clone(), Value::from("contracts"), Value::from(0));
            if contracts.clone() > Value::from(0) {
                cache.append(position.clone());
            };
            i += 1;
        };
        // don't remove the future from the .futures cache
        if client.get(futures.clone()).contains_key(message_hash.clone()) {
            let mut future: Value = client.get(futures.clone()).get(message_hash.clone());
            future.resolve(cache.clone());
            client.resolve(cache.clone(), Value::from("positions"));
        };
        Value::Undefined
    }

    fn handle_positions(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //    {
        //        "topic":"position",
        //        "ts":1705292345255,
        //        "data":{
        //           "positions":{
        //              "PERP_LTC_USDT":{
        //                 "holding":1,
        //                 "pendingLongQty":0,
        //                 "pendingShortQty":0,
        //                 "averageOpenPrice":71.53,
        //                 "pnl24H":0,
        //                 "fee24H":0.07153,
        //                 "settlePrice":71.53,
        //                 "markPrice":71.32098452065145,
        //                 "version":7886,
        //                 "openingTime":1705292304267,
        //                 "pnl24HPercentage":0,
        //                 "adlQuantile":1,
        //                 "positionSide":"BOTH"
        //              }
        //           }
        //        }
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::new_object());
        let mut raw_positions: Value = self.safe_value(data.clone(), Value::from("positions"), Value::new_object());
        let mut postitions_ids: Value = raw_positions.clone().keys();
        if self.get("positions".into()).is_nullish() {
            self.set("positions".into(), ArrayCacheBySymbolBySide::new());
        };
        let mut cache: Value = self.get("positions".into());
        let mut new_positions: Value = Value::new_array();
        let mut i: usize = 0;
        while i < postitions_ids.len() {
            let mut market_id: Value = postitions_ids.get(i.into());
            let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            let mut raw_position: Value = raw_positions.get(market_id.clone());
            let mut position: Value = self.parse_position(raw_position.clone(), market.clone());
            new_positions.push(position.clone());
            cache.append(position.clone());
            let mut message_hash: Value = Value::from("positions::") + market.get(Value::from("symbol"));
            client.resolve(position.clone(), message_hash.clone());
            i += 1;
        };
        client.resolve(new_positions.clone(), Value::from("positions"));
        Value::Undefined
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut topic: Value = Value::from("balance");
        let mut message_hash: Value = topic.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "topic": topic
        }))).unwrap());
        let mut message: Value = extend_2(request.clone(), params.clone());
        return <Self as Woo>::watch_private(self, message_hash.clone(), message.clone()).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //   {
        //       "topic": "balance",
        //       "ts": 1695716888789,
        //       "data": {
        //          "balances": {
        //             "USDT": {
        //                "holding": 266.56059176,
        //                "frozen": 0,
        //                "interest": 0,
        //                "pendingShortQty": 0,
        //                "pendingExposure": 0,
        //                "pendingLongQty": 0,
        //                "pendingLongExposure": 0,
        //                "version": 37,
        //                "staked": 0,
        //                "unbonding": 0,
        //                "vault": 0,
        //                "averageOpenPrice": 0,
        //                "pnl24H": 0,
        //                "fee24H": 0,
        //                "markPrice": 1,
        //                "pnl24HPercentage": 0
        //             }
        //          }
        //
        //    }
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from("data"), Value::Undefined);
        let mut balances: Value = self.safe_value(data.clone(), Value::from("balances"), Value::Undefined);
        let mut keys: Value = balances.clone().keys();
        let mut ts: Value = self.safe_integer(message.clone(), Value::from("ts"), Value::Undefined);
        self.get("balance".into()).set("info".into(), data.clone());
        self.get("balance".into()).set("timestamp".into(), ts.clone());
        self.get("balance".into()).set("datetime".into(), self.iso8601(ts.clone()));
        let mut i: usize = 0;
        while i < keys.len() {
            let mut key: Value = keys.get(i.into());
            let mut value: Value = balances.get(key.clone());
            let mut code: Value = self.safe_currency_code(key.clone(), Value::Undefined);
            let mut account: Value = if self.get("balance".into()).contains_key(code.clone()) { self.get("balance".into()).get(code.clone()) } else { self.account() };
            let mut total: Value = self.safe_string(value.clone(), Value::from("holding"), Value::Undefined);
            let mut used: Value = self.safe_string(value.clone(), Value::from("frozen"), Value::Undefined);
            account.set("total".into(), total.clone());
            account.set("used".into(), used.clone());
            account.set("free".into(), Precise::string_sub(total.clone(), used.clone()));
            self.get("balance".into()).set(code.clone(), account.clone());
            i += 1;
        };
        self.set("balance".into(), self.safe_balance(self.get("balance".into())));
        client.resolve(self.get("balance".into()), Value::from("balance"));
        Value::Undefined
    }

    fn handle_error_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {"id":"1","event":"subscribe","success":false,"ts":1710780997216,"errorMsg":"Auth is needed."}
        //
        if !message.contains_key(Value::from("success")) {
            return false.into();
        };
        let mut success: Value = self.safe_bool(message.clone(), Value::from("success"), Value::Undefined);
        if success.is_truthy() {
            return false.into();
        };
        let mut error_message: Value = self.safe_string(message.clone(), Value::from("errorMsg"), Value::Undefined);
                if error_message.clone().is_nonnullish() {
            let mut feedback: Value = self.get("id".into()) + Value::from(" ") + self.json(message.clone());
            self.throw_exactly_matched_exception(self.get("exceptions".into()).get(Value::from("exact")), error_message.clone(), feedback.clone());
        };
        return false.into();
        // catch block omitted (no exception support in Value runtime)
;
        Value::Undefined
    }

    fn handle_un_subscription(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "id": "2",
        //         "event": "unsubscribe",
        //         "success": true,
        //         "ts": 1759568478343,
        //         "data": "SPOT_BTC_USDT@orderbook"
        //     }
        //
        let mut subscribe_hash: Value = self.safe_string(message.clone(), Value::from("data"), Value::Undefined);
        let mut unsubscribe_hash: Value = Value::from("unsubscribe::") + subscribe_hash.clone();
        let mut subscription: Value = self.safe_dict(client.get(subscriptions.clone()), unsubscribe_hash.clone(), Value::new_object());
        let mut sub_message_hashes: Value = self.safe_list(subscription.clone(), Value::from("subMessageHashes"), Value::new_array());
        let mut unsub_message_hashes: Value = self.safe_list(subscription.clone(), Value::from("unsubMessageHashes"), Value::new_array());
        let mut i: usize = 0;
        while i < sub_message_hashes.len() {
            let mut sub_hash: Value = sub_message_hashes.get(i.into());
            let mut unsub_hash: Value = unsub_message_hashes.get(i.into());
            self.clean_unsubscription(client.clone(), sub_hash.clone(), unsub_hash.clone(), Value::Undefined);
            i += 1;
        };
        self.clean_cache(subscription.clone());
        Value::Undefined
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        if <Self as Woo>::handle_error_message(self, client.clone(), message.clone()).is_truthy() {
            return Value::Undefined;
        };
        let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
            "ping": self.get("handlePing".into()),
            "pong": self.get("handlePong".into()),
            "subscribe": self.get("handleSubscribe".into()),
            "unsubscribe": self.get("handleUnSubscription".into()),
            "orderbook": self.get("handleOrderBook".into()),
            "orderbookupdate": self.get("handleOrderBook".into()),
            "ticker": self.get("handleTicker".into()),
            "tickers": self.get("handleTickers".into()),
            "kline": self.get("handleOHLCV".into()),
            "auth": self.get("handleAuth".into()),
            "executionreport": self.get("handleOrderUpdate".into()),
            "algoexecutionreportv2": self.get("handleOrderUpdate".into()),
            "trade": self.get("handleTrade".into()),
            "balance": self.get("handleBalance".into()),
            "position": self.get("handlePositions".into()),
            "bbos": self.get("handleBidAsk".into())
        }))).unwrap());
        let mut event: Value = self.safe_string(message.clone(), Value::from("event"), Value::Undefined);
        let mut method: Value = self.safe_value(methods.clone(), event.clone(), Value::Undefined);
        if method.clone().is_nonnullish() {
            method.call(self, client.clone(), message.clone());
            return Value::Undefined;
        };
        let mut topic: Value = self.safe_string(message.clone(), Value::from("topic"), Value::Undefined);
        if topic.clone().is_nonnullish() {
            method = self.safe_value(methods.clone(), topic.clone(), Value::Undefined);
            if method.clone().is_nonnullish() {
                method.call(self, client.clone(), message.clone());
                return Value::Undefined;
            };
            let mut split_topic: Value = topic.split(Value::from("@"));
            let mut split_length: usize = split_topic.len();
            if split_length.clone() == Value::from(2) {
                let mut name: Value = self.safe_string(split_topic.clone(), Value::from(1), Value::Undefined);
                method = self.safe_value(methods.clone(), name.clone(), Value::Undefined);
                if method.clone().is_nonnullish() {
                    method.call(self, client.clone(), message.clone());
                    return Value::Undefined;
                };
                let mut split_name: Value = name.split(Value::from("_"));
                let mut split_name_length: usize = split_topic.len();
                if split_name_length.clone() == Value::from(2) {
                    method = self.safe_value(methods.clone(), self.safe_string(split_name.clone(), Value::from(0), Value::Undefined), Value::Undefined);
                    if method.clone().is_nonnullish() {
                        method.call(self, client.clone(), message.clone());
                    };
                };
            };
        };
        Value::Undefined
    }

    fn ping(&mut self, mut client: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "event": "ping"
        }))).unwrap());
    }

    fn handle_ping(&mut self, mut client: Value, mut message: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "event": "pong"
        }))).unwrap());
    }

    fn handle_pong(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // { event: "pong", ts: 1657117026090 }
        //
        client.set("last_pong".into(), self.milliseconds());
        return message.clone();
    }

    fn handle_subscribe(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "id": "666888",
        //         "event": "subscribe",
        //         "success": true,
        //         "ts": 1657117712212
        //     }
        //
        let mut id: Value = self.safe_string(message.clone(), Value::from("id"), Value::Undefined);
        let mut subscriptions_by_id: Value = self.index_by(client.get(subscriptions.clone()), Value::from("id"));
        let mut subscription: Value = self.safe_value(subscriptions_by_id.clone(), id.clone(), Value::new_object());
        let mut method: Value = self.safe_value(subscription.clone(), Value::from("method"), Value::Undefined);
        if method.clone().is_nonnullish() {
            method.call(self, client.clone(), message.clone(), subscription.clone());
        };
        return message.clone();
    }

    fn handle_auth(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "event": "auth",
        //         "success": true,
        //         "ts": 1657463158812
        //     }
        //
        let mut message_hash: Value = Value::from("authenticated");
        let mut success: Value = self.safe_value(message.clone(), Value::from("success"), Value::Undefined);
        if success.is_truthy() {
            // client.resolve (message, messageHash);
            let mut future: Value = self.safe_value(client.get(futures.clone()), Value::from("authenticated"), Value::Undefined);
            future.resolve(true.into());
        } else {
            let mut error: Value = AuthenticationError::new(self.json(message.clone()));
            client.reject(error.clone(), message_hash.clone());
            // allows further authentication attempts
            if client.get(subscriptions.clone()).contains_key(message_hash.clone()) {
                client.get(subscriptions.clone()).get(Value::from("authenticated"));
            };
        };
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "v1PubGetHistkline" => self.request("hist/kline".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PubGetHisttrades" => self.request("hist/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetInfo" => self.request("info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetInfosymbol" => self.request("info/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSysteminfo" => self.request("system_info".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetMarkettrades" => self.request("market_trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetToken" => self.request("token".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetTokennetwork" => self.request("token_network".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetFundingrates" => self.request("funding_rates".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetFundingratesymbol" => self.request("funding_rate/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetFundingratehistory" => self.request("funding_rate_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetFutures" => self.request("futures".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetFuturessymbol" => self.request("futures/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetOrderbooksymbol" => self.request("orderbook/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetKline" => self.request("kline".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClienttoken" => self.request("client/token".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetOrderoid" => self.request("order/{oid}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientorderclientorderid" => self.request("client/order/{client_order_id}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetOrders" => self.request("orders".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClienttradetid" => self.request("client/trade/{tid}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetOrderoidtrades" => self.request("order/{oid}/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClienttrades" => self.request("client/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClienthisttrades" => self.request("client/hist_trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetStakingyieldhistory" => self.request("staking/yield_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientholding" => self.request("client/holding".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetAssetdeposit" => self.request("asset/deposit".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetAssethistory" => self.request("asset/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetSubaccountall" => self.request("sub_account/all".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetSubaccountassets" => self.request("sub_account/assets".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetSubaccountassetdetail" => self.request("sub_account/asset_detail".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetSubaccountiprestriction" => self.request("sub_account/ip_restriction".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetAssetmainsubtransferhistory" => self.request("asset/main_sub_transfer_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetTokeninterest" => self.request("token_interest".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetTokeninteresttoken" => self.request("token_interest/{token}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetInteresthistory" => self.request("interest/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetInterestrepay" => self.request("interest/repay".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetFundingfeehistory" => self.request("funding_fee/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetPositions" => self.request("positions".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetPositionsymbol" => self.request("position/{symbol}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClienttransactionhistory" => self.request("client/transaction_history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetClientfuturesleverage" => self.request("client/futures_leverage".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostOrder" => self.request("order".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostOrdercancelallafter" => self.request("order/cancel_all_after".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostAssetltv" => self.request("asset/ltv".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostAssetinternalwithdraw" => self.request("asset/internal_withdraw".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostInterestrepay" => self.request("interest/repay".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientaccountmode" => self.request("client/account_mode".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientpositionmode" => self.request("client/position_mode".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientleverage" => self.request("client/leverage".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientfuturesleverage" => self.request("client/futures_leverage".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostClientisolatedmargin" => self.request("client/isolated_margin".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteOrder" => self.request("order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteClientorder" => self.request("client/order".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteOrders" => self.request("orders".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteAssetwithdraw" => self.request("asset/withdraw".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2PrivateGetClientholding" => self.request("client/holding".into(), "v2".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetSysteminfo" => self.request("systemInfo".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetInstruments" => self.request("instruments".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetToken" => self.request("token".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetTokennetwork" => self.request("tokenNetwork".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetTokeninfo" => self.request("tokenInfo".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarkettrades" => self.request("marketTrades".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarkettradeshistory" => self.request("marketTradesHistory".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetOrderbook" => self.request("orderbook".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetKline" => self.request("kline".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetKlinehistory" => self.request("klineHistory".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetFutures" => self.request("futures".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetFundingrate" => self.request("fundingRate".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetFundingratehistory" => self.request("fundingRateHistory".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetInsurancefund" => self.request("insuranceFund".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTradeorder" => self.request("trade/order".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTradeorders" => self.request("trade/orders".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTradealgoorder" => self.request("trade/algoOrder".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTradealgoorders" => self.request("trade/algoOrders".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTradetransaction" => self.request("trade/transaction".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTradetransactionhistory" => self.request("trade/transactionHistory".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTradetradingfee" => self.request("trade/tradingFee".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccountinfo" => self.request("account/info".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccounttokenconfig" => self.request("account/tokenConfig".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccountsymbolconfig" => self.request("account/symbolConfig".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccountsubaccountsall" => self.request("account/subAccounts/all".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccountreferralsummary" => self.request("account/referral/summary".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccountreferralrewardhistory" => self.request("account/referral/rewardHistory".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccountcredentials" => self.request("account/credentials".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAssetbalances" => self.request("asset/balances".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAssettokenhistory" => self.request("asset/token/history".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAssettransferhistory" => self.request("asset/transfer/history".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAssetwallethistory" => self.request("asset/wallet/history".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAssetwalletdeposit" => self.request("asset/wallet/deposit".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAssetstakingyieldhistory" => self.request("asset/staking/yieldHistory".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetFuturespositions" => self.request("futures/positions".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetFuturesleverage" => self.request("futures/leverage".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetFuturesdefaultmarginmode" => self.request("futures/defaultMarginMode".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetFuturesfundingfeehistory" => self.request("futures/fundingFee/history".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetSpotmargininterestrate" => self.request("spotMargin/interestRate".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetSpotmargininteresthistory" => self.request("spotMargin/interestHistory".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetSpotmarginmaxmargin" => self.request("spotMargin/maxMargin".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAlgoorderoid" => self.request("algo/order/{oid}".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAlgoorders" => self.request("algo/orders".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetPositions" => self.request("positions".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetBuypower" => self.request("buypower".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetConvertexchangeinfo" => self.request("convert/exchangeInfo".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetConvertassetinfo" => self.request("convert/assetInfo".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetConvertrfq" => self.request("convert/rfq".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetConverttrade" => self.request("convert/trade".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetConverttrades" => self.request("convert/trades".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostTradeorder" => self.request("trade/order".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostTradealgoorder" => self.request("trade/algoOrder".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostTradecancelallafter" => self.request("trade/cancelAllAfter".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostAccounttradingmode" => self.request("account/tradingMode".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostAccountlistenkey" => self.request("account/listenKey".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostAssettransfer" => self.request("asset/transfer".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostAssetwalletwithdraw" => self.request("asset/wallet/withdraw".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostSpotmarginleverage" => self.request("spotMargin/leverage".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostSpotmargininterestrepay" => self.request("spotMargin/interestRepay".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostAlgoorder" => self.request("algo/order".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostConvertrft" => self.request("convert/rft".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutTradeorder" => self.request("trade/order".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutTradealgoorder" => self.request("trade/algoOrder".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutFuturesleverage" => self.request("futures/leverage".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutFuturespositionmode" => self.request("futures/positionMode".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutOrderoid" => self.request("order/{oid}".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutOrderclientclientorderid" => self.request("order/client/{client_order_id}".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutAlgoorderoid" => self.request("algo/order/{oid}".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutAlgoorderclientclientorderid" => self.request("algo/order/client/{client_order_id}".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteTradeorder" => self.request("trade/order".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteTradeorders" => self.request("trade/orders".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteTradealgoorder" => self.request("trade/algoOrder".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteTradealgoorders" => self.request("trade/algoOrders".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteTradeallorders" => self.request("trade/allOrders".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteAlgoorderorderid" => self.request("algo/order/{order_id}".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteAlgoorderspending" => self.request("algo/orders/pending".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteAlgoorderspendingsymbol" => self.request("algo/orders/pending/{symbol}".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateDeleteOrderspending" => self.request("orders/pending".into(), "v3".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct WooImpl(Value);
impl Exchange for WooImpl {}
impl Woo for WooImpl {}
impl ValueTrait for WooImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl WooImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = WooImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Woo::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


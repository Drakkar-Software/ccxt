#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Backpack : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "backpack",
            "name": "Backpack",
            "countries": [
                "JP"
            ],
            "enableRateLimit": true,
            "rateLimit": 50,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "sandbox": false,
                "spot": true,
                "margin": true,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "cancelOrders": false,
                "closePosition": false,
                "createDepositAddress": false,
                "createLimitBuyOrder": true,
                "createLimitOrder": true,
                "createLimitSellOrder": true,
                "createMarketBuyOrder": true,
                "createMarketBuyOrderWithCost": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": true,
                "createMarketSellOrder": true,
                "createMarketSellOrderWithCost": true,
                "createOrder": true,
                "createOrders": true,
                "createOrderWithTakeProfitAndStopLoss": true,
                "createPostOnlyOrder": true,
                "createReduceOnlyOrder": true,
                "createStopLossOrder": false,
                "createTakeProfitOrder": false,
                "createTrailingAmountOrder": false,
                "createTrailingPercentOrder": false,
                "createTriggerOrder": true,
                "editOrder": "emulated",
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchCanceledAndClosedOrders": false,
                "fetchCanceledOrders": false,
                "fetchClosedOrder": false,
                "fetchClosedOrders": false,
                "fetchConvertCurrencies": false,
                "fetchConvertQuote": false,
                "fetchConvertTrade": false,
                "fetchConvertTradeHistory": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": false,
                "fetchDepositWithdrawFees": false,
                "fetchFundingHistory": true,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": true,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIndexOHLCV": true,
                "fetchMarginAdjustmentHistory": false,
                "fetchL2OrderBook": true,
                "fetchLedger": false,
                "fetchLeverage": false,
                "fetchLeverageTiers": false,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": true,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterest": true,
                "fetchOpenInterestHistory": true,
                "fetchOpenOrder": true,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": false,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchOrderTrades": false,
                "fetchPosition": false,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPositionsForSymbol": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": false,
                "fetchTransactions": false,
                "fetchTransfers": false,
                "fetchVolatilityHistory": false,
                "fetchWithdrawals": true,
                "reduceMargin": false,
                "setLeverage": false,
                "setMargin": false,
                "setMarginMode": false,
                "setPositionMode": false,
                "transfer": false,
                "watchBalance": false,
                "watchMyTrades": false,
                "watchOHLCV": true,
                "watchOHLCVForSymbols": true,
                "watchOrderBook": true,
                "watchBidsAsks": true,
                "watchOrderBookForSymbols": true,
                "watchOrders": true,
                "watchPositions": true,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": true,
                "unWatchOrders": true,
                "unWatchTrades": true,
                "unWatchTradesForSymbols": true,
                "unWatchPositions": true,
                "withdraw": true,
                "ws": true,
                "cancelAllOrdersAfter": false,
                "cancelWithdraw": false,
                "createConvertTrade": false,
                "fetchAllGreeks": false,
                "unwatchBidsAsks": true,
                "unwatchOHLCV": true,
                "unwatchOHLCVForSymbols": true,
                "unwatchOrderBook": true,
                "unwatchOrderBookForSymbols": true,
                "unwatchTicker": true,
                "unwatchTickers": true
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/cc04c278-679f-4554-9f72-930dd632b80f",
                "api": {
                    "public": "https://api.backpack.exchange",
                    "private": "https://api.backpack.exchange",
                    "ws": {
                        "public": "wss://ws.backpack.exchange",
                        "private": "wss://ws.backpack.exchange"
                    }
                },
                "www": "https://backpack.exchange/",
                "doc": "https://docs.backpack.exchange/",
                "referral": "https://backpack.exchange/join/ccxt"
            },
            "api": {
                "public": {
                    "get": {
                        "api/v1/assets": 1,
                        "api/v1/collateral": 1,
                        "api/v1/borrowLend/markets": 1,
                        "api/v1/borrowLend/markets/history": 1,
                        "api/v1/markets": 1,
                        "api/v1/market": 1,
                        "api/v1/ticker": 1,
                        "api/v1/tickers": 1,
                        "api/v1/depth": 1,
                        "api/v1/klines": 1,
                        "api/v1/markPrices": 1,
                        "api/v1/openInterest": 1,
                        "api/v1/fundingRates": 1,
                        "api/v1/status": 1,
                        "api/v1/ping": 1,
                        "api/v1/time": 1,
                        "api/v1/wallets": 1,
                        "api/v1/trades": 1,
                        "api/v1/trades/history": 1
                    }
                },
                "private": {
                    "get": {
                        "api/v1/account": 1,
                        "api/v1/account/limits/borrow": 1,
                        "api/v1/account/limits/order": 1,
                        "api/v1/account/limits/withdrawal": 1,
                        "api/v1/borrowLend/positions": 1,
                        "api/v1/capital": 1,
                        "api/v1/capital/collateral": 1,
                        "wapi/v1/capital/deposits": 1,
                        "wapi/v1/capital/deposit/address": 1,
                        "wapi/v1/capital/withdrawals": 1,
                        "api/v1/position": 1,
                        "wapi/v1/history/borrowLend": 1,
                        "wapi/v1/history/interest": 1,
                        "wapi/v1/history/borrowLend/positions": 1,
                        "wapi/v1/history/dust": 1,
                        "wapi/v1/history/fills": 1,
                        "wapi/v1/history/funding": 1,
                        "wapi/v1/history/orders": 1,
                        "wapi/v1/history/rfq": 1,
                        "wapi/v1/history/quote": 1,
                        "wapi/v1/history/settlement": 1,
                        "wapi/v1/history/strategies": 1,
                        "api/v1/order": 1,
                        "api/v1/orders": 1
                    },
                    "post": {
                        "api/v1/account/convertDust": 1,
                        "api/v1/borrowLend": 1,
                        "wapi/v1/capital/withdrawals": 1,
                        "api/v1/order": 1,
                        "api/v1/orders": 1,
                        "api/v1/rfq": 1,
                        "api/v1/rfq/accept": 1,
                        "api/v1/rfq/refresh": 1,
                        "api/v1/rfq/cancel": 1,
                        "api/v1/rfq/quote": 1
                    },
                    "delete": {
                        "api/v1/order": 1,
                        "api/v1/orders": 1
                    },
                    "patch": {
                        "api/v1/account": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "15": "15m",
                "30": "30m",
                "1m": "1m",
                "3m": "3m",
                "5m": "5m",
                "1h": "1h",
                "2h": "2h",
                "4h": "4h",
                "6h": "6h",
                "8h": "8h",
                "12h": "12h",
                "1d": "1d",
                "3d": "3d",
                "1w": "1w",
                "1M": "1month"
            },
            "fees": {
                "trading": {},
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "features": {
                "default": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerDirection": false,
                        "stopLossPrice": true,
                        "takeProfitPrice": true,
                        "timeInForce": {
                            "GTC": true,
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyByCost": true,
                        "marketBuyRequiresPrice": true,
                        "selfTradePrevention": {
                            "EXPIRE_MAKER": true,
                            "EXPIRE_TAKER": true,
                            "EXPIRE_BOTH": true,
                            "NONE": false
                        },
                        "iceberg": false
                    },
                    "createOrders": {
                        "max": 20
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 1000,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOHLCV": {
                        "paginate": false,
                        "limit": 1000
                    }
                },
                "spot": {
                    "extends": "default"
                },
                "swap": {},
                "future": {}
            },
            "options": {
                "instructions": {
                    "api/v1/account": {
                        "GET": "accountQuery",
                        "PATCH": "accountUpdate"
                    },
                    "api/v1/capital": {
                        "GET": "balanceQuery"
                    },
                    "api/v1/account/limits/borrow": {
                        "GET": "maxBorrowQuantity"
                    },
                    "api/v1/account/limits/order": {
                        "GET": "maxOrderQuantity"
                    },
                    "api/v1/account/limits/withdrawal": {
                        "GET": "maxWithdrawalQuantity"
                    },
                    "api/v1/borrowLend/positions": {
                        "GET": "borrowLendPositionQuery"
                    },
                    "api/v1/borrowLend": {
                        "POST": "borrowLendExecute"
                    },
                    "wapi/v1/history/borrowLend/positions": {
                        "GET": "borrowPositionHistoryQueryAll"
                    },
                    "wapi/v1/history/borrowLend": {
                        "GET": "borrowHistoryQueryAll"
                    },
                    "wapi/v1/history/dust": {
                        "GET": "dustHistoryQueryAll"
                    },
                    "api/v1/capital/collateral": {
                        "GET": "collateralQuery"
                    },
                    "wapi/v1/capital/deposit/address": {
                        "GET": "depositAddressQuery"
                    },
                    "wapi/v1/capital/deposits": {
                        "GET": "depositQueryAll"
                    },
                    "wapi/v1/history/fills": {
                        "GET": "fillHistoryQueryAll"
                    },
                    "wapi/v1/history/funding": {
                        "GET": "fundingHistoryQueryAll"
                    },
                    "wapi/v1/history/interest": {
                        "GET": "interestHistoryQueryAll"
                    },
                    "api/v1/order": {
                        "GET": "orderQuery",
                        "POST": "orderExecute",
                        "DELETE": "orderCancel"
                    },
                    "api/v1/orders": {
                        "GET": "orderQueryAll",
                        "POST": "orderExecute",
                        "DELETE": "orderCancelAll"
                    },
                    "wapi/v1/history/orders": {
                        "GET": "orderHistoryQueryAll"
                    },
                    "wapi/v1/history/pnl": {
                        "GET": "pnlHistoryQueryAll"
                    },
                    "wapi/v1/history/rfq": {
                        "GET": "rfqHistoryQueryAll"
                    },
                    "wapi/v1/history/quote": {
                        "GET": "quoteHistoryQueryAll"
                    },
                    "wapi/v1/history/settlement": {
                        "GET": "settlementHistoryQueryAll"
                    },
                    "api/v1/position": {
                        "GET": "positionQuery"
                    },
                    "api/v1/rfq/quote": {
                        "POST": "quoteSubmit"
                    },
                    "wapi/v1/history/strategies": {
                        "GET": "strategyHistoryQueryAll"
                    },
                    "wapi/v1/capital/withdrawals": {
                        "GET": "withdrawalQueryAll",
                        "POST": "withdraw"
                    }
                },
                "recvWindow": 5000,
                "brokerId": "",
                "broker": "",
                "timeDifference": 0,
                "adjustForTimeDifference": false,
                "networks": {
                    "APT": "Aptos",
                    "ARB": "Arbitrum",
                    "AVAX": "Avalanche",
                    "BASE": "Base",
                    "BERA": "Berachain",
                    "BTC": "Bitcoin",
                    "BCH": "BitcoinCash",
                    "BSC": "Bsc",
                    "ADA": "Cardano",
                    "DOGE": "Dogecoin",
                    "ECLIPSE": "Eclipse",
                    "EQUALSMONEY": "EqualsMoney",
                    "ERC20": "Ethereum",
                    "HYP": "Hyperliquid",
                    "LTC": "Litecoin",
                    "OPTIMISM": "Optimism",
                    "MATIC": "Polygon",
                    "SEI": "Sei",
                    "SUI": "Sui",
                    "SOL": "Solana",
                    "STORY": "Story",
                    "TRC20": "Tron",
                    "XRP": "XRP"
                },
                "networksById": {
                    "aptos": "APT",
                    "arbitrum": "ARB",
                    "avalanche": "AVAX",
                    "base": "BASE",
                    "berachain": "BERA",
                    "bitcoin": "BTC",
                    "bitcoincash": "BCH",
                    "bsc": "BSC",
                    "cardano": "ADA",
                    "dogecoin": "DOGE",
                    "eclipse": "ECLIPSE",
                    "equalsmoney": "EQUALSMONEY",
                    "ethereum": "ERC20",
                    "hyperliquid": "HYP",
                    "litecoin": "LTC",
                    "optimism": "OPTIMISM",
                    "polygon": "MATIC",
                    "sei": "SEI",
                    "sui": "SUI",
                    "solana": "SOL",
                    "story": "STORY",
                    "tron": "TRC20",
                    "xrp": "XRP"
                },
                "timeframes": {}
            },
            "streaming": {
                "keepAlive": 119000
            }
        }"###).unwrap())
    }

    async fn watch_public(&mut self, mut topics: Value, mut message_hashes: Value, mut params: Value, mut unwatch: Value) -> Value {
        params = params.or_default(Value::new_object());
        unwatch = unwatch.or_default(false.into());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        let mut method: Value = if unwatch.is_truthy() { Value::from("UNSUBSCRIBE") } else { Value::from("SUBSCRIBE") };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": method,
            "params": topics
        }))).unwrap());
        let mut message: Value = self.deep_extend_2(request.clone(), params.clone());
        if unwatch.is_truthy() {
            <Self as Backpack>::handle_unsubscriptions(self, url.clone(), message_hashes.clone(), message.clone());
            return Value::Undefined;
        };
        return self.watch_multiple(url.clone(), message_hashes.clone(), message.clone(), message_hashes.clone()).await;
    }

    async fn watch_private(&mut self, mut topics: Value, mut message_hashes: Value, mut params: Value, mut unwatch: Value) -> Value {
        params = params.or_default(Value::new_object());
        unwatch = unwatch.or_default(false.into());
        self.check_required_credentials();
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut instruction: Value = Value::from("subscribe");
        let mut ts: Value = self.nonce().to_string();
        let mut method: Value = if unwatch.is_truthy() { Value::from("UNSUBSCRIBE") } else { Value::from("SUBSCRIBE") };
        let mut recv_window: Value = self.safe_string_2(self.get("options".into()), Value::from("recvWindow"), Value::from("X-Window"), Value::from("5000"));
        let mut payload: Value = Value::from("instruction=") + instruction.clone() + Value::from("&") + Value::from("timestamp=") + ts.clone() + Value::from("&window=") + recv_window.clone();
        let mut secret_bytes: Value = self.base64_to_binary(self.get("secret".into()));
        let mut seed: Value = self.array_slice(secret_bytes.clone(), Value::from(0), Value::from(32));
        let mut signature: Value = eddsa(self.encode(payload.clone()), seed.clone(), ed25519().clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": method,
            "params": topics,
            "signature": Value::Json(serde_json::Value::Array(vec![self.get("apiKey".into()).into(), signature.clone().into(), ts.clone().into(), recv_window.clone().into()]))
        }))).unwrap());
        let mut message: Value = self.deep_extend_2(request.clone(), params.clone());
        if unwatch.is_truthy() {
            <Self as Backpack>::handle_unsubscriptions(self, url.clone(), message_hashes.clone(), message.clone());
            return Value::Undefined;
        };
        return self.watch_multiple(url.clone(), message_hashes.clone(), message.clone(), message_hashes.clone()).await;
    }

    fn handle_unsubscriptions(&mut self, mut url: Value, mut message_hashes: Value, mut message: Value) -> Value {
        let mut client: Value = self.client(url.clone());
        self.watch_multiple(url.clone(), message_hashes.clone(), message.clone(), message_hashes.clone());
        let mut i: usize = 0;
        while i < message_hashes.len() {
            let mut message_hash: Value = message_hashes.get(i.into());
            let mut sub_message_hash: Value = message_hash.replace(Value::from("unsubscribe:"), Value::from(""));
            self.clean_unsubscription(client.clone(), sub_message_hash.clone(), message_hash.clone());
            if message_hash.index_of(Value::from("ticker")) >= Value::from(0) {
                let mut symbol: Value = message_hash.replace(Value::from("unsubscribe:ticker:"), Value::from(""));
                if self.get("tickers".into()).contains_key(symbol.clone()) {
                    self.get("tickers".into()).get(symbol.clone());
                };
            } else if message_hash.index_of(Value::from("bidask")) >= Value::from(0) {
                let mut symbol: Value = message_hash.replace(Value::from("unsubscribe:bidask:"), Value::from(""));
                if self.get("bidsasks".into()).contains_key(symbol.clone()) {
                    self.get("bidsasks".into()).get(symbol.clone());
                };
            } else if message_hash.index_of(Value::from("candles")) >= Value::from(0) {
                let mut split_hashes: Value = message_hash.split(Value::from(":"));
                let mut symbol: Value = self.safe_string(split_hashes.clone(), Value::from(2), Value::Undefined);
                let mut timeframe: Value = self.safe_string(split_hashes.clone(), Value::from(3), Value::Undefined);
                if self.get("ohlcvs".into()).contains_key(symbol.clone()) {
                    if self.get("ohlcvs".into()).get(symbol.clone()).contains_key(timeframe.clone()) {
                        self.get("ohlcvs".into()).get(symbol.clone()).get(timeframe.clone());
                    };
                };
            } else if message_hash.index_of(Value::from("orderbook")) >= Value::from(0) {
                let mut symbol: Value = message_hash.replace(Value::from("unsubscribe:orderbook:"), Value::from(""));
                if self.get("orderbooks".into()).contains_key(symbol.clone()) {
                    self.get("orderbooks".into()).get(symbol.clone());
                };
            } else if message_hash.index_of(Value::from("trades")) >= Value::from(0) {
                let mut symbol: Value = message_hash.replace(Value::from("unsubscribe:trades:"), Value::from(""));
                if self.get("trades".into()).contains_key(symbol.clone()) {
                    self.get("trades".into()).get(symbol.clone());
                };
            } else if message_hash.index_of(Value::from("orders")) >= Value::from(0) {
                if message_hash.clone() == Value::from("unsubscribe:orders") {
                    let mut cache: Value = self.get("orders".into());
                    let mut keys: Value = cache.clone().keys();
                    let mut j: usize = 0;
                    while j < keys.len() {
                        let mut symbol: Value = keys.get(j.into());
                        self.get("orders".into()).get(symbol.clone());
                        j += 1;
                    };
                } else {
                    let mut symbol: Value = message_hash.replace(Value::from("unsubscribe:orders:"), Value::from(""));
                    if self.get("orders".into()).contains_key(symbol.clone()) {
                        self.get("orders".into()).get(symbol.clone());
                    };
                };
            } else if message_hash.index_of(Value::from("positions")) >= Value::from(0) {
                if message_hash.clone() == Value::from("unsubscribe:positions") {
                    let mut cache: Value = self.get("positions".into());
                    let mut keys: Value = cache.clone().keys();
                    let mut j: usize = 0;
                    while j < keys.len() {
                        let mut symbol: Value = keys.get(j.into());
                        self.get("positions".into()).get(symbol.clone());
                        j += 1;
                    };
                } else {
                    let mut symbol: Value = message_hash.replace(Value::from("unsubscribe:positions:"), Value::from(""));
                    if self.get("positions".into()).contains_key(symbol.clone()) {
                        self.get("positions".into()).get(symbol.clone());
                    };
                };
            };
            i += 1;
        };
        Value::Undefined
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut topic: Value = Value::from("ticker") + Value::from(".") + market.get(Value::from("id"));
        let mut message_hash: Value = Value::from("ticker") + Value::from(":") + symbol.clone();
        return <Self as Backpack>::watch_public(self, Value::Json(serde_json::Value::Array(vec![topic.clone().into()])), Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()])), params.clone(), Value::Undefined).await;
    }

    async fn un_watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Backpack>::un_watch_tickers(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into());
        let mut message_hashes: Value = Value::new_array();
        let mut topics: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            message_hashes.push(Value::from("ticker:") + symbol.clone());
            topics.push(Value::from("ticker.") + market_id.clone());
            i += 1;
        };
        <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), Value::Undefined).await;
        return self.filter_by_array(self.get("tickers".into()), Value::from("symbol"), symbols.clone());
    }

    async fn un_watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into());
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("ticker.") + market_id.clone());
            message_hashes.push(Value::from("unsubscribe:ticker:") + symbol.clone());
            i += 1;
        };
        return <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), true.into()).await;
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         data: {
        //             E: '1754176123312507',
        //             V: '19419526.742584',
        //             c: '3398.57',
        //             e: 'ticker',
        //             h: '3536.65',
        //             l: '3371.8',
        //             n: 17152,
        //             o: '3475.45',
        //             s: 'ETH_USDC',
        //             v: '5573.5827'
        //         },
        //         stream: 'bookTicker.ETH_USDC'
        //     }
        //
        let mut ticker: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("s"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut parsed_ticker: Value = <Self as Backpack>::parse_ws_ticker(self, ticker.clone(), market.clone());
        let mut message_hash: Value = Value::from("ticker") + Value::from(":") + symbol.clone();
        self.get("tickers".into()).set(symbol.clone(), parsed_ticker.clone());
        client.resolve(parsed_ticker.clone(), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     {
        //         E: '1754178406415232',
        //         V: '19303818.6923',
        //         c: '3407.54',
        //         e: 'ticker',
        //         h: '3536.65',
        //         l: '3369.18',
        //         n: 17272,
        //         o: '3481.71',
        //         s: 'ETH_USDC',
        //         v: '5542.3911'
        //     }
        //
        let mut microseconds: Value = self.safe_integer(ticker.clone(), Value::from("E"), Value::Undefined);
        let mut timestamp: Value = self.parse_to_int(microseconds.clone() / Value::from(1000));
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("s"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("c"), Value::Undefined);
        let mut open: Value = self.safe_string(ticker.clone(), Value::from("o"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "high": self.safe_number(ticker.clone(), Value::from("h"), Value::Undefined),
            "low": self.safe_number(ticker.clone(), Value::from("l"), Value::Undefined),
            "bid": Value::Undefined,
            "bidVolume": Value::Undefined,
            "ask": Value::Undefined,
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": open,
            "close": last,
            "last": last,
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": self.safe_string(ticker.clone(), Value::from("v"), Value::Undefined),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("V"), Value::Undefined),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into());
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("bookTicker.") + market_id.clone());
            message_hashes.push(Value::from("bidask:") + symbol.clone());
            i += 1;
        };
        <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), Value::Undefined).await;
        return self.filter_by_array(self.get("bidsasks".into()), Value::from("symbol"), symbols.clone());
    }

    async fn un_watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into());
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("bookTicker.") + market_id.clone());
            message_hashes.push(Value::from("unsubscribe:bidask:") + symbol.clone());
            i += 1;
        };
        return <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), true.into()).await;
    }

    fn handle_bid_ask(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         data: {
        //             A: '0.4087',
        //             B: '0.0020',
        //             E: '1754517402450016',
        //             T: '1754517402449064',
        //             a: '3667.50',
        //             b: '3667.49',
        //             e: 'bookTicker',
        //             s: 'ETH_USDC',
        //             u: 1328288557
        //         },
        //         stream: 'bookTicker.ETH_USDC'
        //     }
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("s"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut parsed_bid_ask: Value = <Self as Backpack>::parse_ws_bid_ask(self, data.clone(), market.clone());
        let mut message_hash: Value = Value::from("bidask") + Value::from(":") + symbol.clone();
        self.get("bidsasks".into()).set(symbol.clone(), parsed_bid_ask.clone());
        client.resolve(parsed_bid_ask.clone(), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_bid_ask(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     {
        //         A: '0.4087',
        //         B: '0.0020',
        //         E: '1754517402450016',
        //         T: '1754517402449064',
        //         a: '3667.50',
        //         b: '3667.49',
        //         e: 'bookTicker',
        //         s: 'ETH_USDC',
        //         u: 1328288557
        //     }
        //
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("s"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        let mut microseconds: Value = self.safe_integer(ticker.clone(), Value::from("E"), Value::Undefined);
        let mut timestamp: Value = self.parse_to_int(microseconds.clone() / Value::from(1000));
        let mut ask: Value = self.safe_string(ticker.clone(), Value::from("a"), Value::Undefined);
        let mut ask_volume: Value = self.safe_string(ticker.clone(), Value::from("A"), Value::Undefined);
        let mut bid: Value = self.safe_string(ticker.clone(), Value::from("b"), Value::Undefined);
        let mut bid_volume: Value = self.safe_string(ticker.clone(), Value::from("B"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "ask": ask,
            "askVolume": ask_volume,
            "bid": bid,
            "bidVolume": bid_volume,
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        let mut result: Value = <Self as Backpack>::watch_ohlcv_for_symbols(self, Value::Json(serde_json::Value::Array(vec![Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into()])).into()])), since.clone(), limit.clone(), params.clone()).await;
        return result.get(symbol.clone()).get(timeframe.clone());
    }

    async fn un_watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        return <Self as Backpack>::un_watch_ohlcv_for_symbols(self, Value::Json(serde_json::Value::Array(vec![Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into()])).into()])), params.clone()).await;
    }

    async fn watch_ohlcv_for_symbols(&mut self, mut symbols_and_timeframes: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut symbols_length: usize = symbols_and_timeframes.len();
        if symbols_length.clone() == Value::from(0) || !Array::is_array(symbols_and_timeframes.get(Value::from(0))).is_truthy() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" watchOHLCVForSymbols() requires a an array of symbols and timeframes, like  ['ETH/USDC', '1m']"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols_and_timeframes.len() {
            let mut symbol_and_timeframe: Value = symbols_and_timeframes.get(i.into());
            let mut market_id: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(0), Value::Undefined);
            let mut market: Value = self.market(market_id.clone());
            let mut tf: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(1), Value::Undefined);
            let mut interval: Value = self.safe_string(self.get("timeframes".into()), tf.clone(), tf.clone());
            topics.push(Value::from("kline.") + interval.clone() + Value::from(".") + market.get(Value::from("id")));
            message_hashes.push(Value::from("candles:") + market.get(Value::from("symbol")) + Value::from(":") + interval.clone());
            i += 1;
        };
        let (mut symbol, mut timeframe, mut candles) = shift_3(<Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), Value::Undefined).await);
        if self.get("newUpdates".into()).is_truthy() {
            limit = candles.get_limit(symbol.clone(), limit.clone());
        };
        let mut filtered: Value = self.filter_by_since_limit(candles.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
        return self.create_ohlcv_object(symbol.clone(), timeframe.clone(), filtered.clone());
    }

    async fn un_watch_ohlcv_for_symbols(&mut self, mut symbols_and_timeframes: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut symbols_length: usize = symbols_and_timeframes.len();
        if symbols_length.clone() == Value::from(0) || !Array::is_array(symbols_and_timeframes.get(Value::from(0))).is_truthy() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" unWatchOHLCVForSymbols() requires a an array of symbols and timeframes, like  ['ETH/USDC', '1m']"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols_and_timeframes.len() {
            let mut symbol_and_timeframe: Value = symbols_and_timeframes.get(i.into());
            let mut market_id: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(0), Value::Undefined);
            let mut market: Value = self.market(market_id.clone());
            let mut tf: Value = self.safe_string(symbol_and_timeframe.clone(), Value::from(1), Value::Undefined);
            let mut interval: Value = self.safe_string(self.get("timeframes".into()), tf.clone(), tf.clone());
            topics.push(Value::from("kline.") + interval.clone() + Value::from(".") + market.get(Value::from("id")));
            message_hashes.push(Value::from("unsubscribe:candles:") + market.get(Value::from("symbol")) + Value::from(":") + interval.clone());
            i += 1;
        };
        return <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), true.into()).await;
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         data: {
        //             E: '1754519557526056',
        //             T: '2025-08-07T00:00:00',
        //             X: false,
        //             c: '3680.520000000',
        //             e: 'kline',
        //             h: '3681.370000000',
        //             l: '3667.650000000',
        //             n: 255,
        //             o: '3670.150000000',
        //             s: 'ETH_USDC',
        //             t: '2025-08-06T22:00:00',
        //             v: '62.2621000'
        //         },
        //         stream: 'kline.2h.ETH_USDC'
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("s"), Value::Undefined);
        let mut market: Value = self.market(market_id.clone());
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut stream: Value = self.safe_string(message.clone(), Value::from("stream"), Value::Undefined);
        let mut parts: Value = stream.split(Value::from("."));
        let mut timeframe: Value = self.safe_string(parts.clone(), Value::from(1), Value::Undefined);
        if !self.get("ohlcvs".into()).contains_key(symbol.clone()) {
            self.get("ohlcvs".into()).set(symbol.clone(), Value::new_object());
        };
        if !self.get("ohlcvs".into()).get(symbol.clone()).contains_key(timeframe.clone()) {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            let mut stored: Value = ArrayCacheByTimestamp::new(limit);
            self.get("ohlcvs".into()).get(symbol.clone()).set(timeframe.clone(), stored.clone());
        };
        let mut ohlcv: Value = self.get("ohlcvs".into()).get(symbol.clone()).get(timeframe.clone());
        let mut parsed: Value = <Self as Backpack>::parse_ws_ohlcv(self, data.clone(), Value::Undefined);
        ohlcv.append(parsed.clone());
        let mut message_hash: Value = Value::from("candles:") + symbol.clone() + Value::from(":") + timeframe.clone();
        client.resolve(Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into(), ohlcv.clone().into()])), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        //     {
        //         E: '1754519557526056',
        //         T: '2025-08-07T00:00:00',
        //         X: false,
        //         c: '3680.520000000',
        //         e: 'kline',
        //         h: '3681.370000000',
        //         l: '3667.650000000',
        //         n: 255,
        //         o: '3670.150000000',
        //         s: 'ETH_USDC',
        //         t: '2025-08-06T22:00:00',
        //         v: '62.2621000'
        //     },
        //
        return Value::Json(serde_json::Value::Array(vec![self.parse8601(self.safe_string(ohlcv.clone(), Value::from("T"), Value::Undefined)).into(), self.safe_number(ohlcv.clone(), Value::from("o"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("h"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("l"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("c"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("v"), Value::Undefined).into()]));
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Backpack>::watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn un_watch_trades(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Backpack>::un_watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
    }

    async fn watch_trades_for_symbols(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone());
        let mut symbols_length: usize = symbols.len();
        if symbols_length.clone() == Value::from(0) {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" watchTradesForSymbols() requires a non-empty array of symbols"))"###);
        };
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("trade.") + market_id.clone());
            message_hashes.push(Value::from("trades:") + symbol.clone());
            i += 1;
        };
        let mut trades: Value = <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut first: Value = self.safe_value(trades.clone(), Value::from(0), Value::Undefined);
            let mut trade_symbol: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
            limit = trades.get_limit(trade_symbol.clone(), limit.clone());
        };
        let mut result: Value = self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
        return self.sort_by(result.clone(), Value::from("timestamp"), Value::Undefined, Value::Undefined);
    }

    async fn un_watch_trades_for_symbols(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone());
        let mut symbols_length: usize = symbols.len();
        if symbols_length.clone() == Value::from(0) {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" unWatchTradesForSymbols() requires a non-empty array of symbols"))"###);
        };
        let mut topics: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            let mut market_id: Value = self.market_id(symbol.clone());
            topics.push(Value::from("trade.") + market_id.clone());
            message_hashes.push(Value::from("unsubscribe:trades:") + symbol.clone());
            i += 1;
        };
        return <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), true.into()).await;
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         data: {
        //             E: '1754601477746429',
        //             T: '1754601477744000',
        //             a: '5121860761',
        //             b: '5121861755',
        //             e: 'trade',
        //             m: false,
        //             p: '3870.25',
        //             q: '0.0008',
        //             s: 'ETH_USDC_PERP',
        //             t: 10782547
        //         },
        //         stream: 'trade.ETH_USDC_PERP'
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("s"), Value::Undefined);
        let mut market: Value = self.market(market_id.clone());
        let mut symbol: Value = market.get(Value::from("symbol"));
        if !self.get("trades".into()).contains_key(symbol.clone()) {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            let mut stored: Value = ArrayCache::new(limit);
            self.get("trades".into()).set(symbol.clone(), stored.clone());
        };
        let mut cache: Value = self.get("trades".into()).get(symbol.clone());
        let mut trade: Value = <Self as Backpack>::parse_ws_trade(self, data.clone(), market.clone());
        cache.append(trade.clone());
        let mut message_hash: Value = Value::from("trades:") + symbol.clone();
        client.resolve(cache.clone(), message_hash.clone());
        client.resolve(cache.clone(), Value::from("trades"));
        Value::Undefined
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        //     {
        //         E: '1754601477746429',
        //         T: '1754601477744000',
        //         a: '5121860761',
        //         b: '5121861755',
        //         e: 'trade',
        //         m: false,
        //         p: '3870.25',
        //         q: '0.0008',
        //         s: 'ETH_USDC_PERP',
        //         t: 10782547
        //     }
        //
        let mut microseconds: Value = self.safe_integer(trade.clone(), Value::from("E"), Value::Undefined);
        let mut timestamp: Value = self.parse_to_int(microseconds.clone() / Value::from(1000));
        let mut id: Value = self.safe_string(trade.clone(), Value::from("t"), Value::Undefined);
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("s"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut is_maker: Value = self.safe_bool(trade.clone(), Value::from("m"), Value::Undefined);
        let mut side: Value = if is_maker.is_truthy() { Value::from("sell") } else { Value::from("buy") };
        let mut taker_or_maker: Value = if is_maker.is_truthy() { Value::from("maker") } else { Value::from("taker") };
        let mut price: Value = self.safe_string(trade.clone(), Value::from("p"), Value::Undefined);
        let mut amount: Value = self.safe_string(trade.clone(), Value::from("q"), Value::Undefined);
        let mut order_id: Value = Value::Undefined;
        if side.clone() == Value::from("buy") {
            order_id = self.safe_string(trade.clone(), Value::from("b"), Value::Undefined);
        } else {
            order_id = self.safe_string(trade.clone(), Value::from("a"), Value::Undefined);
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade,
            "id": id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": market.get(Value::from("symbol")),
            "order": order_id,
            "type": Value::Undefined,
            "side": side,
            "takerOrMaker": taker_or_maker,
            "price": price,
            "amount": amount,
            "cost": Value::Undefined,
            "fee": Value::Json(normalize(&Value::Json(json!({
                "currency": Value::Undefined,
                "cost": Value::Undefined
            }))).unwrap())
        }))).unwrap()), market.clone());
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Backpack>::watch_order_book_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), limit.clone(), params.clone()).await;
    }

    async fn watch_order_book_for_symbols(&mut self, mut symbols: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into());
        let mut market_ids: Value = self.market_ids(symbols.clone());
        let mut message_hashes: Value = Value::new_array();
        let mut topics: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            message_hashes.push(Value::from("orderbook:") + symbol.clone());
            let mut market_id: Value = market_ids.get(i.into());
            let mut topic: Value = Value::from("depth.") + market_id.clone();
            topics.push(topic.clone());
            i += 1;
        };
        let mut orderbook: Value = <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), Value::Undefined).await;
        return orderbook.limit();
    }

    async fn un_watch_order_book(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Backpack>::un_watch_order_book_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
    }

    async fn un_watch_order_book_for_symbols(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into());
        let mut market_ids: Value = self.market_ids(symbols.clone());
        let mut message_hashes: Value = Value::new_array();
        let mut topics: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut symbol: Value = symbols.get(i.into());
            message_hashes.push(Value::from("unsubscribe:orderbook:") + symbol.clone());
            let mut market_id: Value = market_ids.get(i.into());
            let mut topic: Value = Value::from("depth.") + market_id.clone();
            topics.push(topic.clone());
            i += 1;
        };
        return <Self as Backpack>::watch_public(self, topics.clone(), message_hashes.clone(), params.clone(), true.into()).await;
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // initial snapshot is fetched with ccxt's fetchOrderBook
        // the feed does not include a snapshot, just the deltas
        //
        //     {
        //         "data": {
        //             "E": "1754903057555305",
        //             "T": "1754903057554352",
        //             "U": 1345937436,
        //             "a": [],
        //             "b": [],
        //             "e": "depth",
        //             "s": "ETH_USDC",
        //             "u": 1345937436
        //         },
        //         "stream": "depth.ETH_USDC"
        //     }
        //
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("s"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            self.get("orderbooks".into()).set(symbol.clone(), self.order_book());
        };
        let mut stored_order_book: Value = self.get("orderbooks".into()).get(symbol.clone());
        let mut nonce: Value = self.safe_integer(stored_order_book.clone(), Value::from("nonce"), Value::Undefined);
        let mut delta_nonce: Value = self.safe_integer(data.clone(), Value::from("u"), Value::Undefined);
        let mut message_hash: Value = Value::from("orderbook:") + symbol.clone();
        if nonce.clone().is_nullish() {
            let mut cache_length: usize = stored_order_book.get(cache.clone()).len();
            // the rest API is very delayed
            // usually it takes at least 9 deltas to resolve
            let mut snapshot_delay: Value = self.handle_option(Value::from("watchOrderBook"), Value::from("snapshotDelay"), Value::from(10));
            if cache_length.clone() == snapshot_delay.clone() {
                self.spawn(self.get("loadOrderBook".into()), client.clone(), message_hash.clone(), symbol.clone(), Value::null(), Value::new_object());
            };
            stored_order_book.get(cache.clone()).push(data.clone());
            return Value::Undefined;
        } else if nonce.clone() > delta_nonce.clone() {
            return Value::Undefined;
        };
        <Self as Backpack>::handle_delta(self, stored_order_book.clone(), data.clone());
        client.resolve(stored_order_book.clone(), message_hash.clone());
        Value::Undefined
    }

    fn handle_delta(&mut self, mut orderbook: Value, mut delta: Value) -> Value {
        let mut timestamp: Value = self.parse_to_int(self.safe_integer(delta.clone(), Value::from("T"), Value::Undefined) / Value::from(1000));
        orderbook.set("timestamp".into(), timestamp.clone());
        orderbook.set("datetime".into(), self.iso8601(timestamp.clone()));
        orderbook.set("nonce".into(), self.safe_integer(delta.clone(), Value::from("u"), Value::Undefined));
        let mut bids: Value = self.safe_list(delta.clone(), Value::from("b"), Value::new_array());
        let mut asks: Value = self.safe_list(delta.clone(), Value::from("a"), Value::new_array());
        let mut stored_bids: Value = orderbook.get(Value::from("bids"));
        let mut stored_asks: Value = orderbook.get(Value::from("asks"));
        <Self as Backpack>::handle_bid_asks(self, stored_bids.clone(), bids.clone());
        <Self as Backpack>::handle_bid_asks(self, stored_asks.clone(), asks.clone());
        Value::Undefined
    }

    fn handle_bid_asks(&mut self, mut book_side: Value, mut bid_asks: Value) -> Value {
        let mut i: usize = 0;
        while i < bid_asks.len() {
            let mut bid_ask: Value = self.parse_bid_ask(bid_asks.get(i.into()), Value::Undefined, Value::Undefined);
            book_side.store_array(bid_ask.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn get_cache_index(&mut self, mut orderbook: Value, mut cache: Value) -> Value {
        //
        // {"E":"1759338824897386","T":"1759338824895616","U":1662976171,"a":[],"b":[["117357.0","0.00000"]],"e":"depth","s":"BTC_USDC_PERP","u":1662976171}
        let mut first_delta: Value = self.safe_dict(cache.clone(), Value::from(0), Value::Undefined);
        let mut nonce: Value = self.safe_integer(orderbook.clone(), Value::from("nonce"), Value::Undefined);
        let mut first_delta_start: Value = self.safe_integer(first_delta.clone(), Value::from("U"), Value::Undefined);
        if nonce.clone() < first_delta_start.clone() - Value::from(1) {
            return Value::from(1).neg();
        };
        let mut i: usize = 0;
        while i < cache.len() {
            let mut delta: Value = cache.get(i.into());
            let mut delta_start: Value = self.safe_integer(delta.clone(), Value::from("U"), Value::Undefined);
            let mut delta_end: Value = self.safe_integer(delta.clone(), Value::from("u"), Value::Undefined);
            if nonce.clone() >= delta_start.clone() - Value::from(1) && nonce.clone() < delta_end.clone() {
                return Value::from(i);
            };
            i += 1;
        };
        return cache.len().into();
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
        };
        let mut topic: Value = Value::from("account.orderUpdate");
        let mut message_hash: Value = Value::from("orders");
        if market.clone().is_nonnullish() {
            topic = Value::from("account.orderUpdate.") + market.get(Value::from("id"));
            message_hash = Value::from("orders:") + symbol.clone();
        };
        let mut orders: Value = <Self as Backpack>::watch_private(self, Value::Json(serde_json::Value::Array(vec![topic.clone().into()])), Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()])), params.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    async fn un_watch_orders(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            symbol = market.get(Value::from("symbol"));
        };
        let mut topic: Value = Value::from("account.orderUpdate");
        let mut message_hash: Value = Value::from("unsubscribe:orders");
        if market.clone().is_nonnullish() {
            topic = Value::from("account.orderUpdate.") + market.get(Value::from("id"));
            message_hash = Value::from("unsubscribe:orders:") + symbol.clone();
        };
        return <Self as Backpack>::watch_private(self, Value::Json(serde_json::Value::Array(vec![topic.clone().into()])), Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()])), params.clone(), true.into()).await;
    }

    fn handle_order(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         data: {
        //             E: '1754939110175843',
        //             O: 'USER',
        //             Q: '4.30',
        //             S: 'Bid',
        //             T: '1754939110174703',
        //             V: 'RejectTaker',
        //             X: 'New',
        //             Z: '0',
        //             e: 'orderAccepted',
        //             f: 'GTC',
        //             i: '5406825793',
        //             o: 'MARKET',
        //             q: '0.0010',
        //             r: false,
        //             s: 'ETH_USDC',
        //             t: null,
        //             z: '0'
        //         },
        //         stream: 'account.orderUpdate.ETH_USDC'
        //     }
        //
        let mut message_hash: Value = Value::from("orders");
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("s"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut parsed: Value = <Self as Backpack>::parse_ws_order(self, data.clone(), market.clone());
        let mut orders: Value = self.get("orders".into());
        if orders.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
            orders = ArrayCacheBySymbolById::new(limit);
            self.set("orders".into(), orders.clone());
        };
        orders.append(parsed.clone());
        client.resolve(orders.clone(), message_hash.clone());
        let mut symbol_specific_message_hash: Value = message_hash.clone() + Value::from(":") + symbol.clone();
        client.resolve(orders.clone(), symbol_specific_message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        //     {
        //         E: '1754939110175879',
        //         L: '4299.16',
        //         N: 'ETH',
        //         O: 'USER',
        //         Q: '4.30',
        //         S: 'Bid',
        //         T: '1754939110174705',
        //         V: 'RejectTaker',
        //         X: 'Filled',
        //         Z: '4.299160',
        //         e: 'orderFill',
        //         f: 'GTC',
        //         i: '5406825793',
        //         l: '0.0010',
        //         m: false,
        //         n: '0.000001',
        //         o: 'MARKET',
        //         q: '0.0010',
        //         r: false,
        //         s: 'ETH_USDC',
        //         t: 2888471,
        //         z: '0.0010'
        //     },
        //
        let mut id: Value = self.safe_string(order.clone(), Value::from("i"), Value::Undefined);
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("c"), Value::Undefined);
        let mut microseconds: Value = self.safe_integer(order.clone(), Value::from("E"), Value::Undefined);
        let mut timestamp: Value = self.parse_to_int(microseconds.clone() / Value::from(1000));
        let mut status: Value = <Self as Backpack>::parse_ws_order_status(self, self.safe_string(order.clone(), Value::from("X"), Value::Undefined), market.clone());
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("s"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut r#type: Value = self.safe_string_lower(order.clone(), Value::from("o"), Value::Undefined);
        let mut time_in_force: Value = self.safe_string(order.clone(), Value::from("f"), Value::Undefined);
        let mut side: Value = <Self as Backpack>::parse_ws_order_side(self, self.safe_string(order.clone(), Value::from("S"), Value::Undefined));
        let mut price: Value = self.safe_string(order.clone(), Value::from("p"), Value::Undefined);
        let mut trigger_price: Value = self.safe_number(order.clone(), Value::from("P"), Value::Undefined);
        let mut amount: Value = self.safe_string(order.clone(), Value::from("q"), Value::Undefined);
        let mut cost: Value = self.safe_string(order.clone(), Value::from("Z"), Value::Undefined);
        let mut filled: Value = self.safe_string(order.clone(), Value::from("l"), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        let mut fee_currency: Value = self.safe_string(order.clone(), Value::from("N"), Value::Undefined);
        if fee_currency.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "currency": fee_currency,
                "cost": Value::Undefined
            }))).unwrap());
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": id,
            "clientOrderId": client_order_id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": Value::Undefined,
            "status": status,
            "symbol": symbol,
            "type": r#type,
            "timeInForce": time_in_force,
            "side": side,
            "price": price,
            "stopPrice": Value::Undefined,
            "triggerPrice": trigger_price,
            "average": Value::Undefined,
            "amount": amount,
            "cost": cost,
            "filled": filled,
            "remaining": Value::Undefined,
            "fee": fee,
            "trades": Value::Undefined,
            "info": order
        }))).unwrap()), market.clone());
    }

    fn parse_ws_order_status(&self, mut status: Value, mut market: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "New": "open",
            "Filled": "closed",
            "Cancelled": "canceled",
            "Expired": "canceled",
            "PartiallyFilled": "open",
            "TriggerPending": "open",
            "TriggerFailed": "rejected"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_ws_order_side(&self, mut side: Value) -> Value {
        let mut sides: Value = Value::Json(normalize(&Value::Json(json!({
            "Bid": "buy",
            "Ask": "sell"
        }))).unwrap());
        return self.safe_string(sides.clone(), side.clone(), side.clone());
    }

    async fn watch_positions(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone());
        let mut message_hashes: Value = Value::new_array();
        let mut topics: Value = Value::new_array();
        if symbols.clone().is_nonnullish() {
            let mut i: usize = 0;
            while i < symbols.len() {
                let mut symbol: Value = symbols.get(i.into());
                message_hashes.push(Value::from("positions") + Value::from(":") + symbol.clone());
                topics.push(Value::from("account.positionUpdate.") + self.market_id(symbol.clone()));
                i += 1;
            };
        } else {
            message_hashes.push(Value::from("positions"));
            topics.push(Value::from("account.positionUpdate"));
        };
        let mut positions: Value = <Self as Backpack>::watch_private(self, topics.clone(), message_hashes.clone(), params.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            return positions.clone();
        };
        return self.filter_by_symbols_since_limit(self.get("positions".into()), symbols.clone(), since.clone(), limit.clone(), true.into());
    }

    async fn un_watch_positions(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone());
        let mut message_hashes: Value = Value::new_array();
        let mut topics: Value = Value::new_array();
        if symbols.clone().is_nonnullish() {
            let mut i: usize = 0;
            while i < symbols.len() {
                let mut symbol: Value = symbols.get(i.into());
                message_hashes.push(Value::from("unsubscribe:positions") + Value::from(":") + symbol.clone());
                topics.push(Value::from("account.positionUpdate.") + self.market_id(symbol.clone()));
                i += 1;
            };
        } else {
            message_hashes.push(Value::from("unsubscribe:positions"));
            topics.push(Value::from("account.positionUpdate"));
        };
        return <Self as Backpack>::watch_private(self, topics.clone(), message_hashes.clone(), params.clone(), true.into()).await;
    }

    fn handle_positions(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         data: {
        //             B: '4236.36',
        //             E: '1754943862040486',
        //             M: '4235.88650933',
        //             P: '-0.000473',
        //             Q: '0.0010',
        //             T: '1754943862040487',
        //             b: '4238.479',
        //             e: 'positionOpened',
        //             f: '0.02',
        //             i: 5411399049,
        //             l: '0',
        //             m: '0.0125',
        //             n: '4.23588650933',
        //             p: '0',
        //             q: '0.0010',
        //             s: 'ETH_USDC_PERP'
        //         },
        //         stream: 'account.positionUpdate'
        //     }
        //
        let mut message_hash: Value = Value::from("positions");
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::new_object());
        if self.get("positions".into()).is_nullish() {
            self.set("positions".into(), ArrayCacheBySymbolById::new());
        };
        let mut cache: Value = self.get("positions".into());
        let mut parsed_position: Value = <Self as Backpack>::parse_ws_position(self, data.clone(), Value::Undefined);
        let mut microseconds: Value = self.safe_integer(data.clone(), Value::from("E"), Value::Undefined);
        let mut timestamp: Value = self.parse_to_int(microseconds.clone() / Value::from(1000));
        parsed_position.set("timestamp".into(), timestamp.clone());
        parsed_position.set("datetime".into(), self.iso8601(timestamp.clone()));
        cache.append(parsed_position.clone());
        let mut symbol_specific_message_hash: Value = message_hash.clone() + Value::from(":") + parsed_position.get(Value::from("symbol"));
        client.resolve(Value::Json(serde_json::Value::Array(vec![parsed_position.clone().into()])), message_hash.clone());
        client.resolve(Value::Json(serde_json::Value::Array(vec![parsed_position.clone().into()])), symbol_specific_message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_position(&self, mut position: Value, mut market: Value) -> Value {
        //
        //     {
        //         B: '4236.36',
        //         E: '1754943862040486',
        //         M: '4235.88650933',
        //         P: '-0.000473',
        //         Q: '0.0010',
        //         T: '1754943862040487',
        //         b: '4238.479',
        //         e: 'positionOpened',
        //         f: '0.02',
        //         i: 5411399049,
        //         l: '0',
        //         m: '0.0125',
        //         n: '4.23588650933',
        //         p: '0',
        //         q: '0.0010',
        //         s: 'ETH_USDC_PERP'
        //     }
        //
        let mut id: Value = self.safe_string(position.clone(), Value::from("i"), Value::Undefined);
        let mut market_id: Value = self.safe_string(position.clone(), Value::from("s"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut notional: Value = self.safe_string(position.clone(), Value::from("n"), Value::Undefined);
        let mut liquidation_price: Value = self.safe_string(position.clone(), Value::from("l"), Value::Undefined);
        let mut entry_price: Value = self.safe_string(position.clone(), Value::from("b"), Value::Undefined);
        let mut realized_pnl: Value = self.safe_string(position.clone(), Value::from("p"), Value::Undefined);
        let mut unrealised_pnl: Value = self.safe_string(position.clone(), Value::from("P"), Value::Undefined);
        let mut contracts: Value = self.safe_string(position.clone(), Value::from("Q"), Value::Undefined);
        let mut mark_price: Value = self.safe_string(position.clone(), Value::from("M"), Value::Undefined);
        let mut net_quantity: Value = self.safe_number(position.clone(), Value::from("q"), Value::Undefined);
        let mut hedged: Value = false.into();
        let mut side: Value = Value::from("long");
        if net_quantity.clone() < Value::from(0) {
            side = Value::from("short");
        };
        if net_quantity.clone().is_nullish() {
            hedged = Value::Undefined;
            side = Value::Undefined;
        };
        let mut microseconds: Value = self.safe_integer(position.clone(), Value::from("E"), Value::Undefined);
        let mut timestamp: Value = self.parse_to_int(microseconds.clone() / Value::from(1000));
        let mut maintenance_margin_percentage: Value = self.safe_number(position.clone(), Value::from("m"), Value::Undefined);
        let mut initial_margin_percentage: Value = self.safe_number(position.clone(), Value::from("f"), Value::Undefined);
        return self.safe_position(Value::Json(normalize(&Value::Json(json!({
            "info": position,
            "id": id,
            "symbol": symbol,
            "notional": notional,
            "marginMode": Value::Undefined,
            "liquidationPrice": liquidation_price,
            "entryPrice": entry_price,
            "realizedPnl": realized_pnl,
            "unrealizedPnl": unrealised_pnl,
            "percentage": Value::Undefined,
            "contracts": contracts,
            "contractSize": Value::Undefined,
            "markPrice": mark_price,
            "side": side,
            "hedged": hedged,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "maintenanceMargin": Value::Undefined,
            "maintenanceMarginPercentage": maintenance_margin_percentage,
            "collateral": Value::Undefined,
            "initialMargin": Value::Undefined,
            "initialMarginPercentage": initial_margin_percentage,
            "leverage": Value::Undefined,
            "marginRatio": Value::Undefined
        }))).unwrap()));
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        if !<Self as Backpack>::handle_error_message(self, client.clone(), message.clone()).is_truthy() {
            return Value::Undefined;
        };
        let mut data: Value = self.safe_dict(message.clone(), Value::from("data"), Value::Undefined);
        let mut event: Value = self.safe_string(data.clone(), Value::from("e"), Value::Undefined);
        if event.clone() == Value::from("ticker") {
            <Self as Backpack>::handle_ticker(self, client.clone(), message.clone());
        } else if event.clone() == Value::from("bookTicker") {
            <Self as Backpack>::handle_bid_ask(self, client.clone(), message.clone());
        } else if event.clone() == Value::from("kline") {
            <Self as Backpack>::handle_ohlcv(self, client.clone(), message.clone());
        } else if event.clone() == Value::from("trade") {
            <Self as Backpack>::handle_trades(self, client.clone(), message.clone());
        } else if event.clone() == Value::from("depth") {
            <Self as Backpack>::handle_order_book(self, client.clone(), message.clone());
        } else if event.clone() == Value::from("orderAccepted") || event.clone() == Value::from("orderUpdate") || event.clone() == Value::from("orderFill") || event.clone() == Value::from("orderCancelled") || event.clone() == Value::from("orderExpired") || event.clone() == Value::from("orderModified") || event.clone() == Value::from("triggerPlaced") || event.clone() == Value::from("triggerFailed") {
            <Self as Backpack>::handle_order(self, client.clone(), message.clone());
        } else if event.clone() == Value::from("positionAdjusted") || event.clone() == Value::from("positionOpened") || event.clone() == Value::from("positionClosed") || event.clone() == Value::from("positionUpdated") {
            <Self as Backpack>::handle_positions(self, client.clone(), message.clone());
        };
        Value::Undefined
    }

    fn handle_error_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         id: null,
        //         error: {
        //             code: 4006,
        //             message: 'Invalid stream'
        //         }
        //     }
        //
        let mut error: Value = self.safe_dict(message.clone(), Value::from("error"), Value::new_object());
        let mut code: Value = self.safe_integer(error.clone(), Value::from("code"), Value::Undefined);
                if code.clone().is_nonnullish() {
            let mut msg: Value = self.safe_string(error.clone(), Value::from("message"), Value::Undefined);
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" ") + msg.clone())"###);
        };
        return true.into();
        // catch block omitted (no exception support in Value runtime)
;
        return true.into();
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetApiv1assets" => self.request("api/v1/assets".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1collateral" => self.request("api/v1/collateral".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1borrowlendmarkets" => self.request("api/v1/borrowLend/markets".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1borrowlendmarketshistory" => self.request("api/v1/borrowLend/markets/history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1markets" => self.request("api/v1/markets".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1market" => self.request("api/v1/market".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1ticker" => self.request("api/v1/ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1tickers" => self.request("api/v1/tickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1depth" => self.request("api/v1/depth".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1klines" => self.request("api/v1/klines".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1markprices" => self.request("api/v1/markPrices".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1openinterest" => self.request("api/v1/openInterest".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1fundingrates" => self.request("api/v1/fundingRates".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1status" => self.request("api/v1/status".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1ping" => self.request("api/v1/ping".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1time" => self.request("api/v1/time".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1wallets" => self.request("api/v1/wallets".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1trades" => self.request("api/v1/trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetApiv1tradeshistory" => self.request("api/v1/trades/history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1account" => self.request("api/v1/account".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountlimitsborrow" => self.request("api/v1/account/limits/borrow".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountlimitsorder" => self.request("api/v1/account/limits/order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountlimitswithdrawal" => self.request("api/v1/account/limits/withdrawal".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1borrowlendpositions" => self.request("api/v1/borrowLend/positions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1capital" => self.request("api/v1/capital".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1capitalcollateral" => self.request("api/v1/capital/collateral".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1capitaldeposits" => self.request("wapi/v1/capital/deposits".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1capitaldepositaddress" => self.request("wapi/v1/capital/deposit/address".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1capitalwithdrawals" => self.request("wapi/v1/capital/withdrawals".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1position" => self.request("api/v1/position".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyborrowlend" => self.request("wapi/v1/history/borrowLend".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyinterest" => self.request("wapi/v1/history/interest".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyborrowlendpositions" => self.request("wapi/v1/history/borrowLend/positions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historydust" => self.request("wapi/v1/history/dust".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyfills" => self.request("wapi/v1/history/fills".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyfunding" => self.request("wapi/v1/history/funding".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyorders" => self.request("wapi/v1/history/orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyrfq" => self.request("wapi/v1/history/rfq".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historyquote" => self.request("wapi/v1/history/quote".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historysettlement" => self.request("wapi/v1/history/settlement".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWapiv1historystrategies" => self.request("wapi/v1/history/strategies".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1order" => self.request("api/v1/order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1orders" => self.request("api/v1/orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1accountconvertdust" => self.request("api/v1/account/convertDust".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1borrowlend" => self.request("api/v1/borrowLend".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWapiv1capitalwithdrawals" => self.request("wapi/v1/capital/withdrawals".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1order" => self.request("api/v1/order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1orders" => self.request("api/v1/orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1rfq" => self.request("api/v1/rfq".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1rfqaccept" => self.request("api/v1/rfq/accept".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1rfqrefresh" => self.request("api/v1/rfq/refresh".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1rfqcancel" => self.request("api/v1/rfq/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1rfqquote" => self.request("api/v1/rfq/quote".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1order" => self.request("api/v1/order".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1orders" => self.request("api/v1/orders".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BackpackImpl(Value);
impl Exchange for BackpackImpl {}
impl Backpack for BackpackImpl {}
impl ValueTrait for BackpackImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl BackpackImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = BackpackImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Backpack::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256") }
fn sha384() -> Value { Value::from("sha384") }
fn sha512() -> Value { Value::from("sha512") }
fn md5() -> Value { Value::from("md5") }
fn ed25519() -> Value { Value::from("ed25519") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1") }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Deribit : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "deribit",
            "name": "Deribit",
            "countries": [
                "NL"
            ],
            "enableRateLimit": true,
            "rateLimit": 50,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "CORS": true,
                "sandbox": true,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": true,
                "option": true,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "cancelOrders": false,
                "createDepositAddress": true,
                "createLimitOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createOrder": true,
                "createReduceOnlyOrder": true,
                "createStopLimitOrder": true,
                "createStopMarketOrder": true,
                "createStopOrder": true,
                "createTrailingAmountOrder": true,
                "editOrder": true,
                "fetchAccounts": true,
                "fetchBalance": true,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDeposit": false,
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchDepositWithdrawFees": true,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": true,
                "fetchGreeks": true,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchLeverageTiers": false,
                "fetchLiquidations": true,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyLiquidations": true,
                "fetchMySettlementHistory": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterest": true,
                "fetchOpenInterests": false,
                "fetchOpenOrders": true,
                "fetchOption": true,
                "fetchOptionChain": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": false,
                "fetchOrderTrades": true,
                "fetchPosition": true,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchTransactions": false,
                "fetchTransfer": false,
                "fetchTransfers": true,
                "fetchUnderlyingAssets": false,
                "fetchVolatilityHistory": true,
                "fetchWithdrawal": false,
                "fetchWithdrawals": true,
                "transfer": true,
                "watchBalance": true,
                "watchMyTrades": true,
                "watchOHLCV": true,
                "watchOHLCVForSymbols": true,
                "watchOrderBook": true,
                "watchBidsAsks": true,
                "watchOrderBookForSymbols": true,
                "watchOrders": true,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": true,
                "withdraw": true,
                "ws": true
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/1294454/41933112-9e2dd65a-798b-11e8-8440-5bab2959fcb8.jpg",
                "api": {
                    "rest": "https://www.deribit.com",
                    "ws": "wss://www.deribit.com/ws/api/v2"
                },
                "www": "https://www.deribit.com",
                "doc": [
                    "https://docs.deribit.com/v2",
                    "https://github.com/deribit"
                ],
                "fees": "https://www.deribit.com/pages/information/fees",
                "test": {
                    "rest": "https://test.deribit.com",
                    "ws": "wss://test.deribit.com/ws/api/v2"
                },
                "referral": {
                    "url": "https://www.deribit.com/reg-1189.4038",
                    "discount": 0.1
                }
            },
            "api": {
                "public": {
                    "get": {
                        "auth": 1,
                        "exchange_token": 1,
                        "fork_token": 1,
                        "set_heartbeat": 1,
                        "disable_heartbeat": 1,
                        "get_time": 1,
                        "hello": 1,
                        "status": 1,
                        "test": 1,
                        "subscribe": 1,
                        "unsubscribe": 1,
                        "unsubscribe_all": 1,
                        "get_announcements": 1,
                        "get_book_summary_by_currency": 1,
                        "get_book_summary_by_instrument": 1,
                        "get_contract_size": 1,
                        "get_currencies": 1,
                        "get_delivery_prices": 1,
                        "get_funding_chart_data": 1,
                        "get_funding_rate_history": 1,
                        "get_funding_rate_value": 1,
                        "get_historical_volatility": 1,
                        "get_index": 1,
                        "get_index_price": 1,
                        "get_index_price_names": 1,
                        "get_instrument": 1,
                        "get_instruments": 1,
                        "get_last_settlements_by_currency": 1,
                        "get_last_settlements_by_instrument": 1,
                        "get_last_trades_by_currency": 1,
                        "get_last_trades_by_currency_and_time": 1,
                        "get_last_trades_by_instrument": 1,
                        "get_last_trades_by_instrument_and_time": 1,
                        "get_mark_price_history": 1,
                        "get_order_book": 1,
                        "get_trade_volumes": 1,
                        "get_tradingview_chart_data": 1,
                        "get_volatility_index_data": 1,
                        "ticker": 1
                    }
                },
                "private": {
                    "get": {
                        "logout": 1,
                        "enable_cancel_on_disconnect": 1,
                        "disable_cancel_on_disconnect": 1,
                        "get_cancel_on_disconnect": 1,
                        "subscribe": 1,
                        "unsubscribe": 1,
                        "unsubscribe_all": 1,
                        "change_api_key_name": 1,
                        "change_scope_in_api_key": 1,
                        "change_subaccount_name": 1,
                        "create_api_key": 1,
                        "create_subaccount": 1,
                        "disable_api_key": 1,
                        "disable_tfa_for_subaccount": 1,
                        "enable_affiliate_program": 1,
                        "enable_api_key": 1,
                        "get_access_log": 1,
                        "get_account_summary": 1,
                        "get_account_summaries": 1,
                        "get_affiliate_program_info": 1,
                        "get_email_language": 1,
                        "get_new_announcements": 1,
                        "get_portfolio_margins": 1,
                        "get_position": 1,
                        "get_positions": 1,
                        "get_subaccounts": 1,
                        "get_subaccounts_details": 1,
                        "get_transaction_log": 1,
                        "list_api_keys": 1,
                        "remove_api_key": 1,
                        "remove_subaccount": 1,
                        "reset_api_key": 1,
                        "set_announcement_as_read": 1,
                        "set_api_key_as_default": 1,
                        "set_email_for_subaccount": 1,
                        "set_email_language": 1,
                        "set_password_for_subaccount": 1,
                        "toggle_notifications_from_subaccount": 1,
                        "toggle_subaccount_login": 1,
                        "execute_block_trade": 4,
                        "get_block_trade": 1,
                        "get_last_block_trades_by_currency": 1,
                        "invalidate_block_trade_signature": 1,
                        "verify_block_trade": 4,
                        "buy": 4,
                        "sell": 4,
                        "edit": 4,
                        "edit_by_label": 4,
                        "cancel": 4,
                        "cancel_all": 4,
                        "cancel_all_by_currency": 4,
                        "cancel_all_by_instrument": 4,
                        "cancel_by_label": 4,
                        "close_position": 4,
                        "get_margins": 1,
                        "get_mmp_config": 1,
                        "get_open_orders_by_currency": 1,
                        "get_open_orders_by_instrument": 1,
                        "get_order_history_by_currency": 1,
                        "get_order_history_by_instrument": 1,
                        "get_order_margin_by_ids": 1,
                        "get_order_state": 1,
                        "get_stop_order_history": 1,
                        "get_trigger_order_history": 1,
                        "get_user_trades_by_currency": 1,
                        "get_user_trades_by_currency_and_time": 1,
                        "get_user_trades_by_instrument": 1,
                        "get_user_trades_by_instrument_and_time": 1,
                        "get_user_trades_by_order": 1,
                        "reset_mmp": 1,
                        "set_mmp_config": 1,
                        "get_settlement_history_by_instrument": 1,
                        "get_settlement_history_by_currency": 1,
                        "cancel_transfer_by_id": 1,
                        "cancel_withdrawal": 1,
                        "create_deposit_address": 1,
                        "get_current_deposit_address": 1,
                        "get_deposits": 1,
                        "get_transfers": 1,
                        "get_withdrawals": 1,
                        "submit_transfer_to_subaccount": 1,
                        "submit_transfer_to_user": 1,
                        "withdraw": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1",
                "3m": "3",
                "5m": "5",
                "10m": "10",
                "15m": "15",
                "30m": "30",
                "1h": "60",
                "2h": "120",
                "3h": "180",
                "6h": "360",
                "12h": "720",
                "1d": "1D"
            },
            "fees": {
                "trading": {},
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {},
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v2",
            "features": {
                "default": {
                    "sandbox": true,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerPriceType": {
                            "last": true,
                            "mark": true,
                            "index": true
                        },
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": true
                        },
                        "hedged": false,
                        "selfTradePrevention": false,
                        "trailing": true,
                        "leverage": false,
                        "marketBuyByCost": true,
                        "marketBuyRequiresPrice": false,
                        "iceberg": true
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": true
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "daysBackCanceled": 1,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "spot": {
                    "extends": "default"
                },
                "swap": {
                    "linear": {
                        "extends": "default"
                    },
                    "inverse": {
                        "extends": "default"
                    }
                },
                "future": {
                    "linear": {
                        "extends": "default"
                    },
                    "inverse": {
                        "extends": "default"
                    }
                }
            },
            "options": {
                "code": "BTC",
                "fetchBalance": {
                    "code": "BTC"
                },
                "transfer": {
                    "method": "privateGetSubmitTransferToSubaccount"
                },
                "ws": {
                    "timeframes": {
                        "1m": "1",
                        "3m": "3",
                        "5m": "5",
                        "15m": "15",
                        "30m": "30",
                        "1h": "60",
                        "2h": "120",
                        "4h": "180",
                        "6h": "360",
                        "12h": "720",
                        "1d": "1D"
                    },
                    "watchTradesForSymbols": {
                        "interval": "100ms"
                    },
                    "watchOrderBookForSymbols": {
                        "interval": "100ms",
                        "useDepthEndpoint": false,
                        "depth": "20",
                        "group": "none"
                    }
                },
                "currencies": [
                    "BTC",
                    "ETH",
                    "SOL",
                    "USDC"
                ]
            },
            "streaming": {}
        }"###).unwrap())
    }

    fn request_id(&mut self) -> Value {
        let mut request_id: Value = self.sum(self.safe_integer(self.get("options".into()), Value::from("requestId"), Value::from(0)), Value::from(1));
        self.get("options".into()).set("requestId".into(), request_id.clone());
        return request_id.clone();
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Deribit>::authenticate(self, params.clone()).await;
        let mut message_hash: Value = Value::from("balance");
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut currencies: Value = self.safe_value(self.get("options".into()), Value::from("currencies"), Value::new_array());
        let mut channels: Value = Value::new_array();
        let mut i: usize = 0;
        while i < currencies.len() {
            let mut currency_code: Value = currencies.get(i.into());
            channels.push(Value::from("user.portfolio.") + currency_code.clone());
            i += 1;
        };
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "jsonrpc": "2.0",
            "method": "private/subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channels": channels
            }))).unwrap()),
            "id": <Self as Deribit>::request_id(self)
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(subscribe.clone(), params.clone());
        return self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), request.clone()).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // subscription
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "user.portfolio.btc",
        //             "data": {
        //                 "total_pl": 0,
        //                 "session_upl": 0,
        //                 "session_rpl": 0,
        //                 "projected_maintenance_margin": 0,
        //                 "projected_initial_margin": 0,
        //                 "projected_delta_total": 0,
        //                 "portfolio_margining_enabled": false,
        //                 "options_vega": 0,
        //                 "options_value": 0,
        //                 "options_theta": 0,
        //                 "options_session_upl": 0,
        //                 "options_session_rpl": 0,
        //                 "options_pl": 0,
        //                 "options_gamma": 0,
        //                 "options_delta": 0,
        //                 "margin_balance": 0.0015,
        //                 "maintenance_margin": 0,
        //                 "initial_margin": 0,
        //                 "futures_session_upl": 0,
        //                 "futures_session_rpl": 0,
        //                 "futures_pl": 0,
        //                 "fee_balance": 0,
        //                 "estimated_liquidation_ratio_map": {},
        //                 "estimated_liquidation_ratio": 0,
        //                 "equity": 0.0015,
        //                 "delta_total_map": {},
        //                 "delta_total": 0,
        //                 "currency": "BTC",
        //                 "balance": 0.0015,
        //                 "available_withdrawal_funds": 0.0015,
        //                 "available_funds": 0.0015
        //             }
        //         }
        //     }
        //
        let mut params: Value = self.safe_value(message.clone(), Value::from("params"), Value::new_object());
        let mut data: Value = self.safe_value(params.clone(), Value::from("data"), Value::new_object());
        self.get("balance".into()).set("info".into(), data.clone());
        let mut currency_id: Value = self.safe_string(data.clone(), Value::from("currency"), Value::Undefined);
        let mut currency_code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
        let mut balance: Value = self.parse_balance(data.clone());
        self.get("balance".into()).set(currency_code.clone(), balance.clone());
        let mut message_hash: Value = Value::from("balance");
        client.resolve(self.get("balance".into()), message_hash.clone());
        Value::Undefined
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut interval: Value = self.safe_string(params.clone(), Value::from("interval"), Value::from("100ms"));
        params = self.omit(params.clone(), Value::from("interval"));
        self.load_markets(Value::Undefined, Value::Undefined).await;
        if interval.clone() == Value::from("raw") {
            <Self as Deribit>::authenticate(self).await;
        };
        let mut channel: Value = Value::from("ticker.") + market.get(Value::from("id")) + Value::from(".") + interval.clone();
        let mut message: Value = Value::Json(normalize(&Value::Json(json!({
            "jsonrpc": "2.0",
            "method": "public/subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channels": Value::Json(serde_json::Value::Array(vec![Value::from("ticker.") + market.get(Value::from("id")) + Value::from(".") + interval.clone().into()]))
            }))).unwrap()),
            "id": <Self as Deribit>::request_id(self)
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(message.clone(), params.clone());
        return self.watch(url.clone(), channel.clone(), request.clone(), channel.clone(), request.clone()).await;
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut interval: Value = self.safe_string(params.clone(), Value::from("interval"), Value::from("100ms"));
        params = self.omit(params.clone(), Value::from("interval"));
        self.load_markets(Value::Undefined, Value::Undefined).await;
        if interval.clone() == Value::from("raw") {
            <Self as Deribit>::authenticate(self).await;
        };
        let mut channels: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut market: Value = self.market(symbols.get(i.into()));
            channels.push(Value::from("ticker.") + market.get(Value::from("id")) + Value::from(".") + interval.clone());
            i += 1;
        };
        let mut message: Value = Value::Json(normalize(&Value::Json(json!({
            "jsonrpc": "2.0",
            "method": "public/subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channels": channels
            }))).unwrap()),
            "id": <Self as Deribit>::request_id(self)
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(message.clone(), params.clone());
        let mut new_tickers: Value = self.watch_multiple(url.clone(), channels.clone(), request.clone(), channels.clone(), request.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut tickers: Value = Value::new_object();
            tickers.set(new_tickers.get(Value::from("symbol")), new_tickers.clone());
            return tickers.clone();
        };
        return self.filter_by_array(self.get("tickers".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "ticker.BTC_USDC-PERPETUAL.raw",
        //             "data": {
        //                 "timestamp": 1655393725040,
        //                 "stats": [Object],
        //                 "state": "open",
        //                 "settlement_price": 21729.5891,
        //                 "open_interest": 164.501,
        //                 "min_price": 20792.9376,
        //                 "max_price": 21426.225,
        //                 "mark_price": 21109.555,
        //                 "last_price": 21132,
        //                 "instrument_name": "BTC_USDC-PERPETUAL",
        //                 "index_price": 21122.3937,
        //                 "funding_8h": -0.00022427,
        //                 "estimated_delivery_price": 21122.3937,
        //                 "current_funding": -0.00010782,
        //                 "best_bid_price": 21106,
        //                 "best_bid_amount": 1.143,
        //                 "best_ask_price": 21113,
        //                 "best_ask_amount": 0.327
        //             }
        //         }
        //     }
        //
        let mut params: Value = self.safe_value(message.clone(), Value::from("params"), Value::new_object());
        let mut data: Value = self.safe_value(params.clone(), Value::from("data"), Value::new_object());
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("instrument_name"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut ticker: Value = self.parse_ticker(data.clone(), Value::Undefined);
        let mut message_hash: Value = self.safe_string(params.clone(), Value::from("channel"), Value::Undefined);
        self.get("tickers".into()).set(symbol.clone(), ticker.clone());
        client.resolve(ticker.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut channels: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut market: Value = self.market(symbols.get(i.into()));
            channels.push(Value::from("quote.") + market.get(Value::from("id")));
            i += 1;
        };
        let mut message: Value = Value::Json(normalize(&Value::Json(json!({
            "jsonrpc": "2.0",
            "method": "public/subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channels": channels
            }))).unwrap()),
            "id": <Self as Deribit>::request_id(self)
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(message.clone(), params.clone());
        let mut new_tickers: Value = self.watch_multiple(url.clone(), channels.clone(), request.clone(), channels.clone(), request.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut tickers: Value = Value::new_object();
            tickers.set(new_tickers.get(Value::from("symbol")), new_tickers.clone());
            return tickers.clone();
        };
        return self.filter_by_array(self.get("bidsasks".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    fn handle_bid_ask(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "quote.BTC_USDT",
        //             "data": {
        //                 "best_bid_amount": 0.026,
        //                 "best_ask_amount": 0.026,
        //                 "best_bid_price": 63908,
        //                 "best_ask_price": 63940,
        //                 "instrument_name": "BTC_USDT",
        //                 "timestamp": 1727765131750
        //             }
        //         }
        //     }
        //
        let mut params: Value = self.safe_dict(message.clone(), Value::from("params"), Value::new_object());
        let mut data: Value = self.safe_dict(params.clone(), Value::from("data"), Value::new_object());
        let mut ticker: Value = <Self as Deribit>::parse_ws_bid_ask(self, data.clone());
        let mut symbol: Value = ticker.get(Value::from("symbol"));
        self.get("bidsasks".into()).set(symbol.clone(), ticker.clone());
        let mut message_hash: Value = self.safe_string(params.clone(), Value::from("channel"), Value::Undefined);
        client.resolve(ticker.clone(), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_bid_ask(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("instrument_name"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("timestamp"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "ask": self.safe_string(ticker.clone(), Value::from("best_ask_price"), Value::Undefined),
            "askVolume": self.safe_string(ticker.clone(), Value::from("best_ask_amount"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("best_bid_price"), Value::Undefined),
            "bidVolume": self.safe_string(ticker.clone(), Value::from("best_bid_amount"), Value::Undefined),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        params.set("callerMethodName".into(), Value::from("watchTrades"));
        return <Self as Deribit>::watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn watch_trades_for_symbols(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut interval: Value = Value::Undefined;
        (interval, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchTradesForSymbols"), Value::from("interval"), Value::from("100ms")));
        if interval.clone() == Value::from("raw") {
            <Self as Deribit>::authenticate(self).await;
        };
        let mut trades: Value = <Self as Deribit>::watch_multiple_wrapper(self, Value::from("trades"), interval.clone(), symbols.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut first: Value = self.safe_dict(trades.clone(), Value::from(0), Value::Undefined);
            let mut trade_symbol: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
            limit = trades.get_limit(trade_symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "trades.BTC_USDC-PERPETUAL.100ms",
        //             "data": [{
        //                 "trade_seq": 501899,
        //                 "trade_id": "USDC-2436803",
        //                 "timestamp": 1655397355998,
        //                 "tick_direction": 2,
        //                 "price": 21026,
        //                 "mark_price": 21019.9719,
        //                 "instrument_name": "BTC_USDC-PERPETUAL",
        //                 "index_price": 21031.7847,
        //                 "direction": "buy",
        //                 "amount": 0.049
        //             }]
        //         }
        //     }
        //
        let mut params: Value = self.safe_dict(message.clone(), Value::from("params"), Value::new_object());
        let mut channel: Value = self.safe_string(params.clone(), Value::from("channel"), Value::from(""));
        let mut parts: Value = channel.split(Value::from("."));
        let mut market_id: Value = self.safe_string(parts.clone(), Value::from(1), Value::Undefined);
        let mut interval: Value = self.safe_string(parts.clone(), Value::from(2), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut trades: Value = self.safe_list(params.clone(), Value::from("data"), Value::new_array());
        if self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            self.get("trades".into()).set(symbol.clone(), ArrayCache::new(limit));
        };
        let mut stored: Value = self.get("trades".into()).get(symbol.clone());
        let mut i: usize = 0;
        while i < trades.len() {
            let mut trade: Value = trades.get(i.into());
            let mut parsed: Value = self.parse_trade(trade.clone(), market.clone());
            stored.append(parsed.clone());
            i += 1;
        };
        self.get("trades".into()).set(symbol.clone(), stored.clone());
        let mut message_hash: Value = Value::from("trades|") + symbol.clone() + Value::from("|") + interval.clone();
        client.resolve(self.get("trades".into()).get(symbol.clone()), message_hash.clone());
        Value::Undefined
    }

    async fn watch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Deribit>::authenticate(self, params.clone()).await;
        if symbol.clone().is_nonnullish() {
            self.load_markets(Value::Undefined, Value::Undefined).await;
            symbol = self.symbol(symbol.clone());
        };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut interval: Value = self.safe_string(params.clone(), Value::from("interval"), Value::from("raw"));
        params = self.omit(params.clone(), Value::from("interval"));
        let mut channel: Value = Value::from("user.trades.any.any.") + interval.clone();
        let mut message: Value = Value::Json(normalize(&Value::Json(json!({
            "jsonrpc": "2.0",
            "method": "private/subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channels": Value::Json(serde_json::Value::Array(vec![channel.clone().into()]))
            }))).unwrap()),
            "id": <Self as Deribit>::request_id(self)
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(message.clone(), params.clone());
        let mut trades: Value = self.watch(url.clone(), channel.clone(), request.clone(), channel.clone(), request.clone()).await;
        return self.filter_by_symbol_since_limit(trades.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_my_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "user.trades.any.any.raw",
        //             "data": [{
        //                 "trade_seq": 149546319,
        //                 "trade_id": "219381310",
        //                 "timestamp": 1655421193564,
        //                 "tick_direction": 0,
        //                 "state": "filled",
        //                 "self_trade": false,
        //                 "reduce_only": false,
        //                 "profit_loss": 0,
        //                 "price": 20236.5,
        //                 "post_only": false,
        //                 "order_type": "market",
        //                 "order_id": "46108941243",
        //                 "matching_id": null,
        //                 "mark_price": 20233.96,
        //                 "liquidity": "T",
        //                 "instrument_name": "BTC-PERPETUAL",
        //                 "index_price": 20253.31,
        //                 "fee_currency": "BTC",
        //                 "fee": 2.5e-7,
        //                 "direction": "buy",
        //                 "amount": 10
        //             }]
        //         }
        //     }
        //
        let mut params: Value = self.safe_value(message.clone(), Value::from("params"), Value::new_object());
        let mut channel: Value = self.safe_string(params.clone(), Value::from("channel"), Value::from(""));
        let mut trades: Value = self.safe_value(params.clone(), Value::from("data"), Value::new_array());
        let mut cached_trades: Value = self.get("myTrades".into());
        if cached_trades.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            cached_trades = ArrayCacheBySymbolById::new(limit);
        };
        let mut parsed: Value = self.parse_trades(trades.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut market_ids: Value = Value::new_object();
        let mut i: usize = 0;
        while i < parsed.len() {
            let mut trade: Value = parsed.get(i.into());
            cached_trades.append(trade.clone());
            let mut symbol: Value = trade.get(Value::from("symbol"));
            market_ids.set(symbol.clone(), true.into());
            i += 1;
        };
        client.resolve(cached_trades.clone(), channel.clone());
        Value::Undefined
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        params.set("callerMethodName".into(), Value::from("watchOrderBook"));
        return <Self as Deribit>::watch_order_book_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), limit.clone(), params.clone()).await;
    }

    async fn watch_order_book_for_symbols(&mut self, mut symbols: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut interval: Value = Value::Undefined;
        (interval, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchOrderBookForSymbols"), Value::from("interval"), Value::from("100ms")));
        if interval.clone() == Value::from("raw") {
            <Self as Deribit>::authenticate(self).await;
        };
        let mut descriptor: Value = Value::from("");
        let mut use_depth_endpoint: Value = Value::Undefined;
        // for more info, see comment in .options
        (use_depth_endpoint, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchOrderBookForSymbols"), Value::from("useDepthEndpoint"), false.into()));
        if use_depth_endpoint.is_truthy() {
            let mut depth: Value = Value::Undefined;
            (depth, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchOrderBookForSymbols"), Value::from("depth"), Value::from("20")));
            let mut group: Value = Value::Undefined;
            (group, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("watchOrderBookForSymbols"), Value::from("group"), Value::from("none")));
            descriptor = group.clone() + Value::from(".") + depth.clone() + Value::from(".") + interval.clone();
        } else {
            descriptor = interval.clone();
        };
        let mut orderbook: Value = <Self as Deribit>::watch_multiple_wrapper(self, Value::from("book"), descriptor.clone(), symbols.clone(), params.clone()).await;
        return orderbook.limit();
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //  snapshot
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "book.BTC_USDC-PERPETUAL.raw",
        //             "data": {
        //                 "type": "snapshot",
        //                 "timestamp": 1655395057025,
        //                 "instrument_name": "BTC_USDC-PERPETUAL",
        //                 "change_id": 1550694837,
        //                 "bids": [
        //                     ["new", 20987, 0.487],
        //                     ["new", 20986, 0.238],
        //                 ],
        //                 "asks": [
        //                     ["new", 20999, 0.092],
        //                     ["new", 21000, 1.238],
        //                 ]
        //             }
        //         }
        //     }
        //
        //  change
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "book.BTC_USDC-PERPETUAL.raw",
        //             "data": {
        //                 "type": "change",
        //                 "timestamp": 1655395168086,
        //                 "prev_change_id": 1550724481,
        //                 "instrument_name": "BTC_USDC-PERPETUAL",
        //                 "change_id": 1550724483,
        //                 "bids": [
        //                     ["new", 20977, 0.109],
        //                     ["delete", 20975, 0]
        //                 ],
        //                 "asks": []
        //             }
        //         }
        //     }
        //
        let mut params: Value = self.safe_value(message.clone(), Value::from("params"), Value::new_object());
        let mut data: Value = self.safe_value(params.clone(), Value::from("data"), Value::new_object());
        let mut channel: Value = self.safe_string(params.clone(), Value::from("channel"), Value::Undefined);
        let mut parts: Value = channel.split(Value::from("."));
        let mut descriptor: Value = Value::from("");
        let mut parts_length: usize = parts.len();
        let mut is_detailed: Value = (parts_length.clone() == Value::from(5)).into();
        if is_detailed.is_truthy() {
            let mut group: Value = self.safe_string(parts.clone(), Value::from(2), Value::Undefined);
            let mut depth: Value = self.safe_string(parts.clone(), Value::from(3), Value::Undefined);
            let mut interval: Value = self.safe_string(parts.clone(), Value::from(4), Value::Undefined);
            descriptor = group.clone() + Value::from(".") + depth.clone() + Value::from(".") + interval.clone();
        } else {
            let mut interval: Value = self.safe_string(parts.clone(), Value::from(2), Value::Undefined);
            descriptor = interval.clone();
        };
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("instrument_name"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut timestamp: Value = self.safe_integer(data.clone(), Value::from("timestamp"), Value::Undefined);
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            self.get("orderbooks".into()).set(symbol.clone(), self.counted_order_book(Value::Undefined, Value::Undefined));
        };
        let mut stored_order_book: Value = self.get("orderbooks".into()).get(symbol.clone());
        let mut asks: Value = self.safe_list(data.clone(), Value::from("asks"), Value::new_array());
        let mut bids: Value = self.safe_list(data.clone(), Value::from("bids"), Value::new_array());
        <Self as Deribit>::handle_deltas(self, stored_order_book.get(Value::from("asks")), asks.clone());
        <Self as Deribit>::handle_deltas(self, stored_order_book.get(Value::from("bids")), bids.clone());
        stored_order_book.set("nonce".into(), timestamp.clone());
        stored_order_book.set("timestamp".into(), timestamp.clone());
        stored_order_book.set("datetime".into(), self.iso8601(timestamp.clone()));
        stored_order_book.set("symbol".into(), symbol.clone());
        self.get("orderbooks".into()).set(symbol.clone(), stored_order_book.clone());
        let mut message_hash: Value = Value::from("book|") + symbol.clone() + Value::from("|") + descriptor.clone();
        client.resolve(stored_order_book.clone(), message_hash.clone());
        Value::Undefined
    }

    fn clean_order_book(&mut self, mut data: Value) -> Value {
        let mut bids: Value = self.safe_list(data.clone(), Value::from("bids"), Value::new_array());
        let mut asks: Value = self.safe_list(data.clone(), Value::from("asks"), Value::new_array());
        let mut cleaned_bids: Value = Value::new_array();
        let mut i: usize = 0;
        while i < bids.len() {
            cleaned_bids.push(Value::Json(serde_json::Value::Array(vec![bids.get(i.into()).get(Value::from(1)).into(), bids.get(i.into()).get(Value::from(2)).into()])));
            i += 1;
        };
        let mut cleaned_asks: Value = Value::new_array();
        let mut i: usize = 0;
        while i < asks.len() {
            cleaned_asks.push(Value::Json(serde_json::Value::Array(vec![asks.get(i.into()).get(Value::from(1)).into(), asks.get(i.into()).get(Value::from(2)).into()])));
            i += 1;
        };
        data.set("bids".into(), cleaned_bids.clone());
        data.set("asks".into(), cleaned_asks.clone());
        return data.clone();
    }

    fn handle_delta(&mut self, mut bookside: Value, mut delta: Value) -> Value {
        let mut price: Value = delta.get(Value::from(1));
        let mut amount: Value = delta.get(Value::from(2));
        if delta.get(Value::from(0)) == Value::from("new") || delta.get(Value::from(0)) == Value::from("change") {
            bookside.store_array(Value::Json(serde_json::Value::Array(vec![price.clone().into(), amount.clone().into(), Value::from(1).into()])));
        } else if delta.get(Value::from(0)) == Value::from("delete") {
            bookside.store_array(Value::Json(serde_json::Value::Array(vec![price.clone().into(), amount.clone().into(), Value::from(0).into()])));
        };
        Value::Undefined
    }

    fn handle_deltas(&mut self, mut bookside: Value, mut deltas: Value) -> Value {
        let mut i: usize = 0;
        while i < deltas.len() {
            <Self as Deribit>::handle_delta(self, bookside.clone(), deltas.get(i.into()));
            i += 1;
        };
        Value::Undefined
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        <Self as Deribit>::authenticate(self, params.clone()).await;
        if symbol.clone().is_nonnullish() {
            symbol = self.symbol(symbol.clone());
        };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut currency: Value = self.safe_string(params.clone(), Value::from("currency"), Value::from("any"));
        let mut interval: Value = self.safe_string(params.clone(), Value::from("interval"), Value::from("raw"));
        let mut kind: Value = self.safe_string(params.clone(), Value::from("kind"), Value::from("any"));
        params = self.omit(params.clone(), Value::from("interval"));
        let mut channel: Value = Value::from("user.orders.") + kind.clone() + Value::from(".") + currency.clone() + Value::from(".") + interval.clone();
        let mut message: Value = Value::Json(normalize(&Value::Json(json!({
            "jsonrpc": "2.0",
            "method": "private/subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channels": Value::Json(serde_json::Value::Array(vec![channel.clone().into()]))
            }))).unwrap()),
            "id": <Self as Deribit>::request_id(self)
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(message.clone(), params.clone());
        let mut orders: Value = self.watch(url.clone(), channel.clone(), request.clone(), channel.clone(), request.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_orders(&mut self, mut client: Value, mut message: Value) -> Value {
        // Does not return a snapshot of current orders
        //
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "user.orders.any.any.raw",
        //             "data": {
        //                 "web": true,
        //                 "time_in_force": "good_til_cancelled",
        //                 "replaced": false,
        //                 "reduce_only": false,
        //                 "profit_loss": 0,
        //                 "price": 50000,
        //                 "post_only": false,
        //                 "order_type": "limit",
        //                 "order_state": "open",
        //                 "order_id": "46094375191",
        //                 "max_show": 10,
        //                 "last_update_timestamp": 1655401625037,
        //                 "label": '',
        //                 "is_liquidation": false,
        //                 "instrument_name": "BTC-PERPETUAL",
        //                 "filled_amount": 0,
        //                 "direction": "sell",
        //                 "creation_timestamp": 1655401625037,
        //                 "commission": 0,
        //                 "average_price": 0,
        //                 "api": false,
        //                 "amount": 10
        //             }
        //         }
        //     }
        //
        if self.get("orders".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
            self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
        };
        let mut params: Value = self.safe_value(message.clone(), Value::from("params"), Value::new_object());
        let mut channel: Value = self.safe_string(params.clone(), Value::from("channel"), Value::from(""));
        let mut data: Value = self.safe_value(params.clone(), Value::from("data"), Value::new_object());
        let mut orders: Value = Value::new_array();
        if Array::is_array(data.clone()).is_truthy() {
            orders = self.parse_orders(data.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        } else {
            let mut order: Value = self.parse_order(data.clone(), Value::Undefined);
            orders = Value::Json(serde_json::Value::Array(vec![order.clone().into()]));
        };
        let mut cached_orders: Value = self.get("orders".into());
        let mut i: usize = 0;
        while i < orders.len() {
            cached_orders.append(orders.get(i.into()));
            i += 1;
        };
        client.resolve(self.get("orders".into()), channel.clone());
        Value::Undefined
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbol = self.symbol(symbol.clone());
        let mut ohlcvs: Value = <Self as Deribit>::watch_ohlcv_for_symbols(self, Value::Json(serde_json::Value::Array(vec![Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), timeframe.clone().into()])).into()])), since.clone(), limit.clone(), params.clone()).await;
        return ohlcvs.get(symbol.clone()).get(timeframe.clone());
    }

    async fn watch_ohlcv_for_symbols(&mut self, mut symbols_and_timeframes: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut symbols_length: usize = symbols_and_timeframes.len();
        if symbols_length.clone() == Value::from(0) || !Array::is_array(symbols_and_timeframes.get(Value::from(0))).is_truthy() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" watchOHLCVForSymbols() requires a an array of symbols and timeframes, like  [['BTC/USDT', '1m'], ['LTC/USDT', '5m']]"))"###);
        };
        let (mut symbol, mut timeframe, mut candles) = shift_3(<Self as Deribit>::watch_multiple_wrapper(self, Value::from("chart.trades"), Value::Undefined, symbols_and_timeframes.clone(), params.clone()).await);
        if self.get("newUpdates".into()).is_truthy() {
            limit = candles.get_limit(symbol.clone(), limit.clone());
        };
        let mut filtered: Value = self.filter_by_since_limit(candles.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
        return self.create_ohlcv_object(symbol.clone(), timeframe.clone(), filtered.clone());
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "chart.trades.BTC_USDC-PERPETUAL.1",
        //             "data": {
        //                 "volume": 0,
        //                 "tick": 1655403420000,
        //                 "open": 20951,
        //                 "low": 20951,
        //                 "high": 20951,
        //                 "cost": 0,
        //                 "close": 20951
        //             }
        //         }
        //     }
        //
        let mut params: Value = self.safe_dict(message.clone(), Value::from("params"), Value::new_object());
        let mut channel: Value = self.safe_string(params.clone(), Value::from("channel"), Value::from(""));
        let mut parts: Value = channel.split(Value::from("."));
        let mut market_id: Value = self.safe_string(parts.clone(), Value::from(2), Value::Undefined);
        let mut raw_timeframe: Value = self.safe_string(parts.clone(), Value::from(3), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut ws_options: Value = self.safe_dict(self.get("options".into()), Value::from("ws"), Value::new_object());
        let mut timeframes: Value = self.safe_dict(ws_options.clone(), Value::from("timeframes"), Value::new_object());
        let mut unified_timeframe: Value = self.find_timeframe(raw_timeframe.clone(), timeframes.clone());
        self.get("ohlcvs".into()).set(symbol.clone(), self.safe_dict(self.get("ohlcvs".into()), symbol.clone(), Value::new_object()));
        if self.safe_value(self.get("ohlcvs".into()).get(symbol.clone()), unified_timeframe.clone(), Value::Undefined).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            self.get("ohlcvs".into()).get(symbol.clone()).set(unified_timeframe.clone(), ArrayCacheByTimestamp::new(limit));
        };
        let mut stored: Value = self.get("ohlcvs".into()).get(symbol.clone()).get(unified_timeframe.clone());
        let mut ohlcv: Value = self.safe_dict(params.clone(), Value::from("data"), Value::new_object());
        // data contains a single OHLCV candle
        let mut parsed: Value = <Self as Deribit>::parse_ws_ohlcv(self, ohlcv.clone(), market.clone());
        stored.append(parsed.clone());
        self.get("ohlcvs".into()).get(symbol.clone()).set(unified_timeframe.clone(), stored.clone());
        let mut resolve_data: Value = Value::Json(serde_json::Value::Array(vec![symbol.clone().into(), unified_timeframe.clone().into(), stored.clone().into()]));
        let mut message_hash: Value = Value::from("chart.trades|") + symbol.clone() + Value::from("|") + raw_timeframe.clone();
        client.resolve(resolve_data.clone(), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        //    {
        //        "c": "28909.0",
        //        "o": "28915.4",
        //        "h": "28915.4",
        //        "l": "28896.1",
        //        "v": "27.6919",
        //        "T": 1696687499999,
        //        "t": 1696687440000
        //    }
        //
        return Value::Json(serde_json::Value::Array(vec![self.safe_integer(ohlcv.clone(), Value::from("tick"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("open"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("high"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("low"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("close"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("volume"), Value::Undefined).into()]));
    }

    async fn watch_multiple_wrapper(&mut self, mut channel_name: Value, mut channel_descriptor: Value, mut symbols_array: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut raw_subscriptions: Value = Value::new_array();
        let mut message_hashes: Value = Value::new_array();
        let mut is_ohlcv: Value = (channel_name.clone() == Value::from("chart.trades")).into();
        let mut symbols: Value = if is_ohlcv.is_truthy() { self.get_list_from_object_values(symbols_array.clone(), Value::from(0)) } else { symbols_array.clone() };
        self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        let mut i: usize = 0;
        while i < symbols_array.len() {
            let mut current: Value = symbols_array.get(i.into());
            let mut market: Value = Value::Undefined;
            if is_ohlcv.is_truthy() {
                market = self.market(current.get(Value::from(0)));
                let mut unified_tf: Value = current.get(Value::from(1));
                let mut raw_tf: Value = self.safe_string(self.get("timeframes".into()), unified_tf.clone(), unified_tf.clone());
                channel_descriptor = raw_tf.clone();
            } else {
                market = self.market(current.clone());
            };
            let mut message: Value = channel_name.clone() + Value::from(".") + market.get(Value::from("id")) + Value::from(".") + channel_descriptor.clone();
            raw_subscriptions.push(message.clone());
            message_hashes.push(channel_name.clone() + Value::from("|") + market.get(Value::from("symbol")) + Value::from("|") + channel_descriptor.clone());
            i += 1;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "jsonrpc": "2.0",
            "method": "public/subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channels": raw_subscriptions
            }))).unwrap()),
            "id": <Self as Deribit>::request_id(self)
        }))).unwrap());
        let mut extended_request: Value = self.deep_extend_2(request.clone(), params.clone());
        let mut max_message_byte_limit: Value = Value::from(32768) - Value::from(1);
        // 'Message Too Big: limit 32768B'
        let mut jsoned_text: Value = self.json(extended_request.clone());
        if jsoned_text.len() >= max_message_byte_limit.clone().into() {
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" requested subscription length over limit, try to reduce symbols amount"))"###);
        };
        return self.watch_multiple(url.clone(), message_hashes.clone(), extended_request.clone(), raw_subscriptions.clone(), Value::Undefined).await;
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // error
        //     {
        //         "jsonrpc": "2.0",
        //         "id": 1,
        //         "error": {
        //             "message": "Invalid params",
        //             "data": {
        //                 "reason": "invalid format",
        //                 "param": "nonce"
        //             },
        //             "code": -32602
        //         },
        //         "usIn": "1655391709417993",
        //         "usOut": "1655391709418049",
        //         "usDiff": 56,
        //         "testnet": false
        //     }
        //
        // subscribe
        //     {
        //         "jsonrpc": "2.0",
        //         "id": 2,
        //         "result": ["ticker.BTC_USDC-PERPETUAL.raw"],
        //         "usIn": "1655393625889396",
        //         "usOut": "1655393625889518",
        //         "usDiff": 122,
        //         "testnet": false
        //     }
        //
        // notification
        //     {
        //         "jsonrpc": "2.0",
        //         "method": "subscription",
        //         "params": {
        //             "channel": "ticker.BTC_USDC-PERPETUAL.raw",
        //             "data": {
        //                 "timestamp": 1655393724752,
        //                 "stats": [Object],
        //                 "state": "open",
        //                 "settlement_price": 21729.5891,
        //                 "open_interest": 164.501,
        //                 "min_price": 20792.9001,
        //                 "max_price": 21426.1864,
        //                 "mark_price": 21109.4757,
        //                 "last_price": 21132,
        //                 "instrument_name": "BTC_USDC-PERPETUAL",
        //                 "index_price": 21122.3937,
        //                 "funding_8h": -0.00022427,
        //                 "estimated_delivery_price": 21122.3937,
        //                 "current_funding": -0.00011158,
        //                 "best_bid_price": 21106,
        //                 "best_bid_amount": 1.143,
        //                 "best_ask_price": 21113,
        //                 "best_ask_amount": 0.402
        //             }
        //         }
        //     }
        //
        let mut error: Value = self.safe_value(message.clone(), Value::from("error"), Value::Undefined);
        if error.clone().is_nonnullish() {
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" ") + self.json(error.clone()))"###);
        };
        let mut params: Value = self.safe_value(message.clone(), Value::from("params"), Value::Undefined);
        let mut channel: Value = self.safe_string(params.clone(), Value::from("channel"), Value::Undefined);
        if channel.clone().is_nonnullish() {
            let mut parts: Value = channel.split(Value::from("."));
            let mut channel_id: Value = self.safe_string(parts.clone(), Value::from(0), Value::Undefined);
            let mut user_handlers: Value = Value::Json(normalize(&Value::Json(json!({
                "trades": self.get("handleMyTrades".into()),
                "portfolio": self.get("handleBalance".into()),
                "orders": self.get("handleOrders".into())
            }))).unwrap());
            let mut handlers: Value = Value::Json(normalize(&Value::Json(json!({
                "ticker": self.get("handleTicker".into()),
                "quote": self.get("handleBidAsk".into()),
                "book": self.get("handleOrderBook".into()),
                "trades": self.get("handleTrades".into()),
                "chart": self.get("handleOHLCV".into()),
                "user": self.safe_value(user_handlers.clone(), self.safe_string(parts.clone(), Value::from(1), Value::Undefined), Value::Undefined)
            }))).unwrap());
            let mut handler: Value = self.safe_value(handlers.clone(), channel_id.clone(), Value::Undefined);
            if handler.clone().is_nonnullish() {
                handler.call(self, client.clone(), message.clone());
                return Value::Undefined;
            };
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" no handler found for this message ") + self.json(message.clone()))"###);
        };
        let mut result: Value = self.safe_value(message.clone(), Value::from("result"), Value::new_object());
        let mut access_token: Value = self.safe_string(result.clone(), Value::from("access_token"), Value::Undefined);
        if access_token.clone().is_nonnullish() {
            <Self as Deribit>::handle_authentication_message(self, client.clone(), message.clone());
        };
        Value::Undefined
    }

    fn handle_authentication_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "jsonrpc": "2.0",
        //         "id": 1,
        //         "result": {
        //             "token_type": "bearer",
        //             "scope": "account:read_write block_trade:read_write connection custody:read_write mainaccount name:ccxt trade:read_write wallet:read_write",
        //             "refresh_token": "1686927372328.1EzFBRmt.logRQWXkPA1oE_Tk0gRsls9Hau7YN6a321XUBnxvR4x6cryhbkKcniUJU-czA8_zKXrqQGpQmfoDwhLIjIsWCvRuu6otbg-LKWlrtTX1GQqLcPaTTHAdZGTMV-HM8HiS03QBd9MIXWRfF53sKj2hdR9nZPZ6MH1XrkpAZPB_peuEEB9wlcc3elzWEZFtCmiy1fnQ8TPHwAJMt3nuUmEcMLt_-F554qrsg_-I66D9xMiifJj4dBemdPfV_PkGPRIwIoKlxDjyv2-xfCw-4eKyo6Hu1m2h6gT1DPOTxSXcBgfBQjpi-_uY3iAIj7U6xjC46PHthEdquhEuCTZl7UfCRZSAWwZA",
        //             "expires_in": 31536000,
        //             "access_token": "1686923272328.1CkwEx-u.qHradpIulmuoeboKMEi8PkQ1_4DF8yFE2zywBTtkD32sruVC53b1HwL5OWRuh2nYAndXff4xuXIMRkkEfMAFCeq24prihxxinoS8DDVkKBxedGx4CUPJFeXjmh7wuRGqQOLg1plXOpbF3fwF2KPEkAuETwcpcVY6K9HUVjutNRfxFe2TR7CvuS9x8TATvoPeu7H1ezYl-LkKSaRifdTXuwituXgp4oDbPRyQLniEBWuYF9rY7qbABxuOJlXI1VZ63u7Bh0mGWei-KeVeqHGNpy6OgrFRPXPxa9_U7vaxCyHW3zZ9959TQ1QUMLWtUX-NLBEv3BT5eCieW9HORYIOKfsgkpd3"
        //         },
        //         "usIn": "1655391872327712",
        //         "usOut": "1655391872328515",
        //         "usDiff": 803,
        //         "testnet": false
        //     }
        //
        let mut message_hash: Value = Value::from("authenticated");
        client.resolve(message.clone(), message_hash.clone());
        return message.clone();
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws"));
        let mut client: Value = self.client(url.clone());
        let mut time: Value = self.milliseconds();
        let mut time_string: Value = self.number_to_string(time.clone());
        let mut nonce: Value = time_string.clone();
        let mut message_hash: Value = Value::from("authenticated");
        let mut future: Value = self.safe_value(client.get(subscriptions.clone()), message_hash.clone(), Value::Undefined);
        if future.clone().is_nullish() {
            self.check_required_credentials(Value::Undefined);
            let mut request_id: Value = <Self as Deribit>::request_id(self);
            let mut line_break: Value = Value::from("
");
            // eslint-disable-line quotes
            let mut signature: Value = self.hmac(self.encode(time_string.clone() + line_break.clone() + nonce.clone() + line_break.clone()), self.encode(self.get("secret".into())), sha256.clone(), Value::Undefined);
            let mut request: Value = Value::Json(normalize(&Value::Json(json!({
                "jsonrpc": "2.0",
                "id": request_id,
                "method": "public/auth",
                "params": Value::Json(normalize(&Value::Json(json!({
                    "grant_type": "client_signature",
                    "client_id": self.get("apiKey".into()),
                    "timestamp": time,
                    "signature": signature,
                    "nonce": nonce,
                    "data": ""
                }))).unwrap())
            }))).unwrap());
            future = self.watch(url.clone(), message_hash.clone(), extend_2(request.clone(), params.clone()), message_hash.clone(), Value::Undefined).await;
            client.get(subscriptions.clone()).set(message_hash.clone(), future.clone());
        };
        return future.clone();
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetAuth" => Deribit::request(self, "auth".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetExchangetoken" => Deribit::request(self, "exchange_token".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetForktoken" => Deribit::request(self, "fork_token".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetSetheartbeat" => Deribit::request(self, "set_heartbeat".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetDisableheartbeat" => Deribit::request(self, "disable_heartbeat".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGettime" => Deribit::request(self, "get_time".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetHello" => Deribit::request(self, "hello".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStatus" => Deribit::request(self, "status".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTest" => Deribit::request(self, "test".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetSubscribe" => Deribit::request(self, "subscribe".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetUnsubscribe" => Deribit::request(self, "unsubscribe".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetUnsubscribeall" => Deribit::request(self, "unsubscribe_all".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetannouncements" => Deribit::request(self, "get_announcements".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetbooksummarybycurrency" => Deribit::request(self, "get_book_summary_by_currency".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetbooksummarybyinstrument" => Deribit::request(self, "get_book_summary_by_instrument".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetcontractsize" => Deribit::request(self, "get_contract_size".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetcurrencies" => Deribit::request(self, "get_currencies".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetdeliveryprices" => Deribit::request(self, "get_delivery_prices".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetfundingchartdata" => Deribit::request(self, "get_funding_chart_data".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetfundingratehistory" => Deribit::request(self, "get_funding_rate_history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetfundingratevalue" => Deribit::request(self, "get_funding_rate_value".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGethistoricalvolatility" => Deribit::request(self, "get_historical_volatility".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetindex" => Deribit::request(self, "get_index".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetindexprice" => Deribit::request(self, "get_index_price".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetindexpricenames" => Deribit::request(self, "get_index_price_names".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetinstrument" => Deribit::request(self, "get_instrument".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetinstruments" => Deribit::request(self, "get_instruments".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetlastsettlementsbycurrency" => Deribit::request(self, "get_last_settlements_by_currency".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetlastsettlementsbyinstrument" => Deribit::request(self, "get_last_settlements_by_instrument".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetlasttradesbycurrency" => Deribit::request(self, "get_last_trades_by_currency".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetlasttradesbycurrencyandtime" => Deribit::request(self, "get_last_trades_by_currency_and_time".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetlasttradesbyinstrument" => Deribit::request(self, "get_last_trades_by_instrument".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetlasttradesbyinstrumentandtime" => Deribit::request(self, "get_last_trades_by_instrument_and_time".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetmarkpricehistory" => Deribit::request(self, "get_mark_price_history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetorderbook" => Deribit::request(self, "get_order_book".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGettradevolumes" => Deribit::request(self, "get_trade_volumes".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGettradingviewchartdata" => Deribit::request(self, "get_tradingview_chart_data".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetGetvolatilityindexdata" => Deribit::request(self, "get_volatility_index_data".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTicker" => Deribit::request(self, "ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetLogout" => Deribit::request(self, "logout".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetEnablecancelondisconnect" => Deribit::request(self, "enable_cancel_on_disconnect".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetDisablecancelondisconnect" => Deribit::request(self, "disable_cancel_on_disconnect".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetcancelondisconnect" => Deribit::request(self, "get_cancel_on_disconnect".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubscribe" => Deribit::request(self, "subscribe".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUnsubscribe" => Deribit::request(self, "unsubscribe".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUnsubscribeall" => Deribit::request(self, "unsubscribe_all".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetChangeapikeyname" => Deribit::request(self, "change_api_key_name".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetChangescopeinapikey" => Deribit::request(self, "change_scope_in_api_key".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetChangesubaccountname" => Deribit::request(self, "change_subaccount_name".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCreateapikey" => Deribit::request(self, "create_api_key".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCreatesubaccount" => Deribit::request(self, "create_subaccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetDisableapikey" => Deribit::request(self, "disable_api_key".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetDisabletfaforsubaccount" => Deribit::request(self, "disable_tfa_for_subaccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetEnableaffiliateprogram" => Deribit::request(self, "enable_affiliate_program".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetEnableapikey" => Deribit::request(self, "enable_api_key".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetaccesslog" => Deribit::request(self, "get_access_log".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetaccountsummary" => Deribit::request(self, "get_account_summary".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetaccountsummaries" => Deribit::request(self, "get_account_summaries".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetaffiliateprograminfo" => Deribit::request(self, "get_affiliate_program_info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetemaillanguage" => Deribit::request(self, "get_email_language".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetnewannouncements" => Deribit::request(self, "get_new_announcements".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetportfoliomargins" => Deribit::request(self, "get_portfolio_margins".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetposition" => Deribit::request(self, "get_position".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetpositions" => Deribit::request(self, "get_positions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetsubaccounts" => Deribit::request(self, "get_subaccounts".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetsubaccountsdetails" => Deribit::request(self, "get_subaccounts_details".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGettransactionlog" => Deribit::request(self, "get_transaction_log".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetListapikeys" => Deribit::request(self, "list_api_keys".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetRemoveapikey" => Deribit::request(self, "remove_api_key".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetRemovesubaccount" => Deribit::request(self, "remove_subaccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetResetapikey" => Deribit::request(self, "reset_api_key".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSetannouncementasread" => Deribit::request(self, "set_announcement_as_read".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSetapikeyasdefault" => Deribit::request(self, "set_api_key_as_default".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSetemailforsubaccount" => Deribit::request(self, "set_email_for_subaccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSetemaillanguage" => Deribit::request(self, "set_email_language".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSetpasswordforsubaccount" => Deribit::request(self, "set_password_for_subaccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTogglenotificationsfromsubaccount" => Deribit::request(self, "toggle_notifications_from_subaccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTogglesubaccountlogin" => Deribit::request(self, "toggle_subaccount_login".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetExecuteblocktrade" => Deribit::request(self, "execute_block_trade".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetblocktrade" => Deribit::request(self, "get_block_trade".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetlastblocktradesbycurrency" => Deribit::request(self, "get_last_block_trades_by_currency".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetInvalidateblocktradesignature" => Deribit::request(self, "invalidate_block_trade_signature".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetVerifyblocktrade" => Deribit::request(self, "verify_block_trade".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetBuy" => Deribit::request(self, "buy".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSell" => Deribit::request(self, "sell".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetEdit" => Deribit::request(self, "edit".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetEditbylabel" => Deribit::request(self, "edit_by_label".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCancel" => Deribit::request(self, "cancel".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCancelall" => Deribit::request(self, "cancel_all".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCancelallbycurrency" => Deribit::request(self, "cancel_all_by_currency".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCancelallbyinstrument" => Deribit::request(self, "cancel_all_by_instrument".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCancelbylabel" => Deribit::request(self, "cancel_by_label".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCloseposition" => Deribit::request(self, "close_position".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetmargins" => Deribit::request(self, "get_margins".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetmmpconfig" => Deribit::request(self, "get_mmp_config".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetopenordersbycurrency" => Deribit::request(self, "get_open_orders_by_currency".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetopenordersbyinstrument" => Deribit::request(self, "get_open_orders_by_instrument".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetorderhistorybycurrency" => Deribit::request(self, "get_order_history_by_currency".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetorderhistorybyinstrument" => Deribit::request(self, "get_order_history_by_instrument".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetordermarginbyids" => Deribit::request(self, "get_order_margin_by_ids".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetorderstate" => Deribit::request(self, "get_order_state".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetstoporderhistory" => Deribit::request(self, "get_stop_order_history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGettriggerorderhistory" => Deribit::request(self, "get_trigger_order_history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetusertradesbycurrency" => Deribit::request(self, "get_user_trades_by_currency".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetusertradesbycurrencyandtime" => Deribit::request(self, "get_user_trades_by_currency_and_time".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetusertradesbyinstrument" => Deribit::request(self, "get_user_trades_by_instrument".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetusertradesbyinstrumentandtime" => Deribit::request(self, "get_user_trades_by_instrument_and_time".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetusertradesbyorder" => Deribit::request(self, "get_user_trades_by_order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetResetmmp" => Deribit::request(self, "reset_mmp".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSetmmpconfig" => Deribit::request(self, "set_mmp_config".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetsettlementhistorybyinstrument" => Deribit::request(self, "get_settlement_history_by_instrument".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetsettlementhistorybycurrency" => Deribit::request(self, "get_settlement_history_by_currency".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCanceltransferbyid" => Deribit::request(self, "cancel_transfer_by_id".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCancelwithdrawal" => Deribit::request(self, "cancel_withdrawal".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCreatedepositaddress" => Deribit::request(self, "create_deposit_address".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetcurrentdepositaddress" => Deribit::request(self, "get_current_deposit_address".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetdeposits" => Deribit::request(self, "get_deposits".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGettransfers" => Deribit::request(self, "get_transfers".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetGetwithdrawals" => Deribit::request(self, "get_withdrawals".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubmittransfertosubaccount" => Deribit::request(self, "submit_transfer_to_subaccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSubmittransfertouser" => Deribit::request(self, "submit_transfer_to_user".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWithdraw" => Deribit::request(self, "withdraw".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct DeribitImpl(Value);
impl Exchange for DeribitImpl {}
impl Deribit for DeribitImpl {}
impl ValueTrait for DeribitImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value) -> Value { self.0.slice(start) }
}

impl DeribitImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = DeribitImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Deribit::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


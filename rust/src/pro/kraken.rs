#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Kraken : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "kraken",
            "name": "Kraken",
            "countries": [
                "US"
            ],
            "enableRateLimit": true,
            "rateLimit": 1000,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": true,
                "swap": false,
                "future": false,
                "option": false,
                "addMargin": false,
                "cancelAllOrders": true,
                "cancelAllOrdersWs": true,
                "cancelOrder": true,
                "cancelOrderWs": true,
                "cancelOrders": true,
                "cancelOrdersWs": true,
                "createDepositAddress": true,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrderWs": true,
                "createOrders": true,
                "createStopLimitOrder": true,
                "createStopMarketOrder": true,
                "createStopOrder": true,
                "createTrailingAmountOrder": true,
                "createTrailingPercentOrder": true,
                "editOrder": true,
                "editOrderWs": true,
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingRates": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLedgerEntry": true,
                "fetchLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrderTrades": "emulated",
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": true,
                "fetchTradingFees": false,
                "fetchWithdrawals": true,
                "setLeverage": false,
                "setMarginMode": false,
                "transfer": true,
                "watchBalance": true,
                "watchMyTrades": true,
                "watchOHLCV": true,
                "watchOrderBook": true,
                "watchBidsAsks": true,
                "watchOrderBookForSymbols": true,
                "watchOrders": true,
                "watchTicker": true,
                "watchTickers": true,
                "watchTrades": true,
                "watchTradesForSymbols": true,
                "withdraw": true,
                "ws": true,
                "cancelAllOrdersAfter": true
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/51840849/76173629-fc67fb00-61b1-11ea-84fe-f2de582f58a3.jpg",
                "api": {
                    "public": "https://api.kraken.com",
                    "private": "https://api.kraken.com",
                    "zendesk": "https://kraken.zendesk.com/api/v2/help_center/en-us/articles",
                    "ws": {
                        "public": "wss://ws.kraken.com",
                        "private": "wss://ws-auth.kraken.com",
                        "privateV2": "wss://ws-auth.kraken.com/v2",
                        "publicV2": "wss://ws.kraken.com/v2",
                        "beta": "wss://beta-ws.kraken.com",
                        "beta-private": "wss://beta-ws-auth.kraken.com"
                    }
                },
                "www": "https://www.kraken.com",
                "doc": "https://docs.kraken.com/rest/",
                "fees": "https://www.kraken.com/en-us/features/fee-schedule"
            },
            "api": {
                "zendesk": {
                    "get": [
                        "360000292886",
                        "201893608"
                    ]
                },
                "public": {
                    "get": {
                        "Assets": 1,
                        "AssetPairs": 1,
                        "Depth": 1.2,
                        "OHLC": 1.2,
                        "Spread": 1,
                        "SystemStatus": 1,
                        "Ticker": 1,
                        "Time": 1,
                        "Trades": 1.2
                    }
                },
                "private": {
                    "post": {
                        "AddOrder": 0,
                        "AddOrderBatch": 0,
                        "AddExport": 3,
                        "AmendOrder": 0,
                        "Balance": 3,
                        "CancelAll": 3,
                        "CancelAllOrdersAfter": 3,
                        "CancelOrder": 0,
                        "CancelOrderBatch": 0,
                        "ClosedOrders": 3,
                        "DepositAddresses": 3,
                        "DepositMethods": 3,
                        "DepositStatus": 3,
                        "EditOrder": 0,
                        "ExportStatus": 3,
                        "GetWebSocketsToken": 3,
                        "Ledgers": 6,
                        "OpenOrders": 3,
                        "OpenPositions": 3,
                        "QueryLedgers": 3,
                        "QueryOrders": 3,
                        "QueryTrades": 3,
                        "RetrieveExport": 3,
                        "RemoveExport": 3,
                        "BalanceEx": 3,
                        "TradeBalance": 3,
                        "TradesHistory": 6,
                        "TradeVolume": 3,
                        "Withdraw": 3,
                        "WithdrawCancel": 3,
                        "WithdrawInfo": 3,
                        "WithdrawMethods": 3,
                        "WithdrawAddresses": 3,
                        "WithdrawStatus": 3,
                        "WalletTransfer": 3,
                        "CreateSubaccount": 3,
                        "AccountTransfer": 3,
                        "Earn/Allocate": 3,
                        "Earn/Deallocate": 3,
                        "Earn/AllocateStatus": 3,
                        "Earn/DeallocateStatus": 3,
                        "Earn/Strategies": 3,
                        "Earn/Allocations": 3
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": 1,
                "5m": 5,
                "15m": 15,
                "30m": 30,
                "1h": 60,
                "4h": 240,
                "1d": 1440,
                "1w": 10080,
                "2w": 21600
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.0026,
                    "maker": 0.0016,
                    "tiers": {
                        "taker": [
                            [
                                0,
                                0.0026
                            ],
                            [
                                50000,
                                0.0024
                            ],
                            [
                                100000,
                                0.0022
                            ],
                            [
                                250000,
                                0.002
                            ],
                            [
                                500000,
                                0.0018
                            ],
                            [
                                1000000,
                                0.0016
                            ],
                            [
                                2500000,
                                0.0014
                            ],
                            [
                                5000000,
                                0.0012
                            ],
                            [
                                10000000,
                                0.0001
                            ]
                        ],
                        "maker": [
                            [
                                0,
                                0.0016
                            ],
                            [
                                50000,
                                0.0014
                            ],
                            [
                                100000,
                                0.0012
                            ],
                            [
                                250000,
                                0.001
                            ],
                            [
                                500000,
                                0.0008
                            ],
                            [
                                1000000,
                                0.0006
                            ],
                            [
                                2500000,
                                0.0004
                            ],
                            [
                                5000000,
                                0.0002
                            ],
                            [
                                10000000,
                                0
                            ]
                        ]
                    }
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {},
                "ws": {
                    "exact": {},
                    "broad": {}
                }
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "LUNA": "LUNC",
                "LUNA2": "LUNA",
                "REPV2": "REP",
                "REP": "REPV1",
                "UST": "USTC",
                "XDG": "DOGE",
                "FEE": "KFEE",
                "XETC": "ETC",
                "XETH": "ETH",
                "XLTC": "LTC",
                "XMLN": "MLN",
                "XREP": "REP",
                "XXBT": "BTC",
                "XXDG": "DOGE",
                "XXLM": "XLM",
                "XXMR": "XMR",
                "XXRP": "XRP",
                "XZEC": "ZEC",
                "ZAUD": "AUD",
                "ZCAD": "CAD",
                "ZEUR": "EUR",
                "ZGBP": "GBP",
                "ZJPY": "JPY",
                "ZUSD": "USD"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "0",
            "handleContentTypeApplicationZip": true,
            "options": {
                "timeDifference": 0,
                "adjustForTimeDifference": false,
                "marketsByAltname": {},
                "delistedMarketsById": {},
                "inactiveCurrencies": [
                    "CAD",
                    "USD",
                    "JPY",
                    "GBP"
                ],
                "networks": {
                    "ETH": "ERC20",
                    "TRX": "TRC20"
                },
                "depositMethods": {
                    "1INCH": "1inch (1INCH)",
                    "AAVE": "Aave",
                    "ADA": "ADA",
                    "ALGO": "Algorand",
                    "ANKR": "ANKR (ANKR)",
                    "ANT": "Aragon (ANT)",
                    "ATOM": "Cosmos",
                    "AXS": "Axie Infinity Shards (AXS)",
                    "BADGER": "Bager DAO (BADGER)",
                    "BAL": "Balancer (BAL)",
                    "BAND": "Band Protocol (BAND)",
                    "BAT": "BAT",
                    "BCH": "Bitcoin Cash",
                    "BNC": "Bifrost (BNC)",
                    "BNT": "Bancor (BNT)",
                    "BTC": "Bitcoin",
                    "CHZ": "Chiliz (CHZ)",
                    "COMP": "Compound (COMP)",
                    "CQT": "\tCovalent Query Token (CQT)",
                    "CRV": "Curve DAO Token (CRV)",
                    "CTSI": "Cartesi (CTSI)",
                    "DAI": "Dai",
                    "DASH": "Dash",
                    "DOGE": "Dogecoin",
                    "DOT": "Polkadot",
                    "DYDX": "dYdX (DYDX)",
                    "ENJ": "Enjin Coin (ENJ)",
                    "EOS": "EOS",
                    "ETC": "Ether Classic (Hex)",
                    "ETH": "Ether (Hex)",
                    "EWT": "Energy Web Token",
                    "FEE": "Kraken Fee Credit",
                    "FIL": "Filecoin",
                    "FLOW": "Flow",
                    "GHST": "Aavegotchi (GHST)",
                    "GNO": "GNO",
                    "GRT": "GRT",
                    "ICX": "Icon",
                    "INJ": "Injective Protocol (INJ)",
                    "KAR": "Karura (KAR)",
                    "KAVA": "Kava",
                    "KEEP": "Keep Token (KEEP)",
                    "KNC": "Kyber Network (KNC)",
                    "KSM": "Kusama",
                    "LINK": "Link",
                    "LPT": "Livepeer Token (LPT)",
                    "LRC": "Loopring (LRC)",
                    "LSK": "Lisk",
                    "LTC": "Litecoin",
                    "MANA": "MANA",
                    "MATIC": "Polygon (MATIC)",
                    "MINA": "Mina",
                    "MIR": "Mirror Protocol (MIR)",
                    "MKR": "Maker (MKR)",
                    "MLN": "MLN",
                    "MOVR": "Moonriver (MOVR)",
                    "NANO": "NANO",
                    "OCEAN": "OCEAN",
                    "OGN": "Origin Protocol (OGN)",
                    "OMG": "OMG",
                    "OXT": "Orchid (OXT)",
                    "OXY": "Oxygen (OXY)",
                    "PAXG": "PAX (Gold)",
                    "PERP": "Perpetual Protocol (PERP)",
                    "PHA": "Phala (PHA)",
                    "QTUM": "QTUM",
                    "RARI": "Rarible (RARI)",
                    "RAY": "Raydium (RAY)",
                    "REN": "Ren Protocol (REN)",
                    "REP": "REPv2",
                    "REPV1": "REP",
                    "SAND": "The Sandbox (SAND)",
                    "SC": "Siacoin",
                    "SDN": "Shiden (SDN)",
                    "SOL": "Solana",
                    "SNX": "Synthetix  Network (SNX)",
                    "SRM": "Serum",
                    "STORJ": "Storj (STORJ)",
                    "SUSHI": "Sushiswap (SUSHI)",
                    "TBTC": "tBTC",
                    "TRX": "Tron",
                    "UNI": "UNI",
                    "USDC": "USDC",
                    "USDT": "Tether USD (ERC20)",
                    "USDT-TRC20": "Tether USD (TRC20)",
                    "WAVES": "Waves",
                    "WBTC": "Wrapped Bitcoin (WBTC)",
                    "XLM": "Stellar XLM",
                    "XMR": "Monero",
                    "XRP": "Ripple XRP",
                    "XTZ": "XTZ",
                    "YFI": "YFI",
                    "ZEC": "Zcash (Transparent)",
                    "ZRX": "0x (ZRX)"
                },
                "withdrawMethods": {
                    "Lightning": "Lightning",
                    "Bitcoin": "BTC",
                    "Ripple": "XRP",
                    "Litecoin": "LTC",
                    "Dogecoin": "DOGE",
                    "Stellar": "XLM",
                    "Ethereum": "ERC20",
                    "Arbitrum One": "Arbitrum",
                    "Polygon": "MATIC",
                    "Arbitrum Nova": "Arbitrum",
                    "Optimism": "Optimism",
                    "zkSync Era": "zkSync",
                    "Ethereum Classic": "ETC",
                    "Zcash": "ZEC",
                    "Monero": "XMR",
                    "Tron": "TRC20",
                    "Solana": "SOL",
                    "EOS": "EOS",
                    "Bitcoin Cash": "BCH",
                    "Cardano": "ADA",
                    "Qtum": "QTUM",
                    "Tezos": "XTZ",
                    "Cosmos": "ATOM",
                    "Nano": "NANO",
                    "Siacoin": "SC",
                    "Lisk": "LSK",
                    "Waves": "WAVES",
                    "ICON": "ICX",
                    "Algorand": "ALGO",
                    "Polygon - USDC.e": "MATIC",
                    "Arbitrum One - USDC.e": "Arbitrum",
                    "Polkadot": "DOT",
                    "Kava": "KAVA",
                    "Filecoin": "FIL",
                    "Kusama": "KSM",
                    "Flow": "FLOW",
                    "Energy Web": "EW",
                    "Mina": "MINA",
                    "Centrifuge": "CFG",
                    "Karura": "KAR",
                    "Moonriver": "MOVR",
                    "Shiden": "SDN",
                    "Khala": "PHA",
                    "Bifrost Kusama": "BNC",
                    "Songbird": "SGB",
                    "Terra classic": "LUNC",
                    "KILT": "KILT",
                    "Basilisk": "BSX",
                    "Flare": "FLR",
                    "Avalanche C-Chain": "AVAX",
                    "Kintsugi": "KINT",
                    "Altair": "AIR",
                    "Moonbeam": "GLMR",
                    "Acala": "ACA",
                    "Astar": "ASTR",
                    "Akash": "AKT",
                    "Robonomics": "XRT",
                    "Fantom": "FTM",
                    "Elrond": "EGLD",
                    "THORchain": "RUNE",
                    "Secret": "SCRT",
                    "Near": "NEAR",
                    "Internet Computer Protocol": "ICP",
                    "Picasso": "PICA",
                    "Crust Shadow": "CSM",
                    "Integritee": "TEER",
                    "Parallel Finance": "PARA",
                    "HydraDX": "HDX",
                    "Interlay": "INTR",
                    "Fetch.ai": "FET",
                    "NYM": "NYM",
                    "Terra 2.0": "LUNA2",
                    "Juno": "JUNO",
                    "Nodle": "NODL",
                    "Stacks": "STX",
                    "Ethereum PoW": "ETHW",
                    "Aptos": "APT",
                    "Sui": "SUI",
                    "Genshiro": "GENS",
                    "Aventus": "AVT",
                    "Sei": "SEI",
                    "OriginTrail": "OTP",
                    "Celestia": "TIA"
                },
                "marketHelperProps": [
                    "marketsByAltname",
                    "delistedMarketsById"
                ],
                "tradesLimit": 1000,
                "OHLCVLimit": 1000,
                "ordersLimit": 1000,
                "symbolsByOrderId": {},
                "watchOrderBook": {
                    "checksum": false
                }
            },
            "features": {
                "spot": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": true,
                        "takeProfitPrice": true,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": true,
                        "leverage": false,
                        "marketBuyByCost": true,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": true,
                        "iceberg": true
                    },
                    "createOrders": {
                        "min": 2,
                        "max": 15,
                        "sameSymbolOnly": true
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 720
                    }
                },
                "swap": {},
                "future": {}
            },
            "streaming": {
                "keepAlive": 6000
            }
        }"###).unwrap())
    }

    fn order_request_ws(&mut self, mut method: Value, mut symbol: Value, mut r#type: Value, mut request: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut is_limit_order: Value = r#type.ends_with(Value::from("limit"));
        // supporting limit, stop-loss-limit, take-profit-limit, etc
        if is_limit_order.is_truthy() {
            if price.clone().is_nullish() {
                panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" limit orders require a price argument"))"###);
            };
            request.get(Value::from("params")).set("limit_price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), price.clone()), Value::Undefined));
        };
        let mut is_market: Value = (r#type.clone() == Value::from("market")).into();
        let mut post_only: Value = Value::Undefined;
        (post_only, params) = shift_2(self.handle_post_only(is_market.clone(), false.into(), params.clone()));
        if post_only.is_truthy() {
            request.get(Value::from("params")).set("post_only".into(), true.into());
        };
        let mut client_order_id: Value = self.safe_string(params.clone(), Value::from("clientOrderId"), Value::Undefined);
        if client_order_id.clone().is_nonnullish() {
            request.get(Value::from("params")).set("cl_ord_id".into(), client_order_id.clone());
        };
        let mut cost: Value = self.safe_string(params.clone(), Value::from("cost"), Value::Undefined);
        if cost.clone().is_nonnullish() {
            request.get(Value::from("params")).set("order_qty".into(), self.parse_to_numeric(self.cost_to_precision(symbol.clone(), cost.clone()), Value::Undefined));
        };
        let mut stop_loss: Value = self.safe_dict(params.clone(), Value::from("stopLoss"), Value::new_object());
        let mut take_profit: Value = self.safe_dict(params.clone(), Value::from("takeProfit"), Value::new_object());
        let mut preset_stop_loss: Value = self.safe_string(stop_loss.clone(), Value::from("triggerPrice"), Value::Undefined);
        let mut preset_take_profit: Value = self.safe_string(take_profit.clone(), Value::from("triggerPrice"), Value::Undefined);
        let mut preset_stop_loss_limit: Value = self.safe_string(stop_loss.clone(), Value::from("price"), Value::Undefined);
        let mut preset_take_profit_limit: Value = self.safe_string(take_profit.clone(), Value::from("price"), Value::Undefined);
        let mut is_preset_stop_loss: Value = (preset_stop_loss.clone().is_nonnullish()).into();
        let mut is_preset_take_profit: Value = (preset_take_profit.clone().is_nonnullish()).into();
        let mut stop_loss_price: Value = self.safe_string(params.clone(), Value::from("stopLossPrice"), Value::Undefined);
        let mut take_profit_price: Value = self.safe_string(params.clone(), Value::from("takeProfitPrice"), Value::Undefined);
        let mut is_stop_loss_price_order: Value = (stop_loss_price.clone().is_nonnullish()).into();
        let mut is_take_profit_price_order: Value = (take_profit_price.clone().is_nonnullish()).into();
        let mut trailing_amount: Value = self.safe_string(params.clone(), Value::from("trailingAmount"), Value::Undefined);
        let mut trailing_percent: Value = self.safe_string(params.clone(), Value::from("trailingPercent"), Value::Undefined);
        let mut trailing_limit_amount: Value = self.safe_string(params.clone(), Value::from("trailingLimitAmount"), Value::Undefined);
        let mut trailing_limit_percent: Value = self.safe_string(params.clone(), Value::from("trailingLimitPercent"), Value::Undefined);
        let mut is_trailing_amount_order: Value = (trailing_amount.clone().is_nonnullish()).into();
        let mut is_trailing_percent_order: Value = (trailing_percent.clone().is_nonnullish()).into();
        let mut is_trailing_limit_amount_order: Value = (trailing_limit_amount.clone().is_nonnullish()).into();
        let mut is_trailing_limit_percent_order: Value = (trailing_limit_percent.clone().is_nonnullish()).into();
        let mut offset: Value = self.safe_string(params.clone(), Value::from("offset"), Value::from(""));
        // can set this to - for minus
        let mut trailing_amount_string: Value = if trailing_amount.clone().is_nonnullish() { offset.clone() + self.number_to_string(trailing_amount.clone()) } else { Value::Undefined };
        let mut trailing_percent_string: Value = if trailing_percent.clone().is_nonnullish() { offset.clone() + self.number_to_string(trailing_percent.clone()) } else { Value::Undefined };
        let mut trailing_limit_amount_string: Value = if trailing_limit_amount.clone().is_nonnullish() { offset.clone() + self.number_to_string(trailing_limit_amount.clone()) } else { Value::Undefined };
        let mut trailing_limit_percent_string: Value = if trailing_limit_percent.clone().is_nonnullish() { offset.clone() + self.number_to_string(trailing_limit_percent.clone()) } else { Value::Undefined };
        let mut price_type: Value = if is_trailing_percent_order.is_truthy() || is_trailing_limit_percent_order.is_truthy() { Value::from("pct") } else { Value::from("quote") };
        if method.clone() == Value::from("createOrderWs") {
            let mut reduce_only: Value = self.safe_bool(params.clone(), Value::from("reduceOnly"), Value::Undefined);
            if reduce_only.is_truthy() {
                request.get(Value::from("params")).set("reduce_only".into(), true.into());
            };
            let mut time_in_force: Value = self.safe_string_lower(params.clone(), Value::from("timeInForce"), Value::Undefined);
            if time_in_force.clone().is_nonnullish() {
                request.get(Value::from("params")).set("time_in_force".into(), time_in_force.clone());
            };
            params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("reduceOnly").into(), Value::from("timeInForce").into()])));
            if is_stop_loss_price_order.is_truthy() || is_take_profit_price_order.is_truthy() || is_trailing_amount_order.is_truthy() || is_trailing_percent_order.is_truthy() || is_trailing_limit_amount_order.is_truthy() || is_trailing_limit_percent_order.is_truthy() {
                request.get(Value::from("params")).set("triggers".into(), Value::new_object());
            };
            if is_preset_stop_loss.is_truthy() || is_preset_take_profit.is_truthy() {
                request.get(Value::from("params")).set("conditional".into(), Value::new_object());
                if is_preset_stop_loss.is_truthy() {
                    request.get(Value::from("params")).get(Value::from("conditional")).set("order_type".into(), Value::from("stop-loss"));
                    request.get(Value::from("params")).get(Value::from("conditional")).set("trigger_price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), preset_stop_loss.clone()), Value::Undefined));
                } else if is_preset_take_profit.is_truthy() {
                    request.get(Value::from("params")).get(Value::from("conditional")).set("order_type".into(), Value::from("take-profit"));
                    request.get(Value::from("params")).get(Value::from("conditional")).set("trigger_price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), preset_take_profit.clone()), Value::Undefined));
                };
                if preset_stop_loss_limit.clone().is_nonnullish() {
                    request.get(Value::from("params")).get(Value::from("conditional")).set("order_type".into(), Value::from("stop-loss-limit"));
                    request.get(Value::from("params")).get(Value::from("conditional")).set("limit_price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), preset_stop_loss_limit.clone()), Value::Undefined));
                } else if preset_take_profit_limit.clone().is_nonnullish() {
                    request.get(Value::from("params")).get(Value::from("conditional")).set("order_type".into(), Value::from("take-profit-limit"));
                    request.get(Value::from("params")).get(Value::from("conditional")).set("limit_price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), preset_take_profit_limit.clone()), Value::Undefined));
                };
                params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stopLoss").into(), Value::from("takeProfit").into()])));
            } else if is_stop_loss_price_order.is_truthy() || is_take_profit_price_order.is_truthy() {
                if is_stop_loss_price_order.is_truthy() {
                    request.get(Value::from("params")).get(Value::from("triggers")).set("price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), stop_loss_price.clone()), Value::Undefined));
                    if is_limit_order.is_truthy() {
                        request.get(Value::from("params")).set("order_type".into(), Value::from("stop-loss-limit"));
                    } else {
                        request.get(Value::from("params")).set("order_type".into(), Value::from("stop-loss"));
                    };
                } else {
                    request.get(Value::from("params")).get(Value::from("triggers")).set("price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), take_profit_price.clone()), Value::Undefined));
                    if is_limit_order.is_truthy() {
                        request.get(Value::from("params")).set("order_type".into(), Value::from("take-profit-limit"));
                    } else {
                        request.get(Value::from("params")).set("order_type".into(), Value::from("take-profit"));
                    };
                };
            } else if is_trailing_amount_order.is_truthy() || is_trailing_percent_order.is_truthy() || is_trailing_limit_amount_order.is_truthy() || is_trailing_limit_percent_order.is_truthy() {
                request.get(Value::from("params")).get(Value::from("triggers")).set("price_type".into(), price_type.clone());
                if !is_limit_order.is_truthy() && is_trailing_amount_order.is_truthy() || is_trailing_percent_order.is_truthy() {
                    request.get(Value::from("params")).set("order_type".into(), Value::from("trailing-stop"));
                    if is_trailing_amount_order.is_truthy() {
                        request.get(Value::from("params")).get(Value::from("triggers")).set("price".into(), self.parse_to_numeric(trailing_amount_string.clone(), Value::Undefined));
                    } else {
                        request.get(Value::from("params")).get(Value::from("triggers")).set("price".into(), self.parse_to_numeric(trailing_percent_string.clone(), Value::Undefined));
                    };
                } else {
                    // trailing limit orders are not conventionally supported because the static limit_price_type param is not available for trailing-stop-limit orders
                    request.get(Value::from("params")).set("limit_price_type".into(), price_type.clone());
                    request.get(Value::from("params")).set("order_type".into(), Value::from("trailing-stop-limit"));
                    if is_trailing_limit_amount_order.is_truthy() {
                        request.get(Value::from("params")).get(Value::from("triggers")).set("price".into(), self.parse_to_numeric(trailing_limit_amount_string.clone(), Value::Undefined));
                    } else {
                        request.get(Value::from("params")).get(Value::from("triggers")).set("price".into(), self.parse_to_numeric(trailing_limit_percent_string.clone(), Value::Undefined));
                    };
                };
            };
        } else if method.clone() == Value::from("editOrderWs") {
            if is_preset_stop_loss.is_truthy() || is_preset_take_profit.is_truthy() {
                panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" editing the stopLoss and takeProfit on existing orders is currently not supported"))"###);
            };
            if is_stop_loss_price_order.is_truthy() || is_take_profit_price_order.is_truthy() {
                if is_stop_loss_price_order.is_truthy() {
                    request.get(Value::from("params")).set("trigger_price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), stop_loss_price.clone()), Value::Undefined));
                } else {
                    request.get(Value::from("params")).set("trigger_price".into(), self.parse_to_numeric(self.price_to_precision(symbol.clone(), take_profit_price.clone()), Value::Undefined));
                };
            } else if is_trailing_amount_order.is_truthy() || is_trailing_percent_order.is_truthy() || is_trailing_limit_amount_order.is_truthy() || is_trailing_limit_percent_order.is_truthy() {
                request.get(Value::from("params")).set("trigger_price_type".into(), price_type.clone());
                if !is_limit_order.is_truthy() && is_trailing_amount_order.is_truthy() || is_trailing_percent_order.is_truthy() {
                    if is_trailing_amount_order.is_truthy() {
                        request.get(Value::from("params")).set("trigger_price".into(), self.parse_to_numeric(trailing_amount_string.clone(), Value::Undefined));
                    } else {
                        request.get(Value::from("params")).set("trigger_price".into(), self.parse_to_numeric(trailing_percent_string.clone(), Value::Undefined));
                    };
                } else {
                    request.get(Value::from("params")).set("limit_price_type".into(), price_type.clone());
                    if is_trailing_limit_amount_order.is_truthy() {
                        request.get(Value::from("params")).set("trigger_price".into(), self.parse_to_numeric(trailing_limit_amount_string.clone(), Value::Undefined));
                    } else {
                        request.get(Value::from("params")).set("trigger_price".into(), self.parse_to_numeric(trailing_limit_percent_string.clone(), Value::Undefined));
                    };
                };
            };
        };
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("clientOrderId").into(), Value::from("cost").into(), Value::from("offset").into(), Value::from("stopLossPrice").into(), Value::from("takeProfitPrice").into(), Value::from("trailingAmount").into(), Value::from("trailingPercent").into(), Value::from("trailingLimitAmount").into(), Value::from("trailingLimitPercent").into()])));
        return Value::Json(serde_json::Value::Array(vec![request.clone().into(), params.clone().into()]));
    }

    async fn create_order_ws(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut token: Value = <Self as Kraken>::authenticate(self).await;
        let mut market: Value = self.market(symbol.clone());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("privateV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut message_hash: Value = self.number_to_string(request_id.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "add_order",
            "params": Value::Json(normalize(&Value::Json(json!({
                "order_type": r#type,
                "side": side,
                "order_qty": self.parse_to_numeric(self.amount_to_precision(symbol.clone(), amount.clone()), Value::Undefined),
                "symbol": market.get(Value::from("symbol")),
                "token": token
            }))).unwrap()),
            "req_id": request_id
        }))).unwrap());
        (request, params) = shift_2(<Self as Kraken>::order_request_ws(self, Value::from("createOrderWs"), symbol.clone(), r#type.clone(), request.clone(), amount.clone(), price.clone(), params.clone()));
        return self.watch(url.clone(), message_hash.clone(), extend_2(request.clone(), params.clone()), message_hash.clone(), Value::Undefined).await;
    }

    fn handle_create_edit_order(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //  createOrder
        //     {
        //         "method": "add_order",
        //         "req_id": 1,
        //         "result": {
        //             "order_id": "OXM2QD-EALR2-YBAVEU"
        //         },
        //         "success": true,
        //         "time_in": "2025-05-13T10:12:13.876173Z",
        //         "time_out": "2025-05-13T10:12:13.890137Z"
        //     }
        //
        //  editOrder
        //     {
        //         "method": "amend_order",
        //         "req_id": 1,
        //         "result": {
        //             "amend_id": "TYDLSQ-OYNYU-3MNRER",
        //             "order_id": "OGL7HR-SWFO4-NRQTHO"
        //         },
        //         "success": true,
        //         "time_in": "2025-05-14T13:54:10.840342Z",
        //         "time_out": "2025-05-14T13:54:10.855046Z"
        //     }
        //
        let mut result: Value = self.safe_dict(message.clone(), Value::from("result"), Value::new_object());
        let mut order: Value = self.parse_order(result.clone(), Value::Undefined);
        let mut message_hash: Value = self.safe_string_2(message.clone(), Value::from("reqid"), Value::from("req_id"), Value::Undefined);
        client.resolve(order.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn edit_order_ws(&mut self, mut id: Value, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut token: Value = <Self as Kraken>::authenticate(self).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("privateV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut message_hash: Value = self.number_to_string(request_id.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "amend_order",
            "params": Value::Json(normalize(&Value::Json(json!({
                "order_id": id,
                "order_qty": self.parse_to_numeric(self.amount_to_precision(symbol.clone(), amount.clone()), Value::Undefined),
                "token": token
            }))).unwrap()),
            "req_id": request_id
        }))).unwrap());
        (request, params) = shift_2(<Self as Kraken>::order_request_ws(self, Value::from("editOrderWs"), symbol.clone(), r#type.clone(), request.clone(), amount.clone(), price.clone(), params.clone()));
        return self.watch(url.clone(), message_hash.clone(), extend_2(request.clone(), params.clone()), message_hash.clone(), Value::Undefined).await;
    }

    async fn cancel_orders_ws(&mut self, mut ids: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nonnullish() {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" cancelOrdersWs () does not support cancelling orders for a specific symbol."))"###);
        };
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut token: Value = <Self as Kraken>::authenticate(self).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("privateV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut message_hash: Value = self.number_to_string(request_id.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "cancel_order",
            "params": Value::Json(normalize(&Value::Json(json!({
                "order_id": ids,
                "token": token
            }))).unwrap()),
            "req_id": request_id
        }))).unwrap());
        return self.watch(url.clone(), message_hash.clone(), extend_2(request.clone(), params.clone()), message_hash.clone(), Value::Undefined).await;
    }

    async fn cancel_order_ws(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nonnullish() {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" cancelOrderWs () does not support cancelling orders for a specific symbol."))"###);
        };
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut token: Value = <Self as Kraken>::authenticate(self).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("privateV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut message_hash: Value = self.number_to_string(request_id.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "cancel_order",
            "params": Value::Json(normalize(&Value::Json(json!({
                "order_id": Value::Json(serde_json::Value::Array(vec![id.clone().into()])),
                "token": token
            }))).unwrap()),
            "req_id": request_id
        }))).unwrap());
        return self.watch(url.clone(), message_hash.clone(), extend_2(request.clone(), params.clone()), message_hash.clone(), Value::Undefined).await;
    }

    fn handle_cancel_order(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "method": "cancel_order",
        //         "req_id": 123456789,
        //         "result": {
        //             "order_id": "OKAGJC-YHIWK-WIOZWG"
        //         },
        //         "success": true,
        //         "time_in": "2023-09-21T14:36:57.428972Z",
        //         "time_out": "2023-09-21T14:36:57.437952Z"
        //     }
        //
        let mut req_id: Value = self.safe_string(message.clone(), Value::from("req_id"), Value::Undefined);
        client.resolve(message.clone(), req_id.clone());
        Value::Undefined
    }

    async fn cancel_all_orders_ws(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nonnullish() {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" cancelAllOrdersWs () does not support cancelling orders in a specific market."))"###);
        };
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut token: Value = <Self as Kraken>::authenticate(self).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("privateV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut message_hash: Value = self.number_to_string(request_id.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "cancel_all",
            "params": Value::Json(normalize(&Value::Json(json!({
                "token": token
            }))).unwrap()),
            "req_id": request_id
        }))).unwrap());
        return self.watch(url.clone(), message_hash.clone(), extend_2(request.clone(), params.clone()), message_hash.clone(), Value::Undefined).await;
    }

    fn handle_cancel_all_orders(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "method": "cancel_all",
        //         "req_id": 123456789,
        //         "result": {
        //             "count": 1
        //         },
        //         "success": true,
        //         "time_in": "2023-09-21T14:36:57.428972Z",
        //         "time_out": "2023-09-21T14:36:57.437952Z"
        //     }
        //
        let mut req_id: Value = self.safe_string(message.clone(), Value::from("req_id"), Value::Undefined);
        client.resolve(message.clone(), req_id.clone());
        Value::Undefined
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "channel": "ticker",
        //         "type": "snapshot",
        //         "data": [
        //             {
        //                 "symbol": "BTC/USD",
        //                 "bid": 108359.8,
        //                 "bid_qty": 0.01362603,
        //                 "ask": 108359.9,
        //                 "ask_qty": 17.17988863,
        //                 "last": 108359.8,
        //                 "volume": 2158.32346723,
        //                 "vwap": 108894.5,
        //                 "low": 106824,
        //                 "high": 111300,
        //                 "change": -2679.9,
        //                 "change_pct": -2.41
        //             }
        //         ]
        //     }
        //
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut ticker: Value = data.get(Value::from(0));
        let mut symbol: Value = self.safe_string(ticker.clone(), Value::from("symbol"), Value::Undefined);
        let mut message_hash: Value = <Self as Kraken>::get_message_hash(self, Value::from("ticker"), Value::Undefined, symbol.clone());
        let mut vwap: Value = self.safe_string(ticker.clone(), Value::from("vwap"), Value::Undefined);
        let mut quote_volume: Value = Value::Undefined;
        let mut base_volume: Value = self.safe_string(ticker.clone(), Value::from("volume"), Value::Undefined);
        if base_volume.clone().is_nonnullish() && vwap.clone().is_nonnullish() {
            quote_volume = Precise::string_mul(base_volume.clone(), vwap.clone());
        };
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("last"), Value::Undefined);
        let mut result: Value = self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "high": self.safe_string(ticker.clone(), Value::from("high"), Value::Undefined),
            "low": self.safe_string(ticker.clone(), Value::from("low"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("bid"), Value::Undefined),
            "bidVolume": self.safe_string(ticker.clone(), Value::from("bid_qty"), Value::Undefined),
            "ask": self.safe_string(ticker.clone(), Value::from("ask"), Value::Undefined),
            "askVolume": self.safe_string(ticker.clone(), Value::from("ask_qty"), Value::Undefined),
            "vwap": vwap,
            "open": Value::Undefined,
            "close": last,
            "last": last,
            "previousClose": Value::Undefined,
            "change": self.safe_string(ticker.clone(), Value::from("change"), Value::Undefined),
            "percentage": self.safe_string(ticker.clone(), Value::from("change_pct"), Value::Undefined),
            "average": Value::Undefined,
            "baseVolume": base_volume,
            "quoteVolume": quote_volume,
            "info": ticker
        }))).unwrap()), Value::Undefined);
        self.get("tickers".into()).set(symbol.clone(), result.clone());
        client.resolve(result.clone(), message_hash.clone());
        Value::Undefined
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "channel": "trade",
        //         "type": "update",
        //         "data": [
        //             {
        //                 "symbol": "MATIC/USD",
        //                 "side": "sell",
        //                 "price": 0.5117,
        //                 "qty": 40.0,
        //                 "ord_type": "market",
        //                 "trade_id": 4665906,
        //                 "timestamp": "2023-09-25T07:49:37.708706Z"
        //             }
        //         ]
        //     }
        //
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut trade: Value = data.get(Value::from(0));
        let mut symbol: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        let mut message_hash: Value = <Self as Kraken>::get_message_hash(self, Value::from("trade"), Value::Undefined, symbol.clone());
        let mut stored: Value = self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined);
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            stored = ArrayCache::new(limit);
            self.get("trades".into()).set(symbol.clone(), stored.clone());
        };
        let mut market: Value = self.market(symbol.clone());
        let mut parsed: Value = self.parse_trades(data.clone(), market.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut i: usize = 0;
        while i < parsed.len() {
            stored.append(parsed.get(i.into()));
            i += 1;
        };
        client.resolve(stored.clone(), message_hash.clone());
        Value::Undefined
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "channel": "ohlc",
        //         "type": "update",
        //         "timestamp": "2023-10-04T16:26:30.524394914Z",
        //         "data": [
        //             {
        //                 "symbol": "MATIC/USD",
        //                 "open": 0.5624,
        //                 "high": 0.5628,
        //                 "low": 0.5622,
        //                 "close": 0.5627,
        //                 "trades": 12,
        //                 "volume": 30927.68066226,
        //                 "vwap": 0.5626,
        //                 "interval_begin": "2023-10-04T16:25:00.000000000Z",
        //                 "interval": 5,
        //                 "timestamp": "2023-10-04T16:30:00.000000Z"
        //             }
        //         ]
        //     }
        //
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut first: Value = data.get(Value::from(0));
        let mut market_id: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        if !self.get("ohlcvs".into()).contains_key(symbol.clone()) {
            self.get("ohlcvs".into()).set(symbol.clone(), Value::new_object());
        };
        let mut interval: Value = self.safe_integer(first.clone(), Value::from("interval"), Value::Undefined);
        let mut timeframe: Value = self.find_timeframe(interval.clone(), Value::Undefined);
        let mut message_hash: Value = <Self as Kraken>::get_message_hash(self, Value::from("ohlcv"), Value::Undefined, symbol.clone());
        let mut stored: Value = self.safe_value(self.get("ohlcvs".into()).get(symbol.clone()), timeframe.clone(), Value::Undefined);
        self.get("ohlcvs".into()).set(symbol.clone(), self.safe_value(self.get("ohlcvs".into()), symbol.clone(), Value::new_object()));
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            stored = ArrayCacheByTimestamp::new(limit);
            self.get("ohlcvs".into()).get(symbol.clone()).set(timeframe.clone(), stored.clone());
        };
        let mut ohlcvs_length: usize = data.len();
        let mut i: usize = 0;
        while i < ohlcvs_length.clone().into() {
            let mut candle: Value = data.get(ohlcvs_length.clone() - Value::from(i) - Value::from(1).clone());
            let mut datetime: Value = self.safe_string(candle.clone(), Value::from("timestamp"), Value::Undefined);
            let mut timestamp: Value = self.parse8601(datetime.clone());
            let mut parsed: Value = Value::Json(serde_json::Value::Array(vec![timestamp.clone().into(), self.safe_string(candle.clone(), Value::from("open"), Value::Undefined).into(), self.safe_string(candle.clone(), Value::from("high"), Value::Undefined).into(), self.safe_string(candle.clone(), Value::from("low"), Value::Undefined).into(), self.safe_string(candle.clone(), Value::from("close"), Value::Undefined).into(), self.safe_string(candle.clone(), Value::from("volume"), Value::Undefined).into()]));
            stored.append(parsed.clone());
            i += 1;
        };
        client.resolve(stored.clone(), message_hash.clone());
        Value::Undefined
    }

    fn request_id(&mut self) -> Value {
        // their support said that reqid must be an int32, not documented
        self.lock_id();
        let mut reqid: Value = self.sum(self.safe_integer(self.get("options".into()), Value::from("reqid"), Value::from(0)), Value::from(1));
        self.get("options".into()).set("reqid".into(), reqid.clone());
        self.unlock_id();
        return reqid.clone();
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        symbol = self.symbol(symbol.clone());
        let mut tickers: Value = <Self as Kraken>::watch_tickers(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), params.clone()).await;
        return tickers.get(symbol.clone());
    }

    async fn watch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        let mut ticker: Value = <Self as Kraken>::watch_multi_helper(self, Value::from("ticker"), Value::from("ticker"), symbols.clone(), Value::Undefined, params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut result: Value = Value::new_object();
            result.set(ticker.get(Value::from("symbol")), ticker.clone());
            return result.clone();
        };
        return self.filter_by_array(self.get("tickers".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    async fn watch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), Value::Undefined, Value::Undefined);
        params.set("event_trigger".into(), Value::from("bbo"));
        let mut ticker: Value = <Self as Kraken>::watch_multi_helper(self, Value::from("bidask"), Value::from("ticker"), symbols.clone(), Value::Undefined, params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut result: Value = Value::new_object();
            result.set(ticker.get(Value::from("symbol")), ticker.clone());
            return result.clone();
        };
        return self.filter_by_array(self.get("bidsasks".into()), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Kraken>::watch_trades_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn watch_trades_for_symbols(&mut self, mut symbols: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut trades: Value = <Self as Kraken>::watch_multi_helper(self, Value::from("trade"), Value::from("trade"), symbols.clone(), Value::Undefined, params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            let mut first: Value = self.safe_list(trades.clone(), Value::from(0), Value::Undefined);
            let mut trade_symbol: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
            limit = trades.get_limit(trade_symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Kraken>::watch_order_book_for_symbols(self, Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])), limit.clone(), params.clone()).await;
    }

    async fn watch_order_book_for_symbols(&mut self, mut symbols: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut required_params: Value = Value::new_object();
        if limit.clone().is_nonnullish() {
            if self.in_array(limit.clone(), Value::Json(serde_json::Value::Array(vec![Value::from(10).into(), Value::from(25).into(), Value::from(100).into(), Value::from(500).into(), Value::from(1000).into()]))).is_truthy() {
                required_params.set("depth".into(), limit.clone());
            } else {
                // default 10, valid options 10, 25, 100, 500, 1000
                panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" watchOrderBook accepts limit values of 10, 25, 100, 500 and 1000 only"))"###);
            };
        };
        let mut orderbook: Value = <Self as Kraken>::watch_multi_helper(self, Value::from("orderbook"), Value::from("book"), symbols.clone(), Value::Json(normalize(&Value::Json(json!({
            "limit": limit
        }))).unwrap()), extend_2(required_params.clone(), params.clone())).await;
        return orderbook.limit();
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut name: Value = Value::from("ohlc");
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("publicV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut message_hash: Value = <Self as Kraken>::get_message_hash(self, Value::from("ohlcv"), Value::Undefined, symbol.clone());
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channel": name,
                "symbol": Value::Json(serde_json::Value::Array(vec![symbol.clone().into()])),
                "interval": self.safe_value(self.get("timeframes".into()), timeframe.clone(), timeframe.clone())
            }))).unwrap()),
            "req_id": request_id
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(subscribe.clone(), params.clone());
        let mut ohlcv: Value = self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = ohlcv.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(ohlcv.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    async fn load_markets(&mut self, mut reload: Value, mut params: Value) -> Value {
        reload = reload.or_default(false.into());
        params = params.or_default(Value::new_object());
        let mut markets: Value = krakenRest::load_markets(self, reload.clone(), params.clone()).await;
        let mut markets_by_ws_name: Value = self.safe_value(self.get("options".into()), Value::from("marketsByWsName"), Value::Undefined);
        if markets_by_ws_name.clone().is_nullish() || reload.is_truthy() {
            markets_by_ws_name = Value::new_object();
            let mut i: usize = 0;
            while i < self.get("symbols".into()).len() {
                let mut symbol: Value = self.get("symbols".into()).get(i.into());
                let mut market: Value = self.get("markets".into()).get(symbol.clone());
                let mut info: Value = self.safe_value(market.clone(), Value::from("info"), Value::new_object());
                let mut ws_name: Value = self.safe_string(info.clone(), Value::from("wsname"), Value::Undefined);
                markets_by_ws_name.set(ws_name.clone(), market.clone());
                i += 1;
            };
            self.get("options".into()).set("marketsByWsName".into(), markets_by_ws_name.clone());
        };
        return markets.clone();
    }

    fn ping(&mut self, mut client: Value) -> Value {
        let mut url: Value = client.get(url.clone());
        let mut request: Value = Value::new_object();
        if url.index_of(Value::from("v2")) >= Value::from(0) {
            request.set("method".into(), Value::from("ping"));
        } else {
            request.set("event".into(), Value::from("ping"));
        };
        return request.clone();
    }

    fn handle_pong(&mut self, mut client: Value, mut message: Value) -> Value {
        client.set("last_pong".into(), self.milliseconds());
        return message.clone();
    }

    async fn watch_heartbeat(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut event: Value = Value::from("heartbeat");
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("publicV2"));
        return self.watch(url.clone(), event.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
    }

    fn handle_heartbeat(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // every second (approx) if no other updates are sent
        //
        //     { "channel": "heartbeat" }
        //
        let mut event: Value = self.safe_string(message.clone(), Value::from("channel"), Value::Undefined);
        client.resolve(message.clone(), event.clone());
        Value::Undefined
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // first message (snapshot)
        //
        //     {
        //         "channel": "book",
        //         "type": "snapshot",
        //         "data": [
        //             {
        //                 "symbol": "MATIC/USD",
        //                 "bids": [
        //                     {
        //                         "price": 0.5666,
        //                         "qty": 4831.75496356
        //                     },
        //                     {
        //                         "price": 0.5665,
        //                         "qty": 6658.22734739
        //                     }
        //                 ],
        //                 "asks": [
        //                     {
        //                         "price": 0.5668,
        //                         "qty": 4410.79769741
        //                     },
        //                     {
        //                         "price": 0.5669,
        //                         "qty": 4655.40412487
        //                     }
        //                 ],
        //                 "checksum": 2439117997
        //             }
        //         ]
        //     }
        //
        // subsequent updates
        //
        //     {
        //         "channel": "book",
        //         "type": "update",
        //         "data": [
        //             {
        //                 "symbol": "MATIC/USD",
        //                 "bids": [
        //                     {
        //                         "price": 0.5657,
        //                         "qty": 1098.3947558
        //                     }
        //                 ],
        //                 "asks": [],
        //                 "checksum": 2114181697,
        //                 "timestamp": "2023-10-06T17:35:55.440295Z"
        //             }
        //         ]
        //     }
        //
        let mut r#type: Value = self.safe_string(message.clone(), Value::from("type"), Value::Undefined);
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut first: Value = self.safe_dict(data.clone(), Value::from(0), Value::new_object());
        let mut symbol: Value = self.safe_string(first.clone(), Value::from("symbol"), Value::Undefined);
        let mut a: Value = self.safe_value(first.clone(), Value::from("asks"), Value::new_array());
        let mut b: Value = self.safe_value(first.clone(), Value::from("bids"), Value::new_array());
        let mut c: Value = self.safe_integer(first.clone(), Value::from("checksum"), Value::Undefined);
        let mut message_hash: Value = <Self as Kraken>::get_message_hash(self, Value::from("orderbook"), Value::Undefined, symbol.clone());
        let mut orderbook: Value = Value::Undefined;
        if r#type.clone() == Value::from("update") {
            orderbook = self.get("orderbooks".into()).get(symbol.clone());
            let mut stored_asks: Value = orderbook.get(Value::from("asks"));
            let mut stored_bids: Value = orderbook.get(Value::from("bids"));
            if a.clone().is_nonnullish() {
                <Self as Kraken>::custom_handle_deltas(self, stored_asks.clone(), a.clone());
            };
            if b.clone().is_nonnullish() {
                <Self as Kraken>::custom_handle_deltas(self, stored_bids.clone(), b.clone());
            };
            let mut datetime: Value = self.safe_string(first.clone(), Value::from("timestamp"), Value::Undefined);
            orderbook.set("symbol".into(), symbol.clone());
            orderbook.set("timestamp".into(), self.parse8601(datetime.clone()));
            orderbook.set("datetime".into(), datetime.clone());
        } else {
            // snapshot
            let mut depth: usize = a.len();
            self.get("orderbooks".into()).set(symbol.clone(), self.order_book(Value::new_object(), Value::from(depth)));
            orderbook = self.get("orderbooks".into()).get(symbol.clone());
            let mut keys: Value = Value::Json(serde_json::Value::Array(vec![Value::from("asks").into(), Value::from("bids").into()]));
            let mut i: usize = 0;
            while i < keys.len() {
                let mut key: Value = keys.get(i.into());
                let mut bookside: Value = orderbook.get(key.clone());
                let mut deltas: Value = self.safe_value(first.clone(), key.clone(), Value::new_array());
                if deltas.len() > 0 {
                    <Self as Kraken>::custom_handle_deltas(self, bookside.clone(), deltas.clone());
                };
                i += 1;
            };
            orderbook.set("symbol".into(), symbol.clone());
        };
        orderbook.limit();
        // checksum temporarily disabled because the exchange checksum was not reliable
        let mut checksum: Value = self.handle_option(Value::from("watchOrderBook"), Value::from("checksum"), false.into());
        if checksum.is_truthy() {
            let mut payload_array: Value = Value::new_array();
            if c.clone().is_nonnullish() {
                let mut check_asks: Value = orderbook.get(Value::from("asks"));
                let mut check_bids: Value = orderbook.get(Value::from("bids"));
                // const checkAsks = asks.map ((elem) => [ elem['price'], elem['qty'] ]);
                // const checkBids = bids.map ((elem) => [ elem['price'], elem['qty'] ]);
                let mut i: usize = 0;
                while i < 10 {
                    let mut current_ask: Value = self.safe_value(check_asks.clone(), Value::from(i), Value::new_object());
                    let mut formatted_ask: Value = <Self as Kraken>::format_number(self, current_ask.get(Value::from(0))) + <Self as Kraken>::format_number(self, current_ask.get(Value::from(1)));
                    payload_array.push(formatted_ask.clone());
                    i += 1;
                };
                let mut i: usize = 0;
                while i < 10 {
                    let mut current_bid: Value = self.safe_value(check_bids.clone(), Value::from(i), Value::new_object());
                    let mut formatted_bid: Value = <Self as Kraken>::format_number(self, current_bid.get(Value::from(0))) + <Self as Kraken>::format_number(self, current_bid.get(Value::from(1)));
                    payload_array.push(formatted_bid.clone());
                    i += 1;
                };
            };
            let mut payload: Value = payload_array.join(Value::from(""));
            let mut local_checksum: Value = self.crc32(payload.clone(), false.into());
            if local_checksum.clone() != c.clone() {
                let mut error: Value = ChecksumError::new(self.get("id".into()) + Value::from(" ") + self.orderbook_checksum_message(symbol.clone()));
                client.get(subscriptions.clone()).get(message_hash.clone());
                self.get("orderbooks".into()).get(symbol.clone());
                client.reject(error.clone(), message_hash.clone());
                return Value::Undefined;
            };
        };
        client.resolve(orderbook.clone(), message_hash.clone());
        Value::Undefined
    }

    fn custom_handle_deltas(&mut self, mut bookside: Value, mut deltas: Value) -> Value {
        // const sortOrder = (key === 'bids') ? true : false;
        let mut j: usize = 0;
        while j < deltas.len() {
            let mut delta: Value = deltas.get(j.into());
            let mut price: Value = self.safe_number(delta.clone(), Value::from("price"), Value::Undefined);
            let mut amount: Value = self.safe_number(delta.clone(), Value::from("qty"), Value::Undefined);
            bookside.store(price.clone(), amount.clone());
            j += 1;
        };
        Value::Undefined
    }

    fn format_number(&mut self, mut data: Value) -> Value {
        let mut parts: Value = data.split(Value::from("."));
        let mut integer: Value = self.safe_string(parts.clone(), Value::from(0), Value::Undefined);
        let mut decimals: Value = self.safe_string(parts.clone(), Value::from(1), Value::from(""));
        let mut joined_result: Value = integer.clone() + decimals.clone();
        let mut i: usize = 0;
        while joined_result.get(i.into()) == Value::from("0"){
            i = i +  Value::from(1);
        };
        if i > 0 {
            joined_result = joined_result.slice(Value::from(i), Value::Undefined);
        };
        return joined_result.clone();
    }

    fn handle_system_status(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // todo: answer the question whether handleSystemStatus should be renamed
        // and unified as handleStatus for any usage pattern that
        // involves system status and maintenance updates
        //
        //     {
        //         "connectionID": 15527282728335292000,
        //         "event": "systemStatus",
        //         "status": "online", // online|maintenance|(custom status tbd)
        //         "version": "0.2.0"
        //     }
        //
        // v2
        //     {
        //         channel: 'status',
        //         type: 'update',
        //         data: [
        //             {
        //                 version: '2.0.10',
        //                 system: 'online',
        //                 api_version: 'v2',
        //                 connection_id: 6447481662169813000
        //             }
        //         ]
        //     }
        //
        return message.clone();
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut client: Value = self.client(url.clone());
        let mut authenticated: Value = Value::from("authenticated");
        let mut subscription: Value = self.safe_value(client.get(subscriptions.clone()), authenticated.clone(), Value::Undefined);
        let mut now: Value = self.seconds();
        let mut start: Value = self.safe_integer(subscription.clone(), Value::from("start"), Value::Undefined);
        let mut expires: Value = self.safe_integer(subscription.clone(), Value::from("expires"), Value::Undefined);
        if subscription.clone().is_nullish() || subscription.clone().is_nonnullish() && start.clone() + expires.clone() <= now.clone() {
            // https://docs.kraken.com/api/docs/rest-api/get-websockets-token
            let mut response: Value = self.dispatch("privatePostGetWebSocketsToken".into(), params.clone(), Value::Undefined).await;
            //
            //     {
            //         "error":[],
            //         "result":{
            //             "token":"xeAQ\/RCChBYNVh53sTv1yZ5H4wIbwDF20PiHtTF+4UI",
            //             "expires":900
            //         }
            //     }
            //
            subscription = self.safe_dict(response.clone(), Value::from("result"), Value::Undefined);
            subscription.set("start".into(), now.clone());
            client.get(subscriptions.clone()).set(authenticated.clone(), subscription.clone());
        };
        return self.safe_string(subscription.clone(), Value::from("token"), Value::Undefined);
    }

    async fn watch_private(&mut self, mut name: Value, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut token: Value = <Self as Kraken>::authenticate(self).await;
        let mut subscription_hash: Value = Value::from("executions");
        let mut message_hash: Value = name.clone();
        if symbol.clone().is_nonnullish() {
            symbol = self.symbol(symbol.clone());
            message_hash = message_hash +  Value::from(":") + symbol.clone();
        };
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("privateV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channel": "executions",
                "token": token
            }))).unwrap()),
            "req_id": request_id
        }))).unwrap());
        if params.clone().is_nonnullish() {
            subscribe.set("params".into(), self.deep_extend_2(subscribe.get(Value::from("params")), params.clone()));
        };
        let mut result: Value = self.watch(url.clone(), message_hash.clone(), subscribe.clone(), subscription_hash.clone(), Value::Undefined).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = result.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(result.clone(), symbol.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn watch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        params.set("snap_trades".into(), true.into());
        return <Self as Kraken>::watch_private(self, Value::from("myTrades"), symbol.clone(), since.clone(), limit.clone(), params.clone()).await;
    }

    fn handle_my_trades(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        //     {
        //         "channel": "executions",
        //         "type": "update",
        //         "data": [
        //             {
        //                 "order_id": "O6NTZC-K6FRH-ATWBCK",
        //                 "exec_id": "T5DIUI-5N4KO-Z5BPXK",
        //                 "exec_type": "trade",
        //                 "trade_id": 8253473,
        //                 "symbol": "USDC/USD",
        //                 "side": "sell",
        //                 "last_qty": 15.44,
        //                 "last_price": 1.0002,
        //                 "liquidity_ind": "t",
        //                 "cost": 15.443088,
        //                 "order_userref": 0,
        //                 "order_status": "filled",
        //                 "order_type": "market",
        //                 "fee_usd_equiv": 0.03088618,
        //                 "fees": [
        //                     {
        //                         "asset": "USD",
        //                         "qty": 0.3458
        //                     }
        //                 ]
        //             }
        //         ],
        //         "sequence": 10
        //     }
        //
        let mut all_trades: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut all_trades_length: usize = all_trades.len();
        if all_trades_length.clone() > Value::from(0) {
            if self.get("myTrades".into()).is_nullish() {
                let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
                self.set("my_trades".into(), ArrayCache::new(limit));
            };
            let mut stored: Value = self.get("myTrades".into());
            let mut symbols: Value = Value::new_object();
            let mut i: usize = 0;
            while i < all_trades.len() {
                let mut trade: Value = self.safe_dict(all_trades.clone(), Value::from(i), Value::new_object());
                let mut parsed: Value = <Self as Kraken>::parse_ws_trade(self, trade.clone(), Value::Undefined);
                stored.append(parsed.clone());
                let mut symbol: Value = parsed.get(Value::from("symbol"));
                symbols.set(symbol.clone(), true.into());
                i += 1;
            };
            let mut name: Value = Value::from("myTrades");
            client.resolve(self.get("myTrades".into()), name.clone());
            let mut keys: Value = symbols.clone().keys();
            let mut i: usize = 0;
            while i < keys.len() {
                let mut message_hash: Value = name.clone() + Value::from(":") + keys.get(i.into());
                client.resolve(self.get("myTrades".into()), message_hash.clone());
                i += 1;
            };
        };
        Value::Undefined
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        //     {
        //         "order_id": "O6NTZC-K6FRH-ATWBCK",
        //         "exec_id": "T5DIUI-5N4KO-Z5BPXK",
        //         "exec_type": "trade",
        //         "trade_id": 8253473,
        //         "symbol": "USDC/USD",
        //         "side": "sell",
        //         "last_qty": 15.44,
        //         "last_price": 1.0002,
        //         "liquidity_ind": "t",
        //         "cost": 15.443088,
        //         "order_userref": 0,
        //         "order_status": "filled",
        //         "order_type": "market",
        //         "fee_usd_equiv": 0.03088618,
        //         "fees": [
        //             {
        //                 "asset": "USD",
        //                 "qty": 0.3458
        //             }
        //         ]
        //     }
        //
        let mut symbol: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        if market.clone().is_nonnullish() {
            symbol = market.get(Value::from("symbol"));
        };
        let mut fee: Value = Value::Undefined;
        if trade.contains_key(Value::from("fees")) {
            let mut fees: Value = self.safe_list(trade.clone(), Value::from("fees"), Value::new_array());
            let mut first_fee: Value = self.safe_dict(fees.clone(), Value::from(0), Value::new_object());
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": self.safe_number(first_fee.clone(), Value::from("qty"), Value::Undefined),
                "currency": self.safe_string(first_fee.clone(), Value::from("asset"), Value::Undefined)
            }))).unwrap());
        };
        let mut datetime: Value = self.safe_string(trade.clone(), Value::from("timestamp"), Value::Undefined);
        let mut liquidity_indicator: Value = self.safe_string(trade.clone(), Value::from("liquidity_ind"), Value::Undefined);
        let mut taker_or_maker: Value = if liquidity_indicator.clone() == Value::from("t") { Value::from("taker") } else { Value::from("maker") };
        return Value::Json(normalize(&Value::Json(json!({
            "info": trade,
            "id": self.safe_string(trade.clone(), Value::from("exec_id"), Value::Undefined),
            "order": self.safe_string(trade.clone(), Value::from("order_id"), Value::Undefined),
            "timestamp": self.parse8601(datetime.clone()),
            "datetime": datetime,
            "symbol": symbol,
            "type": self.safe_string(trade.clone(), Value::from("order_type"), Value::Undefined),
            "side": self.safe_string(trade.clone(), Value::from("side"), Value::Undefined),
            "takerOrMaker": taker_or_maker,
            "price": self.safe_number(trade.clone(), Value::from("last_price"), Value::Undefined),
            "amount": self.safe_number(trade.clone(), Value::from("last_qty"), Value::Undefined),
            "cost": self.safe_number(trade.clone(), Value::from("cost"), Value::Undefined),
            "fee": fee
        }))).unwrap());
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Kraken>::watch_private(self, Value::from("orders"), symbol.clone(), since.clone(), limit.clone(), extend_2(params.clone(), Value::Json(normalize(&Value::Json(json!({
            "snap_orders": true
        }))).unwrap()))).await;
    }

    fn handle_orders(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        //     {
        //         "channel": "executions",
        //         "type": "update",
        //         "data": [
        //             {
        //                 "order_id": "OK4GJX-KSTLS-7DZZO5",
        //                 "order_userref": 3,
        //                 "symbol": "BTC/USD",
        //                 "order_qty": 0.005,
        //                 "cum_cost": 0.0,
        //                 "time_in_force": "GTC",
        //                 "exec_type": "pending_new",
        //                 "side": "sell",
        //                 "order_type": "limit",
        //                 "limit_price_type": "static",
        //                 "limit_price": 26500.0,
        //                 "stop_price": 0.0,
        //                 "order_status": "pending_new",
        //                 "fee_usd_equiv": 0.0,
        //                 "fee_ccy_pref": "fciq",
        //                 "timestamp": "2023-09-22T10:33:05.709950Z"
        //             }
        //         ],
        //         "sequence": 8
        //     }
        //
        let mut all_orders: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut all_orders_length: usize = all_orders.len();
        if all_orders_length.clone() > Value::from(0) {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
            if self.get("orders".into()).is_nullish() {
                self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
            };
            let mut stored: Value = self.get("orders".into());
            let mut symbols: Value = Value::new_object();
            let mut i: usize = 0;
            while i < all_orders.len() {
                let mut order: Value = self.safe_dict(all_orders.clone(), Value::from(i), Value::new_object());
                let mut id: Value = self.safe_string(order.clone(), Value::from("order_id"), Value::Undefined);
                let mut parsed: Value = <Self as Kraken>::parse_ws_order(self, order.clone(), Value::Undefined);
                let mut symbol: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
                let mut previous_orders: Value = self.safe_value(stored.get(hashmap.clone()), symbol.clone(), Value::Undefined);
                let mut previous_order: Value = self.safe_value(previous_orders.clone(), id.clone(), Value::Undefined);
                let mut new_order: Value = parsed.clone();
                if previous_order.clone().is_nonnullish() {
                    let mut new_raw_order: Value = extend_2(previous_order.get(Value::from("info")), new_order.get(Value::from("info")));
                    new_order = <Self as Kraken>::parse_ws_order(self, new_raw_order.clone(), Value::Undefined);
                };
                let mut length: usize = stored.len();
                if length == limit.clone().into() && previous_order.clone().is_nullish() {
                    let mut first: Value = stored.get(Value::from(0));
                    let mut symbols_by_order_id: Value = self.safe_value(self.get("options".into()), Value::from("symbolsByOrderId"), Value::new_object());
                    if symbols_by_order_id.contains_key(first.get(Value::from("id"))) {
                        symbols_by_order_id.get(first.get(Value::from("id")).clone());
                    };
                };
                stored.append(new_order.clone());
                if symbol.clone().is_nonnullish() {
                    symbols.set(symbol.clone(), true.into());
                };
                i += 1;
            };
            let mut name: Value = Value::from("orders");
            client.resolve(self.get("orders".into()), name.clone());
            let mut keys: Value = symbols.clone().keys();
            let mut i: usize = 0;
            while i < keys.length {
                let mut message_hash: Value = name.clone() + Value::from(":") + keys.get(i.into());
                client.resolve(self.get("orders".into()), message_hash.clone());
                i += 1;
            };
        };
        Value::Undefined
    }

    fn parse_ws_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        // watchOrders
        //
        // open order
        //     {
        //         "order_id": "OK4GJX-KSTLS-7DZZO5",
        //         "order_userref": 3,
        //         "symbol": "BTC/USD",
        //         "order_qty": 0.005,
        //         "cum_cost": 0.0,
        //         "time_in_force": "GTC",
        //         "exec_type": "pending_new",
        //         "side": "sell",
        //         "order_type": "limit",
        //         "limit_price_type": "static",
        //         "limit_price": 26500.0,
        //         "stop_price": 0.0,
        //         "order_status": "pending_new",
        //         "fee_usd_equiv": 0.0,
        //         "fee_ccy_pref": "fciq",
        //         "timestamp": "2023-09-22T10:33:05.709950Z"
        //     }
        //
        // canceled order
        //
        //     {
        //         "timestamp": "2025-10-11T15:11:47.695226Z",
        //         "order_status": "canceled",
        //         "exec_type": "canceled",
        //         "order_userref": 0,
        //         "order_id": "OGAB7Y-BKX5F-PTK5RW",
        //         "cum_qty": 0,
        //         "cum_cost": 0,
        //         "fee_usd_equiv": 0,
        //         "avg_price": 0,
        //         "cancel_reason": "User requested",
        //         "reason": "User requested"
        //     }
        //
        let mut fee: Value = Value::Json(normalize(&Value::Json(json!({
            "cost": self.safe_string(order.clone(), Value::from("fee_usd_equiv"), Value::Undefined),
            "currency": "USD"
        }))).unwrap());
        let mut stop_price: Value = self.safe_string(order.clone(), Value::from("stop_price"), Value::Undefined);
        let mut datetime: Value = self.safe_string(order.clone(), Value::from("timestamp"), Value::Undefined);
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(order.clone(), Value::from("order_id"), Value::Undefined),
            "clientOrderId": self.safe_string(order.clone(), Value::from("order_userref"), Value::Undefined),
            "info": order,
            "timestamp": self.parse8601(datetime.clone()),
            "datetime": datetime,
            "lastTradeTimestamp": Value::Undefined,
            "status": <Self as Kraken>::parse_order_status(self, self.safe_string(order.clone(), Value::from("order_status"), Value::Undefined)),
            "symbol": self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined),
            "type": self.safe_string(order.clone(), Value::from("order_type"), Value::Undefined),
            "timeInForce": self.safe_string(order.clone(), Value::from("time_in_force"), Value::Undefined),
            "postOnly": Value::Undefined,
            "side": self.safe_string(order.clone(), Value::from("side"), Value::Undefined),
            "price": self.safe_string(order.clone(), Value::from("limit_price"), Value::Undefined),
            "stopPrice": stop_price,
            "triggerPrice": stop_price,
            "cost": self.safe_string(order.clone(), Value::from("cum_cost"), Value::Undefined),
            "amount": self.safe_string_2(order.clone(), Value::from("order_qty"), Value::from("cum_qty"), Value::Undefined),
            "filled": Value::Undefined,
            "average": self.safe_string(order.clone(), Value::from("avg_price"), Value::Undefined),
            "remaining": Value::Undefined,
            "fee": fee,
            "trades": Value::Undefined
        }))).unwrap()), Value::Undefined);
    }

    async fn watch_multi_helper(&mut self, mut unified_name: Value, mut channel_name: Value, mut symbols: Value, mut subscription_args: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        // symbols are required
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, false.into(), true.into(), false.into());
        let mut message_hashes: Value = Value::new_array();
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut event_trigger: Value = self.safe_string(params.clone(), Value::from("event_trigger"), Value::Undefined);
            if event_trigger.clone().is_nonnullish() {
                message_hashes.push(<Self as Kraken>::get_message_hash(self, channel_name.clone(), Value::Undefined, self.symbol(symbols.get(i.into()))));
            } else {
                message_hashes.push(<Self as Kraken>::get_message_hash(self, unified_name.clone(), Value::Undefined, self.symbol(symbols.get(i.into()))));
            };
            i += 1;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "subscribe",
            "params": Value::Json(normalize(&Value::Json(json!({
                "channel": channel_name,
                "symbol": symbols
            }))).unwrap()),
            "req_id": <Self as Kraken>::request_id(self)
        }))).unwrap());
        request.set("params".into(), self.deep_extend_2(request.get(Value::from("params")), params.clone()));
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("publicV2"));
        return self.watch_multiple(url.clone(), message_hashes.clone(), request.clone(), message_hashes.clone(), subscription_args.clone()).await;
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        <Self as Kraken>::load_markets(self, Value::Undefined, Value::Undefined).await;
        let mut token: Value = <Self as Kraken>::authenticate(self).await;
        let mut message_hash: Value = Value::from("balances");
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("privateV2"));
        let mut request_id: Value = <Self as Kraken>::request_id(self);
        let mut subscribe: Value = Value::Json(normalize(&Value::Json(json!({
            "method": "subscribe",
            "req_id": request_id,
            "params": Value::Json(normalize(&Value::Json(json!({
                "channel": "balances",
                "token": token
            }))).unwrap())
        }))).unwrap());
        let mut request: Value = self.deep_extend_2(subscribe.clone(), params.clone());
        return self.watch(url.clone(), message_hash.clone(), request.clone(), message_hash.clone(), Value::Undefined).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "channel": "balances",
        //         "data": [
        //             {
        //                 "asset": "BTC",
        //                 "asset_class": "currency",
        //                 "balance": 1.2,
        //                 "wallets": [
        //                     {
        //                         "type": "spot",
        //                         "id": "main",
        //                         "balance": 1.2
        //                     }
        //                 ]
        //             }
        //         ],
        //         "type": "snapshot",
        //         "sequence": 1
        //     }
        //
        let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": message
        }))).unwrap());
        let mut i: usize = 0;
        while i < data.len() {
            let mut currency_id: Value = self.safe_string(data.get(i.into()), Value::from("asset"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut account: Value = self.account();
            let mut eq: Value = self.safe_string(data.get(i.into()), Value::from("balance"), Value::Undefined);
            account.set("total".into(), eq.clone());
            result.set(code.clone(), account.clone());
            i += 1;
        };
        let mut r#type: Value = Value::from("spot");
        let mut balance: Value = self.safe_balance(result.clone());
        let mut old_balance: Value = self.safe_value(self.get("balance".into()), r#type.clone(), Value::new_object());
        let mut new_balance: Value = self.deep_extend_2(old_balance.clone(), balance.clone());
        self.get("balance".into()).set(r#type.clone(), self.safe_balance(new_balance.clone()));
        let mut channel: Value = self.safe_string(message.clone(), Value::from("channel"), Value::Undefined);
        client.resolve(self.get("balance".into()).get(r#type.clone()), channel.clone());
        Value::Undefined
    }

    fn get_message_hash(&mut self, mut unified_element_name: Value, mut sub_channel_name: Value, mut symbol: Value) -> Value {
        // unifiedElementName can be : orderbook, trade, ticker, bidask ...
        // subChannelName only applies to channel that needs specific variation (i.e. depth_50, depth_100..) to be selected
        let mut with_symbol: Value = (symbol.clone().is_nonnullish()).into();
        let mut message_hash: Value = unified_element_name.clone();
        if !with_symbol.is_truthy() {
            message_hash = message_hash +  Value::from("s");
        } else {
            message_hash = message_hash +  Value::from("@") + symbol.clone();
        };
        if sub_channel_name.clone().is_nonnullish() {
            message_hash = message_hash +  Value::from("#") + sub_channel_name.clone();
        };
        return message_hash.clone();
    }

    fn handle_subscription_status(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // public
        //
        //     {
        //         "channelID": 210,
        //         "channelName": "book-10",
        //         "event": "subscriptionStatus",
        //         "reqid": 1574146735269,
        //         "pair": "ETH/XBT",
        //         "status": "subscribed",
        //         "subscription": { depth: 10, name: "book" }
        //     }
        //
        // private
        //
        //     {
        //         "channelName": "openOrders",
        //         "event": "subscriptionStatus",
        //         "reqid": 1,
        //         "status": "subscribed",
        //         "subscription": { maxratecount: 125, name: "openOrders" }
        //     }
        //
        let mut channel_id: Value = self.safe_string(message.clone(), Value::from("channelID"), Value::Undefined);
        if channel_id.clone().is_nonnullish() {
            client.get(subscriptions.clone()).set(channel_id.clone(), message.clone());
        };
        Value::Undefined
    }

    fn handle_error_message(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "errorMessage": "Currency pair not in ISO 4217-A3 format foobar",
        //         "event": "subscriptionStatus",
        //         "pair": "foobar",
        //         "reqid": 1574146735269,
        //         "status": "error",
        //         "subscription": { name: "ticker" }
        //     }
        //
        // v2
        //     {
        //         "error": "Unsupported field: 'price' for the given msg type: add order",
        //         "method": "add_order",
        //         "success": false,
        //         "time_in": "2025-05-13T08:59:44.803511Z",
        //         "time_out": "2025-05-13T08:59:44.803542Z'
        //     }
        //
        let mut error_message: Value = self.safe_string_2(message.clone(), Value::from("errorMessage"), Value::from("error"), Value::Undefined);
        if error_message.clone().is_nonnullish() {
            let mut request_id: Value = self.safe_string_2(message.clone(), Value::from("reqid"), Value::from("req_id"), Value::Undefined);
            let mut broad: Value = self.get("exceptions".into()).get(Value::from("ws")).get(Value::from("broad"));
            let mut broad_key: Value = self.find_broadly_matched_key(broad.clone(), error_message.clone());
            let mut exception: Value = Value::Undefined;
            if broad_key.clone().is_nullish() {
                exception = ExchangeError::new(error_message);
            } else {
                // c# requirement to convert the errorMessage to string
                exception = broad.get(broad_key.clone())::new(error_message);
            };
            if request_id.clone().is_nonnullish() {
                client.reject(exception.clone(), request_id.clone());
            };
            return false.into();
        };
        return true.into();
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        let mut channel: Value = self.safe_string(message.clone(), Value::from("channel"), Value::Undefined);
        if channel.clone().is_nonnullish() {
            if channel.clone() == Value::from("executions") {
                let mut data: Value = self.safe_list(message.clone(), Value::from("data"), Value::new_array());
                let mut first: Value = self.safe_dict(data.clone(), Value::from(0), Value::new_object());
                let mut exec_type: Value = self.safe_string(first.clone(), Value::from("exec_type"), Value::Undefined);
                channel = if exec_type.clone() == Value::from("trade") { Value::from("myTrades") } else { Value::from("orders") };
            };
            let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
                "balances": self.get("handleBalance".into()),
                "book": self.get("handleOrderBook".into()),
                "ohlc": self.get("handleOHLCV".into()),
                "ticker": self.get("handleTicker".into()),
                "trade": self.get("handleTrades".into()),
                "myTrades": self.get("handleMyTrades".into()),
                "orders": self.get("handleOrders".into())
            }))).unwrap());
            // private
            let mut method: Value = self.safe_value(methods.clone(), channel.clone(), Value::Undefined);
            if method.clone().is_nonnullish() {
                method.call(self, client.clone(), message.clone());
            };
        };
        if <Self as Kraken>::handle_error_message(self, client.clone(), message.clone()).is_truthy() {
            let mut event: Value = self.safe_string_2(message.clone(), Value::from("event"), Value::from("method"), Value::Undefined);
            let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
                "heartbeat": self.get("handleHeartbeat".into()),
                "systemStatus": self.get("handleSystemStatus".into()),
                "subscriptionStatus": self.get("handleSubscriptionStatus".into()),
                "add_order": self.get("handleCreateEditOrder".into()),
                "amend_order": self.get("handleCreateEditOrder".into()),
                "cancel_order": self.get("handleCancelOrder".into()),
                "cancel_all": self.get("handleCancelAllOrders".into()),
                "pong": self.get("handlePong".into())
            }))).unwrap());
            let mut method: Value = self.safe_value(methods.clone(), event.clone(), Value::Undefined);
            if method.clone().is_nonnullish() {
                method.call(self, client.clone(), message.clone());
            };
        };
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "zendeskGet360000292886" => self.request("360000292886".into(), "zendesk".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "zendeskGet201893608" => self.request("201893608".into(), "zendesk".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssets" => self.request("Assets".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetpairs" => self.request("AssetPairs".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetDepth" => self.request("Depth".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOhlc" => self.request("OHLC".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetSpread" => self.request("Spread".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetSystemstatus" => self.request("SystemStatus".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTicker" => self.request("Ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTime" => self.request("Time".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTrades" => self.request("Trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAddorder" => self.request("AddOrder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAddorderbatch" => self.request("AddOrderBatch".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAddexport" => self.request("AddExport".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAmendorder" => self.request("AmendOrder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostBalance" => self.request("Balance".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCancelall" => self.request("CancelAll".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCancelallordersafter" => self.request("CancelAllOrdersAfter".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCancelorder" => self.request("CancelOrder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCancelorderbatch" => self.request("CancelOrderBatch".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostClosedorders" => self.request("ClosedOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostDepositaddresses" => self.request("DepositAddresses".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostDepositmethods" => self.request("DepositMethods".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostDepositstatus" => self.request("DepositStatus".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostEditorder" => self.request("EditOrder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostExportstatus" => self.request("ExportStatus".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostGetwebsocketstoken" => self.request("GetWebSocketsToken".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostLedgers" => self.request("Ledgers".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOpenorders" => self.request("OpenOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOpenpositions" => self.request("OpenPositions".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostQueryledgers" => self.request("QueryLedgers".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostQueryorders" => self.request("QueryOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostQuerytrades" => self.request("QueryTrades".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostRetrieveexport" => self.request("RetrieveExport".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostRemoveexport" => self.request("RemoveExport".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostBalanceex" => self.request("BalanceEx".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradebalance" => self.request("TradeBalance".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeshistory" => self.request("TradesHistory".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradevolume" => self.request("TradeVolume".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdraw" => self.request("Withdraw".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawcancel" => self.request("WithdrawCancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawinfo" => self.request("WithdrawInfo".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawmethods" => self.request("WithdrawMethods".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawaddresses" => self.request("WithdrawAddresses".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawstatus" => self.request("WithdrawStatus".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWallettransfer" => self.request("WalletTransfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCreatesubaccount" => self.request("CreateSubaccount".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccounttransfer" => self.request("AccountTransfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostEarnallocate" => self.request("Earn/Allocate".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostEarndeallocate" => self.request("Earn/Deallocate".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostEarnallocatestatus" => self.request("Earn/AllocateStatus".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostEarndeallocatestatus" => self.request("Earn/DeallocateStatus".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostEarnstrategies" => self.request("Earn/Strategies".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostEarnallocations" => self.request("Earn/Allocations".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct KrakenImpl(Value);
impl Exchange for KrakenImpl {}
impl Kraken for KrakenImpl {}
impl ValueTrait for KrakenImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl KrakenImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = KrakenImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Kraken::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


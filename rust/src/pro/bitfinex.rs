#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Bitfinex : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "bitfinex",
            "name": "Bitfinex",
            "countries": [
                "VG"
            ],
            "enableRateLimit": true,
            "rateLimit": 250,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": true,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "cancelOrders": true,
                "createDepositAddress": true,
                "createLimitOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createOrder": true,
                "createPostOnlyOrder": true,
                "createReduceOnlyOrder": true,
                "createStopLimitOrder": true,
                "createStopMarketOrder": true,
                "createStopOrder": true,
                "createTrailingAmountOrder": true,
                "createTrailingPercentOrder": false,
                "createTriggerOrder": true,
                "editOrder": true,
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchClosedOrder": true,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDepositsWithdrawals": true,
                "fetchFundingHistory": false,
                "fetchFundingRate": "emulated",
                "fetchFundingRateHistory": true,
                "fetchFundingRates": true,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLeverage": false,
                "fetchLeverageTiers": false,
                "fetchLiquidations": true,
                "fetchMarginMode": false,
                "fetchMarketLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterest": true,
                "fetchOpenInterests": true,
                "fetchOpenInterestHistory": true,
                "fetchOpenOrder": true,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrderBooks": false,
                "fetchOrderTrades": true,
                "fetchPosition": false,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": false,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchTransactions": "emulated",
                "fetchVolatilityHistory": false,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "setLeverage": false,
                "setMargin": true,
                "setMarginMode": false,
                "setPositionMode": false,
                "signIn": false,
                "transfer": true,
                "watchBalance": true,
                "watchMyTrades": true,
                "watchOHLCV": true,
                "watchOrderBook": true,
                "watchOrders": true,
                "watchTicker": true,
                "watchTickers": false,
                "watchTrades": true,
                "watchTradesForSymbols": false,
                "unWatchTrades": true,
                "unWatchOrderBook": true,
                "unWatchTicker": true,
                "unWatchOHLCV": true,
                "withdraw": true,
                "ws": true,
                "fetchAllGreeks": false
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/4a8e947f-ab46-481a-a8ae-8b20e9b03178",
                "api": {
                    "v1": "https://api.bitfinex.com",
                    "public": "https://api-pub.bitfinex.com",
                    "private": "https://api.bitfinex.com",
                    "ws": {
                        "public": "wss://api-pub.bitfinex.com/ws/2",
                        "private": "wss://api.bitfinex.com/ws/2"
                    }
                },
                "www": "https://www.bitfinex.com",
                "doc": [
                    "https://docs.bitfinex.com/v2/docs/",
                    "https://github.com/bitfinexcom/bitfinex-api-node"
                ],
                "fees": "https://www.bitfinex.com/fees"
            },
            "api": {
                "public": {
                    "get": {
                        "conf/{config}": 2.7,
                        "conf/pub:{action}:{object}": 2.7,
                        "conf/pub:{action}:{object}:{detail}": 2.7,
                        "conf/pub:map:{object}": 2.7,
                        "conf/pub:map:{object}:{detail}": 2.7,
                        "conf/pub:map:currency:{detail}": 2.7,
                        "conf/pub:map:currency:sym": 2.7,
                        "conf/pub:map:currency:label": 2.7,
                        "conf/pub:map:currency:unit": 2.7,
                        "conf/pub:map:currency:undl": 2.7,
                        "conf/pub:map:currency:pool": 2.7,
                        "conf/pub:map:currency:explorer": 2.7,
                        "conf/pub:map:currency:tx:fee": 2.7,
                        "conf/pub:map:tx:method": 2.7,
                        "conf/pub:list:{object}": 2.7,
                        "conf/pub:list:{object}:{detail}": 2.7,
                        "conf/pub:list:currency": 2.7,
                        "conf/pub:list:pair:exchange": 2.7,
                        "conf/pub:list:pair:margin": 2.7,
                        "conf/pub:list:pair:futures": 2.7,
                        "conf/pub:list:competitions": 2.7,
                        "conf/pub:info:{object}": 2.7,
                        "conf/pub:info:{object}:{detail}": 2.7,
                        "conf/pub:info:pair": 2.7,
                        "conf/pub:info:pair:futures": 2.7,
                        "conf/pub:info:tx:status": 2.7,
                        "conf/pub:fees": 2.7,
                        "platform/status": 8,
                        "tickers": 2.7,
                        "ticker/{symbol}": 2.7,
                        "tickers/hist": 2.7,
                        "trades/{symbol}/hist": 2.7,
                        "book/{symbol}/{precision}": 1,
                        "book/{symbol}/P0": 1,
                        "book/{symbol}/P1": 1,
                        "book/{symbol}/P2": 1,
                        "book/{symbol}/P3": 1,
                        "book/{symbol}/R0": 1,
                        "stats1/{key}:{size}:{symbol}:{side}/{section}": 2.7,
                        "stats1/{key}:{size}:{symbol}:{side}/last": 2.7,
                        "stats1/{key}:{size}:{symbol}:{side}/hist": 2.7,
                        "stats1/{key}:{size}:{symbol}/{section}": 2.7,
                        "stats1/{key}:{size}:{symbol}/last": 2.7,
                        "stats1/{key}:{size}:{symbol}/hist": 2.7,
                        "stats1/{key}:{size}:{symbol}:long/last": 2.7,
                        "stats1/{key}:{size}:{symbol}:long/hist": 2.7,
                        "stats1/{key}:{size}:{symbol}:short/last": 2.7,
                        "stats1/{key}:{size}:{symbol}:short/hist": 2.7,
                        "candles/trade:{timeframe}:{symbol}:{period}/{section}": 2.7,
                        "candles/trade:{timeframe}:{symbol}/{section}": 2.7,
                        "candles/trade:{timeframe}:{symbol}/last": 2.7,
                        "candles/trade:{timeframe}:{symbol}/hist": 2.7,
                        "status/{type}": 2.7,
                        "status/deriv": 2.7,
                        "status/deriv/{symbol}/hist": 2.7,
                        "liquidations/hist": 80,
                        "rankings/{key}:{timeframe}:{symbol}/{section}": 2.7,
                        "rankings/{key}:{timeframe}:{symbol}/hist": 2.7,
                        "pulse/hist": 2.7,
                        "pulse/profile/{nickname}": 2.7,
                        "funding/stats/{symbol}/hist": 10,
                        "ext/vasps": 1
                    },
                    "post": {
                        "calc/trade/avg": 2.7,
                        "calc/fx": 2.7
                    }
                },
                "private": {
                    "post": {
                        "auth/r/wallets": 2.7,
                        "auth/r/wallets/hist": 2.7,
                        "auth/r/orders": 2.7,
                        "auth/r/orders/{symbol}": 2.7,
                        "auth/w/order/submit": 2.7,
                        "auth/w/order/update": 2.7,
                        "auth/w/order/cancel": 2.7,
                        "auth/w/order/multi": 2.7,
                        "auth/w/order/cancel/multi": 2.7,
                        "auth/r/orders/{symbol}/hist": 2.7,
                        "auth/r/orders/hist": 2.7,
                        "auth/r/order/{symbol}:{id}/trades": 2.7,
                        "auth/r/trades/{symbol}/hist": 2.7,
                        "auth/r/trades/hist": 2.7,
                        "auth/r/ledgers/{currency}/hist": 2.7,
                        "auth/r/ledgers/hist": 2.7,
                        "auth/r/info/margin/{key}": 2.7,
                        "auth/r/info/margin/base": 2.7,
                        "auth/r/info/margin/sym_all": 2.7,
                        "auth/r/positions": 2.7,
                        "auth/w/position/claim": 2.7,
                        "auth/w/position/increase:": 2.7,
                        "auth/r/position/increase/info": 2.7,
                        "auth/r/positions/hist": 2.7,
                        "auth/r/positions/audit": 2.7,
                        "auth/r/positions/snap": 2.7,
                        "auth/w/deriv/collateral/set": 2.7,
                        "auth/w/deriv/collateral/limits": 2.7,
                        "auth/r/funding/offers": 2.7,
                        "auth/r/funding/offers/{symbol}": 2.7,
                        "auth/w/funding/offer/submit": 2.7,
                        "auth/w/funding/offer/cancel": 2.7,
                        "auth/w/funding/offer/cancel/all": 2.7,
                        "auth/w/funding/close": 2.7,
                        "auth/w/funding/auto": 2.7,
                        "auth/w/funding/keep": 2.7,
                        "auth/r/funding/offers/{symbol}/hist": 2.7,
                        "auth/r/funding/offers/hist": 2.7,
                        "auth/r/funding/loans": 2.7,
                        "auth/r/funding/loans/hist": 2.7,
                        "auth/r/funding/loans/{symbol}": 2.7,
                        "auth/r/funding/loans/{symbol}/hist": 2.7,
                        "auth/r/funding/credits": 2.7,
                        "auth/r/funding/credits/hist": 2.7,
                        "auth/r/funding/credits/{symbol}": 2.7,
                        "auth/r/funding/credits/{symbol}/hist": 2.7,
                        "auth/r/funding/trades/{symbol}/hist": 2.7,
                        "auth/r/funding/trades/hist": 2.7,
                        "auth/r/info/funding/{key}": 2.7,
                        "auth/r/info/user": 2.7,
                        "auth/r/summary": 2.7,
                        "auth/r/logins/hist": 2.7,
                        "auth/r/permissions": 2.7,
                        "auth/w/token": 2.7,
                        "auth/r/audit/hist": 2.7,
                        "auth/w/transfer": 2.7,
                        "auth/w/deposit/address": 24,
                        "auth/w/deposit/invoice": 24,
                        "auth/w/withdraw": 24,
                        "auth/r/movements/{currency}/hist": 2.7,
                        "auth/r/movements/hist": 2.7,
                        "auth/r/alerts": 5.34,
                        "auth/w/alert/set": 2.7,
                        "auth/w/alert/price:{symbol}:{price}/del": 2.7,
                        "auth/w/alert/{type}:{symbol}:{price}/del": 2.7,
                        "auth/calc/order/avail": 2.7,
                        "auth/w/settings/set": 2.7,
                        "auth/r/settings": 2.7,
                        "auth/w/settings/del": 2.7,
                        "auth/r/pulse/hist": 2.7,
                        "auth/w/pulse/add": 16,
                        "auth/w/pulse/del": 2.7
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1h",
                "3h": "3h",
                "4h": "4h",
                "6h": "6h",
                "12h": "12h",
                "1d": "1D",
                "1w": "7D",
                "2w": "14D",
                "1M": "1M"
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.002,
                    "maker": 0.001,
                    "feeSide": "get",
                    "tiers": {
                        "taker": [
                            [
                                0,
                                0.002
                            ],
                            [
                                500000,
                                0.002
                            ],
                            [
                                1000000,
                                0.002
                            ],
                            [
                                2500000,
                                0.002
                            ],
                            [
                                5000000,
                                0.002
                            ],
                            [
                                7500000,
                                0.002
                            ],
                            [
                                10000000,
                                0.0018
                            ],
                            [
                                15000000,
                                0.0016
                            ],
                            [
                                20000000,
                                0.0014
                            ],
                            [
                                25000000,
                                0.0012
                            ],
                            [
                                30000000,
                                0.001
                            ]
                        ],
                        "maker": [
                            [
                                0,
                                0.001
                            ],
                            [
                                500000,
                                0.0008
                            ],
                            [
                                1000000,
                                0.0006
                            ],
                            [
                                2500000,
                                0.0004
                            ],
                            [
                                5000000,
                                0.0002
                            ],
                            [
                                7500000,
                                0
                            ],
                            [
                                10000000,
                                0
                            ],
                            [
                                15000000,
                                0
                            ],
                            [
                                20000000,
                                0
                            ],
                            [
                                25000000,
                                0
                            ],
                            [
                                30000000,
                                0
                            ]
                        ]
                    }
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "UST": "USDT",
                "EUTF0": "EURT",
                "USTF0": "USDT",
                "ALG": "ALGO",
                "AMP": "AMPL",
                "ATO": "ATOM",
                "BCHABC": "XEC",
                "BCHN": "BCH",
                "DAT": "DATA",
                "DOG": "MDOGE",
                "DSH": "DASH",
                "EDO": "PNT",
                "EUS": "EURS",
                "EUT": "EURT",
                "HTX": "HT",
                "IDX": "ID",
                "IOT": "IOTA",
                "IQX": "IQ",
                "LUNA": "LUNC",
                "LUNA2": "LUNA",
                "MNA": "MANA",
                "ORS": "ORS Group",
                "PAS": "PASS",
                "QSH": "QASH",
                "QTM": "QTUM",
                "RBT": "RBTC",
                "SNG": "SNGLS",
                "STJ": "STORJ",
                "TERRAUST": "USTC",
                "TSD": "TUSD",
                "YGG": "YEED",
                "YYW": "YOYOW",
                "UDC": "USDC",
                "VSY": "VSYS",
                "WAX": "WAXP",
                "XCH": "XCHF",
                "ZBT": "ZB"
            },
            "precisionMode": 3,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v2",
            "options": {
                "precision": "R0",
                "exchangeTypes": {
                    "MARKET": "market",
                    "EXCHANGE MARKET": "market",
                    "LIMIT": "limit",
                    "EXCHANGE LIMIT": "limit",
                    "EXCHANGE STOP": "market",
                    "EXCHANGE FOK": "limit",
                    "EXCHANGE STOP LIMIT": "limit",
                    "EXCHANGE IOC": "limit"
                },
                "orderTypes": {
                    "market": "EXCHANGE MARKET",
                    "limit": "EXCHANGE LIMIT"
                },
                "fiat": {
                    "USD": "USD",
                    "EUR": "EUR",
                    "JPY": "JPY",
                    "GBP": "GBP",
                    "CHN": "CHN"
                },
                "v2AccountsByType": {
                    "spot": "exchange",
                    "exchange": "exchange",
                    "funding": "funding",
                    "margin": "margin",
                    "derivatives": "margin",
                    "future": "margin",
                    "swap": "margin"
                },
                "withdraw": {
                    "includeFee": false
                },
                "networks": {
                    "BTC": "BITCOIN",
                    "LTC": "LITECOIN",
                    "ERC20": "ETHEREUM",
                    "OMNI": "TETHERUSO",
                    "LIQUID": "TETHERUSL",
                    "TRC20": "TETHERUSX",
                    "EOS": "TETHERUSS",
                    "AVAX": "TETHERUSDTAVAX",
                    "SOL": "TETHERUSDTSOL",
                    "ALGO": "TETHERUSDTALG",
                    "BCH": "TETHERUSDTBCH",
                    "KSM": "TETHERUSDTKSM",
                    "DVF": "TETHERUSDTDVF",
                    "OMG": "TETHERUSDTOMG"
                },
                "networksById": {
                    "TETHERUSE": "ERC20"
                },
                "watchOrderBook": {
                    "prec": "P0",
                    "freq": "F0",
                    "checksum": true
                },
                "ordersLimit": 1000
            },
            "features": {
                "default": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": true,
                        "triggerPrice": true,
                        "triggerDirection": false,
                        "stopLossPrice": true,
                        "takeProfitPrice": true,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": true,
                        "leverage": true,
                        "marketBuyRequiresPrice": false,
                        "marketBuyByCost": true,
                        "selfTradePrevention": false,
                        "iceberg": false
                    },
                    "createOrders": {
                        "max": 75
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 2500,
                        "untilDays": 100000,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 10000
                    }
                },
                "spot": {
                    "extends": "default"
                },
                "swap": {
                    "linear": {
                        "extends": "default"
                    }
                },
                "future": {}
            }
        }"###).unwrap())
    }

    async fn subscribe(&mut self, mut channel: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut market_id: Value = market.get(Value::from("id"));
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        let mut client: Value = self.client(url.clone());
        let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "channel": channel,
            "symbol": market_id
        }))).unwrap());
        let mut result: Value = self.watch(url.clone(), message_hash.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hash.clone(), Value::Json(normalize(&Value::Json(json!({
            "checksum": false
        }))).unwrap())).await;
        let mut checksum: Value = self.safe_bool(self.get("options".into()), Value::from("checksum"), true.into());
        if checksum.is_truthy() && channel.clone() == Value::from("book") {
            let mut sub: Value = client.get(subscriptions.clone()).get(message_hash.clone());
            if sub.is_truthy() && !sub.get(Value::from("checksum")).is_truthy() {
                client.get(subscriptions.clone()).get(message_hash.clone()).set("checksum".into(), true.into());
                client.send(Value::Json(normalize(&Value::Json(json!({
                    "event": "conf",
                    "flags": 131072
                }))).unwrap())).await;
            };
        };
        return result.clone();
    }

    async fn un_subscribe(&mut self, mut channel: Value, mut topic: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut market_id: Value = market.get(Value::from("id"));
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        let mut client: Value = self.client(url.clone());
        let mut sub_message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        let mut message_hash: Value = Value::from("unsubscribe:") + channel.clone() + Value::from(":") + market_id.clone();
        let mut un_sub_topic: Value = Value::from("unsubscribe") + Value::from(":") + topic.clone() + Value::from(":") + symbol.clone();
        let mut channel_id: Value = self.safe_string(client.get(subscriptions.clone()), un_sub_topic.clone(), Value::Undefined);
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "unsubscribe",
            "chanId": channel_id
        }))).unwrap());
        let mut un_sub_chan_msg: Value = Value::from("unsubscribe:") + channel_id.clone();
        client.get(subscriptions.clone()).set(un_sub_chan_msg.clone(), sub_message_hash.clone());
        let mut subscription: Value = Value::Json(normalize(&Value::Json(json!({
            "messageHashes": Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()])),
            "subMessageHashes": Value::Json(serde_json::Value::Array(vec![sub_message_hash.clone().into()])),
            "topic": topic,
            "unsubscribe": true,
            "symbols": Value::Json(serde_json::Value::Array(vec![symbol.clone().into()]))
        }))).unwrap());
        return self.watch(url.clone(), message_hash.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hash.clone(), subscription.clone()).await;
    }

    async fn subscribe_private(&mut self, mut message_hash: Value) -> Value {
        self.load_markets(Value::Undefined, Value::Undefined).await;
        <Self as Bitfinex>::authenticate(self, Value::Undefined).await;
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        return self.watch(url.clone(), message_hash.clone(), Value::Undefined, Value::from(1)).await;
    }

    async fn watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut interval: Value = self.safe_string(self.get("timeframes".into()), timeframe.clone(), timeframe.clone());
        let mut channel: Value = Value::from("candles");
        let mut key: Value = Value::from("trade:") + interval.clone() + Value::from(":") + market.get(Value::from("id"));
        let mut message_hash: Value = channel.clone() + Value::from(":") + interval.clone() + Value::from(":") + market.get(Value::from("id"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "subscribe",
            "channel": channel,
            "key": key
        }))).unwrap());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        // not using subscribe here because this message has a different format
        let mut ohlcv: Value = self.watch(url.clone(), message_hash.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hash.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = ohlcv.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(ohlcv.clone(), since.clone(), limit.clone(), Value::from(0), true.into());
    }

    async fn un_watch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut params: Value) -> Value {
        timeframe = timeframe.or_default(Value::from("1m"));
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        symbol = market.get(Value::from("symbol"));
        let mut interval: Value = self.safe_string(self.get("timeframes".into()), timeframe.clone(), timeframe.clone());
        let mut channel: Value = Value::from("candles");
        let mut sub_message_hash: Value = channel.clone() + Value::from(":") + interval.clone() + Value::from(":") + market.get(Value::from("id"));
        let mut message_hash: Value = Value::from("unsubscribe:") + sub_message_hash.clone();
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("public"));
        let mut client: Value = self.client(url.clone());
        let mut sub_id: Value = Value::from("unsubscribe:trade:") + interval.clone() + Value::from(":") + market.get(Value::from("id"));
        // trade here because we use the key
        let mut channel_id: Value = self.safe_string(client.get(subscriptions.clone()), sub_id.clone(), Value::Undefined);
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "event": "unsubscribe",
            "chanId": channel_id
        }))).unwrap());
        let mut un_sub_chan_msg: Value = Value::from("unsubscribe:") + channel_id.clone();
        client.get(subscriptions.clone()).set(un_sub_chan_msg.clone(), sub_message_hash.clone());
        let mut subscription: Value = Value::Json(normalize(&Value::Json(json!({
            "messageHashes": Value::Json(serde_json::Value::Array(vec![message_hash.clone().into()])),
            "subMessageHashes": Value::Json(serde_json::Value::Array(vec![sub_message_hash.clone().into()])),
            "topic": "ohlcv",
            "unsubscribe": true,
            "symbols": Value::Json(serde_json::Value::Array(vec![symbol.clone().into()]))
        }))).unwrap());
        return self.watch(url.clone(), message_hash.clone(), self.deep_extend_2(request.clone(), params.clone()), message_hash.clone(), subscription.clone()).await;
    }

    fn handle_ohlcv(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // initial snapshot
        //   [
        //       341527, // channel id
        //       [
        //          [
        //             1654705860000, // timestamp
        //             1802.6, // open
        //             1800.3, // close
        //             1802.8, // high
        //             1800.3, // low
        //             86.49588236 // volume
        //          ],
        //          [
        //             1654705800000,
        //             1803.6,
        //             1802.6,
        //             1804.9,
        //             1802.3,
        //             74.6348086
        //          ],
        //          [
        //             1654705740000,
        //             1802.5,
        //             1803.2,
        //             1804.4,
        //             1802.4,
        //             23.61801085
        //          ]
        //       ]
        //   ]
        //
        // update
        //   [
        //       211171,
        //       [
        //          1654705680000,
        //          1801,
        //          1802.4,
        //          1802.9,
        //          1800.4,
        //          23.91911091
        //       ]
        //   ]
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from(1), Value::new_array());
        let mut ohlcvs: Value = Value::Undefined;
        let mut first: Value = self.safe_value(data.clone(), Value::from(0), Value::Undefined);
        if Array::is_array(first.clone()).is_truthy() {
            // snapshot
            ohlcvs = data.clone();
        } else {
            // update
            ohlcvs = Value::Json(serde_json::Value::Array(vec![data.clone().into()]));
        };
        let mut channel: Value = self.safe_value(subscription.clone(), Value::from("channel"), Value::Undefined);
        let mut key: Value = self.safe_string(subscription.clone(), Value::from("key"), Value::Undefined);
        let mut key_parts: Value = key.split(Value::from(":"));
        let mut interval: Value = self.safe_string(key_parts.clone(), Value::from(1), Value::Undefined);
        let mut market_id: Value = key.clone();
        market_id = market_id.replace(Value::from("trade:"), Value::from(""));
        market_id = market_id.replace(interval.clone() + Value::from(":"), Value::from(""));
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut timeframe: Value = self.find_timeframe(interval.clone());
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut message_hash: Value = channel.clone() + Value::from(":") + interval.clone() + Value::from(":") + market_id.clone();
        self.get("ohlcvs".into()).set(symbol.clone(), self.safe_value(self.get("ohlcvs".into()), symbol.clone(), Value::new_object()));
        let mut stored: Value = self.safe_value(self.get("ohlcvs".into()).get(symbol.clone()), timeframe.clone(), Value::Undefined);
        if stored.clone().is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("OHLCVLimit"), Value::from(1000));
            stored = ArrayCacheByTimestamp::new(limit);
            self.get("ohlcvs".into()).get(symbol.clone()).set(timeframe.clone(), stored.clone());
        };
        let mut ohlcvs_length: usize = ohlcvs.len();
        let mut i: usize = 0;
        while i < ohlcvs_length.clone().into() {
            let mut ohlcv: Value = ohlcvs.get(ohlcvs_length.clone() - Value::from(i) - Value::from(1).clone());
            let mut parsed: Value = self.parse_ohlcv(ohlcv.clone(), market.clone());
            stored.append(parsed.clone());
            i += 1;
        };
        client.resolve(stored.clone(), message_hash.clone());
        Value::Undefined
    }

    async fn watch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut trades: Value = <Self as Bitfinex>::subscribe(self, Value::from("trades"), symbol.clone(), params.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_since_limit(trades.clone(), since.clone(), limit.clone(), Value::from("timestamp"), true.into());
    }

    async fn un_watch_trades(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Bitfinex>::un_subscribe(self, Value::from("trades"), Value::from("trades"), symbol.clone(), params.clone()).await;
    }

    async fn watch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut message_hash: Value = Value::from("myTrade");
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            message_hash = message_hash +  Value::from(":") + market.get(Value::from("id"));
        };
        let mut trades: Value = <Self as Bitfinex>::subscribe_private(self, message_hash.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = trades.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(trades.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    async fn watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Bitfinex>::subscribe(self, Value::from("ticker"), symbol.clone(), params.clone()).await;
    }

    async fn un_watch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Bitfinex>::un_subscribe(self, Value::from("ticker"), Value::from("ticker"), symbol.clone(), params.clone()).await;
    }

    fn handle_my_trade(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        subscription = subscription.or_default(Value::new_object());
        //
        // trade execution
        // [
        //     0,
        //     "te", // or tu
        //     [
        //        1133411090,
        //        "tLTCUST",
        //        1655110144598,
        //        97084883506,
        //        0.1,
        //        42.821,
        //        "EXCHANGE MARKET",
        //        42.799,
        //        -1,
        //        null,
        //        null,
        //        1655110144596
        //     ]
        // ]
        //
        let mut name: Value = Value::from("myTrade");
        let mut data: Value = self.safe_value(message.clone(), Value::from(2), Value::Undefined);
        let mut trade: Value = <Self as Bitfinex>::parse_ws_trade(self, data.clone(), Value::Undefined);
        let mut symbol: Value = trade.get(Value::from("symbol"));
        let mut market: Value = self.market(symbol.clone());
        let mut message_hash: Value = name.clone() + Value::from(":") + market.get(Value::from("id"));
        if self.get("myTrades".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
            self.set("my_trades".into(), ArrayCacheBySymbolById::new(limit));
        };
        let mut trades_array: Value = self.get("myTrades".into());
        trades_array.append(trade.clone());
        self.set("my_trades".into(), trades_array.clone());
        // generic subscription
        client.resolve(trades_array.clone(), name.clone());
        // specific subscription
        client.resolve(trades_array.clone(), message_hash.clone());
        Value::Undefined
    }

    fn handle_trades(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // initial snapshot
        //
        //    [
        //        188687, // channel id
        //        [
        //          [ 1128060675, 1654701572690, 0.00217533, 1815.3 ], // id, mts, amount, price
        //          [ 1128060665, 1654701551231, -0.00280472, 1814.1 ],
        //          [ 1128060664, 1654701550996, -0.00364444, 1814.1 ],
        //          [ 1128060656, 1654701527730, -0.00265203, 1814.2 ],
        //          [ 1128060647, 1654701505193, 0.00262395, 1815.2 ],
        //          [ 1128060642, 1654701484656, -0.13411443, 1816 ],
        //          [ 1128060641, 1654701484656, -0.00088557, 1816 ],
        //          [ 1128060639, 1654701478326, -0.002, 1816 ],
        //        ]
        //    ]
        // update
        //
        //    [
        //        360141,
        //        "te",
        //        [
        //            1128060969, // id
        //            1654702500098, // mts
        //            0.00325131, // amount positive buy, negative sell
        //            1818.5, // price
        //        ],
        //    ]
        //
        //
        let mut channel: Value = self.safe_value(subscription.clone(), Value::from("channel"), Value::Undefined);
        let mut market_id: Value = self.safe_string(subscription.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        let mut trades_limit: Value = self.safe_integer(self.get("options".into()), Value::from("tradesLimit"), Value::from(1000));
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut stored: Value = self.safe_value(self.get("trades".into()), symbol.clone(), Value::Undefined);
        if stored.clone().is_nullish() {
            stored = ArrayCache::new(trades_limit);
            self.get("trades".into()).set(symbol.clone(), stored.clone());
        };
        let mut message_length: usize = message.len();
        if message_length.clone() == Value::from(2) {
            // initial snapshot
            let mut trades: Value = self.safe_list(message.clone(), Value::from(1), Value::new_array());
            // needs to be reversed to make chronological order
            let mut length: usize = trades.len();
            let mut i: usize = 0;
            while i < length {
                let mut index: Value = Value::from(length) - Value::from(i) - Value::from(1);
                let mut parsed: Value = <Self as Bitfinex>::parse_ws_trade(self, trades.get(index.clone()), market.clone());
                stored.append(parsed.clone());
                i += 1;
            };
        } else {
            // update
            let mut r#type: Value = self.safe_string(message.clone(), Value::from(1), Value::Undefined);
            if r#type.clone() == Value::from("tu") {
                // don't resolve for a duplicate update
                // since te and tu updates are duplicated on the public stream
                return Value::Undefined;
            };
            let mut trade: Value = self.safe_value(message.clone(), Value::from(2), Value::new_array());
            let mut parsed: Value = <Self as Bitfinex>::parse_ws_trade(self, trade.clone(), market.clone());
            stored.append(parsed.clone());
        };
        client.resolve(stored.clone(), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        //    [
        //        1128060969, // id
        //        1654702500098, // mts
        //        0.00325131, // amount positive buy, negative sell
        //        1818.5, // price
        //    ]
        //
        // trade execution
        //
        //    [
        //        1133411090, // id
        //        "tLTCUST", // symbol
        //        1655110144598, // create ms
        //        97084883506, // order id
        //        0.1, // amount
        //        42.821, // price
        //        "EXCHANGE MARKET", // order type
        //        42.799, // order price
        //        -1, // maker
        //        null, // fee
        //        null, // fee currency
        //        1655110144596 // cid
        //    ]
        //
        // trade update
        //
        //    [
        //       1133411090,
        //       "tLTCUST",
        //       1655110144598,
        //       97084883506,
        //       0.1,
        //       42.821,
        //       "EXCHANGE MARKET",
        //       42.799,
        //       -1,
        //       -0.0002,
        //       "LTC",
        //       1655110144596
        //    ]
        //
        let mut num_fields: usize = trade.len();
        let mut is_public: Value = (num_fields.clone() <= Value::from(8)).into();
        let mut market_id: Value = if !is_public.is_truthy() { self.safe_string(trade.clone(), Value::from(1), Value::Undefined) } else { Value::Undefined };
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut created_key: Value = if is_public.is_truthy() { Value::from(1) } else { Value::from(2) };
        let mut price_key: Value = if is_public.is_truthy() { Value::from(3) } else { Value::from(5) };
        let mut amount_key: Value = if is_public.is_truthy() { Value::from(2) } else { Value::from(4) };
        market_id = market.get(Value::from("id"));
        let mut r#type: Value = self.safe_string(trade.clone(), Value::from(6), Value::Undefined);
        if r#type.clone().is_nonnullish() {
            if r#type.index_of(Value::from("LIMIT")) > Value::from(1).neg() {
                r#type = Value::from("limit");
            } else if r#type.index_of(Value::from("MARKET")) > Value::from(1).neg() {
                r#type = Value::from("market");
            };
        };
        let mut order_id: Value = if !is_public.is_truthy() { self.safe_string(trade.clone(), Value::from(3), Value::Undefined) } else { Value::Undefined };
        let mut id: Value = self.safe_string(trade.clone(), Value::from(0), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(trade.clone(), created_key.clone(), Value::Undefined);
        let mut price: Value = self.safe_string(trade.clone(), price_key.clone(), Value::Undefined);
        let mut amount_string: Value = self.safe_string(trade.clone(), amount_key.clone(), Value::Undefined);
        let mut amount: Value = self.parse_number(Precise::string_abs(amount_string.clone()), Value::Undefined);
        let mut side: Value = Value::Undefined;
        if amount.clone().is_nonnullish() {
            side = if Precise::string_gt(amount_string.clone(), Value::from("0")) { Value::from("buy") } else { Value::from("sell") };
        };
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut fee_value: Value = self.safe_string(trade.clone(), Value::from(9), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        if fee_value.clone().is_nonnullish() {
            let mut currency_id: Value = self.safe_string(trade.clone(), Value::from(10), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_value,
                "currency": code
            }))).unwrap());
        };
        let mut maker: Value = self.safe_integer(trade.clone(), Value::from(8), Value::Undefined);
        let mut taker_or_maker: Value = Value::Undefined;
        if maker.clone().is_nonnullish() {
            taker_or_maker = if maker.clone() == Value::from(1).neg() { Value::from("taker") } else { Value::from("maker") };
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol,
            "id": id,
            "order": order_id,
            "type": r#type,
            "takerOrMaker": taker_or_maker,
            "side": side,
            "price": price,
            "amount": amount,
            "cost": Value::Undefined,
            "fee": fee
        }))).unwrap()), market.clone());
    }

    fn handle_ticker(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // [
        //    340432, // channel ID
        //     [
        //         236.62,        // 1 BID float Price of last highest bid
        //         9.0029,        // 2 BID_SIZE float Size of the last highest bid
        //         236.88,        // 3 ASK float Price of last lowest ask
        //         7.1138,        // 4 ASK_SIZE float Size of the last lowest ask
        //         -1.02,         // 5 DAILY_CHANGE float Amount that the last price has changed since yesterday
        //         0,             // 6 DAILY_CHANGE_PERC float Amount that the price has changed expressed in percentage terms
        //         236.52,        // 7 LAST_PRICE float Price of the last trade.
        //         5191.36754297, // 8 VOLUME float Daily volume
        //         250.01,        // 9 HIGH float Daily high
        //         220.05,        // 10 LOW float Daily low
        //     ]
        //  ]
        //
        let mut ticker: Value = self.safe_value(message.clone(), Value::from(1), Value::Undefined);
        let mut market_id: Value = self.safe_string(subscription.clone(), Value::from("symbol"), Value::Undefined);
        let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut parsed: Value = <Self as Bitfinex>::parse_ws_ticker(self, ticker.clone(), market.clone());
        let mut channel: Value = Value::from("ticker");
        let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        self.get("tickers".into()).set(symbol.clone(), parsed.clone());
        client.resolve(parsed.clone(), message_hash.clone());
        Value::Undefined
    }

    fn parse_ws_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     [
        //         236.62,        // 1 BID float Price of last highest bid
        //         9.0029,        // 2 BID_SIZE float Size of the last highest bid
        //         236.88,        // 3 ASK float Price of last lowest ask
        //         7.1138,        // 4 ASK_SIZE float Size of the last lowest ask
        //         -1.02,         // 5 DAILY_CHANGE float Amount that the last price has changed since yesterday
        //         0,             // 6 DAILY_CHANGE_PERC float Amount that the price has changed expressed in percentage terms
        //         236.52,        // 7 LAST_PRICE float Price of the last trade.
        //         5191.36754297, // 8 VOLUME float Daily volume
        //         250.01,        // 9 HIGH float Daily high
        //         220.05,        // 10 LOW float Daily low
        //     ]
        //
        market = self.safe_market(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut last: Value = self.safe_string(ticker.clone(), Value::from(6), Value::Undefined);
        let mut change: Value = self.safe_string(ticker.clone(), Value::from(4), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "high": self.safe_string(ticker.clone(), Value::from(8), Value::Undefined),
            "low": self.safe_string(ticker.clone(), Value::from(9), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from(0), Value::Undefined),
            "bidVolume": self.safe_string(ticker.clone(), Value::from(1), Value::Undefined),
            "ask": self.safe_string(ticker.clone(), Value::from(2), Value::Undefined),
            "askVolume": self.safe_string(ticker.clone(), Value::from(3), Value::Undefined),
            "vwap": Value::Undefined,
            "open": Value::Undefined,
            "close": last,
            "last": last,
            "previousClose": Value::Undefined,
            "change": change,
            "percentage": self.safe_string(ticker.clone(), Value::from(5), Value::Undefined),
            "average": Value::Undefined,
            "baseVolume": self.safe_string(ticker.clone(), Value::from(7), Value::Undefined),
            "quoteVolume": Value::Undefined,
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn watch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if limit.clone().is_nonnullish() {
            if limit.clone() != Value::from(25) && limit.clone() != Value::from(100) {
                panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" watchOrderBook limit argument must be undefined, 25 or 100"))"###);
            };
        };
        let mut options: Value = self.safe_value(self.get("options".into()), Value::from("watchOrderBook"), Value::new_object());
        let mut prec: Value = self.safe_string(options.clone(), Value::from("prec"), Value::from("P0"));
        let mut freq: Value = self.safe_string(options.clone(), Value::from("freq"), Value::from("F0"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "prec": prec,
            "freq": freq
        }))).unwrap());
        // string, frequency of updates 'F0' = realtime, 'F1' = 2 seconds, default is 'F0'
        if limit.clone().is_nonnullish() {
            request.set("len".into(), limit.clone());
        };
        // string, number of price points, '25', '100', default = '25'
        let mut orderbook: Value = <Self as Bitfinex>::subscribe(self, Value::from("book"), symbol.clone(), self.deep_extend_2(request.clone(), params.clone())).await;
        return orderbook.limit();
    }

    fn handle_order_book(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // first message (snapshot)
        //
        //     [
        //         18691, // channel id
        //         [
        //             [ 7364.8, 10, 4.354802 ], // price, count, size > 0 = bid
        //             [ 7364.7, 1, 0.00288831 ],
        //             [ 7364.3, 12, 0.048 ],
        //             [ 7364.9, 3, -0.42028976 ], // price, count, size < 0 = ask
        //             [ 7365, 1, -0.25 ],
        //             [ 7365.5, 1, -0.00371937 ],
        //         ]
        //     ]
        //
        // subsequent updates
        //
        //     [
        //         358169, // channel id
        //         [
        //            1807.1, // price
        //            0, // cound
        //            1 // size
        //         ]
        //     ]
        //
        let mut market_id: Value = self.safe_string(subscription.clone(), Value::from("symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut channel: Value = Value::from("book");
        let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        let mut prec: Value = self.safe_string(subscription.clone(), Value::from("prec"), Value::from("P0"));
        let mut is_raw: Value = (prec.clone() == Value::from("R0")).into();
        // if it is an initial snapshot
        if !self.get("orderbooks".into()).contains_key(symbol.clone()) {
            let mut limit: Value = self.safe_integer(subscription.clone(), Value::from("len"), Value::Undefined);
            if is_raw.is_truthy() {
                // raw order books
                self.get("orderbooks".into()).set(symbol.clone(), self.indexed_order_book(Value::new_object(), limit.clone()));
            } else {
                // P0, P1, P2, P3, P4
                self.get("orderbooks".into()).set(symbol.clone(), self.counted_order_book(Value::new_object(), limit.clone()));
            };
            let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
            if is_raw.is_truthy() {
                let mut deltas: Value = message.get(Value::from(1));
                let mut i: usize = 0;
                while i < deltas.len() {
                    let mut delta: Value = deltas.get(i.into());
                    let mut delta2: Value = delta.get(Value::from(2));
                    let mut size: Value = if delta2.clone() < Value::from(0) { delta2.clone().neg() } else { delta2.clone() };
                    let mut side: Value = if delta2.clone() < Value::from(0) { Value::from("asks") } else { Value::from("bids") };
                    let mut bookside: Value = orderbook.get(side.clone());
                    let mut id_string: Value = self.safe_string(delta.clone(), Value::from(0), Value::Undefined);
                    let mut price: Value = self.safe_float(delta.clone(), Value::from(1), Value::Undefined);
                    bookside.store_array(Value::Json(serde_json::Value::Array(vec![price.clone().into(), size.clone().into(), id_string.clone().into()])));
                    i += 1;
                };
            } else {
                let mut deltas: Value = message.get(Value::from(1));
                let mut i: usize = 0;
                while i < deltas.len() {
                    let mut delta: Value = deltas.get(i.into());
                    let mut amount: Value = self.safe_number(delta.clone(), Value::from(2), Value::Undefined);
                    let mut counter: Value = self.safe_number(delta.clone(), Value::from(1), Value::Undefined);
                    let mut price: Value = self.safe_number(delta.clone(), Value::from(0), Value::Undefined);
                    let mut size: Value = if amount.clone() < Value::from(0) { amount.clone().neg() } else { amount.clone() };
                    let mut side: Value = if amount.clone() < Value::from(0) { Value::from("asks") } else { Value::from("bids") };
                    let mut bookside: Value = orderbook.get(side.clone());
                    bookside.store_array(Value::Json(serde_json::Value::Array(vec![price.clone().into(), size.clone().into(), counter.clone().into()])));
                    i += 1;
                };
            };
            orderbook.set("symbol".into(), symbol.clone());
            client.resolve(orderbook.clone(), message_hash.clone());
        } else {
            let mut orderbook: Value = self.get("orderbooks".into()).get(symbol.clone());
            let mut deltas: Value = message.get(Value::from(1));
            let mut orderbook_item: Value = self.get("orderbooks".into()).get(symbol.clone());
            if is_raw.is_truthy() {
                let mut price: Value = self.safe_string(deltas.clone(), Value::from(1), Value::Undefined);
                let mut deltas2: Value = deltas.get(Value::from(2));
                let mut size: Value = if deltas2.clone() < Value::from(0) { deltas2.clone().neg() } else { deltas2.clone() };
                let mut side: Value = if deltas2.clone() < Value::from(0) { Value::from("asks") } else { Value::from("bids") };
                let mut bookside: Value = orderbook_item.get(side.clone());
                // price = 0 means that you have to remove the order from your book
                let mut amount: Value = if Precise::string_gt(price.clone(), Value::from("0")) { size.clone() } else { Value::from("0") };
                let mut id_string: Value = self.safe_string(deltas.clone(), Value::from(0), Value::Undefined);
                bookside.store_array(Value::Json(serde_json::Value::Array(vec![self.parse_number(price.clone(), Value::Undefined).into(), self.parse_number(amount.clone(), Value::Undefined).into(), id_string.clone().into()])));
            } else {
                let mut amount: Value = self.safe_string(deltas.clone(), Value::from(2), Value::Undefined);
                let mut counter: Value = self.safe_string(deltas.clone(), Value::from(1), Value::Undefined);
                let mut price: Value = self.safe_string(deltas.clone(), Value::from(0), Value::Undefined);
                let mut size: Value = if Precise::string_lt(amount.clone(), Value::from("0")) { Precise::string_neg(amount.clone()) } else { amount.clone() };
                let mut side: Value = if Precise::string_lt(amount.clone(), Value::from("0")) { Value::from("asks") } else { Value::from("bids") };
                let mut bookside: Value = orderbook_item.get(side.clone());
                bookside.store_array(Value::Json(serde_json::Value::Array(vec![self.parse_number(price.clone(), Value::Undefined).into(), self.parse_number(size.clone(), Value::Undefined).into(), self.parse_number(counter.clone(), Value::Undefined).into()])));
            };
            client.resolve(orderbook.clone(), message_hash.clone());
        };
        Value::Undefined
    }

    fn handle_checksum(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // [ 173904, "cs", -890884919 ]
        //
        let mut market_id: Value = self.safe_string(subscription.clone(), Value::from("symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut channel: Value = Value::from("book");
        let mut message_hash: Value = channel.clone() + Value::from(":") + market_id.clone();
        let mut book: Value = self.safe_value(self.get("orderbooks".into()), symbol.clone(), Value::Undefined);
        if book.clone().is_nullish() {
            return Value::Undefined;
        };
        let mut depth: usize = 25;
        // covers the first 25 bids and asks
        let mut string_array: Value = Value::new_array();
        let mut bids: Value = book.get(Value::from("bids"));
        let mut asks: Value = book.get(Value::from("asks"));
        let mut prec: Value = self.safe_string(subscription.clone(), Value::from("prec"), Value::from("P0"));
        let mut is_raw: Value = (prec.clone() == Value::from("R0")).into();
        let mut id_to_check: Value = if is_raw.is_truthy() { Value::from(2) } else { Value::from(0) };
        // pepperoni pizza from bitfinex
        let mut i: usize = 0;
        while i < depth {
            let mut bid: Value = self.safe_value(bids.clone(), Value::from(i), Value::Undefined);
            let mut ask: Value = self.safe_value(asks.clone(), Value::from(i), Value::Undefined);
            if bid.clone().is_nonnullish() {
                string_array.push(self.number_to_string(bids.get(i.into()).get(id_to_check.clone())));
                string_array.push(self.number_to_string(bids.get(i.into()).get(Value::from(1))));
            };
            if ask.clone().is_nonnullish() {
                string_array.push(self.number_to_string(asks.get(i.into()).get(id_to_check.clone())));
                let mut aski1: Value = asks.get(i.into()).get(Value::from(1));
                string_array.push(self.number_to_string(aski1.clone().neg()));
            };
            i += 1;
        };
        let mut payload: Value = string_array.join(Value::from(":"));
        let mut local_checksum: Value = self.crc32(payload.clone(), true.into());
        let mut response_checksum: Value = self.safe_integer(message.clone(), Value::from(2), Value::Undefined);
        if response_checksum.clone() != local_checksum.clone() {
            client.get(subscriptions.clone()).get(message_hash.clone());
            self.get("orderbooks".into()).get(symbol.clone());
            let mut checksum: Value = self.handle_option(Value::from("watchOrderBook"), Value::from("checksum"), true.into());
            if checksum.is_truthy() {
                let mut error: Value = ChecksumError::new(self.get("id".into()) + Value::from(" ") + self.orderbook_checksum_message(symbol.clone()));
                client.reject(error.clone(), message_hash.clone());
            };
        };
        Value::Undefined
    }

    async fn watch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut balance_type: Value = self.safe_string(params.clone(), Value::from("wallet"), Value::from("exchange"));
        // exchange, margin
        params = self.omit(params.clone(), Value::from("wallet"));
        let mut message_hash: Value = Value::from("balance:") + balance_type.clone();
        return <Self as Bitfinex>::subscribe_private(self, message_hash.clone()).await;
    }

    fn handle_balance(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // snapshot (exchange + margin together)
        //   [
        //       0,
        //       "ws",
        //       [
        //           [
        //               "exchange",
        //               "LTC",
        //               0.05479727,
        //               0,
        //               null,
        //               "Trading fees for 0.05 LTC (LTCUST) @ 51.872 on BFX (0.2%)",
        //               null,
        //           ]
        //           [
        //               "margin",
        //               "USTF0",
        //               11.960650700086292,
        //               0,
        //               null,
        //               "Trading fees for 0.1 LTCF0 (LTCF0:USTF0) @ 51.844 on BFX (0.065%)",
        //               null,
        //           ],
        //       ],
        //   ]
        //
        // spot
        //   [
        //       0,
        //       "wu",
        //       [
        //         "exchange",
        //         "LTC", // currency
        //         0.06729727, // wallet balance
        //         0, // unsettled balance
        //         0.06729727, // available balance might be null
        //         "Exchange 0.4 LTC for UST @ 65.075",
        //         {
        //           "reason": "TRADE",
        //           "order_id": 96596397973,
        //           "order_id_oppo": 96596632735,
        //           "trade_price": "65.075",
        //           "trade_amount": "-0.4",
        //           "order_cid": 1654636218766,
        //           "order_gid": null
        //         }
        //       ]
        //   ]
        //
        // margin
        //
        //   [
        //       "margin",
        //       "USTF0",
        //       11.960650700086292, // total
        //       0,
        //       6.776250700086292, // available
        //       "Trading fees for 0.1 LTCF0 (LTCF0:USTF0) @ 51.844 on BFX (0.065%)",
        //       null
        //   ]
        //
        let mut update_type: Value = self.safe_value(message.clone(), Value::from(1), Value::Undefined);
        let mut data: Value = Value::Undefined;
        if update_type.clone() == Value::from("ws") {
            data = self.safe_value(message.clone(), Value::from(2), Value::Undefined);
        } else {
            data = Value::Json(serde_json::Value::Array(vec![self.safe_value(message.clone(), Value::from(2), Value::Undefined).into()]));
        };
        let mut updated_types: Value = Value::new_object();
        let mut i: usize = 0;
        while i < data.len() {
            let mut raw_balance: Value = data.get(i.into());
            let mut currency_id: Value = self.safe_string(raw_balance.clone(), Value::from(1), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut balance: Value = <Self as Bitfinex>::parse_ws_balance(self, raw_balance.clone());
            let mut balance_type: Value = self.safe_string(raw_balance.clone(), Value::from(0), Value::Undefined);
            let mut old_balance: Value = self.safe_value(self.get("balance".into()), balance_type.clone(), Value::new_object());
            old_balance.set(code.clone(), balance.clone());
            old_balance.set("info".into(), message.clone());
            self.get("balance".into()).set(balance_type.clone(), self.safe_balance(old_balance.clone()));
            updated_types.set(balance_type.clone(), true.into());
            i += 1;
        };
        let mut updates_keys: Value = updated_types.clone().keys();
        let mut i: usize = 0;
        while i < updates_keys.len() {
            let mut r#type: Value = updates_keys.get(i.into());
            let mut message_hash: Value = Value::from("balance:") + r#type.clone();
            client.resolve(self.get("balance".into()).get(r#type.clone()), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn parse_ws_balance(&self, mut balance: Value) -> Value {
        //
        //     [
        //         "exchange",
        //         "LTC",
        //         0.05479727, // balance
        //         0,
        //         null, // available null if not calculated yet
        //         "Trading fees for 0.05 LTC (LTCUST) @ 51.872 on BFX (0.2%)",
        //         null,
        //     ]
        //
        let mut total_balance: Value = self.safe_string(balance.clone(), Value::from(2), Value::Undefined);
        let mut available_balance: Value = self.safe_string(balance.clone(), Value::from(4), Value::Undefined);
        let mut account: Value = self.account();
        if available_balance.clone().is_nonnullish() {
            account.set("free".into(), available_balance.clone());
        };
        account.set("total".into(), total_balance.clone());
        return account.clone();
    }

    fn handle_system_status(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "event": "info",
        //         "version": 2,
        //         "serverId": "e293377e-7bb7-427e-b28c-5db045b2c1d1",
        //         "platform": { status: 1 }, // 1 for operative, 0 for maintenance
        //     }
        //
        return message.clone();
    }

    fn handle_unsubscription_status(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        // {
        //     "event": "unsubscribed",
        //     "status": "OK",
        //     "chanId": CHANNEL_ID
        // }
        //
        let mut channel_id: Value = self.safe_string(message.clone(), Value::from("chanId"), Value::Undefined);
        let mut un_sub_channel: Value = Value::from("unsubscribe:") + channel_id.clone();
        let mut sub_message_hash: Value = self.safe_string(client.get(subscriptions.clone()), un_sub_channel.clone(), Value::Undefined);
        let mut subscription: Value = self.safe_dict(client.get(subscriptions.clone()), Value::from("unsubscribe:") + sub_message_hash.clone(), Value::Undefined);
        client.get(subscriptions.clone()).get(un_sub_channel.clone());
        let mut message_hashes: Value = self.safe_list(subscription.clone(), Value::from("messageHashes"), Value::new_array());
        let mut sub_message_hashes: Value = self.safe_list(subscription.clone(), Value::from("subMessageHashes"), Value::new_array());
        let mut i: usize = 0;
        while i < message_hashes.len() {
            let mut message_hash: Value = message_hashes.get(i.into());
            let mut sub_hash: Value = sub_message_hashes.get(i.into());
            self.clean_unsubscription(client.clone(), sub_hash.clone(), message_hash.clone());
            i += 1;
        };
        self.clean_cache(subscription.clone());
        return true.into();
    }

    fn handle_subscription_status(&mut self, mut client: Value, mut message: Value) -> Value {
        //
        //     {
        //         "event": "subscribed",
        //         "channel": "book",
        //         "chanId": 67473,
        //         "symbol": "tBTCUSD",
        //         "prec": "P0",
        //         "freq": "F0",
        //         "len": "25",
        //         "pair": "BTCUSD"
        //     }
        //
        //   {
        //       event: 'subscribed',
        //       channel: 'candles',
        //       chanId: 128306,
        //       key: 'trade:1m:tBTCUST'
        //  }
        //
        let mut channel_id: Value = self.safe_string(message.clone(), Value::from("chanId"), Value::Undefined);
        client.get(subscriptions.clone()).set(channel_id.clone(), message.clone());
        // store the opposite direction too for unWatch
        let mut mappings: Value = Value::Json(normalize(&Value::Json(json!({
            "book": "orderbook",
            "candles": "ohlcv",
            "ticker": "ticker",
            "trades": "trades"
        }))).unwrap());
        let mut unified_channel: Value = self.safe_string(mappings.clone(), self.safe_string(message.clone(), Value::from("channel"), Value::Undefined), Value::Undefined);
        if message.contains_key(Value::from("key")) {
            // handle ohlcv differently because the message is different
            let mut key: Value = self.safe_string(message.clone(), Value::from("key"), Value::Undefined);
            let mut sub_key_id: Value = Value::from("unsubscribe:") + key.clone();
            client.get(subscriptions.clone()).set(sub_key_id.clone(), channel_id.clone());
        } else {
            let mut market_id: Value = self.safe_string(message.clone(), Value::from("symbol"), Value::Undefined);
            let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            if unified_channel.clone().is_nonnullish() {
                let mut sub_id: Value = Value::from("unsubscribe:") + unified_channel.clone() + Value::from(":") + symbol.clone();
                client.get(subscriptions.clone()).set(sub_id.clone(), channel_id.clone());
            };
        };
        return message.clone();
    }

    async fn authenticate(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("ws")).get(Value::from("private"));
        let mut client: Value = self.client(url.clone());
        let mut message_hash: Value = Value::from("authenticated");
        let mut future: Value = client.reusable_future(message_hash.clone());
        let mut authenticated: Value = self.safe_value(client.get(subscriptions.clone()), message_hash.clone(), Value::Undefined);
        if authenticated.clone().is_nullish() {
            let mut nonce: Value = self.milliseconds();
            let mut payload: Value = Value::from("AUTH") + nonce.to_string();
            let mut signature: Value = self.hmac(self.encode(payload.clone()), self.encode(self.get("secret".into())), sha384().clone(), Value::from("hex"));
            let mut event: Value = Value::from("auth");
            let mut request: Value = Value::Json(normalize(&Value::Json(json!({
                "apiKey": self.get("apiKey".into()),
                "authSig": signature,
                "authNonce": nonce,
                "authPayload": payload,
                "event": event
            }))).unwrap());
            let mut message: Value = extend_2(request.clone(), params.clone());
            self.watch(url.clone(), message_hash.clone(), message.clone(), message_hash.clone());
        };
        return future.clone();
    }

    fn handle_authentication_message(&mut self, mut client: Value, mut message: Value) -> Value {
        let mut message_hash: Value = Value::from("authenticated");
        let mut status: Value = self.safe_string(message.clone(), Value::from("status"), Value::Undefined);
        if status.clone() == Value::from("OK") {
            // we resolve the future here permanently so authentication only happens once
            let mut future: Value = self.safe_value(client.get(futures.clone()), message_hash.clone(), Value::Undefined);
            future.resolve(true.into());
        } else {
            let mut error: Value = AuthenticationError::new(self.json(message.clone()));
            client.reject(error.clone(), message_hash.clone());
            // allows further authentication attempts
            if client.get(subscriptions.clone()).contains_key(message_hash.clone()) {
                client.get(subscriptions.clone()).get(message_hash.clone());
            };
        };
        Value::Undefined
    }

    async fn watch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut message_hash: Value = Value::from("orders");
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            message_hash = message_hash +  Value::from(":") + market.get(Value::from("id"));
        };
        let mut orders: Value = <Self as Bitfinex>::subscribe_private(self, message_hash.clone()).await;
        if self.get("newUpdates".into()).is_truthy() {
            limit = orders.get_limit(symbol.clone(), limit.clone());
        };
        return self.filter_by_symbol_since_limit(orders.clone(), symbol.clone(), since.clone(), limit.clone(), true.into());
    }

    fn handle_orders(&mut self, mut client: Value, mut message: Value, mut subscription: Value) -> Value {
        //
        // limit order
        //    [
        //        0,
        //        "on", // ou or oc
        //        [
        //           96923856256, // order id
        //           null, // gid
        //           1655029337026, // cid
        //           "tLTCUST", // symbol
        //           1655029337027, // created timestamp
        //           1655029337029, // updated timestamp
        //           0.1, // amount
        //           0.1, // amount_orig
        //           "EXCHANGE LIMIT", // order type
        //           null, // type_prev
        //           null, // mts_tif
        //           null, // placeholder
        //           0, // flags
        //           "ACTIVE", // status
        //           null,
        //           null,
        //           30, // price
        //           0, // price average
        //           0, // price_trailling
        //           0, // price_aux_limit
        //           null,
        //           null,
        //           null,
        //           0, // notify
        //           0,
        //           null,
        //           null,
        //           null,
        //           "BFX",
        //           null,
        //           null,
        //        ]
        //    ]
        //
        let mut data: Value = self.safe_value(message.clone(), Value::from(2), Value::new_array());
        let mut message_type: Value = self.safe_string(message.clone(), Value::from(1), Value::Undefined);
        if self.get("orders".into()).is_nullish() {
            let mut limit: Value = self.safe_integer(self.get("options".into()), Value::from("ordersLimit"), Value::from(1000));
            self.set("orders".into(), ArrayCacheBySymbolById::new(limit));
        };
        let mut orders: Value = self.get("orders".into());
        let mut symbol_ids: Value = Value::new_object();
        if message_type.clone() == Value::from("os") {
            let mut snapshot_length: usize = data.len();
            if snapshot_length.clone() == Value::from(0) {
                return Value::Undefined;
            };
            let mut i: usize = 0;
            while i < data.len() {
                let mut value: Value = data.get(i.into());
                let mut parsed: Value = <Self as Bitfinex>::parse_ws_order(self, value.clone(), Value::Undefined);
                let mut symbol: Value = parsed.get(Value::from("symbol"));
                symbol_ids.set(symbol.clone(), true.into());
                orders.append(parsed.clone());
                i += 1;
            };
        } else {
            let mut parsed: Value = <Self as Bitfinex>::parse_ws_order(self, data.clone(), Value::Undefined);
            orders.append(parsed.clone());
            let mut symbol: Value = parsed.get(Value::from("symbol"));
            symbol_ids.set(symbol.clone(), true.into());
        };
        let mut name: Value = Value::from("orders");
        client.resolve(self.get("orders".into()), name.clone());
        let mut keys: Value = symbol_ids.clone().keys();
        let mut i: usize = 0;
        while i < keys.len() {
            let mut symbol: Value = keys.get(i.into());
            let mut market: Value = self.market(symbol.clone());
            let mut message_hash: Value = name.clone() + Value::from(":") + market.get(Value::from("id"));
            client.resolve(self.get("orders".into()), message_hash.clone());
            i += 1;
        };
        Value::Undefined
    }

    fn parse_ws_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "ACTIVE": "open",
            "CANCELED": "canceled",
            "EXECUTED": "closed",
            "PARTIALLY": "open"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_ws_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        //   [
        //       97084883506, // order id
        //       null,
        //       1655110144596, // clientOrderId
        //       "tLTCUST", // symbol
        //       1655110144596, // created timestamp
        //       1655110144598, // updated timestamp
        //       0, // amount
        //       0.1, // amount_orig negative if sell order
        //       "EXCHANGE MARKET", // type
        //       null,
        //       null,
        //       null,
        //       0,
        //       "EXECUTED @ 42.821(0.1)", // status
        //       null,
        //       null,
        //       42.799, // price
        //       42.821, // price average
        //       0, // price trailling
        //       0, // price_aux_limit
        //       null,
        //       null,
        //       null,
        //       0,
        //       0,
        //       null,
        //       null,
        //       null,
        //       "BFX",
        //       null,
        //       null,
        //       {}
        //   ]
        //
        let mut id: Value = self.safe_string(order.clone(), Value::from(0), Value::Undefined);
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from(1), Value::Undefined);
        let mut market_id: Value = self.safe_string(order.clone(), Value::from(3), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        market = self.safe_market(symbol.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut amount: Value = self.safe_string(order.clone(), Value::from(7), Value::Undefined);
        let mut side: Value = Value::from("buy");
        if Precise::string_lt(amount.clone(), Value::from("0")) {
            amount = Precise::string_abs(amount.clone());
            side = Value::from("sell");
        };
        let mut remaining: Value = Precise::string_abs(self.safe_string(order.clone(), Value::from(6), Value::Undefined));
        let mut r#type: Value = self.safe_string(order.clone(), Value::from(8), Value::Undefined);
        if r#type.index_of(Value::from("LIMIT")) > Value::from(1).neg() {
            r#type = Value::from("limit");
        } else if r#type.index_of(Value::from("MARKET")) > Value::from(1).neg() {
            r#type = Value::from("market");
        };
        let mut raw_state: Value = self.safe_string(order.clone(), Value::from(13), Value::Undefined);
        let mut state_parts: Value = raw_state.split(Value::from(" "));
        let mut trimmed_status: Value = self.safe_string(state_parts.clone(), Value::from(0), Value::Undefined);
        let mut status: Value = <Self as Bitfinex>::parse_ws_order_status(self, trimmed_status.clone());
        let mut price: Value = self.safe_string(order.clone(), Value::from(16), Value::Undefined);
        let mut timestamp: Value = self.safe_integer_2(order.clone(), Value::from(5), Value::from(4), Value::Undefined);
        let mut average: Value = self.safe_string(order.clone(), Value::from(17), Value::Undefined);
        let mut stop_price: Value = self.omit_zero(self.safe_string(order.clone(), Value::from(18), Value::Undefined));
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "id": id,
            "clientOrderId": client_order_id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": Value::Undefined,
            "symbol": symbol,
            "type": r#type,
            "side": side,
            "price": price,
            "stopPrice": stop_price,
            "triggerPrice": stop_price,
            "average": average,
            "amount": amount,
            "remaining": remaining,
            "filled": Value::Undefined,
            "status": status,
            "fee": Value::Undefined,
            "cost": Value::Undefined,
            "trades": Value::Undefined
        }))).unwrap()), market.clone());
    }

    fn handle_message(&mut self, mut client: Value, mut message: Value) -> Value {
        let mut channel_id: Value = self.safe_string(message.clone(), Value::from(0), Value::Undefined);
        //
        //     [
        //         1231,
        //         "hb",
        //     ]
        //
        // auth message
        //    {
        //        "event": "auth",
        //        "status": "OK",
        //        "chanId": 0,
        //        "userId": 3159883,
        //        "auth_id": "ac7108e7-2f26-424d-9982-c24700dc02ca",
        //        "caps": {
        //          "orders": { read: 1, write: 1 },
        //          "account": { read: 1, write: 1 },
        //          "funding": { read: 1, write: 1 },
        //          "history": { read: 1, write: 0 },
        //          "wallets": { read: 1, write: 1 },
        //          "withdraw": { read: 0, write: 1 },
        //          "positions": { read: 1, write: 1 },
        //          "ui_withdraw": { read: 0, write: 0 }
        //        }
        //    }
        //
        if Array::is_array(message.clone()).is_truthy() {
            if message.get(Value::from(1)) == Value::from("hb") {
                return Value::Undefined;
            };
            // skip heartbeats within subscription channels for now
            let mut subscription: Value = self.safe_value(client.get(subscriptions.clone()), channel_id.clone(), Value::new_object());
            let mut channel: Value = self.safe_string(subscription.clone(), Value::from("channel"), Value::Undefined);
            let mut name: Value = self.safe_string(message.clone(), Value::from(1), Value::Undefined);
            let mut public_methods: Value = Value::Json(normalize(&Value::Json(json!({
                "book": self.get("handleOrderBook".into()),
                "cs": self.get("handleChecksum".into()),
                "candles": self.get("handleOHLCV".into()),
                "ticker": self.get("handleTicker".into()),
                "trades": self.get("handleTrades".into())
            }))).unwrap());
            let mut private_methods: Value = Value::Json(normalize(&Value::Json(json!({
                "os": self.get("handleOrders".into()),
                "ou": self.get("handleOrders".into()),
                "on": self.get("handleOrders".into()),
                "oc": self.get("handleOrders".into()),
                "wu": self.get("handleBalance".into()),
                "ws": self.get("handleBalance".into()),
                "tu": self.get("handleMyTrade".into())
            }))).unwrap());
            let mut method: Value = Value::Undefined;
            if channel_id.clone() == Value::from("0") {
                method = self.safe_value(private_methods.clone(), name.clone(), Value::Undefined);
            } else {
                method = self.safe_value_2(public_methods.clone(), name.clone(), channel.clone(), Value::Undefined);
            };
            if method.clone().is_nonnullish() {
                method.call(self, client.clone(), message.clone(), subscription.clone());
            };
        } else {
            let mut event: Value = self.safe_string(message.clone(), Value::from("event"), Value::Undefined);
            if event.clone().is_nonnullish() {
                let mut methods: Value = Value::Json(normalize(&Value::Json(json!({
                    "info": self.get("handleSystemStatus".into()),
                    "subscribed": self.get("handleSubscriptionStatus".into()),
                    "unsubscribed": self.get("handleUnsubscriptionStatus".into()),
                    "auth": self.get("handleAuthenticationMessage".into())
                }))).unwrap());
                let mut method: Value = self.safe_value(methods.clone(), event.clone(), Value::Undefined);
                if method.clone().is_nonnullish() {
                    method.call(self, client.clone(), message.clone());
                };
            };
        };
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetConfconfig" => self.request("conf/{config}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubactionobject" => self.request("conf/pub:{action}:{object}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubactionobjectdetail" => self.request("conf/pub:{action}:{object}:{detail}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapobject" => self.request("conf/pub:map:{object}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapobjectdetail" => self.request("conf/pub:map:{object}:{detail}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencydetail" => self.request("conf/pub:map:currency:{detail}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencysym" => self.request("conf/pub:map:currency:sym".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencylabel" => self.request("conf/pub:map:currency:label".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencyunit" => self.request("conf/pub:map:currency:unit".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencyundl" => self.request("conf/pub:map:currency:undl".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencypool" => self.request("conf/pub:map:currency:pool".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencyexplorer" => self.request("conf/pub:map:currency:explorer".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmapcurrencytxfee" => self.request("conf/pub:map:currency:tx:fee".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubmaptxmethod" => self.request("conf/pub:map:tx:method".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpublistobject" => self.request("conf/pub:list:{object}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpublistobjectdetail" => self.request("conf/pub:list:{object}:{detail}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpublistcurrency" => self.request("conf/pub:list:currency".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpublistpairexchange" => self.request("conf/pub:list:pair:exchange".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpublistpairmargin" => self.request("conf/pub:list:pair:margin".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpublistpairfutures" => self.request("conf/pub:list:pair:futures".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpublistcompetitions" => self.request("conf/pub:list:competitions".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubinfoobject" => self.request("conf/pub:info:{object}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubinfoobjectdetail" => self.request("conf/pub:info:{object}:{detail}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubinfopair" => self.request("conf/pub:info:pair".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubinfopairfutures" => self.request("conf/pub:info:pair:futures".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubinfotxstatus" => self.request("conf/pub:info:tx:status".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetConfpubfees" => self.request("conf/pub:fees".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetPlatformstatus" => self.request("platform/status".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTickers" => self.request("tickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTickersymbol" => self.request("ticker/{symbol}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTickershist" => self.request("tickers/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTradessymbolhist" => self.request("trades/{symbol}/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetBooksymbolprecision" => self.request("book/{symbol}/{precision}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetBooksymbolp0" => self.request("book/{symbol}/P0".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetBooksymbolp1" => self.request("book/{symbol}/P1".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetBooksymbolp2" => self.request("book/{symbol}/P2".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetBooksymbolp3" => self.request("book/{symbol}/P3".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetBooksymbolr0" => self.request("book/{symbol}/R0".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbolsidesection" => self.request("stats1/{key}:{size}:{symbol}:{side}/{section}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbolsidelast" => self.request("stats1/{key}:{size}:{symbol}:{side}/last".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbolsidehist" => self.request("stats1/{key}:{size}:{symbol}:{side}/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbolsection" => self.request("stats1/{key}:{size}:{symbol}/{section}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbollast" => self.request("stats1/{key}:{size}:{symbol}/last".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbolhist" => self.request("stats1/{key}:{size}:{symbol}/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbollonglast" => self.request("stats1/{key}:{size}:{symbol}:long/last".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbollonghist" => self.request("stats1/{key}:{size}:{symbol}:long/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbolshortlast" => self.request("stats1/{key}:{size}:{symbol}:short/last".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStats1keysizesymbolshorthist" => self.request("stats1/{key}:{size}:{symbol}:short/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCandlestradetimeframesymbolperiodsection" => self.request("candles/trade:{timeframe}:{symbol}:{period}/{section}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCandlestradetimeframesymbolsection" => self.request("candles/trade:{timeframe}:{symbol}/{section}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCandlestradetimeframesymbollast" => self.request("candles/trade:{timeframe}:{symbol}/last".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCandlestradetimeframesymbolhist" => self.request("candles/trade:{timeframe}:{symbol}/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStatustype" => self.request("status/{type}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStatusderiv" => self.request("status/deriv".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetStatusderivsymbolhist" => self.request("status/deriv/{symbol}/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetLiquidationshist" => self.request("liquidations/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetRankingskeytimeframesymbolsection" => self.request("rankings/{key}:{timeframe}:{symbol}/{section}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetRankingskeytimeframesymbolhist" => self.request("rankings/{key}:{timeframe}:{symbol}/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetPulsehist" => self.request("pulse/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetPulseprofilenickname" => self.request("pulse/profile/{nickname}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetFundingstatssymbolhist" => self.request("funding/stats/{symbol}/hist".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetExtvasps" => self.request("ext/vasps".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicPostCalctradeavg" => self.request("calc/trade/avg".into(), "public".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicPostCalcfx" => self.request("calc/fx".into(), "public".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrwallets" => self.request("auth/r/wallets".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrwalletshist" => self.request("auth/r/wallets/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrorders" => self.request("auth/r/orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrorderssymbol" => self.request("auth/r/orders/{symbol}".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwordersubmit" => self.request("auth/w/order/submit".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthworderupdate" => self.request("auth/w/order/update".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwordercancel" => self.request("auth/w/order/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwordermulti" => self.request("auth/w/order/multi".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwordercancelmulti" => self.request("auth/w/order/cancel/multi".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrorderssymbolhist" => self.request("auth/r/orders/{symbol}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrordershist" => self.request("auth/r/orders/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrordersymbolidtrades" => self.request("auth/r/order/{symbol}:{id}/trades".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrtradessymbolhist" => self.request("auth/r/trades/{symbol}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrtradeshist" => self.request("auth/r/trades/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrledgerscurrencyhist" => self.request("auth/r/ledgers/{currency}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrledgershist" => self.request("auth/r/ledgers/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrinfomarginkey" => self.request("auth/r/info/margin/{key}".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrinfomarginbase" => self.request("auth/r/info/margin/base".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrinfomarginsymall" => self.request("auth/r/info/margin/sym_all".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrpositions" => self.request("auth/r/positions".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwpositionclaim" => self.request("auth/w/position/claim".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwpositionincrease" => self.request("auth/w/position/increase:".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrpositionincreaseinfo" => self.request("auth/r/position/increase/info".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrpositionshist" => self.request("auth/r/positions/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrpositionsaudit" => self.request("auth/r/positions/audit".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrpositionssnap" => self.request("auth/r/positions/snap".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwderivcollateralset" => self.request("auth/w/deriv/collateral/set".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwderivcollaterallimits" => self.request("auth/w/deriv/collateral/limits".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingoffers" => self.request("auth/r/funding/offers".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingofferssymbol" => self.request("auth/r/funding/offers/{symbol}".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwfundingoffersubmit" => self.request("auth/w/funding/offer/submit".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwfundingoffercancel" => self.request("auth/w/funding/offer/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwfundingoffercancelall" => self.request("auth/w/funding/offer/cancel/all".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwfundingclose" => self.request("auth/w/funding/close".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwfundingauto" => self.request("auth/w/funding/auto".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwfundingkeep" => self.request("auth/w/funding/keep".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingofferssymbolhist" => self.request("auth/r/funding/offers/{symbol}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingoffershist" => self.request("auth/r/funding/offers/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingloans" => self.request("auth/r/funding/loans".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingloanshist" => self.request("auth/r/funding/loans/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingloanssymbol" => self.request("auth/r/funding/loans/{symbol}".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingloanssymbolhist" => self.request("auth/r/funding/loans/{symbol}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingcredits" => self.request("auth/r/funding/credits".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingcreditshist" => self.request("auth/r/funding/credits/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingcreditssymbol" => self.request("auth/r/funding/credits/{symbol}".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingcreditssymbolhist" => self.request("auth/r/funding/credits/{symbol}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingtradessymbolhist" => self.request("auth/r/funding/trades/{symbol}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrfundingtradeshist" => self.request("auth/r/funding/trades/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrinfofundingkey" => self.request("auth/r/info/funding/{key}".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrinfouser" => self.request("auth/r/info/user".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrsummary" => self.request("auth/r/summary".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrloginshist" => self.request("auth/r/logins/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrpermissions" => self.request("auth/r/permissions".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwtoken" => self.request("auth/w/token".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthraudithist" => self.request("auth/r/audit/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwtransfer" => self.request("auth/w/transfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwdepositaddress" => self.request("auth/w/deposit/address".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwdepositinvoice" => self.request("auth/w/deposit/invoice".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwwithdraw" => self.request("auth/w/withdraw".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrmovementscurrencyhist" => self.request("auth/r/movements/{currency}/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrmovementshist" => self.request("auth/r/movements/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthralerts" => self.request("auth/r/alerts".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwalertset" => self.request("auth/w/alert/set".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwalertpricesymbolpricedel" => self.request("auth/w/alert/price:{symbol}:{price}/del".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwalerttypesymbolpricedel" => self.request("auth/w/alert/{type}:{symbol}:{price}/del".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthcalcorderavail" => self.request("auth/calc/order/avail".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwsettingsset" => self.request("auth/w/settings/set".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrsettings" => self.request("auth/r/settings".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwsettingsdel" => self.request("auth/w/settings/del".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthrpulsehist" => self.request("auth/r/pulse/hist".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwpulseadd" => self.request("auth/w/pulse/add".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAuthwpulsedel" => self.request("auth/w/pulse/del".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BitfinexImpl(Value);
impl Exchange for BitfinexImpl {}
impl Bitfinex for BitfinexImpl {}
impl ValueTrait for BitfinexImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl BitfinexImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = BitfinexImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Bitfinex::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


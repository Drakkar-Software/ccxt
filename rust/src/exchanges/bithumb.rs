#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256") }
fn sha384() -> Value { Value::from("sha384") }
fn sha512() -> Value { Value::from("sha512") }
fn md5() -> Value { Value::from("md5") }
fn ed25519() -> Value { Value::from("ed25519") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1") }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Bithumb : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "bithumb",
            "name": "Bithumb",
            "countries": [
                "KR"
            ],
            "enableRateLimit": true,
            "rateLimit": 500,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "CORS": true,
                "spot": true,
                "margin": false,
                "swap": false,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "borrowMargin": false,
                "cancelOrder": true,
                "closeAllPositions": false,
                "closePosition": false,
                "createLimitOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createOrder": true,
                "createOrderWithTakeProfitAndStopLoss": false,
                "createOrderWithTakeProfitAndStopLossWs": false,
                "createReduceOnlyOrder": false,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": false,
                "fetchCurrenciesWs": "emulated",
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchIsolatedPositions": false,
                "fetchL2OrderBook": true,
                "fetchLeverage": false,
                "fetchLeverages": false,
                "fetchLeverageTiers": false,
                "fetchLiquidations": false,
                "fetchLongShortRatio": false,
                "fetchLongShortRatioHistory": false,
                "fetchMarginMode": false,
                "fetchMarginModes": false,
                "fetchMarketLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyLiquidations": false,
                "fetchMySettlementHistory": false,
                "fetchOHLCV": true,
                "fetchOpenInterest": false,
                "fetchOpenInterests": false,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchPosition": false,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": false,
                "fetchPositionsForSymbol": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchSettlementHistory": false,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchMarkPrices": false,
                "fetchTrades": true,
                "fetchTransfer": false,
                "fetchTransfers": false,
                "fetchVolatilityHistory": false,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "setLeverage": false,
                "setMargin": false,
                "setMarginMode": false,
                "setPositionMode": false,
                "transfer": false,
                "withdraw": true
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/c9e0eefb-4777-46b9-8f09-9d7f7c4af82d",
                "api": {
                    "public": "https://api.{hostname}/public",
                    "private": "https://api.{hostname}"
                },
                "www": "https://www.bithumb.com",
                "doc": "https://apidocs.bithumb.com",
                "fees": "https://en.bithumb.com/customer_support/info_fee"
            },
            "api": {
                "public": {
                    "get": [
                        "ticker/ALL_{quoteId}",
                        "ticker/{baseId}_{quoteId}",
                        "orderbook/ALL_{quoteId}",
                        "orderbook/{baseId}_{quoteId}",
                        "transaction_history/{baseId}_{quoteId}",
                        "network-info",
                        "assetsstatus/multichain/ALL",
                        "assetsstatus/multichain/{currency}",
                        "withdraw/minimum/ALL",
                        "withdraw/minimum/{currency}",
                        "assetsstatus/ALL",
                        "assetsstatus/{baseId}",
                        "candlestick/{baseId}_{quoteId}/{interval}"
                    ]
                },
                "private": {
                    "post": [
                        "info/account",
                        "info/balance",
                        "info/wallet_address",
                        "info/ticker",
                        "info/orders",
                        "info/user_transactions",
                        "info/order_detail",
                        "trade/place",
                        "trade/cancel",
                        "trade/btc_withdrawal",
                        "trade/krw_deposit",
                        "trade/krw_withdrawal",
                        "trade/market_buy",
                        "trade/market_sell",
                        "trade/stop_limit"
                    ]
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "3m": "3m",
                "5m": "5m",
                "10m": "10m",
                "30m": "30m",
                "1h": "1h",
                "6h": "6h",
                "12h": "12h",
                "1d": "24h"
            },
            "fees": {
                "trading": {
                    "taker": 0.0025,
                    "maker": 0.0025
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {},
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "ALT": "ArchLoot",
                "FTC": "FTC2",
                "SOC": "Soda Coin"
            },
            "precisionMode": 3,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "hostname": "bithumb.com",
            "features": {
                "spot": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": false,
                            "FOK": false,
                            "PO": false,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyRequiresPrice": false,
                        "marketBuyByCost": false,
                        "selfTradePrevention": false,
                        "iceberg": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "swap": {},
                "future": {}
            },
            "options": {
                "quoteCurrencies": {
                    "BTC": {
                        "limits": {
                            "cost": {
                                "min": 0.0002,
                                "max": 100
                            }
                        }
                    },
                    "KRW": {
                        "limits": {
                            "cost": {
                                "min": 500,
                                "max": 5000000000
                            }
                        }
                    },
                    "USDT": {
                        "limits": {
                            "cost": {}
                        }
                    }
                }
            }
        }"###).unwrap())
    }

    fn safe_market(&self, mut market_id: Value, mut market: Value, mut delimiter: Value, mut market_type: Value) -> Value {
        // bithumb has a different type of conflict in markets, because
        // their ids are the base currency (BTC for instance), so we can have
        // multiple "BTC" ids representing the different markets (BTC/ETH, "BTC/DOGE", etc)
        // since they're the same we just need to return one
        return Exchange::safe_market(self, market_id.clone(), market.clone(), delimiter.clone(), Value::from("spot"));
    }

    fn amount_to_precision(&mut self, mut symbol: Value, mut amount: Value) -> Value {
        return self.decimal_to_precision(amount.clone(), TRUNCATE.into(), self.get("markets".into()).get(symbol.clone()).get(Value::from("precision")).get(Value::from("amount")), DECIMAL_PLACES.into(), Value::Undefined);
    }

    

    fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response
        }))).unwrap());
        let mut balances: Value = self.safe_dict(response.clone(), Value::from("data"), Value::Undefined);
        let mut codes: Value = self.get("currencies".into()).keys();
        let mut i: usize = 0;
        while i < codes.len() {
            let mut code: Value = codes.get(i.into());
            let mut account: Value = self.account();
            let mut currency: Value = self.currency(code.clone());
            let mut lower_currency_id: Value = self.safe_string_lower(currency.clone(), Value::from("id"), Value::Undefined);
            account.set("total".into(), self.safe_string(balances.clone(), Value::from("total_") + lower_currency_id.clone(), Value::Undefined));
            account.set("used".into(), self.safe_string(balances.clone(), Value::from("in_use_") + lower_currency_id.clone(), Value::Undefined));
            account.set("free".into(), self.safe_string(balances.clone(), Value::from("available_") + lower_currency_id.clone(), Value::Undefined));
            result.set(code.clone(), account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "currency": "ALL"
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostInfoBalance".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return <Self as Bithumb>::parse_balance(self, response.clone());
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bithumb>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = <Self as Bithumb>::request(self,path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Bithumb>::request(self,path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        // fetchTicker, fetchTickers
        //
        //     {
        //         "opening_price":"227100",
        //         "closing_price":"228400",
        //         "min_price":"222300",
        //         "max_price":"230000",
        //         "units_traded":"82618.56075337",
        //         "acc_trade_value":"18767376138.6031",
        //         "prev_closing_price":"227100",
        //         "units_traded_24H":"151871.13484676",
        //         "acc_trade_value_24H":"34247610416.8974",
        //         "fluctate_24H":"8700",
        //         "fluctate_rate_24H":"3.96",
        //         "date":"1587710327264", // fetchTickers inject this
        //     }
        //
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("date"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined);
        let mut open: Value = self.safe_string(ticker.clone(), Value::from("opening_price"), Value::Undefined);
        let mut close: Value = self.safe_string(ticker.clone(), Value::from("closing_price"), Value::Undefined);
        let mut base_volume: Value = self.safe_string(ticker.clone(), Value::from("units_traded_24H"), Value::Undefined);
        let mut quote_volume: Value = self.safe_string(ticker.clone(), Value::from("acc_trade_value_24H"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "high": self.safe_string(ticker.clone(), Value::from("max_price"), Value::Undefined),
            "low": self.safe_string(ticker.clone(), Value::from("min_price"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("buy_price"), Value::Undefined),
            "bidVolume": Value::Undefined,
            "ask": self.safe_string(ticker.clone(), Value::from("sell_price"), Value::Undefined),
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": open,
            "close": close,
            "last": close,
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": base_volume,
            "quoteVolume": quote_volume,
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bithumb>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = <Self as Bithumb>::request(self,path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Bithumb>::request(self,path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bithumb>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = <Self as Bithumb>::request(self,path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Bithumb>::request(self,path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        //     [
        //         1576823400000, // 기준 시간
        //         "8284000", // 시가
        //         "8286000", // 종가
        //         "8289000", // 고가
        //         "8276000", // 저가
        //         "15.41503692" // 거래량
        //     ]
        //
        return Value::Json(serde_json::Value::Array(vec![self.safe_integer(ohlcv.clone(), Value::from(0), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(1), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(3), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(4), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(2), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(5), Value::Undefined).into()]));
    }

    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bithumb>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["klines", "candles", "ohlcv"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = <Self as Bithumb>::request(self,path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Bithumb>::request(self,path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_trade(&mut self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchTrades (public)
        //
        //     {
        //         "transaction_date":"2020-04-23 22:21:46",
        //         "type":"ask",
        //         "units_traded":"0.0125",
        //         "price":"8667000",
        //         "total":"108337"
        //     }
        //
        // fetchOrder (private)
        //
        //     {
        //         "transaction_date": "1572497603902030",
        //         "price": "8601000",
        //         "units": "0.005",
        //         "fee_currency": "KRW",
        //         "fee": "107.51",
        //         "total": "43005"
        //     }
        //
        // a workaround for their bug in date format, hours are not 0-padded
        let mut timestamp: Value = Value::Undefined;
        let mut transaction_datetime: Value = self.safe_string(trade.clone(), Value::from("transaction_date"), Value::Undefined);
        if transaction_datetime.clone().is_nonnullish() {
            let mut parts: Value = transaction_datetime.split(Value::from(" "));
            let mut num_parts: usize = parts.len();
            if num_parts.clone() > Value::from(1) {
                let mut transaction_date: Value = parts.get(Value::from(0));
                let mut transaction_time: Value = parts.get(Value::from(1));
                if transaction_time.len() < 8 {
                    transaction_time = Value::from("0") + transaction_time.clone();
                };
                timestamp = self.parse8601(transaction_date.clone() + Value::from(" ") + transaction_time.clone());
            } else {
                timestamp = self.safe_integer_product(trade.clone(), Value::from("transaction_date"), Value::from(0.001));
            };
        };
        if timestamp.clone().is_nonnullish() {
            timestamp = timestamp -  Value::from(9) * Value::from(3600000);
        };
        // they report UTC + 9 hours, server in Korean timezone
        let mut r#type: Value = Value::Undefined;
        let mut side: Value = self.safe_string(trade.clone(), Value::from("type"), Value::Undefined);
        side = if side.clone() == Value::from("ask") { Value::from("sell") } else { Value::from("buy") };
        let mut id: Value = self.safe_string(trade.clone(), Value::from("cont_no"), Value::Undefined);
        market = <Self as Bithumb>::safe_market(self, Value::Undefined, market.clone(), Value::Undefined, Value::Undefined);
        let mut price_string: Value = self.safe_string(trade.clone(), Value::from("price"), Value::Undefined);
        let mut amount_string: Value = <Self as Bithumb>::fix_comma_number(self, self.safe_string_2(trade.clone(), Value::from("units_traded"), Value::from("units"), Value::Undefined));
        let mut cost_string: Value = self.safe_string(trade.clone(), Value::from("total"), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        let mut fee_cost_string: Value = self.safe_string(trade.clone(), Value::from("fee"), Value::Undefined);
        if fee_cost_string.clone().is_nonnullish() {
            let mut fee_currency_id: Value = self.safe_string(trade.clone(), Value::from("fee_currency"), Value::Undefined);
            let mut fee_currency_code: Value = self.common_currency_code(fee_currency_id.clone());
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_cost_string,
                "currency": fee_currency_code
            }))).unwrap());
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": id,
            "info": trade,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": market.get(Value::from("symbol")),
            "order": Value::Undefined,
            "type": r#type,
            "side": side,
            "takerOrMaker": Value::Undefined,
            "price": price_string,
            "amount": amount_string,
            "cost": cost_string,
            "fee": fee
        }))).unwrap()), market.clone());
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Bithumb>::request(self,path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_currency": market.get(Value::from("id")),
            "payment_currency": market.get(Value::from("quote")),
            "units": amount
        }))).unwrap());
        let mut method: Value = Value::from("privatePostTradePlace");
        if r#type.clone() == Value::from("limit") {
            request.set("price".into(), price.clone());
            request.set("type".into(), if side.clone() == Value::from("buy") { Value::from("bid") } else { Value::from("ask") });
        } else {
            method = Value::from("privatePostTradeMarket") + self.capitalize(side.clone());
        };
        let mut response: Value = self.dispatch(method, extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut id: Value = self.safe_string(response.clone(), Value::from("order_id"), Value::Undefined);
        if id.clone().is_nullish() {
            panic!(r###"InvalidOrder::new(self.get("id".into()) + Value::from(" createOrder() did not return an order id"))"###);
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": response,
            "symbol": symbol,
            "type": r#type,
            "side": side,
            "id": id
        }))).unwrap()), market.clone());
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" fetchOrder() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id,
            "count": 1,
            "order_currency": market.get(Value::from("base")),
            "payment_currency": market.get(Value::from("quote"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostInfoOrderDetail".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "status": "0000",
        //         "data": {
        //             "order_date": "1603161798539254",
        //             "type": "ask",
        //             "order_status": "Cancel",
        //             "order_currency": "BTC",
        //             "payment_currency": "KRW",
        //             "watch_price": "0",
        //             "order_price": "13344000",
        //             "order_qty": "0.0125",
        //             "cancel_date": "1603161803809993",
        //             "cancel_type": "사용자취소",
        //             "contract": [
        //                 {
        //                     "transaction_date": "1603161799976383",
        //                     "price": "13344000",
        //                     "units": "0.0015",
        //                     "fee_currency": "KRW",
        //                     "fee": "0",
        //                     "total": "20016"
        //                 }
        //             ],
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::Undefined);
        return <Self as Bithumb>::parse_order(self, extend_2(data.clone(), Value::Json(normalize(&Value::Json(json!({
            "order_id": id
        }))).unwrap())), market.clone());
    }

    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "Pending": "open",
            "Completed": "closed",
            "Cancel": "canceled"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_order(&mut self, mut order: Value, mut market: Value) -> Value {
        //
        //
        // fetchOrder
        //
        //     {
        //         "transaction_date": "1572497603668315",
        //         "type": "bid",
        //         "order_status": "Completed", // Completed, Cancel ...
        //         "order_currency": "BTC",
        //         "payment_currency": "KRW",
        //         "watch_price": "0", // present in Cancel order
        //         "order_price": "8601000",
        //         "order_qty": "0.007",
        //         "cancel_date": "", // filled in Cancel order
        //         "cancel_type": "", // filled in Cancel order, i.e. 사용자취소
        //         "contract": [
        //             {
        //                 "transaction_date": "1572497603902030",
        //                 "price": "8601000",
        //                 "units": "0.005",
        //                 "fee_currency": "KRW",
        //                 "fee": "107.51",
        //                 "total": "43005"
        //             },
        //         ]
        //     }
        //
        // fetchOpenOrders
        //
        //     {
        //         "order_currency": "BTC",
        //         "payment_currency": "KRW",
        //         "order_id": "C0101000007408440032",
        //         "order_date": "1571728739360570",
        //         "type": "bid",
        //         "units": "5.0",
        //         "units_remaining": "5.0",
        //         "price": "501000",
        //     }
        //
        let mut timestamp: Value = self.safe_integer_product(order.clone(), Value::from("order_date"), Value::from(0.001));
        let mut side_property: Value = self.safe_string_2(order.clone(), Value::from("type"), Value::from("side"), Value::Undefined);
        let mut side: Value = if side_property.clone() == Value::from("bid") { Value::from("buy") } else { Value::from("sell") };
        let mut status: Value = <Self as Bithumb>::parse_order_status(self, self.safe_string(order.clone(), Value::from("order_status"), Value::Undefined));
        let mut price: Value = self.safe_string_2(order.clone(), Value::from("order_price"), Value::from("price"), Value::Undefined);
        let mut r#type: Value = Value::from("limit");
        if Precise::string_equals(price.clone(), Value::from("0")) {
            r#type = Value::from("market");
        };
        let mut amount: Value = <Self as Bithumb>::fix_comma_number(self, self.safe_string_2(order.clone(), Value::from("order_qty"), Value::from("units"), Value::Undefined));
        let mut remaining: Value = <Self as Bithumb>::fix_comma_number(self, self.safe_string(order.clone(), Value::from("units_remaining"), Value::Undefined));
        if remaining.clone().is_nullish() {
            if status.clone() == Value::from("closed") {
                remaining = Value::from("0");
            } else if status.clone() != Value::from("canceled") {
                remaining = amount.clone();
            };
        };
        let mut symbol: Value = Value::Undefined;
        let mut base_id: Value = self.safe_string(order.clone(), Value::from("order_currency"), Value::Undefined);
        let mut quote_id: Value = self.safe_string(order.clone(), Value::from("payment_currency"), Value::Undefined);
        let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        if base.clone().is_nonnullish() && quote.clone().is_nonnullish() {
            symbol = base.clone() + Value::from("/") + quote.clone();
        };
        if symbol.clone().is_nullish() {
            market = <Self as Bithumb>::safe_market(self, Value::Undefined, market.clone(), Value::Undefined, Value::Undefined);
            symbol = market.get(Value::from("symbol"));
        };
        let mut id: Value = self.safe_string(order.clone(), Value::from("order_id"), Value::Undefined);
        let mut raw_trades: Value = self.safe_list(order.clone(), Value::from("contract"), Value::new_array());
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "id": id,
            "clientOrderId": Value::Undefined,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": Value::Undefined,
            "symbol": symbol,
            "type": r#type,
            "timeInForce": Value::Undefined,
            "postOnly": Value::Undefined,
            "side": side,
            "price": price,
            "triggerPrice": Value::Undefined,
            "amount": amount,
            "cost": Value::Undefined,
            "average": Value::Undefined,
            "filled": Value::Undefined,
            "remaining": remaining,
            "status": status,
            "fee": Value::Undefined,
            "trades": raw_trades
        }))).unwrap()), market.clone());
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" fetchOpenOrders() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        if limit.clone().is_nullish() {
            limit = Value::from(100);
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "count": limit,
            "order_currency": market.get(Value::from("base")),
            "payment_currency": market.get(Value::from("quote"))
        }))).unwrap());
        if since.clone().is_nonnullish() {
            request.set("after".into(), since.clone());
        };
        let mut response: Value = self.dispatch("privatePostInfoOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "status": "0000",
        //         "data": [
        //             {
        //                 "order_currency": "BTC",
        //                 "payment_currency": "KRW",
        //                 "order_id": "C0101000007408440032",
        //                 "order_date": "1571728739360570",
        //                 "type": "bid",
        //                 "units": "5.0",
        //                 "units_remaining": "5.0",
        //                 "price": "501000",
        //             }
        //         ]
        //     }
        //
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_orders(data.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" cancelOrder() requires a symbol argument"))"###);
        };
        let mut side_in_params: Value = params.contains_key(Value::from("side")).into();
        if !side_in_params.is_truthy() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" cancelOrder() requires a `side` parameter (sell or buy)"))"###);
        };
        let mut market: Value = self.market(symbol.clone());
        let mut side: Value = if params.get(Value::from("side")) == Value::from("buy") { Value::from("bid") } else { Value::from("ask") };
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("side").into(), Value::from("currency").into()])));
        // https://github.com/ccxt/ccxt/issues/6771
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id,
            "type": side,
            "order_currency": market.get(Value::from("base")),
            "payment_currency": market.get(Value::from("quote"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostTradeCancel".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    {
        //       'status': 'string',
        //    }
        //
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": response
        }))).unwrap()), Value::Undefined);
    }

    async fn cancel_unified_order(&mut self, mut order: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "side": order.get(Value::from("side"))
        }))).unwrap());
        return <Self as Bithumb>::cancel_order(self, order.get(Value::from("id")), order.get(Value::from("symbol")), extend_2(request.clone(), params.clone())).await;
    }

    async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, mut tag: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        (tag, params) = shift_2(self.handle_withdraw_tag_and_params(tag.clone(), params.clone()));
        self.check_address(address.clone());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "units": amount,
            "address": address,
            "currency": currency.get(Value::from("id"))
        }))).unwrap());
        if code.clone() == Value::from("XRP") || code.clone() == Value::from("XMR") || code.clone() == Value::from("EOS") || code.clone() == Value::from("STEEM") || code.clone() == Value::from("TON") {
            let mut destination: Value = self.safe_string(params.clone(), Value::from("destination"), Value::Undefined);
            if tag.clone().is_nullish() && destination.clone().is_nullish() {
                panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" ") + code.clone() + Value::from(" withdraw() requires a tag argument or an extra destination param"))"###);
            } else if tag.clone().is_nonnullish() {
                request.set("destination".into(), tag.clone());
            };
        };
        let mut response: Value = self.dispatch("privatePostTradeBtcWithdrawal".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // { "status" : "0000"}
        //
        return <Self as Bithumb>::parse_transaction(self, response.clone(), currency.clone());
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value) -> Value {
        //
        // withdraw
        //
        //     { "status" : "0000"}
        //
        currency = self.safe_currency(Value::Undefined, currency.clone());
        return Value::Json(normalize(&Value::Json(json!({
            "id": Value::Undefined,
            "txid": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "network": Value::Undefined,
            "addressFrom": Value::Undefined,
            "address": Value::Undefined,
            "addressTo": Value::Undefined,
            "amount": Value::Undefined,
            "type": Value::Undefined,
            "currency": currency.get(Value::from("code")),
            "status": Value::Undefined,
            "updated": Value::Undefined,
            "tagFrom": Value::Undefined,
            "tag": Value::Undefined,
            "tagTo": Value::Undefined,
            "comment": Value::Undefined,
            "internal": Value::Undefined,
            "fee": Value::Undefined,
            "info": transaction
        }))).unwrap());
    }

    fn fix_comma_number(&mut self, mut number_str: Value) -> Value {
        // some endpoints need this https://github.com/ccxt/ccxt/issues/11031
        if number_str.clone().is_nullish() {
            return Value::Undefined;
        };
        let mut final_number_str: Value = number_str.clone();
        while final_number_str.index_of(Value::from(",")) > Value::from(1).neg(){
            final_number_str = final_number_str.replace(Value::from(","), Value::from(""));
        };
        return final_number_str.clone();
    }

    fn nonce(&self) -> Value {
        return self.milliseconds();
    }

    

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetTickerallquoteid" => Bithumb::request(self, "ticker/ALL_{quoteId}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTickerbaseidquoteid" => Bithumb::request(self, "ticker/{baseId}_{quoteId}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOrderbookallquoteid" => Bithumb::request(self, "orderbook/ALL_{quoteId}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOrderbookbaseidquoteid" => Bithumb::request(self, "orderbook/{baseId}_{quoteId}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTransactionhistorybaseidquoteid" => Bithumb::request(self, "transaction_history/{baseId}_{quoteId}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetNetworkinfo" => Bithumb::request(self, "network-info".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetsstatusmultichainall" => Bithumb::request(self, "assetsstatus/multichain/ALL".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetsstatusmultichaincurrency" => Bithumb::request(self, "assetsstatus/multichain/{currency}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetWithdrawminimumall" => Bithumb::request(self, "withdraw/minimum/ALL".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetWithdrawminimumcurrency" => Bithumb::request(self, "withdraw/minimum/{currency}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetsstatusall" => Bithumb::request(self, "assetsstatus/ALL".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetsstatusbaseid" => Bithumb::request(self, "assetsstatus/{baseId}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCandlestickbaseidquoteidinterval" => Bithumb::request(self, "candlestick/{baseId}_{quoteId}/{interval}".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostInfoaccount" => Bithumb::request(self, "info/account".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostInfobalance" => Bithumb::request(self, "info/balance".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostInfowalletaddress" => Bithumb::request(self, "info/wallet_address".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostInfoticker" => Bithumb::request(self, "info/ticker".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostInfoorders" => Bithumb::request(self, "info/orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostInfousertransactions" => Bithumb::request(self, "info/user_transactions".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostInfoorderdetail" => Bithumb::request(self, "info/order_detail".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeplace" => Bithumb::request(self, "trade/place".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecancel" => Bithumb::request(self, "trade/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradebtcwithdrawal" => Bithumb::request(self, "trade/btc_withdrawal".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradekrwdeposit" => Bithumb::request(self, "trade/krw_deposit".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradekrwwithdrawal" => Bithumb::request(self, "trade/krw_withdrawal".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTrademarketbuy" => Bithumb::request(self, "trade/market_buy".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTrademarketsell" => Bithumb::request(self, "trade/market_sell".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradestoplimit" => Bithumb::request(self, "trade/stop_limit".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BithumbImpl(Value);
impl Exchange for BithumbImpl {}
impl Bithumb for BithumbImpl {}
impl ValueTrait for BithumbImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value) -> Value { self.0.slice(start) }
}

impl BithumbImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = BithumbImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Bithumb::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


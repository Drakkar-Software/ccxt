#![allow(clippy::all)]
#![allow(non_snake_case)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Hibachi : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "hibachi",
            "name": "Hibachi",
            "countries": [
                "US"
            ],
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": true,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "cancelOrders": true,
                "closeAllPositions": false,
                "closePosition": false,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": false,
                "createMarketOrder": false,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrders": true,
                "createOrderWithTakeProfitAndStopLoss": false,
                "createReduceOnlyOrder": false,
                "createStopLimitOrder": false,
                "createStopLossOrder": false,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "createTakeProfitOrder": false,
                "createTrailingAmountOrder": false,
                "createTrailingPercentOrder": false,
                "createTriggerOrder": false,
                "editOrder": true,
                "editOrders": true,
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchCanceledOrders": false,
                "fetchClosedOrder": false,
                "fetchClosedOrders": false,
                "fetchConvertCurrencies": false,
                "fetchConvertQuote": false,
                "fetchCurrencies": false,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": false,
                "fetchFundingHistory": false,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": true,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchFundingRates": false,
                "fetchIndexOHLCV": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLeverage": false,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterest": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrder": false,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": false,
                "fetchOrderTrades": false,
                "fetchPosition": false,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": false,
                "fetchTicker": true,
                "fetchTickers": false,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchTradingLimits": false,
                "fetchTransactions": "emulated",
                "fetchTransfers": false,
                "fetchWithdrawals": true,
                "reduceMargin": false,
                "setLeverage": false,
                "setMargin": false,
                "setPositionMode": false,
                "transfer": false,
                "withdraw": true,
                "cancelWithdraw": false,
                "createConvertTrade": false
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/7301bbb1-4f27-4167-8a55-75f74b14e973",
                "api": {
                    "public": "https://data-api.hibachi.xyz",
                    "private": "https://api.hibachi.xyz"
                },
                "www": "https://www.hibachi.xyz/",
                "referral": {
                    "url": "hibachi.xyz/r/ZBL2YFWIHU"
                }
            },
            "api": {
                "public": {
                    "get": {
                        "market/exchange-info": 1,
                        "market/data/trades": 1,
                        "market/data/prices": 1,
                        "market/data/stats": 1,
                        "market/data/klines": 1,
                        "market/data/orderbook": 1,
                        "market/data/open-interest": 1,
                        "market/data/funding-rates": 1,
                        "exchange/utc-timestamp": 1
                    }
                },
                "private": {
                    "get": {
                        "capital/deposit-info": 1,
                        "capital/history": 1,
                        "trade/account/trading_history": 1,
                        "trade/account/info": 1,
                        "trade/order": 1,
                        "trade/account/trades": 1,
                        "trade/orders": 1
                    },
                    "put": {
                        "trade/order": 1
                    },
                    "delete": {
                        "trade/order": 1,
                        "trade/orders": 1
                    },
                    "post": {
                        "trade/order": 1,
                        "trade/orders": 1,
                        "capital/withdraw": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": false,
                "uid": false,
                "accountId": true,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": true,
                "walletAddress": false,
                "token": false
            },
            "currencies": {
                "USDT": {
                    "info": {},
                    "id": "USDT",
                    "code": "USDT",
                    "precision": 0.000001,
                    "type": "fiat",
                    "name": "USDT",
                    "active": true,
                    "deposit": true,
                    "withdraw": true,
                    "fees": {},
                    "networks": {
                        "ARBITRUM": {
                            "id": "ARBITRUM",
                            "network": "ARBITRUM",
                            "limits": {
                                "withdraw": {},
                                "deposit": {}
                            },
                            "info": {}
                        }
                    },
                    "limits": {
                        "deposit": {},
                        "withdraw": {}
                    }
                }
            },
            "timeframes": {
                "1m": "1min",
                "5m": "5min",
                "15m": "15min",
                "1h": "1h",
                "4h": "4h",
                "1d": "1d",
                "1w": "1w"
            },
            "fees": {
                "trading": {
                    "tierBased": false,
                    "percentage": true,
                    "taker": 0.00045,
                    "maker": 0.00015
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "userAgent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/62.0.3202.94 Safari/537.36",
            "options": {},
            "features": {
                "default": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": false,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": false,
                        "iceberg": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {}
                },
                "swap": {
                    "linear": {
                        "extends": "default"
                    }
                },
                "future": {
                    "linear": {
                        "extends": "default"
                    }
                }
            }
        }"###).unwrap())
    }

    fn get_account_id(&mut self) -> Value {
        self.check_required_credentials(Value::Undefined);
        let mut id: Value = self.parse_to_int(self.get("accountId".into()));
        return id.clone();
    }

    fn parse_market(&self, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        let mut numeric_id: Value = self.safe_number(market.clone(), Value::from("id"), Value::Undefined);
        let mut market_type: Value = Value::from("swap");
        let mut base_id: Value = self.safe_string(market.clone(), Value::from("underlyingSymbol"), Value::Undefined);
        let mut quote_id: Value = self.safe_string(market.clone(), Value::from("settlementSymbol"), Value::Undefined);
        let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        let mut settle_id: Value = self.safe_string(market.clone(), Value::from("settlementSymbol"), Value::Undefined);
        let mut settle: Value = self.safe_currency_code(settle_id.clone(), Value::Undefined);
        let mut symbol: Value = base.clone() + Value::from("/") + quote.clone() + Value::from(":") + settle.clone();
        let mut created: Value = self.safe_integer_product(market.clone(), Value::from("marketCreationTimestamp"), Value::from(1000), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "id": market_id,
            "numericId": numeric_id,
            "symbol": symbol,
            "base": base,
            "quote": quote,
            "settle": settle,
            "baseId": base_id,
            "quoteId": quote_id,
            "settleId": settle_id,
            "type": market_type,
            "spot": false,
            "margin": false,
            "swap": true,
            "future": false,
            "option": false,
            "active": self.safe_string(market.clone(), Value::from("status"), Value::Undefined) == Value::from("LIVE"),
            "contract": true,
            "linear": true,
            "inverse": false,
            "contractSize": self.parse_number(Value::from("1"), Value::Undefined),
            "expiry": Value::Undefined,
            "expiryDatetime": Value::Undefined,
            "strike": Value::Undefined,
            "optionType": Value::Undefined,
            "precision": Value::Json(normalize(&Value::Json(json!({
                "amount": self.parse_number(self.parse_precision(self.safe_string(market.clone(), Value::from("underlyingDecimals"), Value::Undefined)), Value::Undefined),
                "price": self.parse_number(self.safe_list(market.clone(), Value::from("orderbookGranularities"), Value::Undefined).get(Value::from(0)), Value::Undefined) / Value::from(10000)
            }))).unwrap()),
            "limits": Value::Json(normalize(&Value::Json(json!({
                "leverage": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "amount": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "price": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "cost": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(market.clone(), Value::from("minNotional"), Value::Undefined),
                    "max": Value::Undefined
                }))).unwrap())
            }))).unwrap()),
            "created": created,
            "info": market
        }))).unwrap());
    }

    

    fn hardcoded_currencies(&mut self) -> Value {
        // Hibachi only supports USDT on Arbitrum at this time
        // We don't have an API endpoint to expose this information yet
        let mut result: Value = Value::new_object();
        let mut networks: Value = Value::new_object();
        let mut network_id: Value = Value::from("ARBITRUM");
        networks.set(network_id.clone(), Value::Json(normalize(&Value::Json(json!({
            "id": network_id,
            "network": network_id,
            "limits": Value::Json(normalize(&Value::Json(json!({
                "withdraw": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "deposit": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap())
            }))).unwrap()),
            "active": Value::Undefined,
            "deposit": Value::Undefined,
            "withdraw": Value::Undefined,
            "info": Value::new_object()
        }))).unwrap()));
        let mut code: Value = self.safe_currency_code(Value::from("USDT"), Value::Undefined);
        result.set(code.clone(), self.safe_currency_structure(Value::Json(normalize(&Value::Json(json!({
            "id": "USDT",
            "name": "USDT",
            "type": "fiat",
            "code": code,
            "precision": self.parse_number(Value::from("0.000001"), Value::Undefined),
            "active": true,
            "fee": Value::Undefined,
            "networks": networks,
            "deposit": true,
            "withdraw": true,
            "limits": Value::Json(normalize(&Value::Json(json!({
                "deposit": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "withdraw": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap())
            }))).unwrap()),
            "info": Value::new_object()
        }))).unwrap())));
        return result.clone();
    }

    fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response
        }))).unwrap());
        // Hibachi only supports USDT on Arbitrum at this time
        let mut code: Value = self.safe_currency_code(Value::from("USDT"), Value::Undefined);
        let mut account: Value = self.account();
        account.set("total".into(), self.safe_string(response.clone(), Value::from("balance"), Value::Undefined));
        account.set("free".into(), self.safe_string(response.clone(), Value::from("maximalWithdraw"), Value::Undefined));
        result.set(code.clone(), account.clone());
        return self.safe_balance(result.clone());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetTradeAccountInfo".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     assets: [ { quantity: '3.000000', symbol: 'USDT' } ],
        //     balance: '3.000000',
        //     maximalWithdraw: '3.000000',
        //     numFreeTransfersRemaining: '100',
        //     positions: [],
        //     totalOrderNotional: '0.000000',
        //     totalPositionNotional: '0.000000',
        //     totalUnrealizedFundingPnl: '0.000000',
        //     totalUnrealizedPnl: '0.000000',
        //     totalUnrealizedTradingPnl: '0.000000',
        //     tradeMakerFeeRate: '0.00000000',
        //     tradeTakerFeeRate: '0.00020000'
        // }
        //
        return <Self as Hibachi>::parse_balance(self, response.clone());
    }

    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut prices: Value = self.safe_dict(ticker.clone(), Value::from("prices"), Value::Undefined);
        let mut stats: Value = self.safe_dict(ticker.clone(), Value::from("stats"), Value::Undefined);
        let mut bid: Value = self.safe_number(prices.clone(), Value::from("bidPrice"), Value::Undefined);
        let mut ask: Value = self.safe_number(prices.clone(), Value::from("askPrice"), Value::Undefined);
        let mut last: Value = self.safe_number(prices.clone(), Value::from("tradePrice"), Value::Undefined);
        let mut high: Value = self.safe_number(stats.clone(), Value::from("high24h"), Value::Undefined);
        let mut low: Value = self.safe_number(stats.clone(), Value::from("low24h"), Value::Undefined);
        let mut volume: Value = self.safe_number(stats.clone(), Value::from("volume24h"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": self.safe_symbol(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "bid": bid,
            "ask": ask,
            "last": last,
            "high": high,
            "low": low,
            "bidVolume": Value::Undefined,
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": Value::Undefined,
            "close": last,
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": Value::Undefined,
            "quoteVolume": volume,
            "info": ticker
        }))).unwrap()), market.clone());
    }

    fn parse_trade(&mut self, mut trade: Value, mut market: Value) -> Value {
        // public fetchTrades:
        //      {
        //          "price": "3512.431902",
        //          "quantity": "1.414780098",
        //          "takerSide": "Buy",
        //          "timestamp": 1712692147
        //      }
        //
        // private fetchMyTrades:
        //      {
        //          "askAccountId": 221,
        //          "askOrderId": 589168494921909200,
        //          "bidAccountId": 132,
        //          "bidOrderId": 589168494829895700,
        //          "fee": "0.000477",
        //          "id": 199511136,
        //          "orderType": "MARKET",
        //          "price": "119257.90000",
        //          "quantity": "0.0000200000",
        //          "realizedPnl": "-0.000352",
        //          "side": "Sell",
        //          "symbol": "BTC/USDT-P",
        //          "timestamp": 1752543391
        //      }
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut id: Value = self.safe_string(trade.clone(), Value::from("id"), Value::Undefined);
        let mut price: Value = self.safe_string(trade.clone(), Value::from("price"), Value::Undefined);
        let mut amount: Value = self.safe_string(trade.clone(), Value::from("quantity"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer_product(trade.clone(), Value::from("timestamp"), Value::from(1000), Value::Undefined);
        let mut cost: Value = Precise::string_mul(price.clone(), amount.clone());
        let mut side: Value = Value::Undefined;
        let mut fee: Value = Value::Undefined;
        let mut order_type: Value = Value::Undefined;
        let mut order_id: Value = Value::Undefined;
        let mut taker_or_maker: Value = Value::Undefined;
        if id.clone().is_nullish() {
            // public trades
            side = self.safe_string_lower(trade.clone(), Value::from("takerSide"), Value::Undefined);
            taker_or_maker = Value::from("taker");
        } else {
            // private trades
            side = self.safe_string_lower(trade.clone(), Value::from("side"), Value::Undefined);
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": self.safe_string(trade.clone(), Value::from("fee"), Value::Undefined),
                "currency": "USDT"
            }))).unwrap());
            order_type = self.safe_string_lower(trade.clone(), Value::from("orderType"), Value::Undefined);
            if side.clone() == Value::from("buy") {
                order_id = self.safe_string(trade.clone(), Value::from("bidOrderId"), Value::Undefined);
            } else {
                order_id = self.safe_string(trade.clone(), Value::from("askOrderId"), Value::Undefined);
            };
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol,
            "side": side,
            "price": price,
            "amount": amount,
            "cost": cost,
            "order": order_id,
            "takerOrMaker": taker_or_maker,
            "type": order_type,
            "fee": fee,
            "info": trade
        }))).unwrap()), market.clone());
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Hibachi>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "PENDING": "open",
            "CHILD_PENDING": "open",
            "SCHEDULED_TWAP": "open",
            "PLACED": "open",
            "PARTIALLY_FILLED": "open",
            "FILLED": "closed",
            "CANCELLED": "canceled",
            "REJECTED": "rejected"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_order(&mut self, mut order: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut status: Value = self.safe_string(order.clone(), Value::from("status"), Value::Undefined);
        let mut r#type: Value = self.safe_string_lower(order.clone(), Value::from("orderType"), Value::Undefined);
        let mut price: Value = self.safe_string(order.clone(), Value::from("price"), Value::Undefined);
        let mut raw_side: Value = self.safe_string(order.clone(), Value::from("side"), Value::Undefined);
        let mut side: Value = Value::Undefined;
        if raw_side.clone() == Value::from("BID") {
            side = Value::from("buy");
        } else if raw_side.clone() == Value::from("ASK") {
            side = Value::from("sell");
        };
        let mut amount: Value = self.safe_string(order.clone(), Value::from("totalQuantity"), Value::Undefined);
        let mut remaining: Value = self.safe_string(order.clone(), Value::from("availableQuantity"), Value::Undefined);
        let mut total_quantity: Value = self.safe_string(order.clone(), Value::from("totalQuantity"), Value::Undefined);
        let mut available_quantity: Value = self.safe_string(order.clone(), Value::from("availableQuantity"), Value::Undefined);
        let mut filled: Value = Value::Undefined;
        if total_quantity.clone().is_nonnullish() && available_quantity.clone().is_nonnullish() {
            filled = Precise::string_sub(total_quantity.clone(), available_quantity.clone());
        };
        let mut time_in_force: Value = Value::from("GTC");
        let mut order_flags: Value = self.safe_value(order.clone(), Value::from("orderFlags"), Value::Undefined);
        let mut post_only: Value = false.into();
        let mut reduce_only: Value = false.into();
        if order_flags.clone() == Value::from("POST_ONLY") {
            time_in_force = Value::from("PO");
            post_only = true.into();
        } else if order_flags.clone() == Value::from("IOC") {
            time_in_force = Value::from("IOC");
        } else if order_flags.clone() == Value::from("REDUCE_ONLY") {
            reduce_only = true.into();
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "id": self.safe_string(order.clone(), Value::from("orderId"), Value::Undefined),
            "clientOrderId": Value::Undefined,
            "datetime": Value::Undefined,
            "timestamp": Value::Undefined,
            "lastTradeTimestamp": Value::Undefined,
            "lastUpdateTimestamp": Value::Undefined,
            "status": <Self as Hibachi>::parse_order_status(self, status.clone()),
            "symbol": market.get(Value::from("symbol")),
            "type": r#type,
            "timeInForce": time_in_force,
            "side": side,
            "price": price,
            "average": Value::Undefined,
            "amount": amount,
            "filled": filled,
            "remaining": remaining,
            "cost": Value::Undefined,
            "trades": Value::Undefined,
            "fee": Value::Undefined,
            "reduceOnly": reduce_only,
            "postOnly": post_only,
            "triggerPrice": self.safe_number(order.clone(), Value::from("triggerPrice"), Value::Undefined)
        }))).unwrap()), market.clone());
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "orderId": id,
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetTradeOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return <Self as Hibachi>::parse_order(self, response.clone(), market.clone());
    }

    async fn fetch_trading_fees(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetTradeAccountInfo".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //    {
        //        "tradeMakerFeeRate": "0.00000000",
        //        "tradeTakerFeeRate": "0.00020000"
        //    },
        let mut maker_fee_rate: Value = self.safe_number(response.clone(), Value::from("tradeMakerFeeRate"), Value::Undefined);
        let mut taker_fee_rate: Value = self.safe_number(response.clone(), Value::from("tradeTakerFeeRate"), Value::Undefined);
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < self.get("symbols".into()).len() {
            let mut symbol: Value = self.get("symbols".into()).get(i.into());
            result.set(symbol.clone(), Value::Json(normalize(&Value::Json(json!({
                "info": response,
                "symbol": symbol,
                "maker": maker_fee_rate,
                "taker": taker_fee_rate,
                "percentage": true
            }))).unwrap()));
            i += 1;
        };
        return result.clone();
    }

    fn order_message(&mut self, mut market: Value, mut nonce: Value, mut fee_rate: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value) -> Value {
        let mut side_internal: Value = Value::from(0);
        if side.clone() == Value::from("sell") {
            side_internal = Value::from(0);
        } else if side.clone() == Value::from("buy") {
            side_internal = Value::from(1);
        };
        // Converting them to internal representation:
        // - Quantity: Internal = External * (10^underlyingDecimals)
        // - Price: Internal = External * (2^32) * (10^(settlementDecimals-underlyingDecimals))
        // - FeeRate: Internal = External * (10^8)
        let mut amount_str: Value = self.amount_to_precision(self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined), amount.clone());
        let mut fee_rate_str: Value = self.number_to_string(fee_rate.clone());
        let mut info: Value = self.safe_dict(market.clone(), Value::from("info"), Value::Undefined);
        let mut underlying: Value = Value::from("1e") + self.safe_string(info.clone(), Value::from("underlyingDecimals"), Value::Undefined);
        let mut settlement: Value = Value::from("1e") + self.safe_string(info.clone(), Value::from("settlementDecimals"), Value::Undefined);
        let mut one: Value = Value::from("1");
        let mut fee_rate_factor: Value = Value::from("100000000");
        // 10^8
        let mut price_factor: Value = Value::from("4294967296");
        // 2^32
        let mut quantity_internal: Value = Precise::string_div(Precise::string_mul(amount_str.clone(), underlying.clone()), one.clone(), Value::from(0));
        let mut fee_rate_internal: Value = Precise::string_div(Precise::string_mul(fee_rate_str.clone(), fee_rate_factor.clone()), one.clone(), Value::from(0));
        // Encoding
        let mut nonce16: Value = self.int_to_base16(nonce.clone());
        let mut nonce_padded: Value = nonce16.pad_start(Value::from(16), Value::from("0"));
        let mut encoded_nonce: Value = self.base16_to_binary(nonce_padded.clone());
        let mut numeric_id: Value = self.int_to_base16(self.safe_integer(market.clone(), Value::from("numericId"), Value::Undefined));
        let mut numeric_id_padded: Value = numeric_id.pad_start(Value::from(8), Value::from("0"));
        let mut encoded_market_id: Value = self.base16_to_binary(numeric_id_padded.clone());
        let mut quantity16: Value = self.int_to_base16(self.parse_to_int(quantity_internal.clone()));
        let mut quantity_padded: Value = quantity16.pad_start(Value::from(16), Value::from("0"));
        let mut encoded_quantity: Value = self.base16_to_binary(quantity_padded.clone());
        let mut side_internal16: Value = self.int_to_base16(side_internal.clone());
        let mut side_padded: Value = side_internal16.pad_start(Value::from(8), Value::from("0"));
        let mut encoded_side: Value = self.base16_to_binary(side_padded.clone());
        let mut fee_rate_internal16: Value = self.int_to_base16(self.parse_to_int(fee_rate_internal.clone()));
        let mut fee_rate_padded: Value = fee_rate_internal16.pad_start(Value::from(16), Value::from("0"));
        let mut encoded_fee_rate: Value = self.base16_to_binary(fee_rate_padded.clone());
        let mut encoded_price: Value = self.binary_concat(Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        if r#type.clone() == Value::from("limit") {
            let mut price_str: Value = self.price_to_precision(self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined), price.clone());
            let mut price_internal: Value = Precise::string_div(Precise::string_div(Precise::string_mul(Precise::string_mul(price_str.clone(), price_factor.clone()), settlement.clone()), underlying.clone(), Value::Undefined), one.clone(), Value::from(0));
            let mut price16: Value = self.int_to_base16(self.parse_to_int(price_internal.clone()));
            let mut price_padded: Value = price16.pad_start(Value::from(16), Value::from("0"));
            encoded_price = self.base16_to_binary(price_padded.clone());
        };
        let mut message: Value = self.binary_concat(encoded_nonce.clone(), encoded_market_id.clone(), encoded_quantity.clone(), encoded_side.clone());
        return message.clone();
    }

    fn create_order_request(&mut self, mut nonce: Value, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut market: Value = self.market(symbol.clone());
        let mut fee_rate: Value = Math::max(self.safe_number(market.clone(), Value::from("taker"), self.safe_number(self.get("options".into()), Value::from("defaultTakerFee"), Value::from(0.00045))), self.safe_number(market.clone(), Value::from("maker"), self.safe_number(self.get("options".into()), Value::from("defaultMakerFee"), Value::from(0.00015))));
        let mut side_internal: Value = Value::from("");
        if side.clone() == Value::from("sell") {
            side_internal = Value::from("ASK");
        } else if side.clone() == Value::from("buy") {
            side_internal = Value::from("BID");
        };
        let mut price_internal: Value = Value::from("");
        if price.is_truthy() {
            price_internal = self.price_to_precision(symbol.clone(), price.clone());
        };
        let mut message: Value = <Self as Hibachi>::order_message(self, market.clone(), nonce.clone(), fee_rate.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone());
        let mut signature: Value = <Self as Hibachi>::sign_message(self, message.clone(), self.get("privateKey".into()));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": self.safe_string(market.clone(), Value::from("id"), Value::Undefined),
            "nonce": nonce,
            "side": side_internal,
            "orderType": r#type.to_upper_case(),
            "quantity": self.amount_to_precision(symbol.clone(), amount.clone()),
            "price": price_internal,
            "signature": signature,
            "maxFeesPercent": self.number_to_string(fee_rate.clone())
        }))).unwrap());
        let mut post_only: Value = self.is_post_only((r#type.to_upper_case() == Value::from("MARKET")).into(), Value::Undefined, params.clone());
        let mut reduce_only: Value = self.safe_bool_2(params.clone(), Value::from("reduceOnly"), Value::from("reduce_only"), Value::Undefined);
        let mut time_in_force: Value = self.safe_string_lower(params.clone(), Value::from("timeInForce"), Value::Undefined);
        let mut trigger_price: Value = self.safe_string_2(params.clone(), Value::from("triggerPrice"), Value::from("stopPrice"), Value::Undefined);
        if post_only.is_truthy() {
            request.set("orderFlags".into(), Value::from("POST_ONLY"));
        } else if time_in_force.clone() == Value::from("ioc") {
            request.set("orderFlags".into(), Value::from("IOC"));
        } else if reduce_only.is_truthy() {
            request.set("orderFlags".into(), Value::from("REDUCE_ONLY"));
        };
        if trigger_price.clone().is_nonnullish() {
            request.set("triggerPrice".into(), trigger_price.clone());
        };
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("reduceOnly").into(), Value::from("reduce_only").into(), Value::from("postOnly").into(), Value::from("timeInForce").into(), Value::from("stopPrice").into(), Value::from("triggerPrice").into()])));
        return extend_2(request.clone(), params.clone());
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut nonce: Value = <Self as Hibachi>::nonce(self);
        let mut request: Value = <Self as Hibachi>::create_order_request(self, nonce.clone(), symbol.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), params.clone());
        request.set("accountId".into(), <Self as Hibachi>::get_account_id(self));
        let mut response: Value = self.dispatch("privatePostTradeOrder".into(), request.clone(), Value::Undefined).await;
        //
        // {
        //     "orderId": "578721673790138368"
        // }
        //
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(response.clone(), Value::from("orderId"), Value::Undefined),
            "status": "pending"
        }))).unwrap()), Value::Undefined);
    }

    async fn create_orders(&mut self, mut orders: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut nonce: Value = <Self as Hibachi>::nonce(self);
        let mut request_orders: Value = Value::new_array();
        let mut i: usize = 0;
        while i < orders.len() {
            let mut raw_order: Value = orders.get(i.into());
            let mut symbol: Value = self.safe_string(raw_order.clone(), Value::from("symbol"), Value::Undefined);
            let mut r#type: Value = self.safe_string(raw_order.clone(), Value::from("type"), Value::Undefined);
            let mut side: Value = self.safe_string(raw_order.clone(), Value::from("side"), Value::Undefined);
            let mut amount: Value = self.safe_value(raw_order.clone(), Value::from("amount"), Value::Undefined);
            let mut price: Value = self.safe_value(raw_order.clone(), Value::from("price"), Value::Undefined);
            let mut order_params: Value = self.safe_dict(raw_order.clone(), Value::from("params"), Value::new_object());
            let mut order_request: Value = <Self as Hibachi>::create_order_request(self, nonce.clone() + Value::from(i), symbol.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), order_params.clone());
            order_request.set("action".into(), Value::from("place"));
            request_orders.push(order_request.clone());
            i += 1;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self),
            "orders": request_orders
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostTradeOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // { "orders": [ { nonce: '1754349993908', orderId: '589642085255349248' } ] }
        //
        let mut ret: Value = Value::new_array();
        let mut response_orders: Value = self.safe_list(response.clone(), Value::from("orders"), Value::Undefined);
        let mut i: usize = 0;
        while i < response_orders.len() {
            let mut response_order: Value = response_orders.get(i.into());
            ret.push(self.safe_order(Value::Json(normalize(&Value::Json(json!({
                "info": response_order,
                "id": self.safe_string(response_order.clone(), Value::from("orderId"), Value::Undefined),
                "status": "pending"
            }))).unwrap()), Value::Undefined));
            i += 1;
        };
        return ret.clone();
    }

    fn edit_order_request(&mut self, mut nonce: Value, mut id: Value, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut market: Value = self.market(symbol.clone());
        let mut fee_rate: Value = Math::max(self.safe_number(market.clone(), Value::from("taker"), Value::Undefined), self.safe_number(market.clone(), Value::from("maker"), Value::Undefined));
        let mut message: Value = <Self as Hibachi>::order_message(self, market.clone(), nonce.clone(), fee_rate.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone());
        let mut signature: Value = <Self as Hibachi>::sign_message(self, message.clone(), self.get("privateKey".into()));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "orderId": id,
            "nonce": nonce,
            "updatedQuantity": self.amount_to_precision(symbol.clone(), amount.clone()),
            "updatedPrice": self.price_to_precision(symbol.clone(), price.clone()),
            "maxFeesPercent": self.number_to_string(fee_rate.clone()),
            "signature": signature
        }))).unwrap());
        return extend_2(request.clone(), params.clone());
    }

    async fn edit_order(&mut self, mut id: Value, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut nonce: Value = <Self as Hibachi>::nonce(self);
        let mut request: Value = <Self as Hibachi>::edit_order_request(self, nonce.clone(), id.clone(), symbol.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), params.clone());
        request.set("accountId".into(), <Self as Hibachi>::get_account_id(self));
        self.dispatch("privatePutTradeOrder".into(), request.clone(), Value::Undefined).await;
        // At this time the response body is empty. A 200 response means the update request is accepted and sent to process
        //
        // {}
        //
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": id,
            "status": "pending"
        }))).unwrap()), Value::Undefined);
    }

    async fn edit_orders(&mut self, mut orders: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut nonce: Value = <Self as Hibachi>::nonce(self);
        let mut request_orders: Value = Value::new_array();
        let mut i: usize = 0;
        while i < orders.len() {
            let mut raw_order: Value = orders.get(i.into());
            let mut id: Value = self.safe_string(raw_order.clone(), Value::from("id"), Value::Undefined);
            let mut symbol: Value = self.safe_string(raw_order.clone(), Value::from("symbol"), Value::Undefined);
            let mut r#type: Value = self.safe_string(raw_order.clone(), Value::from("type"), Value::Undefined);
            let mut side: Value = self.safe_string(raw_order.clone(), Value::from("side"), Value::Undefined);
            let mut amount: Value = self.safe_value(raw_order.clone(), Value::from("amount"), Value::Undefined);
            let mut price: Value = self.safe_value(raw_order.clone(), Value::from("price"), Value::Undefined);
            let mut order_params: Value = self.safe_dict(raw_order.clone(), Value::from("params"), Value::new_object());
            let mut order_request: Value = <Self as Hibachi>::edit_order_request(self, nonce.clone() + Value::from(i), id.clone(), symbol.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), order_params.clone());
            order_request.set("action".into(), Value::from("modify"));
            request_orders.push(order_request.clone());
            i += 1;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self),
            "orders": request_orders
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostTradeOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // { "orders": [ { "orderId": "589636801329628160" } ] }
        //
        let mut ret: Value = Value::new_array();
        let mut response_orders: Value = self.safe_list(response.clone(), Value::from("orders"), Value::Undefined);
        let mut i: usize = 0;
        while i < response_orders.len() {
            let mut response_order: Value = response_orders.get(i.into());
            ret.push(self.safe_order(Value::Json(normalize(&Value::Json(json!({
                "info": response_order,
                "id": self.safe_string(response_order.clone(), Value::from("orderId"), Value::Undefined),
                "status": "pending"
            }))).unwrap()), Value::Undefined));
            i += 1;
        };
        return ret.clone();
    }

    fn cancel_order_request(&mut self, mut id: Value) -> Value {
        let mut bigid: Value = self.convert_to_big_int(id.clone());
        let mut idbase16: Value = self.int_to_base16(bigid.clone());
        let mut id_padded: Value = idbase16.pad_start(Value::from(16), Value::from("0"));
        let mut message: Value = self.base16_to_binary(id_padded.clone());
        let mut signature: Value = <Self as Hibachi>::sign_message(self, message.clone(), self.get("privateKey".into()));
        return Value::Json(normalize(&Value::Json(json!({
            "orderId": id,
            "signature": signature
        }))).unwrap());
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut request: Value = <Self as Hibachi>::cancel_order_request(self, id.clone());
        request.set("accountId".into(), <Self as Hibachi>::get_account_id(self));
        let mut response: Value = self.dispatch("privateDeleteTradeOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // At this time the response body is empty. A 200 response means the cancel request is accepted and sent to cancel
        //
        // {}
        //
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": response,
            "id": id,
            "status": "canceled"
        }))).unwrap()), Value::Undefined);
    }

    async fn cancel_orders(&mut self, mut ids: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut orders: Value = Value::new_array();
        let mut i: usize = 0;
        while i < ids.len() {
            let mut order_request: Value = <Self as Hibachi>::cancel_order_request(self, ids.get(i.into()));
            order_request.set("action".into(), Value::from("cancel"));
            orders.push(order_request.clone());
            i += 1;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self),
            "orders": orders
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostTradeOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // { "orders": [ { "orderId": "589636801329628160" } ] }
        //
        let mut ret: Value = Value::new_array();
        let mut response_orders: Value = self.safe_list(response.clone(), Value::from("orders"), Value::Undefined);
        let mut i: usize = 0;
        while i < response_orders.len() {
            let mut response_order: Value = response_orders.get(i.into());
            ret.push(self.safe_order(Value::Json(normalize(&Value::Json(json!({
                "info": response_order,
                "id": self.safe_string(response_order.clone(), Value::from("orderId"), Value::Undefined),
                "status": "canceled"
            }))).unwrap()), Value::Undefined));
            i += 1;
        };
        return ret.clone();
    }

    async fn cancel_all_orders(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut nonce: Value = <Self as Hibachi>::nonce(self);
        let mut nonce16: Value = self.int_to_base16(nonce.clone());
        let mut nonce_padded: Value = nonce16.pad_start(Value::from(16), Value::from("0"));
        let mut message: Value = self.base16_to_binary(nonce_padded.clone());
        let mut signature: Value = <Self as Hibachi>::sign_message(self, message.clone(), self.get("privateKey".into()));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self),
            "nonce": nonce,
            "signature": signature
        }))).unwrap());
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            request.set("contractId".into(), self.safe_integer(market.clone(), Value::from("numericId"), Value::Undefined));
        };
        let mut response: Value = self.dispatch("privateDeleteTradeOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // At this time the response body is empty. A 200 response means the cancel request is accepted and sent to process
        //
        // {}
        //
        return Value::Json(serde_json::Value::Array(vec![self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": response
        }))).unwrap()), Value::Undefined).into()]));
    }

    fn encode_withdraw_message(&mut self, mut amount: Value, mut max_fees: Value, mut address: Value) -> Value {
        // Converting them to internal representation:
        // - Quantity: Internal = External * (10^6)
        // - maxFees: Internal = External * (10^6)
        // We only have USDT as our currency as this time
        let mut USDTAssetId: usize = 1;
        let mut USDTFactor: Value = Value::from("1000000");
        let mut amount_str: Value = self.number_to_string(amount.clone());
        let mut max_fees_str: Value = self.number_to_string(max_fees.clone());
        let mut one: Value = Value::from("1");
        let mut quantity_internal: Value = Precise::string_div(Precise::string_mul(amount_str.clone(), USDTFactor.clone()), one.clone(), Value::from(0));
        let mut max_fees_internal: Value = Precise::string_div(Precise::string_mul(max_fees_str.clone(), USDTFactor.clone()), one.clone(), Value::from(0));
        // Encoding
        let mut usdt_asset16: Value = self.int_to_base16(Value::from(USDTAssetId));
        let mut usdt_asset_padded: Value = usdt_asset16.pad_start(Value::from(8), Value::from("0"));
        let mut encoded_asset_id: Value = self.base16_to_binary(usdt_asset_padded.clone());
        let mut quantity16: Value = self.int_to_base16(self.parse_to_int(quantity_internal.clone()));
        let mut quantity_padded: Value = quantity16.pad_start(Value::from(16), Value::from("0"));
        let mut encoded_quantity: Value = self.base16_to_binary(quantity_padded.clone());
        let mut max_fees16: Value = self.int_to_base16(self.parse_to_int(max_fees_internal.clone()));
        let mut max_fees_padded: Value = max_fees16.pad_start(Value::from(16), Value::from("0"));
        let mut encoded_max_fees: Value = self.base16_to_binary(max_fees_padded.clone());
        let mut encoded_address: Value = self.base16_to_binary(address.clone());
        let mut message: Value = self.binary_concat(encoded_asset_id.clone(), encoded_quantity.clone(), encoded_max_fees.clone(), encoded_address.clone());
        return message.clone();
    }

    async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, mut tag: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut withdraw_address: Value = address.slice(Value::from(40).neg(), Value::Undefined);
        // Get the withdraw fees
        let mut exchange_info: Value = self.dispatch("publicGetMarketExchangeInfo".into(), params.clone(), Value::Undefined).await;
        // {
        //      "feeConfig": {
        //          "depositFees": "0.004518",
        //          "tradeMakerFeeRate": "0.00000000",
        //          "tradeTakerFeeRate": "0.00020000",
        //          "transferFeeRate": "0.00010000",
        //          "withdrawalFees": "0.012050"
        //    },
        // }
        let mut fee_config: Value = self.safe_dict(exchange_info.clone(), Value::from("feeConfig"), Value::Undefined);
        let mut max_fees: Value = self.safe_number(fee_config.clone(), Value::from("withdrawalFees"), Value::Undefined);
        // Generate the signature
        let mut message: Value = <Self as Hibachi>::encode_withdraw_message(self, amount.clone(), max_fees.clone(), withdraw_address.clone());
        let mut signature: Value = <Self as Hibachi>::sign_message(self, message.clone(), self.get("privateKey".into()));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self),
            "coin": "USDT",
            "network": "ARBITRUM",
            "withdrawAddress": withdraw_address,
            "selfWithdrawal": false,
            "quantity": self.number_to_string(amount.clone()),
            "maxFees": self.number_to_string(max_fees.clone()),
            "signature": signature
        }))).unwrap());
        self.dispatch("privatePostCapitalWithdraw".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // At this time the response body is empty. A 200 response means the withdraw request is accepted and sent to process
        //
        // {}
        //
        return Value::Json(normalize(&Value::Json(json!({
            "info": Value::Undefined,
            "id": Value::Undefined,
            "txid": Value::Undefined,
            "timestamp": self.milliseconds(),
            "datetime": Value::Undefined,
            "address": Value::Undefined,
            "addressFrom": Value::Undefined,
            "addressTo": withdraw_address,
            "tag": Value::Undefined,
            "tagFrom": Value::Undefined,
            "tagTo": Value::Undefined,
            "type": "withdrawal",
            "amount": amount,
            "currency": code,
            "status": "pending",
            "fee": Value::Json(normalize(&Value::Json(json!({
                "currency": "USDT",
                "cost": max_fees
            }))).unwrap()),
            "network": "ARBITRUM",
            "updated": Value::Undefined,
            "comment": Value::Undefined,
            "internal": Value::Undefined
        }))).unwrap());
    }

    fn nonce(&self) -> Value {
        return self.milliseconds();
    }

    fn sign_message(&mut self, mut message: Value, mut private_key: Value) -> Value {
        if private_key.len() == 44 {
            // For Exchange Managed account, the key length is 44 and we use HMAC to sign the message
            return self.hmac(message.clone(), self.encode(private_key.clone()), sha256().clone(), Value::from("hex"));
        } else {
            // For Trustless account, the key length is 66 including '0x' and we use ECDSA to sign the message
            let mut hash: Value = self.hash(message.clone(), sha256().clone(), Value::from("hex"));
            let mut signature: Value = ecdsa(hash.slice(Value::from(64).neg(), Value::Undefined), private_key.slice(Value::from(64).neg(), Value::Undefined), secp256k1().clone(), Value::Undefined);
            let mut r: Value = signature.get(Value::from("r"));
            let mut s: Value = signature.get(Value::from("s"));
            let mut v: Value = self.int_to_base16(signature.get(Value::from("v")));
            return r.pad_start(Value::from(64), Value::from("0")) + s.pad_start(Value::from(64), Value::from("0")) + v.pad_start(Value::from(2), Value::from("0"));
        };
        Value::Undefined
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Hibachi>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetTradeAccountTrades".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "trades": [
        //         {
        //             "askAccountId": 221,
        //             "askOrderId": 589168494921909200,
        //             "bidAccountId": 132,
        //             "bidOrderId": 589168494829895700,
        //             "fee": "0.000477",
        //             "id": 199511136,
        //             "orderType": "MARKET",
        //             "price": "119257.90000",
        //             "quantity": "0.0000200000",
        //             "realizedPnl": "-0.000352",
        //             "side": "Sell",
        //             "symbol": "BTC/USDT-P",
        //             "timestamp": 1752543391
        //         }
        //     ]
        // }
        //
        let mut trades: Value = self.safe_list(response.clone(), Value::from("trades"), Value::Undefined);
        return self.parse_trades(trades.clone(), market.clone(), since.clone(), limit.clone(), params.clone());
    }

    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        // [
        //     {
        //       "close": "3704.751036",
        //       "high": "3716.530378",
        //       "interval": "1h",
        //       "low": "3699.627883",
        //       "open": "3716.406894",
        //       "timestamp": 1712628000,
        //       "volumeNotional": "1637355.846362"
        //     }
        //   ]
        //
        return Value::Json(serde_json::Value::Array(vec![self.safe_integer_product(ohlcv.clone(), Value::from("timestamp"), Value::from(1000), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("open"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("high"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("low"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("close"), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from("volumeNotional"), Value::Undefined).into()]));
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetTradeOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // [
        //     {
        //         "accountId": 12452,
        //         "availableQuantity": "0.0000230769",
        //         "contractId": 2,
        //         "creationTime": 1752684501,
        //         "orderId": "589205486123876352",
        //         "orderType": "LIMIT",
        //         "price": "130000.00000",
        //         "side": "ASK",
        //         "status": "PLACED",
        //         "symbol": "BTC/USDT-P",
        //         "totalQuantity": "0.0000230769"
        //     },
        //     {
        //         "accountId": 12452,
        //         "availableQuantity": "1.234000000",
        //         "contractId": 1,
        //         "creationTime": 1752240682,
        //         "orderId": "589089141754429441",
        //         "orderType": "LIMIT",
        //         "price": "1.234000",
        //         "side": "BID",
        //         "status": "PLACED",
        //         "symbol": "ETH/USDT-P",
        //         "totalQuantity": "1.234000000"
        //     }
        // ]
        return self.parse_orders(response.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Hibachi>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["klines", "candles", "ohlcv"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_positions(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetTradeAccountInfo".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "assets": [
        //       {
        //         "quantity": "14.130626",
        //         "symbol": "USDT"
        //       }
        //     ],
        //     "balance": "14.186087",
        //     "maximalWithdraw": "4.152340",
        //     "numFreeTransfersRemaining": 96,
        //     "positions": [
        //       {
        //         "direction": "Short",
        //         "entryNotional": "10.302213",
        //         "notionalValue": "10.225008",
        //         "quantity": "0.004310550",
        //         "symbol": "ETH/USDT-P",
        //         "unrealizedFundingPnl": "0.000000",
        //         "unrealizedTradingPnl": "0.077204"
        //       },
        //       {
        //         "direction": "Short",
        //         "entryNotional": "2.000016",
        //         "notionalValue": "1.999390",
        //         "quantity": "0.0000328410",
        //         "symbol": "BTC/USDT-P",
        //         "unrealizedFundingPnl": "0.000000",
        //         "unrealizedTradingPnl": "0.000625"
        //       },
        //       {
        //         "direction": "Short",
        //         "entryNotional": "2.000015",
        //         "notionalValue": "2.022384",
        //         "quantity": "0.01470600",
        //         "symbol": "SOL/USDT-P",
        //         "unrealizedFundingPnl": "0.000000",
        //         "unrealizedTradingPnl": "-0.022369"
        //       }
        //     ],
        //   }
        //
        let mut data: Value = self.safe_list(response.clone(), Value::from("positions"), Value::new_array());
        return self.parse_positions(data.clone(), symbols.clone(), Value::Undefined);
    }

    fn parse_position(&self, mut position: Value, mut market: Value) -> Value {
        //
        // {
        //     "direction": "Short",
        //     "entryNotional": "10.302213",
        //     "notionalValue": "10.225008",
        //     "quantity": "0.004310550",
        //     "symbol": "ETH/USDT-P",
        //     "unrealizedFundingPnl": "0.000000",
        //     "unrealizedTradingPnl": "0.077204"
        // }
        //
        let mut market_id: Value = self.safe_string(position.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut side: Value = self.safe_string_lower(position.clone(), Value::from("direction"), Value::Undefined);
        let mut quantity: Value = self.safe_string(position.clone(), Value::from("quantity"), Value::Undefined);
        let mut unrealized_funding: Value = self.safe_string(position.clone(), Value::from("unrealizedFundingPnl"), Value::from("0"));
        let mut unrealized_trading: Value = self.safe_string(position.clone(), Value::from("unrealizedTradingPnl"), Value::from("0"));
        let mut unrealized_pnl: Value = Precise::string_add(unrealized_funding.clone(), unrealized_trading.clone());
        return self.safe_position(Value::Json(normalize(&Value::Json(json!({
            "info": position,
            "id": Value::Undefined,
            "symbol": symbol,
            "entryPrice": self.safe_string(position.clone(), Value::from("average_entry_price"), Value::Undefined),
            "markPrice": Value::Undefined,
            "notional": self.safe_string(position.clone(), Value::from("notionalValue"), Value::Undefined),
            "collateral": Value::Undefined,
            "unrealizedPnl": unrealized_pnl,
            "side": side,
            "contracts": self.parse_number(quantity.clone(), Value::Undefined),
            "contractSize": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "hedged": Value::Undefined,
            "maintenanceMargin": Value::Undefined,
            "maintenanceMarginPercentage": Value::Undefined,
            "initialMargin": Value::Undefined,
            "initialMarginPercentage": Value::Undefined,
            "leverage": Value::Undefined,
            "liquidationPrice": Value::Undefined,
            "marginRatio": Value::Undefined,
            "marginMode": Value::Undefined,
            "percentage": Value::Undefined
        }))).unwrap()));
    }

    

    

    fn parse_transaction_type(&self, mut r#type: Value) -> Value {
        let mut types: Value = Value::Json(normalize(&Value::Json(json!({
            "deposit": "transaction",
            "withdrawal": "transaction",
            "transfer-in": "transfer",
            "transfer-out": "transfer"
        }))).unwrap());
        return self.safe_string(types.clone(), r#type.clone(), r#type.clone());
    }

    fn parse_transaction_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "pending": "pending",
            "claimable": "pending",
            "completed": "ok",
            "failed": "canceled"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_ledger_entry(&self, mut item: Value, mut currency: Value) -> Value {
        let mut transaction_type: Value = self.safe_string(item.clone(), Value::from("transactionType"), Value::Undefined);
        let mut timestamp: Value = Value::Undefined;
        let mut r#type: Value = Value::Undefined;
        let mut direction: Value = Value::Undefined;
        let mut amount: Value = Value::Undefined;
        let mut fee: Value = Value::Undefined;
        let mut reference_id: Value = Value::Undefined;
        let mut reference_account: Value = Value::Undefined;
        let mut status: Value = Value::Undefined;
        if transaction_type.clone().is_nullish() {
            // response from TradeAccountTradingHistory
            timestamp = self.safe_integer_product(item.clone(), Value::from("timestamp"), Value::from(1000), Value::Undefined);
            r#type = Value::from("trade");
            let mut amount_str: Value = self.safe_string(item.clone(), Value::from("realizedPnl"), Value::Undefined);
            if Precise::string_lt(amount_str.clone(), Value::from("0")) {
                direction = Value::from("out");
                amount_str = Precise::string_neg(amount_str.clone());
            } else {
                direction = Value::from("in");
            };
            amount = self.parse_number(amount_str.clone(), Value::Undefined);
            fee = Value::Json(normalize(&Value::Json(json!({
                "currency": "USDT",
                "cost": self.safe_number(item.clone(), Value::from("fee"), Value::Undefined)
            }))).unwrap());
            status = Value::from("ok");
        } else {
            // response from CapitalHistory
            timestamp = self.safe_integer_product(item.clone(), Value::from("timestampSec"), Value::from(1000), Value::Undefined);
            amount = self.safe_number(item.clone(), Value::from("quantity"), Value::Undefined);
            direction = if transaction_type.clone() == Value::from("deposit") || transaction_type.clone() == Value::from("transfer-in") { Value::from("in") } else { Value::from("out") };
            r#type = <Self as Hibachi>::parse_transaction_type(self, transaction_type.clone());
            status = <Self as Hibachi>::parse_transaction_status(self, self.safe_string(item.clone(), Value::from("status"), Value::Undefined));
            if transaction_type.clone() == Value::from("transfer-in") {
                reference_account = self.safe_string(item.clone(), Value::from("srcAccountId"), Value::Undefined);
            } else if transaction_type.clone() == Value::from("transfer-out") {
                reference_account = self.safe_string(item.clone(), Value::from("receivingAccountId"), Value::Undefined);
            };
            reference_id = self.safe_string(item.clone(), Value::from("transactionHash"), Value::Undefined);
        };
        return self.safe_ledger_entry(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(item.clone(), Value::from("id"), Value::Undefined),
            "currency": self.currency(Value::from("USDT")),
            "account": self.number_to_string(self.get("accountId".into())),
            "referenceAccount": reference_account,
            "referenceId": reference_id,
            "status": status,
            "amount": amount,
            "before": Value::Undefined,
            "after": Value::Undefined,
            "fee": fee,
            "direction": direction,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "type": r#type,
            "info": item
        }))).unwrap()), currency.clone());
    }

    async fn fetch_ledger(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(Value::from("USDT"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut raw_promises: Value = Value::Json(serde_json::Value::Array(vec![self.dispatch("privateGetCapitalHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await.into(), self.dispatch("privateGetTradeAccountTradingHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await.into()]));
        let mut promises: Value = Promise::all(raw_promises.clone()).await;
        let mut response_capital_history: Value = promises.get(Value::from(0));
        //
        // {
        //     "transactions": [
        //         {
        //             "assetId": 1,
        //             "blockNumber": 358396669,
        //             "chain": "Arbitrum",
        //             "etaTsSec": null,
        //             "id": 358396669,
        //             "quantity": "0.999500",
        //             "status": "pending",
        //             "timestampSec": 1752692872,
        //             "token": "USDT",
        //             "transactionHash": "0x408e48881e0ba77d8638e3fe57bc06bdec513ddaa8b672e0aefa7e22e2f18b5e",
        //             "transactionType": "deposit"
        //         },
        //         {
        //             "assetId": 1,
        //             "etaTsSec": null,
        //             "id": 13116,
        //             "instantWithdrawalChain": null,
        //             "instantWithdrawalToken": null,
        //             "isInstantWithdrawal": false,
        //             "quantity": "0.040000",
        //             "status": "completed",
        //             "timestampSec": 1752542708,
        //             "transactionHash": "0xe89cf90b2408d1a273dc9427654145def102d9449e5e2cfc10690ccffc3d7e28",
        //             "transactionType": "withdrawal",
        //             "withdrawalAddress": "0x23625d5fc6a6e32638d908eb4c3a3415e5121f76"
        //         },
        //         {
        //             "assetId": 1,
        //             "id": 167,
        //             "quantity": "10.000000",
        //             "srcAccountId": 175,
        //             "srcAddress": "0xc2f77ce029438a3fdfe68ddee25991a9fb985a86",
        //             "status": "completed",
        //             "timestampSec": 1732224729,
        //             "transactionType": "transfer-in"
        //         },
        //         {
        //             "assetId": 1,
        //             "id": 170,
        //             "quantity": "10.000000",
        //             "receivingAccountId": 175,
        //             "receivingAddress": "0xc2f77ce029438a3fdfe68ddee25991a9fb985a86",
        //             "status": "completed",
        //             "timestampSec": 1732225631,
        //             "transactionType": "transfer-out"
        //         },
        //     ]
        // }
        //
        let mut rows_capital_history: Value = self.safe_list(response_capital_history.clone(), Value::from("transactions"), Value::Undefined);
        let mut response_trading_history: Value = promises.get(Value::from(1));
        //
        // {
        //     "tradingHistory": [
        //         {
        //             "eventType": "MARKET",
        //             "fee": "0.000008",
        //             "priceOrFundingRate": "119687.82481",
        //             "quantity": "0.0000003727",
        //             "realizedPnl": "0.004634",
        //             "side": "Sell",
        //             "symbol": "BTC/USDT-P",
        //             "timestamp": 1752522571
        //         },
        //         {
        //             "eventType": "FundingEvent",
        //             "fee": "0",
        //             "priceOrFundingRate": "0.000203",
        //             "quantity": "0.0000003727",
        //             "realizedPnl": "-0.000009067899008751979",
        //             "side": "Long",
        //             "symbol": "BTC/USDT-P",
        //             "timestamp": 1752508800
        //         },
        //     ]
        // }
        //
        let mut rows_trading_history: Value = self.safe_list(response_trading_history.clone(), Value::from("tradingHistory"), Value::Undefined);
        let mut rows: Value = self.array_concat(rows_capital_history.clone(), rows_trading_history.clone());
        return self.parse_ledger(rows.clone(), currency.clone(), since.clone(), limit.clone(), params.clone());
    }

    async fn fetch_deposit_address(&mut self, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "publicKey": self.safe_string(params.clone(), Value::from("publicKey"), Value::Undefined),
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetCapitalDepositInfo".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "depositAddressEvm": "0x0b95d90b9345dadf1460bd38b9f4bb0d2f4ed788"
        // }
        return Value::Json(normalize(&Value::Json(json!({
            "info": response,
            "currency": "USDT",
            "network": "ARBITRUM",
            "address": self.safe_string(response.clone(), Value::from("depositAddressEvm"), Value::Undefined),
            "tag": Value::Undefined
        }))).unwrap());
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value) -> Value {
        let mut timestamp: Value = self.safe_integer_product(transaction.clone(), Value::from("timestampSec"), Value::from(1000), Value::Undefined);
        let mut address: Value = self.safe_string(transaction.clone(), Value::from("withdrawalAddress"), Value::Undefined);
        let mut transaction_type: Value = self.safe_string(transaction.clone(), Value::from("transactionType"), Value::Undefined);
        if transaction_type.clone() != Value::from("deposit") && transaction_type.clone() != Value::from("withdrawal") {
            transaction_type = <Self as Hibachi>::parse_transaction_type(self, transaction_type.clone());
        };
        return Value::Json(normalize(&Value::Json(json!({
            "info": transaction,
            "id": self.safe_string(transaction.clone(), Value::from("id"), Value::Undefined),
            "txid": self.safe_string(transaction.clone(), Value::from("transactionHash"), Value::Undefined),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "network": "ARBITRUM",
            "address": address,
            "addressTo": address,
            "addressFrom": Value::Undefined,
            "tag": Value::Undefined,
            "tagTo": Value::Undefined,
            "tagFrom": Value::Undefined,
            "type": transaction_type,
            "amount": self.safe_number(transaction.clone(), Value::from("quantity"), Value::Undefined),
            "currency": "USDT",
            "status": <Self as Hibachi>::parse_transaction_status(self, self.safe_string(transaction.clone(), Value::from("status"), Value::Undefined)),
            "updated": Value::Undefined,
            "internal": Value::Undefined,
            "comment": Value::Undefined,
            "fee": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_deposits(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut currency: Value = self.safe_currency(code.clone(), Value::Undefined);
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetCapitalHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "transactions": [
        //         {
        //             "assetId": 1,
        //             "blockNumber": 0,
        //             "chain": null,
        //             "etaTsSec": 1752758789,
        //             "id": 42688,
        //             "quantity": "6.130000",
        //             "status": "completed",
        //             "timestampSec": 1752758788,
        //             "token": null,
        //             "transactionHash": "0x8dcd7bd1155b5624fb5e38a1365888f712ec633a57434340e05080c70b0e3bba",
        //             "transactionType": "deposit"
        //         },
        //         {
        //             "assetId": 1,
        //             "etaTsSec": null,
        //             "id": 12993,
        //             "instantWithdrawalChain": null,
        //             "instantWithdrawalToken": null,
        //             "isInstantWithdrawal": false,
        //             "quantity": "0.111930",
        //             "status": "completed",
        //             "timestampSec": 1752387891,
        //             "transactionHash": "0x32ab5fe5b90f6d753bab83523ebc8465eb9daef54580e13cb9ff031d400c5620",
        //             "transactionType": "withdrawal",
        //             "withdrawalAddress": "0x43f15ef2ef2ab5e61e987ee3d652a5872aea8a6c"
        //         },
        //     ]
        // }
        let mut transactions: Value = self.safe_list(response.clone(), Value::from("transactions"), Value::Undefined);
        let mut deposits: Value = Value::new_array();
        let mut i: usize = 0;
        while i < transactions.len() {
            let mut transaction: Value = transactions.get(i.into());
            if self.safe_string(transaction.clone(), Value::from("transactionType"), Value::Undefined) == Value::from("deposit") {
                deposits.push(transaction.clone());
            };
            i += 1;
        };
        return self.parse_transactions(deposits.clone(), currency.clone(), since.clone(), limit.clone(), params.clone());
    }

    async fn fetch_withdrawals(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut currency: Value = self.safe_currency(code.clone(), Value::Undefined);
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "accountId": <Self as Hibachi>::get_account_id(self)
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetCapitalHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "transactions": [
        //         {
        //             "assetId": 1,
        //             "blockNumber": 0,
        //             "chain": null,
        //             "etaTsSec": 1752758789,
        //             "id": 42688,
        //             "quantity": "6.130000",
        //             "status": "completed",
        //             "timestampSec": 1752758788,
        //             "token": null,
        //             "transactionHash": "0x8dcd7bd1155b5624fb5e38a1365888f712ec633a57434340e05080c70b0e3bba",
        //             "transactionType": "deposit"
        //         },
        //         {
        //             "assetId": 1,
        //             "etaTsSec": null,
        //             "id": 12993,
        //             "instantWithdrawalChain": null,
        //             "instantWithdrawalToken": null,
        //             "isInstantWithdrawal": false,
        //             "quantity": "0.111930",
        //             "status": "completed",
        //             "timestampSec": 1752387891,
        //             "transactionHash": "0x32ab5fe5b90f6d753bab83523ebc8465eb9daef54580e13cb9ff031d400c5620",
        //             "transactionType": "withdrawal",
        //             "withdrawalAddress": "0x43f15ef2ef2ab5e61e987ee3d652a5872aea8a6c"
        //         },
        //     ]
        // }
        let mut transactions: Value = self.safe_list(response.clone(), Value::from("transactions"), Value::Undefined);
        let mut withdrawals: Value = Value::new_array();
        let mut i: usize = 0;
        while i < transactions.len() {
            let mut transaction: Value = transactions.get(i.into());
            if self.safe_string(transaction.clone(), Value::from("transactionType"), Value::Undefined) == Value::from("withdrawal") {
                withdrawals.push(transaction.clone());
            };
            i += 1;
        };
        return self.parse_transactions(withdrawals.clone(), currency.clone(), since.clone(), limit.clone(), params.clone());
    }

    async fn fetch_time(&mut self, mut params: Value) -> Value {
        let candidates = vec![("public", "GET", "time"), ("public", "GET", "server/time"), ("public", "GET", "timestamp")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_open_interest(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("publicGetMarketDataOpenInterest".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //   { "totalQuantity" : "2.3299770166" }
        //
        let mut timestamp: Value = self.milliseconds();
        return self.safe_open_interest(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "openInterestAmount": self.safe_string(response.clone(), Value::from("totalQuantity"), Value::Undefined),
            "openInterestValue": Value::Undefined,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "info": response
        }))).unwrap()), market.clone());
    }

    async fn fetch_funding_rate(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("publicGetMarketDataPrices".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "askPrice": "3514.650296",
        //     "bidPrice": "3513.596112",
        //     "fundingRateEstimation": {
        //         "estimatedFundingRate": "0.000001",
        //         "nextFundingTimestamp": 1712707200
        //     },
        //     "markPrice": "3514.288858",
        //     "spotPrice": "3514.715000",
        //     "symbol": "ETH/USDT-P",
        //     "tradePrice": "2372.746570"
        // }
        //
        let mut funding: Value = self.safe_dict(response.clone(), Value::from("fundingRateEstimation"), Value::new_object());
        let mut timestamp: Value = self.milliseconds();
        let mut next_funding_timestamp: Value = self.safe_integer_product(funding.clone(), Value::from("nextFundingTimestamp"), Value::from(1000), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": funding,
            "symbol": market.get(Value::from("symbol")),
            "markPrice": Value::Undefined,
            "indexPrice": Value::Undefined,
            "interestRate": self.parse_number(Value::from("0"), Value::Undefined),
            "estimatedSettlePrice": Value::Undefined,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "fundingRate": self.safe_number(funding.clone(), Value::from("estimatedFundingRate"), Value::Undefined),
            "fundingTimestamp": next_funding_timestamp,
            "fundingDatetime": self.iso8601(next_funding_timestamp.clone()),
            "nextFundingRate": Value::Undefined,
            "nextFundingTimestamp": Value::Undefined,
            "nextFundingDatetime": Value::Undefined,
            "previousFundingRate": Value::Undefined,
            "previousFundingTimestamp": Value::Undefined,
            "previousFundingDatetime": Value::Undefined,
            "interval": "8h"
        }))).unwrap());
    }

    async fn fetch_funding_rate_history(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("publicGetMarketDataFundingRates".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "data": [
        //         {
        //             "contractId": 2,
        //             "fundingTimestamp": 1753488000,
        //             "fundingRate": "0.000137",
        //             "indexPrice": "117623.65010"
        //         }
        //     ]
        // }
        //
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::Undefined);
        let mut rates: Value = Value::new_array();
        let mut i: usize = 0;
        while i < data.len() {
            let mut entry: Value = data.get(i.into());
            let mut timestamp: Value = self.safe_integer_product(entry.clone(), Value::from("fundingTimestamp"), Value::from(1000), Value::Undefined);
            rates.push(Value::Json(normalize(&Value::Json(json!({
                "info": entry,
                "symbol": symbol,
                "fundingRate": self.safe_number(entry.clone(), Value::from("fundingRate"), Value::Undefined),
                "timestamp": timestamp,
                "datetime": self.iso8601(timestamp.clone())
            }))).unwrap()));
            i += 1;
        };
        let mut sorted: Value = self.sort_by(rates.clone(), Value::from("timestamp"), Value::Undefined, Value::Undefined);
        return self.filter_by_symbol_since_limit(sorted.clone(), symbol.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetMarketexchangeinfo" => self.request("market/exchange-info".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketdatatrades" => self.request("market/data/trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketdataprices" => self.request("market/data/prices".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketdatastats" => self.request("market/data/stats".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketdataklines" => self.request("market/data/klines".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketdataorderbook" => self.request("market/data/orderbook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketdataopeninterest" => self.request("market/data/open-interest".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketdatafundingrates" => self.request("market/data/funding-rates".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetExchangeutctimestamp" => self.request("exchange/utc-timestamp".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCapitaldepositinfo" => self.request("capital/deposit-info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCapitalhistory" => self.request("capital/history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeaccounttradinghistory" => self.request("trade/account/trading_history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeaccountinfo" => self.request("trade/account/info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorder" => self.request("trade/order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeaccounttrades" => self.request("trade/account/trades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorders" => self.request("trade/orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePutTradeorder" => self.request("trade/order".into(), "private".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteTradeorder" => self.request("trade/order".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteTradeorders" => self.request("trade/orders".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeorder" => self.request("trade/order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeorders" => self.request("trade/orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCapitalwithdraw" => self.request("capital/withdraw".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct HibachiImpl(Value);
impl Exchange for HibachiImpl {}
impl Hibachi for HibachiImpl {}
impl ValueTrait for HibachiImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl HibachiImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = HibachiImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Hibachi::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


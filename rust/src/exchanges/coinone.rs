#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, JSON, Array, Object, Math, parse_int, shift_2, extend_2, normalize};

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Coinone : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "coinone",
            "name": "CoinOne",
            "countries": [
                "KR"
            ],
            "enableRateLimit": true,
            "rateLimit": 50,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": false,
                "swap": false,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "borrowMargin": false,
                "cancelOrder": true,
                "closeAllPositions": false,
                "closePosition": false,
                "createLimitOrder": true,
                "createMarketOrder": false,
                "createMarketOrderWs": true,
                "createOrder": true,
                "createOrderWithTakeProfitAndStopLoss": false,
                "createOrderWithTakeProfitAndStopLossWs": false,
                "createPostOnlyOrder": false,
                "createReduceOnlyOrder": false,
                "createStopLimitOrder": false,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchClosedOrders": false,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": false,
                "fetchDepositAddresses": true,
                "fetchDepositAddressesByNetwork": false,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchIsolatedPositions": false,
                "fetchL2OrderBook": true,
                "fetchLeverage": false,
                "fetchLeverages": false,
                "fetchLeverageTiers": false,
                "fetchLiquidations": false,
                "fetchLongShortRatio": false,
                "fetchLongShortRatioHistory": false,
                "fetchMarginMode": false,
                "fetchMarginModes": false,
                "fetchMarketLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyLiquidations": false,
                "fetchMySettlementHistory": false,
                "fetchMyTrades": true,
                "fetchOpenInterest": false,
                "fetchOpenInterests": false,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchPosition": false,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": false,
                "fetchPositionsForSymbol": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchSettlementHistory": false,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchMarkPrices": false,
                "fetchTrades": true,
                "fetchVolatilityHistory": false,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "setLeverage": false,
                "setMargin": false,
                "setMarginMode": false,
                "setPositionMode": false,
                "ws": true,
                "repayMargin": false
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/1294454/38003300-adc12fba-323f-11e8-8525-725f53c4a659.jpg",
                "api": {
                    "rest": "https://api.coinone.co.kr",
                    "v2Public": "https://api.coinone.co.kr/public/v2",
                    "v2Private": "https://api.coinone.co.kr/v2",
                    "v2_1Private": "https://api.coinone.co.kr/v2.1"
                },
                "www": "https://coinone.co.kr",
                "doc": "https://doc.coinone.co.kr"
            },
            "api": {
                "public": {
                    "get": [
                        "orderbook",
                        "ticker",
                        "ticker_utc",
                        "trades"
                    ]
                },
                "v2Public": {
                    "get": [
                        "range_units",
                        "markets/{quote_currency}",
                        "markets/{quote_currency}/{target_currency}",
                        "orderbook/{quote_currency}/{target_currency}",
                        "trades/{quote_currency}/{target_currency}",
                        "ticker_new/{quote_currency}",
                        "ticker_new/{quote_currency}/{target_currency}",
                        "ticker_utc_new/{quote_currency}",
                        "ticker_utc_new/{quote_currency}/{target_currency}",
                        "currencies",
                        "currencies/{currency}",
                        "chart/{quote_currency}/{target_currency}"
                    ]
                },
                "private": {
                    "post": [
                        "account/deposit_address",
                        "account/btc_deposit_address",
                        "account/balance",
                        "account/daily_balance",
                        "account/user_info",
                        "account/virtual_account",
                        "order/cancel_all",
                        "order/cancel",
                        "order/limit_buy",
                        "order/limit_sell",
                        "order/complete_orders",
                        "order/limit_orders",
                        "order/order_info",
                        "transaction/auth_number",
                        "transaction/history",
                        "transaction/krw/history",
                        "transaction/btc",
                        "transaction/coin"
                    ]
                },
                "v2Private": {
                    "post": [
                        "account/balance",
                        "account/deposit_address",
                        "account/user_info",
                        "account/virtual_account",
                        "order/cancel",
                        "order/limit_buy",
                        "order/limit_sell",
                        "order/limit_orders",
                        "order/complete_orders",
                        "order/query_order",
                        "transaction/auth_number",
                        "transaction/btc",
                        "transaction/history",
                        "transaction/krw/history"
                    ]
                },
                "v2_1Private": {
                    "post": [
                        "account/balance/all",
                        "account/balance",
                        "account/trade_fee",
                        "account/trade_fee/{quote_currency}/{target_currency}",
                        "order/limit",
                        "order/cancel",
                        "order/cancel/all",
                        "order/open_orders",
                        "order/open_orders/all",
                        "order/complete_orders",
                        "order/complete_orders/all",
                        "order/info",
                        "transaction/krw/history",
                        "transaction/coin/history",
                        "transaction/coin/withdrawal/limit"
                    ]
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "fees": {
                "trading": {
                    "tierBased": false,
                    "percentage": true,
                    "taker": 0.002,
                    "maker": 0.002
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {},
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "SOC": "Soda Coin"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v2",
            "features": {
                "spot": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": false,
                            "FOK": false,
                            "PO": false,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": false,
                        "iceberg": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": true
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    }
                },
                "swap": {},
                "future": {}
            }
        }"###).unwrap())
    }

    async fn fetch_currencies(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.v2_public_get_currencies(params.clone()).await;
        //
        //     {
        //         "result": "success",
        //         "error_code": "0",
        //         "server_time": 1701054555578,
        //         "currencies": [
        //           {
        //             "name": "Polygon",
        //             "symbol": "MATIC",
        //             "deposit_status": "normal",
        //             "withdraw_status": "normal",
        //             "deposit_confirm_count": 150,
        //             "max_precision": 8,
        //             "deposit_fee": "0.0",
        //             "withdrawal_min_amount": "1.0",
        //             "withdrawal_fee": "3.0"
        //           }
        //         ]
        //     }
        //
        let mut result: Value = Value::new_object();
        let mut currencies: Value = self.safe_list(response.clone(), Value::from("currencies"), Value::new_array());
        let mut i: usize = 0;
        while i < currencies.len() {
            let mut entry: Value = currencies.get(i.into());
            let mut id: Value = self.safe_string(entry.clone(), Value::from("symbol"));
            let mut code: Value = self.safe_currency_code(id.clone(), Value::Undefined);
            let mut is_withdraw_enabled: Value = (self.safe_string(entry.clone(), Value::from("withdraw_status"), Value::from("")) == Value::from("normal")).into();
            let mut is_deposit_enabled: Value = (self.safe_string(entry.clone(), Value::from("deposit_status"), Value::from("")) == Value::from("normal")).into();
            let mut r#type: Value = if code.clone() != Value::from("KRW") { Value::from("crypto") } else { Value::from("fiat") };
            result.set(code.clone(), self.safe_currency_structure(Value::Json(normalize(&Value::Json(json!({
                "id": id,
                "code": code,
                "info": entry,
                "name": self.safe_string(entry.clone(), Value::from("name")),
                "active": Value::Undefined,
                "deposit": is_deposit_enabled,
                "withdraw": is_withdraw_enabled,
                "fee": self.safe_number(entry.clone(), Value::from("withdrawal_fee"), Value::Undefined),
                "precision": self.parse_number(self.parse_precision(self.safe_string(entry.clone(), Value::from("max_precision"))), Value::Undefined),
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "amount": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "withdraw": Value::Json(normalize(&Value::Json(json!({
                        "min": self.safe_number(entry.clone(), Value::from("withdrawal_min_amount"), Value::Undefined),
                        "max": Value::Undefined
                    }))).unwrap())
                }))).unwrap()),
                "networks": Value::new_object(),
                "type": r#type
            }))).unwrap())));
            i += 1;
        };
        return result.clone();
    }

    async fn fetch_markets(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "quote_currency": "KRW"
        }))).unwrap());
        let mut response: Value = self.v2_public_get_ticker_new_quote_currency(request.clone()).await;
        //
        //     {
        //         "result": "success",
        //         "error_code": "0",
        //         "server_time": 1701067923060,
        //         "tickers": [
        //             {
        //                 "quote_currency": "krw",
        //                 "target_currency": "stg",
        //                 "timestamp": 1701067920001,
        //                 "high": "667.5",
        //                 "low": "667.5",
        //                 "first": "667.5",
        //                 "last": "667.5",
        //                 "quote_volume": "0.0",
        //                 "target_volume": "0.0",
        //                 "best_asks": [
        //                     {
        //                         "price": "777.0",
        //                         "qty": "73.9098"
        //                     }
        //                 ],
        //                 "best_bids": [
        //                     {
        //                         "price": "690.8",
        //                         "qty": "40.7768"
        //                     }
        //                 ],
        //                 "id": "1701067920001001"
        //             }
        //         ]
        //     }
        //
        let mut tickers: Value = self.safe_list(response.clone(), Value::from("tickers"), Value::new_array());
        let mut result: Value = Value::new_array();
        let mut i: usize = 0;
        while i < tickers.len() {
            let mut entry: Value = self.safe_value(tickers.clone(), Value::from(i));
            let mut id: Value = self.safe_string(entry.clone(), Value::from("id"));
            let mut base_id: Value = self.safe_string_upper(entry.clone(), Value::from("target_currency"));
            let mut quote_id: Value = self.safe_string_upper(entry.clone(), Value::from("quote_currency"));
            let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
            let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
            result.push(Value::Json(normalize(&Value::Json(json!({
                "id": id,
                "symbol": base.clone() + Value::from("/") + quote.clone(),
                "base": base,
                "quote": quote,
                "settle": Value::Undefined,
                "baseId": base_id,
                "quoteId": quote_id,
                "settleId": Value::Undefined,
                "type": "spot",
                "spot": true,
                "margin": false,
                "swap": false,
                "future": false,
                "option": false,
                "active": Value::Undefined,
                "contract": false,
                "linear": Value::Undefined,
                "inverse": Value::Undefined,
                "contractSize": Value::Undefined,
                "expiry": Value::Undefined,
                "expiryDatetime": Value::Undefined,
                "strike": Value::Undefined,
                "optionType": Value::Undefined,
                "precision": Value::Json(normalize(&Value::Json(json!({
                    "amount": self.parse_number(Value::from("1e-4"), Value::Undefined),
                    "price": self.parse_number(Value::from("1e-4"), Value::Undefined),
                    "cost": self.parse_number(Value::from("1e-8"), Value::Undefined)
                }))).unwrap()),
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "leverage": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "amount": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "price": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "cost": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap())
                }))).unwrap()),
                "created": Value::Undefined,
                "info": entry
            }))).unwrap()));
            i += 1;
        };
        return result.clone();
    }

    fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response
        }))).unwrap());
        let mut balances: Value = self.omit(response.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("errorCode").into(), Value::from("result").into(), Value::from("normalWallets").into()])));
        let mut currency_ids: Value = Object::keys(balances.clone());
        let mut i: usize = 0;
        while i < currency_ids.len() {
            let mut currency_id: Value = currency_ids.get(i.into());
            let mut balance: Value = balances.get(currency_id.clone());
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut account: Value = self.account();
            account.set("free".into(), self.safe_string(balance.clone(), Value::from("avail")));
            account.set("total".into(), self.safe_string(balance.clone(), Value::from("balance")));
            result.set(code.clone(), account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.v2_private_post_account_balance(params.clone()).await;
        return <Self as Coinone>::parse_balance(self, response.clone());
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Coinone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = <Self as Coinone>::request(self,path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Coinone>::request(self,path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Coinone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = <Self as Coinone>::request(self,path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Coinone>::request(self,path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Coinone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = <Self as Coinone>::request(self,path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Coinone>::request(self,path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     {
        //         "quote_currency": "krw",
        //         "target_currency": "btc",
        //         "timestamp": 1701073357818,
        //         "high": "50543000.0",
        //         "low": "49945000.0",
        //         "first": "50487000.0",
        //         "last": "50062000.0",
        //         "quote_volume": "11349804285.3859",
        //         "target_volume": "226.07268994",
        //         "best_asks": [
        //             {
        //                 "price": "50081000.0",
        //                 "qty": "0.18471358"
        //             }
        //         ],
        //         "best_bids": [
        //             {
        //                 "price": "50062000.0",
        //                 "qty": "0.04213455"
        //             }
        //         ],
        //         "id": "1701073357818001"
        //     }
        //
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("timestamp"));
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("last"));
        let mut asks: Value = self.safe_list(ticker.clone(), Value::from("best_asks"), Value::new_array());
        let mut bids: Value = self.safe_list(ticker.clone(), Value::from("best_bids"), Value::new_array());
        let mut base_id: Value = self.safe_string(ticker.clone(), Value::from("target_currency"));
        let mut quote_id: Value = self.safe_string(ticker.clone(), Value::from("quote_currency"));
        let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": base.clone() + Value::from("/") + quote.clone(),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "high": self.safe_string(ticker.clone(), Value::from("high")),
            "low": self.safe_string(ticker.clone(), Value::from("low")),
            "bid": self.safe_string(bids.clone(), Value::from("price")),
            "bidVolume": self.safe_string(bids.clone(), Value::from("qty")),
            "ask": self.safe_string(asks.clone(), Value::from("price")),
            "askVolume": self.safe_string(asks.clone(), Value::from("qty")),
            "vwap": Value::Undefined,
            "open": self.safe_string(ticker.clone(), Value::from("first")),
            "close": last,
            "last": last,
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": self.safe_string(ticker.clone(), Value::from("target_volume")),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("quote_volume")),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    fn parse_trade(&mut self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchTrades (public)
        //
        //     {
        //         "id": "1701075265708001",
        //         "timestamp": 1701075265708,
        //         "price": "50020000",
        //         "qty": "0.00155177",
        //         "is_seller_maker": false
        //     }
        //
        // fetchMyTrades (private)
        //
        //     {
        //         "timestamp": "1416561032",
        //         "price": "419000.0",
        //         "type": "bid",
        //         "qty": "0.001",
        //         "feeRate": "-0.0015",
        //         "fee": "-0.0000015",
        //         "orderId": "E84A1AC2-8088-4FA0-B093-A3BCDB9B3C85"
        //     }
        //
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("timestamp"));
        market = self.safe_market(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined);
        let mut is_seller_maker: Value = self.safe_bool(trade.clone(), Value::from("is_seller_maker"), Value::Undefined);
        let mut side: Value = Value::Undefined;
        if is_seller_maker.clone().is_nonnullish() {
            side = if is_seller_maker.is_truthy() { Value::from("sell") } else { Value::from("buy") };
        };
        let mut price_string: Value = self.safe_string(trade.clone(), Value::from("price"));
        let mut amount_string: Value = self.safe_string(trade.clone(), Value::from("qty"));
        let mut order_id: Value = self.safe_string(trade.clone(), Value::from("orderId"));
        let mut fee_cost_string: Value = self.safe_string(trade.clone(), Value::from("fee"));
        let mut fee: Value = Value::Undefined;
        if fee_cost_string.clone().is_nonnullish() {
            fee_cost_string = Precise::string_abs(fee_cost_string.clone());
            let mut fee_rate_string: Value = self.safe_string(trade.clone(), Value::from("feeRate"));
            fee_rate_string = Precise::string_abs(fee_rate_string.clone());
            let mut fee_currency_code: Value = if side.clone() == Value::from("sell") { market.get(Value::from("quote")) } else { market.get(Value::from("base")) };
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_cost_string,
                "currency": fee_currency_code,
                "rate": fee_rate_string
            }))).unwrap());
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(trade.clone(), Value::from("id")),
            "info": trade,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "order": order_id,
            "symbol": market.get(Value::from("symbol")),
            "type": Value::Undefined,
            "side": side,
            "takerOrMaker": Value::Undefined,
            "price": price_string,
            "amount": amount_string,
            "cost": Value::Undefined,
            "fee": fee
        }))).unwrap()), market.clone());
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        for (api_name, method_name, path_name) in candidates {
            let rv = <Self as Coinone>::request(self,path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if r#type.clone() != Value::from("limit") {
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" createOrder() allows limit orders only"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "price": price,
            "currency": market.get(Value::from("id")),
            "qty": amount
        }))).unwrap());
        let mut method: Value = Value::from("privatePostOrder") + self.capitalize(r#type.clone()) + self.capitalize(side.clone());
        let mut response: Value = self.dispatch(method, extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0",
        //         "orderId": "8a82c561-40b4-4cb3-9bc0-9ac9ffc1d63b"
        //     }
        //
        return <Self as Coinone>::parse_order(self, response.clone(), market.clone());
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" fetchOrder() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id,
            "currency": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.v2_private_post_order_query_order(extend_2(request.clone(), params.clone())).await;
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0",
        //         "orderId": "0e3019f2-1e4d-11e9-9ec7-00e04c3600d7",
        //         "baseCurrency": "KRW",
        //         "targetCurrency": "BTC",
        //         "price": "10011000.0",
        //         "originalQty": "3.0",
        //         "executedQty": "0.62",
        //         "canceledQty": "1.125",
        //         "remainQty": "1.255",
        //         "status": "partially_filled",
        //         "side": "bid",
        //         "orderedAt": 1499340941,
        //         "updatedAt": 1499341142,
        //         "feeRate": "0.002",
        //         "fee": "0.00124",
        //         "averageExecutedPrice": "10011000.0"
        //     }
        //
        return <Self as Coinone>::parse_order(self, response.clone(), market.clone());
    }

    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "live": "open",
            "partially_filled": "open",
            "partially_canceled": "open",
            "filled": "closed",
            "canceled": "canceled"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_order(&mut self, mut order: Value, mut market: Value) -> Value {
        //
        // createOrder
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0",
        //         "orderId": "8a82c561-40b4-4cb3-9bc0-9ac9ffc1d63b"
        //     }
        //
        // fetchOrder
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0",
        //         "orderId": "0e3019f2-1e4d-11e9-9ec7-00e04c3600d7",
        //         "baseCurrency": "KRW",
        //         "targetCurrency": "BTC",
        //         "price": "10011000.0",
        //         "originalQty": "3.0",
        //         "executedQty": "0.62",
        //         "canceledQty": "1.125",
        //         "remainQty": "1.255",
        //         "status": "partially_filled",
        //         "side": "bid",
        //         "orderedAt": 1499340941,
        //         "updatedAt": 1499341142,
        //         "feeRate": "0.002",
        //         "fee": "0.00124",
        //         "averageExecutedPrice": "10011000.0"
        //     }
        //
        // fetchOpenOrders
        //
        //     {
        //         "index": "0",
        //         "orderId": "68665943-1eb5-4e4b-9d76-845fc54f5489",
        //         "timestamp": "1449037367",
        //         "price": "444000.0",
        //         "qty": "0.3456",
        //         "type": "ask",
        //         "feeRate": "-0.0015"
        //     }
        //
        let mut id: Value = self.safe_string(order.clone(), Value::from("orderId"));
        let mut base_id: Value = self.safe_string(order.clone(), Value::from("baseCurrency"));
        let mut quote_id: Value = self.safe_string(order.clone(), Value::from("targetCurrency"));
        let mut base: Value = Value::Undefined;
        let mut quote: Value = Value::Undefined;
        if base_id.clone().is_nonnullish() {
            base = self.safe_currency_code(base_id.clone(), Value::Undefined);
        };
        if quote_id.clone().is_nonnullish() {
            quote = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        };
        let mut symbol: Value = Value::Undefined;
        if base.clone().is_nonnullish() && quote.clone().is_nonnullish() {
            symbol = base.clone() + Value::from("/") + quote.clone();
            market = self.safe_market(symbol.clone(), market.clone(), Value::from("/"), Value::Undefined);
        };
        let mut timestamp: Value = self.safe_timestamp_2(order.clone(), Value::from("timestamp"), Value::from("updatedAt"));
        let mut side: Value = self.safe_string_2(order.clone(), Value::from("type"), Value::from("side"));
        if side.clone() == Value::from("ask") {
            side = Value::from("sell");
        } else if side.clone() == Value::from("bid") {
            side = Value::from("buy");
        };
        let mut remaining_string: Value = self.safe_string(order.clone(), Value::from("remainQty"));
        let mut amount_string: Value = self.safe_string_2(order.clone(), Value::from("originalQty"), Value::from("qty"));
        let mut status: Value = self.safe_string(order.clone(), Value::from("status"));
        // https://github.com/ccxt/ccxt/pull/7067
        if status.clone() == Value::from("live") {
            if remaining_string.clone().is_nonnullish() && amount_string.clone().is_nonnullish() {
                let mut is_less_than: Value = Precise::string_lt(remaining_string.clone(), amount_string.clone());
                if is_less_than.is_truthy() {
                    status = Value::from("canceled");
                };
            };
        };
        status = <Self as Coinone>::parse_order_status(self, status.clone());
        let mut fee: Value = Value::Undefined;
        let mut fee_cost_string: Value = self.safe_string(order.clone(), Value::from("fee"));
        if fee_cost_string.clone().is_nonnullish() {
            let mut fee_currency_code: Value = if side.clone() == Value::from("sell") { quote.clone() } else { base.clone() };
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_cost_string,
                "rate": self.safe_string(order.clone(), Value::from("feeRate")),
                "currency": fee_currency_code
            }))).unwrap());
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "id": id,
            "clientOrderId": Value::Undefined,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": Value::Undefined,
            "symbol": symbol,
            "type": "limit",
            "timeInForce": Value::Undefined,
            "postOnly": Value::Undefined,
            "side": side,
            "price": self.safe_string(order.clone(), Value::from("price")),
            "triggerPrice": Value::Undefined,
            "cost": Value::Undefined,
            "average": self.safe_string(order.clone(), Value::from("averageExecutedPrice")),
            "amount": amount_string,
            "filled": self.safe_string(order.clone(), Value::from("executedQty")),
            "remaining": remaining_string,
            "status": status,
            "fee": fee,
            "trades": Value::Undefined
        }))).unwrap()), market.clone());
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        // The returned amount might not be same as the ordered amount. If an order is partially filled, the returned amount means the remaining amount.
        // For the same reason, the returned amount and remaining are always same, and the returned filled and cost are always zero.
        if symbol.clone().is_nullish() {
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" fetchOpenOrders() allows fetching closed orders with a specific symbol"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "currency": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.private_post_order_limit_orders(extend_2(request.clone(), params.clone())).await;
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0",
        //         "limitOrders": [
        //             {
        //                 "index": "0",
        //                 "orderId": "68665943-1eb5-4e4b-9d76-845fc54f5489",
        //                 "timestamp": "1449037367",
        //                 "price": "444000.0",
        //                 "qty": "0.3456",
        //                 "type": "ask",
        //                 "feeRate": "-0.0015"
        //             }
        //         ]
        //     }
        //
        let mut limit_orders: Value = self.safe_list(response.clone(), Value::from("limitOrders"), Value::new_array());
        return self.parse_orders(limit_orders.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" fetchMyTrades() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "currency": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.v2_private_post_order_complete_orders(extend_2(request.clone(), params.clone())).await;
        //
        // despite the name of the endpoint it returns trades which may have a duplicate orderId
        // https://github.com/ccxt/ccxt/pull/7067
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0",
        //         "completeOrders": [
        //             {
        //                 "timestamp": "1416561032",
        //                 "price": "419000.0",
        //                 "type": "bid",
        //                 "qty": "0.001",
        //                 "feeRate": "-0.0015",
        //                 "fee": "-0.0000015",
        //                 "orderId": "E84A1AC2-8088-4FA0-B093-A3BCDB9B3C85"
        //             }
        //         ]
        //     }
        //
        let mut complete_orders: Value = self.safe_list(response.clone(), Value::from("completeOrders"), Value::new_array());
        return self.parse_trades(complete_orders.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            // eslint-disable-next-line quotes
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" cancelOrder() requires a symbol argument. To cancel the order, pass a symbol argument and {'price': 12345, 'qty': 1.2345, 'is_ask': 0} in the params argument of cancelOrder."))"###);
        };
        let mut price: Value = self.safe_number(params.clone(), Value::from("price"), Value::Undefined);
        let mut qty: Value = self.safe_number(params.clone(), Value::from("qty"), Value::Undefined);
        let mut is_ask: Value = self.safe_integer(params.clone(), Value::from("is_ask"));
        if price.clone().is_nullish() || qty.clone().is_nullish() || is_ask.clone().is_nullish() {
            // eslint-disable-next-line quotes
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" cancelOrder() requires {'price': 12345, 'qty': 1.2345, 'is_ask': 0} in the params argument."))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id,
            "price": price,
            "qty": qty,
            "is_ask": is_ask,
            "currency": self.market_id(symbol.clone())
        }))).unwrap());
        let mut response: Value = self.v2_private_post_order_cancel(extend_2(request.clone(), params.clone())).await;
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0"
        //     }
        //
        return self.safe_order(response.clone(), Value::Undefined);
    }

    async fn fetch_deposit_addresses(&mut self, mut codes: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.v2_private_post_account_deposit_address(params.clone()).await;
        //
        //     {
        //         "result": "success",
        //         "errorCode": "0",
        //         "walletAddress": {
        //             "matic": null,
        //             "btc": "mnobqu4i6qMCJWDpf5UimRmr8JCvZ8FLcN",
        //             "xrp": null,
        //             "xrp_tag": "-1",
        //             "kava": null,
        //             "kava_memo": null,
        //         }
        //     }
        //
        let mut wallet_address: Value = self.safe_dict(response.clone(), Value::from("walletAddress"), Value::new_object());
        let mut keys: Value = Object::keys(wallet_address.clone());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < keys.len() {
            let mut key: Value = keys.get(i.into());
            let mut value: Value = wallet_address.get(key.clone());
            if !value.is_truthy() || value.clone() == Value::from("-1") {
                continue;
            };
            let mut parts: Value = key.split(Value::from("_"));
            let mut currency_id: Value = self.safe_value(parts.clone(), Value::from(0));
            let mut second_part: Value = self.safe_value(parts.clone(), Value::from(1));
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut deposit_address: Value = self.safe_value(result.clone(), code.clone());
            if deposit_address.clone().is_nullish() {
                deposit_address = Value::Json(normalize(&Value::Json(json!({
                    "info": value,
                    "currency": code,
                    "network": Value::Undefined,
                    "address": Value::Undefined,
                    "tag": Value::Undefined
                }))).unwrap());
            };
            let mut address: Value = self.safe_string(deposit_address.clone(), Value::from("address"), value.clone());
            self.check_address(address.clone());
            deposit_address.set("address".into(), address.clone());
            deposit_address.set("info".into(), address.clone());
            if second_part.clone() == Value::from("tag") || second_part.clone() == Value::from("memo") {
                deposit_address.set("tag".into(), value.clone());
                deposit_address.set("info".into(), Value::Json(serde_json::Value::Array(vec![address.clone().into(), value.clone().into()])));
            };
            result.set(code.clone(), deposit_address.clone());
            i += 1;
        };
        return result.clone();
    }

    fn sign(&mut self, mut path: Value, mut api: Value, mut method: Value, mut params: Value, mut headers: Value, mut body: Value) -> Value {
        api = api.or_default(Value::from("public"));
        method = method.or_default(Value::from("GET"));
        params = params.or_default(Value::new_object());
        let mut request: Value = self.implode_params(path.clone(), params.clone());
        let mut query: Value = self.omit(params.clone(), self.extract_params(path.clone()));
        let mut url: Value = self.get("urls".into()).get(Value::from("api")).get(Value::from("rest")) + Value::from("/");
        if api.clone() == Value::from("v2Public") {
            url = self.get("urls".into()).get(Value::from("api")).get(Value::from("v2Public")) + Value::from("/");
            api = Value::from("public");
        } else if api.clone() == Value::from("v2Private") {
            url = self.get("urls".into()).get(Value::from("api")).get(Value::from("v2Private")) + Value::from("/");
        } else if api.clone() == Value::from("v2_1Private") {
            url = self.get("urls".into()).get(Value::from("api")).get(Value::from("v2_1Private")) + Value::from("/");
        };
        if api.clone() == Value::from("public") {
            url = url +  request.clone();
            if Object::keys(query.clone()).len() > 0 {
                url = url +  Value::from("?") + self.urlencode(query.clone());
            };
        } else {
            self.check_required_credentials(Value::Undefined);
            url = url +  request.clone();
            let mut nonce: Value = self.nonce().to_string();
            let mut json: Value = self.json(extend_2(Value::Json(normalize(&Value::Json(json!({
                "access_token": self.get("apiKey".into()),
                "nonce": nonce
            }))).unwrap()), params.clone()));
            let mut payload: Value = self.string_to_base64(json.clone());
            body = payload.clone();
            let mut secret: Value = self.get("secret".into()).to_upper_case();
            let mut signature: Value = self.hmac(self.encode(payload.clone()), self.encode(secret.clone()), sha512.clone());
            headers = Value::Json(normalize(&Value::Json(json!({
                "Content-Type": "application/json",
                "X-COINONE-PAYLOAD": payload,
                "X-COINONE-SIGNATURE": signature
            }))).unwrap());
        };
        return Value::Json(normalize(&Value::Json(json!({
            "url": url,
            "method": method,
            "body": body,
            "headers": headers
        }))).unwrap());
    }

    fn handle_errors(&mut self, mut code: Value, mut reason: Value, mut url: Value, mut method: Value, mut headers: Value, mut body: Value, mut response: Value, mut request_headers: Value, mut request_body: Value) -> Value {
        if response.clone().is_nullish() {
            return Value::Undefined;
        };
        // fallback to default error handler
        //
        //     {"result":"error","error_code":"107","error_msg":"Parameter value is wrong"}
        //     {"result":"error","error_code":"108","error_msg":"Unknown CryptoCurrency"}
        //
        let mut error_code: Value = self.safe_string(response.clone(), Value::from("error_code"));
        if error_code.clone().is_nonnullish() && error_code.clone() != Value::from("0") {
            let mut feedback: Value = self.get("id".into()) + Value::from(" ") + body.clone();
            self.throw_exactly_matched_exception(self.get("exceptions".into()), error_code.clone(), feedback.clone());
            panic!(r###"ExchangeError::new(feedback)"###);
        };
        // unknown message
        return Value::Undefined;
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetOrderbook" => Coinone::request(self, "orderbook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTicker" => Coinone::request(self, "ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTickerutc" => Coinone::request(self, "ticker_utc".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTrades" => Coinone::request(self, "trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetRangeunits" => Coinone::request(self, "range_units".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetMarketsquotecurrency" => Coinone::request(self, "markets/{quote_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetMarketsquotecurrencytargetcurrency" => Coinone::request(self, "markets/{quote_currency}/{target_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetOrderbookquotecurrencytargetcurrency" => Coinone::request(self, "orderbook/{quote_currency}/{target_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetTradesquotecurrencytargetcurrency" => Coinone::request(self, "trades/{quote_currency}/{target_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetTickernewquotecurrency" => Coinone::request(self, "ticker_new/{quote_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetTickernewquotecurrencytargetcurrency" => Coinone::request(self, "ticker_new/{quote_currency}/{target_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetTickerutcnewquotecurrency" => Coinone::request(self, "ticker_utc_new/{quote_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetTickerutcnewquotecurrencytargetcurrency" => Coinone::request(self, "ticker_utc_new/{quote_currency}/{target_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetCurrencies" => Coinone::request(self, "currencies".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetCurrenciescurrency" => Coinone::request(self, "currencies/{currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2publicGetChartquotecurrencytargetcurrency" => Coinone::request(self, "chart/{quote_currency}/{target_currency}".into(), "v2Public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountdepositaddress" => Coinone::request(self, "account/deposit_address".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountbtcdepositaddress" => Coinone::request(self, "account/btc_deposit_address".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountbalance" => Coinone::request(self, "account/balance".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountdailybalance" => Coinone::request(self, "account/daily_balance".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountuserinfo" => Coinone::request(self, "account/user_info".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountvirtualaccount" => Coinone::request(self, "account/virtual_account".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdercancelall" => Coinone::request(self, "order/cancel_all".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdercancel" => Coinone::request(self, "order/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrderlimitbuy" => Coinone::request(self, "order/limit_buy".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrderlimitsell" => Coinone::request(self, "order/limit_sell".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdercompleteorders" => Coinone::request(self, "order/complete_orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrderlimitorders" => Coinone::request(self, "order/limit_orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrderorderinfo" => Coinone::request(self, "order/order_info".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTransactionauthnumber" => Coinone::request(self, "transaction/auth_number".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTransactionhistory" => Coinone::request(self, "transaction/history".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTransactionkrwhistory" => Coinone::request(self, "transaction/krw/history".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTransactionbtc" => Coinone::request(self, "transaction/btc".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTransactioncoin" => Coinone::request(self, "transaction/coin".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostAccountbalance" => Coinone::request(self, "account/balance".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostAccountdepositaddress" => Coinone::request(self, "account/deposit_address".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostAccountuserinfo" => Coinone::request(self, "account/user_info".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostAccountvirtualaccount" => Coinone::request(self, "account/virtual_account".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostOrdercancel" => Coinone::request(self, "order/cancel".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostOrderlimitbuy" => Coinone::request(self, "order/limit_buy".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostOrderlimitsell" => Coinone::request(self, "order/limit_sell".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostOrderlimitorders" => Coinone::request(self, "order/limit_orders".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostOrdercompleteorders" => Coinone::request(self, "order/complete_orders".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostOrderqueryorder" => Coinone::request(self, "order/query_order".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostTransactionauthnumber" => Coinone::request(self, "transaction/auth_number".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostTransactionbtc" => Coinone::request(self, "transaction/btc".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostTransactionhistory" => Coinone::request(self, "transaction/history".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v2privatePostTransactionkrwhistory" => Coinone::request(self, "transaction/krw/history".into(), "v2Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostAccountbalanceall" => Coinone::request(self, "account/balance/all".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostAccountbalance" => Coinone::request(self, "account/balance".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostAccounttradefee" => Coinone::request(self, "account/trade_fee".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostAccounttradefeequotecurrencytargetcurrency" => Coinone::request(self, "account/trade_fee/{quote_currency}/{target_currency}".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrderlimit" => Coinone::request(self, "order/limit".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrdercancel" => Coinone::request(self, "order/cancel".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrdercancelall" => Coinone::request(self, "order/cancel/all".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrderopenorders" => Coinone::request(self, "order/open_orders".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrderopenordersall" => Coinone::request(self, "order/open_orders/all".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrdercompleteorders" => Coinone::request(self, "order/complete_orders".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrdercompleteordersall" => Coinone::request(self, "order/complete_orders/all".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostOrderinfo" => Coinone::request(self, "order/info".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostTransactionkrwhistory" => Coinone::request(self, "transaction/krw/history".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostTransactioncoinhistory" => Coinone::request(self, "transaction/coin/history".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v21privatePostTransactioncoinwithdrawallimit" => Coinone::request(self, "transaction/coin/withdrawal/limit".into(), "v2_1Private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => unimplemented!(),
                }
            },
            _ => unimplemented!()
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct CoinoneImpl(Value);
impl Exchange for CoinoneImpl {}
impl Coinone for CoinoneImpl {}
impl ValueTrait for CoinoneImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Vec<Value> { self.0.keys() }
    fn values(&self) -> Vec<Value> { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value) -> Value { self.0.slice(start) }
}

impl CoinoneImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = CoinoneImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Coinone::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


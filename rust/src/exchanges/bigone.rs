#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
use crate::ws::{ArrayCache, ArrayCacheByTimestamp, ArrayCacheBySymbolById, ArrayCacheBySymbolBySide};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors (stub structs with ::new for transpiled `new ErrorType(msg)` patterns)
macro_rules! error_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(msg: Value) -> Value { msg } } }; }
error_stub!(BadRequest);
error_stub!(InvalidOrder);
error_stub!(ExchangeError);
error_stub!(InsufficientFunds);
error_stub!(OrderNotFound);
error_stub!(AuthenticationError);
error_stub!(PermissionDenied);
error_stub!(ExchangeNotAvailable);
error_stub!(ArgumentsRequired);
error_stub!(RateLimitExceeded);
error_stub!(OrderNotFillable);
error_stub!(OrderImmediatelyFillable);
error_stub!(NotSupported);
error_stub!(DuplicateOrderId);
error_stub!(UnsubscribeError);
error_stub!(ChecksumError);
error_stub!(InvalidNonce);
error_stub!(BadSymbol);
// Array-like type stubs (Bids/Asks order book sides)
macro_rules! array_side_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(data: Value, _limit: Value) -> Value { data } } }; }
array_side_stub!(Bids);
array_side_stub!(Asks);

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait(?Send)]
pub trait Bigone : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "bigone",
            "name": "BigONE",
            "countries": [
                "CN"
            ],
            "enableRateLimit": true,
            "rateLimit": 20,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": false,
                "swap": true,
                "option": false,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "borrowMargin": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createPostOnlyOrder": true,
                "createStopLimitOrder": true,
                "createStopMarketOrder": true,
                "createStopOrder": true,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchMarkets": true,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": false,
                "fetchTransactionFees": false,
                "fetchVolatilityHistory": false,
                "fetchWithdrawals": true,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "transfer": true,
                "withdraw": true,
                "fetchAllGreeks": false
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/4e5cfd53-98cc-4b90-92cd-0d7b512653d1",
                "api": {
                    "public": "https://{hostname}/api/v3",
                    "private": "https://{hostname}/api/v3/viewer",
                    "contractPublic": "https://{hostname}/api/contract/v2",
                    "contractPrivate": "https://{hostname}/api/contract/v2",
                    "webExchange": "https://{hostname}/api/"
                },
                "www": "https://big.one",
                "doc": "https://open.big.one/docs/api.html",
                "fees": "https://bigone.zendesk.com/hc/en-us/articles/115001933374-BigONE-Fee-Policy",
                "referral": "https://b1.run/users/new?code=D3LLBVFT"
            },
            "api": {
                "public": {
                    "get": [
                        "ping",
                        "asset_pairs",
                        "asset_pairs/{asset_pair_name}/depth",
                        "asset_pairs/{asset_pair_name}/trades",
                        "asset_pairs/{asset_pair_name}/ticker",
                        "asset_pairs/{asset_pair_name}/candles",
                        "asset_pairs/tickers"
                    ]
                },
                "private": {
                    "get": [
                        "accounts",
                        "fund/accounts",
                        "assets/{asset_symbol}/address",
                        "orders",
                        "orders/{id}",
                        "orders/multi",
                        "trades",
                        "withdrawals",
                        "deposits"
                    ],
                    "post": [
                        "orders",
                        "orders/{id}/cancel",
                        "orders/cancel",
                        "withdrawals",
                        "transfer"
                    ]
                },
                "contractPublic": {
                    "get": [
                        "symbols",
                        "instruments",
                        "depth@{symbol}/snapshot",
                        "instruments/difference",
                        "instruments/prices"
                    ]
                },
                "contractPrivate": {
                    "get": [
                        "accounts",
                        "orders/{id}",
                        "orders",
                        "orders/opening",
                        "orders/count",
                        "orders/opening/count",
                        "trades",
                        "trades/count"
                    ],
                    "post": [
                        "orders",
                        "orders/batch"
                    ],
                    "put": [
                        "positions/{symbol}/margin",
                        "positions/{symbol}/risk-limit"
                    ],
                    "delete": [
                        "orders/{id}",
                        "orders/batch"
                    ]
                },
                "webExchange": {
                    "get": [
                        "v3/assets"
                    ]
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "min1",
                "5m": "min5",
                "15m": "min15",
                "30m": "min30",
                "1h": "hour1",
                "3h": "hour3",
                "4h": "hour4",
                "6h": "hour6",
                "12h": "hour12",
                "1d": "day1",
                "1w": "week1",
                "1M": "month1"
            },
            "fees": {
                "trading": {
                    "taker": 0.001,
                    "maker": 0.001
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "CRE": "Cybereits",
                "FXT": "FXTTOKEN",
                "FREE": "FreeRossDAO",
                "MBN": "Mobilian Coin",
                "ONE": "BigONE Token"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v3",
            "hostname": "big.one",
            "options": {
                "createMarketBuyOrderRequiresPrice": true,
                "accountsByType": {
                    "spot": "SPOT",
                    "fund": "FUND",
                    "funding": "FUND",
                    "future": "CONTRACT",
                    "swap": "CONTRACT"
                },
                "transfer": {
                    "fillResponseFromRequest": true
                },
                "exchangeMillisecondsCorrection": -100,
                "fetchCurrencies": {
                    "webApiEnable": true,
                    "webApiRetries": 5,
                    "webApiMuteFailure": true
                },
                "defaultNetwork": "ERC20",
                "defaultNetworks": {
                    "USDT": "TRC20"
                },
                "networks": {
                    "ABBC": "ABBC",
                    "ACA": "Acala",
                    "AE": "Aeternity",
                    "ALGO": "Algorand",
                    "APT": "Aptos",
                    "AR": "Arweave",
                    "ASTR": "Astar",
                    "AVAXC": "Avax",
                    "AVAXX": "AvaxChain",
                    "BEAM": "Beam",
                    "BEP20": "BinanceSmartChain",
                    "BITCI": "BitciChain",
                    "BTC": "Bitcoin",
                    "BCH": "BitcoinCash",
                    "BSV": "BitcoinSV",
                    "CELO": "Celo",
                    "CKKB": "CKB",
                    "ATOM": "Cosmos",
                    "CRC20": "CRO",
                    "DASH": "Dash",
                    "DOGE": "Dogecoin",
                    "XEC": "ECash",
                    "EOS": "EOS",
                    "ETH": "Ethereum",
                    "ETC": "EthereumClassic",
                    "ETHW": "EthereumPow",
                    "FTM": "Fantom",
                    "FIL": "Filecoin",
                    "FSN": "Fusion",
                    "GRIN": "Grin",
                    "ONE": "Harmony",
                    "HRC20": "Hecochain",
                    "HBAR": "Hedera",
                    "HNT": "Helium",
                    "ZEN": "Horizen",
                    "IOST": "IOST",
                    "IRIS": "IRIS",
                    "KLAY": "Klaytn",
                    "KSM": "Kusama",
                    "LTC": "Litecoin",
                    "XMR": "Monero",
                    "GLMR": "Moonbeam",
                    "NEAR": "Near",
                    "NEO": "Neo",
                    "NEON3": "NeoN3",
                    "OASIS": "Oasis",
                    "OKC": "Okexchain",
                    "ONT": "Ontology",
                    "OPTIMISM": "Optimism",
                    "DOT": "Polkadot",
                    "MATIC": "Polygon",
                    "QTUM": "Qtum",
                    "REI": "REI",
                    "XRP": "Ripple",
                    "SGB": "SGB",
                    "SDN": "Shiden",
                    "SOL": "Solana",
                    "XLM": "Stellar",
                    "TERA": "Tera",
                    "XTZ": "Tezos",
                    "TRC20": "Tron",
                    "VET": "Vechain",
                    "VSYS": "VSystems",
                    "WAX": "WAX",
                    "ZEC": "Zcash"
                }
            },
            "features": {
                "default": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerDirection": true,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": false,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyRequiresPrice": true,
                        "marketBuyByCost": true,
                        "selfTradePrevention": false,
                        "iceberg": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 200,
                        "symbolRequired": true
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 200,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 200,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 200,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": true
                    },
                    "fetchOHLCV": {
                        "limit": 500
                    }
                },
                "spot": {
                    "extends": "default"
                },
                "forDerivatives": {
                    "extends": "default",
                    "createOrder": {
                        "triggerPriceType": {
                            "mark": true,
                            "index": true,
                            "last": true
                        }
                    },
                    "fetchOrders": {
                        "daysBack": 100000,
                        "untilDays": 100000
                    },
                    "fetchClosedOrders": {
                        "daysBack": 100000,
                        "untilDays": 100000
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forDerivatives"
                    },
                    "inverse": {
                        "extends": "forDerivatives"
                    }
                },
                "future": {}
            }
        }"###).unwrap())
    }

    async fn fetch_currencies(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        // we use undocumented link (possible, less informative alternative is : https://big.one/api/uc/v3/assets/accounts)
        let mut data: Value = self.fetch_web_endpoint(Value::from("fetchCurrencies"), Value::from("webExchangeGetV3Assets"), Value::from(true), Value::Undefined, Value::Undefined).await;
        if data.clone().is_nullish() {
            return Value::new_object();
        };
        //
        // {
        //     "code": "0",
        //     "message": "",
        //     "data": [
        //       {
        //             "uuid": "17082d1c-0195-4fb6-8779-2cdbcb9eeb3c",
        //             "symbol": "USDT",
        //             "name": "TetherUS",
        //             "scale": 12,
        //             "is_fiat": false,
        //             "is_transfer_enabled": true,
        //             "transfer_scale": 12,
        //             "binding_gateways": [
        //                 {
        //                     "guid": "07efc37f-d1ec-4bc9-8339-a745256ea2ba",
        //                     "is_deposit_enabled": true,
        //                     "gateway_name": "Ethereum",
        //                     "min_withdrawal_amount": "0.000001",
        //                     "withdrawal_fee": "5.71",
        //                     "is_withdrawal_enabled": true,
        //                     "min_deposit_amount": "0.000001",
        //                     "is_memo_required": false,
        //                     "withdrawal_scale": 6,
        //                     "scale": 12
        //                 },
        //                 {
        //                     "guid": "4e387a9a-a480-40a3-b4ae-ed1773c2db5a",
        //                     "is_deposit_enabled": true,
        //                     "gateway_name": "BinanceSmartChain",
        //                     "min_withdrawal_amount": "10",
        //                     "withdrawal_fee": "5",
        //                     "is_withdrawal_enabled": false,
        //                     "min_deposit_amount": "1",
        //                     "is_memo_required": false,
        //                     "withdrawal_scale": 8,
        //                     "scale": 12
        //                 }
        //             ]
        //         },
        //       ...
        //     ],
        // }
        //
        let mut currencies_data: Value = self.safe_list(data.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < currencies_data.len() {
            let mut currency: Value = currencies_data.get(i.into());
            let mut id: Value = self.safe_string(currency.clone(), Value::from("symbol"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(id.clone(), Value::Undefined);
            let mut name: Value = self.safe_string(currency.clone(), Value::from("name"), Value::Undefined);
            let mut networks: Value = Value::new_object();
            let mut chains: Value = self.safe_list(currency.clone(), Value::from("binding_gateways"), Value::new_array());
            let mut currency_max_precision: Value = self.parse_precision(self.safe_string_2(currency.clone(), Value::from("withdrawal_scale"), Value::from("scale"), Value::Undefined));
            let mut j: usize = 0;
            while j < chains.len() {
                let mut chain: Value = chains.get(j.into());
                let mut network_id: Value = self.safe_string(chain.clone(), Value::from("gateway_name"), Value::Undefined);
                let mut network_code: Value = self.network_id_to_code(network_id.clone(), Value::Undefined);
                let mut deposit: Value = self.safe_bool(chain.clone(), Value::from("is_deposit_enabled"), Value::Undefined);
                let mut withdraw: Value = self.safe_bool(chain.clone(), Value::from("is_withdrawal_enabled"), Value::Undefined);
                let mut min_deposit_amount: Value = self.safe_string(chain.clone(), Value::from("min_deposit_amount"), Value::Undefined);
                let mut min_withdrawal_amount: Value = self.safe_string(chain.clone(), Value::from("min_withdrawal_amount"), Value::Undefined);
                let mut withdrawal_fee: Value = self.safe_string(chain.clone(), Value::from("withdrawal_fee"), Value::Undefined);
                let mut precision: Value = self.parse_precision(self.safe_string_2(chain.clone(), Value::from("withdrawal_scale"), Value::from("scale"), Value::Undefined));
                networks.set(network_code.clone(), Value::Json(normalize(&Value::Json(json!({
                    "id": network_id.clone(),
                    "network": network_code.clone(),
                    "margin": Value::Undefined,
                    "deposit": deposit.clone(),
                    "withdraw": withdraw.clone(),
                    "active": Value::Undefined,
                    "fee": self.parse_number(withdrawal_fee.clone(), Value::Undefined),
                    "precision": self.parse_number(precision.clone(), Value::Undefined),
                    "limits": Value::Json(normalize(&Value::Json(json!({
                        "deposit": Value::Json(normalize(&Value::Json(json!({
                            "min": min_deposit_amount.clone(),
                            "max": Value::Undefined
                        }))).unwrap()),
                        "withdraw": Value::Json(normalize(&Value::Json(json!({
                            "min": min_withdrawal_amount.clone(),
                            "max": Value::Undefined
                        }))).unwrap())
                    }))).unwrap()),
                    "info": chain.clone()
                }))).unwrap()));
                j += 1;
            };
            let mut chain_length: usize = chains.len();
            let mut r#type: Value = Value::Undefined;
            if self.safe_bool(currency.clone(), Value::from("is_fiat"), Value::Undefined).is_truthy() {
                r#type = Value::from("fiat");
            } else if chain_length == Value::from(0) {
                if self.is_leveraged_currency(id.clone(), Value::Undefined, Value::Undefined).is_truthy() {
                    r#type = Value::from("leveraged");
                } else {
                    r#type = Value::from("other");
                };
            } else {
                r#type = Value::from("crypto");
            };
            result.set(code.clone(), self.safe_currency_structure(Value::Json(normalize(&Value::Json(json!({
                "id": id.clone(),
                "code": code.clone(),
                "info": currency.clone(),
                "name": name.clone(),
                "type": r#type.clone(),
                "active": Value::Undefined,
                "deposit": Value::Undefined,
                "withdraw": Value::Undefined,
                "fee": Value::Undefined,
                "precision": self.parse_number(currency_max_precision.clone(), Value::Undefined),
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "amount": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "withdraw": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap())
                }))).unwrap()),
                "networks": networks.clone()
            }))).unwrap())));
            i += 1;
        };
        return result.clone();
    }

    

    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        // spot
        //
        //    {
        //        "asset_pair_name": "ETH-BTC",
        //        "bid": {
        //            "price": "0.021593",
        //            "order_count": 1,
        //            "quantity": "0.20936"
        //        },
        //        "ask": {
        //            "price": "0.021613",
        //            "order_count": 1,
        //            "quantity": "2.87064"
        //        },
        //        "open": "0.021795",
        //        "high": "0.021795",
        //        "low": "0.021471",
        //        "close": "0.021613",
        //        "volume": "117078.90431",
        //        "daily_change": "-0.000182"
        //    }
        //
        // contract
        //
        //    {
        //        "usdtPrice": 1.00031998,
        //        "symbol": "BTCUSD",
        //        "btcPrice": 34700.4,
        //        "ethPrice": 1787.83,
        //        "nextFundingRate": 0.00010,
        //        "fundingRate": 0.00010,
        //        "latestPrice": 34708.5,
        //        "last24hPriceChange": 0.0321,
        //        "indexPrice": 34700.4,
        //        "volume24h": 261319063,
        //        "turnover24h": 8204.129380685496,
        //        "nextFundingTime": 1698285600000,
        //        "markPrice": 34702.4646738,
        //        "last24hMaxPrice": 35127.5,
        //        "volume24hInUsd": 0.0,
        //        "openValue": 32.88054722085945,
        //        "last24hMinPrice": 33552.0,
        //        "openInterest": 1141372.0
        //    }
        //
        let mut market_type: Value = if ticker.contains_key(Value::from("asset_pair_name")) { Value::from("spot") } else { Value::from("swap") };
        let mut market_id: Value = self.safe_string_2(ticker.clone(), Value::from("asset_pair_name"), Value::from("symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::from("-"), market_type.clone());
        let mut close: Value = self.safe_string_2(ticker.clone(), Value::from("close"), Value::from("latestPrice"), Value::Undefined);
        let mut bid: Value = self.safe_dict(ticker.clone(), Value::from("bid"), Value::new_object());
        let mut ask: Value = self.safe_dict(ticker.clone(), Value::from("ask"), Value::new_object());
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol.clone(),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "high": self.safe_string_2(ticker.clone(), Value::from("high"), Value::from("last24hMaxPrice"), Value::Undefined),
            "low": self.safe_string_2(ticker.clone(), Value::from("low"), Value::from("last24hMinPrice"), Value::Undefined),
            "bid": self.safe_string(bid.clone(), Value::from("price"), Value::Undefined),
            "bidVolume": self.safe_string(bid.clone(), Value::from("quantity"), Value::Undefined),
            "ask": self.safe_string(ask.clone(), Value::from("price"), Value::Undefined),
            "askVolume": self.safe_string(ask.clone(), Value::from("quantity"), Value::Undefined),
            "vwap": Value::Undefined,
            "open": self.safe_string(ticker.clone(), Value::from("open"), Value::Undefined),
            "close": close.clone(),
            "last": close.clone(),
            "previousClose": Value::Undefined,
            "change": self.safe_string(ticker.clone(), Value::from("daily_change"), Value::Undefined),
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": self.safe_string_2(ticker.clone(), Value::from("volume"), Value::from("volume24h"), Value::Undefined),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("volume24hInUsd"), Value::Undefined),
            "markPrice": self.safe_string(ticker.clone(), Value::from("markPrice"), Value::Undefined),
            "indexPrice": self.safe_string(ticker.clone(), Value::from("indexPrice"), Value::Undefined),
            "info": ticker.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        // Some exchanges wrap the ticker in {data: {...}} or {result: {...}} — unwrap if so.
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut tk = json_resp.clone();
        if !tk.is_array() && tk.get("last").is_none() && tk.get("close").is_none() && tk.get("bid").is_none() {
            for key in &["data", "result", "ticker"] {
                if let Some(v) = json_resp.get(key) {
                    if v.is_object() { tk = v.clone(); break; }
                    if let Some(arr) = v.as_array() {
                        if let Some(first) = arr.first() { tk = first.clone(); break; }
                    }
                }
            }
        }
        let market: Value = Value::Undefined;
        let parsed = <Self as Exchange>::parse_ticker(self, Value::Json(tk.clone()), market);
        if parsed.is_undefined() { Value::Json(tk) } else { parsed }
    }


    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_time(&mut self, mut params: Value) -> Value {
        let candidates = vec![("public", "GET", "time"), ("public", "GET", "server/time"), ("public", "GET", "timestamp")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut ob = crate::exchange::unwrap_response_order_book(&json_resp);
        if let serde_json::Value::Object(ref mut map) = ob {
            if !map.contains_key("symbol") { map.insert("symbol".to_string(), serde_json::json!(symbol.unwrap_str())); }
        }
        Value::Json(ob)
    }


    fn parse_contract_bids_asks(&self, mut bids_asks: Value) -> Value {
        let mut bids_asks_keys: Value = bids_asks.clone().keys();
        let mut result: Value = Value::new_array();
        let mut i: usize = 0;
        while i < bids_asks_keys.len() {
            let mut price: Value = bids_asks_keys.get(i.into());
            let mut amount: Value = bids_asks.get(price.clone().clone());
            result.push(Value::Json(serde_json::Value::Array(vec![(self.parse_number(price.clone(), Value::Undefined)).into(), (self.parse_number(amount.clone(), Value::Undefined)).into()])));
            i += 1;
        };
        return result.clone();
    }

    fn parse_contract_order_book(&self, mut orderbook: Value, mut symbol: Value, mut limit: Value) -> Value {
        let mut response_bids: Value = self.safe_value(orderbook.clone(), Value::from("bids"), Value::Undefined);
        let mut response_asks: Value = self.safe_value(orderbook.clone(), Value::from("asks"), Value::Undefined);
        let mut bids: Value = <Self as Bigone>::parse_contract_bids_asks(self, response_bids.clone());
        let mut asks: Value = <Self as Bigone>::parse_contract_bids_asks(self, response_asks.clone());
        return Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol.clone(),
            "bids": self.filter_by_limit(self.sort_by(bids.clone(), Value::from(0), Value::from(true), Value::Undefined), limit.clone(), Value::Undefined, Value::Undefined),
            "asks": self.filter_by_limit(self.sort_by(asks.clone(), Value::from(0), Value::Undefined, Value::Undefined), limit.clone(), Value::Undefined, Value::Undefined),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "nonce": Value::Undefined
        }))).unwrap());
    }

    fn parse_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchTrades (public)
        //
        //     {
        //         "id": 38199941,
        //         "price": "3378.67",
        //         "amount": "0.019812",
        //         "taker_side": "ASK",
        //         "created_at": "2019-01-29T06:05:56Z"
        //     }
        //
        // fetchMyTrades (private)
        //
        //     {
        //         "id": 10854280,
        //         "asset_pair_name": "XIN-USDT",
        //         "price": "70",
        //         "amount": "1",
        //         "taker_side": "ASK",
        //         "maker_order_id": 58284908,
        //         "taker_order_id": 58284909,
        //         "maker_fee": "0.0008",
        //         "taker_fee": "0.07",
        //         "side": "SELF_TRADING",
        //         "inserted_at": "2019-04-16T12:00:01Z"
        //     },
        //
        //     {
        //         "id": 10854263,
        //         "asset_pair_name": "XIN-USDT",
        //         "price": "75.7",
        //         "amount": "12.743149",
        //         "taker_side": "BID",
        //         "maker_order_id": null,
        //         "taker_order_id": 58284888,
        //         "maker_fee": null,
        //         "taker_fee": "0.0025486298",
        //         "side": "BID",
        //         "inserted_at": "2019-04-15T06:20:57Z"
        //     }
        //
        let mut timestamp: Value = self.parse8601(self.safe_string_2(trade.clone(), Value::from("created_at"), Value::from("inserted_at"), Value::Undefined));
        let mut price_string: Value = self.safe_string(trade.clone(), Value::from("price"), Value::Undefined);
        let mut amount_string: Value = self.safe_string(trade.clone(), Value::from("amount"), Value::Undefined);
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("asset_pair_name"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::from("-"), Value::Undefined);
        let mut side: Value = self.safe_string(trade.clone(), Value::from("side"), Value::Undefined);
        let mut taker_side: Value = self.safe_string(trade.clone(), Value::from("taker_side"), Value::Undefined);
        let mut taker_or_maker: Value = Value::Undefined;
        if taker_side.clone().is_nonnullish() && side.clone().is_nonnullish() && side.clone() != Value::from("SELF_TRADING") {
            taker_or_maker = if taker_side.clone() == side.clone() { Value::from("taker") } else { Value::from("maker") };
        };
        if side.clone().is_nullish() {
            // taker side is not related to buy/sell side
            // the following code is probably a mistake
            side = if taker_side.clone() == Value::from("ASK") { Value::from("sell") } else { Value::from("buy") };
        } else {
            if side.clone() == Value::from("BID") {
                side = Value::from("buy");
            } else if side.clone() == Value::from("ASK") {
                side = Value::from("sell");
            };
        };
        let mut maker_order_id: Value = self.safe_string(trade.clone(), Value::from("maker_order_id"), Value::Undefined);
        let mut taker_order_id: Value = self.safe_string(trade.clone(), Value::from("taker_order_id"), Value::Undefined);
        let mut order_id: Value = Value::Undefined;
        if maker_order_id.clone().is_nonnullish() {
            order_id = maker_order_id.clone();
        } else if taker_order_id.clone().is_nonnullish() {
            order_id = taker_order_id.clone();
        };
        let mut id: Value = self.safe_string(trade.clone(), Value::from("id"), Value::Undefined);
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": market.get(Value::from("symbol")),
            "order": order_id.clone(),
            "type": Value::from("limit"),
            "side": side.clone(),
            "takerOrMaker": taker_or_maker.clone(),
            "price": price_string.clone(),
            "amount": amount_string.clone(),
            "cost": Value::Undefined,
            "info": trade.clone()
        }))).unwrap());
        let mut maker_currency_code: Value = Value::Undefined;
        let mut taker_currency_code: Value = Value::Undefined;
        if taker_or_maker.clone().is_nonnullish() {
            if side.clone() == Value::from("buy") {
                if taker_or_maker.clone() == Value::from("maker") {
                    maker_currency_code = market.get(Value::from("base"));
                    taker_currency_code = market.get(Value::from("quote"));
                } else {
                    maker_currency_code = market.get(Value::from("quote"));
                    taker_currency_code = market.get(Value::from("base"));
                };
            } else {
                if taker_or_maker.clone() == Value::from("maker") {
                    maker_currency_code = market.get(Value::from("quote"));
                    taker_currency_code = market.get(Value::from("base"));
                } else {
                    maker_currency_code = market.get(Value::from("base"));
                    taker_currency_code = market.get(Value::from("quote"));
                };
            };
        } else if side.clone() == Value::from("SELF_TRADING") {
            if taker_side.clone() == Value::from("BID") {
                maker_currency_code = market.get(Value::from("quote"));
                taker_currency_code = market.get(Value::from("base"));
            } else if taker_side.clone() == Value::from("ASK") {
                maker_currency_code = market.get(Value::from("base"));
                taker_currency_code = market.get(Value::from("quote"));
            };
        };
        let mut maker_fee_cost: Value = self.safe_string(trade.clone(), Value::from("maker_fee"), Value::Undefined);
        let mut taker_fee_cost: Value = self.safe_string(trade.clone(), Value::from("taker_fee"), Value::Undefined);
        if maker_fee_cost.clone().is_nonnullish() {
            if taker_fee_cost.clone().is_nonnullish() {
                result.set("fees".into(), Value::Json(serde_json::Value::Array(vec![(Value::Json(normalize(&Value::Json(json!({
                    "cost": maker_fee_cost.clone(),
                    "currency": maker_currency_code.clone()
                }))).unwrap())).into(), (Value::Json(normalize(&Value::Json(json!({
                    "cost": taker_fee_cost.clone(),
                    "currency": taker_currency_code.clone()
                }))).unwrap())).into()])));
            } else {
                result.set("fee".into(), Value::Json(normalize(&Value::Json(json!({
                    "cost": maker_fee_cost.clone(),
                    "currency": maker_currency_code.clone()
                }))).unwrap()));
            };
        } else if taker_fee_cost.clone().is_nonnullish() {
            result.set("fee".into(), Value::Json(normalize(&Value::Json(json!({
                "cost": taker_fee_cost.clone(),
                "currency": taker_currency_code.clone()
            }))).unwrap()));
        } else {
            result.set("fee".into(), Value::Undefined);
        };
        return self.safe_trade(result.clone(), market.clone());
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        let mut raw: Value = Value::Undefined;
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { raw = rv; break; }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            // Already-parsed trades have both `price` and `amount` keys.
            if item.get("price").is_some() && item.get("amount").is_some() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_trade(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        //     {
        //         "close": "0.021562",
        //         "high": "0.021563",
        //         "low": "0.02156",
        //         "open": "0.021563",
        //         "time": "2019-11-21T07:54:00Z",
        //         "volume": "59.84376"
        //     }
        //
        return Value::Json(serde_json::Value::Array(vec![(self.parse8601(self.safe_string(ohlcv.clone(), Value::from("time"), Value::Undefined))).into(), (self.safe_number(ohlcv.clone(), Value::from("open"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("high"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("low"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("close"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("volume"), Value::Undefined)).into()]));
    }

    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        // Match endpoints by substring on common OHLCV path tokens (klines, candles,
        // ohlcv, barhist, history, kline, candle, ohlc).
        'outer: for token in ["klines", "candles", "ohlcv", "barhist", "kline", "candle", "ohlc"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            if item.is_array() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_ohlcv(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response.clone(),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined
        }))).unwrap());
        let mut balances: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut i: usize = 0;
        while i < balances.len() {
            let mut balance: Value = balances.get(i.into());
            let mut symbol: Value = self.safe_string(balance.clone(), Value::from("asset_symbol"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(symbol.clone(), Value::Undefined);
            let mut account: Value = self.account();
            account.set("total".into(), self.safe_string(balance.clone(), Value::from("balance"), Value::Undefined));
            account.set("used".into(), self.safe_string(balance.clone(), Value::from("locked_balance"), Value::Undefined));
            result.set(code.clone(), account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["account/balance", "balance", "wallet/balance", "accounts/balance", "account"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(&format!("/{}/", token)) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "balance"), ("private", "GET", "account/balance"), ("private", "GET", "wallet/balance")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_type(&self, mut r#type: Value) -> Value {
        let mut types: Value = Value::Json(normalize(&Value::Json(json!({
            "STOP_LIMIT": Value::from("limit"),
            "STOP_MARKET": Value::from("market"),
            "LIMIT": Value::from("limit"),
            "MARKET": Value::from("market")
        }))).unwrap());
        return self.safe_string(types.clone(), r#type.clone(), r#type.clone());
    }

    fn parse_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        //    {
        //        "id": "42154072252",
        //        "asset_pair_name": "SOL-USDT",
        //        "price": "20",
        //        "amount": "0.5",
        //        "filled_amount": "0",
        //        "avg_deal_price": "0",
        //        "side": "ASK",
        //        "state": "PENDING",
        //        "created_at": "2023-09-13T03:42:00Z",
        //        "updated_at": "2023-09-13T03:42:00Z",
        //        "type": "LIMIT",
        //        "stop_price": "0",
        //        "immediate_or_cancel": false,
        //        "post_only": false,
        //        "client_order_id": ''
        //    }
        //
        let mut id: Value = self.safe_string(order.clone(), Value::from("id"), Value::Undefined);
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("asset_pair_name"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::from("-"), Value::Undefined);
        let mut timestamp: Value = self.parse8601(self.safe_string(order.clone(), Value::from("created_at"), Value::Undefined));
        let mut side: Value = self.safe_string(order.clone(), Value::from("side"), Value::Undefined);
        if side.clone() == Value::from("BID") {
            side = Value::from("buy");
        } else {
            side = Value::from("sell");
        };
        let mut trigger_price: Value = self.safe_string(order.clone(), Value::from("stop_price"), Value::Undefined);
        if Precise::string_eq(trigger_price.clone(), Value::from("0")) {
            trigger_price = Value::Undefined;
        };
        let mut immediate_or_cancel: Value = self.safe_bool(order.clone(), Value::from("immediate_or_cancel"), Value::Undefined);
        let mut time_in_force: Value = Value::Undefined;
        if immediate_or_cancel.is_truthy() {
            time_in_force = Value::from("IOC");
        };
        let mut r#type: Value = <Self as Bigone>::parse_type(self, self.safe_string(order.clone(), Value::from("type"), Value::Undefined));
        let mut price: Value = self.safe_string(order.clone(), Value::from("price"), Value::Undefined);
        let mut amount: Value = Value::Undefined;
        let mut filled: Value = Value::Undefined;
        let mut cost: Value = Value::Undefined;
        if r#type.clone() == Value::from("market") && side.clone() == Value::from("buy") {
            cost = self.safe_string(order.clone(), Value::from("filled_amount"), Value::Undefined);
        } else {
            amount = self.safe_string(order.clone(), Value::from("amount"), Value::Undefined);
            filled = self.safe_string(order.clone(), Value::from("filled_amount"), Value::Undefined);
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order.clone(),
            "id": id.clone(),
            "clientOrderId": self.safe_string(order.clone(), Value::from("client_order_id"), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": self.parse8601(self.safe_string(order.clone(), Value::from("updated_at"), Value::Undefined)),
            "symbol": symbol.clone(),
            "type": r#type.clone(),
            "timeInForce": time_in_force.clone(),
            "postOnly": self.safe_bool(order.clone(), Value::from("post_only"), Value::Undefined),
            "side": side.clone(),
            "price": price.clone(),
            "triggerPrice": trigger_price.clone(),
            "amount": amount.clone(),
            "cost": cost.clone(),
            "average": self.safe_string(order.clone(), Value::from("avg_deal_price"), Value::Undefined),
            "filled": filled.clone(),
            "remaining": Value::Undefined,
            "status": <Self as Bigone>::parse_order_status(self, self.safe_string(order.clone(), Value::from("state"), Value::Undefined)),
            "fee": Value::Undefined,
            "trades": Value::Undefined
        }))).unwrap()), market.clone());
    }

    async fn create_market_buy_order_with_cost(&mut self, mut symbol: Value, mut cost: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        if !market.get(Value::from("spot")).is_truthy() {
            return Value::Undefined;
        };
        params.set("createMarketBuyOrderRequiresPrice".into(), Value::from(false));
        return <Self as Bigone>::create_order(self, symbol.clone(), Value::from("market"), Value::from("buy"), cost.clone(), Value::Undefined, params.clone()).await;
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("type".into(), r#type.clone());
        request.set("side".into(), side.clone());
        request.set("amount".into(), amount.clone());
        if price.is_nonnullish() { request.set("price".into(), price.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "POST" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "POST", "order"), ("private", "POST", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("orderId".into(), id.clone());
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "DELETE" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "DELETE", "order"), ("private", "DELETE", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn cancel_all_orders(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "asset_pair_name": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostOrdersCancel".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "code":0,
        //         "data": {
        //             "cancelled":[
        //                 58272370,
        //                 58272377
        //             ],
        //             "failed": []
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut cancelled: Value = self.safe_list(data.clone(), Value::from("cancelled"), Value::new_array());
        let mut failed: Value = self.safe_list(data.clone(), Value::from("failed"), Value::new_array());
        let mut result: Value = Value::new_array();
        let mut i: usize = 0;
        while i < cancelled.len() {
            let mut order_id: Value = cancelled.get(i.into());
            result.push(self.safe_order(Value::Json(normalize(&Value::Json(json!({
                "info": order_id.clone(),
                "id": order_id.clone(),
                "status": Value::from("canceled")
            }))).unwrap()), Value::Undefined));
            i += 1;
        };
        let mut i: usize = 0;
        while i < failed.len() {
            let mut order_id: Value = failed.get(i.into());
            result.push(self.safe_order(Value::Json(normalize(&Value::Json(json!({
                "info": order_id.clone(),
                "id": order_id.clone(),
                "status": Value::from("failed")
            }))).unwrap()), Value::Undefined));
            i += 1;
        };
        return result.clone();
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "id": id.clone()
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetOrdersId".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut order: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Bigone>::parse_order(self, order.clone(), Value::Undefined).await;
    }

    async fn fetch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["allorders", "all_orders", "orders/history", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "allOrders"), ("private", "GET", "orders"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["mytrades", "my_trades", "trades/history", "fills", "user/trades", "executions"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "myTrades"), ("private", "GET", "fills"), ("private", "GET", "trades/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "PENDING": Value::from("open"),
            "FILLED": Value::from("closed"),
            "CANCELLED": Value::from("canceled")
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), Value::Undefined);
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["openorders", "open_orders", "orders/open", "orders/active"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "openOrders"), ("private", "GET", "orders/open"), ("private", "GET", "orders/active")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_closed_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["closedorders", "closed_orders", "orders/closed", "orders/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "closedOrders"), ("private", "GET", "orders/closed"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn nonce(&self) -> Value {
        let mut exchange_time_correction: Value = self.safe_integer(self.get("options".into()), Value::from("exchangeMillisecondsCorrection"), Value::from(0)) * Value::from(1000000);
        return self.sum(self.microseconds() * Value::from(1000), exchange_time_correction.clone());
    }

    

    async fn fetch_deposit_address(&mut self, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "asset_symbol": currency.get(Value::from("id"))
        }))).unwrap());
        let (mut network_code, mut params_omitted) = shift_2(self.handle_network_code_and_params(params.clone()));
        let mut response: Value = self.dispatch("privateGetAssetsAssetSymbolAddress".into(), extend_2(request.clone(), params_omitted.clone()), Value::Undefined).await;
        //
        // the actual response format is not the same as the documented one
        // the data key contains an array in the actual response
        //
        //     {
        //         "code":0,
        //         "message":"",
        //         "data":[
        //             {
        //                 "id":5521878,
        //                 "chain":"Bitcoin",
        //                 "value":"1GbmyKoikhpiQVZ1C9sbF17mTyvBjeobVe",
        //                 "memo":""
        //             }
        //         ]
        //     }
        //
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut data_length: usize = data.len();
        if data_length < Value::from(1) {
            return Value::Undefined;
        };
        let mut chains_indexed_by_id: Value = self.index_by(data.clone(), Value::from("chain"));
        let mut selected_network_id: Value = self.select_network_id_from_raw_networks(code.clone(), network_code.clone(), chains_indexed_by_id.clone());
        let mut address_object: Value = self.safe_dict(chains_indexed_by_id.clone(), selected_network_id.clone(), Value::new_object());
        let mut address: Value = self.safe_string(address_object.clone(), Value::from("value"), Value::Undefined);
        let mut tag: Value = self.safe_string(address_object.clone(), Value::from("memo"), Value::Undefined);
        self.check_address(address.clone());
        return Value::Json(normalize(&Value::Json(json!({
            "info": response.clone(),
            "currency": code.clone(),
            "network": self.network_id_to_code(selected_network_id.clone(), Value::Undefined),
            "address": address.clone(),
            "tag": tag.clone()
        }))).unwrap());
    }

    fn parse_transaction_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "WITHHOLD": Value::from("ok"),
            "UNCONFIRMED": Value::from("pending"),
            "CONFIRMED": Value::from("ok"),
            "COMPLETED": Value::from("ok"),
            "PENDING": Value::from("pending")
        }))).unwrap());
        // what are other statuses here?
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value) -> Value {
        //
        // fetchDeposits
        //
        //     {
        //         "amount": "25.0",
        //         "asset_symbol": "BTS"
        //         "confirms": 100,
        //         "id": 5,
        //         "inserted_at": "2018-02-16T11:39:58.000Z",
        //         "is_internal": false,
        //         "kind": "default",
        //         "memo": "",
        //         "state": "WITHHOLD",
        //         "txid": "72e03037d144dae3d32b68b5045462b1049a0755",
        //         "updated_at": "2018-11-09T10:20:09.000Z",
        //     }
        //
        // fetchWithdrawals
        //
        //     {
        //         "amount": "5",
        //         "asset_symbol": "ETH",
        //         "completed_at": "2018-03-15T16:13:45.610463Z",
        //         "customer_id": "10",
        //         "id": 10,
        //         "inserted_at": "2018-03-15T16:13:45.610463Z",
        //         "is_internal": true,
        //         "note": "2018-03-15T16:13:45.610463Z",
        //         "state": "CONFIRMED",
        //         "target_address": "0x4643bb6b393ac20a6175c713175734a72517c63d6f7"
        //         "txid": "0x4643bb6b393ac20a6175c713175734a72517c63d6f73a3ca90a15356f2e967da0",
        //     }
        //
        // withdraw
        //
        //     {
        //         "id":1077391,
        //         "customer_id":1082679,
        //         "amount":"21.9000000000000000",
        //         "txid":"",
        //         "is_internal":false,
        //         "kind":"on_chain",
        //         "state":"PENDING",
        //         "inserted_at":"2020-06-03T00:50:57+00:00",
        //         "updated_at":"2020-06-03T00:50:57+00:00",
        //         "memo":"",
        //         "target_address":"rDYtYT3dBeuw376rvHqoZBKW3UmvguoBAf",
        //         "fee":"0.1000000000000000",
        //         "asset_symbol":"XRP"
        //     }
        //
        let mut currency_id: Value = self.safe_string(transaction.clone(), Value::from("asset_symbol"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
        let mut id: Value = self.safe_string(transaction.clone(), Value::from("id"), Value::Undefined);
        let mut amount: Value = self.safe_number(transaction.clone(), Value::from("amount"), Value::Undefined);
        let mut status: Value = <Self as Bigone>::parse_transaction_status(self, self.safe_string(transaction.clone(), Value::from("state"), Value::Undefined));
        let mut timestamp: Value = self.parse8601(self.safe_string(transaction.clone(), Value::from("inserted_at"), Value::Undefined));
        let mut updated: Value = self.parse8601(self.safe_string_2(transaction.clone(), Value::from("updated_at"), Value::from("completed_at"), Value::Undefined));
        let mut txid: Value = self.safe_string(transaction.clone(), Value::from("txid"), Value::Undefined);
        let mut address: Value = self.safe_string(transaction.clone(), Value::from("target_address"), Value::Undefined);
        let mut tag: Value = self.safe_string(transaction.clone(), Value::from("memo"), Value::Undefined);
        let mut r#type: Value = if transaction.contains_key(Value::from("customer_id")) { Value::from("withdrawal") } else { Value::from("deposit") };
        let mut internal: Value = self.safe_bool(transaction.clone(), Value::from("is_internal"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": transaction.clone(),
            "id": id.clone(),
            "txid": txid.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "network": Value::Undefined,
            "addressFrom": Value::Undefined,
            "address": Value::Undefined,
            "addressTo": address.clone(),
            "tagFrom": Value::Undefined,
            "tag": tag.clone(),
            "tagTo": Value::Undefined,
            "type": r#type.clone(),
            "amount": amount.clone(),
            "currency": code.clone(),
            "status": status.clone(),
            "updated": updated.clone(),
            "fee": Value::Undefined,
            "comment": Value::Undefined,
            "internal": internal.clone()
        }))).unwrap());
    }

    async fn fetch_deposits(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["deposits", "deposit/history", "deposit_history", "capital/deposit/hisrec"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/deposit/hisrec"), ("private", "GET", "deposits"), ("private", "GET", "deposit/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_withdrawals(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Bigone>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["withdrawals", "withdraw/history", "withdrawal_history", "capital/withdraw/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/withdraw/history"), ("private", "GET", "withdrawals"), ("private", "GET", "withdraw/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn transfer(&mut self, mut code: Value, mut amount: Value, mut from_account: Value, mut to_account: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut accounts_by_type: Value = self.safe_dict(self.get("options".into()), Value::from("accountsByType"), Value::new_object());
        let mut from_id: Value = self.safe_string(accounts_by_type.clone(), from_account.clone(), from_account.clone());
        let mut to_id: Value = self.safe_string(accounts_by_type.clone(), to_account.clone(), to_account.clone());
        let mut guid: Value = self.safe_string(params.clone(), Value::from("guid"), self.uuid());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": currency.get(Value::from("id")),
            "amount": self.currency_to_precision(code.clone(), amount.clone(), Value::Undefined),
            "from": from_id.clone(),
            "to": to_id.clone(),
            "guid": guid.clone()
        }))).unwrap());
        // 'type': type, // NORMAL, MASTER_TO_SUB, SUB_TO_MASTER, SUB_INTERNAL, default is NORMAL
        // 'sub_acccunt': '', // when type is NORMAL, it should be empty, and when type is others it is required
        let mut response: Value = self.dispatch("privatePostTransfer".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "code": 0,
        //         "data": null
        //     }
        //
        let mut transfer: Value = <Self as Bigone>::parse_transfer(self, response.clone(), currency.clone());
        let mut transfer_options: Value = self.safe_dict(self.get("options".into()), Value::from("transfer"), Value::new_object());
        let mut fill_response_from_request: Value = self.safe_bool(transfer_options.clone(), Value::from("fillResponseFromRequest"), Value::from(true));
        if fill_response_from_request.is_truthy() {
            transfer.set("fromAccount".into(), from_account.clone());
            transfer.set("toAccount".into(), to_account.clone());
            transfer.set("amount".into(), amount.clone());
            transfer.set("id".into(), guid.clone());
        };
        return transfer.clone();
    }

    fn parse_transfer(&self, mut transfer: Value, mut currency: Value) -> Value {
        //
        //     {
        //         "code": 0,
        //         "data": null
        //     }
        //
        let mut code: Value = self.safe_string(transfer.clone(), Value::from("code"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": transfer.clone(),
            "id": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "currency": Value::Undefined,
            "amount": Value::Undefined,
            "fromAccount": Value::Undefined,
            "toAccount": Value::Undefined,
            "status": <Self as Bigone>::parse_transfer_status(self, code.clone())
        }))).unwrap());
    }

    fn parse_transfer_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "0": Value::from("ok")
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), Value::from("failed"));
    }

    async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, mut tag: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        (tag, params) = shift_2(self.handle_withdraw_tag_and_params(tag.clone(), params.clone()));
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": currency.get(Value::from("id")),
            "target_address": address.clone(),
            "amount": self.currency_to_precision(code.clone(), amount.clone(), Value::Undefined)
        }))).unwrap());
        if tag.clone().is_nonnullish() {
            request.set("memo".into(), tag.clone());
        };
        let mut network_code: Value = Value::Undefined;
        (network_code, params) = shift_2(self.handle_network_code_and_params(params.clone()));
        if network_code.clone().is_nonnullish() {
            request.set("gateway_name".into(), self.network_code_to_id(network_code.clone(), Value::Undefined));
        };
        // requires write permission on the wallet
        let mut response: Value = self.dispatch("privatePostWithdrawals".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "code":0,
        //         "message":"",
        //         "data":{
        //             "id":1077391,
        //             "customer_id":1082679,
        //             "amount":"21.9000000000000000",
        //             "txid":"",
        //             "is_internal":false,
        //             "kind":"on_chain",
        //             "state":"PENDING",
        //             "inserted_at":"2020-06-03T00:50:57+00:00",
        //             "updated_at":"2020-06-03T00:50:57+00:00",
        //             "memo":"",
        //             "target_address":"rDYtYT3dBeuw376rvHqoZBKW3UmvguoBAf",
        //             "fee":"0.1000000000000000",
        //             "asset_symbol":"XRP"
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Bigone>::parse_transaction(self, data.clone(), currency.clone()).await;
    }

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_str() {
                    "publicGetPing" => self.request("ping".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetpairs" => self.request("asset_pairs".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetpairsassetpairnamedepth" => self.request("asset_pairs/{asset_pair_name}/depth".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetpairsassetpairnametrades" => self.request("asset_pairs/{asset_pair_name}/trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetpairsassetpairnameticker" => self.request("asset_pairs/{asset_pair_name}/ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetpairsassetpairnamecandles" => self.request("asset_pairs/{asset_pair_name}/candles".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetAssetpairstickers" => self.request("asset_pairs/tickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccounts" => self.request("accounts".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetFundaccounts" => self.request("fund/accounts".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetsassetsymboladdress" => self.request("assets/{asset_symbol}/address".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrders" => self.request("orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrdersid" => self.request("orders/{id}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrdersmulti" => self.request("orders/multi".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTrades" => self.request("trades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWithdrawals" => self.request("withdrawals".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetDeposits" => self.request("deposits".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrders" => self.request("orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdersidcancel" => self.request("orders/{id}/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrderscancel" => self.request("orders/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawals" => self.request("withdrawals".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTransfer" => self.request("transfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractpublicGetSymbols" => self.request("symbols".into(), "contractPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractpublicGetInstruments" => self.request("instruments".into(), "contractPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractpublicGetDepthsymbolsnapshot" => self.request("depth@{symbol}/snapshot".into(), "contractPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractpublicGetInstrumentsdifference" => self.request("instruments/difference".into(), "contractPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractpublicGetInstrumentsprices" => self.request("instruments/prices".into(), "contractPublic".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetAccounts" => self.request("accounts".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetOrdersid" => self.request("orders/{id}".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetOrders" => self.request("orders".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetOrdersopening" => self.request("orders/opening".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetOrderscount" => self.request("orders/count".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetOrdersopeningcount" => self.request("orders/opening/count".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetTrades" => self.request("trades".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateGetTradescount" => self.request("trades/count".into(), "contractPrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivatePostOrders" => self.request("orders".into(), "contractPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivatePostOrdersbatch" => self.request("orders/batch".into(), "contractPrivate".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivatePutPositionssymbolmargin" => self.request("positions/{symbol}/margin".into(), "contractPrivate".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivatePutPositionssymbolrisklimit" => self.request("positions/{symbol}/risk-limit".into(), "contractPrivate".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateDeleteOrdersid" => self.request("orders/{id}".into(), "contractPrivate".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "contractprivateDeleteOrdersbatch" => self.request("orders/batch".into(), "contractPrivate".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "webexchangeGetV3assets" => self.request("v3/assets".into(), "webExchange".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BigoneImpl(Value);
#[async_trait(?Send)]
impl Exchange for BigoneImpl {
    fn describe(&self) -> Value { Bigone::describe(self) }
    async fn fetch_ticker(&mut self, symbol: Value, params: Value) -> Value { Bigone::fetch_ticker(self, symbol, params).await }
    async fn fetch_tickers(&mut self, symbols: Value, params: Value) -> Value { Bigone::fetch_tickers(self, symbols, params).await }
    async fn fetch_order_book(&mut self, symbol: Value, limit: Value, params: Value) -> Value { Bigone::fetch_order_book(self, symbol, limit, params).await }
    async fn fetch_ohlcv(&mut self, symbol: Value, timeframe: Value, since: Value, limit: Value, params: Value) -> Value { Bigone::fetch_ohlcv(self, symbol, timeframe, since, limit, params).await }
    async fn fetch_trades(&mut self, symbol: Value, since: Value, limit: Value, params: Value) -> Value { Bigone::fetch_trades(self, symbol, since, limit, params).await }
    async fn fetch_time(&mut self, params: Value) -> Value { Bigone::fetch_time(self, params).await }
    fn parse_trade(&self, trade: Value, market: Value) -> Value { Bigone::parse_trade(self, trade, market) }
    fn parse_order(&self, order: Value, market: Value) -> Value { Bigone::parse_order(self, order, market) }
    fn parse_ticker(&self, ticker: Value, market: Value) -> Value { Bigone::parse_ticker(self, ticker, market) }
    fn parse_balance(&self, response: Value) -> Value { Bigone::parse_balance(self, response) }
    fn parse_transaction(&self, transaction: Value, currency: Value) -> Value { Bigone::parse_transaction(self, transaction, currency) }
    fn parse_transfer(&self, transfer: Value, currency: Value) -> Value { Bigone::parse_transfer(self, transfer, currency) }
    fn parse_order_status(&self, status: Value) -> Value { Bigone::parse_order_status(self, status) }
}
impl Bigone for BigoneImpl {}
impl ValueTrait for BigoneImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl BigoneImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = BigoneImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Bigone::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


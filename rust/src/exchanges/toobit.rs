#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
use crate::ws::{ArrayCache, ArrayCacheByTimestamp, ArrayCacheBySymbolById, ArrayCacheBySymbolBySide};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors (stub structs with ::new for transpiled `new ErrorType(msg)` patterns)
macro_rules! error_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(msg: Value) -> Value { msg } } }; }
error_stub!(BadRequest);
error_stub!(InvalidOrder);
error_stub!(ExchangeError);
error_stub!(InsufficientFunds);
error_stub!(OrderNotFound);
error_stub!(AuthenticationError);
error_stub!(PermissionDenied);
error_stub!(ExchangeNotAvailable);
error_stub!(ArgumentsRequired);
error_stub!(RateLimitExceeded);
error_stub!(OrderNotFillable);
error_stub!(OrderImmediatelyFillable);
error_stub!(NotSupported);
error_stub!(DuplicateOrderId);
error_stub!(UnsubscribeError);
error_stub!(ChecksumError);
error_stub!(InvalidNonce);
error_stub!(BadSymbol);
// Array-like type stubs (Bids/Asks order book sides)
macro_rules! array_side_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(data: Value, _limit: Value) -> Value { data } } }; }
array_side_stub!(Bids);
array_side_stub!(Asks);

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait(?Send)]
pub trait Toobit : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "toobit",
            "name": "Toobit",
            "countries": [
                "KY"
            ],
            "enableRateLimit": true,
            "rateLimit": 20,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": false,
                "swap": true,
                "future": false,
                "option": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "cancelOrders": true,
                "createLimitOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createOrder": true,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchBidsAsks": true,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDeposits": true,
                "fetchFundingRateHistory": true,
                "fetchFundingRates": true,
                "fetchIndexOHLCV": true,
                "fetchL2OrderBook": true,
                "fetchLastPrices": true,
                "fetchLedger": true,
                "fetchMarkets": true,
                "fetchMarkOHLCV": true,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchStatus": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchWithdrawals": true,
                "setMarginMode": true,
                "transfer": true,
                "withdraw": true
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/0c7a97d5-182c-492e-b921-23540c868e0e",
                "api": {
                    "common": "https://api.toobit.com",
                    "private": "https://api.toobit.com"
                },
                "www": "https://www.toobit.com/",
                "doc": [
                    "https://toobit-docs.github.io/apidocs/spot/v1/en/",
                    "https://toobit-docs.github.io/apidocs/usdt_swap/v1/en/"
                ],
                "fees": "https://www.toobit.com/fee",
                "referral": {
                    "url": "https://www.toobit.com/en-US/r?i=IFFPy0",
                    "discount": 0.1
                }
            },
            "api": {
                "common": {
                    "get": {
                        "api/v1/time": 1,
                        "api/v1/ping": 1,
                        "api/v1/exchangeInfo": 1,
                        "quote/v1/depth": 1,
                        "quote/v1/depth/merged": 1,
                        "quote/v1/trades": 1,
                        "quote/v1/klines": 1,
                        "quote/v1/index/klines": 1,
                        "quote/v1/markPrice/klines": 1,
                        "quote/v1/markPrice": 1,
                        "quote/v1/index": 1,
                        "quote/v1/ticker/24hr": 40,
                        "quote/v1/contract/ticker/24hr": 40,
                        "quote/v1/ticker/price": 1,
                        "quote/v1/ticker/bookTicker": 1,
                        "api/v1/futures/fundingRate": 1,
                        "api/v1/futures/historyFundingRate": 1
                    }
                },
                "private": {
                    "get": {
                        "api/v1/account": 5,
                        "api/v1/account/checkApiKey": 1,
                        "api/v1/spot/order": 1.67,
                        "api/v1/spot/openOrders": 1.67,
                        "api/v1/futures/openOrders": 1.67,
                        "api/v1/spot/tradeOrders": 8.35,
                        "api/v1/futures/historyOrders": 8.35,
                        "api/v1/account/trades": 8.35,
                        "api/v1/account/balanceFlow": 5,
                        "api/v1/account/depositOrders": 5,
                        "api/v1/account/withdrawOrders": 5,
                        "api/v1/account/deposit/address": 1,
                        "api/v1/subAccount": 5,
                        "api/v1/futures/accountLeverage": 1,
                        "api/v1/futures/order": 1.67,
                        "api/v1/futures/positions": 8.35,
                        "api/v1/futures/balance": 5,
                        "api/v1/futures/userTrades": 8.35,
                        "api/v1/futures/balanceFlow": 5,
                        "api/v1/futures/commissionRate": 5,
                        "api/v1/futures/todayPnl": 5
                    },
                    "post": {
                        "api/v1/spot/orderTest": 1.67,
                        "api/v1/spot/order": 1.67,
                        "api/v1/futures/order": 1.67,
                        "api/v1/spot/batchOrders": 3.34,
                        "api/v1/subAccount/transfer": 1,
                        "api/v1/account/withdraw": 1,
                        "api/v1/futures/marginType": 1,
                        "api/v1/futures/leverage": 1,
                        "api/v1/futures/batchOrders": 3.34,
                        "api/v1/futures/position/trading-stop": 5.01,
                        "api/v1/futures/positionMargin": 1,
                        "api/v1/userDataStream": 1,
                        "api/v1/listenKey": 1
                    },
                    "delete": {
                        "api/v1/spot/order": 1.67,
                        "api/v1/futures/order": 1.67,
                        "api/v1/spot/openOrders": 8.35,
                        "api/v1/futures/batchOrders": 8.35,
                        "api/v1/spot/cancelOrderByIds": 8.35,
                        "api/v1/futures/cancelOrderByIds": 8.35,
                        "api/v1/listenKey": 1
                    },
                    "put": {
                        "api/v1/listenKey": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "3m": "3m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1h",
                "2h": "2h",
                "4h": "4h",
                "6h": "6h",
                "8h": "8h",
                "12h": "12h",
                "1d": "1d",
                "1w": "1w",
                "1M": "1M"
            },
            "fees": {
                "trading": {},
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "options": {
                "defaultType": "spot",
                "accountsByType": {
                    "spot": "MAIN",
                    "swap": "FUTURES"
                },
                "networks": {
                    "BTC": "BTC",
                    "ERC20": "ETH",
                    "ETH": "ETH",
                    "BEP20": "BSC",
                    "TRC20": "TRX",
                    "SOL": "SOL",
                    "MATIC": "MATIC",
                    "ARBONE": "ARBITRUM",
                    "BASE": "BASE",
                    "TON": "TON",
                    "AVAXC": "AVAXC",
                    "DOGE": "DOGE",
                    "XRP": "XRP",
                    "DOT": "DOT",
                    "ADA": "ADA",
                    "LTC": "LTC",
                    "APT": "APT",
                    "ATOM": "ATOM",
                    "ALGO": "ALGO",
                    "NEAR": "NEAR",
                    "XLM": "XLM",
                    "SUI": "SUI",
                    "ETC": "ETC",
                    "EOS": "EOS",
                    "WAVES": "WAVES",
                    "ICP": "ICP",
                    "ONE": "ONE"
                },
                "networksById": {
                    "ETH": "ERC20",
                    "ERC20": "ERC20"
                }
            },
            "features": {
                "spot": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyRequiresPrice": false,
                        "marketBuyByCost": false,
                        "selfTradePrevention": false,
                        "iceberg": false
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 1000,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": true
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    }
                },
                "forDerivatives": {},
                "swap": {},
                "future": {}
            }
        }"###).unwrap())
    }

    async fn fetch_status(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["status", "ping", "time", "system/status"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "status"), ("public", "GET", "ping"), ("public", "GET", "time"), ("sapi", "GET", "system/status")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_time(&mut self, mut params: Value) -> Value {
        let candidates = vec![("public", "GET", "time"), ("public", "GET", "server/time"), ("public", "GET", "timestamp")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_currencies(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.dispatch("commonGetApiV1ExchangeInfo".into(), params.clone(), Value::Undefined).await;
        self.get("options".into()).set("exchangeInfo".into(), response.clone());
        // we store it in options for later use in fetchMarkets
        //
        //    {
        //        "timezone": "UTC",
        //        "serverTime": "1755583099926",
        //        "brokerFilters": [],
        //        "symbols": [
        //            {
        //                "filters": [
        //                    {
        //                        "minPrice": "0.01",
        //                        "maxPrice": "10000000.00000000",
        //                        "tickSize": "0.01",
        //                        "filterType": "PRICE_FILTER"
        //                    },
        //                    {
        //                        "minQty": "0.0001",
        //                        "maxQty": "4000",
        //                        "stepSize": "0.0001",
        //                        "filterType": "LOT_SIZE"
        //                    },
        //                    {
        //                        "minNotional": "5",
        //                        "filterType": "MIN_NOTIONAL"
        //                    },
        //                    {
        //                        "minAmount": "5",
        //                        "maxAmount": "6600000",
        //                        "minBuyPrice": "0.01",
        //                        "filterType": "TRADE_AMOUNT"
        //                    },
        //                    {
        //                        "maxSellPrice": "99999999",
        //                        "buyPriceUpRate": "0.1",
        //                        "sellPriceDownRate": "0.1",
        //                        "filterType": "LIMIT_TRADING"
        //                    },
        //                    {
        //                        "buyPriceUpRate": "0.1",
        //                        "sellPriceDownRate": "0.1",
        //                        "filterType": "MARKET_TRADING"
        //                    },
        //                    {
        //                        "noAllowMarketStartTime": "0",
        //                        "noAllowMarketEndTime": "0",
        //                        "limitOrderStartTime": "0",
        //                        "limitOrderEndTime": "0",
        //                        "limitMinPrice": "0",
        //                        "limitMaxPrice": "0",
        //                        "filterType": "OPEN_QUOTE"
        //                    }
        //                ],
        //                "exchangeId": "301",
        //                "symbol": "ETHUSDT",
        //                "symbolName": "ETHUSDT",
        //                "status": "TRADING",
        //                "baseAsset": "ETH",
        //                "baseAssetName": "ETH",
        //                "baseAssetPrecision": "0.0001",
        //                "quoteAsset": "USDT",
        //                "quoteAssetName": "USDT",
        //                "quotePrecision": "0.01",
        //                "icebergAllowed": false,
        //                "isAggregate": false,
        //                "allowMargin": true,
        //             }
        //        ],
        //        "options": [],
        //        "contracts": [
        //            {
        //                 "filters": [ ... ],
        //                 "exchangeId": "301",
        //                 "symbol": "BTC-SWAP-USDT",
        //                 "symbolName": "BTC-SWAP-USDTUSDT",
        //                 "status": "TRADING",
        //                 "baseAsset": "BTC-SWAP-USDT",
        //                 "baseAssetPrecision": "0.001",
        //                 "quoteAsset": "USDT",
        //                 "quoteAssetPrecision": "0.1",
        //                 "icebergAllowed": false,
        //                 "inverse": false,
        //                 "index": "BTC",
        //                 "indexToken": "BTCUSDT",
        //                 "marginToken": "USDT",
        //                 "marginPrecision": "0.0001",
        //                 "contractMultiplier": "0.001",
        //                 "underlying": "BTC",
        //                 "riskLimits": [
        //                     {
        //                         "riskLimitId": "200020911",
        //                         "quantity": "42000.0",
        //                         "initialMargin": "0.02",
        //                         "maintMargin": "0.01",
        //                         "isWhite": false
        //                     },
        //                     {
        //                         "riskLimitId": "200020912",
        //                         "quantity": "84000.0",
        //                         "initialMargin": "0.04",
        //                         "maintMargin": "0.02",
        //                         "isWhite": false
        //                     },
        //                     ...
        //                 ]
        //            },
        //        ],
        //        "coins": [
        //            {
        //                "orgId": "9001",
        //                "coinId": "TCOM",
        //                "coinName": "TCOM",
        //                "coinFullName": "TCOM",
        //                "allowWithdraw": true,
        //                "allowDeposit": true,
        //                "chainTypes": [
        //                    {
        //                        "chainType": "BSC",
        //                        "withdrawFee": "49.55478",
        //                        "minWithdrawQuantity": "77",
        //                        "maxWithdrawQuantity": "0",
        //                        "minDepositQuantity": "48",
        //                        "allowDeposit": true,
        //                        "allowWithdraw": false
        //                    }
        //                ],
        //                "isVirtual": false
        //            },
        //          ...
        //
        let mut coins: Value = self.safe_list(response.clone(), Value::from("coins"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < coins.len() {
            let mut coin: Value = coins.get(i.into());
            let mut parsed: Value = <Self as Toobit>::parse_currency(self, coin.clone());
            let mut code: Value = parsed.get(Value::from("code"));
            result.set(code.clone(), parsed.clone());
            i += 1;
        };
        return result.clone();
    }

    fn parse_currency(&self, mut raw_currency: Value) -> Value {
        let mut id: Value = self.safe_string(raw_currency.clone(), Value::from("coinId"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(id.clone(), Value::Undefined);
        let mut networks: Value = Value::new_object();
        let mut raw_networks: Value = self.safe_list(raw_currency.clone(), Value::from("chainTypes"), Value::Undefined);
        let mut j: usize = 0;
        while j < raw_networks.len() {
            let mut raw_network: Value = raw_networks.get(j.into());
            let mut network_id: Value = self.safe_string(raw_network.clone(), Value::from("chainType"), Value::Undefined);
            let mut network_code: Value = self.network_id_to_code(network_id.clone(), Value::Undefined);
            networks.set(network_code.clone(), Value::Json(normalize(&Value::Json(json!({
                "id": network_id.clone(),
                "network": network_code.clone(),
                "margin": Value::Undefined,
                "deposit": self.safe_bool(raw_network.clone(), Value::from("allowDeposit"), Value::Undefined),
                "withdraw": self.safe_bool(raw_network.clone(), Value::from("allowWithdraw"), Value::Undefined),
                "active": Value::Undefined,
                "fee": self.safe_number(raw_network.clone(), Value::from("withdrawFee"), Value::Undefined),
                "precision": Value::Undefined,
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "deposit": Value::Json(normalize(&Value::Json(json!({
                        "min": self.safe_number(raw_network.clone(), Value::from("minDepositQuantity"), Value::Undefined),
                        "max": Value::Undefined
                    }))).unwrap()),
                    "withdraw": Value::Json(normalize(&Value::Json(json!({
                        "min": self.safe_number(raw_network.clone(), Value::from("minWithdrawQuantity"), Value::Undefined),
                        "max": self.safe_number(raw_network.clone(), Value::from("maxWithdrawQuantity"), Value::Undefined)
                    }))).unwrap())
                }))).unwrap()),
                "info": raw_network.clone()
            }))).unwrap()));
            j += 1;
        };
        return self.safe_currency_structure(Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "code": code.clone(),
            "name": self.safe_string(raw_currency.clone(), Value::from("coinFullName"), Value::Undefined),
            "type": Value::Undefined,
            "active": Value::Undefined,
            "deposit": self.safe_bool(raw_currency.clone(), Value::from("allowDeposit"), Value::Undefined),
            "withdraw": self.safe_bool(raw_currency.clone(), Value::from("allowWithdraw"), Value::Undefined),
            "fee": Value::Undefined,
            "precision": Value::Undefined,
            "limits": Value::Json(normalize(&Value::Json(json!({
                "amount": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "withdraw": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap())
            }))).unwrap()),
            "networks": networks.clone(),
            "info": raw_currency.clone()
        }))).unwrap()));
    }

    

    fn parse_market(&self, mut market: Value) -> Value {
        let mut id: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        let mut base_id: Value = self.safe_string(market.clone(), Value::from("baseAsset"), Value::Undefined);
        let mut quote_id: Value = self.safe_string(market.clone(), Value::from("quoteAsset"), Value::Undefined);
        let mut base_parts: Value = base_id.split(Value::from("-"));
        let mut base_id_clean: Value = base_parts.get(Value::from(0));
        let mut base: Value = self.safe_currency_code(base_id_clean.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        let mut settle_id: Value = self.safe_string(market.clone(), Value::from("marginToken"), Value::Undefined);
        let mut settle: Value = self.safe_currency_code(settle_id.clone(), Value::Undefined);
        let mut status: Value = self.safe_string(market.clone(), Value::from("status"), Value::Undefined);
        let mut active: Value = Value::from(status.clone() == Value::from("TRADING"));
        let mut filters: Value = self.safe_list(market.clone(), Value::from("filters"), Value::new_array());
        let mut filters_by_type: Value = self.index_by(filters.clone(), Value::from("filterType"));
        let mut price_filter: Value = self.safe_dict(filters_by_type.clone(), Value::from("PRICE_FILTER"), Value::new_object());
        let mut lot_size_filter: Value = self.safe_dict(filters_by_type.clone(), Value::from("LOT_SIZE"), Value::new_object());
        let mut min_notional_filter: Value = self.safe_dict(filters_by_type.clone(), Value::from("MIN_NOTIONAL"), Value::new_object());
        let mut symbol: Value = base.clone() + Value::from("/") + quote.clone();
        let mut is_contract: Value = market.contains_key(Value::from("contractMultiplier")).into();
        let mut inverse: Value = self.safe_bool_2(market.clone(), Value::from("isInverse"), Value::from("inverse"), Value::Undefined);
        if is_contract.is_truthy() {
            symbol = symbol +  Value::from(":") + settle.clone();
        };
        return self.safe_market_structure(Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "symbol": symbol.clone(),
            "base": base.clone(),
            "quote": quote.clone(),
            "settle": settle.clone(),
            "baseId": base_id.clone(),
            "quoteId": quote_id.clone(),
            "settleId": settle_id.clone(),
            "type": if is_contract.is_truthy() { Value::from("swap") } else { Value::from("spot") },
            "spot": Value::from(!is_contract.is_truthy()),
            "margin": Value::from(false),
            "swap": is_contract.clone(),
            "future": Value::from(false),
            "option": Value::from(false),
            "active": active.clone(),
            "contract": is_contract.clone(),
            "linear": if is_contract.is_truthy() { Value::from(!inverse.is_truthy()) } else { Value::Undefined },
            "inverse": if is_contract.is_truthy() { inverse.clone() } else { Value::Undefined },
            "contractSize": self.safe_number(market.clone(), Value::from("contractMultiplier"), Value::Undefined),
            "expiry": Value::Undefined,
            "expiryDatetime": Value::Undefined,
            "strike": Value::Undefined,
            "optionType": Value::Undefined,
            "precision": Value::Json(normalize(&Value::Json(json!({
                "amount": self.safe_number(lot_size_filter.clone(), Value::from("stepSize"), Value::Undefined),
                "price": self.safe_number(price_filter.clone(), Value::from("tickSize"), Value::Undefined)
            }))).unwrap()),
            "limits": Value::Json(normalize(&Value::Json(json!({
                "leverage": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "amount": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(lot_size_filter.clone(), Value::from("minQty"), Value::Undefined),
                    "max": self.safe_number(lot_size_filter.clone(), Value::from("maxQty"), Value::Undefined)
                }))).unwrap()),
                "price": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(price_filter.clone(), Value::from("minPrice"), Value::Undefined),
                    "max": self.safe_number(price_filter.clone(), Value::from("maxPrice"), Value::Undefined)
                }))).unwrap()),
                "cost": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(min_notional_filter.clone(), Value::from("minNotional"), Value::Undefined),
                    "max": Value::Undefined
                }))).unwrap())
            }))).unwrap()),
            "created": Value::Undefined,
            "info": market.clone()
        }))).unwrap()));
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut ob = crate::exchange::unwrap_response_order_book(&json_resp);
        if let serde_json::Value::Object(ref mut map) = ob {
            if !map.contains_key("symbol") { map.insert("symbol".to_string(), serde_json::json!(symbol.unwrap_str())); }
        }
        Value::Json(ob)
    }


    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        let mut raw: Value = Value::Undefined;
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { raw = rv; break; }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            // Already-parsed trades have both `price` and `amount` keys.
            if item.get("price").is_some() && item.get("amount").is_some() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_trade(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    fn parse_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchTrades
        //
        //        {
        //            "t": "1755594277287",
        //            "p": "115276.99",
        //            "q": "0.001508",
        //            "ibm": true
        //        },
        //        // watchTrades have also an additional fields:
        //             "v": "4864732022868004630",   // trade id
        //             "m": true,                    // is the buyer taker
        //
        // fetchMyTrades
        //
        //        {
        //            "id": "2024934575206059008",
        //            "symbol": "ETHUSDT",
        //            "orderId": "2024934575097029888",
        //            "ticketId": "4864450547563401875",
        //            "price": "4641.21",
        //            "qty": "0.001",
        //            "time": "1756127012094",
        //            "isMaker": false,
        //            "commission": "0.00464121",
        //            "commissionAsset": "USDT",
        //            "makerRebate": "0",
        //            "symbolName": "ETHUSDT",                 // only in SPOT
        //            "isBuyer": false,                        // only in SPOT
        //            "feeAmount": "0.00464121",               // only in SPOT
        //            "feeCoinId": "USDT",                     // only in SPOT
        //            "fee": {                                 // only in SPOT
        //                "feeCoinId": "USDT",
        //                "feeCoinName": "USDT",
        //                "fee": "0.00464121"
        //            },
        //            "type": "LIMIT",                         // only in CONTRACT
        //            "side": "BUY_OPEN",                      // only in CONTRACT
        //            "realizedPnl": "0",                      // only in CONTRACT
        //        },
        //
        let mut timestamp: Value = self.safe_integer_2(trade.clone(), Value::from("t"), Value::from("time"), Value::Undefined);
        let mut price_string: Value = self.safe_string_2(trade.clone(), Value::from("p"), Value::from("price"), Value::Undefined);
        let mut amount_string: Value = self.safe_string_2(trade.clone(), Value::from("q"), Value::from("qty"), Value::Undefined);
        let mut is_buyer: Value = self.safe_bool(trade.clone(), Value::from("isBuyer"), Value::Undefined);
        let mut side: Value = Value::Undefined;
        let mut is_buyer_maker: Value = self.safe_bool(trade.clone(), Value::from("ibm"), Value::Undefined);
        if is_buyer_maker.clone().is_nullish() {
            let mut is_buyer_taker: Value = self.safe_bool(trade.clone(), Value::from("m"), Value::Undefined);
            if is_buyer_taker.clone().is_nonnullish() {
                is_buyer_maker = Value::from(!is_buyer_taker.is_truthy());
            };
        };
        if is_buyer_maker.clone().is_nonnullish() {
            if is_buyer_maker.is_truthy() {
                side = Value::from("sell");
            } else {
                side = Value::from("buy");
            };
        } else {
            if is_buyer.is_truthy() {
                side = Value::from("buy");
            } else {
                side = Value::from("sell");
            };
        };
        let mut fee_currency_id: Value = self.safe_string(trade.clone(), Value::from("feeCoinId"), Value::Undefined);
        let mut fee_amount: Value = self.safe_string(trade.clone(), Value::from("feeAmount"), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        if fee_amount.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "currency": self.safe_currency_code(fee_currency_id.clone(), Value::Undefined),
                "cost": fee_amount.clone()
            }))).unwrap());
        };
        let mut is_maker: Value = self.safe_bool(trade.clone(), Value::from("isMaker"), Value::Undefined);
        let mut taker_or_maker: Value = Value::Undefined;
        if is_maker.clone().is_nonnullish() {
            taker_or_maker = if is_maker.is_truthy() { Value::from("maker") } else { Value::from("taker") };
        };
        market = self.safe_market(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol.clone(),
            "id": self.safe_string_2(trade.clone(), Value::from("id"), Value::from("v"), Value::Undefined),
            "order": self.safe_string(trade.clone(), Value::from("orderId"), Value::Undefined),
            "type": Value::Undefined,
            "side": side.clone(),
            "amount": amount_string.clone(),
            "price": price_string.clone(),
            "cost": Value::Undefined,
            "takerOrMaker": taker_or_maker.clone(),
            "fee": fee.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        // Match endpoints by substring on common OHLCV path tokens (klines, candles,
        // ohlcv, barhist, history, kline, candle, ohlc).
        'outer: for token in ["klines", "candles", "ohlcv", "barhist", "kline", "candle", "ohlc"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            if item.is_array() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_ohlcv(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        return Value::Json(serde_json::Value::Array(vec![(self.safe_integer_n(ohlcv.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from(0)).into(), (Value::from("time")).into(), (Value::from("t")).into()])), Value::Undefined)).into(), (self.safe_number_n(ohlcv.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from(1)).into(), (Value::from("open")).into(), (Value::from("o")).into()])), Value::Undefined)).into(), (self.safe_number_n(ohlcv.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from(2)).into(), (Value::from("high")).into(), (Value::from("h")).into()])), Value::Undefined)).into(), (self.safe_number_n(ohlcv.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from(3)).into(), (Value::from("low")).into(), (Value::from("l")).into()])), Value::Undefined)).into(), (self.safe_number_n(ohlcv.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from(4)).into(), (Value::from("close")).into(), (Value::from("c")).into()])), Value::Undefined)).into(), (self.safe_number_n(ohlcv.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from(5)).into(), (Value::from("volume")).into(), (Value::from("v")).into()])), Value::Undefined)).into()]));
    }

    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("s"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("t"), Value::Undefined);
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("c"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("symbol")),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "high": self.safe_string(ticker.clone(), Value::from("h"), Value::Undefined),
            "low": self.safe_string(ticker.clone(), Value::from("l"), Value::Undefined),
            "bid": Value::Undefined,
            "bidVolume": Value::Undefined,
            "ask": Value::Undefined,
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": self.safe_string(ticker.clone(), Value::from("o"), Value::Undefined),
            "close": last.clone(),
            "last": last.clone(),
            "previousClose": Value::Undefined,
            "change": self.safe_string(ticker.clone(), Value::from("pc"), Value::Undefined),
            "percentage": self.safe_string(ticker.clone(), Value::from("pcp"), Value::Undefined),
            "average": Value::Undefined,
            "baseVolume": self.safe_string(ticker.clone(), Value::from("v"), Value::Undefined),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("qv"), Value::Undefined),
            "info": ticker.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_last_prices(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut request: Value = Value::new_object();
        if symbols.clone().is_nonnullish() {
            let mut length: usize = symbols.len();
            if length == Value::from(1) {
                let mut market: Value = self.market(symbols.get(Value::from(0)));
                request.set("symbol".into(), market.get(Value::from("id")));
            };
        };
        let mut response: Value = self.dispatch("commonGetQuoteV1TickerPrice".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    [
        //        {
        //            "s": "BNTUSDT",
        //            "si": "BNTUSDT",
        //            "p": "0.823"
        //        },
        //
        return self.parse_last_prices(response.clone(), symbols.clone(), Value::Undefined).await;
    }

    fn parse_last_price(&self, mut entry: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(entry.clone(), Value::from("s"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("symbol")),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "price": self.safe_number_omit_zero(entry.clone(), Value::from("price"), Value::Undefined),
            "side": Value::Undefined,
            "info": entry.clone()
        }))).unwrap());
    }

    async fn fetch_bids_asks(&mut self, mut symbols: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbols.is_nonnullish() { request.set("symbols".into(), symbols.clone()); }
        let candidates = vec![("public", "GET", "ticker/bookTicker"), ("public", "GET", "bookticker"), ("public", "GET", "bidsasks"), ("public", "GET", "tickers")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_bids_asks_custom(&self, mut tickers: Value, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut results: Value = Value::new_array();
        let mut i: usize = 0;
        while i < tickers.len() {
            let mut parsed_ticker: Value = <Self as Toobit>::parse_bid_ask_custom(self, tickers.get(i.into()));
            let mut ticker: Value = extend_2(parsed_ticker.clone(), params.clone());
            results.push(ticker.clone());
            i += 1;
        };
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        return self.filter_by_array(results.clone(), Value::from("symbol"), symbols.clone(), Value::Undefined);
    }

    fn parse_bid_ask_custom(&self, mut ticker: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "timestamp": self.safe_string(ticker.clone(), Value::from("t"), Value::Undefined),
            "symbol": self.safe_string(ticker.clone(), Value::from("s"), Value::Undefined),
            "bid": self.safe_number(ticker.clone(), Value::from("b"), Value::Undefined),
            "bidVolume": self.safe_number(ticker.clone(), Value::from("bq"), Value::Undefined),
            "ask": self.safe_number(ticker.clone(), Value::from("a"), Value::Undefined),
            "askVolume": self.safe_number(ticker.clone(), Value::from("aq"), Value::Undefined),
            "info": ticker.clone()
        }))).unwrap());
    }

    async fn fetch_funding_rates(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut request: Value = Value::new_object();
        if symbols.clone().is_nonnullish() {
            let mut length: usize = symbols.len();
            if length == Value::from(1) {
                let mut market: Value = self.market(symbols.get(Value::from(0)));
                request.set("symbol".into(), market.get(Value::from("id")));
            };
        };
        let mut response: Value = self.dispatch("commonGetApiV1FuturesFundingRate".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    [
        //        {
        //            "symbol": "BTC-SWAP-USDT",
        //            "rate": "0.0001071148112848",
        //            "nextFundingTime": "1755964800000"
        //        },...
        //
        return self.parse_funding_rates(response.clone(), symbols.clone()).await;
    }

    fn parse_funding_rate(&self, mut contract: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(contract.clone(), Value::from("symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut next_funding_rate: Value = self.safe_number(contract.clone(), Value::from("rate"), Value::Undefined);
        let mut next_funding_rate_timestamp: Value = self.safe_integer(contract.clone(), Value::from("nextFundingTime"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": contract.clone(),
            "symbol": symbol.clone(),
            "markPrice": Value::Undefined,
            "indexPrice": Value::Undefined,
            "interestRate": Value::Undefined,
            "estimatedSettlePrice": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "previousFundingRate": Value::Undefined,
            "nextFundingRate": Value::Undefined,
            "previousFundingTimestamp": Value::Undefined,
            "nextFundingTimestamp": Value::Undefined,
            "previousFundingDatetime": Value::Undefined,
            "nextFundingDatetime": Value::Undefined,
            "fundingRate": next_funding_rate.clone(),
            "fundingTimestamp": next_funding_rate_timestamp.clone(),
            "fundingDatetime": self.iso8601(next_funding_rate_timestamp.clone()),
            "interval": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_funding_rate_history(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = Value::from(false);
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchFundingRateHistory"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_deterministic(Value::from("fetchFundingRateHistory"), symbol.clone(), since.clone(), limit.clone(), Value::from("8h"), params.clone(), Value::Undefined).await;
        };
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        let mut response: Value = self.dispatch("commonGetApiV1FuturesHistoryFundingRate".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    [
        //        {
        //            "id": "869931",
        //            "symbol": "BTC-SWAP-USDT",
        //            "settleTime": "1755936000000",
        //            "settleRate": "0.0001"
        //        }, ...
        //
        return self.parse_funding_rate_histories(response.clone(), market.clone(), since.clone(), limit.clone()).await;
    }

    fn parse_funding_rate_history(&self, mut contract: Value, mut market: Value) -> Value {
        let mut timestamp: Value = self.safe_integer(contract.clone(), Value::from("settleTime"), Value::Undefined);
        let mut market_id: Value = self.safe_string(contract.clone(), Value::from("symbol"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": contract.clone(),
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined),
            "fundingRate": self.safe_number(contract.clone(), Value::from("settleRate"), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone())
        }))).unwrap());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["account/balance", "balance", "wallet/balance", "accounts/balance", "account"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(&format!("/{}/", token)) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "balance"), ("private", "GET", "account/balance"), ("private", "GET", "wallet/balance")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response.clone(),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined
        }))).unwrap());
        let mut balances: Value = self.safe_list(response.clone(), Value::from("balances"), response.clone());
        let mut i: usize = 0;
        while i < balances.len() {
            let mut balance: Value = balances.get(i.into());
            let mut code: Value = self.safe_currency_code(self.safe_string(balance.clone(), Value::from("asset"), Value::Undefined), Value::Undefined);
            let mut account: Value = self.account();
            account.set("free".into(), self.safe_string_2(balance.clone(), Value::from("free"), Value::from("availableBalance"), Value::Undefined));
            account.set("total".into(), self.safe_string_2(balance.clone(), Value::from("total"), Value::from("balance"), Value::Undefined));
            account.set("used".into(), self.safe_string(balance.clone(), Value::from("locked"), Value::Undefined));
            result.set(code.clone(), account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("type".into(), r#type.clone());
        request.set("side".into(), side.clone());
        request.set("amount".into(), amount.clone());
        if price.is_nonnullish() { request.set("price".into(), price.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "POST" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "POST", "order"), ("private", "POST", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn create_order_request(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut market: Value = self.market(symbol.clone());
        let mut id: Value = market.get(Value::from("id"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": id.clone(),
            "side": side.to_upper_case()
        }))).unwrap());
        if price.clone().is_nonnullish() {
            request.set("price".into(), self.price_to_precision(symbol.clone(), price.clone()));
        };
        let mut cost: Value = Value::Undefined;
        (cost, params) = shift_2(self.handle_param_string(params.clone(), Value::from("cost"), Value::Undefined));
        if r#type.clone() == Value::from("market") {
            if cost.clone().is_nullish() && side.clone() == Value::from("buy") {
                return Value::Undefined;
            } else {
                request.set("quantity".into(), self.cost_to_precision(symbol.clone(), cost.clone()));
            };
        } else {
            request.set("quantity".into(), self.amount_to_precision(symbol.clone(), amount.clone()));
        };
        let mut is_post_only: Value = Value::Undefined;
        (is_post_only, params) = shift_2(self.handle_post_only(Value::from(r#type.clone() == Value::from("market")), Value::from(false), params.clone()));
        if is_post_only.is_truthy() {
            request.set("type".into(), Value::from("LIMIT_MAKER"));
        } else {
            request.set("type".into(), r#type.to_upper_case());
        };
        return Value::Json(serde_json::Value::Array(vec![(request.clone()).into(), (params.clone()).into()]));
    }

    fn create_contract_order_request(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id")),
            "quantity": self.amount_to_precision(symbol.clone(), amount.clone())
        }))).unwrap());
        let mut reduce_only: Value = Value::Undefined;
        (reduce_only, params) = shift_2(self.handle_param_bool(params.clone(), Value::from("reduceOnly"), Value::Undefined));
        if side.clone() == Value::from("buy") {
            side = if reduce_only.is_truthy() { Value::from("SELL_CLOSE") } else { Value::from("BUY_OPEN") };
        } else if side.clone() == Value::from("sell") {
            side = if reduce_only.is_truthy() { Value::from("BUY_CLOSE") } else { Value::from("SELL_OPEN") };
        };
        request.set("side".into(), side.clone());
        if price.clone().is_nonnullish() {
            request.set("price".into(), self.price_to_precision(symbol.clone(), price.clone()));
        };
        if self.in_array(r#type.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from("limit")).into(), (Value::from("LIMIT")).into()]))).is_truthy() {
            request.set("type".into(), r#type.to_upper_case());
            request.set("price".into(), self.price_to_precision(symbol.clone(), price.clone()));
        } else if r#type.clone() == Value::from("market") {
            request.set("type".into(), Value::from("LIMIT"));
            // weird, but exchange works this way
            request.set("priceType".into(), Value::from("MARKET"));
        };
        let mut is_post_only: Value = Value::Undefined;
        (is_post_only, params) = shift_2(self.handle_post_only(Value::from(r#type.clone() == Value::from("market")), Value::from(false), params.clone()));
        if is_post_only.is_truthy() {
            request.set("timeInForce".into(), Value::from("LIMIT_MAKER"));
        };
        let mut values: Value = self.handle_trigger_prices_and_params(symbol.clone(), params.clone(), Value::Undefined);
        let mut trigger_price: Value = values.get(Value::from(0));
        params = values.get(Value::from(3));
        if trigger_price.clone().is_nonnullish() {
            request.set("stopPrice".into(), trigger_price.clone());
        };
        let mut stop_loss: Value = self.safe_dict(params.clone(), Value::from("stopLoss"), Value::Undefined);
        let mut take_profit: Value = self.safe_dict(params.clone(), Value::from("takeProfit"), Value::Undefined);
        let mut has_stop_loss: Value = Value::from(stop_loss.clone().is_nonnullish());
        let mut has_take_profit: Value = Value::from(take_profit.clone().is_nonnullish());
        let mut trigger_price_types: Value = Value::Json(normalize(&Value::Json(json!({
            "mark": Value::from("MARK_PRICE"),
            "last": Value::from("CONTRACT_PRICE")
        }))).unwrap());
        if has_stop_loss.is_truthy() {
            request.set("stopLoss".into(), self.safe_value(stop_loss.clone(), Value::from("triggerPrice"), Value::Undefined));
            let mut limit_price: Value = self.safe_value(stop_loss.clone(), Value::from("price"), Value::Undefined);
            if limit_price.clone().is_nonnullish() {
                request.set("slOrderType".into(), Value::from("LIMIT"));
                request.set("slLimitPrice".into(), self.price_to_precision(symbol.clone(), limit_price.clone()));
            };
            let mut trigger_price_type: Value = self.safe_string(stop_loss.clone(), Value::from("triggerPriceType"), Value::Undefined);
            if trigger_price_type.clone().is_nonnullish() {
                request.set("slTriggerBy".into(), self.safe_string(trigger_price_types.clone(), trigger_price_type.clone(), trigger_price_type.clone()));
            };
            params = self.omit(params.clone(), Value::from("stopLoss"));
        };
        if has_take_profit.is_truthy() {
            request.set("takeProfit".into(), self.safe_value(take_profit.clone(), Value::from("triggerPrice"), Value::Undefined));
            let mut limit_price: Value = self.safe_value(take_profit.clone(), Value::from("price"), Value::Undefined);
            if limit_price.clone().is_nonnullish() {
                request.set("tpOrderType".into(), Value::from("LIMIT"));
                request.set("tpLimitPrice".into(), self.price_to_precision(symbol.clone(), limit_price.clone()));
            };
            let mut trigger_price_type: Value = self.safe_string(take_profit.clone(), Value::from("triggerPriceType"), Value::Undefined);
            if trigger_price_type.clone().is_nonnullish() {
                request.set("tpTriggerBy".into(), self.safe_string(trigger_price_types.clone(), trigger_price_type.clone(), trigger_price_type.clone()));
            };
            params = self.omit(params.clone(), Value::from("takeProfit"));
        };
        if !params.contains_key(Value::from("newClientOrderId")) {
            request.set("newClientOrderId".into(), self.uuid());
        };
        return Value::Json(serde_json::Value::Array(vec![(request.clone()).into(), (params.clone()).into()]));
    }

    fn parse_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        // createOrder, cancelOrder
        //
        //     {
        //         "symbol": "ETHUSDT",
        //         "price": "0",
        //         "origQty": "0.001",
        //         "orderId": "2024837825254460160",
        //         "clientOrderId": "1756115478113679",
        //         "executedQty": "0",
        //         "status": "PENDING_NEW",
        //         "timeInForce": "GTC",
        //         "type": "MARKET",
        //         "side": "SELL"
        //         "accountId": "1783404067076253952",    // only in spot
        //         "symbolName": "ETHUSDT",               // only in spot
        //         "transactTime": "1756115478604",       // only in spot
        //         "time": "1668418485058",               // only in contract
        //         "updateTime": "1668418485058",         // only in contract
        //         "leverage": "2",                       // only in contract
        //         "avgPrice": "0",                       // only in contract
        //         "marginLocked": "9.5",                 // only in contract
        //         "priceType": "INPUT"                   // only in contract
        //     }
        //
        //
        // fetchOrder, fetchOrders, fetchOpenOrders
        //
        //    {
        //        "time": "1756140208069",
        //        "updateTime": "1756140208078",
        //        "orderId": "2025045271033977089",
        //        "clientOrderId": "17561402075722006",
        //        "symbol": "ETHUSDT",
        //        "price": "3000",
        //        "origQty": "0.002",
        //        "executedQty": "0",
        //        "avgPrice": "0",
        //        "type": "LIMIT",
        //        "side": "BUY",
        //        "timeInForce": "GTC",
        //        "status": "NEW",
        //        "accountId": "1783404067076253952",  // only in SPOT
        //        "exchangeId": "301",                 // only in SPOT
        //        "symbolName": "ETHUSDT",             // only in SPOT
        //        "cummulativeQuoteQty": "0",          // only in SPOT
        //        "cumulativeQuoteQty": "0",           // only in SPOT
        //        "stopPrice": "0.0",                  // only in SPOT
        //        "icebergQty": "0.0",                 // only in SPOT
        //        "isWorking": true                    // only in SPOT
        //        "leverage": "2",                     // only in CONTRACT
        //        "marginLocked": "9.5",               // only in CONTRACT
        //        "priceType": "INPUT"                 // only in CONTRACT
        //        "triggerType": "0",                  // only in CONTRACT fetchClosedOrders
        //        "fallType": "0",                     // only in CONTRACT fetchClosedOrders
        //        "activeStatus": "0"                  // only in CONTRACT fetchClosedOrders
        //    }
        //
        let mut timestamp: Value = self.safe_integer_2(order.clone(), Value::from("transactTime"), Value::from("time"), Value::Undefined);
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut raw_type: Value = self.safe_string(order.clone(), Value::from("type"), Value::Undefined);
        let mut raw_side_lower: Value = self.safe_string_lower(order.clone(), Value::from("side"), Value::Undefined);
        let mut trigger_price: Value = self.omit_zero(self.safe_string(order.clone(), Value::from("stopPrice"), Value::Undefined));
        if trigger_price.clone() == Value::from("0.0") {
            trigger_price = Value::Undefined;
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order.clone(),
            "id": self.safe_string(order.clone(), Value::from("orderId"), Value::Undefined),
            "clientOrderId": self.safe_string(order.clone(), Value::from("clientOrderId"), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": Value::Undefined,
            "lastUpdateTimestamp": self.safe_integer(order.clone(), Value::from("updateTime"), Value::Undefined),
            "status": <Self as Toobit>::parse_order_status(self, self.safe_string(order.clone(), Value::from("status"), Value::Undefined)),
            "symbol": market.get(Value::from("symbol")),
            "type": <Self as Toobit>::parse_order_type(self, raw_type.clone()),
            "timeInForce": self.safe_string(order.clone(), Value::from("timeInForce"), Value::Undefined),
            "postOnly": Value::from(raw_type.clone() == Value::from("LIMIT_MAKER")),
            "side": raw_side_lower.clone(),
            "price": self.omit_zero(self.safe_string(order.clone(), Value::from("price"), Value::Undefined)),
            "triggerPrice": trigger_price.clone(),
            "cost": self.omit_zero(self.safe_string(order.clone(), Value::from("cumulativeQuoteQty"), Value::Undefined)),
            "average": self.safe_string(order.clone(), Value::from("avgPrice"), Value::Undefined),
            "amount": self.safe_string(order.clone(), Value::from("origQty"), Value::Undefined),
            "filled": self.safe_string(order.clone(), Value::from("executedQty"), Value::Undefined),
            "remaining": Value::Undefined,
            "trades": Value::Undefined,
            "fee": Value::Undefined,
            "marginMode": Value::Undefined,
            "reduceOnly": Value::Undefined,
            "leverage": Value::Undefined,
            "hedged": Value::Undefined
        }))).unwrap()), market.clone());
    }

    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "PENDING_NEW": Value::from("open"),
            "NEW": Value::from("open"),
            "PARTIALLY_FILLED": Value::from("open"),
            "FILLED": Value::from("closed"),
            "PENDING_CANCEL": Value::from("canceled"),
            "CANCELED": Value::from("canceled"),
            "REJECTED": Value::from("canceled")
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_order_type(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "MARKET": Value::from("market"),
            "LIMIT": Value::from("limit"),
            "LIMIT_MAKER": Value::from("limit")
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("orderId".into(), id.clone());
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "DELETE" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "DELETE", "order"), ("private", "DELETE", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn cancel_all_orders(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::new_object();
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            request.set("symbol".into(), market.get(Value::from("id")));
        };
        let mut market_type: Value = Value::Undefined;
        (market_type, params) = shift_2(self.handle_market_type_and_params(Value::from("cancelAllOrders"), market.clone(), params.clone(), Value::from("none")));
        if market_type.clone() == Value::from("none") {
            return Value::Undefined;
        };
        let mut response: Value = Value::Undefined;
        if market_type.clone() == Value::from("spot") {
            response = self.dispatch("privateDeleteApiV1SpotOpenOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            //
            // {"success":true}  // always same response
            //
            response = self.dispatch("privateDeleteApiV1FuturesBatchOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        // { "code": 200, "message":"success", "timestamp":1541161088303 }
        //
        return Value::Json(serde_json::Value::Array(vec![(self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": response.clone()
        }))).unwrap()), Value::Undefined).await).into()]));
    }

    async fn cancel_orders(&mut self, mut ids: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut ids_string: Value = ids.join(Value::from(","));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "ids": ids_string.clone()
        }))).unwrap());
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        let mut market_type: Value = Value::Undefined;
        (market_type, params) = shift_2(self.handle_market_type_and_params(Value::from("cancelOrders"), market.clone(), params.clone(), Value::from("none")));
        if market_type.clone() == Value::from("none") {
            return Value::Undefined;
        };
        let mut response: Value = Value::Undefined;
        if market_type.clone() == Value::from("spot") {
            response = self.dispatch("privateDeleteApiV1SpotCancelOrderByIds".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            //
            // {"success":true}  // always same response
            //
            response = self.dispatch("privateDeleteApiV1FuturesCancelOrderByIds".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        // {
        //     "code":200,
        //     "result":[
        //         {
        //             "orderId":"1327047813809448704",
        //             "code":-2013
        //         },
        //         {
        //             "orderId":"1327047814212101888",
        //             "code":-2013
        //         }
        //     ]
        // }
        //
        // or empty array if no orders were canceled
        let mut result: Value = self.safe_list(response.clone(), Value::from("result"), Value::new_array());
        return self.parse_orders(result.clone(), market.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            return Value::Undefined;
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "orderId": id.clone()
        }))).unwrap());
        let mut market: Value = self.market(symbol.clone());
        let mut response: Value = Value::Undefined;
        if market.get(Value::from("spot")).is_truthy() {
            response = self.dispatch("privateGetApiV1SpotOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            response = self.dispatch("privateGetApiV1FuturesOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        //    {
        //        "time": "1756140208069",
        //        "updateTime": "1756140208078",
        //        "orderId": "2025045271033977089",
        //        "clientOrderId": "17561402075722006",
        //        "symbol": "ETHUSDT",
        //        "price": "3000",
        //        "origQty": "0.002",
        //        "executedQty": "0",
        //        "avgPrice": "0",
        //        "type": "LIMIT",
        //        "side": "BUY",
        //        "timeInForce": "GTC",
        //        "status": "NEW",
        //        "accountId": "1783404067076253952",  // only in SPOT
        //        "exchangeId": "301",                 // only in SPOT
        //        "symbolName": "ETHUSDT",             // only in SPOT
        //        "cummulativeQuoteQty": "0",          // only in SPOT
        //        "cumulativeQuoteQty": "0",           // only in SPOT
        //        "stopPrice": "0.0",                  // only in SPOT
        //        "icebergQty": "0.0",                 // only in SPOT
        //        "isWorking": true                    // only in SPOT
        //        "leverage": "2",                     // only in CONTRACT
        //        "marginLocked": "9.5",               // only in CONTRACT
        //        "priceType": "INPUT"                 // only in CONTRACT
        //    }
        //
        return <Self as Toobit>::parse_order(self, response.clone(), market.clone()).await;
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["openorders", "open_orders", "orders/open", "orders/active"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "openOrders"), ("private", "GET", "orders/open"), ("private", "GET", "orders/active")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["allorders", "all_orders", "orders/history", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "allOrders"), ("private", "GET", "orders"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_closed_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["closedorders", "closed_orders", "orders/closed", "orders/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "closedOrders"), ("private", "GET", "orders/closed"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["mytrades", "my_trades", "trades/history", "fills", "user/trades", "executions"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "myTrades"), ("private", "GET", "fills"), ("private", "GET", "trades/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn transfer(&mut self, mut code: Value, mut amount: Value, mut from_account: Value, mut to_account: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut accounts_by_type: Value = self.safe_dict(self.get("options".into()), Value::from("accountsByType"), Value::new_object());
        let mut from_id: Value = self.safe_string(accounts_by_type.clone(), from_account.clone(), from_account.clone());
        let mut to_id: Value = self.safe_string(accounts_by_type.clone(), to_account.clone(), to_account.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "asset": currency.get(Value::from("id")),
            "quantity": self.currency_to_precision(code.clone(), amount.clone(), Value::Undefined),
            "fromAccountType": from_id.clone(),
            "toAccountType": to_id.clone()
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostApiV1SubAccountTransfer".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    {
        //     "code": 200, // 200 = success
        //     "msg": "success" // response message
        //    }
        //
        return <Self as Toobit>::parse_transfer(self, response.clone(), currency.clone()).await;
    }

    fn parse_transfer(&self, mut transfer: Value, mut currency: Value) -> Value {
        //
        //    {
        //     "code": 200, // 200 = success
        //     "msg": "success" // response message
        //    }
        //
        return Value::Json(normalize(&Value::Json(json!({
            "info": transfer.clone(),
            "id": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "currency": Value::Undefined,
            "amount": Value::Undefined,
            "fromAccount": Value::Undefined,
            "toAccount": Value::Undefined,
            "status": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_ledger(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = Value::Undefined;
        let mut request: Value = Value::new_object();
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
            request.set("coin".into(), currency.get(Value::from("id")));
        };
        if since.clone().is_nonnullish() {
            request.set("startTime".into(), since.clone());
        };
        (request, params) = shift_2(self.handle_until_option(Value::from("endTime"), request.clone(), params.clone(), Value::Undefined));
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        let mut market_type: Value = Value::Undefined;
        (market_type, params) = shift_2(self.handle_market_type_and_params(Value::from("cancelAllOrders"), Value::Undefined, params.clone(), Value::Undefined));
        let mut response: Value = Value::Undefined;
        if market_type.clone() == Value::from("spot") {
            response = self.dispatch("privateGetApiV1AccountBalanceFlow".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            response = self.dispatch("privateGetApiV1FuturesBalanceFlow".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        // both answers are same format
        //
        // [
        //     {
        //         "id": "539870570957903104",
        //         "accountId": "122216245228131",
        //         "coin": "BTC",
        //         "coinId": "BTC",
        //         "coinName": "BTC",
        //         "flowTypeValue": 51,
        //         "flowType": "USER_ACCOUNT_TRANSFER",
        //         "flowName": "Transfer",
        //         "change": "-12.5",
        //         "total": "379.624059937852365",
        //         "created": "1579093587214"
        //     },
        //
        return self.parse_ledger(response.clone(), currency.clone(), since.clone(), limit.clone(), Value::Undefined).await;
    }

    fn parse_ledger_entry(&self, mut item: Value, mut currency: Value) -> Value {
        let mut currency_id: Value = self.safe_string(item.clone(), Value::from("coinId"), Value::Undefined);
        currency = self.safe_currency(currency_id.clone(), currency.clone());
        let mut timestamp: Value = self.safe_integer(item.clone(), Value::from("created"), Value::Undefined);
        let mut after: Value = self.safe_number(item.clone(), Value::from("total"), Value::Undefined);
        let mut amount_raw: Value = self.safe_string(item.clone(), Value::from("change"), Value::Undefined);
        let mut amount: Value = self.parse_number(Precise::string_abs(amount_raw.clone()), Value::Undefined);
        let mut direction: Value = Value::from("in");
        if amount_raw.starts_with(Value::from("-")) {
            direction = Value::from("out");
        };
        return self.safe_ledger_entry(Value::Json(normalize(&Value::Json(json!({
            "info": item.clone(),
            "id": self.safe_string(item.clone(), Value::from("id"), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "direction": direction.clone(),
            "account": Value::Undefined,
            "referenceId": Value::Undefined,
            "referenceAccount": Value::Undefined,
            "type": <Self as Toobit>::parse_ledger_type(self, self.safe_string(item.clone(), Value::from("flowType"), Value::Undefined)),
            "currency": currency.get(Value::from("code")),
            "amount": amount.clone(),
            "before": Value::Undefined,
            "after": after.clone(),
            "status": Value::Undefined,
            "fee": Value::Undefined
        }))).unwrap()), currency.clone());
    }

    fn parse_ledger_type(&self, mut r#type: Value) -> Value {
        let mut types: Value = Value::Json(normalize(&Value::Json(json!({
            "USER_ACCOUNT_TRANSFER": Value::from("transfer"),
            "AIRDROP": Value::from("rebate")
        }))).unwrap());
        return self.safe_string(types.clone(), r#type.clone(), r#type.clone());
    }

    async fn fetch_trading_fees(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = Value::Undefined;
        let mut market_type: Value = Value::Undefined;
        let mut market: Value = Value::Undefined;
        (market_type, params) = shift_2(self.handle_market_type_and_params(Value::from("fetchTradingFees"), Value::Undefined, params.clone(), Value::Undefined));
        if market_type.clone() == Value::from("spot") {
            return Value::Undefined;
        } else if self.in_array(market_type.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from("swap")).into(), (Value::from("future")).into()]))).is_truthy() {
            let mut symbol: Value = Value::Undefined;
            (symbol, params) = shift_2(self.handle_param_string(params.clone(), Value::from("symbol"), Value::Undefined));
            if symbol.clone().is_nullish() {
                return Value::Undefined;
            };
            market = self.market(symbol.clone());
            let mut request: Value = Value::Json(normalize(&Value::Json(json!({
                "symbol": market.get(Value::from("id"))
            }))).unwrap());
            response = self.dispatch("privateGetApiV1FuturesCommissionRate".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        // {
        //     "openMakerFee": "0.000006", // The trade fee rate for opening pending orders
        //     "openTakerFee": "0.0001", // The trade fee rate for open position taker
        //     "closeMakerFee": "0.0002", // The trade fee rate for closing pending orders
        //     "closeTakerFee": "0.0004" // The trade fee rate for closing a taker order
        // }
        //
        let mut result: Value = Value::new_object();
        let mut entry: Value = response.clone();
        let mut market_id: Value = self.safe_string(entry.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut fee: Value = <Self as Toobit>::parse_trading_fee(self, entry.clone(), market.clone());
        result.set(market.get(Value::from("symbol")), fee.clone());
        return result.clone();
    }

    fn parse_trading_fee(&self, mut data: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(data.clone(), Value::from("symbol"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": data.clone(),
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined),
            "maker": self.safe_number(data.clone(), Value::from("closeMakerFee"), Value::Undefined),
            "taker": self.safe_number(data.clone(), Value::from("closeTakerFee"), Value::Undefined),
            "percentage": Value::Undefined,
            "tierBased": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_deposits(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["deposits", "deposit/history", "deposit_history", "capital/deposit/hisrec"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/deposit/hisrec"), ("private", "GET", "deposits"), ("private", "GET", "deposit/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_withdrawals(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["withdrawals", "withdraw/history", "withdrawal_history", "capital/withdraw/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/withdraw/history"), ("private", "GET", "withdrawals"), ("private", "GET", "withdraw/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_deposits_or_withdrawals_helper(&mut self, mut r#type: Value, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = Value::Undefined;
        let mut request: Value = Value::new_object();
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
            request.set("coin".into(), currency.get(Value::from("id")));
        };
        if since.clone().is_nonnullish() {
            request.set("startTime".into(), since.clone());
        };
        (request, params) = shift_2(self.handle_until_option(Value::from("endTime"), request.clone(), params.clone(), Value::Undefined));
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        let mut response: Value = Value::Undefined;
        if r#type.clone() == Value::from("deposits") {
            response = self.dispatch("privateGetApiV1AccountDepositOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else if r#type.clone() == Value::from("withdrawals") {
            //
            // [
            //     {
            //         "time": 1499865549590,
            //         "id": 100234,
            //         "coinName": "EOS",
            //         "statusCode": "DEPOSIT_CAN_WITHDRAW",
            //         "status": "2", // 2=SUCCESS, 11=REJECT, 12=AUDIT
            //         "address": "deposit2bb",
            //         "txId": "98A3EA560C6B3336D348B6C83F0F95ECE4F1F5919E94BD006E5BF3BF264FACFC",
            //         "txIdUrl": "",
            //         "requiredConfirmTimes": "5",
            //         "confirmTimes": "5",
            //         "quantity": "1.01",
            //         "coin": "EOS",
            //         "fromAddress": "clarkkent",
            //         "fromAddressTag": "19029901"
            //         "addressTag": "19012584",
            //     }
            // ]
            //
            response = self.dispatch("privateGetApiV1AccountWithdrawOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        // [
        //     {
        //         "time":"1536232111669",
        //         "id ":"90161227158286336",
        //         "accountId":"517256161325920",
        //         "coinName":"BHC",
        //         "statusCode":"PROCESSING_STATUS",
        //         "status":3,
        //         "address":"0x815bF1c3cc0f49b8FC66B21A7e48fCb476051209",
        //         "txId ":"",
        //         "txIdUrl ":"",
        //         "requiredConfirmTimes ":0, // Number of confirmation requests
        //         "confirmTimes ":0, // number of confirmations
        //         "quantity":"14", // Withdrawal amount
        //         "coinId ":"BHC",
        //         "addressExt":"address tag",
        //         "arriveQuantity":"14",
        //         "walletHandleTime":"1536232111669",
        //         "feeCoinId ":"BHC",
        //         "feeCoinName ":"BHC",
        //         "fee":"0.1",
        //         "kernelId":"", // Exclusive to BEAM and GRIN
        //         "isInternalTransfer": false // Whether internal transfer
        //     }
        // ]
        //
        return self.parse_transactions(response.clone(), currency.clone(), since.clone(), limit.clone(), params.clone()).await;
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value) -> Value {
        //
        // fetchDeposits & fetchWithdrawals
        //
        //     {
        //         "time": 1499865549590,
        //         "id": 100234,
        //         "coinName": "EOS",
        //         "statusCode": "DEPOSIT_CAN_WITHDRAW",
        //         "status": "2", // 2=SUCCESS, 11=REJECT, 12=AUDIT
        //         "address": "deposit2bb",
        //         "txId": "98A3EA560C6B3336D348B6C83F0F95ECE4F1F5919E94BD006E5BF3BF264FACFC",
        //         "txIdUrl": "",
        //         "requiredConfirmTimes": "5",
        //         "confirmTimes": "5",
        //         "quantity": "1.01",
        //         "coin": "EOS",                     // present in "fetchDeposits"
        //         "coinId ":"BHC",                   // present in "fetchWithdrawals"
        //         "addressTag": "19012584",          // present in "fetchDeposits"
        //         "addressExt":"address tag",        // present in "fetchWithdrawals"
        //         "fromAddress": "clarkkent",        // present in "fetchDeposits"
        //         "fromAddressTag": "19029901"       // present in "fetchDeposits"
        //         "arriveQuantity":"14",             // present in "fetchWithdrawals"
        //         "walletHandleTime":"1536232111669",// present in "fetchWithdrawals"
        //         "feeCoinId ":"BHC",                // present in "fetchWithdrawals"
        //         "feeCoinName ":"BHC",              // present in "fetchWithdrawals"
        //         "fee":"0.1",                       // present in "fetchWithdrawals"
        //         "kernelId":"",                     // present in "fetchWithdrawals"
        //         "isInternalTransfer": false        // present in "fetchWithdrawals"
        //     }
        //
        // withdraw
        //
        //     {
        //         "status": 0,
        //         "success": true,
        //         "needBrokerAudit": false, // Do you need a brokerage review?
        //         "id": "423885103582776064",
        //         "refuseReason":"" // failure rejection reason
        //     }
        //
        let mut timestamp: Value = self.safe_integer(transaction.clone(), Value::from("time"), Value::Undefined);
        let mut currency_id: Value = self.safe_string_2(transaction.clone(), Value::from("coin"), Value::from("coinId"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), currency.clone());
        let mut fee_string: Value = self.safe_string(transaction.clone(), Value::from("fee"), Value::Undefined);
        let mut fee_coin: Value = self.safe_string(transaction.clone(), Value::from("feeCoinName"), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        if fee_string.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": self.parse_number(fee_string.clone(), Value::Undefined),
                "currency": self.safe_currency_code(fee_coin.clone(), Value::Undefined)
            }))).unwrap());
        };
        let mut tag_to: Value = self.safe_string_2(transaction.clone(), Value::from("addressTag"), Value::from("addressExt"), Value::Undefined);
        let mut tag_from: Value = self.safe_string(transaction.clone(), Value::from("fromAddressTag"), Value::Undefined);
        let mut address_to: Value = self.safe_string(transaction.clone(), Value::from("address"), Value::Undefined);
        let mut address_from: Value = self.safe_string(transaction.clone(), Value::from("fromAddress"), Value::Undefined);
        let mut is_withdraw: Value = transaction.contains_key(Value::from("arriveQuantity")).into();
        let mut r#type: Value = if is_withdraw.is_truthy() { Value::from("withdrawal") } else { Value::from("deposit") };
        return Value::Json(normalize(&Value::Json(json!({
            "info": transaction.clone(),
            "id": self.safe_string(transaction.clone(), Value::from("id"), Value::Undefined),
            "txid": self.safe_string(transaction.clone(), Value::from("txId"), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "network": Value::Undefined,
            "address": Value::Undefined,
            "addressTo": address_to.clone(),
            "addressFrom": address_from.clone(),
            "tag": Value::Undefined,
            "tagTo": tag_to.clone(),
            "tagFrom": tag_from.clone(),
            "type": r#type.clone(),
            "amount": self.safe_number(transaction.clone(), Value::from("quantity"), Value::Undefined),
            "currency": code.clone(),
            "status": <Self as Toobit>::parse_transaction_status(self, self.safe_string(transaction.clone(), Value::from("status"), Value::Undefined)),
            "updated": Value::Undefined,
            "fee": fee.clone(),
            "comment": Value::Undefined,
            "internal": Value::Undefined
        }))).unwrap());
    }

    fn parse_transaction_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "2": Value::from("pending"),
            "12": Value::from("pending"),
            "11": Value::from("failed"),
            "3": Value::from("ok")
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    async fn fetch_deposit_address(&mut self, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "coin": currency.get(Value::from("id"))
        }))).unwrap());
        let (mut network_code, mut params_omitted) = shift_2(self.handle_network_code_and_params(extend_2(request.clone(), params.clone())));
        if network_code.clone().is_nullish() {
            return Value::Undefined;
        };
        request.set("chainType".into(), self.network_code_to_id(network_code.clone(), Value::Undefined));
        let mut response: Value = self.dispatch("privateGetApiV1AccountDepositAddress".into(), extend_2(request.clone(), params_omitted.clone()), Value::Undefined).await;
        //
        //     {
        //         "canDeposit":false,//Is it possible to recharge
        //         "address":"0x815bF1c3cc0f49b8FC66B21A7e48fCb476051209",
        //         "addressExt":"address tag",
        //         "minQuantity":"100",//minimum amount
        //         "requiredConfirmTimes ":1,//Arrival confirmation number
        //         "canWithdrawConfirmNum ":12,//Withdrawal confirmation number
        //         "coinType":"ERC20_TOKEN"
        //     }
        //
        return <Self as Toobit>::parse_deposit_address(self, response.clone(), currency.clone()).await;
    }

    fn parse_deposit_address(&self, mut deposit_address: Value, mut currency: Value) -> Value {
        let mut address: Value = self.safe_string(deposit_address.clone(), Value::from("address"), Value::Undefined);
        self.check_address(address.clone());
        return Value::Json(normalize(&Value::Json(json!({
            "info": deposit_address.clone(),
            "currency": self.safe_string(currency.clone(), Value::from("code"), Value::Undefined),
            "network": Value::Undefined,
            "address": address.clone(),
            "tag": self.safe_string(deposit_address.clone(), Value::from("addressExt"), Value::Undefined)
        }))).unwrap());
    }

    async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, mut tag: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.check_address(address.clone());
        let mut network_code: Value = Value::Undefined;
        (network_code, params) = shift_2(self.handle_network_code_and_params(params.clone()));
        if network_code.clone().is_nullish() {
            return Value::Undefined;
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "coin": currency.get(Value::from("id")),
            "address": address.clone(),
            "quantity": self.currency_to_precision(currency.get(Value::from("code")), amount.clone(), Value::Undefined),
            "chainType": network_code.clone(),
            "clientOrderId": self.milliseconds()
        }))).unwrap());
        if tag.clone().is_nonnullish() {
            request.set("addressExt".into(), tag.clone());
        };
        let mut response: Value = self.dispatch("privatePostApiV1AccountWithdraw".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "status": 0,
        //     "success": true,
        //     "needBrokerAudit": false, // Do you need a brokerage review?
        //     "id": "423885103582776064", // Withdrawal successful order id
        //     "refuseReason":"" // failure rejection reason
        // }
        //
        return <Self as Toobit>::parse_transaction(self, response.clone(), currency.clone()).await;
    }

    

    async fn set_leverage(&mut self, mut leverage: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            return Value::Undefined;
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id")),
            "leverage": leverage.clone()
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostApiV1FuturesLeverage".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {"code":200,"symbolId":"BTC-SWAP-USDT","leverage":"19"}
        //
        return response.clone();
    }

    async fn fetch_leverage(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetApiV1FuturesAccountLeverage".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // [
        //     {
        //         "symbol":"BTC-SWAP-USDT", //symbol
        //         "leverage":"20",  // leverage
        //         "marginType":"CROSS" // CROSS;ISOLATED
        //     }
        // ]
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Toobit>::parse_leverage(self, data.clone(), market.clone()).await;
    }

    fn parse_leverage(&self, mut leverage: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(leverage.clone(), Value::from("symbol"), Value::Undefined);
        let mut leverage_value: Value = self.safe_integer(leverage.clone(), Value::from("leverage"), Value::Undefined);
        let mut margin_type: Value = self.safe_string(leverage.clone(), Value::from("marginType"), Value::Undefined);
        let mut margin_mode: Value = if margin_type.clone() == Value::from("crossed") { Value::from("cross") } else { Value::from("isolated") };
        return Value::Json(normalize(&Value::Json(json!({
            "info": leverage.clone(),
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined),
            "marginMode": margin_mode.clone(),
            "longLeverage": leverage_value.clone(),
            "shortLeverage": leverage_value.clone()
        }))).unwrap());
    }

    async fn fetch_positions(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbols.is_nonnullish() { request.set("symbols".into(), symbols.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Toobit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["positions", "position", "positionrisk", "position_risk"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "positions"), ("private", "GET", "positionRisk"), ("fapi", "GET", "positionRisk")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_position(&self, mut position: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(position.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut side: Value = self.safe_string_lower(position.clone(), Value::from("side"), Value::Undefined);
        let mut quantity: Value = self.safe_string(position.clone(), Value::from("position"), Value::Undefined);
        let mut leverage: Value = self.safe_integer(position.clone(), Value::from("leverage"), Value::Undefined);
        return self.safe_position(Value::Json(normalize(&Value::Json(json!({
            "info": position.clone(),
            "id": self.safe_string(position.clone(), Value::from("id"), Value::Undefined),
            "symbol": market.get(Value::from("symbol")),
            "entryPrice": self.safe_string(position.clone(), Value::from("avgPrice"), Value::Undefined),
            "markPrice": self.safe_string(position.clone(), Value::from("markPrice"), Value::Undefined),
            "lastPrice": self.safe_string(position.clone(), Value::from("lastPrice"), Value::Undefined),
            "notional": self.safe_string(position.clone(), Value::from("positionValue"), Value::Undefined),
            "collateral": Value::Undefined,
            "unrealizedPnl": self.safe_string(position.clone(), Value::from("unrealizedPnL"), Value::Undefined),
            "side": side.clone(),
            "contracts": self.parse_number(quantity.clone(), Value::Undefined),
            "contractSize": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "hedged": Value::Undefined,
            "maintenanceMargin": Value::Undefined,
            "maintenanceMarginPercentage": Value::Undefined,
            "initialMargin": self.safe_string(position.clone(), Value::from("margin"), Value::Undefined),
            "initialMarginPercentage": Value::Undefined,
            "leverage": leverage.clone(),
            "liquidationPrice": Value::Undefined,
            "marginRatio": Value::Undefined,
            "marginMode": Value::Undefined,
            "percentage": Value::Undefined
        }))).unwrap()));
    }

    

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_str() {
                    "commonGetApiv1time" => self.request("api/v1/time".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetApiv1ping" => self.request("api/v1/ping".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetApiv1exchangeinfo" => self.request("api/v1/exchangeInfo".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1depth" => self.request("quote/v1/depth".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1depthmerged" => self.request("quote/v1/depth/merged".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1trades" => self.request("quote/v1/trades".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1klines" => self.request("quote/v1/klines".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1indexklines" => self.request("quote/v1/index/klines".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1markpriceklines" => self.request("quote/v1/markPrice/klines".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1markprice" => self.request("quote/v1/markPrice".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1index" => self.request("quote/v1/index".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1ticker24hr" => self.request("quote/v1/ticker/24hr".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1contractticker24hr" => self.request("quote/v1/contract/ticker/24hr".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1tickerprice" => self.request("quote/v1/ticker/price".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetQuotev1tickerbookticker" => self.request("quote/v1/ticker/bookTicker".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetApiv1futuresfundingrate" => self.request("api/v1/futures/fundingRate".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "commonGetApiv1futureshistoryfundingrate" => self.request("api/v1/futures/historyFundingRate".into(), "common".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1account" => self.request("api/v1/account".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountcheckapikey" => self.request("api/v1/account/checkApiKey".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1spotorder" => self.request("api/v1/spot/order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1spotopenorders" => self.request("api/v1/spot/openOrders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futuresopenorders" => self.request("api/v1/futures/openOrders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1spottradeorders" => self.request("api/v1/spot/tradeOrders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futureshistoryorders" => self.request("api/v1/futures/historyOrders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accounttrades" => self.request("api/v1/account/trades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountbalanceflow" => self.request("api/v1/account/balanceFlow".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountdepositorders" => self.request("api/v1/account/depositOrders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountwithdraworders" => self.request("api/v1/account/withdrawOrders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1accountdepositaddress" => self.request("api/v1/account/deposit/address".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1subaccount" => self.request("api/v1/subAccount".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futuresaccountleverage" => self.request("api/v1/futures/accountLeverage".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futuresorder" => self.request("api/v1/futures/order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futurespositions" => self.request("api/v1/futures/positions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futuresbalance" => self.request("api/v1/futures/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futuresusertrades" => self.request("api/v1/futures/userTrades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futuresbalanceflow" => self.request("api/v1/futures/balanceFlow".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futurescommissionrate" => self.request("api/v1/futures/commissionRate".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetApiv1futurestodaypnl" => self.request("api/v1/futures/todayPnl".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1spotordertest" => self.request("api/v1/spot/orderTest".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1spotorder" => self.request("api/v1/spot/order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1futuresorder" => self.request("api/v1/futures/order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1spotbatchorders" => self.request("api/v1/spot/batchOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1subaccounttransfer" => self.request("api/v1/subAccount/transfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1accountwithdraw" => self.request("api/v1/account/withdraw".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1futuresmargintype" => self.request("api/v1/futures/marginType".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1futuresleverage" => self.request("api/v1/futures/leverage".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1futuresbatchorders" => self.request("api/v1/futures/batchOrders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1futurespositiontradingstop" => self.request("api/v1/futures/position/trading-stop".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1futurespositionmargin" => self.request("api/v1/futures/positionMargin".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1userdatastream" => self.request("api/v1/userDataStream".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostApiv1listenkey" => self.request("api/v1/listenKey".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1spotorder" => self.request("api/v1/spot/order".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1futuresorder" => self.request("api/v1/futures/order".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1spotopenorders" => self.request("api/v1/spot/openOrders".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1futuresbatchorders" => self.request("api/v1/futures/batchOrders".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1spotcancelorderbyids" => self.request("api/v1/spot/cancelOrderByIds".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1futurescancelorderbyids" => self.request("api/v1/futures/cancelOrderByIds".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteApiv1listenkey" => self.request("api/v1/listenKey".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePutApiv1listenkey" => self.request("api/v1/listenKey".into(), "private".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct ToobitImpl(Value);
#[async_trait(?Send)]
impl Exchange for ToobitImpl {
    fn describe(&self) -> Value { Toobit::describe(self) }
    async fn fetch_tickers(&mut self, symbols: Value, params: Value) -> Value { Toobit::fetch_tickers(self, symbols, params).await }
    async fn fetch_order_book(&mut self, symbol: Value, limit: Value, params: Value) -> Value { Toobit::fetch_order_book(self, symbol, limit, params).await }
    async fn fetch_ohlcv(&mut self, symbol: Value, timeframe: Value, since: Value, limit: Value, params: Value) -> Value { Toobit::fetch_ohlcv(self, symbol, timeframe, since, limit, params).await }
    async fn fetch_trades(&mut self, symbol: Value, since: Value, limit: Value, params: Value) -> Value { Toobit::fetch_trades(self, symbol, since, limit, params).await }
    async fn fetch_time(&mut self, params: Value) -> Value { Toobit::fetch_time(self, params).await }
    async fn fetch_status(&mut self, params: Value) -> Value { Toobit::fetch_status(self, params).await }
    async fn fetch_bids_asks(&mut self, symbols: Value, params: Value) -> Value { Toobit::fetch_bids_asks(self, symbols, params).await }
    fn parse_trade(&self, trade: Value, market: Value) -> Value { Toobit::parse_trade(self, trade, market) }
    fn parse_order(&self, order: Value, market: Value) -> Value { Toobit::parse_order(self, order, market) }
    fn parse_ticker(&self, ticker: Value, market: Value) -> Value { Toobit::parse_ticker(self, ticker, market) }
    fn parse_position(&self, position: Value, market: Value) -> Value { Toobit::parse_position(self, position, market) }
    fn parse_balance(&self, response: Value) -> Value { Toobit::parse_balance(self, response) }
    fn parse_funding_rate(&self, contract: Value, market: Value) -> Value { Toobit::parse_funding_rate(self, contract, market) }
    fn parse_market(&self, market: Value) -> Value { Toobit::parse_market(self, market) }
    fn parse_currency(&self, raw_currency: Value) -> Value { Toobit::parse_currency(self, raw_currency) }
    fn parse_transaction(&self, transaction: Value, currency: Value) -> Value { Toobit::parse_transaction(self, transaction, currency) }
    fn parse_transfer(&self, transfer: Value, currency: Value) -> Value { Toobit::parse_transfer(self, transfer, currency) }
    fn parse_deposit_address(&self, deposit_address: Value, currency: Value) -> Value { Toobit::parse_deposit_address(self, deposit_address, currency) }
    fn parse_ledger_entry(&self, item: Value, currency: Value) -> Value { Toobit::parse_ledger_entry(self, item, currency) }
    fn parse_leverage(&self, leverage: Value, market: Value) -> Value { Toobit::parse_leverage(self, leverage, market) }
    fn parse_order_status(&self, status: Value) -> Value { Toobit::parse_order_status(self, status) }
    fn parse_last_price(&self, entry: Value, market: Value) -> Value { Toobit::parse_last_price(self, entry, market) }
}
impl Toobit for ToobitImpl {}
impl ValueTrait for ToobitImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl ToobitImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = ToobitImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Toobit::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


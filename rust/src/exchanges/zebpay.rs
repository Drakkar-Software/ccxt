#![allow(clippy::all)]
#![allow(non_snake_case)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Zebpay : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "zebpay",
            "name": "Zebpay",
            "countries": [
                "IN"
            ],
            "enableRateLimit": true,
            "rateLimit": 50,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": false,
                "swap": true,
                "future": false,
                "addMargin": true,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "borrowMargin": false,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "cancelOrders": false,
                "closePosition": true,
                "createLimitOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createOrder": true,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchLeverage": true,
                "fetchLeverages": true,
                "fetchMarkets": true,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrderTrades": true,
                "fetchPositions": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTrades": true,
                "fetchTradingFee": true,
                "reduceMargin": true,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "setLeverage": true
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/8094e7be-55a7-46f4-a087-0ca31b48ecad",
                "api": {
                    "spot": "https://sapi.zebpay.com",
                    "swap": "https://futuresbe.zebpay.com"
                },
                "www": "https://www.zebpay.com",
                "doc": "https://github.com/zebpay/zebpay-api-references",
                "fees": "https://zebpay.com/in/features/pricing",
                "test": {
                    "spot": "https://www.zebstage.com",
                    "swap": "https://dev-futuresbe.zebstage.com"
                }
            },
            "api": {
                "public": {
                    "spot": {
                        "get": {
                            "v2/system/time": 10,
                            "v2/system/status": 10,
                            "v2/market/orderbook": 10,
                            "v2/market/trades": 10,
                            "v2/market/ticker": 10,
                            "v2/market/allTickers": 10,
                            "v2/ex/exchangeInfo": 10,
                            "v2/ex/currencies": 10,
                            "v2/market/klines": 10,
                            "v2/ex/tradefees": 10
                        }
                    },
                    "swap": {
                        "get": {
                            "v1/system/time": 10,
                            "v1/system/status": 10,
                            "v1/exchange/tradefee": 10,
                            "v1/exchange/tradefees": 10,
                            "v1/market/orderBook": 10,
                            "v1/market/ticker24Hr": 10,
                            "v1/market/markets": 10,
                            "v1/market/aggTrade": 10
                        },
                        "post": {
                            "v1/market/klines": 10
                        }
                    }
                },
                "private": {
                    "spot": {
                        "post": {
                            "v2/ex/orders": 10
                        },
                        "get": {
                            "v2/ex/orders": 10,
                            "v2/account/balance": 10,
                            "v2/ex/tradefee": 10,
                            "v2/ex/order": 10,
                            "v2/ex/order/fills": 10
                        },
                        "delete": {
                            "v2/ex/order": 10,
                            "v2/ex/orders": 10,
                            "v2/ex/orders/cancelAll": 10
                        }
                    },
                    "swap": {
                        "get": {
                            "v1/wallet/balance": 10,
                            "v1/trade/order": 10,
                            "v1/trade/order/open-orders": 10,
                            "v1/trade/userLeverages": 10,
                            "v1/trade/userLeverage": 10,
                            "v1/trade/positions": 10,
                            "v1/trade/history": 10
                        },
                        "post": {
                            "v1/trade/order": 10,
                            "v1/trade/order/addTPSL": 10,
                            "v1/trade/addMargin": 10,
                            "v1/trade/reduceMargin": 10,
                            "v1/trade/position/close": 10,
                            "v1/trade/update/userLeverage": 10
                        },
                        "delete": {
                            "v1/trade/order": 10
                        }
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": 1,
                "5m": 5,
                "15m": 15,
                "30m": 30,
                "1h": 60,
                "2h": 120,
                "4h": 480,
                "12h": 720,
                "1d": 1440,
                "1w": 10080
            },
            "fees": {
                "trading": {},
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "options": {
                "fetchMarkets": {
                    "types": [
                        "spot",
                        "swap"
                    ]
                },
                "defaultType": "spot"
            },
            "features": {
                "default": {
                    "fetchOHLCV": {
                        "limit": 100
                    }
                }
            }
        }"###).unwrap())
    }

    async fn fetch_status(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Zebpay>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["status", "ping", "time", "system/status"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "status"), ("public", "GET", "ping"), ("public", "GET", "time"), ("sapi", "GET", "system/status")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_time(&mut self, mut params: Value) -> Value {
        let candidates = vec![("public", "GET", "time"), ("public", "GET", "server/time"), ("public", "GET", "timestamp")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    

    async fn fetch_currencies(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.dispatch("publicSpotGetV2ExCurrencies".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //             "data": [
        //                 {
        //                     "currency": "BTC",
        //                     "name": "BTC",
        //                     "fullName": "150",
        //                     "precision": "0.2",
        //                     "type": "fiat",
        //                     "isDebitEnabled": false,
        //                     "chains": [
        //                         {
        //                             "chainName": "Bitcoin",
        //                             "withdrawalMinSize": "0.000482",
        //                             "depositMinSize": "0.00000001",
        //                             "withdrawalFee": "0.00040000",
        //                             "isWithdrawEnabled": "true",
        //                             "isDepositEnabled": "true",
        //                             "contractAddress": "0x095418A82BC2439703b69fbE1210824F2247D77c",
        //                             "withdrawPrecision": "8",
        //                             "maxWithdraw": "2.43090487000000",
        //                             "maxDeposit": "100.00000000",
        //                             "needTag": "false",
        //                             "chainId": "bitcoin",
        //                             "AddressRegex": "^tb1[qpzry9x8gf2tvdw0s3jn54khce6mua7l]{39,59}|m[a-zA-Z0-9]{25,34}|n[a-zA-Z0-9]{25,34}|^2[a-zA-Z0-9]{25,34}$"
        //                          }
        //                     ]
        //                 }
        //             ]
        //     }
        //
        let mut rows: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < rows.len() {
            let mut currency: Value = rows.get(i.into());
            let mut currency_id: Value = self.safe_string(currency.clone(), Value::from("currency"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut name: Value = self.safe_string(currency.clone(), Value::from("name"), Value::Undefined);
            let mut precision: Value = self.parse_number(self.parse_precision(self.safe_string(currency.clone(), Value::from("precision"), Value::Undefined)), Value::Undefined);
            let mut chains: Value = self.safe_list(currency.clone(), Value::from("chains"), Value::new_array());
            let mut networks: Value = Value::new_object();
            let mut min_withdraw_fee_string: Value = Value::Undefined;
            let mut min_withdraw_string: Value = Value::Undefined;
            let mut min_deposit_string: Value = Value::Undefined;
            let mut deposit: Value = Value::from(false);
            let mut withdraw: Value = Value::from(false);
            let mut j: usize = 0;
            while j < chains.len() {
                let mut chain: Value = chains.get(j.into());
                let mut network_id: Value = self.safe_string(chain.clone(), Value::from("chainId"), Value::Undefined);
                let mut network_code: Value = self.network_id_to_code(network_id.clone(), Value::Undefined);
                let mut deposit_allowed: Value = Value::from(self.safe_bool(chain.clone(), Value::from("isDepositEnabled"), Value::Undefined) == Value::from(true));
                deposit = if deposit_allowed.is_truthy() { deposit_allowed.clone() } else { deposit.clone() };
                let mut withdraw_allowed: Value = Value::from(self.safe_bool(chain.clone(), Value::from("isWithdrawEnabled"), Value::Undefined) == Value::from(true));
                withdraw = if withdraw_allowed.is_truthy() { withdraw_allowed.clone() } else { withdraw.clone() };
                let mut withdraw_fee_string: Value = self.safe_string(chain.clone(), Value::from("withdrawalFee"), Value::Undefined);
                if withdraw_fee_string.clone().is_nonnullish() {
                    min_withdraw_fee_string = if min_withdraw_fee_string.clone().is_nullish() { withdraw_fee_string.clone() } else { Precise::string_min(withdraw_fee_string.clone(), min_withdraw_fee_string.clone()) };
                };
                let mut min_network_withdraw_string: Value = self.safe_string(chain.clone(), Value::from("withdrawalMinSize"), Value::Undefined);
                if min_network_withdraw_string.clone().is_nonnullish() {
                    min_withdraw_string = if min_withdraw_string.clone().is_nullish() { min_network_withdraw_string.clone() } else { Precise::string_min(min_network_withdraw_string.clone(), min_withdraw_string.clone()) };
                };
                let mut min_network_deposit_string: Value = self.safe_string(chain.clone(), Value::from("depositMinSize"), Value::Undefined);
                if min_network_deposit_string.clone().is_nonnullish() {
                    min_deposit_string = if min_deposit_string.clone().is_nullish() { min_network_deposit_string.clone() } else { Precise::string_min(min_network_deposit_string.clone(), min_deposit_string.clone()) };
                };
                networks.set(network_code.clone(), Value::Json(normalize(&Value::Json(json!({
                    "info": chain,
                    "id": network_id,
                    "network": network_code,
                    "active": Value::from(deposit_allowed.is_truthy() && withdraw_allowed.is_truthy()),
                    "deposit": deposit_allowed,
                    "withdraw": withdraw_allowed,
                    "fee": self.parse_number(withdraw_fee_string.clone(), Value::Undefined),
                    "precision": precision,
                    "limits": Value::Json(normalize(&Value::Json(json!({
                        "withdraw": Value::Json(normalize(&Value::Json(json!({
                            "min": self.parse_number(min_network_withdraw_string.clone(), Value::Undefined),
                            "max": Value::Undefined
                        }))).unwrap()),
                        "deposit": Value::Json(normalize(&Value::Json(json!({
                            "min": self.parse_number(min_network_deposit_string.clone(), Value::Undefined),
                            "max": Value::Undefined
                        }))).unwrap())
                    }))).unwrap())
                }))).unwrap()));
                j += 1;
            };
            result.set(code.clone(), self.safe_currency_structure(Value::Json(normalize(&Value::Json(json!({
                "info": currency,
                "code": code,
                "id": currency_id,
                "name": name,
                "active": Value::from(deposit.is_truthy() && withdraw.is_truthy()),
                "deposit": deposit,
                "withdraw": withdraw,
                "fee": self.parse_number(min_withdraw_fee_string.clone(), Value::Undefined),
                "precision": precision,
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "amount": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "withdraw": Value::Json(normalize(&Value::Json(json!({
                        "min": self.parse_number(min_withdraw_string.clone(), Value::Undefined),
                        "max": Value::Undefined
                    }))).unwrap()),
                    "deposit": Value::Json(normalize(&Value::Json(json!({
                        "min": self.parse_number(min_deposit_string.clone(), Value::Undefined),
                        "max": Value::Undefined
                    }))).unwrap())
                }))).unwrap()),
                "networks": networks
            }))).unwrap())));
            i += 1;
        };
        return result.clone();
    }

    async fn fetch_trading_fee(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut response: Value = Value::Undefined;
        let mut data: Value = Value::Undefined;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        if market.get(Value::from("spot")).is_truthy() {
            response = self.dispatch("privateSpotGetV2ExTradefee".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            //
            // {
            //     "statusDescription": "Success",
            //     "data":
            //       {
            //         "symbol": "BTCINR",
            //         "takerFeeRate": "0.01",
            //         "makerFeeRate": "0.05",
            //         "percentage": true
            //       } ,
            //     "statusCode": 200,
            // }
            data = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        } else {
            response = self.dispatch("publicSwapGetV1ExchangeTradefee".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            //
            // {
            //     "statusDescription": "OK",
            //     "data":
            //     [
            //       {
            //         "symbol": "BTCINR",
            //         "takerFee": "0.01",
            //         "makerFee": "0.05"
            //       }
            //     ] ,
            //     "statusCode": 200,
            //     "customMessage": ["OK"]
            // }
            //
            let mut response_data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
            data = self.safe_dict(response_data.clone(), Value::from(0), Value::Undefined);
        };
        return <Self as Zebpay>::parse_trading_fee(self, data.clone(), market.clone());
    }

    async fn fetch_trading_fees(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut r#type: Value = Value::Undefined;
        (r#type, params) = shift_2(self.handle_market_type_and_params(Value::from("fetchTradingFees"), Value::Undefined, params.clone(), Value::Undefined));
        let mut response: Value = Value::Undefined;
        if r#type.clone() == Value::from("spot") {
            response = self.dispatch("publicSpotGetV2ExTradefees".into(), params.clone(), Value::Undefined).await;
        } else {
            response = self.dispatch("publicSwapGetV1ExchangeTradefees".into(), params.clone(), Value::Undefined).await;
        };
        //
        // {
        //     "statusDescription": "OK",
        //     "data": [
        //         {
        //             "symbol": "BTCINR",
        //             "takerFee": "0.01",
        //             "makerFee": "0.05"
        //         }
        //     ],
        //     "statusCode": 200,
        //     "customMessage": ["OK"]
        // }
        //
        let mut fees: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < fees.len() {
            let mut fee: Value = <Self as Zebpay>::parse_trading_fee(self, fees.get(i.into()), Value::Undefined);
            let mut symbol: Value = fee.get(Value::from("symbol"));
            result.set(symbol.clone(), fee.clone());
            i += 1;
        };
        return result.clone();
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Zebpay>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Zebpay>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Zebpay>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Zebpay>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["klines", "candles", "ohlcv"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        let mut r#type: Value = Value::Undefined;
        (r#type, params) = shift_2(self.handle_market_type_and_params(Value::from("fetchMyTrades"), market.clone(), params.clone(), Value::Undefined));
        let mut response: Value = Value::Undefined;
        if r#type.clone() == Value::from("spot") {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" fetchMyTrades() does not support spot markets"))"###);
        } else {
            response = self.dispatch("privateSwapGetV1TradeHistory".into(), params.clone(), Value::Undefined).await;
        };
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut items: Value = self.safe_list(data.clone(), Value::from("items"), Value::new_array());
        return self.parse_trades(items.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn fetch_order_trades(&mut self, mut id: Value, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut r#type: Value = Value::Undefined;
        (r#type, params) = shift_2(self.handle_market_type_and_params(Value::from("fetchOrderTrades"), Value::Undefined, params.clone(), Value::Undefined));
        if r#type.clone() != Value::from("spot") {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" fetchOrderTrades() does not support ") + r#type.clone() + Value::from(" markets"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "orderId": id
        }))).unwrap());
        let mut response: Value = self.dispatch("privateSpotGetV2ExOrderFills".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //         {
        //             "orderId": "456789",
        //             "symbol": "LINK_USDT",
        //             "origQty": "1.5",
        //             "orderId": "30249408733945856",
        //             "side": "BUY",
        //             "type": "LIMIT",
        //             "matchRole": "MAKER",
        //             "createTime": 1648200366864,
        //             "price": "3.1",
        //             "avgExecutedPrice": "2.3456"
        //             "openQty": "1",
        //             "filledQty": "0",
        //             "fees": "0.00145",
        //         }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut trades: Value = Value::Json(serde_json::Value::Array(vec![data.clone().into()]));
        return self.parse_trades(trades.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
    }

    fn parse_trade(&mut self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchMyTrades
        //
        //     {
        //         "id": "32164924331503616",
        //         "symbol": "LINK_USDT",
        //         "accountType": "SPOT",
        //         "orderId": "32164923987566592",
        //         "side": "SELL",
        //         "type": "MARKET",
        //         "matchRole": "TAKER",
        //         "createTime": 1648635115525,
        //         "price": "11",
        //         "quantity": "0.5",
        //         "amount": "5.5",
        //         "feeCurrency": "USDT",
        //         "feeAmount": "0.007975",
        //         "pageId": "32164924331503616",
        //         "clientOrderId": "myOwnId-321"
        //     }
        //   {
        //     aggregateTradeId: '2659115835',
        //     symbol: 'ETHINR',
        //     price: '292848',
        //     quantity: '0.147',
        //     firstTradeId: '7018766077',
        //     lastTradeId: '7018766081',
        //     tradeTime: '1765381971447',
        //     isBuyerMarketMaker: true
        //   }
        //
        //
        let mut id: Value = self.safe_string_2(trade.clone(), Value::from("id"), Value::from("aggregateTradeId"), Value::Undefined);
        let mut order_id: Value = self.safe_string_2(trade.clone(), Value::from("id"), Value::from("order"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer_2(trade.clone(), Value::from("timestamp"), Value::from("tradeTime"), Value::Undefined);
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::from("_"), Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::from("side"), Value::Undefined);
        let mut price_string: Value = self.safe_string(trade.clone(), Value::from("price"), Value::Undefined);
        let mut amount_string: Value = self.safe_string_2(trade.clone(), Value::from("amount"), Value::from("quantity"), Value::Undefined);
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": id,
            "info": trade,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol,
            "order": order_id,
            "type": self.safe_string_lower(trade.clone(), Value::from("type"), Value::Undefined),
            "side": side,
            "takerOrMaker": Value::Undefined,
            "price": price_string,
            "amount": amount_string,
            "cost": self.safe_string(trade.clone(), Value::from("cost"), Value::Undefined),
            "fee": self.safe_dict(trade.clone(), Value::from("fee"), Value::Undefined)
        }))).unwrap()), market.clone());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut r#type: Value = Value::Undefined;
        (r#type, params) = shift_2(self.handle_market_type_and_params(Value::from("fetchBalance"), Value::Undefined, params.clone(), Value::Undefined));
        let mut is_spot: Value = Value::from(r#type.clone() == Value::from("spot"));
        let mut response: Value = Value::Undefined;
        if is_spot.is_truthy() {
            response = self.dispatch("privateSpotGetV2AccountBalance".into(), params.clone(), Value::Undefined).await;
        } else {
            response = self.dispatch("privateSwapGetV1WalletBalance".into(), params.clone(), Value::Undefined).await;
        };
        //
        //     {
        //         "data": [
        //              {
        //                 "free": 200,
        //                 "used": 100,
        //                 "total": 300,
        //                 "currency": "INR"
        //              },
        //              {
        //                 "free": 0,
        //                 "used": 0,
        //                 "total": 0,
        //                 "currency": "USDT"
        //              }
        //         ]
        //     }
        //
        return <Self as Zebpay>::parse_balance(self, response.clone());
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut upper_case_type: Value = r#type.to_upper_case();
        let mut take_profit_price: Value = self.safe_string(params.clone(), Value::from("takeProfitPrice"), Value::Undefined);
        let mut stop_loss_price: Value = self.safe_string(params.clone(), Value::from("stopLossPrice"), Value::Undefined);
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("marginAsset").into(), Value::from("takeProfitPrice").into(), Value::from("takeProfitPrice").into()])));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id")),
            "side": side.to_upper_case()
        }))).unwrap());
        let mut response: Value = Value::Undefined;
        if market.get(Value::from("spot")).is_truthy() {
            (request, params) = shift_2(<Self as Zebpay>::order_request(self, symbol.clone(), r#type.clone(), amount.clone(), request.clone(), price.clone(), params.clone()));
            response = self.dispatch("privateSpotPostV2ExOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            let mut margin_asset: Value = self.safe_string(params.clone(), Value::from("marginAsset"), Value::from("INR"));
            let mut form_type: Value = self.safe_string_upper(params.clone(), Value::from("formType"), Value::from("ORDER_FORM"));
            request.set("formType".into(), form_type.clone());
            let mut amt_prec: Value = self.amount_to_precision(market.get(Value::from("id")), amount.clone());
            request.set("amount".into(), self.parse_to_numeric(amt_prec.clone(), Value::Undefined));
            request.set("marginAsset".into(), margin_asset.clone());
            let mut has_tp: Value = Value::from(take_profit_price.clone().is_nonnullish());
            let mut has_sl: Value = Value::from(stop_loss_price.clone().is_nonnullish());
            if has_tp.is_truthy() || has_sl.is_truthy() {
                if has_tp.is_truthy() {
                    let mut tp_prec: Value = self.price_to_precision(symbol.clone(), take_profit_price.clone());
                    request.set("takeProfitPrice".into(), self.parse_to_numeric(tp_prec.clone(), Value::Undefined));
                };
                if has_sl.is_truthy() {
                    let mut sl_prec: Value = self.price_to_precision(symbol.clone(), stop_loss_price.clone());
                    request.set("stopLossPrice".into(), self.parse_to_numeric(sl_prec.clone(), Value::Undefined));
                };
                response = self.dispatch("privateSwapPostV1TradeOrderAddTPSL".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            } else {
                request.set("type".into(), upper_case_type.clone());
                if r#type.clone() == Value::from("limit") {
                    if price.clone().is_nullish() {
                        panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" createOrder() requires a price argument for limit orders"))"###);
                    };
                    let mut price_prec: Value = self.price_to_precision(symbol.clone(), price.clone());
                    request.set("price".into(), self.parse_to_numeric(price_prec.clone(), Value::Undefined));
                };
                response = self.dispatch("privateSwapPostV1TradeOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            };
        };
        //
        //    {
        //        "data": {
        //            "clientOrderId": "619717484f1d010001510cde",
        //        },
        //    }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Zebpay>::parse_order(self, data.clone(), market.clone());
    }

    fn order_request(&mut self, mut symbol: Value, mut r#type: Value, mut amount: Value, mut request: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut upper_case_type: Value = r#type.to_upper_case();
        let mut trigger_price: Value = self.safe_string(params.clone(), Value::from("stopLossPrice"), Value::Undefined);
        let mut quote_order_qty: Value = self.safe_string_2(params.clone(), Value::from("quoteOrderQty"), Value::from("cost"), Value::Undefined);
        let mut time_in_force: Value = self.safe_string(params.clone(), Value::from("timeInForce"), Value::from("GTC"));
        let mut client_order_id: Value = self.safe_string(params.clone(), Value::from("clientOrderId"), self.uuid());
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stopLossPrice").into(), Value::from("cost").into(), Value::from("timeInForce").into(), Value::from("clientOrderId").into()])));
        request.set("type".into(), upper_case_type.clone());
        request.set("clientOrderId".into(), client_order_id.clone());
        request.set("timeInForce".into(), time_in_force.clone());
        if upper_case_type.clone() == Value::from("MARKET") {
            if quote_order_qty.clone().is_nullish() {
                panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" spot market orders require cost in params"))"###);
            };
            request.set("quoteOrderAmount".into(), self.cost_to_precision(symbol.clone(), quote_order_qty.clone()));
        } else {
            if trigger_price.clone().is_nonnullish() {
                request.set("stopLossPrice".into(), self.price_to_precision(symbol.clone(), trigger_price.clone()));
            };
            request.set("amount".into(), self.amount_to_precision(symbol.clone(), amount.clone()));
            request.set("price".into(), self.price_to_precision(symbol.clone(), price.clone()));
        };
        return Value::Json(serde_json::Value::Array(vec![request.clone().into(), params.clone().into()]));
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut response: Value = Value::Undefined;
        let mut request: Value = Value::new_object();
        if market.get(Value::from("spot")).is_truthy() {
            request.set("orderId".into(), id.clone());
            response = self.dispatch("privateSpotDeleteV2ExOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            let mut client_order_id: Value = self.safe_string(params.clone(), Value::from("clientOrderId"), Value::Undefined);
            if client_order_id.clone().is_nullish() {
                panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" cancelOrder() requires a clientOrderId parameter for swap orders"))"###);
            };
            request.set("clientOrderId".into(), client_order_id.clone());
            request.set("symbol".into(), market.get(Value::from("id")));
            response = self.dispatch("privateSwapDeleteV1TradeOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        //    {
        //        "data": {
        //            "clientOrderId": "619714b8b6353000014c505a",
        //            "status": "canceled"
        //        },
        //    }
        //
        return <Self as Zebpay>::parse_order(self, self.safe_dict(response.clone(), Value::from("data"), Value::Undefined), Value::Undefined);
    }

    async fn cancel_all_orders(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut r#type: Value = Value::Undefined;
        (r#type, params) = shift_2(self.handle_market_type_and_params(Value::from("cancelAllOrders"), Value::Undefined, params.clone(), Value::Undefined));
        if r#type.clone() != Value::from("spot") {
            panic!(r###"NotSupported::new(self.get("id".into()) + Value::from(" cancelAllOrders() does not support ") + r#type.clone() + Value::from(" markets"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("privateSpotDeleteV2ExOrdersCancelAll".into(), params.clone(), Value::Undefined).await;
        //
        //    {
        //        "data": {
        //            "orderId": "12345",
        //            "symbol": 'BTC-INR
        //        },
        //    }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut parsed_order: Value = <Self as Zebpay>::parse_order(self, data.clone(), Value::Undefined);
        return Value::Json(serde_json::Value::Array(vec![parsed_order.clone().into()]));
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = Value::Undefined;
        let mut orders: Value = Value::new_array();
        if market.get(Value::from("spot")).is_truthy() {
            request.set("currentPage".into(), Value::from(1));
            if limit.clone().is_nonnullish() {
                request.set("pageSize".into(), limit.clone());
            };
            response = self.dispatch("privateSpotGetV2ExOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            let mut response_data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
            orders = self.safe_list(response_data.clone(), Value::from("items"), Value::new_array());
        } else {
            if since.clone().is_nonnullish() {
                request.set("since".into(), since.clone());
            };
            if limit.clone().is_nonnullish() {
                request.set("limit".into(), limit.clone());
            };
            response = self.dispatch("privateSwapGetV1TradeOrderOpenOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            let mut response_data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
            orders = self.safe_list(response_data.clone(), Value::from("data"), Value::new_array());
        };
        //
        //     {
        //         "data": {
        //             "nextTimeStamp": null,
        //             "totalCount": 100,
        //             "data": [
        //                 {
        //                     "clientOrderId": "64507d02921f1c0001ff6892-123-zeb",
        //                     "datetime": "2025-03-14T14:34:34.4567",
        //                     "timestamp": 1741962557553,
        //                     "status": "open",
        //                     "symbol": "BTCINR",
        //                     "type": "market",
        //                     "timeInForce": "GTC",
        //                     "side": "buy",
        //                     "price": 700000,
        //                     "amount": 0.002,
        //                     "filled": null,
        //                     "remaining": 0.002,
        //                     "trades": []
        //                 }
        //             ]
        //         }
        //     }
        //
        return self.parse_orders(orders.clone(), market.clone(), Value::Undefined, limit.clone(), Value::Undefined);
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::new_object();
        let mut response: Value = Value::Undefined;
        if market.get(Value::from("spot")).is_truthy() {
            request.set("orderId".into(), id.clone());
            response = self.dispatch("privateSpotGetV2ExOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            request.set("id".into(), id.clone());
            response = self.dispatch("privateSwapGetV1TradeOrder".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        //     {
        //         "data": {
        //             "nextTimeStamp": null,
        //             "totalCount": 100,
        //             "data": [
        //                 {
        //                     "clientOrderId": "64507d02921f1c0001ff6892-123-zeb",
        //                     "datetime": "2025-03-14T14:34:34.4567",
        //                     "timestamp": 1741962557553,
        //                     "status": "open",
        //                     "symbol": "BTCINR",
        //                     "type": "market",
        //                     "timeInForce": "GTC",
        //                     "side": "buy",
        //                     "price": 700000,
        //                     "amount": 0.002,
        //                     "filled": null,
        //                     "remaining": 0.002,
        //                     "trades": []
        //                 }
        //             ]
        //         }
        //     }
        //
        let mut response_data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::Undefined);
        return <Self as Zebpay>::parse_order(self, response_data.clone(), market.clone());
    }

    fn parse_order(&mut self, mut order: Value, mut market: Value) -> Value {
        //
        //      {
        //          "clientOrderId": "64507d02921f1c0001ff6892-123-zeb",
        //          "datetime": "2025-03-14T14:34:34.4567",
        //          "timestamp": 1741962557553,
        //          "status": "open",
        //          "symbol": "BTCINR",
        //          "type": "market",
        //          "timeInForce": "GTC",
        //          "side": "buy",
        //          "price": 700000,
        //          "amount": 0.002,
        //          "filled": null,
        //          "remaining": 0.002,
        //          "trades": []
        //      }
        //
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut r#type: Value = self.safe_string(order.clone(), Value::from("type"), Value::Undefined);
        let mut timestamp: Value = self.safe_number(order.clone(), Value::from("timestamp"), Value::Undefined);
        let mut datetime: Value = self.iso8601(timestamp.clone());
        let mut price: Value = self.safe_string(order.clone(), Value::from("price"), Value::Undefined);
        let mut side: Value = self.safe_string(order.clone(), Value::from("side"), Value::Undefined);
        let mut amount: Value = self.safe_string(order.clone(), Value::from("amount"), Value::Undefined);
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("clientOrderId"), Value::Undefined);
        let mut time_in_force: Value = self.safe_string(order.clone(), Value::from("timeInForce"), Value::Undefined);
        let mut status: Value = self.safe_string_lower(order.clone(), Value::from("status"), Value::Undefined);
        let mut order_id: Value = self.safe_string(order.clone(), Value::from("orderId"), Value::Undefined);
        let mut parsed_order: Value = self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": order_id,
            "clientOrderId": client_order_id,
            "symbol": symbol,
            "type": r#type,
            "timeInForce": time_in_force,
            "postOnly": Value::Undefined,
            "reduceOnly": Value::Undefined,
            "side": side,
            "amount": amount,
            "price": price,
            "triggerPrice": Value::Undefined,
            "cost": Value::Undefined,
            "filled": Value::Undefined,
            "remaining": Value::Undefined,
            "timestamp": timestamp,
            "datetime": datetime,
            "fee": Value::Undefined,
            "status": status,
            "info": order,
            "lastTradeTimestamp": Value::Undefined,
            "lastUpdateTimestamp": Value::Undefined,
            "average": Value::Undefined,
            "trades": Value::Undefined
        }))).unwrap()), market.clone());
        return parsed_order.clone();
    }

    async fn close_position(&mut self, mut symbol: Value, mut side: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privateSwapPostV1TradePositionClose".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Zebpay>::parse_order(self, data.clone(), market.clone());
    }

    async fn fetch_leverages(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("privateSwapGetV1TradeUserLeverages".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //         "leveragePreferences": [
        //             {
        //                 "symbol": "ETHINR",
        //                 "shortLeverage": 1,
        //                 "longLeverage": 10,
        //                 "marginMode": "isolated"
        //             },
        //         ]
        //     }
        //
        let mut leverage_preferences: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_leverages(leverage_preferences.clone(), symbols.clone(), Value::from("symbol"), Value::Undefined);
    }

    async fn fetch_leverage(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id")).to_upper_case()
        }))).unwrap());
        let mut response: Value = self.dispatch("privateSwapGetV1TradeUserLeverage".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "data": { symbol: "ETHINR", longLeverage: 1, shortLeverage: 1, marginMode: "isolated" }
        //     }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Zebpay>::parse_leverage(self, data.clone(), market.clone());
    }

    async fn set_leverage(&mut self, mut leverage: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" setLeverage() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "leverage": leverage,
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        //
        // { data: { "symbol", "longLeverage": 10, "shortLeverage": 1, "marginMode": "isolated" }
        //
        let mut response: Value = self.dispatch("privateSwapPostV1TradeUpdateUserLeverage".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return response.clone();
    }

    async fn fetch_positions(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::new_object();
        if symbols.clone().is_nonnullish() {
            request.set("symbols".into(), self.market_ids(symbols.clone()));
        };
        let mut response: Value = self.dispatch("privateSwapGetV1TradePositions".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    {
        //        "data": [
        //            {
        //                "id": "31998678-6056-413f-9d0d-fc3678641650",
        //                "symbol": "ETHINR",
        //                "entryPrice": "0.7533",
        //                "datetime": "2022-03-03T22:51:16.566Z",
        //                "contractSize": "230"
        //            }
        //        ],
        //    }
        //
        let mut positions: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = self.parse_positions(positions.clone(), Value::Undefined, Value::Undefined);
        return self.filter_by_array_positions(result.clone(), Value::from("symbol"), symbols.clone(), Value::from(false));
    }

    async fn add_margin(&mut self, mut symbol: Value, mut amount: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id")),
            "amount": amount
        }))).unwrap());
        let mut response: Value = self.dispatch("privateSwapPostV1TradeAddMargin".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    {
        //        "code": "200000",
        //        "data": {
        //            "symbol": "BTCINR",
        //            "type": "add",
        //            "amount": 1000,
        //            "code": "INR",
        //            "status": "ok"
        //        }
        //    }
        //
        //
        //    {
        //        "code":"200000",
        //        "msg":"Position does not exist"
        //    }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return extend_2(<Self as Zebpay>::parse_margin_modification(self, data.clone(), market.clone()), Value::Json(normalize(&Value::Json(json!({
            "amount": amount,
            "direction": "in"
        }))).unwrap()));
    }

    async fn reduce_margin(&mut self, mut symbol: Value, mut amount: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id")),
            "amount": amount
        }))).unwrap());
        let mut response: Value = self.dispatch("privateSwapPostV1TradeReduceMargin".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    {
        //        "code": "200000",
        //        "data": {
        //            "symbol": "BTCINR",
        //            "type": "reduce",
        //            "amount": 1000,
        //            "code": "INR",
        //            "status": "ok"
        //        }
        //    }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return extend_2(<Self as Zebpay>::parse_margin_modification(self, data.clone(), market.clone()), Value::Json(normalize(&Value::Json(json!({
            "amount": amount,
            "direction": "out"
        }))).unwrap()));
    }

    async fn fetch_spot_markets(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.dispatch("publicSpotGetV2ExExchangeInfo".into(), params.clone(), Value::Undefined).await;
        //
        //    {
        //        "data": {
        //            "symbol": "ETH-INR",
        //            "name": "ETH-INR",
        //            "baseCurrency": "ETH",
        //            "quoteCurrency": "INR",
        //            "feeCurrency": "INR",
        //            "baseMinSize": "",
        //            "quoteMinSize": "100",
        //            "baseMaxSize": "",
        //            "quoteMaxSize": "2000",
        //            "baseIncrement": "0.00001"
        //            "quoteIncrement": "0.00001",
        //            "enableTrading": true
        //        }
        //    }
        //
        let mut result: Value = Value::new_array();
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut markets: Value = self.safe_list(data.clone(), Value::from("symbols"), Value::new_array());
        let mut i: usize = 0;
        while i < markets.len() {
            let mut market: Value = markets.get(i.into());
            let mut id: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
            let mut base_id: Value = self.safe_string(market.clone(), Value::from("baseAsset"), Value::Undefined);
            let mut quote_id: Value = self.safe_string(market.clone(), Value::from("quoteAsset"), Value::Undefined);
            let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
            let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
            let mut symbol: Value = base.clone() + Value::from("/") + quote.clone();
            result.push(Value::Json(normalize(&Value::Json(json!({
                "id": id,
                "symbol": symbol,
                "base": base,
                "quote": quote,
                "baseId": base_id,
                "quoteId": quote_id,
                "type": "spot",
                "spot": true,
                "swap": false,
                "margin": false,
                "future": false,
                "option": false,
                "active": Value::Undefined,
                "contract": Value::Undefined,
                "taker": self.safe_number(market.clone(), Value::from("takerFee"), Value::Undefined),
                "maker": self.safe_number(market.clone(), Value::from("makerFee"), Value::Undefined),
                "strike": Value::Undefined,
                "optionType": Value::Undefined,
                "precision": Value::Json(normalize(&Value::Json(json!({
                    "amount": self.safe_number(market.clone(), Value::from("lotSz"), Value::Undefined),
                    "price": self.safe_number(market.clone(), Value::from("tickSz"), Value::Undefined)
                }))).unwrap()),
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "amount": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "price": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "cost": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap())
                }))).unwrap()),
                "info": market
            }))).unwrap()));
            i += 1;
        };
        return result.clone();
    }

    async fn fetch_swap_markets(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.dispatch("publicSwapGetV1MarketMarkets".into(), params.clone(), Value::Undefined).await;
        //
        //    {
        //        "data": {
        //            "symbol": "ETHUSDT",
        //            "status": "TRADING",
        //            "mainMarginPercent": "10",
        //            "baseAsset": "ETH",
        //            "quoteAsset": "USDT",
        //            "pricePrecision": 1,
        //            "quantityPrecision": 0.05,
        //            "baseAssetPrecision": 0,
        //            "quotePrecision": 0,
        //            "orderType": ["LIMIT", "MARKET" ]
        //            "timeInForce": ["GTC"],
        //            "makerFee": "0.01",
        //            "takerFee": "0.01",
        //            "minLeverage": "1",
        //            "maxLeverage": "20"
        //            "tickSz": "0.1",
        //            "lotSz": "0.1"
        //        }
        //    }
        //
        let mut result: Value = Value::new_array();
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut markets: Value = self.safe_list(data.clone(), Value::from("symbols"), Value::new_array());
        let mut i: usize = 0;
        while i < markets.len() {
            let mut market: Value = markets.get(i.into());
            let mut id: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
            let mut base_id: Value = self.safe_string(market.clone(), Value::from("baseAsset"), Value::Undefined);
            let mut quote_id: Value = self.safe_string(market.clone(), Value::from("quoteAsset"), Value::Undefined);
            let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
            let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
            let mut settle: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
            let mut status: Value = self.safe_string(market.clone(), Value::from("status"), Value::Undefined);
            let mut symbol: Value = base.clone() + Value::from("/") + quote.clone();
            result.push(self.safe_market_structure(Value::Json(normalize(&Value::Json(json!({
                "id": id,
                "symbol": symbol.clone() + Value::from(":") + settle.clone(),
                "base": base,
                "quote": quote,
                "baseId": base_id,
                "quoteId": quote_id,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": false,
                "type": "swap",
                "option": false,
                "active": Value::from(status.clone() == Value::from("Open")),
                "contract": true,
                "taker": self.safe_number(market.clone(), Value::from("takerFee"), Value::Undefined),
                "maker": self.safe_number(market.clone(), Value::from("makerFee"), Value::Undefined),
                "strike": Value::Undefined,
                "optionType": Value::Undefined,
                "precision": Value::Json(normalize(&Value::Json(json!({
                    "amount": self.safe_number(market.clone(), Value::from("lotSz"), Value::Undefined),
                    "price": self.safe_number(market.clone(), Value::from("tickSz"), Value::Undefined)
                }))).unwrap()),
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "leverage": Value::Json(normalize(&Value::Json(json!({
                        "min": self.safe_number(market.clone(), Value::from("minLeverage"), Value::Undefined),
                        "max": self.safe_number(market.clone(), Value::from("maxLeverage"), Value::Undefined)
                    }))).unwrap())
                }))).unwrap()),
                "info": market
            }))).unwrap())));
            i += 1;
        };
        return result.clone();
    }

    fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined
        }))).unwrap());
        let mut currency_list: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut i: usize = 0;
        while i < currency_list.len() {
            let mut entry: Value = currency_list.get(i.into());
            let mut account: Value = self.account();
            account.set("total".into(), self.safe_string(entry.clone(), Value::from("total"), Value::Undefined));
            account.set("free".into(), self.safe_string(entry.clone(), Value::from("free"), Value::Undefined));
            account.set("used".into(), self.safe_string(entry.clone(), Value::from("used"), Value::Undefined));
            let mut currency_id: Value = self.safe_string(entry.clone(), Value::from("currency"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            result.set(code.clone(), account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    fn parse_position(&self, mut position: Value, mut market: Value) -> Value {
        //
        // isolated
        //    {
        //        "id":"long",
        //        "symbol":"pf_ftmusd",
        //        "entryPrice":"0.4921",
        //        "datetime":"2023-02-22T11:37:16.685Z",
        //        "contractSize":"1",
        //        "leverage":"1.0"
        //    }
        //
        let mut leverage: Value = self.safe_number(position.clone(), Value::from("leverage"), Value::Undefined);
        let mut datetime: Value = self.safe_string(position.clone(), Value::from("datetime"), Value::Undefined);
        let mut market_id: Value = self.safe_string(position.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": position,
            "symbol": market_id,
            "timestamp": self.parse8601(datetime.clone()),
            "datetime": datetime,
            "initialMargin": self.safe_number(position.clone(), Value::from("initialMargin"), Value::Undefined),
            "initialMarginPercentage": Value::Undefined,
            "maintenanceMargin": Value::Undefined,
            "maintenanceMarginPercentage": Value::Undefined,
            "entryPrice": self.safe_number(position.clone(), Value::from("entryPrice"), Value::Undefined),
            "notional": self.safe_number(position.clone(), Value::from("notional"), Value::Undefined),
            "leverage": leverage,
            "unrealizedPnl": Value::Undefined,
            "contracts": self.safe_number(position.clone(), Value::from("contracts"), Value::Undefined),
            "contractSize": self.safe_number(market.clone(), Value::from("contractSize"), Value::Undefined),
            "marginRatio": Value::Undefined,
            "liquidationPrice": self.safe_number(position.clone(), Value::from("liquidationPrice"), Value::Undefined),
            "markPrice": Value::Undefined,
            "collateral": Value::Undefined,
            "marginType": "isolated",
            "side": self.safe_string(position.clone(), Value::from("side"), Value::Undefined),
            "percentage": Value::Undefined
        }))).unwrap());
    }

    fn parse_leverage(&self, mut leverage: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(leverage.clone(), Value::from("symbol"), Value::Undefined);
        let mut info: Value = self.safe_dict(leverage.clone(), Value::from("info"), Value::Undefined);
        let mut leverage_value: Value = self.safe_integer(leverage.clone(), Value::from("longLeverage"), Value::Undefined);
        let mut leverage_value_short: Value = self.safe_integer(leverage.clone(), Value::from("shortLeverage"), Value::Undefined);
        let mut margin_mode: Value = self.safe_string(leverage.clone(), Value::from("marginMode"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": info,
            "symbol": market_id,
            "marginMode": margin_mode,
            "longLeverage": leverage_value,
            "shortLeverage": leverage_value_short
        }))).unwrap());
    }

    fn parse_trading_fee(&self, mut fee: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(fee.clone(), Value::from("symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": fee,
            "symbol": symbol,
            "maker": self.safe_number_2(fee.clone(), Value::from("makerFeeRate"), Value::from("makerFee"), Value::Undefined),
            "taker": self.safe_number_2(fee.clone(), Value::from("takerFeeRate"), Value::from("takerFee"), Value::Undefined),
            "percentage": Value::Undefined,
            "tierBased": Value::Undefined
        }))).unwrap());
    }

    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     [
        //        {
        //            "symbol": "BTC-INR",
        //            "bestBid": "4900000",
        //            "bestBidQty": "0.00014938",
        //            "bestAsk": "",
        //            "bestAskQty": "0",
        //            "priceChange": "-98134.56",
        //            "priceChangePercent": "-1.84",
        //            "high": "5433400",
        //            "low": "5333400",
        //            "vol": "0.0002",
        //            "volValue": "1066.68",
        //            "last": "5333400"
        //        }
        //     ]
        //
        let mut timestamp: Value = self.safe_integer_2(ticker.clone(), Value::from("timestamp"), Value::from("ts"), Value::Undefined);
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
        let mut close: Value = self.safe_string(ticker.clone(), Value::from("close"), Value::Undefined);
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("last"), Value::Undefined);
        let mut percentage: Value = self.safe_string(ticker.clone(), Value::from("percentage"), Value::Undefined);
        let mut bid_volume: Value = self.safe_string(ticker.clone(), Value::from("bidVolume"), Value::Undefined);
        let mut ask_volume: Value = self.safe_string(ticker.clone(), Value::from("askVolume"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "id": market_id,
            "symbol": market.get(Value::from("symbol")),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "high": self.safe_string(ticker.clone(), Value::from("high"), Value::Undefined),
            "low": self.safe_string(ticker.clone(), Value::from("low"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("bid"), Value::Undefined),
            "bidVolume": bid_volume,
            "ask": self.safe_string(ticker.clone(), Value::from("ask"), Value::Undefined),
            "askVolume": ask_volume,
            "vwap": Value::Undefined,
            "open": Value::Undefined,
            "close": close,
            "last": last,
            "previousClose": self.safe_string(ticker.clone(), Value::from("previousClose"), Value::Undefined),
            "change": self.safe_string(ticker.clone(), Value::from("change"), Value::Undefined),
            "percentage": percentage,
            "average": self.safe_string(ticker.clone(), Value::from("average"), Value::Undefined),
            "baseVolume": self.safe_string(ticker.clone(), Value::from("baseVolume"), Value::Undefined),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("quoteVolume"), Value::Undefined),
            "markPrice": Value::Undefined,
            "info": ticker
        }))).unwrap()), market.clone());
    }

    fn parse_margin_modification(&self, mut info: Value, mut market: Value) -> Value {
        //
        //    {
        //         "symbol": "BTCINR",
        //         "type": "reduce",
        //         "amount": 1000,
        //         "code": "INR",
        //         "status": "ok"
        //    }
        //
        let mut timestamp: Value = self.milliseconds();
        return Value::Json(normalize(&Value::Json(json!({
            "info": info,
            "symbol": market.get(Value::from("id")),
            "type": Value::Undefined,
            "marginMode": Value::Undefined,
            "amount": self.safe_number(info.clone(), Value::from("amount"), Value::Undefined),
            "total": Value::Undefined,
            "code": self.safe_string(info.clone(), Value::from("code"), Value::Undefined),
            "status": self.safe_string(info.clone(), Value::from("status"), Value::Undefined),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone())
        }))).unwrap());
    }

    

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicSpotGetV2systemtime" => self.request("v2/system/time".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2systemstatus" => self.request("v2/system/status".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2marketorderbook" => self.request("v2/market/orderbook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2markettrades" => self.request("v2/market/trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2marketticker" => self.request("v2/market/ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2marketalltickers" => self.request("v2/market/allTickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2exexchangeinfo" => self.request("v2/ex/exchangeInfo".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2excurrencies" => self.request("v2/ex/currencies".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2marketklines" => self.request("v2/market/klines".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSpotGetV2extradefees" => self.request("v2/ex/tradefees".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1systemtime" => self.request("v1/system/time".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1systemstatus" => self.request("v1/system/status".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1exchangetradefee" => self.request("v1/exchange/tradefee".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1exchangetradefees" => self.request("v1/exchange/tradefees".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1marketorderbook" => self.request("v1/market/orderBook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1marketticker24hr" => self.request("v1/market/ticker24Hr".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1marketmarkets" => self.request("v1/market/markets".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapGetV1marketaggtrade" => self.request("v1/market/aggTrade".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicSwapPostV1marketklines" => self.request("v1/market/klines".into(), "public".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotPostV2exorders" => self.request("v2/ex/orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotGetV2exorders" => self.request("v2/ex/orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotGetV2accountbalance" => self.request("v2/account/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotGetV2extradefee" => self.request("v2/ex/tradefee".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotGetV2exorder" => self.request("v2/ex/order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotGetV2exorderfills" => self.request("v2/ex/order/fills".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotDeleteV2exorder" => self.request("v2/ex/order".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotDeleteV2exorders" => self.request("v2/ex/orders".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSpotDeleteV2exorderscancelall" => self.request("v2/ex/orders/cancelAll".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapGetV1walletbalance" => self.request("v1/wallet/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapGetV1tradeorder" => self.request("v1/trade/order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapGetV1tradeorderopenorders" => self.request("v1/trade/order/open-orders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapGetV1tradeuserleverages" => self.request("v1/trade/userLeverages".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapGetV1tradeuserleverage" => self.request("v1/trade/userLeverage".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapGetV1tradepositions" => self.request("v1/trade/positions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapGetV1tradehistory" => self.request("v1/trade/history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapPostV1tradeorder" => self.request("v1/trade/order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapPostV1tradeorderaddtpsl" => self.request("v1/trade/order/addTPSL".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapPostV1tradeaddmargin" => self.request("v1/trade/addMargin".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapPostV1tradereducemargin" => self.request("v1/trade/reduceMargin".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapPostV1tradepositionclose" => self.request("v1/trade/position/close".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapPostV1tradeupdateuserleverage" => self.request("v1/trade/update/userLeverage".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateSwapDeleteV1tradeorder" => self.request("v1/trade/order".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct ZebpayImpl(Value);
impl Exchange for ZebpayImpl {}
impl Zebpay for ZebpayImpl {}
impl ValueTrait for ZebpayImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl ZebpayImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = ZebpayImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Zebpay::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


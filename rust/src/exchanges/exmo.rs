#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
use crate::ws::{ArrayCache, ArrayCacheByTimestamp, ArrayCacheBySymbolById, ArrayCacheBySymbolBySide};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors (stub structs with ::new for transpiled `new ErrorType(msg)` patterns)
macro_rules! error_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(msg: Value) -> Value { msg } } }; }
error_stub!(BadRequest);
error_stub!(InvalidOrder);
error_stub!(ExchangeError);
error_stub!(InsufficientFunds);
error_stub!(OrderNotFound);
error_stub!(AuthenticationError);
error_stub!(PermissionDenied);
error_stub!(ExchangeNotAvailable);
error_stub!(ArgumentsRequired);
error_stub!(RateLimitExceeded);
error_stub!(OrderNotFillable);
error_stub!(OrderImmediatelyFillable);
error_stub!(NotSupported);
error_stub!(DuplicateOrderId);
error_stub!(UnsubscribeError);
error_stub!(ChecksumError);
error_stub!(InvalidNonce);
error_stub!(BadSymbol);
// Array-like type stubs (Bids/Asks order book sides)
macro_rules! array_side_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(data: Value, _limit: Value) -> Value { data } } }; }
array_side_stub!(Bids);
array_side_stub!(Asks);

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait(?Send)]
pub trait Exmo : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "exmo",
            "name": "EXMO",
            "countries": [
                "LT"
            ],
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": true,
                "swap": false,
                "future": false,
                "option": false,
                "addMargin": true,
                "cancelOrder": true,
                "cancelOrders": false,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrder": true,
                "createMarketBuyOrderWithCost": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": true,
                "createOrder": true,
                "createStopLimitOrder": true,
                "createStopMarketOrder": true,
                "createStopOrder": true,
                "editOrder": true,
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchCanceledOrders": true,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDeposit": true,
                "fetchDepositAddress": true,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": true,
                "fetchDepositWithdrawFee": "emulated",
                "fetchDepositWithdrawFees": true,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingRates": false,
                "fetchIndexOHLCV": false,
                "fetchL2OrderBook": true,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOrder": "emulated",
                "fetchOrderBook": true,
                "fetchOrderBooks": true,
                "fetchOrderTrades": true,
                "fetchPosition": false,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": true,
                "fetchTransactionFees": true,
                "fetchTransactions": "emulated",
                "fetchTransfer": false,
                "fetchTransfers": false,
                "fetchWithdrawal": true,
                "fetchWithdrawals": true,
                "reduceMargin": true,
                "setMargin": false,
                "transfer": false,
                "withdraw": true
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/1294454/27766491-1b0ea956-5eda-11e7-9225-40d67b481b8d.jpg",
                "api": {
                    "public": "https://api.exmo.com",
                    "private": "https://api.exmo.com",
                    "web": "https://exmo.me"
                },
                "www": "https://exmo.me",
                "doc": [
                    "https://exmo.me/en/api_doc?ref=131685"
                ],
                "fees": "https://exmo.com/en/docs/fees",
                "referral": "https://exmo.me/?ref=131685"
            },
            "api": {
                "web": {
                    "get": [
                        "ctrl/feesAndLimits",
                        "en/docs/fees"
                    ]
                },
                "public": {
                    "get": [
                        "currency",
                        "currency/list/extended",
                        "order_book",
                        "pair_settings",
                        "ticker",
                        "trades",
                        "candles_history",
                        "required_amount",
                        "payments/providers/crypto/list"
                    ]
                },
                "private": {
                    "post": [
                        "user_info",
                        "order_create",
                        "order_cancel",
                        "stop_market_order_create",
                        "stop_market_order_cancel",
                        "user_open_orders",
                        "user_trades",
                        "user_cancelled_orders",
                        "order_trades",
                        "deposit_address",
                        "withdraw_crypt",
                        "withdraw_get_txid",
                        "excode_create",
                        "excode_load",
                        "code_check",
                        "wallet_history",
                        "wallet_operations",
                        "margin/user/order/create",
                        "margin/user/order/update",
                        "margin/user/order/cancel",
                        "margin/user/position/close",
                        "margin/user/position/margin_add",
                        "margin/user/position/margin_remove",
                        "margin/currency/list",
                        "margin/pair/list",
                        "margin/settings",
                        "margin/funding/list",
                        "margin/user/info",
                        "margin/user/order/list",
                        "margin/user/order/history",
                        "margin/user/order/trades",
                        "margin/user/order/max_quantity",
                        "margin/user/position/list",
                        "margin/user/position/margin_remove_info",
                        "margin/user/position/margin_add_info",
                        "margin/user/wallet/list",
                        "margin/user/wallet/history",
                        "margin/user/trade/list",
                        "margin/trades",
                        "margin/liquidation/feed"
                    ]
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1",
                "5m": "5",
                "15m": "15",
                "30m": "30",
                "45m": "45",
                "1h": "60",
                "2h": "120",
                "3h": "180",
                "4h": "240",
                "1d": "D",
                "1w": "W",
                "1M": "M"
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.004,
                    "maker": 0.004,
                    "feeSide": "get"
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                },
                "transaction": {
                    "tierBased": false,
                    "percentage": false
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV",
                "GMT": "GMT Token"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1.1",
            "options": {
                "networks": {
                    "ETH": "ERC20",
                    "TRX": "TRC20"
                },
                "fetchTradingFees": {
                    "method": "fetchPrivateTradingFees"
                },
                "margin": {
                    "fillResponseFromRequest": true
                }
            },
            "features": {
                "spot": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": true,
                        "triggerPrice": true,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": true
                        },
                        "hedged": false,
                        "selfTradePrevention": false,
                        "trailing": false,
                        "leverage": true,
                        "marketBuyByCost": true,
                        "marketBuyRequiresPrice": false,
                        "iceberg": false
                    },
                    "fetchMyTrades": {
                        "marginMode": true,
                        "limit": 100,
                        "symbolRequired": true
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "swap": {},
                "future": {}
            }
        }"###).unwrap())
    }

    async fn modify_margin_helper(&mut self, mut symbol: Value, mut amount: Value, mut r#type: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "position_id": market.get(Value::from("id")),
            "quantity": amount.clone()
        }))).unwrap());
        let mut response: Value = Value::Undefined;
        if r#type.clone() == Value::from("add") {
            response = self.dispatch("privatePostMarginUserPositionMarginAdd".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else if r#type.clone() == Value::from("reduce") {
            response = self.dispatch("privatePostMarginUserPositionMarginRemove".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        //      {}
        //
        let mut margin: Value = <Self as Exmo>::parse_margin_modification(self, response.clone(), market.clone());
        let mut options: Value = self.safe_value(self.get("options".into()), Value::from("margin"), Value::new_object());
        let mut fill_response_from_request: Value = self.safe_bool(options.clone(), Value::from("fillResponseFromRequest"), Value::from(true));
        if fill_response_from_request.is_truthy() {
            margin.set("type".into(), r#type.clone());
            margin.set("amount".into(), amount.clone());
        };
        return margin.clone();
    }

    fn parse_margin_modification(&self, mut data: Value, mut market: Value) -> Value {
        //
        //      {}
        //
        return Value::Json(normalize(&Value::Json(json!({
            "info": data.clone(),
            "symbol": self.safe_symbol(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined),
            "type": Value::Undefined,
            "marginMode": Value::from("isolated"),
            "amount": Value::Undefined,
            "total": Value::Undefined,
            "code": self.safe_value(market.clone(), Value::from("quote"), Value::Undefined),
            "status": Value::from("ok"),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined
        }))).unwrap());
    }

    async fn reduce_margin(&mut self, mut symbol: Value, mut amount: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Exmo>::modify_margin_helper(self, symbol.clone(), amount.clone(), Value::from("reduce"), params.clone()).await;
    }

    async fn add_margin(&mut self, mut symbol: Value, mut amount: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Exmo>::modify_margin_helper(self, symbol.clone(), amount.clone(), Value::from("add"), params.clone()).await;
    }

    async fn fetch_trading_fees(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut options: Value = self.safe_value(self.get("options".into()), Value::from("fetchTradingFees"), Value::new_object());
        let mut default_method: Value = self.safe_string(options.clone(), Value::from("method"), Value::from("fetchPrivateTradingFees"));
        let mut method: Value = self.safe_string(params.clone(), Value::from("method"), default_method.clone());
        params = self.omit(params.clone(), Value::from("method"));
        if method.clone() == Value::from("fetchPrivateTradingFees") {
            return <Self as Exmo>::fetch_private_trading_fees(self, params.clone()).await;
        } else {
            return <Self as Exmo>::fetch_public_trading_fees(self, params.clone()).await;
        };
        Value::Undefined
    }

    async fn fetch_private_trading_fees(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("privatePostMarginPairList".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //         "pairs": [{
        //             "name": "EXM_USD",
        //             "buy_price": "0.02728391",
        //             "sell_price": "0.0276",
        //             "last_trade_price": "0.0276",
        //             "ticker_updated": "1646956050056696046",
        //             "is_fair_price": true,
        //             "max_price_precision": "8",
        //             "min_order_quantity": "1",
        //             "max_order_quantity": "50000",
        //             "min_order_price": "0.00000001",
        //             "max_order_price": "1000",
        //             "max_position_quantity": "50000",
        //             "trade_taker_fee": "0.05",
        //             "trade_maker_fee": "0",
        //             "liquidation_fee": "0.5",
        //             "max_leverage": "3",
        //             "default_leverage": "3",
        //             "liquidation_level": "5",
        //             "margin_call_level": "7.5",
        //             "position": "1",
        //             "updated": "1638976144797807397"
        //         }
        //         ...
        //         ]
        //     }
        //
        let mut pairs: Value = self.safe_value(response.clone(), Value::from("pairs"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < pairs.len() {
            let mut pair: Value = pairs.get(i.into());
            let mut market_id: Value = self.safe_string(pair.clone(), Value::from("name"), Value::Undefined);
            let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::from("_"), Value::Undefined);
            let mut maker_string: Value = self.safe_string(pair.clone(), Value::from("trade_maker_fee"), Value::Undefined);
            let mut taker_string: Value = self.safe_string(pair.clone(), Value::from("trade_taker_fee"), Value::Undefined);
            let mut maker: Value = self.parse_number(Precise::string_div(maker_string.clone(), Value::from("100"), Value::Undefined), Value::Undefined);
            let mut taker: Value = self.parse_number(Precise::string_div(taker_string.clone(), Value::from("100"), Value::Undefined), Value::Undefined);
            result.set(symbol.clone(), Value::Json(normalize(&Value::Json(json!({
                "info": pair.clone(),
                "symbol": symbol.clone(),
                "maker": maker.clone(),
                "taker": taker.clone(),
                "percentage": Value::from(true),
                "tierBased": Value::from(true)
            }))).unwrap()));
            i += 1;
        };
        return result.clone();
    }

    async fn fetch_public_trading_fees(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("publicGetPairSettings".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //         "BTC_USD": {
        //             "min_quantity": "0.00002",
        //             "max_quantity": "1000",
        //             "min_price": "1",
        //             "max_price": "150000",
        //             "max_amount": "500000",
        //             "min_amount": "1",
        //             "price_precision": "2",
        //             "commission_taker_percent": "0.3",
        //             "commission_maker_percent": "0.3"
        //         },
        //     }
        //
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < self.get("symbols".into()).len() {
            let mut symbol: Value = self.get("symbols".into()).get(i.into());
            let mut market: Value = self.market(symbol.clone());
            let mut fee: Value = self.safe_value(response.clone(), market.get(Value::from("id")), Value::new_object());
            let mut maker_string: Value = self.safe_string(fee.clone(), Value::from("commission_maker_percent"), Value::Undefined);
            let mut taker_string: Value = self.safe_string(fee.clone(), Value::from("commission_taker_percent"), Value::Undefined);
            let mut maker: Value = self.parse_number(Precise::string_div(maker_string.clone(), Value::from("100"), Value::Undefined), Value::Undefined);
            let mut taker: Value = self.parse_number(Precise::string_div(taker_string.clone(), Value::from("100"), Value::Undefined), Value::Undefined);
            result.set(symbol.clone(), Value::Json(normalize(&Value::Json(json!({
                "info": fee.clone(),
                "symbol": symbol.clone(),
                "maker": maker.clone(),
                "taker": taker.clone(),
                "percentage": Value::from(true),
                "tierBased": Value::from(true)
            }))).unwrap()));
            i += 1;
        };
        return result.clone();
    }

    fn parse_fixed_float_value(&self, mut input: Value) -> Value {
        if input.clone().is_nullish() || input.clone() == Value::from("-") {
            return Value::Undefined;
        };
        if input.clone() == Value::from("") {
            return Value::from(0);
        };
        let mut is_percentage: Value = Value::from(input.index_of(Value::from("%")) >= Value::from(0));
        let mut parts: Value = input.split(Value::from(" "));
        let mut value: Value = parts.get(Value::from(0)).replace(Value::from("%"), Value::from(""));
        let mut result: Value = parse_float(value.clone());
        if result.clone() > Value::from(0) && is_percentage.is_truthy() {
            return Value::Undefined;
        };
        return result.clone();
    }

    async fn fetch_transaction_fees(&mut self, mut codes: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut crypto_list: Value = self.dispatch("publicGetPaymentsProvidersCryptoList".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //         "BTC":[
        //             { "type":"deposit", "name":"BTC", "currency_name":"BTC", "min":"0.001", "max":"0", "enabled":true,"comment":"Minimum deposit amount is 0.001 BTC. We do not support BSC and BEP20 network, please consider this when sending funds", "commission_desc":"0%", "currency_confirmations":1 },
        //             { "type":"withdraw", "name":"BTC", "currency_name":"BTC", "min":"0.001", "max":"350", "enabled":true,"comment":"Do not withdraw directly to the Crowdfunding or ICO address as your account will not be credited with tokens from such sales.", "commission_desc":"0.0005 BTC", "currency_confirmations":6 }
        //         ],
        //         "ETH":[
        //             { "type":"withdraw", "name":"ETH", "currency_name":"ETH", "min":"0.01", "max":"500", "enabled":true,"comment":"Do not withdraw directly to the Crowdfunding or ICO address as your account will not be credited with tokens from such sales.", "commission_desc":"0.004 ETH", "currency_confirmations":4 },
        //             { "type":"deposit", "name":"ETH", "currency_name":"ETH", "min":"0.01", "max":"0", "enabled":true,"comment":"Minimum deposit amount is 0.01 ETH. We do not support BSC and BEP20 network, please consider this when sending funds", "commission_desc":"0%", "currency_confirmations":1 }
        //         ],
        //         "USDT":[
        //             { "type":"deposit", "name":"USDT (OMNI)", "currency_name":"USDT", "min":"10", "max":"0", "enabled":false,"comment":"Minimum deposit amount is 10 USDT", "commission_desc":"0%", "currency_confirmations":2 },
        //             { "type":"withdraw", "name":"USDT (OMNI)", "currency_name":"USDT", "min":"10", "max":"100000", "enabled":false,"comment":"Do not withdraw directly to the Crowdfunding or ICO address as your account will not be credited with tokens from such sales.", "commission_desc":"5 USDT", "currency_confirmations":6 },
        //             { "type":"deposit", "name":"USDT (ERC20)", "currency_name":"USDT", "min":"10", "max":"0", "enabled":true,"comment":"Minimum deposit amount is 10 USDT", "commission_desc":"0%", "currency_confirmations":2 },
        //             {
        //                 "type":"withdraw",
        //                 "name":"USDT (ERC20)",
        //                 "currency_name":"USDT",
        //                 "min":"55",
        //                 "max":"200000",
        //                 "enabled":true,
        //                 "comment":"Caution! Do not withdraw directly to a crowdfund or ICO address, as your account will not be credited with tokens from such sales. Recommendation: Due to the high load of ERC20 network, using TRC20 address for withdrawal is recommended.",
        //                 "commission_desc":"10 USDT",
        //                 "currency_confirmations":6
        //             },
        //             { "type":"deposit", "name":"USDT (TRC20)", "currency_name":"USDT", "min":"10", "max":"100000", "enabled":true,"comment":"Minimum deposit amount is 10 USDT. Only TRON main network supported", "commission_desc":"0%", "currency_confirmations":2 },
        //             { "type":"withdraw", "name":"USDT (TRC20)", "currency_name":"USDT", "min":"10", "max":"150000", "enabled":true,"comment":"Caution! Do not withdraw directly to a crowdfund or ICO address, as your account will not be credited with tokens from such sales. Only TRON main network supported.", "commission_desc":"1 USDT", "currency_confirmations":6 }
        //         ],
        //         "XLM":[
        //             { "type":"deposit", "name":"XLM", "currency_name":"XLM", "min":"1", "max":"1000000", "enabled":true,"comment":"Attention! A deposit without memo(invoice) will not be credited. Minimum deposit amount is 1 XLM. We do not support BSC and BEP20 network, please consider this when sending funds", "commission_desc":"0%", "currency_confirmations":1 },
        //             { "type":"withdraw", "name":"XLM", "currency_name":"XLM", "min":"21", "max":"1000000", "enabled":true,"comment":"Caution! Do not withdraw directly to a crowdfund or ICO address, as your account will not be credited with tokens from such sales.", "commission_desc":"0.01 XLM", "currency_confirmations":1 }
        //         ],
        //     }
        //
        let mut result: Value = Value::new_object();
        let mut crypto_list_keys: Value = crypto_list.clone().keys();
        let mut i: usize = 0;
        while i < crypto_list_keys.len() {
            let mut code: Value = crypto_list_keys.get(i.into());
            if codes.clone().is_nonnullish() && !self.in_array(code.clone(), codes.clone()).is_truthy() {
                continue;
            };
            result.set(code.clone(), Value::Json(normalize(&Value::Json(json!({
                "deposit": Value::Undefined,
                "withdraw": Value::Undefined
            }))).unwrap()));
            let mut currency: Value = self.currency(code.clone());
            let mut currency_id: Value = self.safe_string(currency.clone(), Value::from("id"), Value::Undefined);
            let mut providers: Value = self.safe_value(crypto_list.clone(), currency_id.clone(), Value::new_array());
            let mut j: usize = 0;
            while j < providers.len() {
                let mut provider: Value = providers.get(j.into());
                let mut type_inner: Value = self.safe_string(provider.clone(), Value::from("type"), Value::Undefined);
                let mut commission_desc: Value = self.safe_string(provider.clone(), Value::from("commission_desc"), Value::Undefined);
                let mut fee: Value = <Self as Exmo>::parse_fixed_float_value(self, commission_desc.clone());
                result.get(code.clone().clone()).set(type_inner.clone(), fee.clone());
                j += 1;
            };
            result.get(code.clone().clone()).set("info".into(), providers.clone());
            i += 1;
        };
        // cache them for later use
        self.get("options".into()).set("transactionFees".into(), result.clone());
        return result.clone();
    }

    async fn fetch_deposit_withdraw_fees(&mut self, mut codes: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("publicGetPaymentsProvidersCryptoList".into(), params.clone(), Value::Undefined).await;
        //
        //    {
        //        "USDT": [
        //            {
        //                "type": "deposit", // or "withdraw"
        //                "name": "USDT (ERC20)",
        //                "currency_name": "USDT",
        //                "min": "10",
        //                "max": "0",
        //                "enabled": true,
        //                "comment": "Minimum deposit amount is 10 USDT",
        //                "commission_desc": "0%",
        //                "currency_confirmations": 2
        //            },
        //            ...
        //        ],
        //        ...
        //    }
        //
        let mut result: Value = self.parse_deposit_withdraw_fees(response.clone(), codes.clone(), Value::Undefined);
        // cache them for later use
        self.get("options".into()).set("transactionFees".into(), result.clone());
        return result.clone();
    }

    fn parse_deposit_withdraw_fee(&self, mut fee: Value, mut currency: Value) -> Value {
        //
        //    [
        //        {
        //            "type": "deposit", // or "withdraw"
        //            "name": "BTC",
        //            "currency_name": "BTC",
        //            "min": "0.001",
        //            "max": "0",
        //            "enabled": true,
        //            "comment": "Minimum deposit amount is 0.001 BTC. We do not support BSC and BEP20 network, please consider this when sending funds",
        //            "commission_desc": "0%",
        //            "currency_confirmations": 1
        //        },
        //        ...
        //    ]
        //
        let mut result: Value = self.deposit_withdraw_fee(fee.clone());
        let mut i: usize = 0;
        while i < fee.len() {
            let mut provider: Value = fee.get(i.into());
            let mut r#type: Value = self.safe_string(provider.clone(), Value::from("type"), Value::Undefined);
            let mut network_id: Value = self.safe_string(provider.clone(), Value::from("name"), Value::Undefined);
            let mut network_code: Value = self.network_id_to_code(network_id.clone(), self.safe_string(currency.clone(), Value::from("code"), Value::Undefined));
            let mut commission_desc: Value = self.safe_string(provider.clone(), Value::from("commission_desc"), Value::Undefined);
            let mut split_commission_desc: Value = Value::new_array();
            let mut percentage: Value = Value::Undefined;
            if commission_desc.clone().is_nonnullish() {
                split_commission_desc = commission_desc.split(Value::from("%"));
                let mut split_commission_desc_length: usize = split_commission_desc.len();
                percentage = Value::from(split_commission_desc_length >= Value::from(2));
            };
            let mut network: Value = self.safe_value(result.get(Value::from("networks")), network_code.clone(), Value::Undefined);
            if network.clone().is_nullish() {
                result.get(Value::from("networks")).set(network_code.clone(), Value::Json(normalize(&Value::Json(json!({
                    "withdraw": Value::Json(normalize(&Value::Json(json!({
                        "fee": Value::Undefined,
                        "percentage": Value::Undefined
                    }))).unwrap()),
                    "deposit": Value::Json(normalize(&Value::Json(json!({
                        "fee": Value::Undefined,
                        "percentage": Value::Undefined
                    }))).unwrap())
                }))).unwrap()));
            };
            result.get(Value::from("networks")).get(network_code.clone().clone()).set(r#type.clone(), Value::Json(normalize(&Value::Json(json!({
                "fee": <Self as Exmo>::parse_fixed_float_value(self, self.safe_string(split_commission_desc.clone(), Value::from(0), Value::Undefined)),
                "percentage": percentage.clone()
            }))).unwrap()));
            i += 1;
        };
        return self.assign_default_deposit_withdraw_fees(result.clone(), Value::Undefined);
    }

    async fn fetch_currencies(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut promises: Value = Value::new_array();
        //
        promises.push(self.dispatch("publicGetCurrencyListExtended".into(), params.clone(), Value::Undefined).await);
        //
        //     [
        //         {"name":"VLX","description":"Velas"},
        //         {"name":"RUB","description":"Russian Ruble"},
        //         {"name":"BTC","description":"Bitcoin"},
        //         {"name":"USD","description":"US Dollar"}
        //     ]
        //
        promises.push(self.dispatch("publicGetPaymentsProvidersCryptoList".into(), params.clone(), Value::Undefined).await);
        //
        //     {
        //         "BTC":[
        //             { "type":"deposit", "name":"BTC", "currency_name":"BTC", "min":"0.001", "max":"0", "enabled":true,"comment":"Minimum deposit amount is 0.001 BTC. We do not support BSC and BEP20 network, please consider this when sending funds", "commission_desc":"0%", "currency_confirmations":1 },
        //             { "type":"withdraw", "name":"BTC", "currency_name":"BTC", "min":"0.001", "max":"350", "enabled":true,"comment":"Do not withdraw directly to the Crowdfunding or ICO address as your account will not be credited with tokens from such sales.", "commission_desc":"0.0005 BTC", "currency_confirmations":6 }
        //         ],
        //         "ETH":[
        //             { "type":"withdraw", "name":"ETH", "currency_name":"ETH", "min":"0.01", "max":"500", "enabled":true,"comment":"Do not withdraw directly to the Crowdfunding or ICO address as your account will not be credited with tokens from such sales.", "commission_desc":"0.004 ETH", "currency_confirmations":4 },
        //             { "type":"deposit", "name":"ETH", "currency_name":"ETH", "min":"0.01", "max":"0", "enabled":true,"comment":"Minimum deposit amount is 0.01 ETH. We do not support BSC and BEP20 network, please consider this when sending funds", "commission_desc":"0%", "currency_confirmations":1 }
        //         ],
        //         "USDT":[
        //             { "type":"deposit", "name":"USDT (OMNI)", "currency_name":"USDT", "min":"10", "max":"0", "enabled":false,"comment":"Minimum deposit amount is 10 USDT", "commission_desc":"0%", "currency_confirmations":2 },
        //             { "type":"withdraw", "name":"USDT (OMNI)", "currency_name":"USDT", "min":"10", "max":"100000", "enabled":false,"comment":"Do not withdraw directly to the Crowdfunding or ICO address as your account will not be credited with tokens from such sales.", "commission_desc":"5 USDT", "currency_confirmations":6 },
        //             { "type":"deposit", "name":"USDT (ERC20)", "currency_name":"USDT", "min":"10", "max":"0", "enabled":true,"comment":"Minimum deposit amount is 10 USDT", "commission_desc":"0%", "currency_confirmations":2 },
        //             { "type":"withdraw", "name":"USDT (ERC20)", "currency_name":"USDT", "min":"55", "max":"200000", "enabled":true, "comment":"Caution! Do not withdraw directly to a crowdfund or ICO address, as your account will not be credited with tokens from such sales. Recommendation: Due to the high load of ERC20 network, using TRC20 address for withdrawal is recommended.",  "commission_desc":"10 USDT", "currency_confirmations":6 },
        //             { "type":"deposit", "name":"USDT (TRC20)", "currency_name":"USDT", "min":"10", "max":"100000", "enabled":true,"comment":"Minimum deposit amount is 10 USDT. Only TRON main network supported", "commission_desc":"0%", "currency_confirmations":2 },
        //             { "type":"withdraw", "name":"USDT (TRC20)", "currency_name":"USDT", "min":"10", "max":"150000", "enabled":true,"comment":"Caution! Do not withdraw directly to a crowdfund or ICO address, as your account will not be credited with tokens from such sales. Only TRON main network supported.", "commission_desc":"1 USDT", "currency_confirmations":6 }
        //         ],
        //         "XLM":[
        //             { "type":"deposit", "name":"XLM", "currency_name":"XLM", "min":"1", "max":"1000000", "enabled":true,"comment":"Attention! A deposit without memo(invoice) will not be credited. Minimum deposit amount is 1 XLM. We do not support BSC and BEP20 network, please consider this when sending funds", "commission_desc":"0%", "currency_confirmations":1 },
        //             { "type":"withdraw", "name":"XLM", "currency_name":"XLM", "min":"21", "max":"1000000", "enabled":true,"comment":"Caution! Do not withdraw directly to a crowdfund or ICO address, as your account will not be credited with tokens from such sales.", "commission_desc":"0.01 XLM", "currency_confirmations":1 }
        //         ],
        //     }
        //
        let mut responses: Value = Promise::all(promises.clone()).await;
        let mut currency_list: Value = responses.get(Value::from(0));
        let mut crypto_list: Value = responses.get(Value::from(1));
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < currency_list.len() {
            let mut currency: Value = currency_list.get(i.into());
            let mut currency_id: Value = self.safe_string(currency.clone(), Value::from("name"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut r#type: Value = Value::from("crypto");
            let mut networks: Value = Value::new_object();
            let mut providers: Value = self.safe_list(crypto_list.clone(), currency_id.clone(), Value::Undefined);
            if providers.clone().is_nullish() {
                r#type = Value::from("fiat");
            } else {
                let mut j: usize = 0;
                while j < providers.len() {
                    let mut provider: Value = providers.get(j.into());
                    let mut name: Value = self.safe_string(provider.clone(), Value::from("name"), Value::Undefined);
                    // get network-id by removing extra things
                    let mut network_id: Value = name.replace(currency_id.clone() + Value::from(" "), Value::from(""));
                    network_id = network_id.replace(Value::from("("), Value::from(""));
                    let mut replace_char: Value = Value::from(")");
                    // transpiler trick
                    network_id = network_id.replace(replace_char.clone(), Value::from(""));
                    let mut network_code: Value = self.network_id_to_code(network_id.clone(), Value::Undefined);
                    if !networks.contains_key(network_code.clone()) {
                        networks.set(network_code.clone(), Value::Json(normalize(&Value::Json(json!({
                            "id": network_id.clone(),
                            "network": network_code.clone(),
                            "active": Value::Undefined,
                            "deposit": Value::Undefined,
                            "withdraw": Value::Undefined,
                            "fee": Value::Undefined,
                            "limits": Value::Json(normalize(&Value::Json(json!({
                                "withdraw": Value::Json(normalize(&Value::Json(json!({
                                    "min": Value::Undefined,
                                    "max": Value::Undefined
                                }))).unwrap()),
                                "deposit": Value::Json(normalize(&Value::Json(json!({
                                    "min": Value::Undefined,
                                    "max": Value::Undefined
                                }))).unwrap())
                            }))).unwrap()),
                            "info": Value::new_array()
                        }))).unwrap()));
                    };
                    // set as array, because of multiple network sub-entries
                    let mut type_inner: Value = self.safe_string(provider.clone(), Value::from("type"), Value::Undefined);
                    let mut min_value: Value = self.safe_string(provider.clone(), Value::from("min"), Value::Undefined);
                    let mut max_value: Value = self.safe_string(provider.clone(), Value::from("max"), Value::Undefined);
                    let mut active_provider: Value = self.safe_bool(provider.clone(), Value::from("enabled"), Value::Undefined);
                    let mut network_entry: Value = networks.get(network_code.clone().clone());
                    if type_inner.clone() == Value::from("deposit") {
                        network_entry.set("deposit".into(), active_provider.clone());
                        network_entry.get(Value::from("limits")).get(Value::from("deposit")).set("min".into(), min_value.clone());
                        network_entry.get(Value::from("limits")).get(Value::from("deposit")).set("max".into(), max_value.clone());
                    } else if type_inner.clone() == Value::from("withdraw") {
                        network_entry.set("withdraw".into(), active_provider.clone());
                        network_entry.get(Value::from("limits")).get(Value::from("withdraw")).set("min".into(), min_value.clone());
                        network_entry.get(Value::from("limits")).get(Value::from("withdraw")).set("max".into(), max_value.clone());
                    };
                    let mut info: Value = self.safe_list(network_entry.clone(), Value::from("info"), Value::Undefined);
                    info.push(provider.clone());
                    network_entry.set("info".into(), info.clone());
                    networks.set(network_code.clone(), network_entry.clone());
                    j += 1;
                };
            };
            result.set(code.clone(), self.safe_currency_structure(Value::Json(normalize(&Value::Json(json!({
                "id": currency_id.clone(),
                "code": code.clone(),
                "name": self.safe_string(currency.clone(), Value::from("description"), Value::Undefined),
                "type": r#type.clone(),
                "active": Value::Undefined,
                "deposit": Value::Undefined,
                "withdraw": Value::Undefined,
                "fee": Value::Undefined,
                "precision": self.parse_number(Value::from("1e-8"), Value::Undefined),
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "withdraw": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "deposit": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap())
                }))).unwrap()),
                "info": Value::Json(normalize(&Value::Json(json!({
                    "currency": currency.clone(),
                    "providers": providers.clone()
                }))).unwrap()),
                "networks": networks.clone()
            }))).unwrap())));
            i += 1;
        };
        return result.clone();
    }

    

    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        // Match endpoints by substring on common OHLCV path tokens (klines, candles,
        // ohlcv, barhist, history, kline, candle, ohlc).
        'outer: for token in ["klines", "candles", "ohlcv", "barhist", "kline", "candle", "ohlc"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            if item.is_array() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_ohlcv(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        //     {
        //         "t":1584057600000,
        //         "o":0.02235144,
        //         "c":0.02400233,
        //         "h":0.025171,
        //         "l":0.02221,
        //         "v":5988.34031761
        //     }
        //
        return Value::Json(serde_json::Value::Array(vec![(self.safe_integer(ohlcv.clone(), Value::from("t"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("o"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("h"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("l"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("c"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("v"), Value::Undefined)).into()]));
    }

    fn parse_balance(&self, mut response: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response.clone()
        }))).unwrap());
        let mut wallets: Value = self.safe_value(response.clone(), Value::from("wallets"), Value::Undefined);
        if wallets.clone().is_nonnullish() {
            let mut currency_ids: Value = wallets.clone().keys();
            let mut i: usize = 0;
            while i < currency_ids.len() {
                let mut currency_id: Value = currency_ids.get(i.into());
                let mut item: Value = wallets.get(currency_id.clone().clone());
                let mut currency: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
                let mut account: Value = self.account();
                account.set("used".into(), self.safe_string(item.clone(), Value::from("used"), Value::Undefined));
                account.set("free".into(), self.safe_string(item.clone(), Value::from("free"), Value::Undefined));
                account.set("total".into(), self.safe_string(item.clone(), Value::from("balance"), Value::Undefined));
                result.set(currency.clone(), account.clone());
                i += 1;
            };
        } else {
            let mut free: Value = self.safe_value(response.clone(), Value::from("balances"), Value::new_object());
            let mut used: Value = self.safe_value(response.clone(), Value::from("reserved"), Value::new_object());
            let mut currency_ids: Value = free.clone().keys();
            let mut i: usize = 0;
            while i < currency_ids.len() {
                let mut currency_id: Value = currency_ids.get(i.into());
                let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
                let mut account: Value = self.account();
                if free.contains_key(currency_id.clone()) {
                    account.set("free".into(), self.safe_string(free.clone(), currency_id.clone(), Value::Undefined));
                };
                if used.contains_key(currency_id.clone()) {
                    account.set("used".into(), self.safe_string(used.clone(), currency_id.clone(), Value::Undefined));
                };
                result.set(code.clone(), account.clone());
                i += 1;
            };
        };
        return self.safe_balance(result.clone());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["account/balance", "balance", "wallet/balance", "accounts/balance", "account"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(&format!("/{}/", token)) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "balance"), ("private", "GET", "account/balance"), ("private", "GET", "wallet/balance")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut ob = crate::exchange::unwrap_response_order_book(&json_resp);
        if let serde_json::Value::Object(ref mut map) = ob {
            if !map.contains_key("symbol") { map.insert("symbol".to_string(), serde_json::json!(symbol.unwrap_str())); }
        }
        Value::Json(ob)
    }


    async fn fetch_order_books(&mut self, mut symbols: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut ids: Value = Value::Undefined;
        if symbols.clone().is_nullish() {
            ids = self.get("ids".into()).join(Value::from(","));
            // max URL length is 2083 symbols, including http schema, hostname, tld, etc...
            if ids.len() > Value::from(2048) {
                let mut num_ids: usize = self.get("ids".into()).len();
                return Value::Undefined;
            };
        } else {
            ids = self.market_ids(symbols.clone());
            ids = ids.join(Value::from(","));
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "pair": ids.clone()
        }))).unwrap());
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        let mut response: Value = self.dispatch("publicGetOrderBook".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut result: Value = Value::new_object();
        let mut market_ids: Value = response.clone().keys();
        let mut i: usize = 0;
        while i < market_ids.len() {
            let mut market_id: Value = market_ids.get(i.into());
            let mut symbol: Value = self.safe_symbol(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            result.set(symbol.clone(), self.parse_order_book(response.get(market_id.clone().clone()), symbol.clone(), Value::Undefined, Value::from("bid"), Value::from("ask"), Value::Undefined, Value::Undefined, Value::Undefined));
            i += 1;
        };
        return result.clone();
    }

    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        //     {
        //         "buy_price":"0.00002996",
        //         "sell_price":"0.00003002",
        //         "last_trade":"0.00002992",
        //         "high":"0.00003028",
        //         "low":"0.00002935",
        //         "avg":"0.00002963",
        //         "vol":"1196546.3163222",
        //         "vol_curr":"35.80066578",
        //         "updated":1642291733
        //     }
        //
        let mut timestamp: Value = self.safe_timestamp(ticker.clone(), Value::from("updated"), Value::Undefined);
        market = self.safe_market(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined);
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("last_trade"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("symbol")),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "high": self.safe_string(ticker.clone(), Value::from("high"), Value::Undefined),
            "low": self.safe_string(ticker.clone(), Value::from("low"), Value::Undefined),
            "bid": self.safe_string(ticker.clone(), Value::from("buy_price"), Value::Undefined),
            "bidVolume": Value::Undefined,
            "ask": self.safe_string(ticker.clone(), Value::from("sell_price"), Value::Undefined),
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": Value::Undefined,
            "close": last.clone(),
            "last": last.clone(),
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": self.safe_string(ticker.clone(), Value::from("avg"), Value::Undefined),
            "baseVolume": self.safe_string(ticker.clone(), Value::from("vol"), Value::Undefined),
            "quoteVolume": self.safe_string(ticker.clone(), Value::from("vol_curr"), Value::Undefined),
            "info": ticker.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        // Some exchanges wrap the ticker in {data: {...}} or {result: {...}} — unwrap if so.
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut tk = json_resp.clone();
        if !tk.is_array() && tk.get("last").is_none() && tk.get("close").is_none() && tk.get("bid").is_none() {
            for key in &["data", "result", "ticker"] {
                if let Some(v) = json_resp.get(key) {
                    if v.is_object() { tk = v.clone(); break; }
                    if let Some(arr) = v.as_array() {
                        if let Some(first) = arr.first() { tk = first.clone(); break; }
                    }
                }
            }
        }
        let market: Value = Value::Undefined;
        let parsed = <Self as Exchange>::parse_ticker(self, Value::Json(tk.clone()), market);
        if parsed.is_undefined() { Value::Json(tk) } else { parsed }
    }


    fn parse_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchTrades (public)
        //
        //     {
        //         "trade_id":165087520,
        //         "date":1587470005,
        //         "type":"buy",
        //         "quantity":"1.004",
        //         "price":"0.02491461",
        //         "amount":"0.02501426"
        //     },
        //
        // fetchMyTrades, fetchOrderTrades
        //
        //     {
        //         "trade_id": 3,
        //         "date": 1435488248,
        //         "type": "buy",
        //         "pair": "BTC_USD",
        //         "order_id": 12345,
        //         "quantity": 1,
        //         "price": 100,
        //         "amount": 100,
        //         "exec_type": "taker",
        //         "commission_amount": "0.02",
        //         "commission_currency": "BTC",
        //         "commission_percent": "0.2"
        //     }
        //
        // fetchMyTrades (margin)
        //
        //    {
        //        "trade_id": "692861757015952517",
        //        "trade_dt": "1693951853197811824",
        //        "trade_type": "buy",
        //        "pair": "ADA_USDT",
        //        "quantity": "1.96607879",
        //        "price": "0.2568",
        //        "amount": "0.50488903"
        //    }
        //
        let mut timestamp: Value = self.safe_timestamp(trade.clone(), Value::from("date"), Value::Undefined);
        let mut id: Value = self.safe_string(trade.clone(), Value::from("trade_id"), Value::Undefined);
        let mut order_id: Value = self.safe_string(trade.clone(), Value::from("order_id"), Value::Undefined);
        let mut price_string: Value = self.safe_string(trade.clone(), Value::from("price"), Value::Undefined);
        let mut amount_string: Value = self.safe_string(trade.clone(), Value::from("quantity"), Value::Undefined);
        let mut cost_string: Value = self.safe_string(trade.clone(), Value::from("amount"), Value::Undefined);
        let mut side: Value = self.safe_string_2(trade.clone(), Value::from("type"), Value::from("trade_type"), Value::Undefined);
        let mut r#type: Value = Value::Undefined;
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("pair"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::from("_"), Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut is_maker: Value = self.safe_value(trade.clone(), Value::from("is_maker"), Value::Undefined);
        let mut taker_or_maker_default: Value = Value::Undefined;
        if is_maker.clone().is_nonnullish() {
            taker_or_maker_default = if is_maker.is_truthy() { Value::from("maker") } else { Value::from("taker") };
        };
        let mut taker_or_maker: Value = self.safe_string(trade.clone(), Value::from("exec_type"), taker_or_maker_default.clone());
        let mut fee: Value = Value::Undefined;
        let mut fee_cost_string: Value = self.safe_string(trade.clone(), Value::from("commission_amount"), Value::Undefined);
        if fee_cost_string.clone().is_nonnullish() {
            let mut fee_currency_id: Value = self.safe_string(trade.clone(), Value::from("commission_currency"), Value::Undefined);
            let mut fee_currency_code: Value = self.safe_currency_code(fee_currency_id.clone(), Value::Undefined);
            let mut fee_rate_string: Value = self.safe_string(trade.clone(), Value::from("commission_percent"), Value::Undefined);
            if fee_rate_string.clone().is_nonnullish() {
                fee_rate_string = Precise::string_div(fee_rate_string.clone(), Value::from("1000"), Value::from(18));
            };
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_cost_string.clone(),
                "currency": fee_currency_code.clone(),
                "rate": fee_rate_string.clone()
            }))).unwrap());
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "info": trade.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol.clone(),
            "order": order_id.clone(),
            "type": r#type.clone(),
            "side": side.clone(),
            "takerOrMaker": taker_or_maker.clone(),
            "price": price_string.clone(),
            "amount": amount_string.clone(),
            "cost": cost_string.clone(),
            "fee": fee.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        let mut raw: Value = Value::Undefined;
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { raw = rv; break; }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            // Already-parsed trades have both `price` and `amount` keys.
            if item.get("price").is_some() && item.get("amount").is_some() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_trade(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["mytrades", "my_trades", "trades/history", "fills", "user/trades", "executions"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "myTrades"), ("private", "GET", "fills"), ("private", "GET", "trades/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn create_market_order_with_cost(&mut self, mut symbol: Value, mut side: Value, mut cost: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        params = extend_2(params.clone(), Value::Json(normalize(&Value::Json(json!({
            "cost": cost.clone()
        }))).unwrap()));
        return <Self as Exmo>::create_order(self, symbol.clone(), Value::from("market"), side.clone(), cost.clone(), Value::Undefined, params.clone()).await;
    }

    async fn create_market_buy_order_with_cost(&mut self, mut symbol: Value, mut cost: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        params = extend_2(params.clone(), Value::Json(normalize(&Value::Json(json!({
            "cost": cost.clone()
        }))).unwrap()));
        return <Self as Exmo>::create_order(self, symbol.clone(), Value::from("market"), Value::from("buy"), cost.clone(), Value::Undefined, params.clone()).await;
    }

    async fn create_market_sell_order_with_cost(&mut self, mut symbol: Value, mut cost: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        params = extend_2(params.clone(), Value::Json(normalize(&Value::Json(json!({
            "cost": cost.clone()
        }))).unwrap()));
        return <Self as Exmo>::create_order(self, symbol.clone(), Value::from("market"), Value::from("sell"), cost.clone(), Value::Undefined, params.clone()).await;
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("type".into(), r#type.clone());
        request.set("side".into(), side.clone());
        request.set("amount".into(), amount.clone());
        if price.is_nonnullish() { request.set("price".into(), price.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "POST" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "POST", "order"), ("private", "POST", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("orderId".into(), id.clone());
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "DELETE" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "DELETE", "order"), ("private", "DELETE", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id.to_string()
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostOrderTrades".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "type": "buy",
        //         "in_currency": "BTC",
        //         "in_amount": "1",
        //         "out_currency": "USD",
        //         "out_amount": "100",
        //         "trades": [
        //             {
        //                 "trade_id": 3,
        //                 "date": 1435488248,
        //                 "type": "buy",
        //                 "pair": "BTC_USD",
        //                 "order_id": 12345,
        //                 "quantity": 1,
        //                 "price": 100,
        //                 "amount": 100
        //             }
        //         ]
        //     }
        //
        let mut order: Value = <Self as Exmo>::parse_order(self, response.clone(), Value::Undefined);
        order.set("id".into(), id.to_string());
        return order.clone();
    }

    async fn fetch_order_trades(&mut self, mut id: Value, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("fetchOrderTrades"), params.clone(), Value::Undefined));
        if margin_mode.clone() == Value::from("cross") {
            return Value::Undefined;
        };
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id.to_string()
        }))).unwrap());
        let mut response: Value = Value::Undefined;
        if margin_mode.clone() == Value::from("isolated") {
            response = self.dispatch("privatePostMarginUserOrderTrades".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            //
            //    {
            //        "trades": [
            //            {
            //                "is_maker": false,
            //                "order_id": "123",
            //                "pair": "BTC_USD",
            //                "price": "54122.25",
            //                "quantity": "0.00069994",
            //                "trade_dt": "1619069561718824428",
            //                "trade_id": "692842802860135010",
            //                "type": "sell"
            //            }
            //        ]
            //    }
            //
            response = self.dispatch("privatePostOrderTrades".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        //
        //     {
        //         "type": "buy",
        //         "in_currency": "BTC",
        //         "in_amount": "1",
        //         "out_currency": "USD",
        //         "out_amount": "100",
        //         "trades": [
        //             {
        //                 "trade_id": 3,
        //                 "date": 1435488248,
        //                 "type": "buy",
        //                 "pair": "BTC_USD",
        //                 "order_id": 12345,
        //                 "quantity": 1,
        //                 "price": 100,
        //                 "amount": 100,
        //                 "exec_type": "taker",
        //                 "commission_amount": "0.02",
        //                 "commission_currency": "BTC",
        //                 "commission_percent": "0.2"
        //             }
        //         ]
        //     }
        //
        let mut trades: Value = self.safe_list(response.clone(), Value::from("trades"), Value::Undefined);
        return self.parse_trades(trades.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined).await;
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["openorders", "open_orders", "orders/open", "orders/active"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "openOrders"), ("private", "GET", "orders/open"), ("private", "GET", "orders/active")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_status(&self, mut status: Value) -> Value {
        if status.clone().is_nullish() {
            return Value::Undefined;
        };
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "cancel_started": Value::from("canceled")
        }))).unwrap());
        if status.index_of(Value::from("cancel")) >= Value::from(0) {
            status = Value::from("canceled");
        };
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_side(&self, mut order_type: Value) -> Value {
        let mut side: Value = Value::Json(normalize(&Value::Json(json!({
            "limit_buy": Value::from("buy"),
            "limit_sell": Value::from("sell"),
            "market_buy": Value::from("buy"),
            "market_sell": Value::from("sell"),
            "stop_buy": Value::from("buy"),
            "stop_sell": Value::from("sell"),
            "stop_limit_buy": Value::from("buy"),
            "stop_limit_sell": Value::from("sell"),
            "trailing_stop_buy": Value::from("buy"),
            "trailing_stop_sell": Value::from("sell"),
            "stop_market_sell": Value::from("sell"),
            "stop_market_buy": Value::from("buy"),
            "buy": Value::from("buy"),
            "sell": Value::from("sell")
        }))).unwrap());
        return self.safe_string(side.clone(), order_type.clone(), order_type.clone());
    }

    fn parse_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        // fetchOrders, fetchOpenOrders, fetchClosedOrders, fetchCanceledOrders
        //
        //     {
        //         "order_id": "14",
        //         "created": "1435517311",
        //         "type": "buy",
        //         "pair": "BTC_USD",
        //         "price": "100",
        //         "quantity": "1",
        //         "amount": "100"
        //     }
        //
        // fetchOrder
        //
        //     {
        //         "type": "buy",
        //         "in_currency": "BTC",
        //         "in_amount": "1",
        //         "out_currency": "USD",
        //         "out_amount": "100",
        //         "trades": [
        //             {
        //                 "trade_id": 3,
        //                 "date": 1435488248,
        //                 "type": "buy",
        //                 "pair": "BTC_USD",
        //                 "order_id": 12345,
        //                 "quantity": 1,
        //                 "price": 100,
        //                 "amount": 100
        //             }
        //         ]
        //     }
        //
        // Margin fetchOpenOrders
        //
        //    {
        //        "client_id": "0",
        //        "comment": "",
        //        "created": "1619068707985325495",
        //        "distance": "0",
        //        "expire": 0,
        //        "funding_currency": "BTC",
        //        "funding_quantity": "0.01",
        //        "funding_rate": "0.02",
        //        "leverage": "2",
        //        "order_id": "123",
        //        "pair": "BTC_USD",
        //        "previous_type": "limit_sell",
        //        "price": "58000",
        //        "quantity": "0.01",
        //        "src": 0,
        //        "stop_price": "0",
        //        "trigger_price": "58000",
        //        "type": "limit_sell",
        //        "updated": 1619068707989411800
        //    }
        //
        // Margin fetchClosedOrders
        //
        //    {
        //        "distance": "0",
        //        "event_id": "692842802860022508",
        //        "event_time": "1619069531190173720",
        //        "event_type": "OrderCancelStarted",
        //        "order_id": "123",
        //        "order_status": "cancel_started",
        //        "order_type": "limit_sell",
        //        "pair": "BTC_USD",
        //        "price": "54115",
        //        "quantity": "0.001",
        //        "stop_price": "0",
        //        "trade_id": "0",
        //        "trade_price": "0",
        //        "trade_quantity": "0",
        //        "trade_type": ""
        //    },
        //
        let mut id: Value = self.safe_string_2(order.clone(), Value::from("order_id"), Value::from("parent_order_id"), Value::Undefined);
        let mut event_time: Value = self.safe_integer_product_2(order.clone(), Value::from("event_time"), Value::from("created"), Value::from(0.000001), Value::Undefined);
        let mut timestamp: Value = self.safe_timestamp(order.clone(), Value::from("created"), event_time.clone());
        let mut order_type: Value = self.safe_string_2(order.clone(), Value::from("type"), Value::from("order_type"), Value::Undefined);
        let mut side: Value = <Self as Exmo>::parse_side(self, order_type.clone());
        let mut market_id: Value = Value::Undefined;
        if order.contains_key(Value::from("pair")) {
            market_id = order.get(Value::from("pair"));
        } else if order.contains_key(Value::from("in_currency")) && order.contains_key(Value::from("out_currency")) {
            if side.clone() == Value::from("buy") {
                market_id = order.get(Value::from("in_currency")) + Value::from("_") + order.get(Value::from("out_currency"));
            } else {
                market_id = order.get(Value::from("out_currency")) + Value::from("_") + order.get(Value::from("in_currency"));
            };
        };
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut amount: Value = self.safe_string(order.clone(), Value::from("quantity"), Value::Undefined);
        if amount.clone().is_nullish() {
            let mut amount_field: Value = if side.clone() == Value::from("buy") { Value::from("in_amount") } else { Value::from("out_amount") };
            amount = self.safe_string(order.clone(), amount_field.clone(), Value::Undefined);
        };
        let mut price: Value = self.safe_string(order.clone(), Value::from("price"), Value::Undefined);
        let mut cost: Value = self.safe_string(order.clone(), Value::from("amount"), Value::Undefined);
        let mut transactions: Value = self.safe_value(order.clone(), Value::from("trades"), Value::new_array());
        let mut client_order_id: Value = self.safe_integer(order.clone(), Value::from("client_id"), Value::Undefined);
        let mut trigger_price: Value = self.safe_string(order.clone(), Value::from("stop_price"), Value::Undefined);
        if trigger_price.clone() == Value::from("0") {
            trigger_price = Value::Undefined;
        };
        let mut r#type: Value = Value::Undefined;
        if order_type.clone() != Value::from("buy") && order_type.clone() != Value::from("sell") {
            r#type = order_type.clone();
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "clientOrderId": client_order_id.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "timestamp": timestamp.clone(),
            "lastTradeTimestamp": self.safe_integer_product(order.clone(), Value::from("updated"), Value::from(0.000001), Value::Undefined),
            "status": <Self as Exmo>::parse_status(self, self.safe_string(order.clone(), Value::from("order_status"), Value::Undefined)),
            "symbol": symbol.clone(),
            "type": r#type.clone(),
            "timeInForce": Value::Undefined,
            "postOnly": Value::Undefined,
            "side": side.clone(),
            "price": price.clone(),
            "triggerPrice": trigger_price.clone(),
            "cost": cost.clone(),
            "amount": amount.clone(),
            "filled": Value::Undefined,
            "remaining": Value::Undefined,
            "average": Value::Undefined,
            "trades": transactions.clone(),
            "fee": Value::Undefined,
            "info": order.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_canceled_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("fetchOrders"), params.clone(), Value::Undefined));
        if margin_mode.clone() == Value::from("cross") {
            return Value::Undefined;
        };
        if limit.clone().is_nullish() {
            limit = Value::from(100);
        };
        let mut is_spot: Value = Value::from(margin_mode.clone() != Value::from("isolated"));
        if symbol.clone().is_nonnullish() {
            let mut market_inner: Value = self.market(symbol.clone());
            symbol = market_inner.get(Value::from("symbol"));
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "limit": limit.clone()
        }))).unwrap());
        request.set("offset".into(), if since.clone().is_nonnullish() { limit.clone() } else { Value::from(0) });
        request.set("limit".into(), limit.clone());
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        let mut response: Value = Value::Undefined;
        if is_spot.is_truthy() {
            response = self.dispatch("privatePostUserCancelledOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            //
            //    [
            //        {
            //            "order_id": "27056153840",
            //            "client_id": "0",
            //            "created": "1653428646",
            //            "type": "buy",
            //            "pair": "BTC_USDT",
            //            "quantity": "0.1",
            //            "price": "10",
            //            "amount": "1"
            //        }
            //    ]
            //
            params = extend_2(params.clone(), Value::Json(normalize(&Value::Json(json!({
                "status": Value::from("canceled")
            }))).unwrap()));
            return self.parse_orders(response.clone(), market.clone(), since.clone(), limit.clone(), params.clone()).await;
        } else {
            let mut response_swap: Value = self.dispatch("privatePostMarginUserOrderHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
            //
            //    {
            //        "items": [
            //            {
            //                "event_id": "692862104574106858",
            //                "event_time": "1694116400173489405",
            //                "event_type": "OrderCancelStarted",
            //                "order_id": "692862104561289319",
            //                "order_type": "stop_limit_sell",
            //                "order_status": "cancel_started",
            //                "trade_id": "0",
            //                "trade_type":"",
            //                "trade_quantity": "0",
            //                "trade_price": "0",
            //                "pair": "ADA_USDT",
            //                "quantity": "12",
            //                "price": "0.23",
            //                "stop_price": "0.22",
            //                "distance": "0"
            //            }
            //            ...
            //        ]
            //    }
            //
            let mut items: Value = self.safe_value(response_swap.clone(), Value::from("items"), Value::Undefined);
            let mut orders: Value = self.parse_orders(items.clone(), market.clone(), since.clone(), limit.clone(), params.clone());
            let mut result: Value = Value::new_array();
            let mut i: usize = 0;
            while i < orders.len() {
                let mut order: Value = orders.get(i.into());
                if order.get(Value::from("status")) == Value::from("canceled") {
                    result.push(order.clone());
                };
                i += 1;
            };
            return result.clone();
        };
        Value::Undefined
    }

    async fn edit_order(&mut self, mut id: Value, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("editOrder"), params.clone(), Value::Undefined));
        if margin_mode.clone() != Value::from("isolated") {
            return Value::Undefined;
        };
        let mut trigger_price: Value = self.safe_number_n(params.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from("triggerPrice")).into(), (Value::from("stopPrice")).into(), (Value::from("stop_price")).into()])), Value::Undefined);
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from("triggerPrice")).into(), (Value::from("stopPrice")).into()])));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id.clone()
        }))).unwrap());
        // id of the open order
        if amount.clone().is_nonnullish() {
            request.set("quantity".into(), amount.clone());
        };
        if price.clone().is_nonnullish() {
            request.set("price".into(), self.price_to_precision(market.get(Value::from("symbol")), price.clone()));
        };
        if trigger_price.clone().is_nonnullish() {
            request.set("stop_price".into(), self.price_to_precision(market.get(Value::from("symbol")), trigger_price.clone()));
        };
        let mut response: Value = self.dispatch("privatePostMarginUserOrderUpdate".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return <Self as Exmo>::parse_order(self, response.clone(), Value::Undefined).await;
    }

    async fn fetch_deposit_address(&mut self, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("privatePostDepositAddress".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //         "TRX":"TBnwrf4ZdoYXE3C8L2KMs7YPSL3fg6q6V9",
        //         "USDTTRC20":"TBnwrf4ZdoYXE3C8L2KMs7YPSL3fg6q6V9"
        //     }
        //
        let mut deposit_address: Value = self.safe_string(response.clone(), code.clone(), Value::Undefined);
        let mut address: Value = Value::Undefined;
        let mut tag: Value = Value::Undefined;
        if deposit_address.is_truthy() {
            let mut address_and_tag: Value = deposit_address.split(Value::from(","));
            address = address_and_tag.get(Value::from(0));
            let mut num_parts: usize = address_and_tag.len();
            if num_parts > Value::from(1) {
                tag = address_and_tag.get(Value::from(1));
            };
        };
        self.check_address(address.clone());
        return Value::Json(normalize(&Value::Json(json!({
            "info": response.clone(),
            "currency": code.clone(),
            "network": Value::Undefined,
            "address": address.clone(),
            "tag": tag.clone()
        }))).unwrap());
    }

    fn get_market_from_trades(&self, mut trades: Value) -> Value {
        let mut trades_by_symbol: Value = self.index_by(trades.clone(), Value::from("pair"));
        let mut symbols: Value = trades_by_symbol.clone().keys();
        let mut num_symbols: usize = symbols.len();
        if num_symbols == Value::from(1) {
            return self.get("markets".into()).get(symbols.get(Value::from(0)).clone());
        };
        return Value::Undefined;
    }

    async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, mut tag: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        (tag, params) = shift_2(self.handle_withdraw_tag_and_params(tag.clone(), params.clone()));
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "amount": amount.clone(),
            "currency": currency.get(Value::from("id")),
            "address": address.clone()
        }))).unwrap());
        if tag.clone().is_nonnullish() {
            request.set("invoice".into(), tag.clone());
        };
        let mut networks: Value = self.safe_value(self.get("options".into()), Value::from("networks"), Value::new_object());
        let mut network: Value = self.safe_string_upper(params.clone(), Value::from("network"), Value::Undefined);
        // this line allows the user to specify either ERC20 or ETH
        network = self.safe_string(networks.clone(), network.clone(), network.clone());
        // handle ERC20>ETH alias
        if network.clone().is_nonnullish() {
            request.set("transport".into(), network.clone());
            params = self.omit(params.clone(), Value::from("network"));
        };
        let mut response: Value = self.dispatch("privatePostWithdrawCrypt".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return <Self as Exmo>::parse_transaction(self, response.clone(), currency.clone()).await;
    }

    fn parse_transaction_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "transferred": Value::from("ok"),
            "paid": Value::from("ok"),
            "pending": Value::from("pending"),
            "processing": Value::from("pending"),
            "verifying": Value::from("pending")
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value) -> Value {
        //
        // fetchDepositsWithdrawals
        //
        //    {
        //        "dt": 1461841192,
        //        "type": "deposit",
        //        "curr": "RUB",
        //        "status": "processing",
        //        "provider": "Qiwi (LA) [12345]",
        //        "amount": "1",
        //        "account": "",
        //        "txid": "ec46f784ad976fd7f7539089d1a129fe46...",
        //    }
        //
        // fetchWithdrawals
        //
        //    {
        //        "operation_id": 47412538520634344,
        //        "created": 1573760013,
        //        "updated": 1573760013,
        //        "type": "withdraw",
        //        "currency": "DOGE",
        //        "status": "Paid",
        //        "amount": "300",
        //        "provider": "DOGE",
        //        "commission": "0",
        //        "account": "DOGE: DBVy8pF1f8yxaCVEHqHeR7kkcHecLQ8nRS",
        //        "order_id": 69670170,
        //        "provider_type": "crypto",
        //        "crypto_address": "DBVy8pF1f8yxaCVEHqHeR7kkcHecLQ8nRS",
        //        "card_number": "",
        //        "wallet_address": "",
        //        "email": "",
        //        "phone": "",
        //        "extra": {
        //            "txid": "f2b66259ae1580f371d38dd27e31a23fff8c04122b65ee3ab5a3f612d579c792",
        //            "confirmations": null,
        //            "excode": "",
        //            "invoice": ""
        //        },
        //        "error": ""
        //    }
        //
        // withdraw
        //
        //    {
        //        "result": true,
        //        "error": "",
        //        "task_id": 11775077
        //    }
        //
        let mut timestamp: Value = self.safe_timestamp_2(transaction.clone(), Value::from("dt"), Value::from("created"), Value::Undefined);
        let mut amount_string: Value = self.safe_string(transaction.clone(), Value::from("amount"), Value::Undefined);
        if amount_string.clone().is_nonnullish() {
            amount_string = Precise::string_abs(amount_string.clone());
        };
        let mut txid: Value = self.safe_string(transaction.clone(), Value::from("txid"), Value::Undefined);
        if txid.clone().is_nullish() {
            let mut extra: Value = self.safe_value(transaction.clone(), Value::from("extra"), Value::new_object());
            let mut extra_txid: Value = self.safe_string(extra.clone(), Value::from("txid"), Value::Undefined);
            if extra_txid.clone() != Value::from("") {
                txid = extra_txid.clone();
            };
        };
        let mut r#type: Value = self.safe_string(transaction.clone(), Value::from("type"), Value::Undefined);
        let mut currency_id: Value = self.safe_string_2(transaction.clone(), Value::from("curr"), Value::from("currency"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), currency.clone());
        let mut address: Value = Value::Undefined;
        let mut comment: Value = Value::Undefined;
        let mut account: Value = self.safe_string(transaction.clone(), Value::from("account"), Value::Undefined);
        if r#type.clone() == Value::from("deposit") {
            comment = account.clone();
        } else if r#type.clone() == Value::from("withdrawal") {
            address = account.clone();
            if address.clone().is_nonnullish() {
                let mut parts: Value = address.split(Value::from(":"));
                let mut num_parts: usize = parts.len();
                if num_parts == Value::from(2) {
                    address = self.safe_string(parts.clone(), Value::from(1), Value::Undefined);
                    address = address.replace(Value::from(" "), Value::from(""));
                };
            };
        };
        let mut fee: Value = Value::Json(normalize(&Value::Json(json!({
            "currency": Value::Undefined,
            "cost": Value::Undefined,
            "rate": Value::Undefined
        }))).unwrap());
        // fixed funding fees only (for now)
        if !self.get("fees".into()).get(Value::from("transaction")).get(Value::from("percentage")).is_truthy() {
            let mut key: Value = if r#type.clone() == Value::from("withdrawal") { Value::from("withdraw") } else { Value::from("deposit") };
            let mut fee_cost: Value = self.safe_string(transaction.clone(), Value::from("commission"), Value::Undefined);
            if fee_cost.clone().is_nullish() {
                let mut transaction_fees: Value = self.safe_value(self.get("options".into()), Value::from("transactionFees"), Value::new_object());
                let mut code_fees: Value = self.safe_value(transaction_fees.clone(), code.clone(), Value::new_object());
                fee_cost = self.safe_string(code_fees.clone(), key.clone(), Value::Undefined);
            };
            // users don't pay for cashbacks, no fees for that
            let mut provider: Value = self.safe_string(transaction.clone(), Value::from("provider"), Value::Undefined);
            if provider.clone() == Value::from("cashback") {
                fee_cost = Value::from("0");
            };
            if fee_cost.clone().is_nonnullish() {
                // withdrawal amount includes the fee
                if r#type.clone() == Value::from("withdrawal") {
                    amount_string = Precise::string_sub(amount_string.clone(), fee_cost.clone());
                };
                fee.set("cost".into(), self.parse_number(fee_cost.clone(), Value::Undefined));
                fee.set("currency".into(), code.clone());
            };
        };
        return Value::Json(normalize(&Value::Json(json!({
            "info": transaction.clone(),
            "id": self.safe_string_2(transaction.clone(), Value::from("order_id"), Value::from("task_id"), Value::Undefined),
            "txid": txid.clone(),
            "type": r#type.clone(),
            "currency": code.clone(),
            "network": self.safe_string(transaction.clone(), Value::from("provider"), Value::Undefined),
            "amount": self.parse_number(amount_string.clone(), Value::Undefined),
            "status": <Self as Exmo>::parse_transaction_status(self, self.safe_string_lower(transaction.clone(), Value::from("status"), Value::Undefined)),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "address": address.clone(),
            "addressFrom": Value::Undefined,
            "addressTo": address.clone(),
            "tag": Value::Undefined,
            "tagFrom": Value::Undefined,
            "tagTo": Value::Undefined,
            "updated": self.safe_timestamp(transaction.clone(), Value::from("updated"), Value::Undefined),
            "comment": comment.clone(),
            "internal": Value::Undefined,
            "fee": fee.clone()
        }))).unwrap());
    }

    async fn fetch_deposits_withdrawals(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::new_object();
        if since.clone().is_nonnullish() {
            request.set("date".into(), self.parse_to_int(since.clone() / Value::from(1000)));
        };
        let mut currency: Value = Value::Undefined;
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
        };
        let mut response: Value = self.dispatch("privatePostWalletHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //       "result": true,
        //       "error": "",
        //       "begin": "1493942400",
        //       "end": "1494028800",
        //       "history": [
        //          {
        //            "dt": 1461841192,
        //            "type": "deposit",
        //            "curr": "RUB",
        //            "status": "processing",
        //            "provider": "Qiwi (LA) [12345]",
        //            "amount": "1",
        //            "account": "",
        //            "txid": "ec46f784ad976fd7f7539089d1a129fe46...",
        //          },
        //          {
        //            "dt": 1463414785,
        //            "type": "withdrawal",
        //            "curr": "USD",
        //            "status": "paid",
        //            "provider": "EXCODE",
        //            "amount": "-1",
        //            "account": "EX-CODE_19371_USDda...",
        //            "txid": "",
        //          },
        //       ],
        //     }
        //
        return self.parse_transactions(response.get(Value::from("history")), currency.clone(), since.clone(), limit.clone(), Value::Undefined).await;
    }

    async fn fetch_withdrawals(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["withdrawals", "withdraw/history", "withdrawal_history", "capital/withdraw/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/withdraw/history"), ("private", "GET", "withdrawals"), ("private", "GET", "withdraw/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_withdrawal(&mut self, mut id: Value, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = Value::Undefined;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id.clone(),
            "type": Value::from("withdraw")
        }))).unwrap());
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
            request.set("currency".into(), currency.get(Value::from("id")));
        };
        let mut response: Value = self.dispatch("privatePostWalletOperations".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "items": [
        //         {
        //             "operation_id": 47412538520634344,
        //             "created": 1573760013,
        //             "updated": 1573760013,
        //             "type": "deposit",
        //             "currency": "DOGE",
        //             "status": "Paid",
        //             "amount": "300",
        //             "provider": "DOGE",
        //             "commission": "0",
        //             "account": "DOGE: DBVy8pF1f8yxaCVEHqHeR7kkcHecLQ8nRS",
        //             "order_id": 69670170,
        //             "extra": {
        //                 "txid": "f2b66259ae1580f371d38dd27e31a23fff8c04122b65ee3ab5a3f612d579c792",
        //                 "excode": "",
        //                 "invoice": ""
        //             },
        //             "error": ""
        //         },
        //     ],
        //         "count": 23
        //     }
        //
        let mut items: Value = self.safe_value(response.clone(), Value::from("items"), Value::new_array());
        let mut first: Value = self.safe_dict(items.clone(), Value::from(0), Value::new_object());
        return <Self as Exmo>::parse_transaction(self, first.clone(), currency.clone()).await;
    }

    async fn fetch_deposit(&mut self, mut id: Value, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = Value::Undefined;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "order_id": id.clone(),
            "type": Value::from("deposit")
        }))).unwrap());
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
            request.set("currency".into(), currency.get(Value::from("id")));
        };
        let mut response: Value = self.dispatch("privatePostWalletOperations".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "items": [
        //         {
        //             "operation_id": 47412538520634344,
        //             "created": 1573760013,
        //             "updated": 1573760013,
        //             "type": "deposit",
        //             "currency": "DOGE",
        //             "status": "Paid",
        //             "amount": "300",
        //             "provider": "DOGE",
        //             "commission": "0",
        //             "account": "DOGE: DBVy8pF1f8yxaCVEHqHeR7kkcHecLQ8nRS",
        //             "order_id": 69670170,
        //             "extra": {
        //                 "txid": "f2b66259ae1580f371d38dd27e31a23fff8c04122b65ee3ab5a3f612d579c792",
        //                 "excode": "",
        //                 "invoice": ""
        //             },
        //             "error": ""
        //         },
        //     ],
        //         "count": 23
        //     }
        //
        let mut items: Value = self.safe_value(response.clone(), Value::from("items"), Value::new_array());
        let mut first: Value = self.safe_dict(items.clone(), Value::from(0), Value::new_object());
        return <Self as Exmo>::parse_transaction(self, first.clone(), currency.clone()).await;
    }

    async fn fetch_deposits(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Exmo>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["deposits", "deposit/history", "deposit_history", "capital/deposit/hisrec"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/deposit/hisrec"), ("private", "GET", "deposits"), ("private", "GET", "deposit/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    

    fn nonce(&self) -> Value {
        return self.milliseconds();
    }

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_str() {
                    "webGetCtrlfeesandlimits" => self.request("ctrl/feesAndLimits".into(), "web".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "webGetEndocsfees" => self.request("en/docs/fees".into(), "web".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCurrency" => self.request("currency".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCurrencylistextended" => self.request("currency/list/extended".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOrderbook" => self.request("order_book".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetPairsettings" => self.request("pair_settings".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTicker" => self.request("ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTrades" => self.request("trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetCandleshistory" => self.request("candles_history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetRequiredamount" => self.request("required_amount".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetPaymentsproviderscryptolist" => self.request("payments/providers/crypto/list".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostUserinfo" => self.request("user_info".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdercreate" => self.request("order_create".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdercancel" => self.request("order_cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostStopmarketordercreate" => self.request("stop_market_order_create".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostStopmarketordercancel" => self.request("stop_market_order_cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostUseropenorders" => self.request("user_open_orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostUsertrades" => self.request("user_trades".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostUsercancelledorders" => self.request("user_cancelled_orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOrdertrades" => self.request("order_trades".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostDepositaddress" => self.request("deposit_address".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawcrypt" => self.request("withdraw_crypt".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawgettxid" => self.request("withdraw_get_txid".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostExcodecreate" => self.request("excode_create".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostExcodeload" => self.request("excode_load".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCodecheck" => self.request("code_check".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWallethistory" => self.request("wallet_history".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWalletoperations" => self.request("wallet_operations".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserordercreate" => self.request("margin/user/order/create".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserorderupdate" => self.request("margin/user/order/update".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserordercancel" => self.request("margin/user/order/cancel".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserpositionclose" => self.request("margin/user/position/close".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserpositionmarginadd" => self.request("margin/user/position/margin_add".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserpositionmarginremove" => self.request("margin/user/position/margin_remove".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMargincurrencylist" => self.request("margin/currency/list".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginpairlist" => self.request("margin/pair/list".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginsettings" => self.request("margin/settings".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginfundinglist" => self.request("margin/funding/list".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserinfo" => self.request("margin/user/info".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserorderlist" => self.request("margin/user/order/list".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserorderhistory" => self.request("margin/user/order/history".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserordertrades" => self.request("margin/user/order/trades".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserordermaxquantity" => self.request("margin/user/order/max_quantity".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserpositionlist" => self.request("margin/user/position/list".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserpositionmarginremoveinfo" => self.request("margin/user/position/margin_remove_info".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserpositionmarginaddinfo" => self.request("margin/user/position/margin_add_info".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserwalletlist" => self.request("margin/user/wallet/list".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginuserwallethistory" => self.request("margin/user/wallet/history".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginusertradelist" => self.request("margin/user/trade/list".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMargintrades" => self.request("margin/trades".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarginliquidationfeed" => self.request("margin/liquidation/feed".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct ExmoImpl(Value);
#[async_trait(?Send)]
impl Exchange for ExmoImpl {
    fn describe(&self) -> Value { Exmo::describe(self) }
    async fn fetch_ticker(&mut self, symbol: Value, params: Value) -> Value { Exmo::fetch_ticker(self, symbol, params).await }
    async fn fetch_tickers(&mut self, symbols: Value, params: Value) -> Value { Exmo::fetch_tickers(self, symbols, params).await }
    async fn fetch_order_book(&mut self, symbol: Value, limit: Value, params: Value) -> Value { Exmo::fetch_order_book(self, symbol, limit, params).await }
    async fn fetch_ohlcv(&mut self, symbol: Value, timeframe: Value, since: Value, limit: Value, params: Value) -> Value { Exmo::fetch_ohlcv(self, symbol, timeframe, since, limit, params).await }
    async fn fetch_trades(&mut self, symbol: Value, since: Value, limit: Value, params: Value) -> Value { Exmo::fetch_trades(self, symbol, since, limit, params).await }
    fn parse_trade(&self, trade: Value, market: Value) -> Value { Exmo::parse_trade(self, trade, market) }
    fn parse_order(&self, order: Value, market: Value) -> Value { Exmo::parse_order(self, order, market) }
    fn parse_ticker(&self, ticker: Value, market: Value) -> Value { Exmo::parse_ticker(self, ticker, market) }
    fn parse_balance(&self, response: Value) -> Value { Exmo::parse_balance(self, response) }
    fn parse_transaction(&self, transaction: Value, currency: Value) -> Value { Exmo::parse_transaction(self, transaction, currency) }
}
impl Exmo for ExmoImpl {}
impl ValueTrait for ExmoImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl ExmoImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = ExmoImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Exmo::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors
fn BadRequest(msg: Value) -> Value { msg }
fn InvalidOrder(msg: Value) -> Value { msg }
fn ExchangeError(msg: Value) -> Value { msg }
fn InsufficientFunds(msg: Value) -> Value { msg }
fn OrderNotFound(msg: Value) -> Value { msg }
fn AuthenticationError(msg: Value) -> Value { msg }
fn PermissionDenied(msg: Value) -> Value { msg }
fn ExchangeNotAvailable(msg: Value) -> Value { msg }
fn ArgumentsRequired(msg: Value) -> Value { msg }
fn RateLimitExceeded(msg: Value) -> Value { msg }
fn OrderNotFillable(msg: Value) -> Value { msg }
fn OrderImmediatelyFillable(msg: Value) -> Value { msg }
fn NotSupported(msg: Value) -> Value { msg }
fn DuplicateOrderId(msg: Value) -> Value { msg }

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait]
pub trait Blofin : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "blofin",
            "name": "BloFin",
            "countries": [
                "US"
            ],
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowMargin": false,
                "cancelAllOrders": false,
                "cancelOrder": true,
                "cancelOrders": true,
                "closeAllPositions": false,
                "closePosition": true,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": false,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrders": true,
                "createOrderWithTakeProfitAndStopLoss": true,
                "createPostOnlyOrder": false,
                "createReduceOnlyOrder": false,
                "createStopLimitOrder": false,
                "createStopLossOrder": true,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "createTakeProfitOrder": true,
                "createTriggerOrder": true,
                "editOrder": false,
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchCanceledOrders": false,
                "fetchClosedOrder": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": false,
                "fetchCurrenciesWs": "emulated",
                "fetchDeposit": false,
                "fetchDepositAddress": false,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": true,
                "fetchDepositsWithdrawals": false,
                "fetchDepositWithdrawFee": "emulated",
                "fetchDepositWithdrawFees": false,
                "fetchFundingHistory": true,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": true,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchL2OrderBook": true,
                "fetchL3OrderBook": false,
                "fetchLedger": true,
                "fetchLeverage": true,
                "fetchLeverages": true,
                "fetchLeverageTiers": false,
                "fetchMarginMode": true,
                "fetchMarginModes": false,
                "fetchMarketLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMySettlementHistory": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterest": false,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOrderBook": true,
                "fetchOrderBooks": false,
                "fetchOrders": false,
                "fetchOrderTrades": true,
                "fetchPosition": true,
                "fetchPositionMode": true,
                "fetchPositions": true,
                "fetchPositionsForSymbol": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchSettlementHistory": false,
                "fetchStatus": false,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTime": false,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": false,
                "fetchTradingLimits": false,
                "fetchTransactionFee": false,
                "fetchTransactionFees": false,
                "fetchTransactions": false,
                "fetchTransfer": false,
                "fetchTransfers": false,
                "fetchUnderlyingAssets": false,
                "fetchVolatilityHistory": false,
                "fetchWithdrawal": false,
                "fetchWithdrawals": true,
                "fetchWithdrawalWhitelist": false,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "setLeverage": true,
                "setMargin": false,
                "setMarginMode": true,
                "setPositionMode": true,
                "signIn": false,
                "transfer": true,
                "withdraw": false
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/518cdf80-f05d-4821-a3e3-d48ceb41d73b",
                "api": {
                    "rest": "https://openapi.blofin.com"
                },
                "www": "https://www.blofin.com",
                "doc": "https://blofin.com/docs",
                "test": {
                    "rest": "https://demo-trading-openapi.blofin.com"
                },
                "referral": {
                    "url": "https://blofin.com/register?referral_code=f79EsS",
                    "discount": 0.05
                }
            },
            "api": {
                "public": {
                    "get": {
                        "market/instruments": 1,
                        "market/tickers": 1,
                        "market/books": 1,
                        "market/trades": 1,
                        "market/candles": 1,
                        "market/mark-price": 1,
                        "market/funding-rate": 1,
                        "market/funding-rate-history": 1
                    }
                },
                "private": {
                    "get": {
                        "asset/balances": 1,
                        "trade/orders-pending": 1,
                        "trade/fills-history": 1,
                        "asset/deposit-history": 1,
                        "asset/withdrawal-history": 1,
                        "asset/bills": 1,
                        "account/balance": 1,
                        "account/positions": 1,
                        "account/leverage-info": 1,
                        "account/margin-mode": 1,
                        "account/position-mode": 1,
                        "account/batch-leverage-info": 1,
                        "trade/orders-tpsl-pending": 1,
                        "trade/orders-algo-pending": 1,
                        "trade/orders-history": 1,
                        "trade/orders-tpsl-history": 1,
                        "trade/orders-algo-history": 1,
                        "trade/order/price-range": 1,
                        "user/query-apikey": 1,
                        "affiliate/basic": 1,
                        "copytrading/instruments": 1,
                        "copytrading/account/balance": 1,
                        "copytrading/account/positions-by-order": 1,
                        "copytrading/account/positions-details-by-order": 1,
                        "copytrading/account/positions-by-contract": 1,
                        "copytrading/account/position-mode": 1,
                        "copytrading/account/leverage-info": 1,
                        "copytrading/trade/orders-pending": 1,
                        "copytrading/trade/pending-tpsl-by-contract": 1,
                        "copytrading/trade/position-history-by-order": 1,
                        "copytrading/trade/orders-history": 1,
                        "copytrading/trade/pending-tpsl-by-order": 1
                    },
                    "post": {
                        "account/set-margin-mode": 1,
                        "account/set-position-mode": 1,
                        "trade/order": 1,
                        "trade/order-algo": 1,
                        "trade/cancel-order": 1,
                        "trade/cancel-algo": 1,
                        "account/set-leverage": 1,
                        "trade/batch-orders": 1,
                        "trade/order-tpsl": 1,
                        "trade/cancel-batch-orders": 1,
                        "trade/cancel-tpsl": 1,
                        "trade/close-position": 1,
                        "asset/transfer": 1,
                        "copytrading/account/set-position-mode": 1,
                        "copytrading/account/set-leverage": 1,
                        "copytrading/trade/place-order": 1,
                        "copytrading/trade/cancel-order": 1,
                        "copytrading/trade/place-tpsl-by-contract": 1,
                        "copytrading/trade/cancel-tpsl-by-contract": 1,
                        "copytrading/trade/place-tpsl-by-order": 1,
                        "copytrading/trade/cancel-tpsl-by-order": 1,
                        "copytrading/trade/close-position-by-order": 1,
                        "copytrading/trade/close-position-by-contract": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": true,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "3m": "3m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1H",
                "2h": "2H",
                "4h": "4H",
                "6h": "6H",
                "8h": "8H",
                "12h": "12H",
                "1d": "1D",
                "3d": "3D",
                "1w": "1W",
                "1M": "1M"
            },
            "fees": {
                "trading": {},
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                },
                "swap": {
                    "taker": 0.0006,
                    "maker": 0.0002
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "hostname": "www.blofin.com",
            "features": {
                "default": {
                    "sandbox": false,
                    "createOrder": {
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "leverage": false,
                        "marketBuyRequiresPrice": false,
                        "marketBuyByCost": false,
                        "selfTradePrevention": false,
                        "trailing": false,
                        "iceberg": false
                    },
                    "createOrders": {
                        "max": 10
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "daysBack": 100000,
                        "daysBackCanceled": 1,
                        "untilDays": 100000,
                        "trigger": true,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1440
                    }
                },
                "spot": {
                    "extends": "default",
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "hedged": false
                    }
                },
                "forDerivatives": {
                    "extends": "default",
                    "createOrder": {
                        "marginMode": true,
                        "triggerPrice": false,
                        "triggerDirection": false,
                        "stopLossPrice": true,
                        "takeProfitPrice": true,
                        "attachedStopLossTakeProfit": {
                            "price": true
                        },
                        "hedged": true
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forDerivatives"
                    }
                },
                "future": {}
            },
            "options": {
                "brokerId": "ec6dd3a7dd982d0b",
                "accountsByType": {
                    "swap": "futures",
                    "funding": "funding",
                    "future": "futures",
                    "copy_trading": "copy_trading",
                    "earn": "earn",
                    "spot": "spot"
                },
                "accountsById": {
                    "funding": "funding",
                    "futures": "swap",
                    "copy_trading": "copy_trading",
                    "earn": "earn",
                    "spot": "spot"
                },
                "defaultNetwork": "ERC20",
                "defaultNetworks": {
                    "ETH": "ERC20",
                    "BTC": "BTC",
                    "USDT": "TRC20"
                },
                "networks": {
                    "BTC": "Bitcoin",
                    "BEP20": "BSC",
                    "ERC20": "ERC20",
                    "TRC20": "TRC20"
                },
                "fetchOpenInterestHistory": {
                    "timeframes": {
                        "5m": "5m",
                        "1h": "1H",
                        "8h": "8H",
                        "1d": "1D",
                        "5M": "5m",
                        "1H": "1H",
                        "8H": "8H",
                        "1D": "1D"
                    }
                },
                "fetchOHLCV": {
                    "timezone": "UTC"
                },
                "fetchPositions": {
                    "method": "privateGetAccountPositions"
                },
                "createOrder": "privatePostTradeOrder",
                "createMarketBuyOrderRequiresPrice": false,
                "fetchMarkets": [
                    "swap"
                ],
                "defaultType": "swap",
                "fetchLedger": {
                    "method": "privateGetAssetBills"
                },
                "fetchOpenOrders": {
                    "method": "privateGetTradeOrdersPending"
                },
                "cancelOrders": {
                    "method": "privatePostTradeCancelBatchOrders"
                },
                "fetchCanceledOrders": {
                    "method": "privateGetTradeOrdersHistory"
                },
                "fetchClosedOrders": {
                    "method": "privateGetTradeOrdersHistory"
                },
                "withdraw": {},
                "exchangeType": {
                    "spot": "SPOT",
                    "swap": "SWAP",
                    "SPOT": "SPOT",
                    "SWAP": "SWAP"
                }
            }
        }"###).unwrap())
    }

    

    fn parse_market(&self, mut market: Value) -> Value {
        let mut id: Value = self.safe_string(market.clone(), Value::from("instId"), Value::Undefined);
        let mut r#type: Value = self.safe_string_lower(market.clone(), Value::from("instType"), Value::Undefined);
        let mut spot: Value = (r#type.clone() == Value::from("spot")).into();
        let mut future: Value = (r#type.clone() == Value::from("future")).into();
        let mut swap: Value = (r#type.clone() == Value::from("swap")).into();
        let mut option: Value = (r#type.clone() == Value::from("option")).into();
        let mut contract: Value = (swap.is_truthy() || future.is_truthy()).into();
        let mut base_id: Value = self.safe_string(market.clone(), Value::from("baseCurrency"), Value::Undefined);
        let mut quote_id: Value = self.safe_string(market.clone(), Value::from("quoteCurrency"), Value::Undefined);
        let mut settle_id: Value = self.safe_string(market.clone(), Value::from("quoteCurrency"), Value::Undefined);
        let mut settle: Value = self.safe_currency_code(settle_id.clone(), Value::Undefined);
        let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        let mut symbol: Value = base.clone() + Value::from("/") + quote.clone();
        if swap.is_truthy() {
            symbol = symbol.clone() + Value::from(":") + settle.clone();
        };
        let mut expiry: Value = Value::Undefined;
        let mut strike_price: Value = Value::Undefined;
        let mut option_type: Value = Value::Undefined;
        let mut tick_size: Value = self.safe_string(market.clone(), Value::from("tickSize"), Value::Undefined);
        let mut fees: Value = self.safe_dict_2(self.get("fees".into()), r#type.clone(), Value::from("trading"), Value::new_object());
        let mut taker: Value = self.safe_number(fees.clone(), Value::from("taker"), Value::Undefined);
        let mut maker: Value = self.safe_number(fees.clone(), Value::from("maker"), Value::Undefined);
        let mut max_leverage: Value = self.safe_string(market.clone(), Value::from("maxLeverage"), Value::from("100"));
        max_leverage = Precise::string_max(max_leverage.clone(), Value::from("1"));
        let mut is_active: Value = (self.safe_string(market.clone(), Value::from("state"), Value::Undefined) == Value::from("live")).into();
        return self.safe_market_structure(Value::Json(normalize(&Value::Json(json!({
            "id": id,
            "symbol": symbol,
            "base": base,
            "quote": quote,
            "baseId": base_id,
            "quoteId": quote_id,
            "settle": settle,
            "settleId": settle_id,
            "type": r#type,
            "spot": spot,
            "option": option,
            "margin": spot.is_truthy() && Precise::string_gt(max_leverage.clone(), Value::from("1")),
            "swap": swap,
            "future": future,
            "active": is_active,
            "taker": taker,
            "maker": maker,
            "contract": contract,
            "linear": if contract.is_truthy() { (quote_id.clone() == settle_id.clone()).into() } else { Value::Undefined },
            "inverse": if contract.is_truthy() { (base_id.clone() == settle_id.clone()).into() } else { Value::Undefined },
            "contractSize": if contract.is_truthy() { self.safe_number(market.clone(), Value::from("contractValue"), Value::Undefined) } else { Value::Undefined },
            "expiry": expiry,
            "expiryDatetime": expiry,
            "strike": strike_price,
            "optionType": option_type,
            "created": self.safe_integer(market.clone(), Value::from("listTime"), Value::Undefined),
            "precision": Value::Json(normalize(&Value::Json(json!({
                "amount": self.safe_number(market.clone(), Value::from("lotSize"), Value::Undefined),
                "price": self.parse_number(tick_size.clone(), Value::Undefined)
            }))).unwrap()),
            "limits": Value::Json(normalize(&Value::Json(json!({
                "leverage": Value::Json(normalize(&Value::Json(json!({
                    "min": self.parse_number(Value::from("1"), Value::Undefined),
                    "max": self.parse_number(max_leverage.clone(), Value::Undefined)
                }))).unwrap()),
                "amount": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(market.clone(), Value::from("minSize"), Value::Undefined),
                    "max": Value::Undefined
                }))).unwrap()),
                "price": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "cost": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap())
            }))).unwrap()),
            "info": market
        }))).unwrap()));
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Blofin>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        // response similar for REST & WS
        //
        //     {
        //         instId: "ADA-USDT",
        //         ts: "1707736811486",
        //         last: "0.5315",
        //         lastSize: "4",
        //         askPrice: "0.5318",
        //         askSize: "248",
        //         bidPrice: "0.5315",
        //         bidSize: "63",
        //         open24h: "0.5555",
        //         high24h: "0.5563",
        //         low24h: "0.5315",
        //         volCurrency24h: "198560100",
        //         vol24h: "1985601",
        //     }
        //
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("ts"), Value::Undefined);
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("instId"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::from("-"), Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("last"), Value::Undefined);
        let mut open: Value = self.safe_string(ticker.clone(), Value::from("open24h"), Value::Undefined);
        let mut spot: Value = self.safe_bool(market.clone(), Value::from("spot"), false.into());
        let mut quote_volume: Value = if spot.is_truthy() { self.safe_string(ticker.clone(), Value::from("volCurrency24h"), Value::Undefined) } else { Value::Undefined };
        let mut base_volume: Value = self.safe_string(ticker.clone(), Value::from("vol24h"), Value::Undefined);
        let mut high: Value = self.safe_string(ticker.clone(), Value::from("high24h"), Value::Undefined);
        let mut low: Value = self.safe_string(ticker.clone(), Value::from("low24h"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "high": high,
            "low": low,
            "bid": self.safe_string(ticker.clone(), Value::from("bidPrice"), Value::Undefined),
            "bidVolume": self.safe_string(ticker.clone(), Value::from("bidSize"), Value::Undefined),
            "ask": self.safe_string(ticker.clone(), Value::from("askPrice"), Value::Undefined),
            "askVolume": self.safe_string(ticker.clone(), Value::from("askSize"), Value::Undefined),
            "vwap": Value::Undefined,
            "open": open,
            "close": last,
            "last": last,
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": base_volume,
            "quoteVolume": quote_volume,
            "indexPrice": self.safe_string(ticker.clone(), Value::from("indexPrice"), Value::Undefined),
            "markPrice": self.safe_string(ticker.clone(), Value::from("markPrice"), Value::Undefined),
            "info": ticker
        }))).unwrap()), market.clone());
    }

    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Blofin>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_mark_price(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("publicGetMarketMarkPrice".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut first: Value = self.safe_dict(data.clone(), Value::from(0), Value::new_object());
        return <Self as Blofin>::parse_ticker(self, first.clone(), market.clone());
    }

    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Blofin>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_trade(&mut self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetch trades (response similar for REST & WS)
        //
        //   {
        //       "tradeId": "3263934920",
        //       "instId": "LTC-USDT",
        //       "price": "67.87",
        //       "size": "1",
        //       "side": "buy",
        //       "ts": "1707232020854"
        //   }
        //
        // my trades
        //   {
        //       "instId": "LTC-USDT",
        //       "tradeId": "1440847",
        //       "orderId": "2075705202",
        //       "fillPrice": "67.850000000000000000",
        //       "fillSize": "1.000000000000000000",
        //       "fillPnl": "0.000000000000000000",
        //       "side": "buy",
        //       "positionSide": "net",
        //       "fee": "0.040710000000000000",
        //       "ts": "1707224678878",
        //       "brokerId": ""
        //   }
        //
        let mut id: Value = self.safe_string(trade.clone(), Value::from("tradeId"), Value::Undefined);
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("instId"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::from("-"), Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("ts"), Value::Undefined);
        let mut price: Value = self.safe_string_2(trade.clone(), Value::from("price"), Value::from("fillPrice"), Value::Undefined);
        let mut amount: Value = self.safe_string_2(trade.clone(), Value::from("size"), Value::from("fillSize"), Value::Undefined);
        let mut side: Value = self.safe_string(trade.clone(), Value::from("side"), Value::Undefined);
        let mut order_id: Value = self.safe_string(trade.clone(), Value::from("orderId"), Value::Undefined);
        let mut fee_cost: Value = self.safe_string(trade.clone(), Value::from("fee"), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        if fee_cost.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": fee_cost,
                "currency": market.get(Value::from("settle"))
            }))).unwrap());
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol,
            "id": id,
            "order": order_id,
            "type": Value::Undefined,
            "takerOrMaker": Value::Undefined,
            "side": side,
            "price": price,
            "amount": amount,
            "cost": Value::Undefined,
            "fee": fee
        }))).unwrap()), market.clone());
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        //
        //     [
        //         "1678928760000", // timestamp
        //         "24341.4", // open
        //         "24344", // high
        //         "24313.2", // low
        //         "24323", // close
        //         "628", // contract volume
        //         "2.5819", // base volume
        //         "62800", // quote volume
        //         "0" // candlestick state
        //     ]
        //
        return Value::Json(serde_json::Value::Array(vec![self.safe_integer(ohlcv.clone(), Value::from(0), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(1), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(2), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(3), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(4), Value::Undefined).into(), self.safe_number(ohlcv.clone(), Value::from(6), Value::Undefined).into()]));
    }

    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Blofin>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["klines", "candles", "ohlcv"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_funding_rate_history(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" fetchFundingRateHistory() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = false.into();
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchFundingRateHistory"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_deterministic(Value::from("fetchFundingRateHistory"), symbol.clone(), since.clone(), limit.clone(), Value::from("8h"), params.clone(), Value::from(100)).await;
        };
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id"))
        }))).unwrap());
        if since.clone().is_nonnullish() {
            request.set("before".into(), Math::max(since.clone() - Value::from(1), Value::from(0)));
        };
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        let mut until: Value = self.safe_integer(params.clone(), Value::from("until"), Value::Undefined);
        if until.clone().is_nonnullish() {
            request.set("after".into(), until.clone());
            params = self.omit(params.clone(), Value::from("until"));
        };
        let mut response: Value = self.dispatch("publicGetMarketFundingRateHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut rates: Value = Value::new_array();
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut i: usize = 0;
        while i < data.len() {
            let mut rate: Value = data.get(i.into());
            let mut timestamp: Value = self.safe_integer(rate.clone(), Value::from("fundingTime"), Value::Undefined);
            rates.push(Value::Json(normalize(&Value::Json(json!({
                "info": rate,
                "symbol": market.get(Value::from("symbol")),
                "fundingRate": self.safe_number(rate.clone(), Value::from("fundingRate"), Value::Undefined),
                "timestamp": timestamp,
                "datetime": self.iso8601(timestamp.clone())
            }))).unwrap()));
            i += 1;
        };
        let mut sorted: Value = self.sort_by(rates.clone(), Value::from("timestamp"), Value::Undefined, Value::Undefined);
        return self.filter_by_symbol_since_limit(sorted.clone(), market.get(Value::from("symbol")), since.clone(), limit.clone(), Value::Undefined);
    }

    fn parse_funding_rate(&self, mut contract: Value, mut market: Value) -> Value {
        //
        //    {
        //        "fundingRate": "0.00027815",
        //        "fundingTime": "1634256000000",
        //        "instId": "BTC-USD-SWAP",
        //    }
        //
        let mut market_id: Value = self.safe_string(contract.clone(), Value::from("instId"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut funding_time: Value = self.safe_integer(contract.clone(), Value::from("fundingTime"), Value::Undefined);
        // > The current interest is 0.
        return Value::Json(normalize(&Value::Json(json!({
            "info": contract,
            "symbol": symbol,
            "markPrice": Value::Undefined,
            "indexPrice": Value::Undefined,
            "interestRate": self.parse_number(Value::from("0"), Value::Undefined),
            "estimatedSettlePrice": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "fundingRate": self.safe_number(contract.clone(), Value::from("fundingRate"), Value::Undefined),
            "fundingTimestamp": funding_time,
            "fundingDatetime": self.iso8601(funding_time.clone()),
            "nextFundingRate": Value::Undefined,
            "nextFundingTimestamp": Value::Undefined,
            "nextFundingDatetime": Value::Undefined,
            "previousFundingRate": Value::Undefined,
            "previousFundingTimestamp": Value::Undefined,
            "previousFundingDatetime": Value::Undefined,
            "interval": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_funding_rate(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        if !market.get(Value::from("swap")).is_truthy() {
            panic!(r###"ExchangeError::new(self.get("id".into()) + Value::from(" fetchFundingRate() is only valid for swap markets"))"###);
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("publicGetMarketFundingRate".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //    {
        //        "code": "0",
        //        "data": [
        //            {
        //                "fundingRate": "0.00027815",
        //                "fundingTime": "1634256000000",
        //                "instId": "BTC-USD-SWAP",
        //            }
        //        ],
        //        "msg": ""
        //    }
        //
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut entry: Value = self.safe_dict(data.clone(), Value::from(0), Value::new_object());
        return <Self as Blofin>::parse_funding_rate(self, entry.clone(), market.clone());
    }

    fn parse_balance_by_type(&self, mut response: Value) -> Value {
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::Undefined);
        if data.clone().is_nonnullish() && Array::is_array(data.clone()).is_truthy() {
            return <Self as Blofin>::parse_funding_balance(self, response.clone());
        } else {
            return <Self as Blofin>::parse_balance(self, response.clone());
        };
        Value::Undefined
    }

    fn parse_balance(&self, mut response: Value) -> Value {
        //
        // "data" similar for REST & WS
        //
        // {
        //     "code": "0",
        //     "msg": "success",
        //     "data": {
        //         "ts": "1697021343571",
        //         "totalEquity": "10011254.077985990315787910",
        //         "isolatedEquity": "861.763132108800000000",
        //         "details": [
        //             {
        //                 "currency": "USDT",
        //                 "equity": "10014042.988958415234430699548",
        //                 "balance": "10013119.885958415234430699",
        //                 "ts": "1697021343571",
        //                 "isolatedEquity": "862.003200000000000000048",
        //                 "available": "9996399.4708691159703362725",
        //                 "availableEquity": "9996399.4708691159703362725",
        //                 "frozen": "15805.149672632597427761",
        //                 "orderFrozen": "14920.994472632597427761",
        //                 "equityUsd": "10011254.077985990315787910",
        //                 "isolatedUnrealizedPnl": "-22.151999999999999999952",
        //                 "bonus": "0" // present only in REST
        //                 "unrealizedPnl": "0" // present only in WS
        //             }
        //         ]
        //     }
        // }
        //
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response
        }))).unwrap());
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut timestamp: Value = self.safe_integer(data.clone(), Value::from("ts"), Value::Undefined);
        let mut details: Value = self.safe_list(data.clone(), Value::from("details"), Value::new_array());
        let mut i: usize = 0;
        while i < details.len() {
            let mut balance: Value = details.get(i.into());
            let mut currency_id: Value = self.safe_string(balance.clone(), Value::from("currency"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut account: Value = self.account();
            // it may be incorrect to use total, free and used for swap accounts
            let mut eq: Value = self.safe_string(balance.clone(), Value::from("equity"), Value::Undefined);
            let mut avail_eq: Value = self.safe_string(balance.clone(), Value::from("available"), Value::Undefined);
            if eq.clone().is_nullish() || avail_eq.clone().is_nullish() {
                account.set("free".into(), self.safe_string(balance.clone(), Value::from("availableEquity"), Value::Undefined));
                account.set("used".into(), self.safe_string(balance.clone(), Value::from("frozen"), Value::Undefined));
            } else {
                account.set("total".into(), eq.clone());
                account.set("free".into(), avail_eq.clone());
            };
            result.set(code.clone(), account.clone());
            i += 1;
        };
        result.set("timestamp".into(), timestamp.clone());
        result.set("datetime".into(), self.iso8601(timestamp.clone()));
        return self.safe_balance(result.clone());
    }

    fn parse_funding_balance(&self, mut response: Value) -> Value {
        //
        //  {
        //      "code": "0",
        //      "msg": "success",
        //      "data": [
        //          {
        //              "currency": "USDT",
        //              "balance": "10012514.919418081548717298",
        //              "available": "9872132.414278782284622898",
        //              "frozen": "138556.471805965930761067",
        //              "bonus": "0"
        //          }
        //      ]
        //  }
        //
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response
        }))).unwrap());
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut i: usize = 0;
        while i < data.len() {
            let mut balance: Value = data.get(i.into());
            let mut currency_id: Value = self.safe_string(balance.clone(), Value::from("currency"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut account: Value = self.account();
            // it may be incorrect to use total, free and used for swap accounts
            account.set("total".into(), self.safe_string(balance.clone(), Value::from("balance"), Value::Undefined));
            account.set("free".into(), self.safe_string(balance.clone(), Value::from("available"), Value::Undefined));
            account.set("used".into(), self.safe_string(balance.clone(), Value::from("frozen"), Value::Undefined));
            result.set(code.clone(), account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    fn parse_trading_fee(&self, mut fee: Value, mut market: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "info": fee,
            "symbol": self.safe_symbol(Value::Undefined, market.clone(), Value::Undefined, Value::Undefined),
            "maker": self.parse_number(Precise::string_neg(self.safe_string_2(fee.clone(), Value::from("maker"), Value::from("makerU"), Value::Undefined)), Value::Undefined),
            "taker": self.parse_number(Precise::string_neg(self.safe_string_2(fee.clone(), Value::from("taker"), Value::from("takerU"), Value::Undefined)), Value::Undefined),
            "percentage": Value::Undefined,
            "tierBased": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut account_type: Value = Value::Undefined;
        (account_type, params) = shift_2(self.handle_option_and_params_2(params.clone(), Value::from("fetchBalance"), Value::from("accountType"), Value::from("type"), Value::Undefined));
        let mut request: Value = Value::new_object();
        let mut response: Value = Value::Undefined;
        if account_type.clone().is_nonnullish() && account_type.clone() != Value::from("swap") {
            let mut options: Value = self.safe_dict(self.get("options".into()), Value::from("accountsByType"), Value::new_object());
            let mut parsed_account_type: Value = self.safe_string(options.clone(), account_type.clone(), account_type.clone());
            request.set("accountType".into(), parsed_account_type.clone());
            response = self.dispatch("privateGetAssetBalances".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        } else {
            response = self.dispatch("privateGetAccountBalance".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        };
        return <Self as Blofin>::parse_balance_by_type(self, response.clone());
    }

    fn create_order_request(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id")),
            "side": side,
            "orderType": r#type,
            "size": self.amount_to_precision(symbol.clone(), amount.clone()),
            "brokerId": self.safe_string(self.get("options".into()), Value::from("brokerId"), Value::from("ec6dd3a7dd982d0b"))
        }))).unwrap());
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("createOrder"), params.clone(), Value::from("cross")));
        request.set("marginMode".into(), margin_mode.clone());
        let mut trigger_price: Value = self.safe_string(params.clone(), Value::from("triggerPrice"), Value::Undefined);
        let mut time_in_force: Value = self.safe_string(params.clone(), Value::from("timeInForce"), Value::from("GTC"));
        let mut is_hedged: Value = self.safe_bool(params.clone(), Value::from("hedged"), false.into());
        if is_hedged.is_truthy() {
            request.set("positionSide".into(), if side.clone() == Value::from("buy") { Value::from("long") } else { Value::from("short") });
        };
        let mut is_market_order: Value = (r#type.clone() == Value::from("market")).into();
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("timeInForce").into()])));
        let mut ioc: Value = (time_in_force.clone() == Value::from("IOC") || r#type.clone() == Value::from("ioc")).into();
        let mut market_ioc: Value = (is_market_order.is_truthy() && ioc.is_truthy()).into();
        if is_market_order.is_truthy() || market_ioc.is_truthy() {
            request.set("orderType".into(), Value::from("market"));
        } else {
            let mut key: Value = if trigger_price.clone().is_nonnullish() { Value::from("orderPrice") } else { Value::from("price") };
            request.set(key.clone(), self.price_to_precision(symbol.clone(), price.clone()));
        };
        let mut post_only: Value = false.into();
        (post_only, params) = shift_2(self.handle_post_only(is_market_order.clone(), (r#type.clone() == Value::from("post_only")).into(), params.clone()));
        if post_only.is_truthy() {
            request.set("type".into(), Value::from("post_only"));
        };
        let mut stop_loss: Value = self.safe_dict(params.clone(), Value::from("stopLoss"), Value::Undefined);
        let mut take_profit: Value = self.safe_dict(params.clone(), Value::from("takeProfit"), Value::Undefined);
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stopLoss").into(), Value::from("takeProfit").into(), Value::from("hedged").into()])));
        let mut has_stop_loss: Value = (stop_loss.clone().is_nonnullish()).into();
        let mut has_take_profit: Value = (take_profit.clone().is_nonnullish()).into();
        if has_stop_loss.is_truthy() || has_take_profit.is_truthy() {
            if has_stop_loss.is_truthy() {
                let mut sl_trigger_price: Value = self.safe_string_2(stop_loss.clone(), Value::from("triggerPrice"), Value::from("stopPrice"), Value::Undefined);
                request.set("slTriggerPrice".into(), self.price_to_precision(symbol.clone(), sl_trigger_price.clone()));
                let mut sl_order_price: Value = self.safe_string(stop_loss.clone(), Value::from("price"), Value::from("-1"));
                request.set("slOrderPrice".into(), self.price_to_precision(symbol.clone(), sl_order_price.clone()));
            };
            if has_take_profit.is_truthy() {
                let mut tp_trigger_price: Value = self.safe_string_2(take_profit.clone(), Value::from("triggerPrice"), Value::from("stopPrice"), Value::Undefined);
                request.set("tpTriggerPrice".into(), self.price_to_precision(symbol.clone(), tp_trigger_price.clone()));
                let mut tp_price: Value = self.safe_string(take_profit.clone(), Value::from("price"), Value::from("-1"));
                request.set("tpOrderPrice".into(), self.price_to_precision(symbol.clone(), tp_price.clone()));
            };
        } else if trigger_price.clone().is_nonnullish() {
            request.set("orderType".into(), Value::from("trigger"));
            request.set("triggerPrice".into(), self.price_to_precision(symbol.clone(), trigger_price.clone()));
            if is_market_order.is_truthy() {
                request.set("orderPrice".into(), Value::from("-1"));
            };
        };
        return extend_2(request.clone(), params.clone());
    }

    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "canceled": "canceled",
            "order_failed": "canceled",
            "live": "open",
            "partially_filled": "open",
            "filled": "closed",
            "effective": "closed"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_order(&mut self, mut order: Value, mut market: Value) -> Value {
        //
        // response similar for REST & WS
        //
        // {
        //     "orderId": "2075628533",
        //     "clientOrderId": "",
        //     "instId": "LTC-USDT",
        //     "marginMode": "cross",
        //     "positionSide": "net",
        //     "side": "buy",
        //     "orderType": "market",
        //     "price": "0.000000000000000000",
        //     "size": "1.000000000000000000",
        //     "reduceOnly": "true",
        //     "leverage": "3",
        //     "state": "filled",
        //     "filledSize": "1.000000000000000000",
        //     "pnl": "-0.050000000000000000",
        //     "averagePrice": "68.110000000000000000",
        //     "fee": "0.040866000000000000",
        //     "createTime": "1706891359010",
        //     "updateTime": "1706891359098",
        //     "orderCategory": "normal",
        //     "tpTriggerPrice": null,
        //     "tpOrderPrice": null,
        //     "slTriggerPrice": null,
        //     "slOrderPrice": null,
        //     "cancelSource": "not_canceled",
        //     "cancelSourceReason": null,
        //     "brokerId": "ec6dd3a7dd982d0b"
        //     "filled_amount": "1.000000000000000000", // filledAmount in "ws" watchOrders
        //     "cancelSource": "", // only in WS
        //     "instType": "SWAP", // only in WS
        // }
        //
        let mut id: Value = self.safe_string_n(order.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("tpslId").into(), Value::from("orderId").into(), Value::from("algoId").into()])), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(order.clone(), Value::from("createTime"), Value::Undefined);
        let mut last_update_timestamp: Value = self.safe_integer(order.clone(), Value::from("updateTime"), Value::Undefined);
        let mut last_trade_timestamp: Value = self.safe_integer(order.clone(), Value::from("fillTime"), Value::Undefined);
        let mut side: Value = self.safe_string(order.clone(), Value::from("side"), Value::Undefined);
        let mut r#type: Value = self.safe_string(order.clone(), Value::from("orderType"), Value::Undefined);
        let mut post_only: Value = Value::Undefined;
        let mut time_in_force: Value = Value::Undefined;
        if r#type.clone() == Value::from("post_only") {
            post_only = true.into();
            r#type = Value::from("limit");
        } else if r#type.clone() == Value::from("fok") {
            time_in_force = Value::from("FOK");
            r#type = Value::from("limit");
        } else if r#type.clone() == Value::from("ioc") {
            time_in_force = Value::from("IOC");
            r#type = Value::from("limit");
        } else if r#type.clone() == Value::from("conditional") {
            r#type = Value::from("trigger");
        };
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("instId"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::from("-"), Value::Undefined);
        let mut filled: Value = self.safe_string(order.clone(), Value::from("filledSize"), Value::Undefined);
        let mut price: Value = self.safe_string_n(order.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("px").into(), Value::from("price").into(), Value::from("orderPrice").into()])), Value::Undefined);
        let mut average: Value = self.safe_string(order.clone(), Value::from("averagePrice"), Value::Undefined);
        let mut status: Value = <Self as Blofin>::parse_order_status(self, self.safe_string(order.clone(), Value::from("state"), Value::Undefined));
        let mut fee_cost_string: Value = self.safe_string(order.clone(), Value::from("fee"), Value::Undefined);
        let mut amount: Value = self.safe_string(order.clone(), Value::from("size"), Value::Undefined);
        let mut leverage: Value = self.safe_string(order.clone(), Value::from("leverage"), Value::from("1"));
        let mut contract_size: Value = self.safe_string(market.clone(), Value::from("contractSize"), Value::Undefined);
        let mut base_amount: Value = Precise::string_mul(contract_size.clone(), filled.clone());
        let mut cost: Value = Value::Undefined;
        if average.clone().is_nonnullish() {
            cost = Precise::string_mul(average.clone(), base_amount.clone());
            cost = Precise::string_div(cost.clone(), leverage.clone(), Value::Undefined);
        };
        // spot market buy: "sz" can refer either to base currency units or to quote currency units
        let mut fee: Value = Value::Undefined;
        if fee_cost_string.clone().is_nonnullish() {
            let mut fee_cost_signed: Value = Precise::string_abs(fee_cost_string.clone());
            let mut fee_currency_id: Value = self.safe_string(order.clone(), Value::from("feeCcy"), Value::from("USDT"));
            let mut fee_currency_code: Value = self.safe_currency_code(fee_currency_id.clone(), Value::Undefined);
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": self.parse_number(fee_cost_signed.clone(), Value::Undefined),
                "currency": fee_currency_code
            }))).unwrap());
        };
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("clientOrderId"), Value::Undefined);
        if client_order_id.clone().is_nonnullish() && client_order_id.len() < 1 {
            client_order_id = Value::Undefined;
        };
        // fix empty clientOrderId string
        let mut stop_loss_trigger_price: Value = self.safe_number(order.clone(), Value::from("slTriggerPrice"), Value::Undefined);
        let mut stop_loss_price: Value = self.safe_number(order.clone(), Value::from("slOrderPrice"), Value::Undefined);
        let mut take_profit_trigger_price: Value = self.safe_number(order.clone(), Value::from("tpTriggerPrice"), Value::Undefined);
        let mut take_profit_price: Value = self.safe_number(order.clone(), Value::from("tpOrderPrice"), Value::Undefined);
        let mut reduce_only_raw: Value = self.safe_string(order.clone(), Value::from("reduceOnly"), Value::Undefined);
        let mut reduce_only: Value = (reduce_only_raw.clone() == Value::from("true")).into();
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": order,
            "id": id,
            "clientOrderId": client_order_id,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": last_trade_timestamp,
            "lastUpdateTimestamp": last_update_timestamp,
            "symbol": symbol,
            "type": r#type,
            "timeInForce": time_in_force,
            "postOnly": post_only,
            "side": side,
            "price": price,
            "stopLossTriggerPrice": stop_loss_trigger_price,
            "takeProfitTriggerPrice": take_profit_trigger_price,
            "stopLossPrice": stop_loss_price,
            "takeProfitPrice": take_profit_price,
            "average": average,
            "cost": cost,
            "amount": amount,
            "filled": filled,
            "remaining": Value::Undefined,
            "status": status,
            "fee": fee,
            "trades": Value::Undefined,
            "reduceOnly": reduce_only
        }))).unwrap()), market.clone());
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut tpsl: Value = self.safe_bool(params.clone(), Value::from("tpsl"), false.into());
        params = self.omit(params.clone(), Value::from("tpsl"));
        let mut method: Value = Value::Undefined;
        (method, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("createOrder"), Value::from("method"), Value::from("privatePostTradeOrder")));
        let mut is_stop_loss_price_defined: Value = (self.safe_string(params.clone(), Value::from("stopLossPrice"), Value::Undefined).is_nonnullish()).into();
        let mut is_take_profit_price_defined: Value = (self.safe_string(params.clone(), Value::from("takeProfitPrice"), Value::Undefined).is_nonnullish()).into();
        let mut has_trigger_price: Value = (self.safe_string(params.clone(), Value::from("triggerPrice"), Value::Undefined).is_nonnullish()).into();
        let mut is_type2_order: Value = (is_stop_loss_price_defined.is_truthy() || is_take_profit_price_defined.is_truthy()).into();
        let mut response: Value = Value::Undefined;
        let mut reduce_only: Value = self.safe_bool(params.clone(), Value::from("reduceOnly"), Value::Undefined);
        if reduce_only.clone().is_nonnullish() {
            params.set("reduceOnly".into(), if reduce_only.is_truthy() { Value::from("true") } else { Value::from("false") });
        };
        let mut is_tpsl_order: Value = (tpsl.is_truthy() || method.clone() == Value::from("privatePostTradeOrderTpsl") || is_type2_order.is_truthy()).into();
        let mut is_trigger_order: Value = (has_trigger_price.is_truthy() || method.clone() == Value::from("privatePostTradeOrderAlgo")).into();
        if is_tpsl_order.is_truthy() {
            let mut tpsl_request: Value = <Self as Blofin>::create_tpsl_order_request(self, symbol.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), params.clone());
            response = self.dispatch("privatePostTradeOrderTpsl".into(), tpsl_request.clone(), Value::Undefined).await;
        } else if is_trigger_order.is_truthy() {
            let mut trigger_request: Value = <Self as Blofin>::create_order_request(self, symbol.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), params.clone());
            response = self.dispatch("privatePostTradeOrderAlgo".into(), trigger_request.clone(), Value::Undefined).await;
        } else {
            let mut request: Value = <Self as Blofin>::create_order_request(self, symbol.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), params.clone());
            response = self.dispatch("privatePostTradeOrder".into(), request.clone(), Value::Undefined).await;
        };
        if is_tpsl_order.is_truthy() || is_trigger_order.is_truthy() {
            let mut data_dict: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
            return <Self as Blofin>::parse_order(self, data_dict.clone(), market.clone());
        };
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut first: Value = self.safe_dict(data.clone(), Value::from(0), Value::Undefined);
        let mut order: Value = <Self as Blofin>::parse_order(self, first.clone(), market.clone());
        order.set("type".into(), r#type.clone());
        order.set("side".into(), side.clone());
        return order.clone();
    }

    fn create_tpsl_order_request(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut market: Value = self.market(symbol.clone());
        let mut position_side: Value = self.safe_string(params.clone(), Value::from("positionSide"), Value::from("net"));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id")),
            "side": side,
            "positionSide": position_side,
            "brokerId": self.safe_string(self.get("options".into()), Value::from("brokerId"), Value::from("ec6dd3a7dd982d0b"))
        }))).unwrap());
        if amount.clone().is_nonnullish() {
            request.set("size".into(), self.amount_to_precision(symbol.clone(), amount.clone()));
        };
        let mut margin_mode: Value = self.safe_string(params.clone(), Value::from("marginMode"), Value::from("cross"));
        // cross or isolated
        if margin_mode.clone() != Value::from("cross") && margin_mode.clone() != Value::from("isolated") {
            panic!(r###"BadRequest::new(self.get("id".into()) + Value::from(" createTpslOrder() requires a marginMode parameter that must be either cross or isolated"))"###);
        };
        let mut stop_loss_price: Value = self.safe_string(params.clone(), Value::from("stopLossPrice"), Value::Undefined);
        let mut take_profit_price: Value = self.safe_string(params.clone(), Value::from("takeProfitPrice"), Value::Undefined);
        if stop_loss_price.clone().is_nonnullish() {
            request.set("slTriggerPrice".into(), self.price_to_precision(symbol.clone(), stop_loss_price.clone()));
            if r#type.clone() == Value::from("market") {
                request.set("slOrderPrice".into(), Value::from("-1"));
            } else {
                request.set("slOrderPrice".into(), self.price_to_precision(symbol.clone(), price.clone()));
            };
        } else if take_profit_price.clone().is_nonnullish() {
            request.set("tpTriggerPrice".into(), self.price_to_precision(symbol.clone(), take_profit_price.clone()));
            if r#type.clone() == Value::from("market") {
                request.set("tpOrderPrice".into(), Value::from("-1"));
            } else {
                request.set("tpOrderPrice".into(), self.price_to_precision(symbol.clone(), price.clone()));
            };
        };
        request.set("marginMode".into(), margin_mode.clone());
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stopLossPrice").into(), Value::from("takeProfitPrice").into()])));
        return extend_2(request.clone(), params.clone());
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" cancelOrder() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id"))
        }))).unwrap());
        let mut is_trigger: Value = self.safe_bool_n(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("trigger").into()])), false.into());
        let mut is_tpsl: Value = self.safe_bool_2(params.clone(), Value::from("tpsl"), Value::from("TPSL"), false.into());
        let mut client_order_id: Value = self.safe_string(params.clone(), Value::from("clientOrderId"), Value::Undefined);
        if client_order_id.clone().is_nonnullish() {
            request.set("clientOrderId".into(), client_order_id.clone());
        } else {
            if !is_trigger.is_truthy() && !is_tpsl.is_truthy() {
                request.set("orderId".into(), id.to_string());
            } else if is_tpsl.is_truthy() {
                request.set("tpslId".into(), id.to_string());
            } else if is_trigger.is_truthy() {
                request.set("algoId".into(), id.to_string());
            };
        };
        let mut query: Value = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("orderId").into(), Value::from("clientOrderId").into(), Value::from("stop").into(), Value::from("trigger").into(), Value::from("tpsl").into()])));
        if is_tpsl.is_truthy() {
            let mut tpsl_response: Value = <Self as Blofin>::cancel_orders(self, Value::Json(serde_json::Value::Array(vec![id.clone().into()])), symbol.clone(), params.clone()).await;
            let mut first: Value = self.safe_dict(tpsl_response.clone(), Value::from(0), Value::Undefined);
            return first.clone();
        } else if is_trigger.is_truthy() {
            let mut trigger_response: Value = self.dispatch("privatePostTradeCancelAlgo".into(), extend_2(request.clone(), query.clone()), Value::Undefined).await;
            let mut trigger_data: Value = self.safe_dict(trigger_response.clone(), Value::from("data"), Value::Undefined);
            return <Self as Blofin>::parse_order(self, trigger_data.clone(), market.clone());
        };
        let mut response: Value = self.dispatch("privatePostTradeCancelOrder".into(), extend_2(request.clone(), query.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut order: Value = self.safe_dict(data.clone(), Value::from(0), Value::Undefined);
        return <Self as Blofin>::parse_order(self, order.clone(), market.clone());
    }

    async fn create_orders(&mut self, mut orders: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut orders_requests: Value = Value::new_array();
        let mut i: usize = 0;
        while i < orders.len() {
            let mut raw_order: Value = orders.get(i.into());
            let mut market_id: Value = self.safe_string(raw_order.clone(), Value::from("symbol"), Value::Undefined);
            let mut r#type: Value = self.safe_string(raw_order.clone(), Value::from("type"), Value::Undefined);
            let mut side: Value = self.safe_string(raw_order.clone(), Value::from("side"), Value::Undefined);
            let mut amount: Value = self.safe_value(raw_order.clone(), Value::from("amount"), Value::Undefined);
            let mut price: Value = self.safe_value(raw_order.clone(), Value::from("price"), Value::Undefined);
            let mut order_params: Value = self.safe_dict(raw_order.clone(), Value::from("params"), Value::new_object());
            let mut extended_params: Value = extend_2(order_params.clone(), params.clone());
            // the request does not accept extra params since it's a list, so we're extending each order with the common params
            let mut order_request: Value = <Self as Blofin>::create_order_request(self, market_id.clone(), r#type.clone(), side.clone(), amount.clone(), price.clone(), extended_params.clone());
            orders_requests.push(order_request.clone());
            i += 1;
        };
        let mut response: Value = self.dispatch("privatePostTradeBatchOrders".into(), orders_requests.clone(), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_orders(data.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
    }

    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = false.into();
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchOpenOrders"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_dynamic(Value::from("fetchOpenOrders"), symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Undefined, Value::Undefined).await;
        };
        let mut request: Value = Value::new_object();
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            request.set("instId".into(), market.get(Value::from("id")));
        };
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        // default 100, max 100
        let mut is_trigger: Value = self.safe_bool_n(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stop").into(), Value::from("trigger").into()])), false.into());
        let mut is_tp_sl: Value = self.safe_bool_2(params.clone(), Value::from("tpsl"), Value::from("TPSL"), false.into());
        let mut method: Value = Value::Undefined;
        (method, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchOpenOrders"), Value::from("method"), Value::from("privateGetTradeOrdersPending")));
        let mut query: Value = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("method").into(), Value::from("stop").into(), Value::from("trigger").into(), Value::from("tpsl").into(), Value::from("TPSL").into()])));
        let mut response: Value = Value::Undefined;
        if is_tp_sl.is_truthy() || method.clone() == Value::from("privateGetTradeOrdersTpslPending") {
            response = self.dispatch("privateGetTradeOrdersTpslPending".into(), extend_2(request.clone(), query.clone()), Value::Undefined).await;
        } else if is_trigger.is_truthy() || method.clone() == Value::from("privateGetTradeOrdersAlgoPending") {
            request.set("orderType".into(), Value::from("trigger"));
            response = self.dispatch("privateGetTradeOrdersAlgoPending".into(), extend_2(request.clone(), query.clone()), Value::Undefined).await;
        } else {
            response = self.dispatch("privateGetTradeOrdersPending".into(), extend_2(request.clone(), query.clone()), Value::Undefined).await;
        };
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_orders(data.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = false.into();
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchMyTrades"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_dynamic(Value::from("fetchMyTrades"), symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Undefined, Value::Undefined).await;
        };
        let mut request: Value = Value::new_object();
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            request.set("instId".into(), market.get(Value::from("id")));
        };
        (request, params) = shift_2(self.handle_until_option(Value::from("end"), request.clone(), params.clone(), Value::Undefined));
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        // default 100, max 100
        let mut response: Value = self.dispatch("privateGetTradeFillsHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_trades(data.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn fetch_deposits(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = false.into();
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchDeposits"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_dynamic(Value::from("fetchDeposits"), code.clone(), since.clone(), limit.clone(), params.clone(), Value::Undefined, Value::Undefined).await;
        };
        let mut request: Value = Value::new_object();
        let mut currency: Value = Value::Undefined;
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
            request.set("currency".into(), currency.get(Value::from("id")));
        };
        if since.clone().is_nonnullish() {
            request.set("before".into(), Math::max(since.clone() - Value::from(1), Value::from(0)));
        };
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        // default 100, max 100
        (request, params) = shift_2(self.handle_until_option(Value::from("after"), request.clone(), params.clone(), Value::Undefined));
        let mut response: Value = self.dispatch("privateGetAssetDepositHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_transactions(data.clone(), currency.clone(), since.clone(), limit.clone(), params.clone());
    }

    async fn fetch_withdrawals(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = false.into();
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchWithdrawals"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_dynamic(Value::from("fetchWithdrawals"), code.clone(), since.clone(), limit.clone(), params.clone(), Value::Undefined, Value::Undefined).await;
        };
        let mut request: Value = Value::new_object();
        let mut currency: Value = Value::Undefined;
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
            request.set("currency".into(), currency.get(Value::from("id")));
        };
        if since.clone().is_nonnullish() {
            request.set("before".into(), Math::max(since.clone() - Value::from(1), Value::from(0)));
        };
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        // default 100, max 100
        (request, params) = shift_2(self.handle_until_option(Value::from("after"), request.clone(), params.clone(), Value::Undefined));
        let mut response: Value = self.dispatch("privateGetAssetWithdrawalHistory".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_transactions(data.clone(), currency.clone(), since.clone(), limit.clone(), params.clone());
    }

    async fn fetch_ledger(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = false.into();
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchLedger"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_dynamic(Value::from("fetchLedger"), code.clone(), since.clone(), limit.clone(), params.clone(), Value::Undefined, Value::Undefined).await;
        };
        let mut request: Value = Value::new_object();
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        let mut currency: Value = Value::Undefined;
        if code.clone().is_nonnullish() {
            currency = self.currency(code.clone());
            request.set("currency".into(), currency.get(Value::from("id")));
        };
        (request, params) = shift_2(self.handle_until_option(Value::from("end"), request.clone(), params.clone(), Value::Undefined));
        let mut response: Value = Value::Undefined;
        response = self.dispatch("privateGetAssetBills".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_ledger(data.clone(), currency.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value) -> Value {
        //
        //
        // fetchDeposits
        //
        //     {
        //         "currency": "USDT",
        //         "chain": "TRC20",
        //         "address": "TGfJLtnsh3B9EqekFEBZ1nR14QanBUf5Bi",
        //         "txId": "892f4e0c32268b29b2e541ef30d32a30bbf10f902adcc4b1428319ed7c3758fd",
        //         "type": "0",
        //         "amount": "86.975843",
        //         "state": "1",
        //         "ts": "1703163304153",
        //         "tag": null,
        //         "confirm": "16",
        //         "depositId": "36c8e2a7ea184a219de72215a696acaf"
        //     }
        // fetchWithdrawals
        //    {
        //       "currency": "USDT",
        //        "chain": "TRC20",
        //        "address": "TYgB3sVXHPEDQUu288EG1uMFh9Pk2swLgW",
        //        "txId": "1fd5ac52df414d7ea66194cadd9a5b4d2422c2b9720037f66d98207f9858fd96",
        //        "type": "0",
        //        "amount": "9",
        //        "fee": "1",
        //        "feeCurrency": "USDT",
        //        "state": "3",
        //        "clientId": null,
        //        "ts": "1707217439351",
        //        "tag": null,
        //        "memo": null,
        //        "withdrawId": "e0768698cfdf4aee8e54654c3775914b"
        //    }
        //
        let mut r#type: Value = Value::Undefined;
        let mut id: Value = Value::Undefined;
        let mut withdrawal_id: Value = self.safe_string(transaction.clone(), Value::from("withdrawId"), Value::Undefined);
        let mut deposit_id: Value = self.safe_string(transaction.clone(), Value::from("depositId"), Value::Undefined);
        let mut address_to: Value = self.safe_string(transaction.clone(), Value::from("address"), Value::Undefined);
        let mut address: Value = address_to.clone();
        let mut tag_to: Value = self.safe_string(transaction.clone(), Value::from("tag"), Value::Undefined);
        if withdrawal_id.clone().is_nonnullish() {
            r#type = Value::from("withdrawal");
            id = withdrawal_id.clone();
        } else {
            id = deposit_id.clone();
            r#type = Value::from("deposit");
        };
        let mut currency_id: Value = self.safe_string(transaction.clone(), Value::from("currency"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
        let mut amount: Value = self.safe_number(transaction.clone(), Value::from("amount"), Value::Undefined);
        let mut status: Value = <Self as Blofin>::parse_transaction_status(self, self.safe_string(transaction.clone(), Value::from("state"), Value::Undefined));
        let mut txid: Value = self.safe_string(transaction.clone(), Value::from("txId"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(transaction.clone(), Value::from("ts"), Value::Undefined);
        let mut fee_currency_id: Value = self.safe_string(transaction.clone(), Value::from("feeCurrency"), Value::Undefined);
        let mut fee_code: Value = self.safe_currency_code(fee_currency_id.clone(), Value::Undefined);
        let mut fee_cost: Value = self.safe_number(transaction.clone(), Value::from("fee"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": transaction,
            "id": id,
            "currency": code,
            "amount": amount,
            "network": Value::Undefined,
            "addressFrom": Value::Undefined,
            "addressTo": address_to,
            "address": address,
            "tagFrom": Value::Undefined,
            "tagTo": tag_to,
            "tag": tag_to,
            "status": status,
            "type": r#type,
            "updated": Value::Undefined,
            "txid": txid,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "internal": Value::Undefined,
            "comment": Value::Undefined,
            "fee": Value::Json(normalize(&Value::Json(json!({
                "currency": fee_code,
                "cost": fee_cost
            }))).unwrap())
        }))).unwrap());
    }

    fn parse_transaction_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "0": "pending",
            "1": "ok",
            "2": "failed",
            "3": "pending"
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_ledger_entry_type(&self, mut r#type: Value) -> Value {
        let mut types: Value = Value::Json(normalize(&Value::Json(json!({
            "1": "transfer",
            "2": "trade",
            "3": "trade",
            "4": "rebate",
            "5": "trade",
            "6": "transfer",
            "7": "trade",
            "8": "fee",
            "9": "trade",
            "10": "trade",
            "11": "trade"
        }))).unwrap());
        // system token conversion
        return self.safe_string(types.clone(), r#type.clone(), r#type.clone());
    }

    fn parse_ledger_entry(&self, mut item: Value, mut currency: Value) -> Value {
        let mut currency_id: Value = self.safe_string(item.clone(), Value::from("currency"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), currency.clone());
        currency = self.safe_currency(currency_id.clone(), currency.clone());
        let mut timestamp: Value = self.safe_integer(item.clone(), Value::from("ts"), Value::Undefined);
        return self.safe_ledger_entry(Value::Json(normalize(&Value::Json(json!({
            "info": item,
            "id": self.safe_string(item.clone(), Value::from("transferId"), Value::Undefined),
            "direction": Value::Undefined,
            "account": Value::Undefined,
            "referenceId": self.safe_string(item.clone(), Value::from("clientId"), Value::Undefined),
            "referenceAccount": Value::Undefined,
            "type": <Self as Blofin>::parse_ledger_entry_type(self, self.safe_string(item.clone(), Value::from("type"), Value::Undefined)),
            "currency": code,
            "amount": self.safe_number(item.clone(), Value::from("amount"), Value::Undefined),
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "before": Value::Undefined,
            "after": Value::Undefined,
            "status": "ok",
            "fee": Value::Undefined
        }))).unwrap()), currency.clone());
    }

    /// Returns list of order ids
    ///
    /// @ignore
    ///
    /// # Arguments
    ///
    /// * `ids` {string[]|string} - order ids
    fn parse_ids(&self, mut ids: Value) -> Value {
        if ids.typeof_() == Value::from("string") {
            return ids.split(Value::from(","));
        } else {
            return ids.clone();
        };
        Value::Undefined
    }

    async fn cancel_orders(&mut self, mut ids: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        // TODO : the original endpoint signature differs, according to that you can skip individual symbol and assign ids in batch. At this moment, `params` is not being used too.
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" cancelOrders() requires a symbol argument"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::new_array();
        let mut options: Value = self.safe_dict(self.get("options".into()), Value::from("cancelOrders"), Value::new_object());
        let mut default_method: Value = self.safe_string(options.clone(), Value::from("method"), Value::from("privatePostTradeCancelBatchOrders"));
        let mut method: Value = self.safe_string(params.clone(), Value::from("method"), default_method.clone());
        let mut client_order_ids: Value = <Self as Blofin>::parse_ids(self, self.safe_value(params.clone(), Value::from("clientOrderId"), Value::Undefined));
        let mut tpsl_ids: Value = <Self as Blofin>::parse_ids(self, self.safe_value(params.clone(), Value::from("tpslId"), Value::Undefined));
        let mut trigger: Value = self.safe_bool_n(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stop").into(), Value::from("trigger").into(), Value::from("tpsl").into()])), Value::Undefined);
        if trigger.is_truthy() {
            method = Value::from("privatePostTradeCancelTpsl");
        };
        if client_order_ids.clone().is_nullish() {
            ids = <Self as Blofin>::parse_ids(self, ids.clone());
            if tpsl_ids.clone().is_nonnullish() {
                let mut i: usize = 0;
                while i < tpsl_ids.len() {
                    request.push(Value::Json(normalize(&Value::Json(json!({
                        "tpslId": tpsl_ids.get(i.into()),
                        "instId": market.get(Value::from("id"))
                    }))).unwrap()));
                    i += 1;
                };
            };
            let mut i: usize = 0;
            while i < ids.len() {
                if trigger.is_truthy() {
                    request.push(Value::Json(normalize(&Value::Json(json!({
                        "tpslId": ids.get(i.into()),
                        "instId": market.get(Value::from("id"))
                    }))).unwrap()));
                } else {
                    request.push(Value::Json(normalize(&Value::Json(json!({
                        "orderId": ids.get(i.into()),
                        "instId": market.get(Value::from("id"))
                    }))).unwrap()));
                };
                i += 1;
            };
        } else {
            let mut i: usize = 0;
            while i < client_order_ids.len() {
                request.push(Value::Json(normalize(&Value::Json(json!({
                    "instId": market.get(Value::from("id")),
                    "clientOrderId": client_order_ids.get(i.into())
                }))).unwrap()));
                i += 1;
            };
        };
        let mut response: Value = Value::Undefined;
        if method.clone() == Value::from("privatePostTradeCancelTpsl") {
            response = self.dispatch("privatePostTradeCancelTpsl".into(), request.clone(), Value::Undefined).await;
        } else {
            // * dont extend with params, otherwise ARRAY will be turned into OBJECT
            response = self.dispatch("privatePostTradeCancelBatchOrders".into(), request.clone(), Value::Undefined).await;
        };
        // * dont extend with params, otherwise ARRAY will be turned into OBJECT
        let mut orders_data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_orders(orders_data.clone(), market.clone(), Value::Undefined, Value::Undefined, params.clone());
    }

    async fn transfer(&mut self, mut code: Value, mut amount: Value, mut from_account: Value, mut to_account: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut accounts_by_type: Value = self.safe_dict(self.get("options".into()), Value::from("accountsByType"), Value::new_object());
        let mut from_id: Value = self.safe_string(accounts_by_type.clone(), from_account.clone(), from_account.clone());
        let mut to_id: Value = self.safe_string(accounts_by_type.clone(), to_account.clone(), to_account.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "currency": currency.get(Value::from("id")),
            "amount": self.currency_to_precision(code.clone(), amount.clone(), Value::Undefined),
            "fromAccount": from_id,
            "toAccount": to_id
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostAssetTransfer".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Blofin>::parse_transfer(self, data.clone(), currency.clone());
    }

    fn parse_transfer(&self, mut transfer: Value, mut currency: Value) -> Value {
        let mut id: Value = self.safe_string(transfer.clone(), Value::from("transferId"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": transfer,
            "id": id,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "currency": Value::Undefined,
            "amount": Value::Undefined,
            "fromAccount": Value::Undefined,
            "toAccount": Value::Undefined,
            "status": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_position(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetAccountPositions".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut position: Value = self.safe_dict(data.clone(), Value::from(0), Value::Undefined);
        if position.clone().is_nullish() {
            return Value::Undefined;
        };
        return <Self as Blofin>::parse_position(self, position.clone(), market.clone());
    }

    async fn fetch_positions(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut response: Value = self.dispatch("privateGetAccountPositions".into(), params.clone(), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = self.parse_positions(data.clone(), Value::Undefined, Value::Undefined);
        return self.filter_by_array_positions(result.clone(), Value::from("symbol"), symbols.clone(), false.into());
    }

    fn parse_position(&self, mut position: Value, mut market: Value) -> Value {
        //
        // response similar for REST & WS
        //
        //     {
        //         instType: 'SWAP',
        //         instId: 'LTC-USDT',
        //         marginMode: 'cross',
        //         positionId: '644159',
        //         positionSide: 'net',
        //         positions: '1',
        //         availablePositions: '1',
        //         averagePrice: '68.16',
        //         unrealizedPnl: '0.80631223',
        //         unrealizedPnlRatio: '0.03548909463028169',
        //         leverage: '3',
        //         liquidationPrice: '10.116655172370356435',
        //         markPrice: '68.96',
        //         initialMargin: '22.988770743333333333',
        //         margin: '', // this field might not exist in rest response
        //         marginRatio: '152.523509620342499273',
        //         maintenanceMargin: '0.34483156115',
        //         adl: '4',
        //         createTime: '1707235776528',
        //         updateTime: '1707235776528'
        //     }
        //
        let mut market_id: Value = self.safe_string(position.clone(), Value::from("instId"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut pos: Value = self.safe_string(position.clone(), Value::from("positions"), Value::Undefined);
        let mut contracts_abs: Value = Precise::string_abs(pos.clone());
        let mut side: Value = self.safe_string(position.clone(), Value::from("positionSide"), Value::Undefined);
        let mut hedged: Value = (side.clone() != Value::from("net")).into();
        let mut contracts: Value = self.parse_number(contracts_abs.clone(), Value::Undefined);
        if pos.clone().is_nonnullish() {
            if side.clone() == Value::from("net") {
                if Precise::string_gt(pos.clone(), Value::from("0")) {
                    side = Value::from("long");
                } else if Precise::string_lt(pos.clone(), Value::from("0")) {
                    side = Value::from("short");
                } else {
                    side = Value::Undefined;
                };
            };
        };
        let mut contract_size: Value = self.safe_number(market.clone(), Value::from("contractSize"), Value::Undefined);
        let mut contract_size_string: Value = self.number_to_string(contract_size.clone());
        let mut mark_price_string: Value = self.safe_string(position.clone(), Value::from("markPrice"), Value::Undefined);
        let mut notional_string: Value = self.safe_string(position.clone(), Value::from("notionalUsd"), Value::Undefined);
        if market.get(Value::from("inverse")).is_truthy() {
            notional_string = Precise::string_div(Precise::string_mul(contracts_abs.clone(), contract_size_string.clone()), mark_price_string.clone(), Value::Undefined);
        };
        let mut notional: Value = self.parse_number(notional_string.clone(), Value::Undefined);
        let mut margin_mode: Value = self.safe_string(position.clone(), Value::from("marginMode"), Value::Undefined);
        let mut initial_margin_string: Value = Value::Undefined;
        let mut entry_price_string: Value = self.safe_string(position.clone(), Value::from("averagePrice"), Value::Undefined);
        let mut unrealized_pnl_string: Value = self.safe_string(position.clone(), Value::from("unrealizedPnl"), Value::Undefined);
        let mut leverage_string: Value = self.safe_string(position.clone(), Value::from("leverage"), Value::Undefined);
        let mut initial_margin_percentage: Value = Value::Undefined;
        let mut collateral_string: Value = Value::Undefined;
        if margin_mode.clone() == Value::from("cross") {
            initial_margin_string = self.safe_string(position.clone(), Value::from("initialMargin"), Value::Undefined);
            collateral_string = Precise::string_add(initial_margin_string.clone(), unrealized_pnl_string.clone());
        } else if margin_mode.clone() == Value::from("isolated") {
            initial_margin_percentage = Precise::string_div(Value::from("1"), leverage_string.clone(), Value::Undefined);
            collateral_string = self.safe_string(position.clone(), Value::from("margin"), Value::Undefined);
        };
        let mut maintenance_margin_string: Value = self.safe_string(position.clone(), Value::from("maintenanceMargin"), Value::Undefined);
        let mut maintenance_margin: Value = self.parse_number(maintenance_margin_string.clone(), Value::Undefined);
        let mut maintenance_margin_percentage_string: Value = Precise::string_div(maintenance_margin_string.clone(), notional_string.clone(), Value::Undefined);
        if initial_margin_percentage.clone().is_nullish() {
            initial_margin_percentage = self.parse_number(Precise::string_div(initial_margin_string.clone(), notional_string.clone(), Value::from(4)), Value::Undefined);
        } else if initial_margin_string.clone().is_nullish() {
            initial_margin_string = Precise::string_mul(initial_margin_percentage.clone(), notional_string.clone());
        };
        let mut rounder: Value = Value::from("0.00005");
        // round to closest 0.01%
        let mut maintenance_margin_percentage: Value = self.parse_number(Precise::string_div(Precise::string_add(maintenance_margin_percentage_string.clone(), rounder.clone()), Value::from("1"), Value::from(4)), Value::Undefined);
        let mut liquidation_price: Value = self.safe_number(position.clone(), Value::from("liquidationPrice"), Value::Undefined);
        let mut percentage_string: Value = self.safe_string(position.clone(), Value::from("unrealizedPnlRatio"), Value::Undefined);
        let mut percentage: Value = self.parse_number(Precise::string_mul(percentage_string.clone(), Value::from("100")), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(position.clone(), Value::from("updateTime"), Value::Undefined);
        let mut margin_ratio: Value = self.parse_number(Precise::string_div(maintenance_margin_string.clone(), collateral_string.clone(), Value::from(4)), Value::Undefined);
        return self.safe_position(Value::Json(normalize(&Value::Json(json!({
            "info": position,
            "id": Value::Undefined,
            "symbol": symbol,
            "notional": notional,
            "marginMode": margin_mode,
            "liquidationPrice": liquidation_price,
            "entryPrice": self.parse_number(entry_price_string.clone(), Value::Undefined),
            "unrealizedPnl": self.parse_number(unrealized_pnl_string.clone(), Value::Undefined),
            "percentage": percentage,
            "contracts": contracts,
            "contractSize": contract_size,
            "markPrice": self.parse_number(mark_price_string.clone(), Value::Undefined),
            "lastPrice": Value::Undefined,
            "side": side,
            "hedged": hedged,
            "timestamp": timestamp,
            "datetime": self.iso8601(timestamp.clone()),
            "lastUpdateTimestamp": Value::Undefined,
            "maintenanceMargin": maintenance_margin,
            "maintenanceMarginPercentage": maintenance_margin_percentage,
            "collateral": self.parse_number(collateral_string.clone(), Value::Undefined),
            "initialMargin": self.parse_number(initial_margin_string.clone(), Value::Undefined),
            "initialMarginPercentage": self.parse_number(initial_margin_percentage.clone(), Value::Undefined),
            "leverage": self.parse_number(leverage_string.clone(), Value::Undefined),
            "marginRatio": margin_ratio,
            "stopLossPrice": Value::Undefined,
            "takeProfitPrice": Value::Undefined
        }))).unwrap()));
    }

    async fn fetch_leverages(&mut self, mut symbols: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        if symbols.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" fetchLeverages() requires a symbols argument"))"###);
        };
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("fetchLeverages"), params.clone(), Value::Undefined));
        if margin_mode.clone().is_nullish() {
            margin_mode = self.safe_string(params.clone(), Value::from("marginMode"), Value::from("cross"));
        };
        // cross as default marginMode
        if margin_mode.clone() != Value::from("cross") && margin_mode.clone() != Value::from("isolated") {
            panic!(r###"BadRequest::new(self.get("id".into()) + Value::from(" fetchLeverages() requires a marginMode parameter that must be either cross or isolated"))"###);
        };
        symbols = self.market_symbols(symbols.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined);
        let mut inst_ids: Value = Value::from("");
        let mut i: usize = 0;
        while i < symbols.len() {
            let mut entry: Value = symbols.get(i.into());
            let mut entry_market: Value = self.market(entry.clone());
            if i > 0 {
                inst_ids = inst_ids.clone() + Value::from(",") + entry_market.get(Value::from("id"));
            } else {
                inst_ids = inst_ids.clone() + entry_market.get(Value::from("id"));
            };
            i += 1;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": inst_ids,
            "marginMode": margin_mode
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetAccountBatchLeverageInfo".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "code": "0",
        //         "msg": "success",
        //         "data": [
        //             {
        //                 "leverage": "3",
        //                 "marginMode": "cross",
        //                 "instId": "BTC-USDT"
        //             },
        //         ]
        //     }
        //
        let mut leverages: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_leverages(leverages.clone(), symbols.clone(), Value::from("instId"), Value::Undefined);
    }

    async fn fetch_leverage(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("fetchLeverage"), params.clone(), Value::Undefined));
        if margin_mode.clone().is_nullish() {
            margin_mode = self.safe_string(params.clone(), Value::from("marginMode"), Value::from("cross"));
        };
        // cross as default marginMode
        if margin_mode.clone() != Value::from("cross") && margin_mode.clone() != Value::from("isolated") {
            panic!(r###"BadRequest::new(self.get("id".into()) + Value::from(" fetchLeverage() requires a marginMode parameter that must be either cross or isolated"))"###);
        };
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id")),
            "marginMode": margin_mode
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetAccountLeverageInfo".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "code": "0",
        //         "msg": "success",
        //         "data": {
        //             "leverage": "3",
        //             "marginMode": "cross",
        //             "instId": "BTC-USDT"
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Blofin>::parse_leverage(self, data.clone(), market.clone());
    }

    fn parse_leverage(&self, mut leverage: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(leverage.clone(), Value::from("instId"), Value::Undefined);
        let mut leverage_value: Value = self.safe_integer(leverage.clone(), Value::from("leverage"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": leverage,
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined),
            "marginMode": self.safe_string_lower(leverage.clone(), Value::from("marginMode"), Value::Undefined),
            "longLeverage": leverage_value,
            "shortLeverage": leverage_value
        }))).unwrap());
    }

    async fn set_leverage(&mut self, mut leverage: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            panic!(r###"ArgumentsRequired::new(self.get("id".into()) + Value::from(" setLeverage() requires a symbol argument"))"###);
        };
        // WARNING: THIS WILL INCREASE LIQUIDATION PRICE FOR OPEN ISOLATED LONG POSITIONS
        // AND DECREASE LIQUIDATION PRICE FOR OPEN ISOLATED SHORT POSITIONS
        if leverage.clone() < Value::from(1) || leverage.clone() > Value::from(125) {
            panic!(r###"BadRequest::new(self.get("id".into()) + Value::from(" setLeverage() leverage should be between 1 and 125"))"###);
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("setLeverage"), params.clone(), Value::from("cross")));
        if margin_mode.clone() != Value::from("cross") && margin_mode.clone() != Value::from("isolated") {
            panic!(r###"BadRequest::new(self.get("id".into()) + Value::from(" setLeverage() requires a marginMode parameter that must be either cross or isolated"))"###);
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "leverage": leverage,
            "marginMode": margin_mode,
            "instId": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostAccountSetLeverage".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return response.clone();
    }

    async fn close_position(&mut self, mut symbol: Value, mut side: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut client_order_id: Value = self.safe_string(params.clone(), Value::from("clientOrderId"), Value::Undefined);
        let mut margin_mode: Value = Value::Undefined;
        (margin_mode, params) = shift_2(self.handle_margin_mode_and_params(Value::from("closePosition"), params.clone(), Value::from("cross")));
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "instId": market.get(Value::from("id")),
            "marginMode": margin_mode
        }))).unwrap());
        if client_order_id.clone().is_nonnullish() {
            request.set("clientOrderId".into(), client_order_id.clone());
        };
        let mut response: Value = self.dispatch("privatePostTradeClosePosition".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return self.safe_dict(response.clone(), Value::from("data"), Value::Undefined);
    }

    async fn fetch_closed_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = false.into();
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchClosedOrders"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_dynamic(Value::from("fetchClosedOrders"), symbol.clone(), since.clone(), limit.clone(), params.clone(), Value::Undefined, Value::Undefined).await;
        };
        let mut request: Value = Value::new_object();
        let mut market: Value = Value::Undefined;
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            request.set("instId".into(), market.get(Value::from("id")));
        };
        if limit.clone().is_nonnullish() {
            request.set("limit".into(), limit.clone());
        };
        // default 100, max 100
        if since.clone().is_nonnullish() {
            request.set("begin".into(), since.clone());
        };
        let mut is_trigger: Value = self.safe_bool_n(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("stop").into(), Value::from("trigger").into(), Value::from("tpsl").into(), Value::from("TPSL").into()])), false.into());
        let mut method: Value = Value::Undefined;
        (method, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchOpenOrders"), Value::from("method"), Value::from("privateGetTradeOrdersHistory")));
        let mut query: Value = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![Value::from("method").into(), Value::from("stop").into(), Value::from("trigger").into(), Value::from("tpsl").into(), Value::from("TPSL").into()])));
        let mut response: Value = Value::Undefined;
        if is_trigger.is_truthy() || method.clone() == Value::from("privateGetTradeOrdersTpslHistory") {
            response = self.dispatch("privateGetTradeOrdersTpslHistory".into(), extend_2(request.clone(), query.clone()), Value::Undefined).await;
        } else {
            response = self.dispatch("privateGetTradeOrdersHistory".into(), extend_2(request.clone(), query.clone()), Value::Undefined).await;
        };
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_orders(data.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined);
    }

    async fn fetch_margin_mode(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut response: Value = self.dispatch("privateGetAccountMarginMode".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //         "code": "0",
        //         "msg": "success",
        //         "data": {
        //             "marginMode": "cross"
        //         }
        //     }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Blofin>::parse_margin_mode(self, data.clone(), market.clone());
    }

    fn parse_margin_mode(&self, mut margin_mode: Value, mut market: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "info": margin_mode,
            "symbol": self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined),
            "marginMode": self.safe_string(margin_mode.clone(), Value::from("marginMode"), Value::Undefined)
        }))).unwrap());
    }

    

    async fn fetch_position_mode(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.dispatch("privateGetAccountPositionMode".into(), params.clone(), Value::Undefined).await;
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        let mut position_mode: Value = self.safe_string(data.clone(), Value::from("positionMode"), Value::Undefined);
        //
        //     {
        //         "code": "0",
        //         "msg": "success",
        //         "data": {
        //             "positionMode": "long_short_mode"
        //         }
        //     }
        //
        return Value::Json(normalize(&Value::Json(json!({
            "info": data,
            "hedged": position_mode.clone() == Value::from("long_short_mode")
        }))).unwrap());
    }

    async fn set_position_mode(&mut self, mut hedged: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "positionMode": if hedged.is_truthy() { Value::from("long_short_mode") } else { Value::from("net_mode") }
        }))).unwrap());
        //
        //     {
        //         "code": "0",
        //         "msg": "success",
        //         "data": {
        //             "positionMode": "net_mode"
        //         }
        //     }
        //
        return self.dispatch("privatePostAccountSetPositionMode".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
    }

    

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_ref() {
                    "publicGetMarketinstruments" => self.request("market/instruments".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarkettickers" => self.request("market/tickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketbooks" => self.request("market/books".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarkettrades" => self.request("market/trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketcandles" => self.request("market/candles".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketmarkprice" => self.request("market/mark-price".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketfundingrate" => self.request("market/funding-rate".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetMarketfundingratehistory" => self.request("market/funding-rate-history".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetbalances" => self.request("asset/balances".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderspending" => self.request("trade/orders-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradefillshistory" => self.request("trade/fills-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetdeposithistory" => self.request("asset/deposit-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetwithdrawalhistory" => self.request("asset/withdrawal-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAssetbills" => self.request("asset/bills".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountbalance" => self.request("account/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountpositions" => self.request("account/positions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountleverageinfo" => self.request("account/leverage-info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountmarginmode" => self.request("account/margin-mode".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountpositionmode" => self.request("account/position-mode".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountbatchleverageinfo" => self.request("account/batch-leverage-info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderstpslpending" => self.request("trade/orders-tpsl-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeordersalgopending" => self.request("trade/orders-algo-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeordershistory" => self.request("trade/orders-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderstpslhistory" => self.request("trade/orders-tpsl-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeordersalgohistory" => self.request("trade/orders-algo-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTradeorderpricerange" => self.request("trade/order/price-range".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetUserqueryapikey" => self.request("user/query-apikey".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAffiliatebasic" => self.request("affiliate/basic".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradinginstruments" => self.request("copytrading/instruments".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountbalance" => self.request("copytrading/account/balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionsbyorder" => self.request("copytrading/account/positions-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionsdetailsbyorder" => self.request("copytrading/account/positions-details-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionsbycontract" => self.request("copytrading/account/positions-by-contract".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountpositionmode" => self.request("copytrading/account/position-mode".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingaccountleverageinfo" => self.request("copytrading/account/leverage-info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradeorderspending" => self.request("copytrading/trade/orders-pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradependingtpslbycontract" => self.request("copytrading/trade/pending-tpsl-by-contract".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradepositionhistorybyorder" => self.request("copytrading/trade/position-history-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradeordershistory" => self.request("copytrading/trade/orders-history".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetCopytradingtradependingtpslbyorder" => self.request("copytrading/trade/pending-tpsl-by-order".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountsetmarginmode" => self.request("account/set-margin-mode".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountsetpositionmode" => self.request("account/set-position-mode".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeorder" => self.request("trade/order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeorderalgo" => self.request("trade/order-algo".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecancelorder" => self.request("trade/cancel-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecancelalgo" => self.request("trade/cancel-algo".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccountsetleverage" => self.request("account/set-leverage".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradebatchorders" => self.request("trade/batch-orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradeordertpsl" => self.request("trade/order-tpsl".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecancelbatchorders" => self.request("trade/cancel-batch-orders".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecanceltpsl" => self.request("trade/cancel-tpsl".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostTradecloseposition" => self.request("trade/close-position".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAssettransfer" => self.request("asset/transfer".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingaccountsetpositionmode" => self.request("copytrading/account/set-position-mode".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingaccountsetleverage" => self.request("copytrading/account/set-leverage".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeplaceorder" => self.request("copytrading/trade/place-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradecancelorder" => self.request("copytrading/trade/cancel-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeplacetpslbycontract" => self.request("copytrading/trade/place-tpsl-by-contract".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradecanceltpslbycontract" => self.request("copytrading/trade/cancel-tpsl-by-contract".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeplacetpslbyorder" => self.request("copytrading/trade/place-tpsl-by-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradecanceltpslbyorder" => self.request("copytrading/trade/cancel-tpsl-by-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeclosepositionbyorder" => self.request("copytrading/trade/close-position-by-order".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostCopytradingtradeclosepositionbycontract" => self.request("copytrading/trade/close-position-by-contract".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct BlofinImpl(Value);
impl Exchange for BlofinImpl {}
impl Blofin for BlofinImpl {}
impl ValueTrait for BlofinImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl BlofinImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = BlofinImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Blofin::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


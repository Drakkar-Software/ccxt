#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
use crate::ws::{ArrayCache, ArrayCacheByTimestamp, ArrayCacheBySymbolById, ArrayCacheBySymbolBySide};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors (stub structs with ::new for transpiled `new ErrorType(msg)` patterns)
macro_rules! error_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(msg: Value) -> Value { msg } } }; }
error_stub!(BadRequest);
error_stub!(InvalidOrder);
error_stub!(ExchangeError);
error_stub!(InsufficientFunds);
error_stub!(OrderNotFound);
error_stub!(AuthenticationError);
error_stub!(PermissionDenied);
error_stub!(ExchangeNotAvailable);
error_stub!(ArgumentsRequired);
error_stub!(RateLimitExceeded);
error_stub!(OrderNotFillable);
error_stub!(OrderImmediatelyFillable);
error_stub!(NotSupported);
error_stub!(DuplicateOrderId);
error_stub!(UnsubscribeError);
error_stub!(ChecksumError);
error_stub!(InvalidNonce);
error_stub!(BadSymbol);
// Array-like type stubs (Bids/Asks order book sides)
macro_rules! array_side_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(data: Value, _limit: Value) -> Value { data } } }; }
array_side_stub!(Bids);
array_side_stub!(Asks);

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait(?Send)]
pub trait Foxbit : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "foxbit",
            "name": "Foxbit",
            "countries": [
                "pt-BR"
            ],
            "enableRateLimit": true,
            "rateLimit": 33.334,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "CORS": true,
                "sandbox": false,
                "spot": true,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "createLimitBuyOrder": true,
                "createLimitOrder": true,
                "createLimitSellOrder": true,
                "createMarketBuyOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createMarketSellOrder": true,
                "createOrder": true,
                "editOrder": "emulated",
                "fetchBalance": true,
                "fetchCanceledOrders": true,
                "fetchClosedOrders": true,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchDeposits": true,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchMarkets": true,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchTrades": true,
                "fetchTradingFee": true,
                "fetchTradingFees": true,
                "fetchTransactions": true,
                "fetchWithdrawals": true,
                "withdraw": true,
                "ws": false,
                "fecthOrderBook": true,
                "loadMarkets": true
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/1f8faca2-ae2f-4222-b33e-5671e7d873dd",
                "api": {
                    "public": "https://api.foxbit.com.br",
                    "private": "https://api.foxbit.com.br",
                    "status": "https://metadata-v2.foxbit.com.br/api"
                },
                "www": "https://app.foxbit.com.br",
                "doc": [
                    "https://docs.foxbit.com.br"
                ]
            },
            "api": {
                "v3": {
                    "public": {
                        "get": {
                            "currencies": 5,
                            "markets": 5,
                            "markets/ticker/24hr": 60,
                            "markets/{market}/orderbook": 6,
                            "markets/{market}/candlesticks": 12,
                            "markets/{market}/trades/history": 12,
                            "markets/{market}/ticker/24hr": 15
                        }
                    },
                    "private": {
                        "get": {
                            "accounts": 2,
                            "accounts/{symbol}/transactions": 60,
                            "orders": 2,
                            "orders/by-order-id/{id}": 2,
                            "trades": 6,
                            "deposits/address": 10,
                            "deposits": 10,
                            "withdrawals": 10,
                            "me/fees/trading": 60
                        },
                        "post": {
                            "orders": 2,
                            "orders/batch": 7.5,
                            "orders/cancel-replace": 3,
                            "withdrawals": 10
                        },
                        "put": {
                            "orders/cancel": 2
                        }
                    }
                },
                "status": {
                    "public": {
                        "get": {
                            "status": 30
                        }
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1h",
                "2h": "2h",
                "4h": "4h",
                "6h": "6h",
                "12h": "12h",
                "1d": "1d",
                "1w": "1w",
                "2w": "2w",
                "1M": "1M"
            },
            "fees": {
                "trading": {
                    "tierBased": false,
                    "percentage": true,
                    "taker": 0.005,
                    "maker": 0.0025,
                    "feeSide": "get"
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 2,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "1",
            "comment": "Foxbit Exchange",
            "options": {
                "sandboxMode": false,
                "networksById": {
                    "algorand": "ALGO",
                    "arbitrum": "ARBITRUM",
                    "avalanchecchain": "AVAX",
                    "bitcoin": "BTC",
                    "bitcoincash": "BCH",
                    "bsc": "BEP20",
                    "cardano": "ADA",
                    "cosmos": "ATOM",
                    "dogecoin": "DOGE",
                    "erc20": "ETH",
                    "hedera": "HBAR",
                    "litecoin": "LTC",
                    "near": "NEAR",
                    "optimism": "OPTIMISM",
                    "polkadot": "DOT",
                    "polygon": "MATIC",
                    "ripple": "XRP",
                    "solana": "SOL",
                    "stacks": "STX",
                    "stellar": "XLM",
                    "tezos": "XTZ",
                    "trc20": "TRC20"
                },
                "networks": {
                    "ALGO": "algorand",
                    "ARBITRUM": "arbitrum",
                    "AVAX": "avalanchecchain",
                    "BTC": "bitcoin",
                    "BCH": "bitcoincash",
                    "BEP20": "bsc",
                    "ADA": "cardano",
                    "ATOM": "cosmos",
                    "DOGE": "dogecoin",
                    "ETH": "erc20",
                    "HBAR": "hedera",
                    "LTC": "litecoin",
                    "NEAR": "near",
                    "OPTIMISM": "optimism",
                    "DOT": "polkadot",
                    "MATIC": "polygon",
                    "XRP": "ripple",
                    "SOL": "solana",
                    "STX": "stacks",
                    "XLM": "stellar",
                    "XTZ": "tezos",
                    "TRC20": "trc20"
                }
            },
            "features": {
                "spot": {
                    "sandbox": false,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerPriceType": {
                            "last": true,
                            "mark": false,
                            "index": false
                        },
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "GTC": true,
                            "FOK": true,
                            "IOC": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "leverage": false,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": {
                            "expire_maker": true,
                            "expire_taker": true,
                            "expire_both": true,
                            "none": true
                        },
                        "trailing": false,
                        "icebergAmount": false
                    },
                    "createOrders": {
                        "max": 5
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 90,
                        "untilDays": 10000,
                        "symbolRequired": true
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "limit": 1,
                        "daysBack": 90,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 100,
                        "daysBack": 90,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": true,
                        "limit": 100,
                        "daysBack": 90,
                        "untilDays": 10000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": true,
                        "limit": 100,
                        "daysBack": 90,
                        "daysBackCanceled": 90,
                        "untilDays": 10000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 500
                    }
                }
            }
        }"###).unwrap())
    }

    async fn fetch_currencies(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.dispatch("v3PublicGetCurrencies".into(), params.clone(), Value::Undefined).await;
        // {
        //   "data": [
        //     {
        //       "symbol": "btc",
        //       "name": "Bitcoin",
        //       "type": "CRYPTO",
        //       "precision": 8,
        //       "deposit_info": {
        //         "min_to_confirm": "1",
        //         "min_amount": "0.0001"
        //       },
        //       "withdraw_info": {
        //         "enabled": true,
        //         "min_amount": "0.0001",
        //         "fee": "0.0001"
        //       },
        //       "category": {
        //           "code": "cripto",
        //         "name": "Cripto"
        //       },
        //       "networks": [
        //           {
        //               "name": "Bitcoin",
        //               "code": "btc",
        //               "deposit_info": {
        //                  status: "ENABLED",
        //               },
        //               "withdraw_info": {
        //                  "status": "ENABLED",
        //                  "fee": "0.0001",
        //               },
        //               "has_destination_tag": false
        //           }
        //       ]
        //     }
        //   ]
        // }
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < data.len() {
            let mut currency: Value = data.get(i.into());
            let mut precision: Value = self.safe_integer(currency.clone(), Value::from("precision"), Value::Undefined);
            let mut currency_id: Value = self.safe_string(currency.clone(), Value::from("symbol"), Value::Undefined);
            let mut name: Value = self.safe_string(currency.clone(), Value::from("name"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut deposit_info: Value = self.safe_dict(currency.clone(), Value::from("deposit_info"), Value::Undefined);
            let mut withdraw_info: Value = self.safe_dict(currency.clone(), Value::from("withdraw_info"), Value::Undefined);
            let mut networks: Value = self.safe_list(currency.clone(), Value::from("networks"), Value::new_array());
            let mut r#type: Value = self.safe_string_lower(currency.clone(), Value::from("type"), Value::Undefined);
            let mut parsed_networks: Value = Value::new_object();
            let mut j: usize = 0;
            while j < networks.len() {
                let mut network: Value = networks.get(j.into());
                let mut network_id: Value = self.safe_string(network.clone(), Value::from("code"), Value::Undefined);
                let mut network_code: Value = self.network_id_to_code(network_id.clone(), code.clone());
                let mut network_withdraw_info: Value = self.safe_dict(network.clone(), Value::from("withdraw_info"), Value::Undefined);
                let mut network_deposit_info: Value = self.safe_dict(network.clone(), Value::from("deposit_info"), Value::Undefined);
                let mut is_withdraw_enabled: Value = Value::from(self.safe_string(network_withdraw_info.clone(), Value::from("status"), Value::Undefined) == Value::from("ENABLED"));
                let mut is_deposit_enabled: Value = Value::from(self.safe_string(network_deposit_info.clone(), Value::from("status"), Value::Undefined) == Value::from("ENABLED"));
                parsed_networks.set(network_code.clone(), Value::Json(normalize(&Value::Json(json!({
                    "info": currency.clone(),
                    "id": network_id.clone(),
                    "network": network_code.clone(),
                    "name": self.safe_string(network.clone(), Value::from("name"), Value::Undefined),
                    "deposit": is_deposit_enabled.clone(),
                    "withdraw": is_withdraw_enabled.clone(),
                    "active": Value::from(true),
                    "precision": precision.clone(),
                    "fee": self.safe_number(network_withdraw_info.clone(), Value::from("fee"), Value::Undefined),
                    "limits": Value::Json(normalize(&Value::Json(json!({
                        "amount": Value::Json(normalize(&Value::Json(json!({
                            "min": Value::Undefined,
                            "max": Value::Undefined
                        }))).unwrap()),
                        "deposit": Value::Json(normalize(&Value::Json(json!({
                            "min": self.safe_number(deposit_info.clone(), Value::from("min_amount"), Value::Undefined),
                            "max": Value::Undefined
                        }))).unwrap()),
                        "withdraw": Value::Json(normalize(&Value::Json(json!({
                            "min": self.safe_number(withdraw_info.clone(), Value::from("min_amount"), Value::Undefined),
                            "max": Value::Undefined
                        }))).unwrap())
                    }))).unwrap())
                }))).unwrap()));
                j += 1;
            };
            if self.safe_dict(result.clone(), code.clone(), Value::Undefined).is_nullish() {
                result.set(code.clone(), self.safe_currency_structure(Value::Json(normalize(&Value::Json(json!({
                    "id": currency_id.clone(),
                    "code": code.clone(),
                    "info": currency.clone(),
                    "name": name.clone(),
                    "active": Value::from(true),
                    "type": r#type.clone(),
                    "deposit": self.safe_bool(deposit_info.clone(), Value::from("enabled"), Value::from(false)),
                    "withdraw": self.safe_bool(withdraw_info.clone(), Value::from("enabled"), Value::from(false)),
                    "fee": self.safe_number(withdraw_info.clone(), Value::from("fee"), Value::Undefined),
                    "precision": precision.clone(),
                    "limits": Value::Json(normalize(&Value::Json(json!({
                        "amount": Value::Json(normalize(&Value::Json(json!({
                            "min": Value::Undefined,
                            "max": Value::Undefined
                        }))).unwrap()),
                        "deposit": Value::Json(normalize(&Value::Json(json!({
                            "min": self.safe_number(deposit_info.clone(), Value::from("min_amount"), Value::Undefined),
                            "max": Value::Undefined
                        }))).unwrap()),
                        "withdraw": Value::Json(normalize(&Value::Json(json!({
                            "min": self.safe_number(withdraw_info.clone(), Value::from("min_amount"), Value::Undefined),
                            "max": Value::Undefined
                        }))).unwrap())
                    }))).unwrap()),
                    "networks": parsed_networks.clone()
                }))).unwrap())));
            };
            i += 1;
        };
        return result.clone();
    }

    

    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        // Some exchanges wrap the ticker in {data: {...}} or {result: {...}} — unwrap if so.
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut tk = json_resp.clone();
        if !tk.is_array() && tk.get("last").is_none() && tk.get("close").is_none() && tk.get("bid").is_none() {
            for key in &["data", "result", "ticker"] {
                if let Some(v) = json_resp.get(key) {
                    if v.is_object() { tk = v.clone(); break; }
                    if let Some(arr) = v.as_array() {
                        if let Some(first) = arr.first() { tk = first.clone(); break; }
                    }
                }
            }
        }
        let market: Value = Value::Undefined;
        let parsed = <Self as Exchange>::parse_ticker(self, Value::Json(tk.clone()), market);
        if parsed.is_undefined() { Value::Json(tk) } else { parsed }
    }


    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_trading_fees(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("v3PrivateGetMeFeesTrading".into(), params.clone(), Value::Undefined).await;
        // [
        //     {
        //         "market_symbol": "btcbrl",
        //         "maker": "0.0025",
        //         "taker": "0.005"
        //     }
        // ]
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < data.len() {
            let mut entry: Value = data.get(i.into());
            let mut market_id: Value = self.safe_string(entry.clone(), Value::from("market_symbol"), Value::Undefined);
            let mut market: Value = self.safe_market(market_id.clone(), Value::Undefined, Value::Undefined, Value::Undefined);
            let mut symbol: Value = market.get(Value::from("symbol"));
            result.set(symbol.clone(), <Self as Foxbit>::parse_trading_fee(self, entry.clone(), market.clone()));
            i += 1;
        };
        return result.clone();
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut ob = crate::exchange::unwrap_response_order_book(&json_resp);
        if let serde_json::Value::Object(ref mut map) = ob {
            if !map.contains_key("symbol") { map.insert("symbol".to_string(), serde_json::json!(symbol.unwrap_str())); }
        }
        Value::Json(ob)
    }


    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        let mut raw: Value = Value::Undefined;
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { raw = rv; break; }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            // Already-parsed trades have both `price` and `amount` keys.
            if item.get("price").is_some() && item.get("amount").is_some() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_trade(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        // Match endpoints by substring on common OHLCV path tokens (klines, candles,
        // ohlcv, barhist, history, kline, candle, ohlc).
        'outer: for token in ["klines", "candles", "ohlcv", "barhist", "kline", "candle", "ohlc"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            if item.is_array() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_ohlcv(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["account/balance", "balance", "wallet/balance", "accounts/balance", "account"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(&format!("/{}/", token)) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "balance"), ("private", "GET", "account/balance"), ("private", "GET", "wallet/balance")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["openorders", "open_orders", "orders/open", "orders/active"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "openOrders"), ("private", "GET", "orders/open"), ("private", "GET", "orders/active")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_closed_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["closedorders", "closed_orders", "orders/closed", "orders/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "closedOrders"), ("private", "GET", "orders/closed"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_canceled_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        return <Self as Foxbit>::fetch_orders_by_status(self, Value::from("CANCELED"), symbol.clone(), since.clone(), limit.clone(), params.clone()).await;
    }

    async fn fetch_orders_by_status(&mut self, mut status: Value, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = Value::Undefined;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "state": status.clone()
        }))).unwrap());
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            request.set("market_symbol".into(), market.get(Value::from("id")));
        };
        if since.clone().is_nonnullish() {
            request.set("start_time".into(), self.iso8601(since.clone()));
        };
        if limit.clone().is_nonnullish() {
            request.set("page_size".into(), limit.clone());
            if limit.clone() > Value::from(100) {
                request.set("page_size".into(), Value::from(100));
            };
        };
        let mut response: Value = self.dispatch("v3PrivateGetOrders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_orders(data.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined).await;
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("type".into(), r#type.clone());
        request.set("side".into(), side.clone());
        request.set("amount".into(), amount.clone());
        if price.is_nonnullish() { request.set("price".into(), price.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "POST" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "POST", "order"), ("private", "POST", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn create_orders(&mut self, mut orders: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut orders_requests: Value = Value::new_array();
        let mut i: usize = 0;
        while i < orders.len() {
            let mut order: Value = self.safe_dict(orders.clone(), Value::from(i), Value::Undefined);
            let mut symbol: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
            let mut market: Value = self.market(symbol.clone());
            let mut r#type: Value = self.safe_string_upper(order.clone(), Value::from("type"), Value::Undefined);
            let mut order_params: Value = self.safe_dict(order.clone(), Value::from("params"), Value::new_object());
            if r#type.clone() != Value::from("LIMIT") && r#type.clone() != Value::from("MARKET") && r#type.clone() != Value::from("STOP_MARKET") && r#type.clone() != Value::from("STOP_LIMIT") && r#type.clone() != Value::from("INSTANT") {
                return Value::Undefined;
            };
            let mut time_in_force: Value = self.safe_string_upper(order_params.clone(), Value::from("timeInForce"), Value::Undefined);
            let mut post_only: Value = self.safe_bool(order_params.clone(), Value::from("postOnly"), Value::from(false));
            let mut trigger_price: Value = self.safe_number(order_params.clone(), Value::from("triggerPrice"), Value::Undefined);
            let mut request: Value = Value::Json(normalize(&Value::Json(json!({
                "market_symbol": market.get(Value::from("id")),
                "side": self.safe_string_upper(order.clone(), Value::from("side"), Value::Undefined),
                "type": r#type.clone()
            }))).unwrap());
            if r#type.clone() == Value::from("STOP_MARKET") || r#type.clone() == Value::from("STOP_LIMIT") {
                if trigger_price.clone().is_nullish() {
                    return Value::Undefined;
                };
            };
            if time_in_force.clone().is_nonnullish() {
                if time_in_force.clone() == Value::from("PO") {
                    request.set("post_only".into(), Value::from(true));
                } else {
                    request.set("time_in_force".into(), time_in_force.clone());
                };
                order_params.get(Value::from("timeInForce"));
            };
            if post_only.is_truthy() {
                request.set("post_only".into(), Value::from(true));
                order_params.get(Value::from("postOnly"));
            };
            if trigger_price.clone().is_nonnullish() {
                request.set("stop_price".into(), self.price_to_precision(symbol.clone(), trigger_price.clone()));
                order_params.get(Value::from("triggerPrice"));
            };
            if r#type.clone() == Value::from("INSTANT") {
                request.set("amount".into(), self.price_to_precision(symbol.clone(), self.safe_string(order.clone(), Value::from("amount"), Value::Undefined)));
            } else {
                request.set("quantity".into(), self.amount_to_precision(symbol.clone(), self.safe_string(order.clone(), Value::from("amount"), Value::Undefined)));
            };
            if r#type.clone() == Value::from("LIMIT") || r#type.clone() == Value::from("STOP_LIMIT") {
                request.set("price".into(), self.price_to_precision(symbol.clone(), self.safe_string(order.clone(), Value::from("price"), Value::Undefined)));
            };
            orders_requests.push(extend_2(request.clone(), order_params.clone()));
            i += 1;
        };
        let mut create_orders_request: Value = Value::Json(normalize(&Value::Json(json!({
            "data": orders_requests.clone()
        }))).unwrap());
        let mut response: Value = self.dispatch("v3PrivatePostOrdersBatch".into(), extend_2(create_orders_request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "data": [
        //         {
        //         "side": "BUY",
        //         "type": "LIMIT",
        //         "market_symbol": "btcbrl",
        //         "client_order_id": "451637946501",
        //         "remark": "A remarkable note for the order.",
        //         "quantity": "0.42",
        //         "price": "250000.0",
        //         "post_only": true,
        //         "time_in_force": "GTC"
        //         }
        //     ]
        // }
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_orders(data.clone(), Value::Undefined, Value::Undefined, Value::Undefined, Value::Undefined).await;
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("orderId".into(), id.clone());
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "DELETE" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "DELETE", "order"), ("private", "DELETE", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn cancel_all_orders(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "type": Value::from("ALL")
        }))).unwrap());
        if symbol.clone().is_nonnullish() {
            let mut market: Value = self.market(symbol.clone());
            request.set("type".into(), Value::from("MARKET"));
            request.set("market_symbol".into(), market.get(Value::from("id")));
        };
        let mut response: Value = self.dispatch("v3PrivatePutOrdersCancel".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "data": [
        //         {
        //           "sn": "OKMAKSDHRVVREK",
        //           "id": 123456789
        //         }
        //     ]
        // }
        return Value::Json(serde_json::Value::Array(vec![(self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": response.clone()
        }))).unwrap()), Value::Undefined).await).into()]));
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "id": id.clone()
        }))).unwrap());
        let mut response: Value = self.dispatch("v3PrivateGetOrdersByOrderIdId".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "id": "1234567890",
        //     "sn": "OKMAKSDHRVVREK",
        //     "client_order_id": "451637946501",
        //     "market_symbol": "btcbrl",
        //     "side": "BUY",
        //     "type": "LIMIT",
        //     "state": "ACTIVE",
        //     "price": "290000.0",
        //     "price_avg": "295333.3333",
        //     "quantity": "0.42",
        //     "quantity_executed": "0.41",
        //     "instant_amount": "290.0",
        //     "instant_amount_executed": "290.0",
        //     "created_at": "2021-02-15T22:06:32.999Z",
        //     "trades_count": "2",
        //     "remark": "A remarkable note for the order.",
        //     "funds_received": "290.0"
        // }
        return <Self as Foxbit>::parse_order(self, response.clone(), Value::Undefined).await;
    }

    async fn fetch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["allorders", "all_orders", "orders/history", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "allOrders"), ("private", "GET", "orders"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["mytrades", "my_trades", "trades/history", "fills", "user/trades", "executions"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "myTrades"), ("private", "GET", "fills"), ("private", "GET", "trades/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_deposit_address(&mut self, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "currency_symbol": currency.get(Value::from("id"))
        }))).unwrap());
        let (mut network_code, mut params_omited) = shift_2(self.handle_network_code_and_params(params.clone()));
        if network_code.clone().is_nonnullish() {
            request.set("network_code".into(), self.network_code_to_id(network_code.clone(), code.clone()));
        };
        let mut response: Value = self.dispatch("v3PrivateGetDepositsAddress".into(), extend_2(request.clone(), params_omited.clone()), Value::Undefined).await;
        // {
        //     "currency_symbol": "btc",
        //     "address": "2N9sS8LgrY19rvcCWDmE1ou1tTVmqk4KQAB",
        //     "message": "Address was retrieved successfully",
        //     "destination_tag": "string",
        //     "network": {
        //         "name": "Bitcoin Network",
        //         "code": "btc"
        //     }
        // }
        return <Self as Foxbit>::parse_deposit_address(self, response.clone(), currency.clone()).await;
    }

    async fn fetch_deposits(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["deposits", "deposit/history", "deposit_history", "capital/deposit/hisrec"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/deposit/hisrec"), ("private", "GET", "deposits"), ("private", "GET", "deposit/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_withdrawals(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if code.is_nonnullish() { request.set("coin".into(), code.clone()); request.set("currency".into(), code.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["withdrawals", "withdraw/history", "withdrawal_history", "capital/withdraw/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("sapi", "GET", "capital/withdraw/history"), ("private", "GET", "withdrawals"), ("private", "GET", "withdraw/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_transactions(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut withdrawals: Value = <Self as Foxbit>::fetch_withdrawals(self, code.clone(), since.clone(), limit.clone(), params.clone()).await;
        let mut deposits: Value = <Self as Foxbit>::fetch_deposits(self, code.clone(), since.clone(), limit.clone(), params.clone()).await;
        let mut all_transactions: Value = self.array_concat(withdrawals.clone(), deposits.clone());
        let mut result: Value = self.sort_by(all_transactions.clone(), Value::from("timestamp"), Value::Undefined, Value::Undefined);
        return result.clone();
    }

    async fn fetch_status(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Foxbit>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["status", "ping", "time", "system/status"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "status"), ("public", "GET", "ping"), ("public", "GET", "time"), ("sapi", "GET", "system/status")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn edit_order(&mut self, mut id: Value, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            return Value::Undefined;
        };
        r#type = r#type.to_upper_case();
        if r#type.clone() != Value::from("LIMIT") && r#type.clone() != Value::from("MARKET") && r#type.clone() != Value::from("STOP_MARKET") && r#type.clone() != Value::from("INSTANT") {
            return Value::Undefined;
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "mode": Value::from("ALLOW_FAILURE"),
            "cancel": Value::Json(normalize(&Value::Json(json!({
                "type": Value::from("ID"),
                "id": self.parse_number(id.clone(), Value::Undefined)
            }))).unwrap()),
            "create": Value::Json(normalize(&Value::Json(json!({
                "type": r#type.clone(),
                "side": side.to_upper_case(),
                "market_symbol": market.get(Value::from("id"))
            }))).unwrap())
        }))).unwrap());
        if r#type.clone() == Value::from("LIMIT") || r#type.clone() == Value::from("MARKET") {
            request.get(Value::from("create")).set("quantity".into(), self.amount_to_precision(symbol.clone(), amount.clone()));
            if r#type.clone() == Value::from("LIMIT") {
                request.get(Value::from("create")).set("price".into(), self.price_to_precision(symbol.clone(), price.clone()));
            };
        };
        if r#type.clone() == Value::from("STOP_MARKET") {
            request.get(Value::from("create")).set("stop_price".into(), self.price_to_precision(symbol.clone(), price.clone()));
            request.get(Value::from("create")).set("quantity".into(), self.amount_to_precision(symbol.clone(), amount.clone()));
        };
        if r#type.clone() == Value::from("INSTANT") {
            request.get(Value::from("create")).set("amount".into(), self.price_to_precision(symbol.clone(), amount.clone()));
        };
        let mut response: Value = self.dispatch("v3PrivatePostOrdersCancelReplace".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "cancel": {
        //         "id": 123456789
        //     },
        //     "create": {
        //         "id": 1234567890,
        //         "client_order_id": "451637946501"
        //     }
        // }
        return <Self as Foxbit>::parse_order(self, response.get(Value::from("create")), market.clone()).await;
    }

    async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, mut tag: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        (tag, params) = shift_2(self.handle_withdraw_tag_and_params(tag.clone(), params.clone()));
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "currency_symbol": currency.get(Value::from("id")),
            "amount": self.number_to_string(amount.clone()),
            "destination_address": address.clone()
        }))).unwrap());
        if tag.clone().is_nonnullish() {
            request.set("destination_tag".into(), tag.clone());
        };
        let mut network_code: Value = Value::Undefined;
        (network_code, params) = shift_2(self.handle_network_code_and_params(params.clone()));
        if network_code.clone().is_nonnullish() {
            request.set("network_code".into(), self.network_code_to_id(network_code.clone(), Value::Undefined));
        };
        let mut response: Value = self.dispatch("v3PrivatePostWithdrawals".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        // {
        //     "amount": "2",
        //     "currency_symbol": "xrp",
        //     "network_code": "ripple",
        //     "destination_address": "0x1234567890123456789012345678",
        //     "destination_tag": "123456"
        // }
        return <Self as Foxbit>::parse_transaction(self, response.clone(), Value::Undefined).await;
    }

    async fn fetch_ledger(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::new_object();
        if code.clone().is_nullish() {
            return Value::Undefined;
        };
        if limit.clone().is_nonnullish() {
            request.set("page_size".into(), limit.clone());
            if limit.clone() > Value::from(100) {
                request.set("page_size".into(), Value::from(100));
            };
        };
        if since.clone().is_nonnullish() {
            request.set("start_time".into(), self.iso8601(since.clone()));
        };
        let mut currency: Value = self.currency(code.clone());
        request.set("symbol".into(), currency.get(Value::from("id")));
        let mut response: Value = self.dispatch("v3PrivateGetAccountsSymbolTransactions".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_ledger(data.clone(), currency.clone(), since.clone(), limit.clone(), Value::Undefined).await;
    }

    fn parse_market(&self, mut market: Value) -> Value {
        let mut id: Value = self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined);
        let mut base_assets: Value = self.safe_dict(market.clone(), Value::from("base"), Value::Undefined);
        let mut base_id: Value = self.safe_string(base_assets.clone(), Value::from("symbol"), Value::Undefined);
        let mut quote_assets: Value = self.safe_dict(market.clone(), Value::from("quote"), Value::Undefined);
        let mut quote_id: Value = self.safe_string(quote_assets.clone(), Value::from("symbol"), Value::Undefined);
        let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        let mut symbol: Value = base.clone() + Value::from("/") + quote.clone();
        let mut fees: Value = self.safe_dict(market.clone(), Value::from("default_fees"), Value::Undefined);
        return self.safe_market_structure(Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "symbol": symbol.clone(),
            "base": base.clone(),
            "quote": quote.clone(),
            "baseId": base_id.clone(),
            "quoteId": quote_id.clone(),
            "active": Value::from(true),
            "type": Value::from("spot"),
            "spot": Value::from(true),
            "margin": Value::from(false),
            "future": Value::from(false),
            "swap": Value::from(false),
            "option": Value::from(false),
            "contract": Value::from(false),
            "settle": Value::Undefined,
            "settleId": Value::Undefined,
            "contractSize": Value::Undefined,
            "linear": Value::Undefined,
            "inverse": Value::Undefined,
            "expiry": Value::Undefined,
            "expiryDatetime": Value::Undefined,
            "strike": Value::Undefined,
            "optionType": Value::Undefined,
            "taker": self.safe_number(fees.clone(), Value::from("taker"), Value::Undefined),
            "maker": self.safe_number(fees.clone(), Value::from("maker"), Value::Undefined),
            "percentage": Value::from(true),
            "tierBased": Value::from(false),
            "feeSide": Value::from("get"),
            "precision": Value::Json(normalize(&Value::Json(json!({
                "price": self.safe_integer(quote_assets.clone(), Value::from("precision"), Value::Undefined),
                "amount": self.safe_integer(base_assets.clone(), Value::from("precision"), Value::Undefined),
                "cost": self.safe_integer(quote_assets.clone(), Value::from("precision"), Value::Undefined)
            }))).unwrap()),
            "limits": Value::Json(normalize(&Value::Json(json!({
                "amount": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(market.clone(), Value::from("quantity_min"), Value::Undefined),
                    "max": Value::Undefined
                }))).unwrap()),
                "price": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(market.clone(), Value::from("price_min"), Value::Undefined),
                    "max": Value::Undefined
                }))).unwrap()),
                "cost": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap()),
                "leverage": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": Value::Undefined
                }))).unwrap())
            }))).unwrap()),
            "info": market.clone()
        }))).unwrap()));
    }

    fn parse_trading_fee(&self, mut entry: Value, mut market: Value) -> Value {
        return Value::Json(normalize(&Value::Json(json!({
            "info": entry.clone(),
            "symbol": market.get(Value::from("symbol")),
            "maker": self.safe_number(entry.clone(), Value::from("maker"), Value::Undefined),
            "taker": self.safe_number(entry.clone(), Value::from("taker"), Value::Undefined),
            "percentage": Value::from(true),
            "tierBased": Value::from(true)
        }))).unwrap());
    }

    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("market_symbol"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::from("spot"));
        let mut rolling_24h: Value = ticker.get(Value::from("rolling_24h"));
        let mut best: Value = self.safe_dict(ticker.clone(), Value::from("best"), Value::Undefined);
        let mut best_ask: Value = self.safe_dict(best.clone(), Value::from("ask"), Value::Undefined);
        let mut best_bid: Value = self.safe_dict(best.clone(), Value::from("bid"), Value::Undefined);
        let mut last_trade: Value = ticker.get(Value::from("last_trade"));
        let mut last_price: Value = self.safe_string(last_trade.clone(), Value::from("price"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol.clone(),
            "timestamp": self.parse_date(self.safe_string(last_trade.clone(), Value::from("date"), Value::Undefined)),
            "datetime": self.iso8601(self.parse_date(self.safe_string(last_trade.clone(), Value::from("date"), Value::Undefined))),
            "high": self.safe_number(rolling_24h.clone(), Value::from("high"), Value::Undefined),
            "low": self.safe_number(rolling_24h.clone(), Value::from("low"), Value::Undefined),
            "bid": self.safe_number(best_bid.clone(), Value::from("price"), Value::Undefined),
            "bidVolume": self.safe_number(best_bid.clone(), Value::from("volume"), Value::Undefined),
            "ask": self.safe_number(best_ask.clone(), Value::from("price"), Value::Undefined),
            "askVolume": self.safe_number(best_ask.clone(), Value::from("volume"), Value::Undefined),
            "vwap": Value::Undefined,
            "open": self.safe_number(rolling_24h.clone(), Value::from("open"), Value::Undefined),
            "close": last_price.clone(),
            "last": last_price.clone(),
            "previousClose": Value::Undefined,
            "change": self.safe_string(rolling_24h.clone(), Value::from("price_change"), Value::Undefined),
            "percentage": self.safe_string(rolling_24h.clone(), Value::from("price_change_percent"), Value::Undefined),
            "average": Value::Undefined,
            "baseVolume": self.safe_string(rolling_24h.clone(), Value::from("volume"), Value::Undefined),
            "quoteVolume": self.safe_string(rolling_24h.clone(), Value::from("quote_volume"), Value::Undefined),
            "info": ticker.clone()
        }))).unwrap()), market.clone());
    }

    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        return Value::Json(serde_json::Value::Array(vec![(self.safe_integer(ohlcv.clone(), Value::from(0), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from(1), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from(2), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from(3), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from(4), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from(6), Value::Undefined)).into()]));
    }

    fn parse_trade(&self, mut trade: Value, mut market: Value) -> Value {
        let mut timestamp: Value = self.parse_date(self.safe_string(trade.clone(), Value::from("created_at"), Value::Undefined));
        let mut price: Value = self.safe_string(trade.clone(), Value::from("price"), Value::Undefined);
        let mut amount: Value = self.safe_string(trade.clone(), Value::from("volume"), self.safe_string(trade.clone(), Value::from("quantity"), Value::Undefined));
        let mut private_side_field: Value = self.safe_string_lower(trade.clone(), Value::from("side"), Value::Undefined);
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::from("taker_side"), private_side_field.clone());
        let mut cost: Value = Precise::string_mul(price.clone(), amount.clone());
        let mut fee: Value = Value::Json(normalize(&Value::Json(json!({
            "currency": self.safe_symbol(self.safe_string(trade.clone(), Value::from("fee_currency_symbol"), Value::Undefined), Value::Undefined, Value::Undefined, Value::Undefined),
            "cost": self.safe_number(trade.clone(), Value::from("fee"), Value::Undefined),
            "rate": Value::Undefined
        }))).unwrap());
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(trade.clone(), Value::from("id"), Value::Undefined),
            "info": trade.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": market.get(Value::from("symbol")),
            "order": Value::Undefined,
            "type": Value::Undefined,
            "side": side.clone(),
            "takerOrMaker": Value::Undefined,
            "price": price.clone(),
            "amount": amount.clone(),
            "cost": cost.clone(),
            "fee": fee.clone()
        }))).unwrap()), market.clone());
    }

    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "PARTIALLY_CANCELED": Value::from("open"),
            "ACTIVE": Value::from("open"),
            "PARTIALLY_FILLED": Value::from("open"),
            "FILLED": Value::from("closed"),
            "PENDING_CANCEL": Value::from("canceled"),
            "CANCELED": Value::from("canceled")
        }))).unwrap());
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_order(&self, mut order: Value, mut market: Value) -> Value {
        let mut symbol: Value = self.safe_string(order.clone(), Value::from("market_symbol"), Value::Undefined);
        if market.clone().is_nullish() && symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
        };
        if market.clone().is_nonnullish() {
            symbol = market.get(Value::from("symbol"));
        };
        let mut timestamp: Value = self.parse_date(self.safe_string(order.clone(), Value::from("created_at"), Value::Undefined));
        let mut price: Value = self.safe_string(order.clone(), Value::from("price"), Value::Undefined);
        let mut filled: Value = self.safe_string(order.clone(), Value::from("quantity_executed"), Value::Undefined);
        let mut remaining: Value = self.safe_string(order.clone(), Value::from("quantity"), Value::Undefined);
        // TODO: validate logic of amount here, should this be calculated?
        let mut amount: Value = Value::Undefined;
        if remaining.clone().is_nonnullish() && filled.clone().is_nonnullish() {
            amount = Precise::string_add(remaining.clone(), filled.clone());
        };
        let mut cost: Value = self.safe_string(order.clone(), Value::from("funds_received"), Value::Undefined);
        if !cost.is_truthy() {
            let mut price_average: Value = self.safe_string(order.clone(), Value::from("price_avg"), Value::Undefined);
            let mut price_to_calculate: Value = self.safe_string(order.clone(), Value::from("price"), price_average.clone());
            cost = Precise::string_mul(price_to_calculate.clone(), amount.clone());
        };
        let mut side: Value = self.safe_string_lower(order.clone(), Value::from("side"), Value::Undefined);
        let mut fee_currency: Value = self.safe_string_upper(market.clone(), Value::from("quoteId"), Value::Undefined);
        if side.clone() == Value::from("buy") {
            fee_currency = self.safe_string_upper(market.clone(), Value::from("baseId"), Value::Undefined);
        };
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": self.safe_string(order.clone(), Value::from("id"), Value::Undefined),
            "info": order.clone(),
            "clientOrderId": self.safe_string(order.clone(), Value::from("client_order_id"), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": Value::Undefined,
            "status": <Self as Foxbit>::parse_order_status(self, self.safe_string(order.clone(), Value::from("state"), Value::Undefined)),
            "symbol": self.safe_string(market.clone(), Value::from("symbol"), Value::Undefined),
            "type": self.safe_string(order.clone(), Value::from("type"), Value::Undefined),
            "timeInForce": self.safe_string(order.clone(), Value::from("time_in_force"), Value::Undefined),
            "postOnly": self.safe_bool(order.clone(), Value::from("post_only"), Value::Undefined),
            "reduceOnly": Value::Undefined,
            "side": side.clone(),
            "price": self.parse_number(price.clone(), Value::Undefined),
            "triggerPrice": self.safe_number(order.clone(), Value::from("stop_price"), Value::Undefined),
            "takeProfitPrice": Value::Undefined,
            "stopLossPrice": Value::Undefined,
            "cost": self.parse_number(cost.clone(), Value::Undefined),
            "average": self.safe_number(order.clone(), Value::from("price_avg"), Value::Undefined),
            "amount": self.parse_number(amount.clone(), Value::Undefined),
            "filled": self.parse_number(filled.clone(), Value::Undefined),
            "remaining": self.parse_number(remaining.clone(), Value::Undefined),
            "trades": Value::Undefined,
            "fee": Value::Json(normalize(&Value::Json(json!({
                "currency": fee_currency.clone(),
                "cost": self.safe_number(order.clone(), Value::from("fee_paid"), Value::Undefined)
            }))).unwrap())
        }))).unwrap()), Value::Undefined);
    }

    fn parse_deposit_address(&self, mut deposit_address: Value, mut currency: Value) -> Value {
        let mut network: Value = self.safe_dict(deposit_address.clone(), Value::from("network"), Value::Undefined);
        let mut network_id: Value = self.safe_string(network.clone(), Value::from("code"), Value::Undefined);
        let mut currency_code: Value = self.safe_currency_code(Value::Undefined, currency.clone());
        let mut unified_network: Value = self.network_id_to_code(network_id.clone(), currency_code.clone());
        return Value::Json(normalize(&Value::Json(json!({
            "address": self.safe_string(deposit_address.clone(), Value::from("address"), Value::Undefined),
            "tag": self.safe_string(deposit_address.clone(), Value::from("tag"), Value::Undefined),
            "currency": currency_code.clone(),
            "network": unified_network.clone(),
            "info": deposit_address.clone()
        }))).unwrap());
    }

    fn parse_transaction_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "SUBMITTING": Value::from("pending"),
            "SUBMITTED": Value::from("pending"),
            "REJECTED": Value::from("failed"),
            "CANCELLED": Value::from("canceled"),
            "ACCEPTED": Value::from("ok"),
            "WARNING": Value::from("pending"),
            "UNBLOCKED": Value::from("pending"),
            "BLOCKED": Value::from("pending"),
            "PROCESSING": Value::from("pending"),
            "CANCELED": Value::from("canceled"),
            "FAILED": Value::from("failed"),
            "DONE": Value::from("ok")
        }))).unwrap());
        // BOTH
        // DEPOSIT-SPECIFIC
        // WITHDRAWAL-SPECIFIC
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value, mut since: Value, mut limit: Value) -> Value {
        let mut crypto_details: Value = self.safe_dict(transaction.clone(), Value::from("details_crypto"), Value::Undefined);
        let mut address: Value = self.safe_string_2(crypto_details.clone(), Value::from("receiving_address"), Value::from("destination_address"), Value::Undefined);
        let mut sn: Value = self.safe_string(transaction.clone(), Value::from("sn"), Value::Undefined);
        let mut r#type: Value = Value::from("withdrawal");
        if sn.clone().is_nonnullish() && sn.get(Value::from(0)) == Value::from("D") {
            r#type = Value::from("deposit");
        };
        let mut fee: Value = self.safe_string(transaction.clone(), Value::from("fee"), Value::from("0"));
        let mut amount: Value = self.safe_string(transaction.clone(), Value::from("amount"), Value::Undefined);
        let mut currency_symbol: Value = self.safe_string(transaction.clone(), Value::from("currency_symbol"), Value::Undefined);
        let mut actual_amount: Value = amount.clone();
        let mut currency_code: Value = self.safe_currency_code(currency_symbol.clone(), Value::Undefined);
        let mut status: Value = <Self as Foxbit>::parse_transaction_status(self, self.safe_string(transaction.clone(), Value::from("state"), Value::Undefined));
        let mut created_at: Value = self.safe_string(transaction.clone(), Value::from("created_at"), Value::Undefined);
        let mut timestamp: Value = self.parse_date(created_at.clone());
        let mut datetime: Value = self.iso8601(timestamp.clone());
        if fee.clone().is_nonnullish() && amount.clone().is_nonnullish() {
            // actualAmount = amount - fee;
            actual_amount = Precise::string_sub(amount.clone(), fee.clone());
        };
        let mut fee_rate: Value = Precise::string_div(fee.clone(), actual_amount.clone(), Value::Undefined);
        let mut fee_obj: Value = Value::Json(normalize(&Value::Json(json!({
            "cost": self.parse_number(fee.clone(), Value::Undefined),
            "currency": currency_code.clone(),
            "rate": self.parse_number(fee_rate.clone(), Value::Undefined)
        }))).unwrap());
        return Value::Json(normalize(&Value::Json(json!({
            "info": transaction.clone(),
            "id": self.safe_string(transaction.clone(), Value::from("sn"), Value::Undefined),
            "txid": self.safe_string(crypto_details.clone(), Value::from("transaction_id"), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": datetime.clone(),
            "network": self.safe_string(transaction.clone(), Value::from("network_code"), Value::Undefined),
            "address": address.clone(),
            "addressTo": address.clone(),
            "addressFrom": Value::Undefined,
            "tag": self.safe_string(transaction.clone(), Value::from("destination_tag"), Value::Undefined),
            "tagTo": self.safe_string(transaction.clone(), Value::from("destination_tag"), Value::Undefined),
            "tagFrom": Value::Undefined,
            "type": r#type.clone(),
            "amount": self.parse_number(amount.clone(), Value::Undefined),
            "currency": currency_code.clone(),
            "status": status.clone(),
            "updated": Value::Undefined,
            "fee": fee_obj.clone(),
            "comment": Value::Undefined,
            "internal": Value::Undefined
        }))).unwrap());
    }

    fn parse_ledger_entry_type(&self, mut r#type: Value) -> Value {
        let mut types: Value = Value::Json(normalize(&Value::Json(json!({
            "DEPOSITING": Value::from("transaction"),
            "WITHDRAWING": Value::from("transaction"),
            "TRADING": Value::from("trade"),
            "INTERNAL_TRANSFERING": Value::from("transfer"),
            "OTHERS": Value::from("transaction")
        }))).unwrap());
        return self.safe_string(types.clone(), r#type.clone(), r#type.clone());
    }

    fn parse_ledger_entry(&self, mut item: Value, mut currency: Value) -> Value {
        // {
        //     "uuid": "f8e9f2d6-3c1e-4f2d-8f8e-9f2d6c1e4f2d",
        //     "amount": "0.0001",
        //     "balance": "0.0002",
        //     "created_at": "2021-07-01T12:00:00Z",
        //     "currency_symbol": "btc",
        //     "fee": "0.0001",
        //     "locked": "0.0001",
        //     "locked_amount": "0.0001",
        //     "reason_type": "DEPOSITING"
        // }
        let mut id: Value = self.safe_string(item.clone(), Value::from("uuid"), Value::Undefined);
        let mut created_at: Value = self.safe_string(item.clone(), Value::from("created_at"), Value::Undefined);
        let mut timestamp: Value = self.parse8601(created_at.clone());
        let mut reason_type: Value = self.safe_string(item.clone(), Value::from("reason_type"), Value::Undefined);
        let mut r#type: Value = <Self as Foxbit>::parse_ledger_entry_type(self, reason_type.clone());
        let mut exchange_symbol: Value = self.safe_string(item.clone(), Value::from("currency_symbol"), Value::Undefined);
        let mut currency_symbol: Value = self.safe_currency_code(exchange_symbol.clone(), Value::Undefined);
        let mut direction: Value = Value::from("in");
        let mut amount: Value = self.safe_number(item.clone(), Value::from("amount"), Value::Undefined);
        let mut real_amount: Value = amount.clone();
        let mut balance: Value = self.safe_number(item.clone(), Value::from("balance"), Value::Undefined);
        let mut fee: Value = Value::Json(normalize(&Value::Json(json!({
            "cost": self.safe_number(item.clone(), Value::from("fee"), Value::Undefined),
            "currency": currency_symbol.clone()
        }))).unwrap());
        if amount.clone() < Value::from(0) {
            direction = Value::from("out");
            real_amount = amount.clone() * Value::from(1).neg();
        };
        return Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "info": item.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "direction": direction.clone(),
            "account": Value::Undefined,
            "referenceId": Value::Undefined,
            "referenceAccount": Value::Undefined,
            "type": r#type.clone(),
            "currency": currency_symbol.clone(),
            "amount": real_amount.clone(),
            "before": balance.clone() - amount.clone(),
            "after": balance.clone(),
            "status": Value::from("ok"),
            "fee": fee.clone()
        }))).unwrap());
    }

    

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_str() {
                    "v3PublicGetCurrencies" => self.request("currencies".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarkets" => self.request("markets".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarketsticker24hr" => self.request("markets/ticker/24hr".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarketsmarketorderbook" => self.request("markets/{market}/orderbook".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarketsmarketcandlesticks" => self.request("markets/{market}/candlesticks".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarketsmarkettradeshistory" => self.request("markets/{market}/trades/history".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PublicGetMarketsmarketticker24hr" => self.request("markets/{market}/ticker/24hr".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccounts" => self.request("accounts".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetAccountssymboltransactions" => self.request("accounts/{symbol}/transactions".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetOrders" => self.request("orders".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetOrdersbyorderidid" => self.request("orders/by-order-id/{id}".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetTrades" => self.request("trades".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetDepositsaddress" => self.request("deposits/address".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetDeposits" => self.request("deposits".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetWithdrawals" => self.request("withdrawals".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivateGetMefeestrading" => self.request("me/fees/trading".into(), "v3".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostOrders" => self.request("orders".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostOrdersbatch" => self.request("orders/batch".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostOrderscancelreplace" => self.request("orders/cancel-replace".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePostWithdrawals" => self.request("withdrawals".into(), "v3".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v3PrivatePutOrderscancel" => self.request("orders/cancel".into(), "v3".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "statusPublicGetStatus" => self.request("status".into(), "status".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct FoxbitImpl(Value);
#[async_trait(?Send)]
impl Exchange for FoxbitImpl {
    fn describe(&self) -> Value { Foxbit::describe(self) }
    async fn fetch_ticker(&mut self, symbol: Value, params: Value) -> Value { Foxbit::fetch_ticker(self, symbol, params).await }
    async fn fetch_tickers(&mut self, symbols: Value, params: Value) -> Value { Foxbit::fetch_tickers(self, symbols, params).await }
    async fn fetch_order_book(&mut self, symbol: Value, limit: Value, params: Value) -> Value { Foxbit::fetch_order_book(self, symbol, limit, params).await }
    async fn fetch_ohlcv(&mut self, symbol: Value, timeframe: Value, since: Value, limit: Value, params: Value) -> Value { Foxbit::fetch_ohlcv(self, symbol, timeframe, since, limit, params).await }
    async fn fetch_trades(&mut self, symbol: Value, since: Value, limit: Value, params: Value) -> Value { Foxbit::fetch_trades(self, symbol, since, limit, params).await }
    async fn fetch_status(&mut self, params: Value) -> Value { Foxbit::fetch_status(self, params).await }
    fn parse_trade(&self, trade: Value, market: Value) -> Value { Foxbit::parse_trade(self, trade, market) }
    fn parse_order(&self, order: Value, market: Value) -> Value { Foxbit::parse_order(self, order, market) }
    fn parse_ticker(&self, ticker: Value, market: Value) -> Value { Foxbit::parse_ticker(self, ticker, market) }
    fn parse_market(&self, market: Value) -> Value { Foxbit::parse_market(self, market) }
    fn parse_transaction(&self, transaction: Value, currency: Value) -> Value { Foxbit::parse_transaction(self, transaction, currency) }
    fn parse_deposit_address(&self, deposit_address: Value, currency: Value) -> Value { Foxbit::parse_deposit_address(self, deposit_address, currency) }
    fn parse_ledger_entry(&self, item: Value, currency: Value) -> Value { Foxbit::parse_ledger_entry(self, item, currency) }
    fn parse_order_status(&self, status: Value) -> Value { Foxbit::parse_order_status(self, status) }
}
impl Foxbit for FoxbitImpl {}
impl ValueTrait for FoxbitImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl FoxbitImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = FoxbitImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Foxbit::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


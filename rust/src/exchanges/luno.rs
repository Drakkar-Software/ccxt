#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
use crate::ws::{ArrayCache, ArrayCacheByTimestamp, ArrayCacheBySymbolById, ArrayCacheBySymbolBySide};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors (stub structs with ::new for transpiled `new ErrorType(msg)` patterns)
macro_rules! error_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(msg: Value) -> Value { msg } } }; }
error_stub!(BadRequest);
error_stub!(InvalidOrder);
error_stub!(ExchangeError);
error_stub!(InsufficientFunds);
error_stub!(OrderNotFound);
error_stub!(AuthenticationError);
error_stub!(PermissionDenied);
error_stub!(ExchangeNotAvailable);
error_stub!(ArgumentsRequired);
error_stub!(RateLimitExceeded);
error_stub!(OrderNotFillable);
error_stub!(OrderImmediatelyFillable);
error_stub!(NotSupported);
error_stub!(DuplicateOrderId);
error_stub!(UnsubscribeError);
error_stub!(ChecksumError);
error_stub!(InvalidNonce);
error_stub!(BadSymbol);
// Array-like type stubs (Bids/Asks order book sides)
macro_rules! array_side_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(data: Value, _limit: Value) -> Value { data } } }; }
array_side_stub!(Bids);
array_side_stub!(Asks);

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait(?Send)]
pub trait Luno : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "luno",
            "name": "luno",
            "countries": [
                "GB",
                "SG",
                "ZA"
            ],
            "enableRateLimit": true,
            "rateLimit": 200,
            "timeout": 10000,
            "certified": false,
            "pro": true,
            "alias": false,
            "dex": false,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "spot": true,
                "margin": false,
                "swap": false,
                "future": false,
                "option": false,
                "addMargin": false,
                "borrowCrossMargin": false,
                "borrowIsolatedMargin": false,
                "borrowMargin": false,
                "cancelOrder": true,
                "closeAllPositions": false,
                "closePosition": false,
                "createDepositAddress": true,
                "createLimitOrder": true,
                "createMarketOrder": true,
                "createMarketOrderWs": true,
                "createOrder": true,
                "createReduceOnlyOrder": false,
                "editOrder": "emulated",
                "fetchAccounts": true,
                "fetchBalance": true,
                "fetchBorrowInterest": false,
                "fetchBorrowRate": false,
                "fetchBorrowRateHistories": false,
                "fetchBorrowRateHistory": false,
                "fetchBorrowRates": false,
                "fetchBorrowRatesPerSymbol": false,
                "fetchClosedOrders": true,
                "fetchCrossBorrowRate": false,
                "fetchCrossBorrowRates": false,
                "fetchCurrencies": true,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": true,
                "fetchFundingHistory": false,
                "fetchFundingRate": false,
                "fetchFundingRateHistory": false,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchFundingRates": false,
                "fetchGreeks": false,
                "fetchIndexOHLCV": false,
                "fetchIsolatedBorrowRate": false,
                "fetchIsolatedBorrowRates": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchIsolatedPositions": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLeverage": false,
                "fetchLeverages": false,
                "fetchLeverageTiers": false,
                "fetchLiquidations": false,
                "fetchLongShortRatio": false,
                "fetchLongShortRatioHistory": false,
                "fetchMarginMode": false,
                "fetchMarginModes": false,
                "fetchMarketLeverageTiers": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyLiquidations": false,
                "fetchMySettlementHistory": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterest": false,
                "fetchOpenInterests": false,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrders": true,
                "fetchOption": false,
                "fetchOptionChain": false,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchPosition": false,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": false,
                "fetchPositionsForSymbol": false,
                "fetchPositionsForSymbolWs": false,
                "fetchPositionsRisk": false,
                "fetchPremiumIndexOHLCV": false,
                "fetchSettlementHistory": false,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchMarkPrices": false,
                "fetchTrades": true,
                "fetchTradingFee": true,
                "fetchTradingFees": false,
                "fetchUnderlyingAssets": false,
                "fetchVolatilityHistory": false,
                "reduceMargin": false,
                "repayCrossMargin": false,
                "repayIsolatedMargin": false,
                "setLeverage": false,
                "setMargin": false,
                "setMarginMode": false,
                "setPositionMode": false,
                "fetchAllGreeks": false,
                "fetchMarkPrice": false,
                "fetchPositionForSymbolWs": false
            },
            "urls": {
                "logo": "https://user-images.githubusercontent.com/1294454/27766607-8c1a69d8-5ede-11e7-930c-540b5eb9be24.jpg",
                "api": {
                    "public": "https://api.luno.com/api",
                    "private": "https://api.luno.com/api",
                    "exchange": "https://api.luno.com/api/exchange",
                    "exchangePrivate": "https://api.luno.com/api/exchange"
                },
                "www": "https://www.luno.com",
                "doc": [
                    "https://www.luno.com/en/api",
                    "https://npmjs.org/package/bitx",
                    "https://github.com/bausmeier/node-bitx"
                ],
                "referral": "https://www.luno.com/invite/44893A"
            },
            "api": {
                "exchange": {
                    "get": {
                        "markets": 1
                    }
                },
                "exchangePrivate": {
                    "get": {
                        "candles": 1
                    }
                },
                "public": {
                    "get": {
                        "orderbook": 1,
                        "orderbook_top": 1,
                        "ticker": 1,
                        "tickers": 1,
                        "trades": 1
                    }
                },
                "private": {
                    "get": {
                        "accounts/{id}/pending": 1,
                        "accounts/{id}/transactions": 1,
                        "balance": 1,
                        "beneficiaries": 1,
                        "send/networks": 1,
                        "fee_info": 1,
                        "funding_address": 1,
                        "listorders": 1,
                        "listtrades": 1,
                        "send_fee": 1,
                        "orders/{id}": 1,
                        "withdrawals": 1,
                        "withdrawals/{id}": 1,
                        "transfers": 1
                    },
                    "post": {
                        "accounts": 1,
                        "address/validate": 1,
                        "postorder": 1,
                        "marketorder": 1,
                        "stoporder": 1,
                        "funding_address": 1,
                        "withdrawals": 1,
                        "send": 1,
                        "oauth2/grant": 1,
                        "beneficiaries": 1
                    },
                    "put": {
                        "accounts/{id}/name": 1
                    },
                    "delete": {
                        "withdrawals/{id}": 1,
                        "beneficiaries/{id}": 1
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": 60,
                "5m": 300,
                "15m": 900,
                "30m": 1800,
                "1h": 3600,
                "3h": 10800,
                "4h": 14400,
                "1d": 86400,
                "3d": 259200,
                "1w": 604800
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.001,
                    "maker": 0
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "1",
            "features": {
                "spot": {
                    "sandbox": false,
                    "fetchCurrencies": {
                        "private": true
                    },
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerDirection": true,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "trailing": false,
                        "leverage": false,
                        "marketBuyByCost": true,
                        "marketBuyRequiresPrice": false,
                        "selfTradePrevention": false,
                        "iceberg": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 1000,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "symbolRequired": true
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "daysBack": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 1000,
                        "daysBack": 100000,
                        "daysBackCanceled": 1,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {}
                },
                "swap": {},
                "future": {}
            }
        }"###).unwrap())
    }

    async fn fetch_currencies(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if !self.check_required_credentials(Value::from(false)).is_truthy() {
            return Value::new_object();
        };
        let mut response: Value = self.dispatch("privateGetSendNetworks".into(), params.clone(), Value::Undefined).await;
        //
        //     {
        //         "networks": [
        //           {
        //             "id": 0,
        //             "name": "Ethereum",
        //             "native_currency": "ETH"
        //           },
        //           ...
        //         ]
        //     }
        //
        let mut currencies_data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut result: Value = Value::new_object();
        let mut i: usize = 0;
        while i < currencies_data.len() {
            let mut network_entry: Value = currencies_data.get(i.into());
            let mut id: Value = self.safe_string(network_entry.clone(), Value::from("native_currency"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(id.clone(), Value::Undefined);
            if !result.contains_key(code.clone()) {
                result.set(code.clone(), Value::Json(normalize(&Value::Json(json!({
                    "id": id.clone(),
                    "code": code.clone(),
                    "precision": Value::Undefined,
                    "type": Value::Undefined,
                    "name": Value::Undefined,
                    "active": Value::Undefined,
                    "deposit": Value::Undefined,
                    "withdraw": Value::Undefined,
                    "fee": Value::Undefined,
                    "limits": Value::Json(normalize(&Value::Json(json!({
                        "withdraw": Value::Json(normalize(&Value::Json(json!({
                            "min": Value::Undefined,
                            "max": Value::Undefined
                        }))).unwrap()),
                        "deposit": Value::Json(normalize(&Value::Json(json!({
                            "min": Value::Undefined,
                            "max": Value::Undefined
                        }))).unwrap())
                    }))).unwrap()),
                    "networks": Value::new_object(),
                    "info": Value::new_object()
                }))).unwrap()));
            };
            let mut network_id: Value = self.safe_string(network_entry.clone(), Value::from("name"), Value::Undefined);
            let mut network_code: Value = self.network_id_to_code(network_id.clone(), Value::Undefined);
            result.get(code.clone().clone()).get(Value::from("networks")).set(network_code.clone(), Value::Json(normalize(&Value::Json(json!({
                "id": network_id.clone(),
                "network": network_code.clone(),
                "limits": Value::Json(normalize(&Value::Json(json!({
                    "withdraw": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap()),
                    "deposit": Value::Json(normalize(&Value::Json(json!({
                        "min": Value::Undefined,
                        "max": Value::Undefined
                    }))).unwrap())
                }))).unwrap()),
                "active": Value::Undefined,
                "deposit": Value::Undefined,
                "withdraw": Value::Undefined,
                "fee": Value::Undefined,
                "precision": Value::Undefined,
                "info": network_entry.clone()
            }))).unwrap()));
            // add entry in info
            let mut info: Value = self.safe_list(result.get(code.clone().clone()), Value::from("info"), Value::new_array());
            info.push(network_entry.clone());
            result.get(code.clone().clone()).set("info".into(), info.clone());
            i += 1;
        };
        // only after all entries are formed in currencies, restructure each entry
        let mut all_keys: Value = result.clone().keys();
        let mut i: usize = 0;
        while i < all_keys.len() {
            let mut code: Value = all_keys.get(i.into());
            result.set(code.clone(), self.safe_currency_structure(result.get(code.clone().clone())));
            i += 1;
        };
        // this is needed after adding network entry
        return result.clone();
    }

    

    async fn fetch_accounts(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut response: Value = self.dispatch("privateGetBalance".into(), params.clone(), Value::Undefined).await;
        let mut wallets: Value = self.safe_value(response.clone(), Value::from("balance"), Value::new_array());
        let mut result: Value = Value::new_array();
        let mut i: usize = 0;
        while i < wallets.len() {
            let mut account: Value = wallets.get(i.into());
            let mut account_id: Value = self.safe_string(account.clone(), Value::from("account_id"), Value::Undefined);
            let mut currency_id: Value = self.safe_string(account.clone(), Value::from("asset"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            result.push(Value::Json(normalize(&Value::Json(json!({
                "id": account_id.clone(),
                "type": Value::Undefined,
                "currency": code.clone(),
                "info": account.clone()
            }))).unwrap()));
            i += 1;
        };
        return result.clone();
    }

    fn parse_balance(&self, mut response: Value) -> Value {
        let mut wallets: Value = self.safe_value(response.clone(), Value::from("balance"), Value::new_array());
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": response.clone(),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined
        }))).unwrap());
        let mut i: usize = 0;
        while i < wallets.len() {
            let mut wallet: Value = wallets.get(i.into());
            let mut currency_id: Value = self.safe_string(wallet.clone(), Value::from("asset"), Value::Undefined);
            let mut code: Value = self.safe_currency_code(currency_id.clone(), Value::Undefined);
            let mut reserved: Value = self.safe_string(wallet.clone(), Value::from("reserved"), Value::Undefined);
            let mut unconfirmed: Value = self.safe_string(wallet.clone(), Value::from("unconfirmed"), Value::Undefined);
            let mut balance: Value = self.safe_string(wallet.clone(), Value::from("balance"), Value::Undefined);
            let mut reserved_unconfirmed: Value = Precise::string_add(reserved.clone(), unconfirmed.clone());
            let mut balance_unconfirmed: Value = Precise::string_add(balance.clone(), unconfirmed.clone());
            if result.contains_key(code.clone()) {
                result.get(code.clone().clone()).set("used".into(), Precise::string_add(result.get(code.clone().clone()).get(Value::from("used")), reserved_unconfirmed.clone()));
                result.get(code.clone().clone()).set("total".into(), Precise::string_add(result.get(code.clone().clone()).get(Value::from("total")), balance_unconfirmed.clone()));
            } else {
                let mut account: Value = self.account();
                account.set("used".into(), reserved_unconfirmed.clone());
                account.set("total".into(), balance_unconfirmed.clone());
                result.set(code.clone(), account.clone());
            };
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["account/balance", "balance", "wallet/balance", "accounts/balance", "account"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(&format!("/{}/", token)) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "balance"), ("private", "GET", "account/balance"), ("private", "GET", "wallet/balance")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut ob = crate::exchange::unwrap_response_order_book(&json_resp);
        if let serde_json::Value::Object(ref mut map) = ob {
            if !map.contains_key("symbol") { map.insert("symbol".to_string(), serde_json::json!(symbol.unwrap_str())); }
        }
        Value::Json(ob)
    }


    fn parse_order_status(&self, mut status: Value) -> Value {
        let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
            "PENDING": Value::from("open")
        }))).unwrap());
        // todo add other statuses
        return self.safe_string(statuses.clone(), status.clone(), status.clone());
    }

    fn parse_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        //     {
        //         "base": "string",
        //         "completed_timestamp": "string",
        //         "counter": "string",
        //         "creation_timestamp": "string",
        //         "expiration_timestamp": "string",
        //         "fee_base": "string",
        //         "fee_counter": "string",
        //         "limit_price": "string",
        //         "limit_volume": "string",
        //         "order_id": "string",
        //         "pair": "string",
        //         "state": "PENDING",
        //         "type": "BID"
        //     }
        //
        let mut timestamp: Value = self.safe_integer(order.clone(), Value::from("creation_timestamp"), Value::Undefined);
        let mut status: Value = <Self as Luno>::parse_order_status(self, self.safe_string(order.clone(), Value::from("state"), Value::Undefined));
        status = if status.clone() == Value::from("open") { status.clone() } else { status.clone() };
        let mut side: Value = Value::Undefined;
        let mut order_type: Value = self.safe_string(order.clone(), Value::from("type"), Value::Undefined);
        if order_type.clone() == Value::from("ASK") || order_type.clone() == Value::from("SELL") {
            side = Value::from("sell");
        } else if order_type.clone() == Value::from("BID") || order_type.clone() == Value::from("BUY") {
            side = Value::from("buy");
        };
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("pair"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut price: Value = self.safe_string(order.clone(), Value::from("limit_price"), Value::Undefined);
        let mut amount: Value = self.safe_string(order.clone(), Value::from("limit_volume"), Value::Undefined);
        let mut quote_fee: Value = self.safe_number(order.clone(), Value::from("fee_counter"), Value::Undefined);
        let mut base_fee: Value = self.safe_number(order.clone(), Value::from("fee_base"), Value::Undefined);
        let mut filled: Value = self.safe_string(order.clone(), Value::from("base"), Value::Undefined);
        let mut cost: Value = self.safe_string(order.clone(), Value::from("counter"), Value::Undefined);
        let mut fee: Value = Value::Undefined;
        if quote_fee.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": quote_fee.clone(),
                "currency": market.get(Value::from("quote"))
            }))).unwrap());
        } else if base_fee.clone().is_nonnullish() {
            fee = Value::Json(normalize(&Value::Json(json!({
                "cost": base_fee.clone(),
                "currency": market.get(Value::from("base"))
            }))).unwrap());
        };
        let mut id: Value = self.safe_string(order.clone(), Value::from("order_id"), Value::Undefined);
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "clientOrderId": Value::Undefined,
            "datetime": self.iso8601(timestamp.clone()),
            "timestamp": timestamp.clone(),
            "lastTradeTimestamp": Value::Undefined,
            "status": status.clone(),
            "symbol": market.get(Value::from("symbol")),
            "type": Value::Undefined,
            "timeInForce": Value::Undefined,
            "postOnly": Value::Undefined,
            "side": side.clone(),
            "price": price.clone(),
            "triggerPrice": Value::Undefined,
            "amount": amount.clone(),
            "filled": filled.clone(),
            "cost": cost.clone(),
            "remaining": Value::Undefined,
            "trades": Value::Undefined,
            "fee": fee.clone(),
            "info": order.clone(),
            "average": Value::Undefined
        }))).unwrap()), market.clone());
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "id": id.clone()
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetOrdersId".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        return <Self as Luno>::parse_order(self, response.clone(), Value::Undefined).await;
    }

    async fn fetch_orders_by_state(&mut self, mut state: Value, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::new_object();
        let mut market: Value = Value::Undefined;
        if state.clone().is_nonnullish() {
            request.set("state".into(), state.clone());
        };
        if symbol.clone().is_nonnullish() {
            market = self.market(symbol.clone());
            request.set("pair".into(), market.get(Value::from("id")));
        };
        let mut response: Value = self.dispatch("privateGetListorders".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut orders: Value = self.safe_list(response.clone(), Value::from("orders"), Value::new_array());
        return self.parse_orders(orders.clone(), market.clone(), since.clone(), limit.clone(), Value::Undefined).await;
    }

    async fn fetch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["allorders", "all_orders", "orders/history", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "allOrders"), ("private", "GET", "orders"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["openorders", "open_orders", "orders/open", "orders/active"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "openOrders"), ("private", "GET", "orders/open"), ("private", "GET", "orders/active")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_closed_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["closedorders", "closed_orders", "orders/closed", "orders/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "closedOrders"), ("private", "GET", "orders/closed"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        // {
        //     "pair":"XBTAUD",
        //     "timestamp":1642201439301,
        //     "bid":"59972.30000000",
        //     "ask":"59997.99000000",
        //     "last_trade":"59997.99000000",
        //     "rolling_24_hour_volume":"1.89510000",
        //     "status":"ACTIVE"
        // }
        let mut timestamp: Value = self.safe_integer(ticker.clone(), Value::from("timestamp"), Value::Undefined);
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("pair"), Value::Undefined);
        let mut symbol: Value = self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut last: Value = self.safe_string(ticker.clone(), Value::from("last_trade"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "high": Value::Undefined,
            "low": Value::Undefined,
            "bid": self.safe_string(ticker.clone(), Value::from("bid"), Value::Undefined),
            "bidVolume": Value::Undefined,
            "ask": self.safe_string(ticker.clone(), Value::from("ask"), Value::Undefined),
            "askVolume": Value::Undefined,
            "vwap": Value::Undefined,
            "open": Value::Undefined,
            "close": last.clone(),
            "last": last.clone(),
            "previousClose": Value::Undefined,
            "change": Value::Undefined,
            "percentage": Value::Undefined,
            "average": Value::Undefined,
            "baseVolume": self.safe_string(ticker.clone(), Value::from("rolling_24_hour_volume"), Value::Undefined),
            "quoteVolume": Value::Undefined,
            "info": ticker.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        // Some exchanges wrap the ticker in {data: {...}} or {result: {...}} — unwrap if so.
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut tk = json_resp.clone();
        if !tk.is_array() && tk.get("last").is_none() && tk.get("close").is_none() && tk.get("bid").is_none() {
            for key in &["data", "result", "ticker"] {
                if let Some(v) = json_resp.get(key) {
                    if v.is_object() { tk = v.clone(); break; }
                    if let Some(arr) = v.as_array() {
                        if let Some(first) = arr.first() { tk = first.clone(); break; }
                    }
                }
            }
        }
        let market: Value = Value::Undefined;
        let parsed = <Self as Exchange>::parse_ticker(self, Value::Json(tk.clone()), market);
        if parsed.is_undefined() { Value::Json(tk) } else { parsed }
    }


    fn parse_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchTrades (public)
        //
        //      {
        //          "sequence":276989,
        //          "timestamp":1648651276949,
        //          "price":"35773.20000000",
        //          "volume":"0.00300000",
        //          "is_buy":false
        //      }
        //
        // fetchMyTrades (private)
        //
        //      {
        //          "pair":"LTCXBT",
        //          "sequence":3256813,
        //          "order_id":"BXEX6XHHDT5EGW2",
        //          "type":"ASK",
        //          "timestamp":1648652135235,
        //          "price":"0.002786",
        //          "volume":"0.10",
        //          "base":"0.10",
        //          "counter":"0.0002786",
        //          "fee_base":"0.0001",
        //          "fee_counter":"0.00",
        //          "is_buy":false,
        //          "client_order_id":""
        //      }
        //
        // For public trade data (is_buy === True) indicates 'buy' side but for private trade data
        // is_buy indicates maker or taker. The value of "type" (ASK/BID) indicate sell/buy side.
        // Private trade data includes ID field which public trade data does not.
        let mut order_id: Value = self.safe_string(trade.clone(), Value::from("order_id"), Value::Undefined);
        let mut id: Value = self.safe_string(trade.clone(), Value::from("sequence"), Value::Undefined);
        let mut taker_or_maker: Value = Value::Undefined;
        let mut side: Value = Value::Undefined;
        if order_id.clone().is_nonnullish() {
            let mut r#type: Value = self.safe_string(trade.clone(), Value::from("type"), Value::Undefined);
            if r#type.clone() == Value::from("ASK") || r#type.clone() == Value::from("SELL") {
                side = Value::from("sell");
            } else if r#type.clone() == Value::from("BID") || r#type.clone() == Value::from("BUY") {
                side = Value::from("buy");
            };
            if side.clone() == Value::from("sell") && trade.get(Value::from("is_buy")).is_truthy() {
                taker_or_maker = Value::from("maker");
            } else if side.clone() == Value::from("buy") && !trade.get(Value::from("is_buy")).is_truthy() {
                taker_or_maker = Value::from("maker");
            } else {
                taker_or_maker = Value::from("taker");
            };
        } else {
            side = if trade.get(Value::from("is_buy")).is_truthy() { Value::from("buy") } else { Value::from("sell") };
        };
        let mut fee_base_string: Value = self.safe_string(trade.clone(), Value::from("fee_base"), Value::Undefined);
        let mut fee_counter_string: Value = self.safe_string(trade.clone(), Value::from("fee_counter"), Value::Undefined);
        let mut fee_currency: Value = Value::Undefined;
        let mut fee_cost: Value = Value::Undefined;
        if fee_base_string.clone().is_nonnullish() {
            if !Precise::string_equals(fee_base_string.clone(), Value::from("0.0")) {
                fee_currency = market.get(Value::from("base"));
                fee_cost = fee_base_string.clone();
            };
        } else if fee_counter_string.clone().is_nonnullish() {
            if !Precise::string_equals(fee_counter_string.clone(), Value::from("0.0")) {
                fee_currency = market.get(Value::from("quote"));
                fee_cost = fee_counter_string.clone();
            };
        };
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("timestamp"), Value::Undefined);
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "info": trade.clone(),
            "id": id.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": market.get(Value::from("symbol")),
            "order": order_id.clone(),
            "type": Value::Undefined,
            "side": side.clone(),
            "takerOrMaker": taker_or_maker.clone(),
            "price": self.safe_string(trade.clone(), Value::from("price"), Value::Undefined),
            "amount": self.safe_string_2(trade.clone(), Value::from("volume"), Value::from("base"), Value::Undefined),
            "cost": self.safe_string(trade.clone(), Value::from("counter"), Value::Undefined),
            "fee": Value::Json(normalize(&Value::Json(json!({
                "cost": fee_cost.clone(),
                "currency": fee_currency.clone()
            }))).unwrap())
        }))).unwrap()), market.clone());
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        let mut raw: Value = Value::Undefined;
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { raw = rv; break; }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            // Already-parsed trades have both `price` and `amount` keys.
            if item.get("price").is_some() && item.get("amount").is_some() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_trade(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        // Match endpoints by substring on common OHLCV path tokens (klines, candles,
        // ohlcv, barhist, history, kline, candle, ohlc).
        'outer: for token in ["klines", "candles", "ohlcv", "barhist", "kline", "candle", "ohlc"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            if item.is_array() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_ohlcv(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        // {
        //     "timestamp": 1664055240000,
        //     "open": "19612.65",
        //     "close": "19612.65",
        //     "high": "19612.65",
        //     "low": "19612.65",
        //     "volume": "0.00"
        // }
        return Value::Json(serde_json::Value::Array(vec![(self.safe_integer(ohlcv.clone(), Value::from("timestamp"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("open"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("high"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("low"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("close"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("volume"), Value::Undefined)).into()]));
    }

    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["mytrades", "my_trades", "trades/history", "fills", "user/trades", "executions"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "myTrades"), ("private", "GET", "fills"), ("private", "GET", "trades/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_trading_fee(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "pair": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetFeeInfo".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //          "maker_fee": "0.00250000",
        //          "taker_fee": "0.00500000",
        //          "thirty_day_volume": "0"
        //     }
        //
        return Value::Json(normalize(&Value::Json(json!({
            "info": response.clone(),
            "symbol": symbol.clone(),
            "maker": self.safe_number(response.clone(), Value::from("maker_fee"), Value::Undefined),
            "taker": self.safe_number(response.clone(), Value::from("taker_fee"), Value::Undefined),
            "percentage": Value::Undefined,
            "tierBased": Value::Undefined
        }))).unwrap());
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("type".into(), r#type.clone());
        request.set("side".into(), side.clone());
        request.set("amount".into(), amount.clone());
        if price.is_nonnullish() { request.set("price".into(), price.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "POST" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "POST", "order"), ("private", "POST", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("orderId".into(), id.clone());
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Luno>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "DELETE" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "DELETE", "order"), ("private", "DELETE", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_ledger_by_entries(&mut self, mut code: Value, mut entry: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        // by default without entry number or limit number, return most recent entry
        if entry.clone().is_nullish() {
            entry = Value::from(1).neg();
        };
        if limit.clone().is_nullish() {
            limit = Value::from(1);
        };
        let mut since: Value = Value::Undefined;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "min_row": entry.clone(),
            "max_row": self.sum(entry.clone(), limit.clone())
        }))).unwrap());
        return <Self as Luno>::fetch_ledger(self, code.clone(), since.clone(), limit.clone(), extend_2(request.clone(), params.clone())).await;
    }

    async fn fetch_ledger(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        self.load_accounts(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = Value::Undefined;
        let mut id: Value = self.safe_string(params.clone(), Value::from("id"), Value::Undefined);
        // account id
        let mut min_row: Value = self.safe_value(params.clone(), Value::from("min_row"), Value::Undefined);
        let mut max_row: Value = self.safe_value(params.clone(), Value::from("max_row"), Value::Undefined);
        if id.clone().is_nullish() {
            if code.clone().is_nullish() {
                return Value::Undefined;
            };
            currency = self.currency(code.clone());
            let mut accounts_by_currency_code: Value = self.index_by(self.get("accounts".into()), Value::from("currency"));
            let mut account: Value = self.safe_value(accounts_by_currency_code.clone(), code.clone(), Value::Undefined);
            if account.clone().is_nullish() {
                return Value::Undefined;
            };
            id = account.get(Value::from("id"));
        };
        if min_row.clone().is_nullish() && max_row.clone().is_nullish() {
            max_row = Value::from(0);
            // Default to most recent transactions
            min_row = Value::from(1000).neg();
        } else if min_row.clone().is_nullish() || max_row.clone().is_nullish() {
            // Maximum number of records supported
            return Value::Undefined;
        };
        if limit.clone().is_nonnullish() && max_row.clone() - min_row.clone() > limit.clone() {
            if max_row.clone() <= Value::from(0) {
                min_row = max_row.clone() - limit.clone();
            } else if min_row.clone() > Value::from(0) {
                max_row = min_row.clone() + limit.clone();
            };
        };
        if max_row.clone() - min_row.clone() > Value::from(1000) {
            return Value::Undefined;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "min_row": min_row.clone(),
            "max_row": max_row.clone()
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetAccountsIdTransactions".into(), extend_2(params.clone(), request.clone()), Value::Undefined).await;
        let mut entries: Value = self.safe_value(response.clone(), Value::from("transactions"), Value::new_array());
        return self.parse_ledger(entries.clone(), currency.clone(), since.clone(), limit.clone(), Value::Undefined).await;
    }

    fn parse_ledger_comment(&self, mut comment: Value) -> Value {
        let mut words: Value = comment.split(Value::from(" "));
        let mut types: Value = Value::Json(normalize(&Value::Json(json!({
            "Withdrawal": Value::from("fee"),
            "Trading": Value::from("fee"),
            "Payment": Value::from("transaction"),
            "Sent": Value::from("transaction"),
            "Deposit": Value::from("transaction"),
            "Received": Value::from("transaction"),
            "Released": Value::from("released"),
            "Reserved": Value::from("reserved"),
            "Sold": Value::from("trade"),
            "Bought": Value::from("trade"),
            "Failure": Value::from("failed")
        }))).unwrap());
        let mut reference_id: Value = Value::Undefined;
        let mut first_word: Value = self.safe_string(words.clone(), Value::from(0), Value::Undefined);
        let mut third_word: Value = self.safe_string(words.clone(), Value::from(2), Value::Undefined);
        let mut fourth_word: Value = self.safe_string(words.clone(), Value::from(3), Value::Undefined);
        let mut r#type: Value = self.safe_string(types.clone(), first_word.clone(), Value::Undefined);
        if r#type.clone().is_nullish() && third_word.clone() == Value::from("fee") {
            r#type = Value::from("fee");
        };
        if r#type.clone() == Value::from("reserved") && fourth_word.clone() == Value::from("order") {
            reference_id = self.safe_string(words.clone(), Value::from(4), Value::Undefined);
        };
        return Value::Json(normalize(&Value::Json(json!({
            "type": r#type.clone(),
            "referenceId": reference_id.clone()
        }))).unwrap());
    }

    fn parse_ledger_entry(&self, mut entry: Value, mut currency: Value) -> Value {
        // const details = this.safeValue (entry, 'details', {});
        let mut id: Value = self.safe_string(entry.clone(), Value::from("row_index"), Value::Undefined);
        let mut account_id: Value = self.safe_string(entry.clone(), Value::from("account_id"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(entry.clone(), Value::from("timestamp"), Value::Undefined);
        let mut currency_id: Value = self.safe_string(entry.clone(), Value::from("currency"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), currency.clone());
        currency = self.safe_currency(currency_id.clone(), currency.clone());
        let mut available_delta: Value = self.safe_string(entry.clone(), Value::from("available_delta"), Value::Undefined);
        let mut balance_delta: Value = self.safe_string(entry.clone(), Value::from("balance_delta"), Value::Undefined);
        let mut after: Value = self.safe_string(entry.clone(), Value::from("balance"), Value::Undefined);
        let mut comment: Value = self.safe_string(entry.clone(), Value::from("description"), Value::Undefined);
        let mut before: Value = after.clone();
        let mut amount: Value = Value::from("0.0");
        let mut result: Value = <Self as Luno>::parse_ledger_comment(self, comment.clone());
        let mut r#type: Value = result.get(Value::from("type"));
        let mut reference_id: Value = result.get(Value::from("referenceId"));
        let mut direction: Value = Value::Undefined;
        let mut status: Value = Value::Undefined;
        if !Precise::string_equals(balance_delta.clone(), Value::from("0.0")) {
            before = Precise::string_sub(after.clone(), balance_delta.clone());
            status = Value::from("ok");
            amount = Precise::string_abs(balance_delta.clone());
        } else if Precise::string_lt(available_delta.clone(), Value::from("0.0")) {
            status = Value::from("pending");
            amount = Precise::string_abs(available_delta.clone());
        } else if Precise::string_gt(available_delta.clone(), Value::from("0.0")) {
            status = Value::from("canceled");
            amount = Precise::string_abs(available_delta.clone());
        };
        if Precise::string_gt(balance_delta.clone(), Value::from("0")) || Precise::string_gt(available_delta.clone(), Value::from("0")) {
            direction = Value::from("in");
        } else if Precise::string_lt(balance_delta.clone(), Value::from("0")) || Precise::string_lt(available_delta.clone(), Value::from("0")) {
            direction = Value::from("out");
        };
        return self.safe_ledger_entry(Value::Json(normalize(&Value::Json(json!({
            "info": entry.clone(),
            "id": id.clone(),
            "direction": direction.clone(),
            "account": account_id.clone(),
            "referenceId": reference_id.clone(),
            "referenceAccount": Value::Undefined,
            "type": r#type.clone(),
            "currency": code.clone(),
            "amount": self.parse_to_numeric(amount.clone(), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "before": self.parse_to_numeric(before.clone(), Value::Undefined),
            "after": self.parse_to_numeric(after.clone(), Value::Undefined),
            "status": status.clone(),
            "fee": Value::Undefined
        }))).unwrap()), currency.clone());
    }

    async fn create_deposit_address(&mut self, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "asset": currency.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privatePostFundingAddress".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "account_id": "string",
        //         "address": "string",
        //         "address_meta": [
        //             {
        //                 "label": "string",
        //                 "value": "string"
        //             }
        //         ],
        //         "asset": "string",
        //         "assigned_at": 0,
        //         "name": "string",
        //         "network": 0,
        //         "qr_code_uri": "string",
        //         "receive_fee": "string",
        //         "total_received": "string",
        //         "total_unconfirmed": "string"
        //     }
        //
        return <Self as Luno>::parse_deposit_address(self, response.clone(), currency.clone()).await;
    }

    async fn fetch_deposit_address(&mut self, mut code: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "asset": currency.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("privateGetFundingAddress".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {
        //         "account_id": "string",
        //         "address": "string",
        //         "address_meta": [
        //             {
        //                 "label": "string",
        //                 "value": "string"
        //             }
        //         ],
        //         "asset": "string",
        //         "assigned_at": 0,
        //         "name": "string",
        //         "network": 0,
        //         "qr_code_uri": "string",
        //         "receive_fee": "string",
        //         "total_received": "string",
        //         "total_unconfirmed": "string"
        //     }
        //
        return <Self as Luno>::parse_deposit_address(self, response.clone(), currency.clone()).await;
    }

    fn parse_deposit_address(&self, mut deposit_address: Value, mut currency: Value) -> Value {
        //
        //     {
        //         "account_id": "string",
        //         "address": "string",
        //         "address_meta": [
        //             {
        //                 "label": "string",
        //                 "value": "string"
        //             }
        //         ],
        //         "asset": "string",
        //         "assigned_at": 0,
        //         "name": "string",
        //         "network": 0,
        //         "qr_code_uri": "string",
        //         "receive_fee": "string",
        //         "total_received": "string",
        //         "total_unconfirmed": "string"
        //     }
        //
        let mut currency_id: Value = self.safe_string_upper(deposit_address.clone(), Value::from("currency"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), currency.clone());
        return Value::Json(normalize(&Value::Json(json!({
            "info": deposit_address.clone(),
            "currency": code.clone(),
            "network": Value::Undefined,
            "address": self.safe_string(deposit_address.clone(), Value::from("address"), Value::Undefined),
            "tag": self.safe_string(deposit_address.clone(), Value::from("name"), Value::Undefined)
        }))).unwrap());
    }

    

    

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_str() {
                    "exchangeGetMarkets" => self.request("markets".into(), "exchange".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "exchangeprivateGetCandles" => self.request("candles".into(), "exchangePrivate".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOrderbook" => self.request("orderbook".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetOrderbooktop" => self.request("orderbook_top".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTicker" => self.request("ticker".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTickers" => self.request("tickers".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "publicGetTrades" => self.request("trades".into(), "public".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountsidpending" => self.request("accounts/{id}/pending".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetAccountsidtransactions" => self.request("accounts/{id}/transactions".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetBalance" => self.request("balance".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetBeneficiaries" => self.request("beneficiaries".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSendnetworks" => self.request("send/networks".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetFeeinfo" => self.request("fee_info".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetFundingaddress" => self.request("funding_address".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetListorders" => self.request("listorders".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetListtrades" => self.request("listtrades".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetSendfee" => self.request("send_fee".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetOrdersid" => self.request("orders/{id}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWithdrawals" => self.request("withdrawals".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetWithdrawalsid" => self.request("withdrawals/{id}".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateGetTransfers" => self.request("transfers".into(), "private".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAccounts" => self.request("accounts".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostAddressvalidate" => self.request("address/validate".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostPostorder" => self.request("postorder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostMarketorder" => self.request("marketorder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostStoporder" => self.request("stoporder".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostFundingaddress" => self.request("funding_address".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostWithdrawals" => self.request("withdrawals".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostSend" => self.request("send".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostOauth2grant" => self.request("oauth2/grant".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePostBeneficiaries" => self.request("beneficiaries".into(), "private".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privatePutAccountsidname" => self.request("accounts/{id}/name".into(), "private".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteWithdrawalsid" => self.request("withdrawals/{id}".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "privateDeleteBeneficiariesid" => self.request("beneficiaries/{id}".into(), "private".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct LunoImpl(Value);
#[async_trait(?Send)]
impl Exchange for LunoImpl {
    fn describe(&self) -> Value { Luno::describe(self) }
    async fn fetch_ticker(&mut self, symbol: Value, params: Value) -> Value { Luno::fetch_ticker(self, symbol, params).await }
    async fn fetch_tickers(&mut self, symbols: Value, params: Value) -> Value { Luno::fetch_tickers(self, symbols, params).await }
    async fn fetch_order_book(&mut self, symbol: Value, limit: Value, params: Value) -> Value { Luno::fetch_order_book(self, symbol, limit, params).await }
    async fn fetch_ohlcv(&mut self, symbol: Value, timeframe: Value, since: Value, limit: Value, params: Value) -> Value { Luno::fetch_ohlcv(self, symbol, timeframe, since, limit, params).await }
    async fn fetch_trades(&mut self, symbol: Value, since: Value, limit: Value, params: Value) -> Value { Luno::fetch_trades(self, symbol, since, limit, params).await }
    fn parse_trade(&self, trade: Value, market: Value) -> Value { Luno::parse_trade(self, trade, market) }
    fn parse_order(&self, order: Value, market: Value) -> Value { Luno::parse_order(self, order, market) }
    fn parse_ticker(&self, ticker: Value, market: Value) -> Value { Luno::parse_ticker(self, ticker, market) }
    fn parse_balance(&self, response: Value) -> Value { Luno::parse_balance(self, response) }
    fn parse_deposit_address(&self, deposit_address: Value, currency: Value) -> Value { Luno::parse_deposit_address(self, deposit_address, currency) }
    fn parse_ledger_entry(&self, item: Value, currency: Value) -> Value { Luno::parse_ledger_entry(self, item, currency) }
    fn parse_order_status(&self, status: Value) -> Value { Luno::parse_order_status(self, status) }
}
impl Luno for LunoImpl {}
impl ValueTrait for LunoImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl LunoImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = LunoImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Luno::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


#![allow(clippy::all)]
#![allow(dead_code)]
#![allow(unreachable_code)]
#![allow(unused_imports)]
#![allow(unused_assignments)]
#![allow(unused_comparisons)]
#![allow(unused_mut)]
#![allow(unused_variables)]

use async_trait::async_trait;
use std::str::FromStr;
use serde::{Deserialize, Serialize};
use serde_json::json;
use crate::exchange::{Exchange, ExchangeImpl, Precise, Value, ValueTrait, BoolExt, JSON, Array, Object, Math, Promise, parse_int, shift_2, extend_2, normalize};
use crate::ws::{ArrayCache, ArrayCacheByTimestamp, ArrayCacheBySymbolById, ArrayCacheBySymbolBySide};
// Crypto hash identifiers
fn sha256() -> Value { Value::from("sha256()") }
fn sha384() -> Value { Value::from("sha384()") }
fn sha512() -> Value { Value::from("sha512()") }
fn md5() -> Value { Value::from("md5()") }
fn ed25519() -> Value { Value::from("ed25519()") }
fn rsa(msg: Value, secret: Value, _hash: Value) -> Value { msg }
fn eddsa(msg: Value, secret: Value, _curve: Value) -> Value { msg }
fn secp256k1() -> Value { Value::from("secp256k1()") }
fn keccak() -> Value { Value::from("keccak()") }
fn ecdsa(msg: Value, secret: Value, algo: Value, hash_fn: Value) -> Value { msg }
fn totp(secret: Value) -> Value { Value::Undefined }
fn jwt(data: Value, secret: Value, hash: Value, is_rsa: Value) -> Value { Value::Undefined }
fn parse_float(value: Value) -> Value { value }
fn decimals(value: Value) -> Value { Value::from(0) }
fn shift_1(value: Value) -> Value { value }
fn shift_3(value: Value) -> (Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined) }
fn shift_4(value: Value) -> (Value, Value, Value, Value) { (value.clone(), Value::Undefined, Value::Undefined, Value::Undefined) }
// Error type constructors (stub structs with ::new for transpiled `new ErrorType(msg)` patterns)
macro_rules! error_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(msg: Value) -> Value { msg } } }; }
error_stub!(BadRequest);
error_stub!(InvalidOrder);
error_stub!(ExchangeError);
error_stub!(InsufficientFunds);
error_stub!(OrderNotFound);
error_stub!(AuthenticationError);
error_stub!(PermissionDenied);
error_stub!(ExchangeNotAvailable);
error_stub!(ArgumentsRequired);
error_stub!(RateLimitExceeded);
error_stub!(OrderNotFillable);
error_stub!(OrderImmediatelyFillable);
error_stub!(NotSupported);
error_stub!(DuplicateOrderId);
error_stub!(UnsubscribeError);
error_stub!(ChecksumError);
error_stub!(InvalidNonce);
error_stub!(BadSymbol);
// Array-like type stubs (Bids/Asks order book sides)
macro_rules! array_side_stub { ($name:ident) => { pub struct $name; impl $name { pub fn new(data: Value, _limit: Value) -> Value { data } } }; }
array_side_stub!(Bids);
array_side_stub!(Asks);

use crate::exchange::{PRECISE_BASE, TRUNCATE, ROUND, ROUND_UP, ROUND_DOWN};
use crate::exchange::{DECIMAL_PLACES, SIGNIFICANT_DIGITS, TICK_SIZE, NO_PADDING, PAD_WITH_ZERO};

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

#[async_trait(?Send)]
pub trait Defx : Exchange {
    fn describe(&self) -> Value {
        Value::Json(serde_json::Value::from_str(r###"{
            "id": "defx",
            "name": "Defx X",
            "enableRateLimit": true,
            "rateLimit": 100,
            "timeout": 10000,
            "certified": false,
            "pro": false,
            "alias": false,
            "dex": true,
            "has": {
                "publicAPI": true,
                "privateAPI": true,
                "sandbox": true,
                "spot": false,
                "margin": false,
                "swap": true,
                "future": false,
                "option": false,
                "addMargin": true,
                "cancelAllOrders": true,
                "cancelOrder": true,
                "closeAllPositions": true,
                "closePosition": true,
                "createDepositAddress": false,
                "createLimitOrder": true,
                "createMarketBuyOrderWithCost": false,
                "createMarketOrder": false,
                "createMarketOrderWs": true,
                "createMarketOrderWithCost": false,
                "createMarketSellOrderWithCost": false,
                "createOrder": true,
                "createOrderWithTakeProfitAndStopLoss": true,
                "createReduceOnlyOrder": true,
                "createStopLimitOrder": false,
                "createStopLossOrder": false,
                "createStopMarketOrder": false,
                "createStopOrder": false,
                "createTakeProfitOrder": true,
                "createTrailingAmountOrder": false,
                "createTrailingPercentOrder": false,
                "createTriggerOrder": true,
                "editOrder": "emulated",
                "fetchAccounts": false,
                "fetchBalance": true,
                "fetchCanceledOrders": true,
                "fetchClosedOrder": false,
                "fetchClosedOrders": true,
                "fetchConvertCurrencies": false,
                "fetchConvertQuote": false,
                "fetchConvertTrade": false,
                "fetchConvertTradeHistory": false,
                "fetchCurrencies": false,
                "fetchCurrenciesWs": "emulated",
                "fetchDepositAddress": false,
                "fetchDepositAddresses": false,
                "fetchDepositAddressesByNetwork": false,
                "fetchDeposits": false,
                "fetchDepositsWithdrawals": false,
                "fetchFundingHistory": false,
                "fetchFundingRate": true,
                "fetchFundingRateHistory": false,
                "fetchFundingInterval": false,
                "fetchFundingIntervals": false,
                "fetchFundingRates": false,
                "fetchIndexOHLCV": false,
                "fetchMarginAdjustmentHistory": false,
                "fetchL2OrderBook": true,
                "fetchLedger": true,
                "fetchLeverage": false,
                "fetchMarginMode": false,
                "fetchMarkets": true,
                "fetchMarkOHLCV": false,
                "fetchMyTrades": true,
                "fetchOHLCV": true,
                "fetchOpenInterestHistory": false,
                "fetchOpenOrder": false,
                "fetchOpenOrders": true,
                "fetchOrder": true,
                "fetchOrderBook": true,
                "fetchOrders": true,
                "fetchOrderTrades": false,
                "fetchPosition": true,
                "fetchPositionHistory": false,
                "fetchPositionsHistory": false,
                "fetchPositionMode": false,
                "fetchPositions": true,
                "fetchPremiumIndexOHLCV": false,
                "fetchStatus": true,
                "fetchTicker": true,
                "fetchTickers": true,
                "fetchMarkPrices": false,
                "fetchTime": true,
                "fetchTrades": true,
                "fetchTradingFee": false,
                "fetchTradingFees": false,
                "fetchTransactions": false,
                "fetchTransfers": false,
                "fetchWithdrawals": false,
                "reduceMargin": false,
                "setLeverage": true,
                "setMargin": false,
                "setPositionMode": false,
                "transfer": false,
                "withdraw": true,
                "cancelAllOrdersAfter": false,
                "cancelWithdraw": false,
                "createConvertTrade": false,
                "fetchMarkPrice": false
            },
            "urls": {
                "logo": "https://github.com/user-attachments/assets/4e92bace-d7a9-45ea-92be-122168dc87e4",
                "api": {
                    "public": "https://api.{hostname}",
                    "private": "https://api.{hostname}"
                },
                "www": "https://defx.com/home",
                "doc": [
                    "https://docs.defx.com/docs",
                    "https://api-docs.defx.com/"
                ],
                "fees": [
                    ""
                ],
                "test": {
                    "public": "https://api.testnet.{hostname}",
                    "private": "https://api.testnet.{hostname}"
                },
                "referral": {
                    "url": "https://app.defx.com/join/6I2CZ7"
                }
            },
            "api": {
                "v1": {
                    "public": {
                        "get": {
                            "healthcheck/ping": 1,
                            "symbols/{symbol}/ohlc": 1,
                            "symbols/{symbol}/trades": 1,
                            "symbols/{symbol}/prices": 1,
                            "symbols/{symbol}/ticker/24hr": 1,
                            "symbols/{symbol}/depth/{level}/{slab}": 1,
                            "ticker/24HrAgg": 1,
                            "c/markets": 1,
                            "c/markets/metadata": 1,
                            "analytics/market/stats/newUsers": 1,
                            "analytics/market/stats/tvl": 1,
                            "analytics/market/stats/volumeByInstrument": 1,
                            "analytics/market/stats/liquidation": 1,
                            "analytics/market/stats/totalVolume": 1,
                            "analytics/market/stats/openInterest": 1,
                            "analytics/market/stats/totalTrades": 1,
                            "analytics/market/stats/basis": 1,
                            "analytics/market/stats/insuranceFund": 1,
                            "analytics/market/stats/longAndShortRatio": 1,
                            "analytics/market/stats/fundingRate": 1,
                            "analytics/market/overview": 1,
                            "explorer/search": 1,
                            "explorer/transactions": 1,
                            "explorer/blocks": 1
                        }
                    },
                    "private": {
                        "get": {
                            "api/order/{orderId}": 1,
                            "api/orders": 1,
                            "api/orders/oco/{parentOrderId}": 1,
                            "api/trades": 1,
                            "api/position/active": 1,
                            "api/users/metadata/leverage": 1,
                            "api/users/metadata/feeMultiplier": 1,
                            "api/users/metadata/slippage": 1,
                            "api/users/referral": 1,
                            "api/users/apikeys": 1,
                            "connection-signature-message/evm": 1,
                            "api/users/profile/wallets": 1,
                            "api/notifications": 1,
                            "api/wallet/balance": 1,
                            "api/wallet/transactions": 1,
                            "api/analytics/user/overview": 1,
                            "api/analytics/user/pnl": 1,
                            "api/analytics/points/overview": 1,
                            "api/analytics/points/history": 1
                        },
                        "post": {
                            "api/order": 1,
                            "api/position/oco": 1,
                            "api/users/socket/listenKeys": 1,
                            "api/users/metadata/leverage": 1,
                            "api/users/metadata/feeMultiplier": 1,
                            "api/users/metadata/slippage": 1,
                            "api/users/referral/recordReferralSignup": 1,
                            "api/users/apikeys": 1,
                            "api/users/profile/wallets": 1,
                            "api/transfers/withdrawal": 1,
                            "api/transfers/bridge/withdrawal": 1
                        },
                        "put": {
                            "api/position/updatePositionMargin": 1,
                            "api/users/socket/listenKeys/{listenKey}": 1,
                            "api/users/apikeys/{accessKey}/status": 1,
                            "api/users/referral": 1
                        },
                        "patch": {
                            "api/users/apikeys/{accessKey}": 1
                        },
                        "delete": {
                            "api/orders/allOpen": 1,
                            "api/order/{orderId}": 1,
                            "api/position/{positionId}": 1,
                            "api/position/all": 1,
                            "api/users/socket/listenKeys/{listenKey}": 1,
                            "api/users/apikeys/{accessKey}": 1
                        }
                    }
                }
            },
            "requiredCredentials": {
                "apiKey": true,
                "secret": true,
                "uid": false,
                "accountId": false,
                "login": false,
                "password": false,
                "twofa": false,
                "privateKey": false,
                "walletAddress": false,
                "token": false
            },
            "currencies": {},
            "timeframes": {
                "1m": "1m",
                "3m": "3m",
                "5m": "5m",
                "15m": "15m",
                "30m": "30m",
                "1h": "1h",
                "2h": "2h",
                "4h": "4h",
                "12h": "12h",
                "1d": "1d",
                "1w": "1w",
                "1M": "1M"
            },
            "fees": {
                "trading": {
                    "tierBased": true,
                    "percentage": true,
                    "taker": 0.0005,
                    "maker": 0.0002
                },
                "funding": {
                    "withdraw": {},
                    "deposit": {}
                }
            },
            "status": {
                "status": "ok"
            },
            "exceptions": {
                "exact": {},
                "broad": {}
            },
            "httpExceptions": {},
            "commonCurrencies": {
                "XBT": "BTC",
                "BCHSV": "BSV"
            },
            "precisionMode": 4,
            "paddingMode": 5,
            "limits": {
                "leverage": {},
                "amount": {},
                "price": {},
                "cost": {}
            },
            "rollingWindowSize": 60000,
            "version": "v1",
            "hostname": "defx.com",
            "options": {
                "sandboxMode": false
            },
            "features": {
                "forDerivatives": {
                    "sandbox": true,
                    "createOrder": {
                        "marginMode": false,
                        "triggerPrice": true,
                        "triggerPriceType": {
                            "last": true,
                            "mark": true,
                            "index": false
                        },
                        "triggerDirection": false,
                        "stopLossPrice": false,
                        "takeProfitPrice": false,
                        "timeInForce": {
                            "IOC": true,
                            "FOK": true,
                            "PO": true,
                            "GTD": false
                        },
                        "hedged": false,
                        "selfTradePrevention": false,
                        "trailing": false,
                        "iceberg": false,
                        "leverage": false,
                        "marketBuyByCost": false,
                        "marketBuyRequiresPrice": false
                    },
                    "fetchMyTrades": {
                        "marginMode": false,
                        "limit": 1000,
                        "symbolRequired": false
                    },
                    "fetchOrder": {
                        "marginMode": false,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOpenOrders": {
                        "marginMode": true,
                        "limit": 100,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "daysBack": 100000,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchClosedOrders": {
                        "marginMode": false,
                        "limit": 500,
                        "daysBack": 100000,
                        "daysBackCanceled": 1,
                        "untilDays": 100000,
                        "trigger": false,
                        "trailing": false,
                        "symbolRequired": false
                    },
                    "fetchOHLCV": {
                        "limit": 1000
                    }
                },
                "swap": {
                    "linear": {
                        "extends": "forDerivatives"
                    }
                },
                "future": {}
            }
        }"###).unwrap())
    }

    async fn fetch_status(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["status", "ping", "time", "system/status"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "status"), ("public", "GET", "ping"), ("public", "GET", "time"), ("sapi", "GET", "system/status")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_time(&mut self, mut params: Value) -> Value {
        let candidates = vec![("public", "GET", "time"), ("public", "GET", "server/time"), ("public", "GET", "timestamp")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    

    fn parse_market(&self, mut market: Value) -> Value {
        let mut market_id: Value = self.safe_string(market.clone(), Value::from("market"), Value::Undefined);
        let mut parts: Value = market_id.split(Value::from("_"));
        let mut base_id: Value = self.safe_string(parts.clone(), Value::from(0), Value::Undefined);
        let mut quote_id: Value = self.safe_string(parts.clone(), Value::from(1), Value::Undefined);
        let mut base: Value = self.safe_currency_code(base_id.clone(), Value::Undefined);
        let mut quote: Value = self.safe_currency_code(quote_id.clone(), Value::Undefined);
        let mut symbol: Value = base.clone() + Value::from("/") + quote.clone() + Value::from(":") + quote.clone();
        let mut filters: Value = self.safe_list(market.clone(), Value::from("filters"), Value::new_array());
        let mut fees: Value = self.safe_dict(market.clone(), Value::from("fees"), Value::new_object());
        let mut filters_by_type: Value = self.index_by(filters.clone(), Value::from("filterType"));
        let mut price_filter: Value = self.safe_dict(filters_by_type.clone(), Value::from("PRICE_FILTER"), Value::new_object());
        let mut lot_filter: Value = self.safe_dict(filters_by_type.clone(), Value::from("LOT_SIZE"), Value::new_object());
        let mut market_lot_filter: Value = self.safe_dict(filters_by_type.clone(), Value::from("MARKET_LOT_SIZE"), Value::new_object());
        let mut notional_filter: Value = self.safe_dict(filters_by_type.clone(), Value::from("NOTIONAL"), Value::new_object());
        return Value::Json(normalize(&Value::Json(json!({
            "id": market_id.clone(),
            "symbol": symbol.clone(),
            "base": base.clone(),
            "quote": quote.clone(),
            "settle": quote.clone(),
            "baseId": base_id.clone(),
            "quoteId": quote_id.clone(),
            "settleId": quote_id.clone(),
            "type": Value::from("swap"),
            "spot": Value::from(false),
            "margin": Value::from(false),
            "swap": Value::from(true),
            "future": Value::from(false),
            "option": Value::from(false),
            "active": Value::from(self.safe_string(market.clone(), Value::from("status"), Value::from("")) == Value::from("active")),
            "contract": Value::from(true),
            "linear": Value::from(true),
            "inverse": Value::from(false),
            "taker": self.safe_number(fees.clone(), Value::from("taker"), Value::Undefined),
            "maker": self.safe_number(fees.clone(), Value::from("maker"), Value::Undefined),
            "contractSize": self.parse_number(Value::from("1"), Value::Undefined),
            "expiry": Value::Undefined,
            "expiryDatetime": Value::Undefined,
            "strike": Value::Undefined,
            "optionType": Value::Undefined,
            "precision": Value::Json(normalize(&Value::Json(json!({
                "amount": self.safe_number(lot_filter.clone(), Value::from("stepSize"), Value::Undefined),
                "price": self.safe_number(price_filter.clone(), Value::from("tickSize"), Value::Undefined)
            }))).unwrap()),
            "limits": Value::Json(normalize(&Value::Json(json!({
                "leverage": Value::Json(normalize(&Value::Json(json!({
                    "min": Value::Undefined,
                    "max": self.safe_number(market.clone(), Value::from("cappedLeverage"), Value::Undefined)
                }))).unwrap()),
                "amount": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(lot_filter.clone(), Value::from("minQty"), Value::Undefined),
                    "max": self.safe_number(lot_filter.clone(), Value::from("maxQty"), Value::Undefined)
                }))).unwrap()),
                "price": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(price_filter.clone(), Value::from("minPrice"), Value::Undefined),
                    "max": self.safe_number(price_filter.clone(), Value::from("maxPrice"), Value::Undefined)
                }))).unwrap()),
                "cost": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(notional_filter.clone(), Value::from("minNotional"), Value::Undefined),
                    "max": Value::Undefined
                }))).unwrap()),
                "market": Value::Json(normalize(&Value::Json(json!({
                    "min": self.safe_number(market_lot_filter.clone(), Value::from("minQty"), Value::Undefined),
                    "max": self.safe_number(market_lot_filter.clone(), Value::from("maxQty"), Value::Undefined)
                }))).unwrap())
            }))).unwrap()),
            "created": Value::Undefined,
            "info": market.clone()
        }))).unwrap());
    }

    async fn fetch_ticker(&mut self, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["ticker/24hr", "ticker", "ticker/price", "bookticker", "tickers"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "ticker/24hr"), ("public", "GET", "ticker"), ("public", "GET", "ticker/price")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        // Some exchanges wrap the ticker in {data: {...}} or {result: {...}} — unwrap if so.
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut tk = json_resp.clone();
        if !tk.is_array() && tk.get("last").is_none() && tk.get("close").is_none() && tk.get("bid").is_none() {
            for key in &["data", "result", "ticker"] {
                if let Some(v) = json_resp.get(key) {
                    if v.is_object() { tk = v.clone(); break; }
                    if let Some(arr) = v.as_array() {
                        if let Some(first) = arr.first() { tk = first.clone(); break; }
                    }
                }
            }
        }
        let market: Value = Value::Undefined;
        let parsed = <Self as Exchange>::parse_ticker(self, Value::Json(tk.clone()), market);
        if parsed.is_undefined() { Value::Json(tk) } else { parsed }
    }


    async fn fetch_tickers(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["tickers", "ticker/24hr", "ticker", "bookticker"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("public", "GET", "ticker/24hr"), ("public", "GET", "tickers"), ("public", "GET", "ticker")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), params.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_ticker(&self, mut ticker: Value, mut market: Value) -> Value {
        //
        // fetchTicker
        //
        // {
        //     "symbol": "BTC_USDC",
        //     "priceChange": "0",
        //     "priceChangePercent": "0",
        //     "weightedAvgPrice": "0",
        //     "lastPrice": "2.00",
        //     "lastQty": "10.000",
        //     "bestBidPrice": "1646.00",
        //     "bestBidQty": "10.000",
        //     "bestAskPrice": "1646.00",
        //     "bestAskQty": "10.000",
        //     "openPrice": "0.00",
        //     "highPrice": "0.00",
        //     "lowPrice": "0.00",
        //     "volume": "0.000",
        //     "quoteVolume": "0.00",
        //     "openTime": 1700142658697,
        //     "closeTime": 1700142658697,
        //     "openInterestBase": "1.000",
        //     "openInterestQuote": "0.43112300"
        // }
        //
        // fetchTickers
        //
        //     "ETH_USDC": {
        //       "priceChange": "0",
        //       "priceChangePercent": "0",
        //       "openPrice": "1646.15",
        //       "highPrice": "1646.15",
        //       "lowPrice": "1646.15",
        //       "lastPrice": "1646.15",
        //       "quoteVolume": "13.17",
        //       "volume": "0.008",
        //       "markPrice": "1645.15"
        //     }
        //
        // fetchMarkPrice
        //
        // {
        //     "markPrice": "100.00",
        //     "indexPrice": "100.00",
        //     "ltp": "101.34",
        //     "movingFundingRate": "0.08",
        //     "payoutFundingRate": "-0.03",
        //     "nextFundingPayout": 1711555532146
        // }
        //
        let mut market_id: Value = self.safe_string(ticker.clone(), Value::from("symbol"), Value::Undefined);
        if market_id.clone().is_nonnullish() {
            market = self.market(market_id.clone());
        };
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut open: Value = self.safe_string(ticker.clone(), Value::from("openPrice"), Value::Undefined);
        let mut high: Value = self.safe_string(ticker.clone(), Value::from("highPrice"), Value::Undefined);
        let mut low: Value = self.safe_string(ticker.clone(), Value::from("lowPrice"), Value::Undefined);
        let mut close: Value = self.safe_string(ticker.clone(), Value::from("lastPrice"), Value::Undefined);
        let mut quote_volume: Value = self.safe_string(ticker.clone(), Value::from("quoteVolume"), Value::Undefined);
        let mut base_volume: Value = self.safe_string(ticker.clone(), Value::from("volume"), Value::Undefined);
        let mut percentage: Value = self.safe_string(ticker.clone(), Value::from("priceChangePercent"), Value::Undefined);
        let mut change: Value = self.safe_string(ticker.clone(), Value::from("priceChange"), Value::Undefined);
        let mut ts: Value = self.safe_integer(ticker.clone(), Value::from("closeTime"), Value::Undefined);
        if ts.clone() == Value::from(0) {
            ts = Value::Undefined;
        };
        let mut datetime: Value = self.iso8601(ts.clone());
        let mut bid: Value = self.safe_string(ticker.clone(), Value::from("bestBidPrice"), Value::Undefined);
        let mut bid_volume: Value = self.safe_string(ticker.clone(), Value::from("bestBidQty"), Value::Undefined);
        let mut ask: Value = self.safe_string(ticker.clone(), Value::from("bestAskPrice"), Value::Undefined);
        let mut ask_volume: Value = self.safe_string(ticker.clone(), Value::from("bestAskQty"), Value::Undefined);
        return self.safe_ticker(Value::Json(normalize(&Value::Json(json!({
            "symbol": symbol.clone(),
            "timestamp": ts.clone(),
            "datetime": datetime.clone(),
            "high": high.clone(),
            "low": low.clone(),
            "bid": bid.clone(),
            "bidVolume": bid_volume.clone(),
            "ask": ask.clone(),
            "askVolume": ask_volume.clone(),
            "vwap": Value::Undefined,
            "open": open.clone(),
            "close": close.clone(),
            "last": Value::Undefined,
            "previousClose": Value::Undefined,
            "change": change.clone(),
            "percentage": percentage.clone(),
            "average": Value::Undefined,
            "baseVolume": base_volume.clone(),
            "quoteVolume": quote_volume.clone(),
            "markPrice": self.safe_string(ticker.clone(), Value::from("markPrice"), Value::Undefined),
            "indexPrice": self.safe_string(ticker.clone(), Value::from("indexPrice"), Value::Undefined),
            "info": ticker.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_ohlcv(&mut self, mut symbol: Value, mut timeframe: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("timeframe".into(), timeframe.clone());
        request.set("interval".into(), timeframe.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        // Match endpoints by substring on common OHLCV path tokens (klines, candles,
        // ohlcv, barhist, history, kline, candle, ohlc).
        'outer: for token in ["klines", "candles", "ohlcv", "barhist", "kline", "candle", "ohlc"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "klines"), ("public", "GET", "candles"), ("public", "GET", "ohlcv")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            if item.is_array() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_ohlcv(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    fn parse_ohlcv(&self, mut ohlcv: Value, mut market: Value) -> Value {
        // example response in fetchOHLCV
        return Value::Json(serde_json::Value::Array(vec![(self.safe_integer(ohlcv.clone(), Value::from("start"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("open"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("high"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("low"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("close"), Value::Undefined)).into(), (self.safe_number(ohlcv.clone(), Value::from("volume"), Value::Undefined)).into()]));
    }

    async fn fetch_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let candidates = vec![("public", "GET", "trades"), ("public", "GET", "recent_trades"), ("public", "GET", "aggTrades")];
        let mut raw: Value = Value::Undefined;
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { raw = rv; break; }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let arr_val = crate::exchange::unwrap_response_array(&json_resp);
        let items = match arr_val.as_array() { Some(a) => a.clone(), None => return raw };
        let market: Value = Value::Undefined;
        let mut parsed: Vec<serde_json::Value> = Vec::with_capacity(items.len());
        for item in items {
            // Already-parsed trades have both `price` and `amount` keys.
            if item.get("price").is_some() && item.get("amount").is_some() {
                parsed.push(item);
            } else {
                let pv = <Self as Exchange>::parse_trade(self, Value::Json(item.clone()), market.clone());
                if let Value::Json(j) = pv { parsed.push(j); }
            }
        }
        Value::Json(serde_json::Value::Array(parsed))
    }


    async fn fetch_my_trades(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["mytrades", "my_trades", "trades/history", "fills", "user/trades", "executions"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "myTrades"), ("private", "GET", "fills"), ("private", "GET", "trades/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_trade(&self, mut trade: Value, mut market: Value) -> Value {
        //
        // fetchTrades
        //     {
        //       "buyerMaker": "false",
        //       "price": "2.0000",
        //       "qty": "10.0000",
        //       "symbol": "BTC_USDC",
        //       "timestamp": "1702453663894"
        //     }
        //
        // fetchMyTrades
        //     {
        //         "id": "0192f665-c05b-7ba0-a080-8b6c99083489",
        //         "orderId": "757730811259651728",
        //         "time": "2024-11-04T08:58:36.474Z",
        //         "symbol": "SOL_USDC",
        //         "side": "SELL",
        //         "price": "160.43600000",
        //         "qty": "1.00",
        //         "fee": "0.08823980",
        //         "role": "TAKER",
        //         "pnl": "0.00000000"
        //     }
        //
        let mut time: Value = self.safe_string(trade.clone(), Value::from("time"), Value::Undefined);
        let mut timestamp: Value = self.safe_integer(trade.clone(), Value::from("timestamp"), self.parse8601(time.clone()));
        let mut market_id: Value = self.safe_string(trade.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut price: Value = self.safe_string(trade.clone(), Value::from("price"), Value::Undefined);
        let mut amount: Value = self.safe_string(trade.clone(), Value::from("qty"), Value::Undefined);
        let mut id: Value = self.safe_string(trade.clone(), Value::from("id"), Value::Undefined);
        let mut oid: Value = self.safe_string(trade.clone(), Value::from("orderId"), Value::Undefined);
        let mut taker_or_maker: Value = self.safe_string_lower(trade.clone(), Value::from("role"), Value::Undefined);
        let mut buyer_maker: Value = self.safe_bool(trade.clone(), Value::from("buyerMaker"), Value::Undefined);
        let mut side: Value = self.safe_string_lower(trade.clone(), Value::from("side"), Value::Undefined);
        if buyer_maker.clone().is_nonnullish() {
            if buyer_maker.is_truthy() {
                side = Value::from("sell");
            } else {
                side = Value::from("buy");
            };
        };
        return self.safe_trade(Value::Json(normalize(&Value::Json(json!({
            "id": id.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "symbol": symbol.clone(),
            "side": side.clone(),
            "price": price.clone(),
            "amount": amount.clone(),
            "cost": Value::Undefined,
            "order": oid.clone(),
            "takerOrMaker": taker_or_maker.clone(),
            "type": Value::Undefined,
            "fee": Value::Json(normalize(&Value::Json(json!({
                "cost": self.safe_string(trade.clone(), Value::from("fee"), Value::Undefined),
                "currency": Value::from("USDC")
            }))).unwrap()),
            "info": trade.clone()
        }))).unwrap()), market.clone());
    }

    async fn fetch_order_book(&mut self, mut symbol: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        let mut raw: Value = Value::Undefined;
        'outer: for token in ["depth", "orderbook", "order_book"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { raw = rv; break 'outer; }
                }
            }
        }
        if raw.is_undefined() {
            for (api_name, method_name, path_name) in [("public", "GET", "depth"), ("public", "GET", "orderbook"), ("public", "GET", "order_book")] {
                let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                if !rv.is_undefined() { raw = rv; break; }
            }
        }
        if raw.is_undefined() { return Value::Undefined; }
        let json_resp = match &raw { Value::Json(j) => j.clone(), _ => return raw };
        let mut ob = crate::exchange::unwrap_response_order_book(&json_resp);
        if let serde_json::Value::Object(ref mut map) = ob {
            if !map.contains_key("symbol") { map.insert("symbol".to_string(), serde_json::json!(symbol.unwrap_str())); }
        }
        Value::Json(ob)
    }


    async fn fetch_mark_price(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("v1PublicGetSymbolsSymbolPrices".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "markPrice": "100.00",
        //     "indexPrice": "100.00",
        //     "ltp": "101.34",
        //     "movingFundingRate": "0.08",
        //     "payoutFundingRate": "-0.03",
        //     "nextFundingPayout": 1711555532146
        // }
        //
        return <Self as Defx>::parse_ticker(self, response.clone(), market.clone()).await;
    }

    async fn fetch_funding_rate(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("v1PublicGetSymbolsSymbolPrices".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "markPrice": "100.00",
        //     "indexPrice": "100.00",
        //     "ltp": "101.34",
        //     "movingFundingRate": "0.08",
        //     "payoutFundingRate": "-0.03",
        //     "nextFundingPayout": 1711555532146
        // }
        //
        return <Self as Defx>::parse_funding_rate(self, response.clone(), market.clone()).await;
    }

    fn parse_funding_rate(&self, mut contract: Value, mut market: Value) -> Value {
        //
        // {
        //     "markPrice": "100.00",
        //     "indexPrice": "100.00",
        //     "ltp": "101.34",
        //     "movingFundingRate": "0.08",
        //     "payoutFundingRate": "-0.03",
        //     "nextFundingPayout": 1711555532146
        // }
        //
        let mut mark_price: Value = self.safe_number(contract.clone(), Value::from("markPrice"), Value::Undefined);
        let mut index_price: Value = self.safe_number(contract.clone(), Value::from("indexPrice"), Value::Undefined);
        let mut funding_rate_raw: Value = self.safe_string(contract.clone(), Value::from("payoutFundingRate"), Value::Undefined);
        let mut funding_rate: Value = Precise::string_div(funding_rate_raw.clone(), Value::from("100"), Value::Undefined);
        let mut funding_time: Value = self.safe_integer(contract.clone(), Value::from("nextFundingPayout"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": contract.clone(),
            "symbol": market.get(Value::from("symbol")),
            "markPrice": mark_price.clone(),
            "indexPrice": index_price.clone(),
            "interestRate": Value::Undefined,
            "estimatedSettlePrice": Value::Undefined,
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "fundingRate": self.parse_number(funding_rate.clone(), Value::Undefined),
            "fundingTimestamp": funding_time.clone(),
            "fundingDatetime": self.iso8601(funding_time.clone()),
            "nextFundingRate": Value::Undefined,
            "nextFundingTimestamp": Value::Undefined,
            "nextFundingDatetime": Value::Undefined,
            "previousFundingRate": Value::Undefined,
            "previousFundingTimestamp": Value::Undefined,
            "previousFundingDatetime": Value::Undefined,
            "interval": Value::Undefined
        }))).unwrap());
    }

    async fn fetch_balance(&mut self, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["account/balance", "balance", "wallet/balance", "accounts/balance", "account"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(&format!("/{}/", token)) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "balance"), ("private", "GET", "account/balance"), ("private", "GET", "wallet/balance")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_balance(&self, mut balances: Value) -> Value {
        let mut result: Value = Value::Json(normalize(&Value::Json(json!({
            "info": balances.clone()
        }))).unwrap());
        let mut i: usize = 0;
        while i < balances.len() {
            let mut balance: Value = balances.get(i.into());
            let mut code: Value = self.safe_currency_code(self.safe_string(balance.clone(), Value::from("asset"), Value::Undefined), Value::Undefined);
            let mut account: Value = self.account();
            account.set("total".into(), self.safe_string(balance.clone(), Value::from("balance"), Value::Undefined));
            result.set(code.clone(), account.clone());
            i += 1;
        };
        return self.safe_balance(result.clone());
    }

    async fn create_order(&mut self, mut symbol: Value, mut r#type: Value, mut side: Value, mut amount: Value, mut price: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("symbol".into(), symbol.clone());
        request.set("type".into(), r#type.clone());
        request.set("side".into(), side.clone());
        request.set("amount".into(), amount.clone());
        if price.is_nonnullish() { request.set("price".into(), price.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "POST" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "POST", "order"), ("private", "POST", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_order_status(&self, mut status: Value) -> Value {
        if status.clone().is_nonnullish() {
            let mut statuses: Value = Value::Json(normalize(&Value::Json(json!({
                "NEW": Value::from("open"),
                "OPEN": Value::from("open"),
                "CANCELLED": Value::from("canceled"),
                "REJECTED": Value::from("rejected"),
                "FILLED": Value::from("closed")
            }))).unwrap());
            return self.safe_string(statuses.clone(), status.clone(), status.clone());
        };
        return status.clone();
    }

    fn parse_order(&self, mut order: Value, mut market: Value) -> Value {
        //
        // {
        //     "orderId": "746472647227344528",
        //     "createdAt": "2024-10-25T16:49:31.077Z",
        //     "updatedAt": "2024-10-25T16:49:31.378Z",
        //     "clientOrderId": "0192c495-49c3-71ee-b3d3-7442a2090807",
        //     "reduceOnly": false,
        //     "side": "SELL",
        //     "status": "FILLED",
        //     "symbol": "SOL_USDC",
        //     "timeInForce": "GTC",
        //     "type": "MARKET",
        //     "origQty": "0.80",
        //     "executedQty": "0.80",
        //     "cumulativeQuote": "137.87440000",
        //     "avgPrice": "172.34300000",
        //     "totalPnL": "0.00000000",
        //     "totalFee": "0.07583092",
        //     "workingType": null,
        //     "postOnly": false,
        //     "linkedOrderParentType": null,
        //     "isTriggered": false,
        //     "slippagePercentage": "5"
        // }
        //
        let mut order_id: Value = self.safe_string(order.clone(), Value::from("orderId"), Value::Undefined);
        let mut client_order_id: Value = self.safe_string(order.clone(), Value::from("clientOrderId"), Value::Undefined);
        let mut market_id: Value = self.safe_string(order.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut symbol: Value = market.get(Value::from("symbol"));
        let mut price: Value = self.safe_string(order.clone(), Value::from("price"), Value::Undefined);
        let mut amount: Value = self.safe_string(order.clone(), Value::from("origQty"), Value::Undefined);
        let mut order_type: Value = self.safe_string_lower(order.clone(), Value::from("type"), Value::Undefined);
        let mut status: Value = self.safe_string(order.clone(), Value::from("status"), Value::Undefined);
        let mut side: Value = self.safe_string_lower(order.clone(), Value::from("side"), Value::Undefined);
        let mut filled: Value = self.omit_zero(self.safe_string(order.clone(), Value::from("executedQty"), Value::Undefined));
        let mut average: Value = self.omit_zero(self.safe_string(order.clone(), Value::from("avgPrice"), Value::Undefined));
        let mut time_in_force: Value = self.safe_string_lower(order.clone(), Value::from("timeInForce"), Value::Undefined);
        let mut take_profit_price: Value = Value::Undefined;
        let mut trigger_price: Value = Value::Undefined;
        if order_type.clone().is_nonnullish() {
            if order_type.index_of(Value::from("take_profit")) >= Value::from(0) {
                take_profit_price = self.safe_string(order.clone(), Value::from("stopPrice"), Value::Undefined);
            } else {
                trigger_price = self.safe_string(order.clone(), Value::from("stopPrice"), Value::Undefined);
            };
        };
        let mut timestamp: Value = self.parse8601(self.safe_string(order.clone(), Value::from("createdAt"), Value::Undefined));
        let mut last_trade_timestamp: Value = self.parse8601(self.safe_string(order.clone(), Value::from("updatedAt"), Value::Undefined));
        return self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "id": order_id.clone(),
            "clientOrderId": client_order_id.clone(),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "lastTradeTimestamp": last_trade_timestamp.clone(),
            "lastUpdateTimestamp": last_trade_timestamp.clone(),
            "status": <Self as Defx>::parse_order_status(self, status.clone()),
            "symbol": symbol.clone(),
            "type": order_type.clone(),
            "timeInForce": time_in_force.clone(),
            "postOnly": self.safe_bool(order.clone(), Value::from("postOnly"), Value::Undefined),
            "reduceOnly": self.safe_bool(order.clone(), Value::from("reduceOnly"), Value::Undefined),
            "side": side.clone(),
            "price": price.clone(),
            "triggerPrice": trigger_price.clone(),
            "takeProfitPrice": take_profit_price.clone(),
            "stopLossPrice": Value::Undefined,
            "average": average.clone(),
            "amount": amount.clone(),
            "filled": filled.clone(),
            "remaining": Value::Undefined,
            "cost": Value::Undefined,
            "trades": Value::Undefined,
            "fee": Value::Json(normalize(&Value::Json(json!({
                "cost": self.safe_string(order.clone(), Value::from("totalFee"), Value::Undefined),
                "currency": Value::from("USDC")
            }))).unwrap()),
            "info": order.clone()
        }))).unwrap()), market.clone());
    }

    async fn cancel_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        request.set("orderId".into(), id.clone());
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["order", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "DELETE" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.ends_with(&format!("/{}", "orders")) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "DELETE", "order"), ("private", "DELETE", "orders")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn cancel_all_orders(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbols": Value::Json(serde_json::Value::Array(vec![(market.get(Value::from("id"))).into()]))
        }))).unwrap());
        let mut response: Value = self.dispatch("v1PrivateDeleteApiOrdersAllOpen".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "data": {
        //         "msg": "The operation of cancel all open order is done."
        //     }
        // }
        //
        return Value::Json(serde_json::Value::Array(vec![(self.safe_order(Value::Json(normalize(&Value::Json(json!({
            "info": response.clone()
        }))).unwrap()), Value::Undefined).await).into()]));
    }

    async fn fetch_position(&mut self, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            return Value::Undefined;
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut market: Value = self.market(symbol.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "symbol": market.get(Value::from("id"))
        }))).unwrap());
        let mut response: Value = self.dispatch("v1PrivateGetApiPositionActive".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "data": [
        //         {
        //             "positionId": "0192c495-4a68-70ee-9081-9d368bd16dfc",
        //             "symbol": "SOL_USDC",
        //             "positionSide": "SHORT",
        //             "entryPrice": "172.34300000",
        //             "quantity": "0.80",
        //             "marginAmount": "20.11561173",
        //             "marginAsset": "USDC",
        //             "pnl": "0.00000000"
        //         }
        //     ]
        // }
        //
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        let mut first: Value = self.safe_dict(data.clone(), Value::from(0), Value::new_object());
        return <Self as Defx>::parse_position(self, first.clone(), market.clone()).await;
    }

    async fn fetch_positions(&mut self, mut symbols: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbols.is_nonnullish() { request.set("symbols".into(), symbols.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["positions", "position", "positionrisk", "position_risk"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "positions"), ("private", "GET", "positionRisk"), ("fapi", "GET", "positionRisk")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    fn parse_position(&self, mut position: Value, mut market: Value) -> Value {
        //
        // {
        //     "positionId": "0192c495-4a68-70ee-9081-9d368bd16dfc",
        //     "symbol": "SOL_USDC",
        //     "positionSide": "SHORT",
        //     "entryPrice": "172.34300000",
        //     "quantity": "0.80",
        //     "marginAmount": "20.11561173",
        //     "marginAsset": "USDC",
        //     "pnl": "0.00000000"
        // }
        //
        let mut market_id: Value = self.safe_string(position.clone(), Value::from("symbol"), Value::Undefined);
        market = self.safe_market(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined);
        let mut size: Value = Precise::string_abs(self.safe_string(position.clone(), Value::from("quantity"), Value::Undefined));
        let mut side: Value = self.safe_string_lower(position.clone(), Value::from("positionSide"), Value::Undefined);
        let mut unrealised_pnl: Value = self.omit_zero(self.safe_string(position.clone(), Value::from("pnl"), Value::Undefined));
        let mut entry_price: Value = self.omit_zero(self.safe_string(position.clone(), Value::from("entryPrice"), Value::Undefined));
        let mut initial_margin: Value = self.safe_string(position.clone(), Value::from("marginAmount"), Value::Undefined);
        return self.safe_position(Value::Json(normalize(&Value::Json(json!({
            "info": position.clone(),
            "id": self.safe_string(position.clone(), Value::from("positionId"), Value::Undefined),
            "symbol": market.get(Value::from("symbol")),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "lastUpdateTimestamp": Value::Undefined,
            "initialMargin": self.parse_number(initial_margin.clone(), Value::Undefined),
            "initialMarginPercentage": Value::Undefined,
            "maintenanceMargin": Value::Undefined,
            "maintenanceMarginPercentage": Value::Undefined,
            "entryPrice": self.parse_number(entry_price.clone(), Value::Undefined),
            "notional": Value::Undefined,
            "leverage": Value::Undefined,
            "unrealizedPnl": self.parse_number(unrealised_pnl.clone(), Value::Undefined),
            "realizedPnl": Value::Undefined,
            "contracts": self.parse_number(size.clone(), Value::Undefined),
            "contractSize": self.safe_number(market.clone(), Value::from("contractSize"), Value::Undefined),
            "marginRatio": Value::Undefined,
            "liquidationPrice": Value::Undefined,
            "markPrice": Value::Undefined,
            "lastPrice": Value::Undefined,
            "collateral": Value::Undefined,
            "marginMode": Value::Undefined,
            "side": side.clone(),
            "percentage": Value::Undefined,
            "stopLossPrice": Value::Undefined,
            "takeProfitPrice": Value::Undefined,
            "hedged": Value::Undefined
        }))).unwrap()));
    }

    async fn fetch_order(&mut self, mut id: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "orderId": id.clone(),
            "idType": Value::from("orderId")
        }))).unwrap());
        let mut client_order_id: Value = self.safe_string_n(params.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from("clOrdID")).into(), (Value::from("clientOrderId")).into(), (Value::from("client_order_id")).into()])), Value::Undefined);
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from("clOrdID")).into(), (Value::from("clientOrderId")).into(), (Value::from("client_order_id")).into()])));
        if client_order_id.clone().is_nonnullish() {
            if symbol.clone().is_nullish() {
                return Value::Undefined;
            };
            let mut market: Value = self.market(symbol.clone());
            request.set("orderId".into(), client_order_id.clone());
            request.set("idType".into(), Value::from("clientOrderId"));
            request.set("symbol".into(), market.get(Value::from("id")));
        };
        let mut response: Value = self.dispatch("v1PrivateGetApiOrderOrderId".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "success": true,
        //     "data": {
        //         "orderId": "555068654076559792",
        //         "createdAt": "2024-05-08T05:45:42.148Z",
        //         "updatedAt": "2024-05-08T05:45:42.166Z",
        //         "clientOrderId": "dummyClientOrderId",
        //         "reduceOnly": false,
        //         "side": "SELL",
        //         "status": "REJECTED",
        //         "symbol": "BTC_USDC",
        //         "timeInForce": "GTC",
        //         "type": "TAKE_PROFIT_MARKET",
        //         "origQty": "1.000",
        //         "executedQty": "0.000",
        //         "cumulativeQuote": "0.00",
        //         "avgPrice": "0.00",
        //         "stopPrice": "65000.00",
        //         "totalPnL": "0.00",
        //         "workingType": "MARK_PRICE",
        //         "postOnly": false
        //     }
        // }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::Undefined);
        return <Self as Defx>::parse_order(self, data.clone(), Value::Undefined).await;
    }

    async fn fetch_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["allorders", "all_orders", "orders/history", "orders"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "allOrders"), ("private", "GET", "orders"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_open_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["openorders", "open_orders", "orders/open", "orders/active"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "openOrders"), ("private", "GET", "orders/open"), ("private", "GET", "orders/active")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_closed_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        fn collect_routes(node: &serde_json::Value, api_name: &str, out: &mut Vec<(String, String, String)>) {
            if let serde_json::Value::Object(map) = node {
                for (k, v) in map { let kl = k.to_lowercase(); if kl == "get" || kl == "post" || kl == "put" || kl == "delete" { if let serde_json::Value::Object(paths) = v { for (p, _cost) in paths { out.push((api_name.to_string(), kl.to_uppercase(), p.clone())); } } } else { collect_routes(v, api_name, out); } }
            }
        }
        let mut request = if params.is_object() { params.clone() } else { Value::new_object() };
        if symbol.is_nonnullish() { request.set("symbol".into(), symbol.clone()); }
        if since.is_nonnullish() { request.set("since".into(), since.clone()); request.set("startTime".into(), since.clone()); }
        if limit.is_nonnullish() { request.set("limit".into(), limit.clone()); }
        let mut dynamic_calls: Vec<(String, String, String)> = vec![];
        if let Value::Json(serde_json::Value::Object(api_map)) = <Self as Defx>::describe(self).get("api".into()) {
            for (api_name, node) in api_map { collect_routes(&node, &api_name, &mut dynamic_calls); }
        }
        for token in ["closedorders", "closed_orders", "orders/closed", "orders/history"] {
            for (api_name, method_name, path_name) in &dynamic_calls {
                if method_name.as_str() != "GET" || path_name.contains('{') { continue; }
                let p = path_name.to_lowercase();
                if p == token || p.ends_with(&format!("/{}", token)) || p.contains(token) {
                    let rv = self.request(path_name.clone().into(), api_name.clone().into(), method_name.clone().into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
                    if !rv.is_undefined() { return rv; }
                }
            }
        }
        let candidates = vec![("private", "GET", "closedOrders"), ("private", "GET", "orders/closed"), ("private", "GET", "orders/history")];
        for (api_name, method_name, path_name) in candidates {
            let rv = self.request(path_name.into(), api_name.into(), method_name.into(), request.clone(), Value::Undefined, Value::Undefined, Value::Undefined).await;
            if !rv.is_undefined() { return rv; }
        }
        Value::Undefined
    }


    async fn fetch_canceled_orders(&mut self, mut symbol: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        let mut req: Value = Value::Json(normalize(&Value::Json(json!({
            "statuses": Value::from("CANCELED")
        }))).unwrap());
        return <Self as Defx>::fetch_orders(self, symbol.clone(), since.clone(), limit.clone(), extend_2(req.clone(), params.clone())).await;
    }

    async fn close_position(&mut self, mut symbol: Value, mut side: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut position_id: Value = self.safe_string(params.clone(), Value::from("positionId"), Value::Undefined);
        if position_id.clone().is_nullish() {
            return Value::Undefined;
        };
        let mut r#type: Value = self.safe_string_upper(params.clone(), Value::from("type"), Value::Undefined);
        if r#type.clone().is_nullish() {
            return Value::Undefined;
        };
        let mut quantity: Value = self.safe_string(params.clone(), Value::from("quantity"), Value::Undefined);
        if quantity.clone().is_nullish() {
            return Value::Undefined;
        };
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "positionId": position_id.clone(),
            "type": r#type.clone(),
            "quantity": quantity.clone()
        }))).unwrap());
        if r#type.clone() != Value::from("MARKET") {
            let mut price: Value = self.safe_string(params.clone(), Value::from("price"), Value::Undefined);
            if price.clone().is_nullish() {
                return Value::Undefined;
            };
            request.set("price".into(), price.clone());
        };
        params = self.omit(params.clone(), Value::Json(serde_json::Value::Array(vec![(Value::from("positionId")).into(), (Value::from("type")).into(), (Value::from("quantity")).into(), (Value::from("price")).into()])));
        let mut response: Value = self.dispatch("v1PrivateDeleteApiPositionPositionId".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        //     {}
        //
        return response.clone();
    }

    async fn close_all_positions(&mut self, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut response: Value = self.dispatch("v1PrivateDeleteApiPositionAll".into(), params.clone(), Value::Undefined).await;
        //
        // {
        //     "data": [
        //         {
        //             "positionId": "d6ca1a27-28ad-47ae-b244-0bda5ac37b2b",
        //             "success": true
        //         }
        //     ]
        // }
        //
        let mut data: Value = self.safe_list(response.clone(), Value::from("data"), Value::new_array());
        return self.parse_positions(data.clone(), Value::Undefined, params.clone()).await;
    }

    async fn fetch_ledger(&mut self, mut code: Value, mut since: Value, mut limit: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut paginate: Value = Value::from(false);
        (paginate, params) = shift_2(self.handle_option_and_params(params.clone(), Value::from("fetchLedger"), Value::from("paginate"), Value::Undefined));
        if paginate.is_truthy() {
            return self.fetch_paginated_call_dynamic(Value::from("fetchLedger"), code.clone(), since.clone(), limit.clone(), params.clone(), Value::Undefined, Value::Undefined).await;
        };
        let mut request: Value = Value::new_object();
        if since.clone().is_nonnullish() {
            request.set("start".into(), since.clone());
        } else {
            request.set("start".into(), Value::from(0));
        };
        let mut until: Value = self.safe_integer(params.clone(), Value::from("until"), Value::Undefined);
        if until.clone().is_nonnullish() {
            params = self.omit(params.clone(), Value::from("until"));
            request.set("end".into(), until.clone());
        } else {
            request.set("end".into(), self.milliseconds());
        };
        let mut response: Value = self.dispatch("v1PrivateGetApiWalletTransactions".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        let mut data: Value = self.safe_list(response.clone(), Value::from("transactions"), Value::new_array());
        return self.parse_ledger(data.clone(), Value::Undefined, since.clone(), limit.clone(), Value::Undefined).await;
    }

    fn parse_ledger_entry(&self, mut item: Value, mut currency: Value) -> Value {
        //
        // {
        //     "id": "01JCSZS6H5VQND3GF5P98SJ29C",
        //     "timestamp": 1731744012054,
        //     "type": "FundingFee",
        //     "amount": "0.02189287",
        //     "asset": "USDC",
        //     "operation": "CREDIT"
        // }
        //
        let mut amount: Value = self.safe_string(item.clone(), Value::from("amount"), Value::Undefined);
        let mut currency_id: Value = self.safe_string(item.clone(), Value::from("asset"), Value::Undefined);
        let mut code: Value = self.safe_currency_code(currency_id.clone(), currency.clone());
        currency = self.safe_currency(currency_id.clone(), currency.clone());
        let mut timestamp: Value = self.safe_integer(item.clone(), Value::from("timestamp"), Value::Undefined);
        let mut r#type: Value = self.safe_string(item.clone(), Value::from("type"), Value::Undefined);
        return self.safe_ledger_entry(Value::Json(normalize(&Value::Json(json!({
            "info": item.clone(),
            "id": self.safe_string(item.clone(), Value::from("id"), Value::Undefined),
            "direction": Value::Undefined,
            "account": Value::Undefined,
            "referenceAccount": Value::Undefined,
            "referenceId": Value::Undefined,
            "type": <Self as Defx>::parse_ledger_entry_type(self, r#type.clone()),
            "currency": code.clone(),
            "amount": self.parse_number(amount.clone(), Value::Undefined),
            "timestamp": timestamp.clone(),
            "datetime": self.iso8601(timestamp.clone()),
            "before": Value::Undefined,
            "after": Value::Undefined,
            "status": Value::Undefined,
            "fee": Value::Undefined
        }))).unwrap()), currency.clone());
    }

    fn parse_ledger_entry_type(&self, mut r#type: Value) -> Value {
        let mut ledger_type: Value = Value::Json(normalize(&Value::Json(json!({
            "FundingFee": Value::from("fee"),
            "FeeRebate": Value::from("fee"),
            "FeeKickback": Value::from("fee"),
            "RealizedPnl": Value::from("trade"),
            "LiquidationClearance": Value::from("trade"),
            "Transfer": Value::from("transfer"),
            "ReferralPayout": Value::from("referral"),
            "Commission": Value::from("commission")
        }))).unwrap());
        return self.safe_string(ledger_type.clone(), r#type.clone(), r#type.clone());
    }

    async fn withdraw(&mut self, mut code: Value, mut amount: Value, mut address: Value, mut tag: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut currency: Value = self.currency(code.clone());
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "amount": self.currency_to_precision(code.clone(), amount.clone(), Value::Undefined),
            "asset": currency.get(Value::from("id"))
        }))).unwrap());
        // 'network': 'ARB_SEPOLIA',
        // 'chainId': '421614',
        let mut response: Value = self.dispatch("v1PrivatePostApiTransfersBridgeWithdrawal".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "transactionId": "0x301e5851e5aefa733abfbc8b30817ca3b61601e0ddf1df8c59656fb888b0bc9c"
        // }
        //
        return <Self as Defx>::parse_transaction(self, response.clone(), currency.clone()).await;
    }

    fn parse_transaction(&self, mut transaction: Value, mut currency: Value) -> Value {
        //
        // withdraw
        //
        // {
        //     "transactionId": "0x301e5851e5aefa733abfbc8b30817ca3b61601e0ddf1df8c59656fb888b0bc9c"
        // }
        //
        let mut txid: Value = self.safe_string(transaction.clone(), Value::from("transactionId"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": transaction.clone(),
            "id": Value::Undefined,
            "txid": txid.clone(),
            "timestamp": Value::Undefined,
            "datetime": Value::Undefined,
            "network": Value::Undefined,
            "address": Value::Undefined,
            "addressTo": Value::Undefined,
            "addressFrom": Value::Undefined,
            "tag": Value::Undefined,
            "tagTo": Value::Undefined,
            "tagFrom": Value::Undefined,
            "type": Value::Undefined,
            "amount": Value::Undefined,
            "currency": self.safe_currency_code(Value::Undefined, currency.clone()),
            "status": Value::Undefined,
            "updated": Value::Undefined,
            "internal": Value::Undefined,
            "comment": Value::Undefined,
            "fee": Value::Undefined
        }))).unwrap());
    }

    async fn set_leverage(&mut self, mut leverage: Value, mut symbol: Value, mut params: Value) -> Value {
        params = params.or_default(Value::new_object());
        if symbol.clone().is_nullish() {
            return Value::Undefined;
        };
        self.load_markets(Value::Undefined, Value::Undefined).await;
        let mut request: Value = Value::Json(normalize(&Value::Json(json!({
            "leverage": self.number_to_string(leverage.clone())
        }))).unwrap());
        let mut market: Value = self.market(symbol.clone());
        request.set("symbol".into(), market.get(Value::from("id")));
        let mut response: Value = self.dispatch("v1PrivatePostApiUsersMetadataLeverage".into(), extend_2(request.clone(), params.clone()), Value::Undefined).await;
        //
        // {
        //     "success": true,
        //     "data": {
        //       "leverage": "11",
        //       "symbol": "BTC_USDC"
        //     }
        // }
        //
        let mut data: Value = self.safe_dict(response.clone(), Value::from("data"), Value::new_object());
        return <Self as Defx>::parse_leverage(self, data.clone(), market.clone()).await;
    }

    fn parse_leverage(&self, mut leverage: Value, mut market: Value) -> Value {
        //
        //     "data": {
        //       "leverage": "11",
        //       "symbol": "BTC_USDC"
        //     }
        //
        let mut market_id: Value = self.safe_string(leverage.clone(), Value::from("symbol"), Value::Undefined);
        let mut leverage_value: Value = self.safe_integer(leverage.clone(), Value::from("leverage"), Value::Undefined);
        return Value::Json(normalize(&Value::Json(json!({
            "info": leverage.clone(),
            "symbol": self.safe_symbol(market_id.clone(), market.clone(), Value::Undefined, Value::Undefined),
            "marginMode": Value::Undefined,
            "longLeverage": leverage_value.clone(),
            "shortLeverage": leverage_value.clone()
        }))).unwrap());
    }

    fn nonce(&self) -> Value {
        return self.milliseconds();
    }

    

    

    fn default_network_code_for_currency(&mut self, mut code: Value) -> Value {
        let mut currency_item: Value = self.currency(code.clone());
        let mut networks: Value = currency_item.get(Value::from("networks"));
        let mut network_keys: Value = networks.clone().keys();
        let mut i: usize = 0;
        while i < network_keys.len() {
            let mut network: Value = network_keys.get(i.into());
            if network.clone() == Value::from("ETH") {
                return network.clone();
            };
            i += 1;
        };
        // if it was not returned according to above options, then return the first network of currency
        return self.safe_value(network_keys.clone(), Value::from(0), Value::Undefined);
    }

    fn set_sandbox_mode(&mut self, mut enable: Value) -> Value {
        Exchange::set_sandbox_mode(self, enable.clone());
        self.get("options".into()).set("sandboxMode".into(), enable.clone());
        Value::Undefined
    }

    
    async fn dispatch(&mut self, method: Value, params: Value, context: Value) -> Value {
        match method {
            Value::Json(serde_json::Value::String(ref m)) => {
                match m.as_str() {
                    "v1PublicGetHealthcheckping" => self.request("healthcheck/ping".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymbolohlc" => self.request("symbols/{symbol}/ohlc".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymboltrades" => self.request("symbols/{symbol}/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymbolprices" => self.request("symbols/{symbol}/prices".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymbolticker24hr" => self.request("symbols/{symbol}/ticker/24hr".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetSymbolssymboldepthlevelslab" => self.request("symbols/{symbol}/depth/{level}/{slab}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetTicker24hragg" => self.request("ticker/24HrAgg".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetCmarkets" => self.request("c/markets".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetCmarketsmetadata" => self.request("c/markets/metadata".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsnewusers" => self.request("analytics/market/stats/newUsers".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatstvl" => self.request("analytics/market/stats/tvl".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsvolumebyinstrument" => self.request("analytics/market/stats/volumeByInstrument".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsliquidation" => self.request("analytics/market/stats/liquidation".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatstotalvolume" => self.request("analytics/market/stats/totalVolume".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsopeninterest" => self.request("analytics/market/stats/openInterest".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatstotaltrades" => self.request("analytics/market/stats/totalTrades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsbasis" => self.request("analytics/market/stats/basis".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsinsurancefund" => self.request("analytics/market/stats/insuranceFund".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatslongandshortratio" => self.request("analytics/market/stats/longAndShortRatio".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketstatsfundingrate" => self.request("analytics/market/stats/fundingRate".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetAnalyticsmarketoverview" => self.request("analytics/market/overview".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetExplorersearch" => self.request("explorer/search".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetExplorertransactions" => self.request("explorer/transactions".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PublicGetExplorerblocks" => self.request("explorer/blocks".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiorderorderid" => self.request("api/order/{orderId}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiorders" => self.request("api/orders".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiordersocoparentorderid" => self.request("api/orders/oco/{parentOrderId}".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApitrades" => self.request("api/trades".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApipositionactive" => self.request("api/position/active".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersmetadataleverage" => self.request("api/users/metadata/leverage".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersmetadatafeemultiplier" => self.request("api/users/metadata/feeMultiplier".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersmetadataslippage" => self.request("api/users/metadata/slippage".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersreferral" => self.request("api/users/referral".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersapikeys" => self.request("api/users/apikeys".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetConnectionsignaturemessageevm" => self.request("connection-signature-message/evm".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiusersprofilewallets" => self.request("api/users/profile/wallets".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApinotifications" => self.request("api/notifications".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiwalletbalance" => self.request("api/wallet/balance".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApiwallettransactions" => self.request("api/wallet/transactions".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticsuseroverview" => self.request("api/analytics/user/overview".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticsuserpnl" => self.request("api/analytics/user/pnl".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticspointsoverview" => self.request("api/analytics/points/overview".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateGetApianalyticspointshistory" => self.request("api/analytics/points/history".into(), "v1".into(), "GET".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiorder" => self.request("api/order".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApipositionoco" => self.request("api/position/oco".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiuserssocketlistenkeys" => self.request("api/users/socket/listenKeys".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersmetadataleverage" => self.request("api/users/metadata/leverage".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersmetadatafeemultiplier" => self.request("api/users/metadata/feeMultiplier".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersmetadataslippage" => self.request("api/users/metadata/slippage".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersreferralrecordreferralsignup" => self.request("api/users/referral/recordReferralSignup".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersapikeys" => self.request("api/users/apikeys".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApiusersprofilewallets" => self.request("api/users/profile/wallets".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApitransferswithdrawal" => self.request("api/transfers/withdrawal".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePostApitransfersbridgewithdrawal" => self.request("api/transfers/bridge/withdrawal".into(), "v1".into(), "POST".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApipositionupdatepositionmargin" => self.request("api/position/updatePositionMargin".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApiuserssocketlistenkeyslistenkey" => self.request("api/users/socket/listenKeys/{listenKey}".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApiusersapikeysaccesskeystatus" => self.request("api/users/apikeys/{accessKey}/status".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivatePutApiusersreferral" => self.request("api/users/referral".into(), "v1".into(), "PUT".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiordersallopen" => self.request("api/orders/allOpen".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiorderorderid" => self.request("api/order/{orderId}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApipositionpositionid" => self.request("api/position/{positionId}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApipositionall" => self.request("api/position/all".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiuserssocketlistenkeyslistenkey" => self.request("api/users/socket/listenKeys/{listenKey}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    "v1PrivateDeleteApiusersapikeysaccesskey" => self.request("api/users/apikeys/{accessKey}".into(), "v1".into(), "DELETE".into(), params, Value::Undefined, Value::Undefined, Value::Undefined).await,
                    _ => panic!("Unknown API method: {}", m),
                }
            },
            _ => panic!("dispatch: method must be a string, got {:?}", method)
        }
    }
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct DefxImpl(Value);
#[async_trait(?Send)]
impl Exchange for DefxImpl {
    fn describe(&self) -> Value { Defx::describe(self) }
    async fn fetch_ticker(&mut self, symbol: Value, params: Value) -> Value { Defx::fetch_ticker(self, symbol, params).await }
    async fn fetch_tickers(&mut self, symbols: Value, params: Value) -> Value { Defx::fetch_tickers(self, symbols, params).await }
    async fn fetch_order_book(&mut self, symbol: Value, limit: Value, params: Value) -> Value { Defx::fetch_order_book(self, symbol, limit, params).await }
    async fn fetch_ohlcv(&mut self, symbol: Value, timeframe: Value, since: Value, limit: Value, params: Value) -> Value { Defx::fetch_ohlcv(self, symbol, timeframe, since, limit, params).await }
    async fn fetch_trades(&mut self, symbol: Value, since: Value, limit: Value, params: Value) -> Value { Defx::fetch_trades(self, symbol, since, limit, params).await }
    async fn fetch_time(&mut self, params: Value) -> Value { Defx::fetch_time(self, params).await }
    async fn fetch_status(&mut self, params: Value) -> Value { Defx::fetch_status(self, params).await }
    fn parse_trade(&self, trade: Value, market: Value) -> Value { Defx::parse_trade(self, trade, market) }
    fn parse_order(&self, order: Value, market: Value) -> Value { Defx::parse_order(self, order, market) }
    fn parse_ticker(&self, ticker: Value, market: Value) -> Value { Defx::parse_ticker(self, ticker, market) }
    fn parse_position(&self, position: Value, market: Value) -> Value { Defx::parse_position(self, position, market) }
    fn parse_balance(&self, response: Value) -> Value { Defx::parse_balance(self, response) }
    fn parse_funding_rate(&self, contract: Value, market: Value) -> Value { Defx::parse_funding_rate(self, contract, market) }
    fn parse_market(&self, market: Value) -> Value { Defx::parse_market(self, market) }
    fn parse_transaction(&self, transaction: Value, currency: Value) -> Value { Defx::parse_transaction(self, transaction, currency) }
    fn parse_ledger_entry(&self, item: Value, currency: Value) -> Value { Defx::parse_ledger_entry(self, item, currency) }
    fn parse_leverage(&self, leverage: Value, market: Value) -> Value { Defx::parse_leverage(self, leverage, market) }
    fn parse_order_status(&self, status: Value) -> Value { Defx::parse_order_status(self, status) }
}
impl Defx for DefxImpl {}
impl ValueTrait for DefxImpl {
    fn is_undefined(&self) -> bool { self.0.is_undefined() }
    fn is_nullish(&self) -> bool { self.0.is_nullish() }
    fn is_nonnullish(&self) -> bool { self.0.is_nonnullish() }
    fn is_truthy(&self) -> bool { self.0.is_truthy() }
    fn or_default(&self, default: Value) -> Value { self.0.or_default(default) }
    fn is_number(&self) -> bool { self.0.is_number() }
    fn is_string(&self) -> bool { self.0.is_string() }
    fn is_object(&self) -> bool { self.0.is_object() }
    fn is_falsy(&self) -> bool { self.0.is_falsy() }
    fn to_upper_case(&self) -> Value { self.0.to_upper_case() }
    fn unwrap_str(&self) -> &str { self.0.unwrap_str() }
    fn unwrap_usize(&self) -> usize { self.0.unwrap_usize() }
    fn unwrap_bool(&self) -> bool { self.0.unwrap_bool() }
    fn unwrap_precise(&self) -> &Precise { self.0.unwrap_precise() }
    fn unwrap_json(&self) -> &serde_json::Value { self.0.unwrap_json() }
    fn unwrap_json_mut(&mut self) -> &mut serde_json::Value { self.0.unwrap_json_mut() }
    fn unwrap_precise_mut(&mut self) -> &mut Precise { self.0.unwrap_precise_mut() }
    fn len(&self) -> usize { self.0.len() }
    fn get(&self, key: Value) -> Value { self.0.get(key) }
    fn set(&mut self, key: Value, value: Value) { self.0.set(key, value) }
    fn push(&mut self, value: Value) { self.0.push(value) }
    fn split(&self, separator: Value) -> Value { self.0.split(separator) }
    fn contains_key(&self, key: Value) -> bool { self.0.contains_key(key) }
    fn keys(&self) -> Value { self.0.keys() }
    fn values(&self) -> Value { self.0.values() }
    fn to_array(&self, x: Value) -> Value { self.0.to_array(x) }
    fn index_of(&self, x: Value) -> Value { self.0.index_of(x) }
    fn join(&self, glue: Value) -> Value { self.0.join(glue) }
    fn to_string(&self) -> Value { self.0.to_string() }
    fn typeof_(&self) -> Value { self.0.typeof_() }
    fn slice(&self, start: Value, end: Value) -> Value { self.0.slice(start, end) }
}

impl DefxImpl {
    pub fn new(params: Value) -> Self {
        let mut rv = DefxImpl(match params {
            Value::Json(_) => params,
            _ => Value::new_object()
        });
        ExchangeImpl::init(&mut rv.0);

        let config_entries = Defx::describe(&rv);
        for k in config_entries.keys() {
            rv.set(k.clone(), config_entries.get(k).clone());
        }
        rv
    }
}


// WebSocket client skeleton (WIP)
// TODO: define traits/types for WS transport and subscription lifecycle.

#[derive(Debug, Default)]
pub struct WsClient;

impl WsClient {
    pub fn new() -> Self {
        WsClient
    }
}

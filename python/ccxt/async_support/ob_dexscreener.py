# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.async_support.dexscreener import dexscreener
from ccxt.abstract.ob_dexscreener import ImplicitAPI
from ccxt.base.types import Any
from typing import List


class ob_dexscreener(dexscreener, ImplicitAPI):

    def describe(self) -> Any:
        return self.deep_extend(super(ob_dexscreener, self).describe(), {
            'id': 'ob_dexscreener',
            'name': 'DexScreener',
            'certified': False,
            'urls': {
            },
            'has': {
                'CORS': None,
                'spot': True,
                'margin': False,
                'swap': False,
                'future': False,
                'option': False,
            },
            'options': {
                'octobot': {
                    'supportedElements': {
                        'spot': {
                            'orders': [],
                            'bundled_orders': {},
                        },
                        'futures': {
                            'orders': [],
                            'bundled_orders': {},
                        },
                    },
                    'fixMarketStatus': True,
                    'createOhlcvFromTickers': True,
                    'requiresSymbolsParamToFetchTickers': True,
                    'supportsMarketsCache': False,
                    'supportsAllSymbolsListing': False,
                    'lazyLoadMarkets': True,
                },
            },
        })

    async def ob_load_markets_for_symbols(self, symbols: List[str], reload=False, params={}) -> List[dict]:
        """
        lazily loads and returns fixed market status structures for the given symbols

        https://docs.dexscreener.com/api/reference

        :param str[] symbols: list of base/quote symbols, optionally with @network dex suffix
        :param boolean reload: when True, re-fetch symbols even if already cached in self.markets
        :param dict [params]: extra parameters specific to the exchange API endpoint
        :returns dict[]: list of fixed market status structures
        """
        await super(ob_dexscreener, self).ob_load_markets_for_symbols(symbols, reload, params)
        symbolsLength = len(symbols)
        result = []
        for symbolIndex in range(0, symbolsLength):
            result.append(self.ob_get_fixed_market_status(symbols[symbolIndex]))
        return result

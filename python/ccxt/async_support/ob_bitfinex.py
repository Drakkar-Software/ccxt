# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.async_support.bitfinex import bitfinex
from ccxt.abstract.ob_bitfinex import ImplicitAPI
from ccxt.base.types import Any, Market, Ticker


class ob_bitfinex(bitfinex, ImplicitAPI):

    def describe(self) -> Any:
        return self.deep_extend(super(ob_bitfinex, self).describe(), {
            'id': 'ob_bitfinex',
            'name': 'Bitfinex',
            'certified': False,
            'urls': {
            },
            'has': {
                'CORS': None,
                'spot': True,
                'margin': True,
                'swap': False,
                'future': False,
                'option': False,
            },
            'options': {
                'octobot': {
                    'supportedElements': {
                        'spot': {
                            'orders': ['market', 'limit'],
                            'bundled_orders': {},
                        },
                        'futures': {
                            'orders': ['market', 'limit'],
                            'bundled_orders': {},
                        },
                    },
                    'fixMarketStatus': True,
                },
            },
        })

    def parse_ticker(self, ticker: dict, market: Market = None) -> Ticker:
        # override the standard parseTicker to apply OctoBot's BitfinexCCXTAdapter.fix_ticker:
        # bitfinex tickers may be returned with no timestamp, fall back to the current time
        parsed = super(ob_bitfinex, self).parse_ticker(ticker, market)
        if not self.safe_integer(parsed, 'timestamp'):
            parsed['timestamp'] = self.milliseconds()
        return parsed

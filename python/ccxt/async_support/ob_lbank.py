# -*- coding: utf-8 -*-

# PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
# https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

from ccxt.async_support.lbank import lbank
from ccxt.abstract.ob_lbank import ImplicitAPI
from ccxt.base.types import Any, Bool, Str
from typing import List


class ob_lbank(lbank, ImplicitAPI):

    def describe(self) -> Any:
        return self.deep_extend(super(ob_lbank, self).describe(), {
            'id': 'ob_lbank',
            'has': {
                'CORS': False,
                'spot': True,
                'margin': False,
                'swap': None,
                'future': False,
                'option': False,
                'fetchAccountId': True,
                'fetchPermissions': True,
                'isAuthenticatedRequest': True,
            },
            'options': {
                'createMarketBuyOrderRequiresPrice': False,  # disable quote conversion
                'octobot': {
                    'supportedElements': {
                        'spot': {
                            'orders': ['market', 'limit'],
                            'bundled_orders': {},
                        },
                        'futures': {
                            'orders': ['market', 'limit'],
                            'bundled_orders': {},
                        },
                    },
                    'fixMarketStatus': True,
                    'removeMarketStatusPriceLimits': True,
                    'supportFetchingCancelledOrders': False,
                    'enableSpotBuyMarketWithCost': True,
                    'requireOrderFeesFromTrades': True,
                },
            },
        })

    async def fetch_permissions(self, params={}) -> List[str]:
        restrictions = await self.spotPrivatePostSupplementApiRestrictions(params)
        dataDict = self.safe_dict(restrictions, 'data', {})
        rights: List[str] = []
        if self.safe_bool(dataDict, 'enableReading', False):
            rights.append('reading')
        if self.safe_bool(dataDict, 'enableSpotTrading', False):
            rights.append('spotTrading')
            rights.append('marginTrading')
        if self.safe_bool(dataDict, 'enableFuturesTrading', False):
            rights.append('futuresTrading')
        if self.safe_bool(dataDict, 'enableWithdrawals', False):
            rights.append('withdrawals')
        return rights

    async def fetch_account_id(self, params={}, _ccxtTypesImportStr: Str = None) -> Str:
        return 'default_account_id'

    def is_authenticated_request(self, url: Str, method: Str, headers: dict, body, _ccxtTypesImportStr: Str = None) -> Bool:
        return self.ob_is_authenticated_request(url, method, headers, body, 'signatureMethodInHeadersJsonOrSignInBody', {})
